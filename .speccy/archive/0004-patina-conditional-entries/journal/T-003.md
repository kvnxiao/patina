---
spec: SPEC-0004
task: T-003
generated_at: 2026-06-01T22:05:00Z
---

<implementer date="2026-06-01T22:05:00Z" model="claude-opus-4-8[1m]/high" round="1">
- Reuse survey: Mapped the task area (covered REQ-005; suggested files
  `patina-core/src/config/mod.rs` (or a new `config/root.rs`) and
  `patina-core/src/variables/mod.rs`; plus the two analogous parsers it
  sits beside — `profile::load_auto_match_rules` /
  `parse_auto_match_rules_str` (`profile.rs:262-288`, the root-manifest
  NotFound-handling pattern) and `config::parse_module_config_str`
  (`config/mod.rs:124-155`, the per-module `[variables]` +
  reserved-key pattern)). Decisions: reuse-as-is —
  `variables::reject_reserved_keys` (`variables/mod.rs:71`) for the
  reserved-`patina.*` check on both tables (the exact helper the module
  parser already calls, so the rejection error is identical);
  `toml::value::Table` as the raw table type (matches
  `ModuleConfig.variables`, so T-004 ingests the same shape);
  `fs_err::read_to_string` + the `NotFound => Ok(empty)` arm (copied in
  spirit from `load_auto_match_rules`); `serde` `#[serde(default)]` +
  `BTreeMap<String, _>` deserialization for `[profiles.<name>]`. extend —
  none; I deliberately did not touch `RawModule`/`ModuleConfig` or
  `profile.rs`'s `RawRoot`, because the root variable surface is a
  distinct manifest shape and the task says "choose the seam that keeps
  `profile.rs` focused on resolution." write-fresh — a new
  `patina-core/src/config/root.rs` module with `parse_root_config` /
  `parse_root_config_str`, the `RootConfig` return struct
  (`repo_shared` + `per_profile`), the `RootConfigError` enum
  (`Io`/`Toml`/`Variable`, mirroring `ConfigParseError`'s shape), and the
  private `RawRoot`/`RawProfile` serde projections. Searched
  `patina-core/src` (`rg "profiles|RootConfig|parse_root"`) and found no
  existing root `[variables]`/`[profiles]` parser — `profile.rs`'s
  `RawRoot` deserializes only `auto_match`, confirming this is net-new per
  the SPEC assumption (SPEC.md:142-147). New top-level symbols:
  `RootConfig`, `RootConfigError`, `parse_root_config`,
  `parse_root_config_str`, `RawRoot`, `RawProfile` — each justified above
  (no pre-existing equivalent; the reserved-key and raw-table primitives
  they build on are reused, not re-implemented).
- Completed: Added `patina-core/src/config/root.rs` parsing the root
  manifest's repo-shared `[variables]` table and its
  `[profiles.<name>.variables]` tables. `parse_root_config(path)` reads the
  file via `fs_err`, returns an empty `RootConfig` on `NotFound` (a repo
  need not declare root variables), surfaces other IO errors as
  `RootConfigError::Io`, and re-maps the in-memory `<memory>` path on a
  `Toml` error to the real path. `parse_root_config_str(text)`
  deserializes a `RawRoot` (an `Option<Table>` `variables` field and a
  `BTreeMap<String, RawProfile>` `profiles` field), unwraps each table to
  an empty default when absent, runs `variables::reject_reserved_keys`
  over the keys of the repo-shared table and of every profile's table, and
  returns `RootConfig { repo_shared, per_profile }`. A `[profiles.<name>]`
  with no nested `variables` table registers the name with an empty table.
  Wired the module into `config/mod.rs` (`pub mod root;` + four
  re-exports: `RootConfig`, `RootConfigError`, `parse_root_config`,
  `parse_root_config_str`) and re-exported the same four from `lib.rs`'s
  flat `pub use config::*` block, matching the existing
  `parse_module_config` re-export convention. Added ten unit tests in
  `root.rs` (see Evidence).
- Undone: Wiring these tables into the resolver's repo-shared /
  per-profile layers during `apply::plan` is T-004 (this task is
  parse-and-return only, per the task body and SPEC.md:174-175). Selecting
  the *active* profile's table and applying resolution precedence are
  likewise T-004. No `engine.rs`, no `Resolver`, and no template-render
  path was touched. REQ-005's CHK-010 / CHK-011 (end-to-end `apply --yes`
  render scenarios) are T-004's CLI tests by the decomposition and are not
  satisfiable from the parse layer alone.
- Hygiene checks:
  - `cargo test --workspace --locked` (`just test`): exit 0 — full
    workspace suite plus doctests pass, including the ten new
    `config::root` unit tests.
  - `just lint` (nightly `cargo +nightly fmt --all --check`; `cargo clippy
    --workspace --all-targets --all-features --locked -- -D warnings`
    cross-compiled to `x86_64-unknown-linux-gnu` and
    `x86_64-pc-windows-gnu`, macOS leg skipped off a Mac with CI as
    backstop; `RUSTDOCFLAGS="-D warnings" cargo doc`; `cargo deny check`):
    exit 0. `cargo deny` reports advisories / bans / licenses / sources ok
    (pre-existing transitive duplicate-crate warnings only; no dependency
    added — the change uses `std` + already-present
    `serde`/`toml`/`fs-err`/`camino`/`thiserror`). Note: an initial clippy
    run flagged three `panic!`-in-test assertions (`-D clippy::panic`
    applies to test code here too); rewrote them to the codebase's
    `assert!(matches!(...))` convention (the same shape used in
    `variables/mod.rs` tests) and re-ran green.
- Evidence: paper trail at
  `.speccy/specs/0004-patina-conditional-entries/evidence/T-003.md`.
  T-003 covers REQ-005. Roll call for the CHKs under REQ-005:
  - CHK-010 (root `[variables]` value renders into a `.tmpl` target under
    `apply --yes`): judgment-only / deferred → this is an end-to-end apply
    + render scenario the decomposition assigns to T-004 (its
    `<task-scenarios>` name CHK-010 as its CLI integration test). T-003 is
    the parse half; it makes the raw `[variables]` table available but does
    not layer or render it. No scriptable proof is owed by T-003; the parse
    behaviour it *does* own (the root table is read into `repo_shared`) is
    demonstrated by the `reads_root_repo_shared_variables` /
    `reads_variables_from_an_on_disk_manifest` unit tests.
  - CHK-011 (active profile's `[profiles.work.variables]` shadows the root
    value under `apply --yes`): judgment-only / deferred → likewise T-004's
    CLI test (shadowing is a resolver-precedence property the parse layer
    does not implement). T-003's owned parse behaviour (the profile table
    is read and keyed by name) is demonstrated by
    `reads_per_profile_variables_keyed_by_name` and
    `reads_both_repo_shared_and_per_profile_together`.
  The reserved-key `<done-when>` bullet ("a `patina.*` key in the root
  `[variables]` or any `[profiles.*.variables]` table is rejected with the
  existing reserved-key error") is fully owned by T-003 and demonstrated:
  hygiene → `rejects_reserved_key_in_repo_shared_table` and
  `rejects_reserved_key_in_per_profile_table` both assert
  `RootConfigError::Variable(VariableError::ReservedKey { key == "patina.os" })`
  under `cargo test`.
- Discovered issues: none. The `config/mod.rs` module-level docstring
  still describes only the per-module `[variables]` table and references
  the old "T-006" task id for the resolver handoff; that prose predates
  this SPEC's task numbering and is about the module parser, not the new
  root parser. Left untouched to keep this task surgical — it is not
  rendered stale by adding a sibling module, and the doc-drift hard rule
  targets observable-behaviour docs, which this internal handoff comment is
  not. Flagged here for the T-012 docs sweep rather than fixed inline.
- Procedural compliance: This round-1 `<implementer>` block creates
  `journal/T-003.md` with three-field frontmatter per the journal schema;
  no `<implementer-note>` was written into TASKS.md. The TASKS.md
  `state="..."` attribute for T-003 was flipped `pending` → `in-progress`
  at the start of this turn and `in-progress` → `in-review` after both
  hygiene gates passed. The locked `SPEC.md` was not edited. No shipped
  `.claude/skills/speccy-*` bodies required edits during this task.
</implementer>

<review date="2026-06-01T21:26:15Z" model="claude-opus-4-8[1m]/high" persona="business" verdict="pass" round="1">
T-003 faithfully delivers the parse-and-return scope REQ-005 assigns to it. The one `<done-when>` bullet T-003 fully owns — a `patina.*` key in the root `[variables]` or any `[profiles.*.variables]` table is rejected with the existing reserved-key error — is satisfied via `variables::reject_reserved_keys` over both the repo-shared table and every profile table (`patina-core/src/config/root.rs:136,141`) and is demonstrated by `rejects_reserved_key_in_repo_shared_table` / `rejects_reserved_key_in_per_profile_table`, both asserting `VariableError::ReservedKey { key == "patina.os" }`. All four T-003 `<task-scenarios>` map onto passing tests (10/10 green). The remaining REQ-005 done-when bullets and the SPEC scenarios CHK-010/CHK-011 are resolver-precedence and end-to-end render behaviors the decomposition explicitly assigns to T-004 (TASKS.md:202-246); their deferral matches the task split rather than dropping scope. No non-goal violated — the per-machine layer (SPEC.md:95) and all resolver wiring are absent from `root.rs`. No open question silently resolved. Non-blocking note: the `RootConfigError::Variable` doc at `root.rs:79` cites "REQ-005 / REQ-007", but REQ-007 in this SPEC is the symlink-tree orphan requirement (the reserved-key rule is SPEC-0001's REQ-007); an internal doc cross-reference, fine to fold into the T-012 docs sweep.
</review>

<review date="2026-06-01T21:26:15Z" model="claude-opus-4-8[1m]/high" persona="tests" verdict="pass" round="1">
T-003's four `<task-scenarios>` each map to a real unit test driving the production `parse_root_config` / `parse_root_config_str` (`patina-core/src/config/root.rs:96,129`), not mocks: repo-shared read (`reads_root_repo_shared_variables`, on-disk `reads_variables_from_an_on_disk_manifest`), per-profile keyed read (`reads_per_profile_variables_keyed_by_name`, asserting independence from repo_shared), reserved-key rejection asserting the *specific* `VariableError::ReservedKey { key } if key == "patina.os"` on both tables (`root.rs:231,241`), and absent-sections-yield-empty (`absent_sections_yield_empty_results` + `missing_manifest_file_yields_empty_results` NotFound fall-through). Mutation-tested mentally: stubbing the parser to return `RootConfig::default()` or dropping either `reject_reserved_keys` call fails a named test. Evidence roll call covers every REQ-005 CHK: CHK-010/CHK-011 are plausibly `judgment-only` — they are end-to-end `apply --yes` render/shadowing scenarios the decomposition assigns to T-004's CLI tests (TASKS.md:232,237) and are not demonstrable from a parse-only layer; the reserved-key `<done-when>` is `hygiene` with two valid test cites. Evidence command (`cargo test -p patina-core --lib config::root`) is scoped, not the full-suite hygiene run; runner output names 10 tests all present in the diff, no fabrication pattern matched.
</review>

<review date="2026-06-01T21:26:15Z" model="claude-opus-4-8[1m]/medium" persona="security" verdict="pass" round="1">
No security issues in this parse-and-return-only slice. The reserved-`patina.*` rejection routes through the shared `variables::reject_reserved_keys` helper on both the repo-shared table (`patina-core/src/config/root.rs:136`) and every per-profile table (`root.rs:141`), matching the module parser's contract (`config/mod.rs:151`) exactly; both rejection paths are covered by `rejects_reserved_key_in_repo_shared_table` and `rejects_reserved_key_in_per_profile_table` asserting the precise `VariableError::ReservedKey { key }` shape. The known limitation that `reject_reserved_keys` keys off the top-level key name (so a nested `[variables.patina]` sub-table is not caught while a quoted `"patina.os"` key is) is pre-existing behavior inherited verbatim from the module parser, not a regression introduced here — SPEC parity with the module parser is the bar and it is met. No injection/path-traversal/command surface (pure TOML deserialization to `toml::value::Table`), no secret or value-content leakage in error messages, no new dependencies, and NotFound correctly falls through to an empty config. Template rendering of these values is deferred to T-004/T-005 and out of scope.
</review>

<review date="2026-06-01T21:26:15Z" model="claude-sonnet-4-6[1m]/medium" persona="style" verdict="pass" round="1">
`root.rs` is clean and consistent with the project's established conventions.
- Import ordering: `crate::` imports precede external crates (`camino`, `serde`) and `std`, matching `apply/engine.rs` and other modules.
- Parameter types: `parse_root_config` takes `path: &Utf8Path` (not `&Utf8PathBuf`), satisfying the `rust-let-types-carry-invariants.md` / `rust-code-quality.md` parameter-type hierarchy rule that has blocked prior tasks.
- All six imports in `root.rs` are used; no dead imports introduced.
- `#[non_exhaustive]` on `RootConfigError` is consistent with every other error enum in the codebase.
- Error variant shape (`Io { path, source: std::io::Error }` / `Toml { path, source: Box<toml::de::Error> }`) mirrors `ConfigParseError` exactly.
- Tests use `.expect("descriptive message")` (no bare `unwrap()`), and `assert!(matches!(...))` — the same pattern used in `variables/mod.rs` tests.
- `pub mod root;` inserted alphabetically in `config/mod.rs`; re-exports follow the existing ordering. `lib.rs` re-exports alphabetically consistent.
</review>

<review date="2026-06-01T21:26:15Z" model="claude-opus-4-8[1m]/high" persona="correctness" verdict="pass" round="1">
T-003 parse layer is correct. `parse_root_config` maps `NotFound` to an empty `RootConfig::default()` and only other IO errors to `Io` (root.rs:97-108); missing `[variables]` and missing `[profiles]` both yield empties via `Option::unwrap_or_default` and `#[serde(default)]` on the `BTreeMap` (root.rs:135, 156-162). Per-profile tables are keyed by name with a bare `[profiles.<name>]` registering an empty table (root.rs:138-143), reserved-`patina.*` keys are rejected on both the repo-shared table and every profile table via the shared `reject_reserved_keys` (root.rs:136,141), and the `Toml` path-remap in `parse_root_config` preserves the underlying `source` while passing `Variable` errors through unchanged (root.rs:109-115). `BTreeMap` iteration is deterministic so the first-offending-key is stable. No off-by-one, no swallowed `Result`/`Option`, no early-return that bypasses cleanup, no `unwrap`/`expect` in production. No defect at or above the reporting threshold.
</review>
