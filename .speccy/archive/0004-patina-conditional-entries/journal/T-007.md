---
spec: SPEC-0004
task: T-007
generated_at: 2026-06-01T23:05:00Z
---

<implementer date="2026-06-01T23:05:00Z" model="claude-opus-4-8[1m]/high" round="1">
- Reuse survey: Mapped the task area (covered REQ-006 / DEC-005 / DEC-007;
  suggested files `patina-core/src/apply/symlink.rs`,
  `patina-core/src/apply/mod.rs`, `patina-core/src/apply/engine.rs`,
  `patina-cli/tests/symlink_tree.rs`; plus the T-002 plan-time dispatch and
  the T-001 `FileMode::SymlinkTree` mode). Decisions: reuse-as-is —
  `super::walk_files` (`apply/mod.rs:269`, deterministic sorted, regular
  files only, so empty subdirs yield no entry by construction); `link_file`
  (`apply/symlink.rs`, calls `ensure_parent` to create intermediate target
  dirs as real directories on demand and clears any pre-existing entry before
  linking); `create_symlink` / `LinkKind` (the cross-platform OS-primitive
  picker); the `symlink_metadata` NotFound/Io classification pattern shared
  by `per_file_symlink` and `dir_symlink`; `ExecutorError::NotADirectory` and
  `::SourceMissing` (existing variants — no new error type needed); the
  T-002 `planned_operation` arm that already maps `FileMode::SymlinkTree` →
  `PlannedOperation::symlink` (no engine plan-time change needed); the
  `Fixture` harness (`patina-cli/tests/common/mod.rs`) and its `run(..)` to
  invoke `patina rollback` for the backup proof. extend — nothing structural;
  `materialize`'s match gains the real `SymlinkTree` arm and the two stale
  module docstrings ("five modes"; the `symlink` submodule line) gain the
  third symlink mode. write-fresh — `tree_symlink` (the per-leaf executor)
  and the `symlink_tree.rs` CLI test module. Searched the crate
  (`rg "fn .*symlink\|walk_files\|fn .*tree"` across `patina-core/src/apply`)
  for an existing per-leaf-symlink-with-real-dirs walker before writing the
  body: `per_file_symlink`'s directory branch is the exact walk-and-link path,
  but it is keyed off the default `Symlink` mode (now file-only after the
  T-001 split) and conflates the file-source and dir-source cases; rather than
  re-key it, `tree_symlink` reuses the same two leaf-level helpers
  (`walk_files` + `link_file`) so there is no duplicated walk logic — the
  shared primitives are called, not re-implemented. New top-level symbol:
  `tree_symlink` (justified — it is the dir-only per-leaf executor REQ-006
  mandates; reusing `per_file_symlink` directly would mean dispatching a
  `[[directory]]` `symlink-tree` through a function documented as the
  `[[file]]` `Symlink` executor, and would not reject a non-directory source).
- Completed: Implemented the `symlink-tree` per-leaf executor (REQ-006).
  Added `tree_symlink(source, targets)` in `patina-core/src/apply/symlink.rs`:
  it `symlink_metadata`-probes the source (NotFound → `SourceMissing`, other
  IO → `Io`), rejects a non-directory source with `NotADirectory`, collects
  the leaves once via `super::walk_files` (deterministic order), and for each
  target links each leaf through the existing `link_file` — which
  `ensure_parent`-creates intermediate target dirs as real directories and
  clears a pre-existing entry first (the engine's `backup_before_overwrite`
  has already stashed any foreign regular file, DEC-007). Empty source
  subdirectories produce no entry because `walk_files` collects only regular
  files, so neither a target dir nor a link is created (DEC-005). One
  `CompletionRecord` per leaf is preserved (T-008's status set and T-002's
  commit-record index space depend on it). Wired
  `FileMode::SymlinkTree => symlink::tree_symlink(..)` into `materialize`
  (`apply/mod.rs`), replacing the T-002 `SymlinkTreeNotImplemented` guard, and
  removed that now-unreferenced `ExecutorError` variant. Updated the two
  drifted module docstrings (the `apply` module's "five modes" → "six" and the
  `symlink` module header) to name `SymlinkTree`. No `engine.rs` change was
  needed: T-002 already maps the mode to the correct durable symlink op shape.
- Undone: Nothing in T-007's scope is left undone — REQ-006 is complete. Out
  of scope and unchanged here (per the task body / dependency graph): orphan
  reporting and reaping of `symlink-tree` leaves plus the `when`-aware status
  managed-set (T-008), the auto_match/predicate-engine unification (T-009),
  `patina add` kind-routing (T-010), the `[[directory]]` manifest writer
  (T-011), and the docs/superseded-by sweep (T-012). The pre-existing
  `[[file]]`-phrased prose in neighbouring `apply` docstrings is left for the
  T-012 docs sweep (only the two mode-count/list lines this change made
  inaccurate were touched).
- Hygiene checks:
  - `just test` (= `cargo test --workspace --locked`): exit 0 — full
    workspace suite plus doctests pass, including the 6 new `tree_symlink`
    executor unit tests and the 4 new `symlink_tree` CLI integration tests;
    the T-001/T-002/T-005/T-006 tests still pass.
  - `just lint` clippy (`cargo clippy --workspace --all-targets
    --all-features --locked -- -D warnings`, cross-compiled to
    `x86_64-unknown-linux-gnu` and `x86_64-pc-windows-gnu`; macOS leg skipped
    off a Mac with CI as backstop): exit 0. Clippy flagged
    `clippy::expect_used` on the new `symlink_tree.rs` test helpers (the
    crate-level allow that the other `tests/*.rs` integration crates carry was
    missing); added the same `#![expect(clippy::expect_used, ..)]` crate
    attribute the sibling test crates use, then re-ran green.
  - `just lint` fmt (`cargo +nightly fmt --all --check`): exit 0 (rustfmt
    wanted one `assert_eq!` call wrapped across lines in the new unit test;
    applied and re-checked).
  - `just lint` docs (`RUSTDOCFLAGS="-D warnings" cargo doc`): exit 0 — the
    new intra-doc links (`[`SymlinkTree`]`, `[`walk_files`]`, `[`link_file`]`)
    resolve cleanly.
  - `just lint` `cargo deny check`: advisories / bans / licenses / sources
    all ok (pre-existing transitive duplicate-crate warnings only; no
    dependency added — the change reuses `fs_err` and existing helpers).
- Evidence: paper trail at
  `.speccy/specs/0004-patina-conditional-entries/evidence/T-007.md`. T-007
  covers REQ-006. Roll call for the CHKs under REQ-006:
  - CHK-012 (nested-source leaves become symlinks; intermediate target dirs
    stay real): demonstrated → CLI test
    `symlink_tree_links_each_leaf_and_keeps_intermediate_dirs_real` drives the
    real `patina apply --yes` and asserts `~/d/a.conf` / `~/d/sub/b.conf`
    resolve (canonicalized) to the source leaves and `~/d` / `~/d/sub` are
    real directories; the executor unit test
    `tree_symlink_links_each_leaf_and_keeps_intermediate_dirs_real` pins the
    same at the function level. Red/green: a naive `create_dir_all` of the
    full target then a directory symlink would make `~/d` a link, failing the
    `is_dir() && !is_symlink()` assertion.
  - CHK-013 (a pre-existing regular file at a leaf target is backed up, then
    replaced by the link): demonstrated → CLI test
    `symlink_tree_backs_up_pre_existing_leaf_and_replaces_it_with_a_link`
    asserts the leaf is a symlink after apply, then runs `patina rollback
    --yes` and asserts the leaf is restored to a regular file containing the
    original bytes — proving the prior bytes were recorded in a backup via the
    real backup/rollback machinery. The executor unit test
    `tree_symlink_replaces_pre_existing_regular_file_leaf` pins the replace
    leg at the function level.
  - REQ-006 `<done-when>` empty-subdir skip: demonstrated → CLI test
    `symlink_tree_skips_empty_source_subdirectory` and unit test
    `tree_symlink_skips_empty_source_subdirectories` assert `~/d/empty` does
    not exist while the real leaf links.
  - REQ-006 `<done-when>` idempotent re-apply: demonstrated → CLI test
    `symlink_tree_re_apply_over_unchanged_source_is_a_noop` (two `apply --yes`
    runs both exit 0, links intact) and unit test
    `tree_symlink_re_apply_over_unchanged_source_is_a_noop`.
  - REQ-006 source-kind guard (a non-directory source is rejected):
    demonstrated → unit test `tree_symlink_rejects_file_source`
    (`ExecutorError::NotADirectory`) and `tree_symlink_missing_source_is_typed`
    (`ExecutorError::SourceMissing`). The plan-time `[[directory]]`-file-source
    rejection itself is T-006's `validate_source_kind` (already covered there);
    this is the executor-side backstop (DEC-008 parity).
- Discovered issues: none new. The `apply` module's neighbouring docstrings
  still phrase per-target/per-walked-file prose around `[[file]]` entries (a
  pre-`[[directory]]`-split artefact already flagged for the T-012 docs
  sweep); not made worse here and left untouched beyond the two mode-count /
  mode-list lines this change made inaccurate.
- Procedural compliance: This round-1 `<implementer>` block creates
  `journal/T-007.md` with three-field frontmatter per the journal schema; no
  `<implementer-note>` was written into TASKS.md. The TASKS.md `state="..."`
  attribute for T-007 was flipped `pending` → `in-progress` at the start of
  this turn and `in-progress` → `in-review` after all four hygiene gates
  passed. The locked `SPEC.md` was not edited. No shipped
  `.claude/skills/speccy-*` bodies were touched.
</implementer>

<review date="2026-06-01T22:15:18Z" model="claude-opus-4-8[1m]/xhigh" persona="business" verdict="pass" round="1">
T-007 faithfully delivers REQ-006. Every `<done-when>` clause maps to a passing, behavior-gating test driving the real apply path:
- CHK-012 (per-leaf links, intermediate dirs stay real) → `patina-cli/tests/symlink_tree.rs:57` (`read_link_canonical` resolves to source; `assert_real_dir` on `~/d` and `~/d/sub`); executor unit `tree_symlink_links_each_leaf_and_keeps_intermediate_dirs_real`.
- CHK-013 (pre-existing regular file at leaf backed up, then replaced) → `patina-cli/tests/symlink_tree.rs:97` proves the backup via a real `patina rollback --yes` restoring `b"original"`. The engine backs up `op.targets` (the directory target `~/d`) at directory granularity via `backup_before_overwrite` → `clone_entry`, preserving the pre-existing `a.conf`; `tree_symlink` then clears and links the leaf. Rollback restores the whole directory.
- Empty-subdir skip and idempotent re-apply (`<done-when>` 3 and 5) → `symlink_tree_skips_empty_source_subdirectory` and `symlink_tree_re_apply_over_unchanged_source_is_a_noop`, both passing.
Scope respected. Orphan reporting/reaping (REQ-007) correctly left to T-008 — and the one-`CompletionRecord`-per-leaf granularity T-008 depends on is preserved. The removed `ExecutorError::SymlinkTreeNotImplemented` placeholder is cleanly retired. No non-goal leaked in. The SPEC has no open questions.
</review>

<review date="2026-06-01T22:15:18Z" model="claude-opus-4-8[1m]/medium" persona="tests" verdict="pass" round="1">
T-007's tests genuinely exercise REQ-006. Evidence file present with a complete CHK roll call (CHK-012, CHK-013 demonstrated; empty-subdir skip and idempotent re-apply demonstrated under REQ-006 `<done-when>`), all citing named tests that exist in the working-tree diff. CHK-012: `symlink_tree_links_each_leaf_and_keeps_intermediate_dirs_real` drives a real `patina apply --yes` subprocess and asserts each leaf `read_link` canonicalizes to its source while `~/d` and `~/d/sub` are real dirs via `symlink_metadata().is_dir() && !is_symlink()` — the link-vs-realdir distinction is asserted directly, not mocked. CHK-013: `symlink_tree_backs_up_pre_existing_leaf_and_replaces_it_with_a_link` pre-writes `original` bytes, applies, asserts a symlink, then runs real `patina rollback --yes` and asserts the leaf is restored to a regular file containing `original`. Empty-subdir skip and idempotency are covered at both CLI and executor-unit level; the skip is a real property of `walk_files` (regular-files-only walk). I independently confirmed non-vacuity: mutating `tree_symlink` to an atomic directory-symlink implementation fails 4 of the 6 executor unit tests, and reverting makes them green. The fixture invokes the real `CARGO_BIN_EXE_patina` binary. Negative paths assert specific typed errors (`NotADirectory`, `SourceMissing`). No fabrication patterns matched.
</review>

<review date="2026-06-01T22:15:18Z" model="claude-opus-4-8[1m]/medium" persona="security" verdict="pass" round="1">
T-007 `symlink-tree` executor is sound on all three flagged vectors. (1) Source symlink loops cannot cause unbounded recursion: `walk_into` (`patina-core/src/apply/mod.rs:263-301`) classifies entries with `DirEntry::file_type()`, which does not follow symlinks, so a symlinked subdir is treated as a non-recursed leaf. (2) No path traversal via crafted leaf names: leaves are real `read_dir` entry names (no `/`, no `.`/`..`), so `target.join(rel)` at `patina-core/src/apply/symlink.rs:115` cannot escape the target root. (3) DEC-007 overwrite is correctly backed up: the engine stashes the declared directory-root target via `backup_before_overwrite` (`patina-core/src/apply/engine.rs:667-668`), and `clone_entry`'s recursive directory clone (`fsx.rs:89-90`, `copy_tree`) captures any pre-existing leaf under the root before `link_file`→`fsx::remove_entry` clobbers it; the CHK-013 CLI test proves prior bytes are restored via real `patina rollback`. No injection/secret/auth surface; no new dependency. Affected unit tests pass.
</review>

<review date="2026-06-01T22:15:18Z" model="claude-sonnet-4-6[1m]" persona="style" verdict="pass" round="1">
All style conventions are satisfied. Key checks:
- `tree_symlink` signature uses `&Utf8Path` / `&[Utf8PathBuf]` matching the existing `per_file_symlink` and `dir_symlink` signatures, consistent with the `rust-let-types-carry-invariants.md` `&Utf8Path` convention.
- `SymlinkTreeNotImplemented` is fully removed: zero references remain in the tree (`grep` confirms).
- `FileMode::SymlinkTree => symlink::tree_symlink(source, targets)` is cleanly wired; no stale stub comments remain.
- The `#![expect(clippy::expect_used, reason = "...")]` attribute in `symlink_tree.rs` carries a non-bare justification and matches the sibling test-crate pattern.
- Imports in `symlink_tree.rs` are all live; no dead imports.
- The test-local helpers (`read_link_canonical`, `canonical`, `assert_real_dir`) are unique to this crate, not duplicated from `common/mod.rs`, consistent with how other integration test files handle crate-specific helpers.
- Tests gate real invariants: link resolution to canonical source, real-vs-symlink directory metadata, backup/rollback byte-fidelity, idempotency.
- Reuse survey was present and accurate: `walk_files` and `link_file` are called, not reimplemented.
</review>

<review date="2026-06-01T22:15:18Z" model="claude-opus-4-8[1m]/medium" persona="correctness" verdict="pass" round="1">
T-007 `symlink-tree` per-leaf executor is correct. Verified: (1) one `CompletionRecord` per leaf flows through `execute` and `build_apply_record` into per-leaf `ExpectedTarget`s with the plan-time entry index; (2) empty source subdirs yield no leaf and thus no target dir/link — `walk_files`/`walk_into` only pushes non-directory entries; (3) intermediate target dirs are created as real dirs via `ensure_parent`→`create_dir_all`, with `create_symlink` applied only to leaf paths; (4) idempotent re-apply holds because `link_file` clears the path via `remove_entry` before re-linking; (5) the overwrite path is sound — `backup_before_overwrite` runs on the directory target ahead of `materialize` and recursively clones the pre-existing subtree, while per-leaf restore reads the leaf via `mirror_backup_path`; the two coincide because `mirror_backup_path` is pure path-mirroring, so the directory-granular backup and leaf-granular restore resolve to the same path. The per-leaf `op_index` advancing past the planned-op count is not a defect: the progress cursor is advisory and recovery probes the filesystem rather than indexing the plan (REQ-011/012), matching the pre-existing directory-source `per_file_symlink` walk shape. No logic, control-flow, Option/Result, boundary, concurrency, or resource-leak defect found in this slice.
</review>
