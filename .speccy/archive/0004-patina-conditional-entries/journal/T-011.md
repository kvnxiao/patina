---
spec: SPEC-0004
task: T-011
generated_at: 2026-06-01T23:25:03Z
---

<implementer date="2026-06-01T23:25:03Z" model="claude-opus-4-8[1m]/high" round="1">
- Reuse survey: Mapped the task area (covered REQ-008 / REQ-001;
  suggested files `patina-core/src/config/writer.rs`,
  `patina-core/src/config/mod.rs`; plus the T-001 mode model in
  `file_entry.rs` the round-trip tests re-parse through). The bounded
  survey found that T-011's entire nominal production surface had
  *already* landed in two prior commits, so the only honest
  classification is reuse-as-is across the board — no new top-level
  symbol was introduced, so the per-symbol floor is vacuously met.
  reuse-as-is: `append_directory_entry` (`writer.rs:210`, added by T-010
  in commit `62c7855` — `rg "fn append_directory"` shows exactly one
  definition, so the caller's "do NOT duplicate" instruction is honoured
  by writing nothing); `mode_manifest_str` (`writer.rs:311`, collapsed to
  `symlink` / `symlink-tree` / `copy` by T-001 in commit `7b1e714`); the
  `MalformedDirectoryArray` / `ModeNotDirectoryValid` variants
  (`writer.rs:59,68`, T-010); the writer re-exports from `config/mod.rs`
  (`mod.rs:40`) and `lib.rs` (`lib.rs:68`); `parse_module_config_str`,
  `EntryKind`, `FileMode`, `ManagedEntry` (T-001) the round-trip tests
  assert against. extend: none. write-fresh: none — searched
  `rg "fn append_directory" patina-core` (one hit, the existing fn) and
  `rg "symlink-dir|copy-tree" patina-core/src` (only the negative-
  assertion test bodies, confirming the spellings are already gone from
  production), both came up with no gap to fill.
- Completed: T-011's deliverable is verification + documentation, because
  its full production surface pre-existed. Confirmed against all four
  task scenarios that (1) `append_directory_entry` writes
  `mode = "symlink-tree"` and re-parses to directory kind + SymlinkTree;
  (2) it writes recursive `copy` as `mode = "copy"` (never `copy-tree`)
  and re-parses to directory kind + CopyTree; (3) `append_file_entry`
  never emits `symlink-dir` / `copy-tree` for any `FileMode`; (4) a
  `TemplateRender` file entry omits the `mode` key. Each scenario has a
  passing, behaviour-gating test in `config::writer::tests`. Captured a
  red-then-green paper trail proving the collapsed-taxonomy and directory
  round-trip tests are load-bearing (reverting `mode_manifest_str` /
  `append_directory_entry` to the pre-collapse spellings fails them);
  restored the working tree to the committed `writer.rs` so no source
  change ships.
- Undone: nothing in T-011's scope. Deliberately wrote no production code:
  duplicating `append_directory_entry` or re-touching `mode_manifest_str`
  would violate the reuse mandate and the "surgical changes" rule, and
  adding redundant tests would be vacuous. The task body mentions
  re-exporting the writer fn from `config/mod.rs` "so `cmd/add.rs`
  (T-010) can call it" — that re-export already exists (T-010 needed and
  added it). T-012 owns the docs refresh.
- Hygiene checks: ran the four standard gates via `just test` + `just
  lint` (CI's exact gate set and order); all exit 0:
  - `cargo test --workspace --locked` → exit 0 (incl. the 15
    `config::writer` unit tests and all doctests)
  - `cargo clippy --workspace --all-targets --all-features --locked -- -D warnings`
    (linux-gnu + windows-gnu cross targets) → exit 0
  - `cargo +nightly fmt --all --check` → exit 0
  - `cargo deny check` → exit 0 (advisories ok, bans ok, licenses ok, sources ok)
- Evidence: paper trail at
  `.speccy/specs/0004-patina-conditional-entries/evidence/T-011.md`.
  Roll call for CHKs under the covered REQs (REQ-008 / REQ-001). T-011's
  own SPEC `<done-when>` CHKs (CHK-016 / CHK-017, the `patina add`
  routing) were demonstrated and closed by T-010; T-011's four
  deliverables are stated as task scenarios over the writer surface, so
  the roll call below tracks those scenarios:
  - Task scenario 1 (`symlink-tree` directory round-trip): demonstrated →
    evidence Scenario 1; test
    `append_directory_symlink_tree_round_trips_through_parser`.
  - Task scenario 2 (recursive `copy` directory round-trip): demonstrated
    → evidence Scenario 2; test
    `append_directory_copy_round_trips_through_parser` (red on
    `copy-tree` revert).
  - Task scenario 3 (`[[file]]` never emits `symlink-dir` / `copy-tree`):
    demonstrated → evidence Scenario 3; test
    `append_each_mode_uses_the_collapsed_parser_accepted_spelling` (red on
    pre-collapse `mode_manifest_str`).
  - Task scenario 4 (`TemplateRender` omits `mode` key): demonstrated →
    evidence Scenario 4; test `append_template_render_omits_mode_key`.
  - CHK-016 / CHK-017 (REQ-008 `patina add` routing): hygiene → closed by
    T-010's CLI integration tests in `patina-cli/tests/add_directory.rs`;
    they pass under the workspace test command and are not re-asserted
    here.
- Discovered issues: the substantive finding of this task is that T-011's
  production scope was fully satisfied by T-001 (the `mode_manifest_str`
  collapse + file-writer test rename) and T-010 (`append_directory_entry`
  + the two new error variants + the three directory round-trip tests).
  T-010's own journal explicitly flagged that it added the minimal
  directory-writer slice `add` needed and noted "T-011 still owns the
  full directory writer surface" — but in practice the slice it added,
  combined with T-001's collapse, already covers every item T-011's task
  body and scenarios enumerate. The caller's brief anticipated exactly
  this. No `remove_directory_entry` is required: T-011's task body and
  scenarios do not ask for one (only `append_directory_entry` and the
  `mode_manifest_str` collapse), and no caller needs it (`remove` keys on
  `[[file]]` targets only in this SPEC). No follow-up needed.
- Procedural compliance: did not edit the locked `SPEC.md`; did not
  hand-edit `TASKS.md` beyond the single `state` attribute flip (pending
  → in-review); the journal is append-only (this is the only block); no
  task was marked `completed` (the review gate owns that). The
  red-baseline experiment was reverted in full — `git status` shows only
  the `TASKS.md` state flip plus the new journal/evidence files, with
  `writer.rs` byte-identical to its committed state.
</implementer>

<review date="2026-06-01T23:31:28Z" model="claude-opus-4-8[1m]/high" persona="business" verdict="pass" round="1">
T-011 disposition is legitimate. (a) `append_directory_entry` exists exactly once at `patina-core/src/config/writer.rs:210` and emits `symlink`/`symlink-tree`/`copy` for directory modes (rejecting file-only modes via `ModeNotDirectoryValid`); (b) `mode_manifest_str` (writer.rs:311) emits only `symlink`/`symlink-tree`/`copy`/None — `symlink-dir`/`copy-tree` appear in production only as historical doc comments, never as output; (c) all four T-011 task-scenarios map to real, passing tests in `config::writer::tests`: scenario 1 → `append_directory_symlink_tree_round_trips_through_parser`, scenario 2 → `append_directory_copy_round_trips_through_parser`, scenario 3 → `append_each_mode_uses_the_collapsed_parser_accepted_spelling`, scenario 4 → `append_template_render_omits_mode_key`. All 15 writer tests pass. The round-trip tests are load-bearing, not vacuous: `from_raw_directory` rejects any non-collapsed spelling with `UnsupportedDirectoryMode`, so emitting the old names would break the parse. Git history confirms the surface landed in 7b1e714 (T-001) + 62c7855 (T-010) and `writer.rs` is byte-unchanged in the working tree, so writing fresh code would have duplicated `append_directory_entry` against the reuse mandate. CHK-016/CHK-017 are correctly attributed to T-010. No silently-resolved open question and no out-of-scope creep.
</review>

<review date="2026-06-01T23:31:28Z" model="claude-opus-4-8[1m]/high" persona="tests" verdict="pass" round="1">
All four T-011 task scenarios map to real, currently-existing, mutation-killing tests in `patina-core/src/config/writer.rs`, and the evidence red/green is honest.
- Scenario 1 → `append_directory_symlink_tree_round_trips_through_parser` (writer.rs:425): genuine round-trip via `parse_module_config_str`, asserts `EntryKind::Directory` + `FileMode::SymlinkTree`. I mutated the `symlink-tree` arm to `symlink` and the test failed — load-bearing.
- Scenario 2 → `append_directory_copy_round_trips_through_parser` (writer.rs:446): round-trip, asserts directory kind + `FileMode::CopyTree` and never `copy-tree`. Mutating the `CopyTree` arm to `copy-tree` failed it at writer.rs:453 — matches evidence.
- Scenario 3 → `append_each_mode_uses_the_collapsed_parser_accepted_spelling` (writer.rs:401): asserts `[[file]]` text never contains `symlink-dir`/`copy-tree`. Reverting `mode_manifest_str` to pre-collapse spellings failed it at writer.rs:415.
- Scenario 4 → `append_template_render_omits_mode_key` (writer.rs:532): asserts `TemplateRender` emits no `mode` key.
I independently re-ran the implementer's mutation experiments: the pre-collapse mutation reproduces exactly the failing tests, line numbers, and failure strings the evidence records (FAILED vs ok differ materially — no fabrication pattern matched), and the evidence command is scoped per-test. All 15 writer tests pass at HEAD; writer.rs is byte-identical to its committed state after I restored each experiment. Evidence roll call complete: four scenarios `demonstrated`, CHK-016/CHK-017 `hygiene` with a concrete cite to T-010. The "verification-only, no new code/tests" claim is honest and verified.
</review>

<review date="2026-06-01T23:31:28Z" model="claude-opus-4-8[1m]/high" persona="security" verdict="pass" round="1">
No security surface introduced. T-011 is verification-only: writer.rs is byte-identical to its committed state (zero uncommitted production diff), so no new attack surface is added by construction. The nominally-owned writer code is injection-safe: user-supplied `source`/`target` are inserted via `toml_edit::value()` (writer.rs:151-152, 228-229), which stores structured value nodes and escapes all quoting/control characters on serialization — no TOML key/table-injection breakout is possible, and no string-concatenation path bypasses it (the sole `format!` is a test assertion). The `mode` field is a closed `FileMode` enum mapped to `&'static str` literals, so it carries no user-controlled text. Error paths return typed `ConfigWriteError` variants with no sensitive-data leakage and no panics. No secrets, auth boundaries, crypto, or new dependencies in scope.
</review>

<review date="2026-06-01T23:31:28Z" model="claude-sonnet-4-6[1m]/medium" persona="style" verdict="pass" round="1">
All style checks pass for T-011's diff surface in `patina-core/src/config/writer.rs`.
- No orphan `symlink-dir` / `copy-tree` string literals appear in production code; the grep confirms they exist only in doc comments and negative `!text.contains(...)` assertions.
- `append_directory_entry` uses `&str` for `source` and `target`, matching `append_file_entry` exactly — correct, since these are TOML manifest string values, not OS-layer filesystem paths where `&Utf8Path` would apply.
- The two new error variants (`MalformedDirectoryArray`, `ModeNotDirectoryValid`) mirror the pre-existing `MalformedFileArray` pattern.
- `append_directory_entry`'s doc comment carries a full `# Arguments`, `# Errors`, and `# Examples` section consistent with project convention.
- No dead code from the mode-taxonomy collapse: `mode_manifest_str` now handles all six `FileMode` variants exhaustively with no unreachable arms.
- Tests are load-bearing: round-trip tests assert parser-accepted spellings and fully parsed field values; the rejection test asserts the typed error variant.
One pre-existing doc drift (line 109 of `writer.rs` says "four user-declarable modes") is not in T-011's diff and is out of scope for this review (flagged for T-012).
</review>

<review date="2026-06-01T23:31:28Z" model="claude-opus-4-8[1m]/high" persona="correctness" verdict="pass" round="1">
T-011 directory-writer round-trip is correct and lossless. `append_directory_entry` (writer.rs:216-223) maps SymlinkDir→"symlink", SymlinkTree→"symlink-tree", CopyTree→"copy", and `from_raw_directory` (file_entry.rs:238-247) inverts each back to the same FileMode + EntryKind::Directory; file-only modes are rejected via ModeNotDirectoryValid. `mode_manifest_str` (writer.rs:311-318) emits only symlink/symlink-tree/copy (never symlink-dir/copy-tree) and returns None for TemplateRender so re-parse re-derives template-render from the .tmpl suffix (file_entry.rs:193). No cross-table mode-string collision: the array table name selects the constructor, so shared spellings "symlink"/"copy" decode to the table-appropriate FileMode and never produce a wrong kind. Behavior-gating tests cover all four scenarios.
</review>
