---
spec: SPEC-0004
task: T-004
generated_at: 2026-06-01T23:30:00Z
---

<implementer date="2026-06-01T23:30:00Z" model="claude-opus-4-8[1m]/high" round="1">
- Reuse survey: Mapped the task area (covered REQ-005; suggested files
  `patina-core/src/apply/engine.rs` and `patina-cli/tests/variable_layers.rs`;
  plus the upstream T-003 root parser `config::root` and the `Resolver`
  layer API in `variables/mod.rs`). Decisions: reuse-as-is —
  `config::parse_root_config` (`config/root.rs:96`, the T-003 parser) for
  the `RootConfig { repo_shared, per_profile }` it already returns;
  `Resolver::with_repo_shared` (`variables/mod.rs:224`) and
  `Resolver::with_per_profile` (`variables/mod.rs:192`) for the two pushes
  — both already exist and run the reserved-key check, so no new layer code
  is owed; the `Fixture` harness in `patina-cli/tests/common/mod.rs`
  (`module` / `apply` / `apply_with_env`, and its `env_remove("PATINA_PROFILE")`
  default) for the CLI tests; the `.tmpl`-render test shape from
  `apply_cli.rs::cli_variable_override_renders_into_template`
  (`apply_cli.rs:162`). extend — `EngineError` (`error.rs`): added a
  `RootConfig(#[from] crate::config::RootConfigError)` variant so
  `parse_root_config(...)?` threads in `plan()`, mirroring the existing
  per-subsystem `#[from]` variants. write-fresh — a private
  `table_to_layer(&toml::value::Table) -> Vec<(String, String)>` helper in
  `engine.rs`; searched the crate (`rg "fn table_to_layer"`,
  `rg "filter_map.*as_str"`) and found only the inline copy in the
  per-module push (engine.rs, the only `[variables]`→layer projection),
  which I folded into the new helper so all three pushes share it; the new
  CLI test module `patina-cli/tests/variable_layers.rs` (no existing test
  exercised the root-shared or per-profile layers). New top-level symbols:
  `table_to_layer` (replaces a duplicated inline `filter_map`, called by
  all three layer pushes) and the `RootConfig` `EngineError` variant
  (no pre-existing variant covered `RootConfigError`).
- Completed: Wired the two REQ-005 layers into `apply::plan`
  (`patina-core/src/apply/engine.rs`). `plan()` now calls
  `parse_root_config(&root_manifest)` (the root manifest is already joined
  for `load_auto_match_rules`), pushes the repo-shared `[variables]` table
  with `.with_repo_shared(table_to_layer(&root_config.repo_shared))` while
  building the resolver, and — when `root_config.per_profile` contains the
  resolved active profile name — pushes that profile's table with
  `.with_per_profile(...)`. The no-profile fallback (empty profile name)
  matches no `per_profile` key, so no per-profile table is pushed. Added
  the `RootConfig` `#[from]` variant to `EngineError` and the private
  `table_to_layer` helper (string-valued entries only), and replaced the
  per-module loop's inline `filter_map` with a `table_to_layer` call so the
  three pushes share one projection. Added four CLI integration tests in
  `patina-cli/tests/variable_layers.rs` (CHK-010, CHK-011, per-module-beats-
  repo-shared, no-profile fallback).
- Undone: Nothing in REQ-005's scope. Precedence is unchanged by design —
  it is fixed by the resolver's layer order (`variables/mod.rs` `get`), and
  this task only populates two previously-omitted layers, so I added no
  ordering logic. The per-machine layer (SPEC.md:95) remains a future
  concern and is untouched. `when`-gating (T-005), the source kind/existence
  check (T-006), and the `symlink-tree` executor (T-007) are separate tasks
  and were not started.
- Hygiene checks:
  - `cargo test --workspace --locked` (`just test`): exit 0 — full
    workspace suite plus doctests pass, including the four new
    `variable_layers` CLI tests.
  - `cargo clippy --workspace --all-targets --all-features --locked -- -D warnings`
    (run via `just lint`, cross-compiled to `x86_64-unknown-linux-gnu` and
    `x86_64-pc-windows-gnu`; macOS leg skipped off a Mac, CI backstops):
    exit 0. An initial clippy run flagged `clippy::expect_used` on the free
    `root_manifest_with` helper in the new test file (the
    `allow-expect-in-tests` config covers `#[cfg(test)]` modules and
    `#[test]` fns but not free fns at the integration-crate root); added a
    documented crate-level `#![expect(clippy::expect_used, reason = ...)]`,
    matching the precedent in `tests/common/mod.rs`, and re-ran green.
  - `cargo +nightly fmt --all --check` (via `just lint`): exit 0.
  - `cargo deny check` (via `just lint`): exit 0 — advisories / bans /
    licenses / sources all ok (pre-existing transitive `wit-bindgen`
    duplicate-crate warnings only; no dependency added).
- Evidence: paper trail at
  `.speccy/specs/0004-patina-conditional-entries/evidence/T-004.md`.
  T-004 covers REQ-005. Roll call for the CHKs under REQ-005:
  - CHK-010 (a root `[variables]` value renders into a `.tmpl` target under
    `apply --yes`): demonstrated → evidence `root_variable_renders_into_module_template`
    drives the real `patina apply --yes` over a fixture whose root
    `[variables]` sets `editor = "nvim"`; the `.tmpl` target materializes
    `editor = nvim`. Red/green: without the `with_repo_shared` push the
    strict-undefined engine errors on `{{ editor }}` and the apply exits
    non-zero, failing the `code == 0` assertion.
  - CHK-011 (an active profile's `[profiles.work.variables]` shadows the
    root value): demonstrated → evidence `active_profile_variable_shadows_repo_shared`
    runs `PATINA_PROFILE=work patina apply --yes` with `editor = "nvim"`
    (root) and `editor = "code"` (profile `work`); the target materializes
    `editor = code`. The two layer values are distinct so the assertion
    pins precedence, not mere rendering. Without the `with_per_profile`
    push the repo-shared `nvim` would render and fail the assertion.
  The remaining REQ-005 `<done-when>` bullets: "per-module beats
  repo-shared" is demonstrated by `per_module_variable_beats_repo_shared`
  (root `nvim` vs module `emacs` → `emacs` renders); the reserved-`patina.*`
  rejection bullet is hygiene → enforced by `Resolver::with_repo_shared` /
  `with_per_profile` themselves (they call the reserved-key check) and the
  parser's own T-003 unit tests, reused unchanged here.
- Discovered issues: none. The `config/mod.rs` module-level docstring still
  describes only the per-module `[variables]` table (flagged in T-003's
  journal for the T-012 docs sweep); not made worse by this task and left
  untouched to keep the change surgical.
- Procedural compliance: This round-1 `<implementer>` block creates
  `journal/T-004.md` with three-field frontmatter per the journal schema;
  no `<implementer-note>` was written into TASKS.md. The TASKS.md
  `state="..."` attribute for T-004 was flipped `pending` → `in-progress`
  at the start of this turn and `in-progress` → `in-review` after all four
  hygiene gates passed. The locked `SPEC.md` was not edited. No shipped
  `.claude/skills/speccy-*` bodies required edits during this task.
</implementer>

<review date="2026-06-01T21:36:07Z" model="claude-opus-4-8[1m]/high" persona="business" verdict="pass" round="1">
T-004 faithfully delivers REQ-005's wiring. All four `<done-when>` bullets map to the diff: root-only variable renders (CHK-010, `variable_layers.rs:29`), active-profile shadows repo-shared with distinct values pinning precedence not mere rendering (CHK-011, `variable_layers.rs:55`), per-module beats repo-shared (`variable_layers.rs:87`), and reserved-`patina.*` rejection in either root or profile table surfaces as a typed plan error via the new `EngineError::RootConfig(#[from] RootConfigError)` variant over T-003's `reject_reserved_keys` calls. The two pushes land before `discover_modules` and before `assemble_plan_operations`, so the resolver carried in `ResolvedPlan` and reused by executors/hook-`when`/diff sees them, exactly as REQ-005 and the task body require. Precedence is unchanged — the resolver's fixed layer order (CLI > per-machine > per-profile > per-module > repo-shared > built-ins) matches the SPEC verbatim; no ordering logic was added. The no-profile fallback (the SPEC-named edge case) is guarded by `if let Some(table) = root_config.per_profile.get(&profile.name)` and covered by a dedicated test. Non-goals respected: the per-machine layer is correctly left unwired. No scope creep, no silently-resolved open question (SPEC has none), and the four CLI tests pass green.
</review>

<review date="2026-06-01T21:36:07Z" model="claude-opus-4-8[1m]/high" persona="tests" verdict="pass" round="1">
The four CLI integration tests in `patina-cli/tests/variable_layers.rs` drive the real `patina apply --yes` binary (`env!("CARGO_BIN_EXE_patina")` subprocess via the `Fixture` harness) over a tempdir fixture repo with isolated `PATINA_REPO`/`HOME`/state, then read the materialized `~/.editor` target from disk and assert rendered template content — no mocks, real code path end-to-end. Each test is mutation-resistant: without `with_repo_shared` the strict MiniJinja engine errors on the undefined `{{ editor }}` interpolation so CHK-010's `code == 0` fails; CHK-011 and `per_module_variable_beats_repo_shared` use distinct per-layer values (`nvim`/`code`, `nvim`/`emacs`) so the `contains` assertions pin layer precedence, not mere rendering; `no_profile_selects_no_per_profile_table` asserts the repo-shared `nvim` renders (not the inert profile `code`), catching an unconditional-push bug. Evidence carries a complete REQ-005 roll call (CHK-010/CHK-011 demonstrated, per-module and reserved-key bullets covered, the latter `hygiene` with specific cites). Captured runner output is structurally consistent with real libtest output and names only the four tests present in the diff. No fabrication pattern matched.
</review>

<review date="2026-06-01T21:36:07Z" model="claude-opus-4-8[1m]/medium" persona="security" verdict="pass" round="1">
T-004 wires the repo-shared and active-profile variable layers into apply planning; no security boundary is crossed. All inputs are the user's own repo manifest (single-user CLI, no auth/network surface). The one security-relevant invariant — `patina.*` reserved-namespace rejection that prevents spoofing built-in identity vars — is enforced redundantly: `parse_root_config_str` calls `reject_reserved_keys` on both the repo-shared and each per-profile table (`patina-core/src/config/root.rs:136,141`) and `with_repo_shared`/`with_per_profile` re-check via `push_user_layer` (`patina-core/src/variables/mod.rs:277`). No injection vector (`table_to_layer` only projects string-valued entries), no secret/value leakage in error messages (`RootConfigError` names paths and keys, never values), and no new dependency. Pass.
</review>

<review date="2026-06-01T21:36:07Z" model="claude-sonnet-4-6[1m]/medium" persona="style" verdict="pass" round="1">
All style criteria pass. Imports are alphabetically ordered one-symbol-per-`use`, matching the surrounding engine.rs convention. `resolve_entry` takes `&Utf8Path` (not `&Utf8PathBuf`) per the parameter-type hierarchy in `rust-let-types-carry-invariants.md`. The `#![expect(clippy::expect_used, reason = ...)]` annotation in `variable_layers.rs` carries a substantive justification string and matches the `common/mod.rs` precedent exactly. No dead imports or unused symbols are introduced. `table_to_layer` is a genuine consolidation of a pre-existing inline `filter_map`, not reinvention. Test assertions gate real invariants. No `unwrap()`/`expect()` in production code.
</review>

<review date="2026-06-01T21:36:07Z" model="claude-opus-4-8[1m]/high" persona="correctness" verdict="pass" round="1">
T-004 wires the repo-shared and active-profile variable layers correctly. The two new pushes populate distinct resolver `Layer` fields; precedence is fixed by `Resolver::get`'s lookup order (`variables/mod.rs:249-266`) and is unaffected by push order in `plan()`. The mutated resolver is the one moved into `ResolvedPlan`, so executors/hook-when-evaluator/diff-renderer see the layers. The no-profile fallback does `per_profile.get("")`, which selects no table for normally-keyed profiles. The `table_to_layer` extraction is behavior-preserving relative to the prior inline per-module `filter_map`. `parse_root_config(...)?` short-circuits at plan time via the new transparent `EngineError::RootConfig` variant with no mutation or cleanup skipped. All four new CLI tests and the full `patina-core` suite pass locally. No logic, boundary, Option/Result, concurrency, or resource-leak defect found at or above the 80 confidence bar.
</review>
