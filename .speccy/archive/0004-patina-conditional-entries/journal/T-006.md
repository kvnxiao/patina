---
spec: SPEC-0004
task: T-006
generated_at: 2026-06-01T22:10:00Z
---

<implementer date="2026-06-01T22:10:00Z" model="claude-opus-4-8[1m]/high" round="1">
- Reuse survey: Mapped the task area (covered REQ-002 / REQ-009 / DEC-008;
  suggested files `patina-core/src/apply/engine.rs`,
  `patina-core/src/error.rs`, `patina-cli/tests/source_kind_validation.rs`;
  plus the upstream surfaces the check plugs into — the T-005
  `gate_and_resolve_entry` / `resolve_entry` per-entry pipeline, the
  `ManagedEntry.kind` / `EntryKind` model from T-001, and the executor's
  materialize-time existence check that DEC-008 keeps as a backstop).
  Decisions: reuse-as-is — `paths::canonicalize` (`paths.rs:112`, already
  called in `resolve_entry`); `EntryKind` / `ManagedEntry.kind`
  (`config/file_entry.rs`, T-001); `fs_err::metadata` (a project dependency,
  follows symlinks so the validated kind matches what the executor
  materializes); the existing `EngineError::Path(#[from] PathError)` variant
  and its `PathError::Filesystem { path, source }` shape (`paths.rs:59`) for
  the non-NotFound IO error; the T-005 `gate_and_resolve_entry` `Some`/`None`
  branching, which already guarantees the check runs only on `when`-survivors
  (CHK-022); the `Fixture` harness (`patina-cli/tests/common/mod.rs`) and its
  `state_root()` for locating the journal dir. Left untouched (DEC-008
  backstop) — `apply/symlink.rs` `ExecutorError::SourceMissing` and the
  copy/template equivalents. extend — `resolve_entry` gains one
  `validate_source_kind(&source, entry.kind)?` call after canonicalization,
  before target expansion; `EngineError` gains two variants. write-fresh —
  `validate_source_kind` (the plan-time existence-and-kind probe) and the new
  CLI test module `source_kind_validation.rs`. Searched the crate
  (`rg "is_file\|is_dir\|symlink_metadata\|fn validate"` across
  `patina-core/src`) for an existing source-kind validator and found only the
  executor's materialize-time `symlink_metadata` check in `apply/symlink.rs`
  — that is the structurally-distinct materialize-time backstop (no
  "directs to the other table" plan-time semantics), so the plan-time
  validator is genuinely new. New top-level symbols: `validate_source_kind`
  (justified above — no existing plan-time kind check), and the two
  `EngineError` variants (the executor's `SourceMissing` / `NotADirectory`
  are materialize-time and do not carry the table-direction message REQ-002
  mandates).
- Completed: Added the plan-time source existence-and-kind check (REQ-002,
  step 3 of the REQ-009 order). `resolve_entry`
  (`patina-core/src/apply/engine.rs`) now calls
  `validate_source_kind(&source, entry.kind)?` immediately after
  canonicalizing the source and before expanding targets. The helper probes
  the canonical source with `fs_err::metadata`: `NotFound` →
  `EngineError::SourceNotFound { path }`; any other IO error →
  `EngineError::Path(PathError::Filesystem { .. })`; a `[[file]]` whose
  metadata `is_dir()` → `EngineError::SourceKindMismatch` naming the source,
  `directory`, `[[file]]`, and the `[[directory]]` table to use instead; a
  `[[directory]]` whose metadata is not a dir → the symmetric mismatch
  directing to `[[file]]`. Because the check lives inside `resolve_entry`,
  which `gate_and_resolve_entry` calls only on the `when`-survivor branch, a
  `when`-false entry is never canonicalized or kind-checked (REQ-009 / CHK-022).
  Added the two `EngineError` variants in `patina-core/src/error.rs` with
  doc comments tying them to REQ-002 / DEC-008 and noting the retained
  executor backstop. The explicit existence probe (rather than relying on
  canonicalization failing) is necessary because `paths::canonicalize` falls
  back to lexical resolution for a non-existent path. Added 4 engine unit
  tests over `validate_source_kind` and 5 CLI integration tests (see
  Evidence).
- Undone: Nothing in T-006's scope is left undone — REQ-002 and the REQ-009
  step-(3) check are complete. Tasks slotted for later, unchanged here: the
  `symlink-tree` per-leaf executor (T-007), the `when`-aware status
  managed-set and orphan reaping (T-008), the auto_match/predicate-engine
  unification (T-009), `patina add` kind-routing (T-010), the directory
  manifest writer (T-011), and the docs sweep (T-012).
- Hygiene checks:
  - `just test` (= `cargo test --workspace --locked`): exit 0 — full
    workspace suite plus doctests pass, including the 4 new engine unit tests
    and the 5 new `source_kind_validation` CLI tests; the T-002 / T-005
    engine tests still pass.
  - `just lint` clippy (`cargo clippy --workspace --all-targets
    --all-features --locked -- -D warnings`, cross-compiled to
    `x86_64-unknown-linux-gnu` and `x86_64-pc-windows-gnu`; macOS leg skipped
    off a Mac with CI as backstop): exit 0. Clippy initially flagged
    `clippy::panic` on three `panic!`-in-match-arm test assertions (rewrote
    them as `assert!(matches!(&err, ... if ...))`, the repo's panic-free
    error-variant assertion pattern) and `clippy::case_sensitive_file_
    extension_comparisons` on the test journal-artifact filter (switched to a
    `Path::extension().eq_ignore_ascii_case(..)` check); both fixed and
    re-run green.
  - `just lint` fmt (`cargo +nightly fmt --all --check`): exit 0 (ran
    `cargo +nightly fmt --all` to apply the wrapping rustfmt wanted on a doc
    comment, then re-checked).
  - `just lint` docs (`RUSTDOCFLAGS="-D warnings" cargo doc`): exit 0 — no
    broken intra-doc links from the new `EngineError` variants or
    `validate_source_kind` references.
  - `just lint` `cargo deny check`: advisories / bans / licenses / sources
    all ok (pre-existing transitive `wit-bindgen` duplicate-crate warnings
    only; no dependency added — the change is `fs_err` + existing crates).
- Evidence: paper trail at
  `.speccy/specs/0004-patina-conditional-entries/evidence/T-006.md`.
  T-006 covers REQ-002 and REQ-009. Roll call for the CHKs under those REQs:
  - CHK-004 (a `[[file]]` directory source exits 1, names the source and
    `[[directory]]`, no journal plan/COMMIT): demonstrated → evidence
    `file_entry_with_directory_source_fails_and_directs_to_directory_table`
    drives the real `patina apply --yes`; asserts exit 1, stderr contains
    `confdir` and `[[directory]]`, the target is not created, and
    `assert_no_journal_artifacts` confirms no `*.plan` / `*.COMMIT`.
    Red/green: relying on the executor's later check would flush the plan
    journal first, failing the no-artifact assertion.
  - CHK-005 (a `[[directory]]` file source directs to `[[file]]`):
    demonstrated → evidence
    `directory_entry_with_file_source_fails_and_directs_to_file_table`
    asserts exit 1 and stderr contains `[[file]]`.
  - CHK-018 (a `when`-true `[[file]]` with an absent source exits 1, names
    the source, no journal plan/COMMIT): demonstrated → evidence
    `when_true_entry_with_absent_source_fails_as_source_not_found` asserts
    exit 1, stderr names `ghost`, target not created, no journal artifact.
  - CHK-022 (a `when`-false wrong-OS absent-source `[[directory]]` exits 0
    with no kind/missing-source error): demonstrated → evidence
    `when_false_entry_with_absent_wrong_kind_source_is_not_validated` asserts
    exit 0 and stderr contains neither `does not exist` nor `[[file]]`. This
    is the ordering proof — the check must run after the T-005 `when` gate.
  - REQ-002 `<done-when>` (kind mismatch and missing-source variant shapes):
    demonstrated → the 4 engine unit tests over `validate_source_kind`
    (`file_entry_with_directory_source_is_a_kind_mismatch`,
    `directory_entry_with_file_source_is_a_kind_mismatch`,
    `absent_source_is_source_not_found`, `matching_kinds_validate_cleanly`)
    pin the exact variant fields and the matched-kind clean path.
  - REQ-002 `<done-when>` TOCTOU backstop (a source deleted between plan and
    materialize still fails with a typed error, not a panic): hygiene →
    DEC-008's executor-side `ExecutorError::SourceMissing` is left untouched
    by this task; its own coverage lives in the executor tests and runs under
    the project test command. Not re-proved here (T-006 adds the plan-time
    check; it does not modify the backstop).
  - REQ-002 `<done-when>` non-interactive parity (a non-TTY apply surfaces
    the same error and writes nothing): demonstrated → the CLI tests run the
    binary as a subprocess (stdin is not a TTY, per the `Fixture` doc), so
    every CHK-004 / CHK-005 / CHK-018 assertion above already exercises the
    non-interactive path.
- Discovered issues: none new. The `plan()` module-level docstring and some
  neighbouring comments still phrase things in terms of `[[file]]` entries (a
  pre-`[[directory]]`-split artefact already flagged in earlier journals for
  the T-012 docs sweep); not made worse here and left untouched to keep this
  change surgical.
- Procedural compliance: This round-1 `<implementer>` block creates
  `journal/T-006.md` with three-field frontmatter per the journal schema; no
  `<implementer-note>` was written into TASKS.md. The TASKS.md `state="..."`
  attribute for T-006 was flipped `pending` → `in-progress` at the start of
  this turn and `in-progress` → `in-review` after all four hygiene gates
  passed. The locked `SPEC.md` was not edited. No shipped
  `.claude/skills/speccy-*` bodies required edits during this task.
</implementer>

<review date="2026-06-01T22:03:57Z" model="claude-opus-4-8[1m]/xhigh" persona="business" verdict="pass" round="1">
T-006 delivers exactly what REQ-002 and the REQ-009 step-(3) check promise. Every `<done-when>` maps to the diff: `[[file]]`-dir-source and `[[directory]]`-file-source mismatches raise a typed `EngineError::SourceKindMismatch` naming the source path and directing to the correct table (`patina-core/src/error.rs` + the symmetric arms in `validate_source_kind`, `patina-core/src/apply/engine.rs`); an absent `when`-true source raises `EngineError::SourceNotFound` at plan time. All four scenarios are covered by CLI tests in `patina-cli/tests/source_kind_validation.rs` (CHK-004 names `confdir` + `[[directory]]` and asserts no `*.plan`/`*.COMMIT`; CHK-005 directs to `[[file]]`; CHK-018 names `ghost` + no journal artifact; CHK-022 gated-off entry exits 0 with no kind/missing-source error), plus four engine unit tests pinning the variant shapes and the clean matched-kind path. The "before any mutation" guarantee is structurally sound: the check fires inside `plan()` (`patina-cli/src/cmd/apply.rs:88`), well ahead of the lock and journal flush in `execute_plan`. The DEC-008 executor TOCTOU backstop (`apply/symlink.rs:33-44`) is left untouched. CHK-022 ordering falls out correctly because `validate_source_kind` lives in `resolve_entry`, called only on the `when`-survivor branch. No non-goal scope snuck in, Open Questions empty. The `fs_err::metadata` (follows-symlinks) choice is a defensible plan-time-vs-materialize parity decision.
</review>

<review date="2026-06-01T22:03:57Z" model="claude-opus-4-8[1m]/high" persona="tests" verdict="pass" round="1">
All four target CHKs are driven by real subprocess `patina apply` tests that exercise the actual plan-time path, not mocks. CHK-004/CHK-018 `assert_no_journal_artifacts` is non-vacuous: I confirmed in `engine.rs::execute` that `flush_plan_and_fsync` (writes `<ts>.plan`) precedes per-op `materialize` (where the retained `SourceMissing` backstop fires), so dropping the T-006 check would leave a `.plan` artifact and turn the assertion red — the helper reads the real `<state_root>/journal` dir with a `.plan`/`.COMMIT` filter matching the real layout. CHK-005 pins to the symmetric mismatch via the `[[file]]` substring; CHK-022's negative assertion (`!contains("does not exist") && !contains("[[file]]")`) is the tight gate-ordering proof, not catch-any. Evidence roll call covers CHK-004/005/018/022 (`demonstrated`) plus REQ-002 done-when engine unit tests and the TOCTOU backstop (`hygiene`, cited to untouched executor tests); every named test symbol appears in the diff; the scoped `cargo test -p patina-cli --test source_kind_validation` command is correctly distinct from the full-suite hygiene run. Tests are isolated per-`Fixture` with no time/ordering/shared-state flake surface.
</review>

<review date="2026-06-01T22:03:57Z" model="claude-opus-4-8[1m]/high" persona="security" verdict="pass" round="1">
T-006 passes security review. The DEC-008 materialize-time TOCTOU backstop is fully retained: the executor's existence checks (`patina-core/src/apply/symlink.rs:33-44` `ExecutorError::SourceMissing`, `copy.rs:23-24`/`:52-54`, `template.rs:31-32`) are untouched by this slice — the only uncommitted production files are `patina-core/src/apply/engine.rs` and `patina-core/src/error.rs`. The new `validate_source_kind` is purely additive and correctly ordered: it runs inside `resolve_entry` during `plan()`, strictly before the exclusive lock acquisition and journal flush in `execute()`, so a kind/missing-source failure aborts with zero writes and no `*.plan`/`*.COMMIT` artifact — verified by `assert_no_journal_artifacts`. No panics/unwrap/expect in production; non-`NotFound` IO maps to a typed `EngineError::Path` rather than being swallowed; the `EntryKind` match is exhaustive. The plan-time `fs_err::metadata` (follows symlinks) vs executor `symlink_metadata` asymmetry is benign — two independent checks on an already-canonicalized path. No new dependencies, no secrets, no auth or network surface, no injection vectors. Path disclosure in error messages is not a concern for a local single-user CLI.
</review>

<review date="2026-06-01T22:03:57Z" model="claude-sonnet-4-6[1m]/medium" persona="style" verdict="pass" round="1">
All style invariants hold for T-006's working-tree changes across `patina-core/src/apply/engine.rs`, `patina-core/src/error.rs`, and `patina-cli/tests/source_kind_validation.rs`.
- `validate_source_kind` correctly takes `&Utf8Path` (not `&Utf8PathBuf`), matching the `rust-let-types-carry-invariants.md` / `rust-code-quality.md` parameter-type hierarchy; the `Utf8PathBuf` source auto-derefs at the call site.
- `use crate::config::EntryKind;` is inserted alphabetically within the existing `use crate::config::*` block; no import ordering drift.
- No dead imports or unused symbols introduced; `EntryKind` is used in `validate_source_kind` and its tests.
- `err.kind() == std::io::ErrorKind::NotFound` matches the codebase's established pattern (`symlink.rs:34`, `copy.rs:53`, `fsx.rs:53`, `root.rs:99`); `fs-err` returns `std::io::Error`, so `.kind()` is valid.
- `PathError::Filesystem { path, source: err }` construction matches `paths.rs:59-65`.
- All 4 unit tests gate real variant fields; the 5 CLI tests gate real exit-code and stderr-content invariants; none are vacuous.
- The stale "lands in T-006" placeholder in `gate_and_resolve_entry`'s doc is replaced with a description of the actual step-3 placement; `resolve_entry` gains a proper `# Errors` section.
- No `unwrap()` in production code; no panic-family macros outside `#[cfg(test)]`.
</review>

<review date="2026-06-01T22:03:57Z" model="claude-opus-4-8[1m]/high" persona="correctness" verdict="pass" round="1">
T-006 source existence-and-kind check is correct. Verified: (1) `validate_source_kind` (engine.rs:501) is called only from `resolve_entry` (engine.rs:471), itself reached only via the `Some` branch of `gate_and_resolve_entry` (engine.rs:449) — i.e. after `eval_when` returns true and after canonicalization — so a `when`-false entry returns `Ok(None)` and is never canonicalized or kind-checked (CHK-022, confirmed by the gated-off CLI test exiting 0). (2) The existence check is an explicit `fs_err::metadata` probe matching `ErrorKind::NotFound`, not a reliance on canonicalization, which falls back to lexical resolution for a missing path. (3) The kind check reads the same metadata via `is_dir()`; the two mismatch arms (`File`+is_dir, `Directory`+!is_dir) are mutually exclusive with a clean `Ok` catch-all — no mis-firing or shadowed arm. The error is raised in the plan phase strictly before the advisory lock / journal flush, and the `assert_no_journal_artifacts` CLI tests confirm no `*.plan`/`*.COMMIT` is written on failure. The `metadata`-follows-symlinks choice over `symlink_metadata` is a deliberate, documented decision and affects no in-scope scenario. `entry.kind` is sourced from the declared table, so mismatch messages direct to the correct opposite table (REQ-002). No reachable logic, boundary, or resource defect at or above the confidence threshold.
</review>
