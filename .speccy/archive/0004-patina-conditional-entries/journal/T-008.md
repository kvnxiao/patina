---
spec: SPEC-0004
task: T-008
generated_at: 2026-06-01T22:33:32Z
---

<implementer date="2026-06-01T22:33:32Z" model="claude-opus-4-8[1m]" round="1">
- Reuse survey: Mapped the task area (covered REQ-007 / REQ-003, suggested
  files `patina-core/src/status/mod.rs`, `patina-core/src/status/classify.rs`,
  `patina-cli/tests/symlink_tree_orphans.rs`, plus their immediate neighbours
  `apply/engine.rs::plan`/`execute`, `apply/symlink.rs::tree_symlink`,
  `apply/mod.rs::walk_files`, `backups`, `fsx`, `journal/record.rs`).
  Decisions:
  - reuse-as-is — `status::manage_key` (cross-time comparison key, unchanged);
    `backups::backup_before_overwrite` (REQ-014 stash) for the reap's
    pre-removal backup; `fsx::remove_entry`/`engine::remove_target` for the
    removal; `journal::read_latest_commit` + `ApplyRecord::targets` /
    `ExpectedTarget::target()` to enumerate prior-committed targets;
    `Engine::eval_when` for the `when` gate; `paths::expand_tilde`;
    `engine::table_to_layer`. No new symbol for any of these.
  - extend — `status::current_plan_targets` now delegates to a new
    `when`-aware / symlink-tree-aware managed-set computation rather than its
    old `config.files`-only walk; `apply::mod::walk_files` promoted `fn` →
    `pub(crate)` so the status managed-set can reuse the executor's exact leaf
    walk (per `rust-let-types-carry-invariants.md`: promote-and-call the
    sibling helper, don't duplicate the walk).
  - write-fresh — `apply::engine::current_managed_targets` (public),
    `insert_managed_targets` (private), `build_planning_context` +
    `PlanningContext` (factored out of `plan()` so the `when` gate sees the
    same resolver in planning and status), and `reap_orphans` (private). I
    grepped `apply/`, `lib.rs`, and `rollback/` for an existing apply-time
    reap (`current_plan_targets`/`still_managed`/`read_latest_commit`) and
    found none — ORPHANED was a status-only classification, never acted on
    (confirmed against SPEC-0001 REQ-018). See Discovered issues.
- Completed: (1) Made the status managed-set `when`-aware and
  `symlink-tree`-leaf-aware. Factored `plan()`'s repo/profile/resolver/engine
  assembly into `build_planning_context`, then added
  `apply::engine::current_managed_targets`, which walks every module's
  `[[file]]` + `[[directory]]` entries, drops `when`-false ones via
  `Engine::eval_when`, and (for `symlink-tree`) expands each entry into one
  `manage_key` per live source leaf via the shared `walk_files`.
  `status::current_plan_targets` now delegates to it (signature unchanged, so
  `lib.rs::status` is untouched). (2) Added the apply-time orphan reap:
  `execute` calls `reap_orphans` on the committing path (after post_apply
  hooks succeed, before commit), which reads the last commit, finds recorded
  targets absent from `current_managed_targets`, backs each up into the run's
  backup tree (REQ-014), and removes it — never removing a directory (DEC-005
  guard via `symlink_metadata().is_dir()`). The reap is gated on
  `!matches!(policy, LockPolicy::Held(_))` so `patina remove`/`promote` (which
  re-journal one surgically-promoted target under a held lock) do not reap.
  Touched `patina-core/src/apply/engine.rs`, `patina-core/src/status/mod.rs`,
  `patina-core/src/apply/mod.rs`; added `patina-cli/tests/symlink_tree_orphans.rs`
  (CHK-014, CHK-015, CHK-019) and two engine unit tests for
  `insert_managed_targets`.
- Undone: Rollback does not restore a *reaped* target. The reap stashes prior
  bytes per REQ-014/CHK-019, but reaped targets are not added to the COMMIT
  record's `ExpectedTarget` list, so `patina rollback` (which replays only
  recorded targets) will not recreate a reaped file. CHK-019 requires only the
  backup-on-reap, which holds; full rollback-of-reap would need a journal
  wire-format addition DEC-009 explicitly cautions against bumping, so it is
  left out of scope. Flagged under Discovered issues. T-009..T-012 remain per
  their `<task>` elements.
- Hygiene checks: `cargo test --workspace --locked` exited 0 (no failures; the
  full suite including the new `symlink_tree_orphans` 3 tests and the 2 new
  `insert_managed_targets` unit tests passes). `cargo clippy --workspace
  --all-targets --all-features --locked -- -D warnings` exited 0 (also clean
  for both cross targets via `just lint`: `x86_64-unknown-linux-gnu` and
  `x86_64-pc-windows-gnu`). `cargo +nightly fmt --all --check` exited 0.
  `cargo deny check` exited 0 (advisories ok, bans ok, licenses ok, sources
  ok; no new dependency added). The `cargo doc -D warnings` docs gate (run via
  `just lint`) exited 0 after fixing one private-intra-doc-link warning
  (a `[`walk_files`]` link from a `pub` doc to a `pub(crate)` item).
- Evidence: red-then-green paper trail at
  `.speccy/specs/0004-patina-conditional-entries/evidence/T-008.md`.
  Roll call for the CHKs under REQ-007 / REQ-003:
  - CHK-014 (deleted symlink-tree source leaf reported orphaned by status):
    demonstrated → evidence Scenario 1 captures the pre-edit FAILED (managed
    set ignored `[[directory]]`/leaf expansion) versus the post-edit `ok`.
  - CHK-015 (next apply reaps the orphan leaf, keeps sibling + directory):
    demonstrated → evidence Scenario 2 captures the pre-edit FAILED (no reap
    path) versus the post-edit `ok`, including the DEC-005 directory-survival
    and surviving-sibling assertions.
  - CHK-019 (`[[file]]` `when` flipped false → status orphaned, apply reaps
    with backup): demonstrated → evidence Scenario 3 captures the pre-edit
    FAILED (`when`-unaware managed set, no reap) versus the post-edit `ok`,
    with the prior bytes located in the run's backup tree.
  No CHK in T-008's scope is `hygiene` or `judgment-only`; all three are
  scriptable and demonstrated.
- Discovered issues: (1) **No apply-time reap existed before this task.** The
  decompose body for T-008 says orphan leaves are "reaped by the next apply
  through the existing removed-entry reap path," but no such path existed:
  `execute` never reconciled the prior commit against the new plan, and
  SPEC-0001 REQ-018 defines ORPHANED as a status-only classification. REQ-007
  ("the next apply removes those orphaned leaf links") and REQ-003/CHK-019
  ("apply removes the target after recording its prior bytes in a backup")
  nonetheless mandate apply-time removal, so this task *implements* the reap
  rather than reusing one. This widened T-008 beyond the literal
  "make the managed-set when-aware" framing; the reviewer should confirm the
  reap belongs in T-008 (it is the only task covering REQ-007). (2)
  **Rollback does not restore a reaped target** — see Undone; the backup is
  taken but not journaled as a reversible op. (3) The reap runs on every full
  `apply`/watch reapply; a steady-state re-apply over unchanged source reaps
  nothing (managed set matches the prior commit), so REQ-021 byte-identical
  stdout / idempotency is preserved (verified: `deterministic_stdout` and the
  `symlink_tree` idempotency tests still pass).
- Procedural compliance: This entry lands directly in `journal/T-008.md` per
  the journal-file schema; no `<implementer-note>` was written into TASKS.md.
  The TASKS.md `state` for T-008 flips `in-progress` → `in-review` as the final
  step of this turn. No shipped `.claude/skills/speccy-*` bodies were edited.
  No `SPEC.md` edit was made (the source-kind/when/schema behaviour is
  unchanged; only the status managed-set and apply reap were touched).
</implementer>

<review date="2026-06-01T22:39:51Z" model="claude-opus-4-8[1m]/high" persona="business" verdict="pass" round="1">
T-008 faithfully delivers every `<done-when>` and `<scenario>` in its assigned requirements (REQ-007 and the REQ-003 reaping leg); both flagged issues are correctly handled.
Issue 1 (reap implemented rather than reused) — NOT scope creep, it is required by REQ-007. REQ-007's title, `<done-when>`, `<behavior>`, and CHK-015/CHK-019 all mandate apply-time removal. T-008 is the only task with `covers="REQ-007"`. The prior managed-set computation only fed `status::report` and was never consumed by `execute` — so the decompose body's "existing removed-entry reap path" was a factual error, not a SPEC limit. Implementing `reap_orphans` is correct and necessary. The `reap = !matches!(policy, LockPolicy::Held(_))` gate correctly reaps on CLI `apply` (Blocking) and watcher reapply (NonBlocking) while sparing `remove`/`promote` (Held).
Issue 2 (rollback does not restore a reaped target) — a genuine gap, but out of T-008's SPEC contract, so not blocking. CHK-019 requires only "removes the target after recording its prior bytes in a backup," which holds (verified). Rollback-of-reap is named in NO `<done-when>`/`<scenario>`/`<behavior>` in SPEC-0004; it touches SPEC-0001's REQ-019, outside this task's `covers`. The implementer flagged it explicitly rather than silently deciding.
Recommendation to record for the human (not a T-008 blocker): the north-star "Rollback fidelity" quality bar is in tension with a non-reversible reap; resolve via a SPEC amendment or follow-up SPEC.
</review>

<review date="2026-06-01T22:39:51Z" model="claude-opus-4-8[1m]/high" persona="tests" verdict="blocking" round="1">
The CHK-019 backup assertion searches the wrong directory on macOS and will hard-fail CI's macOS leg.
`patina-cli/tests/symlink_tree_orphans.rs:234` verifies the reap's backup by recursively scanning `f.state` for the prior bytes:
`let backup = find_backup_with_bytes(&f.state, ".gitconfig", b"[user]\n  name = me\n");`
`f.state` is wired only to `XDG_STATE_HOME` (Linux) and `LOCALAPPDATA` (Windows) by the fixture (`common/mod.rs:89,94`). On macOS the state dir resolves from `HOME`, not those vars (`patina-core/src/state_dir.rs:168-174`: `$HOME/Library/Application Support/patina`). The fixture points `HOME` at `f.home`, not `f.state`, so on macOS the backup tree lives under `f.home/...`, outside the searched `f.state`. `find_backup_with_bytes` returns `None`, and `assert!(backup.is_some())` fails. This violates the cross-platform-parity quality bar, and the single-OS `cargo test` run could not catch it.
Fix: search `f.state_root()` (`common/mod.rs:69-76`, the per-platform resolver) instead of `f.state`. Better still, follow the sibling backup test `symlink_tree.rs:137-156` (CHK-013), which verifies the backup by driving `patina rollback --yes` and asserting the restored bytes — platform-agnostic and end-to-end.
The rest of the suite is sound and adversarial: all three tests drive the real binary via `CARGO_BIN_EXE_patina`; CHK-014 asserts the orphaned deleted-leaf and the `clean` surviving sibling; CHK-015 asserts the orphan removed, `~/d/sub` still a real directory, and `~/d/a.conf` still a symlink; the red/green transition is materially distinct (3 failed → 3 passed). Fix the macOS backup-search root and this passes.
</review>

<review date="2026-06-01T22:39:51Z" model="claude-opus-4-8[1m]/medium" persona="security" verdict="pass" round="1">
Apply-time orphan reap is safe on all three critical axes.
1. DEC-005 (never remove a directory). `reap_orphans` (`patina-core/src/apply/engine.rs:953`) probes each orphan with `fs_err::symlink_metadata` (no symlink follow) and `if meta.is_dir() { continue; }` before any removal, so a real intermediate directory — even one left empty after its last leaf is reaped — is never deleted. A symlink-to-dir is correctly classified as a reapable non-dir leaf. CHK-015 asserts `~/d/sub` survives as a real directory.
2. Consent — only reaps Patina-owned targets. The reap iterates solely `record.targets` from the last committed `ApplyRecord`, never a filesystem walk. A file created outside Patina was never journaled, so it can never be a reap candidate. A recorded target still in the current `when`-aware / leaf-aware managed set is skipped.
3. Backup before removal. `backup_before_overwrite(...)?` precedes `remove_target(...)?` with `?`-propagation, so a failed backup aborts before deletion; the clone is kind-preserving and captures live on-disk bytes. CHK-019 proves the prior bytes land in the run's backup tree.
Non-blocking note (disclosed, out of REQ scope): a reaped target is recoverable from the backup tree but is not replayed by `patina rollback`. This is a recoverability limitation, not a consent/data-loss defect — the bytes are preserved.
</review>

<review date="2026-06-01T22:39:51Z" model="claude-sonnet-4-6[1m]/medium" persona="style" verdict="pass" round="1">
All new function signatures use `&Utf8Path` for path parameters (`insert_managed_targets`, `reap_orphans`, `gate_and_resolve_entry`, `resolve_entry`, `validate_source_kind`) — the `rust-let-types-carry-invariants.md` rule that blocked earlier tasks is satisfied. The `walk_files` promotion to `pub(crate)` follows the same rule's promote-and-call guidance rather than duplicating the walk. Import ordering for `std::collections::BTreeSet` is alphabetically correct under `group_imports = "One"`. The `#![expect(...)]` in `symlink_tree_orphans.rs` carries a `reason` matching the sibling test file pattern. No dead code, no reinvented helpers, no bare suppressions.
Non-blocking aside: `PlanningContext` (engine.rs) uses inline `crate::profile::Resolution` and `crate::discovery::ModuleHandle` paths in its struct fields, while every other struct definition in the file uses `use`-imported names. Both types are `pub` and importable; the pattern is valid Rust and passes fmt and clippy, but it diverges from the file's own established style.
</review>

<review date="2026-06-01T22:39:51Z" model="claude-opus-4-8[1m]/high" persona="correctness" verdict="pass" round="1">
T-008 is correct on all four focus areas and the flagged rollback boundary is acceptable.
- `current_managed_targets` (`engine.rs:444`) walks every `[[file]]`+`[[directory]]` entry, drops `when`-false ones via `Engine::eval_when`, and for `SymlinkTree` expands one `manage_key` per live source leaf using the shared `walk_files`, the same order the T-007 executor uses. The managed key and the recorded canonical leaf reconcile through `manage_key`'s parent-only canonicalization; verified end-to-end.
- A deleted source leaf is absent from `walk_files` → absent from the managed set → `reap_orphans` classifies it orphaned and removes it; a genuine mid-walk I/O error is not silently swallowed into a spurious mass-reap because the executor's `tree_symlink` walk runs first in `execute` and aborts before `reap_orphans`.
- DEC-005 enforced: `reap_orphans` skips any path whose `symlink_metadata().is_dir()`, so an emptied intermediate directory survives. Backup-before-removal upholds REQ-014.
- The reap is gated `!matches!(policy, LockPolicy::Held(_))`; `remove`/`promote`/`managed` drive under `Held` and never reap their own promoted target; `Blocking`/`NonBlocking` correctly do.
- Rollback-of-reap: replays only the COMMIT record's `ExpectedTarget` list; a reaped target is not added, so rollback won't recreate it. Violates no requirement T-008 covers (REQ-007/REQ-003/CHK-019 require status-orphaned + reap + backup, all satisfied; REQ-014 mandates only backup-before-overwrite). An acceptable, transparently-flagged boundary.
No panic-family calls in the new production code; the reap reads the prior commit (commit written after the reap, no double-reap) and tolerates already-gone orphans.
</review>

<review date="2026-06-01T22:39:51Z" model="claude-opus-4-8[1m]/xhigh" persona="architecture" verdict="pass" round="1">
T-008's architecture is sound for its scope. The apply-time reap is correctly placed in `execute`'s commit path inside the exclusive-lock critical section, after post_apply hooks and recover_orphans, before `journal.commit`, and the `let reap = !matches!(policy, LockPolicy::Held(_))` gate is the right layering — it stops `remove`/`promote` from reaping the single target they just surgically re-journaled. `build_planning_context`/`PlanningContext` is a genuine (not premature) abstraction with two real consumers, `plan` and `current_managed_targets`, and it is the SPEC's own correctness mechanism for making status and apply agree on `when`/`symlink-tree` resolution (SPEC.md:140-147); `walk_files` promotion to `pub(crate)` is sanctioned reuse. REQ-007's reap belongs in T-008 — it is the only task covering REQ-007. DEC-005 directory-survival and REQ-021 idempotency are honored.
One real architectural inconsistency, surfaced but not blocking for this task: a reaped target is backed up but NOT recorded as an `ExpectedTarget` in that run's COMMIT record, so `patina rollback` of a reaping apply will not recreate the reaped target — a mutation that escapes the journal's rollback-fidelity contract (REQ-019). The journal's "DEC-009 cautions against a wire-format addition" rationale is a misread: DEC-009 governs only the `entry: u32` index space, and recording one more existing `ExpectedTarget` for a reaped removal bumps no wire format — SPEC.md:135-139 in fact directs reaping to "reuse... the rollback replay path." However, no T-008-scoped requirement mandates rollback-of-reap; that is REQ-019/SPEC-0001 territory, out of this SPEC's requirement set, and was explicitly disclosed. Recommend tracking as a cross-SPEC follow-up.
Minor (non-blocking) coupling note: `reap_orphans` calls `current_managed_targets()`, re-running `build_planning_context` a second time within the same `execute` after `plan()` already resolved it; threading the plan-time managed set into the reap would remove the redundant re-resolution on the commit hot path.
</review>

<blockers date="2026-06-01T22:39:51Z" round="1">
Fix the macOS-only backup-search root in the CHK-019 test before the next review pass.
- tests (blocking): `patina-cli/tests/symlink_tree_orphans.rs:234` — the CHK-019 backup assertion calls `find_backup_with_bytes(&f.state, ...)`, but `f.state` is wired only to `XDG_STATE_HOME`/`LOCALAPPDATA` (Linux/Windows). On macOS the state dir resolves from `$HOME` (`patina-core/src/state_dir.rs:168-174`), so the backup tree lives outside `f.state` and `assert!(backup.is_some())` hard-fails CI's macOS leg. Search `f.state_root()` (the per-platform resolver at `common/mod.rs:69-76`) instead, OR follow the sibling CHK-013 pattern in `symlink_tree.rs:137-156` and verify the backup by driving `patina rollback --yes` and asserting the restored bytes (platform-agnostic, end-to-end). Re-run `just test` to confirm green. (business, security, style, correctness, architecture all passed.)
- NON-BLOCKING follow-up (do NOT act on in this retry; surfaced for the human): business + architecture both flagged that a reaped target is backed up but not journaled as a reversible op, so `patina rollback` won't undo a reap — in tension with the north-star rollback-fidelity bar (REQ-019). It is out of T-008's `covers="REQ-007 REQ-003"` scope and correctly deferred; resolve via a SPEC amendment / follow-up SPEC, not in this task.
</blockers>

<implementer date="2026-06-01T23:05:00Z" model="claude-opus-4-8[1m]/high" round="2">
- Reuse survey: The round-1 blocker is reuse-related (use the existing
  per-platform resolver instead of the raw env value), so I re-scanned the
  test-area neighbours. No new top-level symbol introduced this round.
  - reuse-as-is — `Fixture::state_root()` (`patina-cli/tests/common/mod.rs:69-76`),
    the per-platform state-dir resolver that already drives `XDG_STATE_HOME` /
    `LOCALAPPDATA` on Linux/Windows and `$HOME/Library/Application
    Support/patina` on macOS via `patina_core::state_dir::resolve_with_env`.
    The CHK-019 backup search now roots at this resolved path. The existing
    `find_backup_with_bytes` recursive helper is reused unchanged.
  - extend — none.
  - write-fresh — none. Considered the sibling CHK-013 rollback pattern
    (`symlink_tree.rs:137-156`) the blocker offered as the preferred
    alternative, but rejected it for this test: `patina rollback` replays only
    the COMMIT record's `ExpectedTarget` list, and a *reaped* target is not
    recorded there (round-1 Undone note, confirmed by the correctness +
    architecture reviewers). Driving `rollback --yes` would therefore NOT
    restore a reaped `[[file]]` target, so it cannot stand in as the
    backup-existence proof here. The `state_root()` swap is both the cleaner
    and the only correct fix; it keeps the test proving exactly what CHK-019
    requires (prior bytes present in the run's backup tree) and is now
    platform-agnostic.
- Completed: Addressed the single round-1 tests blocker. In
  `patina-cli/tests/symlink_tree_orphans.rs`
  (`when_flipped_to_false_orphans_then_reaps_target_with_backup`, the CHK-019
  test), the backup-tree search now roots at `f.state_root()` (the
  per-platform resolver) instead of the raw `f.state` env value. On macOS the
  state dir resolves from `$HOME`, so the prior search root was outside the
  backup tree and `assert!(backup.is_some())` would have hard-failed CI's
  macOS leg; the resolver returns the correct root on all three platforms.
  Updated the surrounding comment to explain the per-platform divergence and
  inlined the assertion's interpolated `{state_root}` for the now-local
  binding. No production code touched; no other test touched.
- Undone: Unchanged from round 1. The non-blocking rollback-of-reap follow-up
  (a reaped target is backed up but not journaled as a reversible
  `ExpectedTarget`, so `patina rollback` won't recreate it) remains out of
  T-008's `covers="REQ-007 REQ-003"` scope and is deferred to a SPEC amendment
  / follow-up SPEC per the round-1 blockers directive. I did not act on it.
- Hygiene checks: `cargo test --workspace --locked` exited 0 (via `just test`;
  the `symlink_tree_orphans` suite's 3 tests pass, including the amended
  CHK-019 test). `cargo clippy --workspace --all-targets --all-features
  --locked -- -D warnings` exited 0 (via `just lint`, including the
  `x86_64-unknown-linux-gnu` and `x86_64-pc-windows-gnu` cross targets).
  `cargo +nightly fmt --all --check` exited 0 (via `just lint`). `cargo deny
  check` exited 0 ("advisories ok, bans ok, licenses ok, sources ok"; the
  pre-existing license-not-encountered / duplicate-crate lines are
  advisory-level warnings that do not fail the gate and are unrelated to this
  change). `just lint` exited 0 overall (fmt, clippy, docs, deny).
- Evidence: red-then-green paper trail at
  `.speccy/specs/0004-patina-conditional-entries/evidence/T-008.md` (round-1
  trail unchanged; the round-2 fix is a test-search-root correction, not a
  behaviour change). Roll call for the CHKs under REQ-007 / REQ-003:
  - CHK-014 (deleted symlink-tree source leaf reported orphaned by status):
    demonstrated → `deleted_symlink_tree_source_leaf_is_reported_orphaned` in
    `patina-cli/tests/symlink_tree_orphans.rs`, unchanged this round, passes.
  - CHK-015 (next apply reaps the orphan leaf, keeps sibling + directory):
    demonstrated → `next_apply_reaps_orphan_leaf_and_keeps_sibling_and_directory`
    in the same file, unchanged this round, passes.
  - CHK-019 (`[[file]]` `when` flipped false → status orphaned, apply reaps
    with backup): demonstrated → `when_flipped_to_false_orphans_then_reaps_target_with_backup`
    in the same file; the backup-existence assertion now searches
    `f.state_root()` so it holds on macOS as well as Linux/Windows.
  No CHK in T-008's scope is `hygiene` or `judgment-only`.
- Discovered issues: None new. The round-1 discovered issues stand as recorded
  (no apply-time reap existed before this task; rollback does not restore a
  reaped target; steady-state re-apply reaps nothing so REQ-021 idempotency is
  preserved).
- Procedural compliance: This round-2 entry is appended at the end of
  `journal/T-008.md`, after the round-1 `<implementer>`, the review blocks, and
  the round-1 `<blockers>`, per the append-only journal schema; no earlier
  block was modified and no `<implementer-note>` was written into TASKS.md. The
  TASKS.md `state` for T-008 flips `pending` → `in-progress` → `in-review`
  across this turn. No shipped `.claude/skills/speccy-*` body was edited. No
  `SPEC.md` edit was made.
</implementer>

<review date="2026-06-01T22:46:47Z" model="claude-opus-4-8[1m]/high" persona="business" verdict="pass" round="2">
Round-2 change is test-only and introduces no scope or behavior regression; T-008 still satisfies every `<done-when>` and `<scenario>` for REQ-007 and the REQ-003 reaping leg. The sole edit is in `patina-cli/tests/symlink_tree_orphans.rs` (CHK-019): the backup-tree search root moved from the raw `f.state` env value to `f.state_root()`, the per-platform resolver, which routes through `patina_core::state_dir::resolve_with_env` exactly as the subprocess does — `XDG_STATE_HOME`/`LOCALAPPDATA` from `self.state` on Linux/Windows and `HOME` (→ `$HOME/Library/Application Support/patina`) on macOS. This is the precise fix the round-1 tests blocker directed, advancing the cross-platform-parity north-star bar. No production code changed; CHK-014 and CHK-015 tests are byte-identical to round 1. The implementer correctly rejected the offered `rollback --yes` alternative — a reaped target is not journaled as an `ExpectedTarget`, so rollback could not prove backup existence. The known rollback-of-reap limitation remains out of T-008's `covers` scope and deferred per the round-1 directive; it was not silently resolved.
</review>

<review date="2026-06-01T22:46:47Z" model="claude-opus-4-8[1m]/high" persona="tests" verdict="pass" round="2">
Round-1 blocker is genuinely fixed. `patina-cli/tests/symlink_tree_orphans.rs:239-240` now roots the CHK-019 backup search at `f.state_root()` instead of the raw `f.state` env value. I verified the resolver equivalence end-to-end: the subprocess locates its backup tree via `crate::state_dir::resolve` = `resolve_with_env(HostOs::current(), <process env>)`, and the fixture's `state_root()` (`common/mod.rs:69-76`) calls `resolve_with_env(HostOs::current(), closure)` feeding `HOME|USERPROFILE → self.home` and `XDG_STATE_HOME|LOCALAPPDATA → self.state` — the same values the subprocess sets in `run`. Both funnel through `compute_root`, which on macOS ignores `XDG_STATE_HOME`/`LOCALAPPDATA` and builds `$HOME/Library/Application Support/patina` from `HOME` alone. So on all three OSes the searched root is byte-identical to where the reap's `backup_before_overwrite` wrote, and `assert!(backup.is_some())` now holds on the macOS leg that round-1 hard-failed. CHK-014/CHK-015 never touch `f.state`, operate on `f.home` paths and `status --json`, and normalize backslashes for Windows. All three tests drive the real binary; the red→green transition is materially distinct; I re-ran the scoped suite on this Windows host: 3 passed, 0 failed.
</review>

<review date="2026-06-01T22:46:47Z" model="claude-opus-4-8[1m]/medium" persona="security" verdict="pass" round="2">
Round-2 change is test-only and the production reap path is unchanged and still sound. The only round-2 edit is in `patina-cli/tests/symlink_tree_orphans.rs:239-244`: the CHK-019 backup search now roots at the per-platform resolver `f.state_root()` instead of the raw `f.state` env value. This strengthens the REQ-014 backup-before-removal assertion to hold on macOS (state dir resolves from `$HOME`) without weakening anything. No production code touched this round. DEC-005 directory guard intact (`reap_orphans`: `fs_err::symlink_metadata` no-follow + `if meta.is_dir() { continue; }`). Consent intact: reap candidates come only from `read_latest_commit`'s `record.targets` and skip anything still in the `when`/symlink-tree-aware `current_managed_targets()`; the `LockPolicy::Held` gate still spares `remove`/`promote`. Backup-before-removal intact (`backup_before_overwrite(...)?` precedes `remove_target(...)?`). No new dependency, no secret/log exposure. The previously-disclosed rollback-of-reap recoverability gap remains out of T-008 scope and non-blocking.
</review>

<review date="2026-06-01T22:46:47Z" model="claude-sonnet-4-6[1m]/medium" persona="style" verdict="pass" round="2">
The round-2 change is a single clean swap: `f.state` → `f.state_root()` at `patina-cli/tests/symlink_tree_orphans.rs:240`, with an explanatory comment added at lines 234-238. No new symbols, no imports, no production code touched. The `state_root` local binding is consumed at both use sites (the `find_backup_with_bytes` call and the assertion interpolation), so no dead variable. The `find_backup_with_bytes` signature takes `&camino::Utf8Path`, which `Utf8PathBuf` derefs to correctly. The added comment accurately describes the per-platform state-dir divergence. No style issues introduced.
</review>

<review date="2026-06-01T22:46:47Z" model="claude-opus-4-8[1m]/high" persona="correctness" verdict="pass" round="2">
Round-2 change is test-only and the round-1 correctness verdict holds. The only modification is the CHK-019 backup-search root in `patina-cli/tests/symlink_tree_orphans.rs:239-240`, now rooted at the per-platform resolver `f.state_root()` instead of the raw `f.state` env value — a test correctness fix, not a production change. The three production invariants are unchanged and intact: the `when`-aware + symlink-tree-leaf-aware managed set (`current_managed_targets`, engine.rs:444, shared by `plan` and the reap and reconciled via `manage_key`), the DEC-005 directory-survival guard in `reap_orphans` (engine.rs:1002, `if meta.is_dir() { continue; }`, with `backup_before_overwrite` preceding `remove_target` under `?` at engine.rs:1008-1009), and the `LockPolicy::Held` reap gate (engine.rs:751, gating the commit-path `reap_orphans` call after post-apply hooks succeed and before commit). No new logic/control-flow, Option/Result, boundary, concurrency, or resource-leak defect introduced or revived.
</review>
