---
spec: SPEC-0004
task: T-010
generated_at: 2026-06-01T23:55:00Z
---

<implementer date="2026-06-01T23:55:00Z" model="claude-opus-4-8[1m]/high" round="1">
- Reuse survey: Mapped the task area (covered REQ-008; suggested files
  `patina-cli/src/cli.rs`, `patina-cli/src/cmd/add.rs`,
  `patina-cli/tests/add_directory.rs`; plus the T-001 mode model in
  `patina-core/src/config/file_entry.rs` and the writer in
  `patina-core/src/config/writer.rs`). Decisions: reuse-as-is —
  `append_file_entry` (`writer.rs:142`) for the `[[file]]` path unchanged;
  `FileMode` (re-exported from `lib.rs`); `expand_tilde`,
  `resolve_repository_root`, `resolve_state_dir`, `acquire_lock`,
  `discover_modules`, `parse_module_config` (existing `add` plumbing);
  `EngineError::from` (already covers `ConfigWriteError`, so the two new
  writer error variants surface through the same `From`); the `Fixture`
  harness (`patina-cli/tests/common/mod.rs`). extend — `find_managed`
  (`add.rs`) now chains `config.files` and `config.directories` so a
  directory target already managed is also detected; `AddArgs` gains a
  `--symlink-tree` flag in the existing clap `mode` group; the
  `ModeFlag`→`AddMode` resolution replaces the old three-mode `AddMode` /
  `file_mode` mapping with a kind-aware model. write-fresh —
  `append_directory_entry` in `writer.rs` (the minimal T-011 surface;
  searched `rg "fn append_directory"` across the crate — only the TASKS.md
  text referenced it, no existing fn); `detect_source_kind`; the
  `SourceKind` / `ModeFlag` / kind-aware `AddMode` enums;
  `copy_dir_recursive` (the recursive directory stager). New top-level
  symbols and why each could not reuse an existing one:
  `append_directory_entry` — mirror of `append_file_entry` for the second
  table-array; it deliberately does *not* call `mode_manifest_str` (which
  collapses file-only modes for the file table) because a `[[directory]]`
  writer must *reject* a file-only `FileMode` rather than silently map it.
  `copy_dir_recursive` — `patina-core`'s `fsx::copy_tree` (`fsx.rs:104`)
  and `clone_entry` are `pub(crate)` to patina-core and unreachable from
  patina-cli; exposing a new public engine API for a CLI staging detail
  would be scope creep, so a minimal recursive copy lives in `cmd/add.rs`.
  `detect_source_kind` / `SourceKind` / `ModeFlag` — net-new because the
  pre-split `add` had no notion of source kind at all.
- Completed: REQ-008 in full. `patina add` detects the source kind from the
  tilde-expanded target's filesystem metadata, validates the selected mode
  flag against it (`--symlink-tree` directory-only, `--template` file-only,
  `--symlink`/`--copy` either kind) before the lock or any mutation, stages
  the source (recursive copy for a directory, byte copy for a file), and
  writes the matching table-array (`append_directory_entry` for a directory,
  `append_file_entry` for a file). Added `--symlink-tree` to `AddArgs`. Added
  the `append_directory_entry` writer surface to `patina-core` (the minimal
  T-011 dependency, since T-011 has not landed) with `MalformedDirectoryArray`
  and `ModeNotDirectoryValid` error variants, re-exported from
  `config/mod.rs` and `lib.rs`. CHK-016 and CHK-017 demonstrated by new CLI
  integration tests in `patina-cli/tests/add_directory.rs`.
- Undone: nothing in REQ-008's scope. T-011 still owns the *full*
  format-preserving directory writer surface (e.g. `remove_directory_entry`,
  the file-writer mode-name update for the collapsed taxonomy, and the
  writer's own round-trip test refresh per its task body). T-010 added only
  the `append_directory_entry` slice `add` needs; T-011's broader writer work
  remains its own task. T-012 owns the docs refresh.
- Hygiene checks: ran the four standard gates via `just lint` + `just test`
  (CI's exact gate set and order); all exit 0:
  - `cargo test --workspace --locked` → exit 0
  - `cargo clippy --workspace --all-targets --all-features --locked -- -D warnings`
    (linux-gnu + windows-gnu cross targets) → exit 0
  - `cargo +nightly fmt --all --check` → exit 0
  - `cargo deny check` → exit 0 (advisories ok, bans ok, licenses ok, sources ok)
- Evidence: paper trail at
  `.speccy/specs/0004-patina-conditional-entries/evidence/T-010.md`.
  Roll call for CHKs under REQ-008:
  - CHK-016 (file source → `[[file]]`, no `[[directory]]`): demonstrated →
    `add_file_writes_file_table_and_no_directory_table` in
    `patina-cli/tests/add_directory.rs`; the distinct file/directory routing
    it depends on is proven red/green by CHK-017's pair (forcing the writer
    dispatch to the file branch makes the directory test fail).
  - CHK-017 (directory `--symlink-tree` → `[[directory]]`
    `mode = "symlink-tree"`): demonstrated →
    `add_directory_symlink_tree_writes_directory_table_with_mode` in
    `patina-cli/tests/add_directory.rs`; red baseline captured by forcing the
    `[[file]]` branch (test panics "no [[file]] entry may be written for a
    directory source"), green with source-kind dispatch restored.
- Discovered issues: none requiring follow-up. One behaviour change worth
  flagging for review: a missing source now fails at `detect_source_kind`
  (a read-only metadata probe before the lock) with "the source does not
  exist or is unreadable", rather than later at the staging `fs_err::copy`.
  This is a strictly clearer error on the same exit path and does not move
  any mutation before the lock.
- Procedural compliance: did not edit the locked `SPEC.md`; did not hand-edit
  `TASKS.md` beyond the single `state` attribute flip (pending → in-review);
  the journal is append-only (this is the only block); no task was marked
  `completed` (the review gate owns that). The `[[directory]]` writer added
  here is the minimal surface T-010 needs given T-011 has not landed, as the
  caller directed — it does not pre-empt T-011's broader writer scope.
</implementer>

<review date="2026-06-01T23:21:41Z" model="claude-opus-4-8[1m]/high" persona="business" verdict="pass" round="1">
REQ-008 is fully delivered and both CHK-016 and CHK-017 map to real, behavior-gating tests. Mapping verified against every `<done-when>` bullet: `patina add <file>` → `[[file]]`, mode defaults to `symlink`, `--copy` → file `copy`; `patina add <dir>` → `[[directory]]` mode `symlink` (serialized as `symlink`, not the removed `symlink-dir`); `--symlink-tree` on a dir → `mode = "symlink-tree"` (CHK-017); `--copy` on a dir → recursive `copy`; `--symlink-tree` on a file and `--template` on a dir rejected with a typed error naming flag + kind, before any mutation. The "directory never emits `[[file]]` and vice versa" invariant is triple-guarded: `AddMode::resolve` makes the `(kind,flag)` pairing the sole constructor, `is_directory()` dispatch picks the writer, and `append_directory_entry` rejects a file-only `FileMode` (`ModeNotDirectoryValid`). The CHK-017 red baseline (forcing the `[[file]]` branch) proves the dispatch is load-bearing. No non-goal scope crept in; the added `append_directory_entry` is the minimal T-011 surface the task body explicitly authorized. One minor non-blocking note: the no-flag directory-default-symlink `<behavior>` is covered at unit level (the non-TTY integration shell requires an explicit flag) — an acceptable coverage split.
</review>

<review date="2026-06-01T23:21:41Z" model="claude-opus-4-8[1m]/high" persona="tests" verdict="pass" round="1">
T-010 tests genuinely exercise REQ-008's source-kind-aware routing; CHK-016 and CHK-017 are both demonstrated against the real `patina` binary.
- CHK-016 `add_file_writes_file_table_and_no_directory_table` spawns the real `add` via the subprocess `Fixture` (`CARGO_BIN_EXE_patina`) — no mock — and asserts presence of `[[file]]`, absence of `[[directory]]`, and `mode = "symlink"`. Catches the file-routing regression on its own.
- CHK-017 `add_directory_symlink_tree_writes_directory_table_with_mode` asserts `parsed.get("file").is_none()`, exactly one `[[directory]]` entry with `mode = "symlink-tree"`, the stored source/target, and recursive staging. The evidence red/green pair is scoped per-test, materially different, and the named test + panic string both appear in the diff.
- Both rejection legs assert the *specific* contract: `add_symlink_tree_on_a_file_is_rejected` asserts exit 1, `stderr.contains("--symlink-tree") && stderr.contains("file")`, and no manifest written; `add_template_on_a_directory_is_rejected` asserts exit 1 and `--template`+`directory`. The kind check runs before the lock, so the no-mutation assertion is real.
- Unit tests converted to the kind-aware model: `file_mode_maps_each_variant` replaced by `add_mode_resolves_each_compatible_flag_kind_pair` (full 6-cell matrix) plus two rejection unit tests; `resolve_mode_*` migrated to `resolve_mode_flag_*`. No stale references remain. Roll call covers CHK-016/CHK-017 as `demonstrated`.
</review>

<review date="2026-06-01T23:21:41Z" model="claude-opus-4-8[1m]/high" persona="security" verdict="pass" round="1">
No security blockers. Kind detection and the kind/flag mismatch rejection (`add.rs:236-237`) run before the lock (`add.rs:242`) and before any mutation; the refusal path writes no manifest (verified by `add_symlink_tree_on_a_file_is_rejected`). `copy_dir_recursive` (`add.rs:382`) recurses only on real directories — `DirEntry::file_type` does not follow symlinks, so symlink cycles cannot cause unbounded recursion; in-tree links are copied as target bytes by design. Staged source names derive only from the path's final component with one leading dot stripped, so no `..`/separator traversal reaches the destination. The detect-then-stage TOCTOU exists but crosses no privilege boundary (single-user CLI on the user's own files), so it is out of threat model. No auth boundaries, secrets, crypto, or new dependencies are touched.
</review>

<review date="2026-06-01T23:21:41Z" model="claude-sonnet-4-6[1m]/medium" persona="style" verdict="pass" round="1">
All conventions from AGENTS.md, `rust-let-types-carry-invariants.md`, and `rust-code-quality.md` are met. `&Utf8Path` (not `&Utf8PathBuf`) is used consistently on all new helper signatures (`stage_into_repo`, `copy_dir_recursive`, `detect_source_kind`, `has_tmpl_suffix`); no `unwrap()` or panics in production code; `copy_dir_recursive` is justified as non-duplicative (`fsx::copy_tree` is `pub(crate)` to `patina-core` and unreachable from `patina-cli`); new error variants carry doc comments and properly-formatted `#[error]` messages; `append_directory_entry` mirrors `append_file_entry`'s signature shape; reuse survey is present and honest.
One minor non-blocking note: the three new writer tests in `patina-core/src/config/writer.rs` each carry a per-function `use crate::config::EntryKind;` inside the test body; the existing `mod tests` block uses module-level imports. Hoisting it would match the established pattern, but this does not rise to blocking.
</review>

<review date="2026-06-01T23:21:41Z" model="claude-opus-4-8[1m]/high" persona="correctness" verdict="pass" round="1">
T-010 passes correctness review. All four focus areas are sound:
(1) Source-kind detection runs before staging and before the lock. `detect_source_kind` (`add.rs:435`) is called at line 236, ahead of `acquire_lock` (242) and `stage_into_repo` (318); a missing/unreadable source returns a typed error before any mutation.
(2) `AddMode::resolve` constructs only kind-legal pairs, and `file_mode()` maps File* variants to file modes and Directory* to directory modes with no crossover. `is_directory()` routes to `append_directory_entry` vs `append_file_entry`; `append_directory_entry` independently rejects file-only modes via `ModeNotDirectoryValid`. A directory source cannot emit `[[file]]` and vice versa.
(3) The incompatible pairs `(File, SymlinkTree)` and `(Directory, Template)` return `Err` from `resolve`, propagated via `?` at `add.rs:237` before the lock — confirmed no manifest is written.
(4) `find_managed` (`add.rs:368`) now iterates `config.files.iter().chain(config.directories.iter())`, so a re-add against an existing directory entry is detected.
Non-blocking observation (below the reporting bar): a symlink-to-directory nested inside a staged tree would hit `fs_err::copy` and surface a typed staging error rather than recursing — a bounded edge case that fails loudly, not silent corruption, and matches the implementer's stated link-following scope.
</review>
