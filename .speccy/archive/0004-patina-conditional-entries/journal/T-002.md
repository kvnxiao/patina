---
spec: SPEC-0004
task: T-002
generated_at: 2026-06-01T21:05:38Z
---

<implementer date="2026-06-01T21:05:38Z" model="claude-opus-4-8[1m]/high" round="1">
- Reuse survey: Mapped the task area (covered REQ-009 / REQ-001 / DEC-009;
  suggested file `patina-core/src/apply/engine.rs`; plus the immediate
  consumers the index space flows into — `ResolvedOperation` /
  `ResolvedPlan` in `engine.rs`, `execute`'s completion loop in the same
  file, and `ExpectedTarget.entry` in `journal/record.rs`). Decisions:
  reuse-as-is — kept the durable `PlannedOperation::symlink/copy/render`
  constructors and the existing `planned_operation` mode→op mapping (a
  `[[directory]]` `symlink`/`SymlinkDir` reuses the same symlink op shape
  as a `[[file]]` `symlink`); reused `canonicalize` + `expand_tilde` for
  source/target resolution unchanged; reused the existing per-target
  fan-out and `materialize` call in `execute` — only the index *source*
  changed there, not the loop shape; reused `ExpectedTarget.entry` /
  `build_apply_record` and the `entry: u32` postcard wire format
  unchanged (no version bump — DEC-009). extend — extended
  `ResolvedOperation` with one `entry_index: u32` field; extended the
  `commit_record.rs` integration fixture with one new test rather than a
  new test file (the fixture already isolates repo/state/home and decodes
  the COMMIT). write-fresh — `assemble_plan_operations` (the
  files-then-directories chain + monotonic index assignment),
  `resolve_entry` (per-entry source/target canonicalization, factored out
  of the old inline file-only loop), and the private `ResolvedEntry`
  bucket struct. Searched `patina-core` for an existing
  files-then-directories ordering helper or a plan-time entry-index
  assigner and found none — the prior index was re-derived from
  `enumerate()` at execute time, not assigned at plan time. New top-level
  symbols: `assemble_plan_operations`, `resolve_entry`, `ResolvedEntry`,
  and the `ResolvedOperation.entry_index` field — each justified above.
- Completed: Reworked the per-module entry loop in `apply::plan`
  (`patina-core/src/apply/engine.rs`) to consume both `config.files` and
  `config.directories` (the T-001 buckets) and emit them in one
  deterministic order — every `[[file]]` entry across all modules in
  declaration order, then every `[[directory]]` entry — via two ordered
  buckets fed into a new `assemble_plan_operations`. That fn chains the
  file bucket then the directory bucket, assigns each managed entry a
  single monotonic `u32` `entry_index` (files first, then directories),
  and carries it on a new `ResolvedOperation.entry_index` field.
  Factored each entry's source/target canonicalization into
  `resolve_entry` so the file and directory paths resolve identically.
  Updated `execute` to record `op.entry_index` (the plan-time index)
  rather than re-deriving an index from `enumerate()` over
  `resolved.operations`, so a `[[file]]` and a `[[directory]]` entry can
  never collide on an index and targets sharing a declared entry stay one
  atomic rollback unit (REQ-019 / DEC-009). `planned_operation` maps the
  `[[directory]]` `symlink` default (`SymlinkDir`) and `copy`
  (`CopyTree`) to the existing atomic op shapes and leaves `SymlinkTree`
  on the same symlink op shape as a clearly-marked dispatch point for
  T-007. Updated the stale `planned_operation` doc comment (it previously
  said T-002 emits no directory entries — it now does). The `entry: u32`
  journal wire format and version envelope are unchanged. Added two
  engine-level unit tests and one CLI integration test (see Evidence);
  updated the `windows/mod.rs` test `op` helper for the new field.
- Undone: `when` evaluation and the `when`-before-canonicalize gate
  (T-005); the plan-time source existence/kind check (T-006, which is
  what makes CHK-022 satisfiable); the real `symlink-tree` per-leaf
  executor (T-007) — its dispatch point stays marked, so a `[[directory]]`
  `symlink-tree` plans (via the symlink op shape) but still errors
  `SymlinkTreeNotImplemented` at materialize, unchanged by this task. All
  are separate tasks per TASKS.md. The REQ-009 byte-identical-second-run
  parity (CHK-007) rides on T-005's `when`-gated CLI test; this task adds
  no `when`-gated entries.
- Hygiene checks:
  - `just test` (= `cargo test --workspace --locked`): exit 0 — full
    workspace suite passes, including the two new engine unit tests and
    the new `commit_record` integration test.
  - `just lint` (nightly `cargo fmt --all --check`; `cargo clippy
    --workspace --all-targets --all-features --locked -- -D warnings`
    cross-compiled to `x86_64-unknown-linux-gnu` and
    `x86_64-pc-windows-gnu`, macOS leg skipped off a Mac with CI as
    backstop; `RUSTDOCFLAGS="-D warnings" cargo doc`; `cargo deny
    check`): exit 0. `cargo deny` reports advisories/bans/licenses/sources
    ok (the `wit-bindgen`/`zvariant` duplicate-crate lines are
    pre-existing transitive warnings, not failures; no new dependency
    added — the change is `std` + existing crates).
- Evidence: paper trail at
  `.speccy/specs/0004-patina-conditional-entries/evidence/T-002.md`.
  T-002 covers REQ-009 and (transitively) REQ-001. Roll call:
  - REQ-009 ordering `<done-when>` (files before directories, each in
    declaration order): demonstrated → evidence Scenario 1 covers the
    pre-edit no-directory-emission gap vs the post-edit
    `files_are_planned_before_directories_in_declaration_order` unit test
    asserting the `["/repo/f0","/repo/f1","/repo/d0"]` source order.
  - REQ-009 index `<done-when>` (single monotonic space across both
    tables; no file/directory index collision): demonstrated → evidence
    Scenario 2 covers the pre-edit execute-time `enumerate()` index vs the
    post-edit `entry_indices_are_a_single_monotonic_space_across_both_tables`
    unit test (`[0,1,2,3]`, disjoint file/dir ranges) and the
    `commit_record.rs` integration test
    `directory_and_file_entries_get_distinct_indices_and_envelope_major_is_unchanged`
    (distinct file indices, shared directory-leaf index, no collision,
    files precede directories).
  - DEC-009 version-envelope unchanged: demonstrated → evidence Scenario 3
    (same integration test) asserts the `<ts>.COMMIT` envelope major is
    still `2`, no version bump.
  - CHK-022 (the only `<scenario>` CHK under REQ-009): judgment-only /
    deferred — it requires the `when` gate (T-005) and the plan-time
    source kind/existence check (T-006), neither of which exists yet, so
    it is explicitly out of T-002's scope and lands with T-006. No
    scriptable proof is owed by this task; flagged so reviewer-tests does
    not expect it green here.
  REQ-001's parse-layer CHKs (CHK-001/002/003) were demonstrated by T-001
  and are not re-proved here; T-002 consumes the T-001 schema unchanged.
- Discovered issues: none new. The `apply/mod.rs` line-235 executor
  comment still reads "the engine never plans a `SymlinkTree` operation
  (the plan loop in T-002 leaves its dispatch point marked)" — after this
  task the plan loop *does* emit a `SymlinkTree` op (mapped to the symlink
  op shape) for a `[[directory]]` `symlink-tree`, which then hits the
  `SymlinkTreeNotImplemented` materialize arm. The comment's intent (the
  real per-leaf executor is unimplemented until T-007) remains accurate;
  the "never plans" phrasing is now slightly stale and is squarely T-007's
  to update when it lands the executor. Left untouched to keep this task
  surgical and avoid editing an unrelated module's prose.
- Procedural compliance: This implementer entry creates and lands directly
  in `journal/T-002.md` per the journal-file schema (round-1 creates the
  file with three-field frontmatter); no `<implementer-note>` was written
  into TASKS.md. The TASKS.md `state="..."` attribute for T-002 was
  flipped `pending` → `in-progress` at the start of this turn and
  `in-progress` → `in-review` after both hygiene gates passed. No shipped
  `.claude/skills/speccy-*` bodies required edits during this task.
</implementer>

<review date="2026-06-01T21:09:16Z" model="claude-opus-4-8[1m]/high" persona="business" verdict="pass" round="1">
T-002 delivers exactly what REQ-009's in-scope `<done-when>` names: files-then-directories emission order and a single monotonic `entry_index` space with no `[[file]]`/`[[directory]]` collision (`patina-core/src/apply/engine.rs` `assemble_plan_operations`), with the index now plan-assigned and carried through `execute` into the COMMIT record's per-entry grouping rather than re-derived from operation position. The `op_index` progress cursor stays correctly distinct from the rollback-grouping `entry_index`.
Scope is clean: no `when` evaluation, no source-kind/existence check, and no `symlink-tree` executor leaked in — all three are explicitly deferred to T-005/T-006/T-007 per the task body and journal. The only `<scenario>` under REQ-009, CHK-022, is genuinely unsatisfiable without the `when` gate (T-005) and plan-time kind check (T-006); the implementer flags this deferral non-silently and the decomposition assigns CHK-022's proof to T-006. No Open Questions existed to silently resolve.
Verified the three new tests pass and gate real invariants: the `commit_record.rs` integration test asserts a directory `copy` entry's two leaves share one `entry_index` while the two file entries get distinct earlier indices and the envelope major stays `2` (DEC-009 no-version-bump) — exercising actual cross-table grouping behavior, not a tautology.
</review>

<review date="2026-06-01T21:09:16Z" model="claude-opus-4-8[1m]/high" persona="tests" verdict="pass" round="1">
T-002 tests genuinely exercise the task's three `<task-scenarios>`. The two engine unit tests drive the real production `assemble_plan_operations` (asserting files-then-directories source order and a gapless monotonic index space `[0,1,2,3]` with disjoint file/dir ranges), and `directory_and_file_entries_get_distinct_indices_and_envelope_major_is_unchanged` (`patina-cli/tests/commit_record.rs`) drives a real `patina apply --yes` over a copy-mode `[[directory]]` (a fully-implemented `copy_tree` materialize path, not the unimplemented `symlink-tree`), then decodes the COMMIT and asserts distinct file indices, one shared index across the directory's two leaves, no file/directory collision (`a != d_one`, `a < d_one`), and envelope major still `2`. No mocking of the SUT. Mutation checks hold: chain-reversal fails Scenario 1, a per-bucket index reset or dropped increment fails Scenarios 2-3. Evidence field present; both scoped commands reproduce byte-for-byte against the working tree, test names all appear in the diff, no fabrication pattern matched. Roll call is complete: CHK-001/002/003 are parse-layer scenarios proven in completed T-001, and CHK-022's judgment-only/deferred label is correct — T-002's `apply::plan` contains no entry-level `when` gate.
</review>

<review date="2026-06-01T21:09:16Z" model="claude-opus-4-8[1m]/high" persona="security" verdict="pass" round="1">
T-002 is a plan-time ordering/indexing refactor with no security-relevant surface change. `resolve_entry` (`patina-core/src/apply/engine.rs:386-405`) relocates the existing source/target canonicalization verbatim — same `canonicalize` + `expand_tilde` path handling, no new traversal vector. The journal `entry: u32` wire format and version envelope are unchanged (DEC-009; integration test asserts COMMIT major stays `2`). No authn/authz boundary, secrets, injection vector, sensitive-data logging, crypto parameter, or new dependency is touched. The lone index-overflow path (`entry_index.saturating_add(1)`) could silently collapse two entries into one rollback unit only past `u32::MAX` declared entries — but the input is the user's own local `patina.toml` with no trust boundary crossed, and it mirrors the pre-existing `op_index` pattern; not a security defect. Correctly defers the `when` gate and source-kind check (T-005/T-006) rather than faking CHK-022.
</review>

<review date="2026-06-01T21:09:16Z" model="claude-sonnet-4-6[1m]/medium" persona="style" verdict="blocking" round="1">
`patina-core/src/apply/engine.rs:363` uses `crate::config::ManagedEntry` as an inline path in the `resolve_entry` function signature rather than importing it at the top of the file. Every other `crate::config::*` symbol in this file — `FileMode`, `HookEntry`, `HookEvent`, `parse_module_config` — appears as a distinct `use crate::config::*` statement in the import block at lines 35–38. The inline path reference breaks that established pattern.
Fix: add `use crate::config::ManagedEntry;` to the import block alongside the other `crate::config::*` uses, then change the function signature parameter type from `entry: &crate::config::ManagedEntry` to `entry: &ManagedEntry`.
</review>

<review date="2026-06-01T21:09:16Z" model="claude-opus-4-8[1m]/high" persona="correctness" verdict="pass" round="1">
T-002 correctly establishes the single monotonic entry-index space. `assemble_plan_operations` (`patina-core/src/apply/engine.rs:339-355`) increments one `u32` counter once per managed entry over the `file_entries.chain(directory_entries)` sequence, so indices are gapless 0..N, files strictly precede directories, and no `[[file]]`/`[[directory]]` entry can share an index. `execute` (`engine.rs:510`) records the plan-assigned `op.entry_index` rather than re-deriving it from `enumerate()`, and the per-entry loop fixes the index across all records a multi-leaf entry's `materialize` emits, so targets of one declared entry share its index — the property atomic per-entry rollback depends on. Verified by the two engine unit tests and the `commit_record` integration test, all passing locally; the `SymlinkTree` placeholder still errors `SymlinkTreeNotImplemented` at materialize so the symlink-op-shape mapping introduces no silent mis-materialization. The `saturating_add` collision edge is unreachable (>4e9 entries). The stale "never plans a `SymlinkTree`" comment at `apply/mod.rs:231-234` is a docs/style nit outside the correctness lane and was already disclosed. No correctness defect at or above the reporting bar.
</review>

<blockers date="2026-06-01T21:09:16Z" round="1">
Fix the inline `crate::config::` path that breaks the file's import convention before the next review pass.
- style (blocking): `patina-core/src/apply/engine.rs:363` — `resolve_entry` uses inline `crate::config::ManagedEntry` instead of a top-of-file `use`. Add `use crate::config::ManagedEntry;` to the import block (lines 35–38 alongside `FileMode`/`HookEntry`/`HookEvent`/`parse_module_config`) and change the parameter type to `entry: &ManagedEntry`. (business, tests, security, correctness all passed.)
</blockers>

<implementer date="2026-06-01T21:30:00Z" model="claude-opus-4-8[1m]/high" round="2">
- Reuse survey: No full area-map re-run — the round-1 blocker is a
  pure import-convention style fix, not a reuse-related blocker, and
  this round introduces no new top-level symbol (per-symbol floor
  not triggered). The fix references the existing
  `crate::config::ManagedEntry` re-export
  (`patina-core/src/config/mod.rs:29` `pub use file_entry::ManagedEntry;`)
  already used throughout `config` — reuse-as-is, nothing new added.
- Completed: Addressed the round-1 style blocker. Added
  `use crate::config::ManagedEntry;` to the top-of-file import block in
  `patina-core/src/apply/engine.rs`, placed in alphabetical order after
  `use crate::config::HookEvent;` and before
  `use crate::config::parse_module_config;` (matching the file's
  existing sorted `crate::config::*` import grouping so the rustfmt
  import-ordering check stays green), and changed the `resolve_entry`
  signature parameter from `entry: &crate::config::ManagedEntry` to
  `entry: &ManagedEntry`. No behavioural change: the inline path and the
  imported name resolve to the same type. The round-1 implementation
  (ordering + monotonic entry-index space) is otherwise untouched.
- Undone: Nothing beyond the round-1 deferrals (T-005 `when` gate,
  T-006 source kind/existence check, T-007 `symlink-tree` executor),
  which remain out of scope for T-002 exactly as in round 1.
- Hygiene checks:
  - `just lint` (nightly `cargo +nightly fmt --all --check`; `cargo
    clippy --workspace --all-targets --all-features --locked -- -D
    warnings` cross-compiled to `x86_64-unknown-linux-gnu` and
    `x86_64-pc-windows-gnu`, macOS leg skipped off a Mac with CI as
    backstop; `RUSTDOCFLAGS="-D warnings" cargo doc`; `cargo deny
    check`): exit 0. The new `use` is consumed by the `resolve_entry`
    signature, so no unused-import warning. `cargo deny` advisories /
    bans / licenses / sources ok (pre-existing transitive
    duplicate-crate warnings only; no dependency added).
  - `just test` (= `cargo test --workspace --locked`): exit 0 — full
    workspace suite plus doctests pass, including the round-1 engine
    unit tests and the `commit_record` integration test, unchanged by
    this import-only edit.
- Evidence: paper trail at
  `.speccy/specs/0004-patina-conditional-entries/evidence/T-002.md`
  (round-1 evidence, unchanged — no test added or modified this round).
  Roll call for CHKs under T-002's covered REQs is unchanged from
  round 1, since this round touches no test and no observable behaviour:
  - REQ-009 ordering `<done-when>` (files before directories): still
    demonstrated → `files_are_planned_before_directories_in_declaration_order`.
  - REQ-009 index `<done-when>` (single monotonic space, no collision):
    still demonstrated →
    `entry_indices_are_a_single_monotonic_space_across_both_tables` and
    the `commit_record.rs` integration test
    `directory_and_file_entries_get_distinct_indices_and_envelope_major_is_unchanged`.
  - DEC-009 version-envelope unchanged: still demonstrated → same
    integration test asserts COMMIT envelope major stays `2`.
  - CHK-022 (only `<scenario>` CHK under REQ-009): judgment-only /
    deferred → requires the T-005 `when` gate and T-006 source kind
    check; out of T-002's scope, lands with T-006. Unchanged from
    round 1.
  REQ-001's parse-layer CHKs (CHK-001/002/003) were demonstrated by
  completed T-001 and are not re-proved here.
- Discovered issues: none new this round. The round-1 note about the
  slightly-stale `apply/mod.rs` "never plans a `SymlinkTree`" comment
  still stands and remains T-007's to update; left untouched to keep
  this retry surgical.
- Procedural compliance: Retry round — the dirty working tree from
  round 1 was amended in place (no `git restore`/`clean`/`checkout`,
  no rewrite from scratch, no state reset). TASKS.md `state` for T-002
  was flipped `pending` → `in-progress` at the start of this turn and
  `in-progress` → `in-review` after the hygiene gate passed. This
  `<implementer round="2">` block was appended after the round-1
  `<blockers>` block; no earlier journal block was modified. No
  `<implementer-note>` was written into TASKS.md. No shipped
  `.claude/skills/speccy-*` bodies were edited.
</implementer>

<review date="2026-06-01T21:14:58Z" model="claude-opus-4-8[1m]/high" persona="business" verdict="pass" round="2">
T-002 round-2 still delivers exactly REQ-009's in-scope `<done-when>`: files-then-directories emission order and a single monotonic `entry_index` space with no `[[file]]`/`[[directory]]` collision (`patina-core/src/apply/engine.rs` `assemble_plan_operations`), with the index plan-assigned and carried through `execute` into the COMMIT record's per-entry grouping. The round-2 change is purely the round-1 style blocker fix (added `use crate::config::ManagedEntry;` at engine.rs:38, `resolve_entry` now takes `&ManagedEntry`) — behavior-neutral, so the requirement-to-diff mapping that passed at round 1 is unchanged. No non-goal breached: the `when` gate (T-005), source kind/existence check (T-006), and real `symlink-tree` executor (T-007) are all correctly absent and deferred, and the `SymlinkTree` dispatch point still errors at materialize rather than silently shipping. CHK-022 is genuinely unsatisfiable without T-005/T-006 and is non-silently deferred to T-006; the SPEC's Open Questions block is empty. The `commit_record.rs` integration test gates the real cross-table grouping, not a tautology.
</review>

<review date="2026-06-01T21:14:58Z" model="claude-opus-4-8[1m]/high" persona="tests" verdict="pass" round="2">
T-002 round 2 is an import-only style fix (`use crate::config::ManagedEntry;` added at engine.rs:38, alphabetically placed; `resolve_entry` signature switched to `&ManagedEntry`) with zero test or behaviour delta — the round-1 tests verdict still holds. The two engine unit tests drive the real production `assemble_plan_operations` (not a mock): `files_are_planned_before_directories_in_declaration_order` asserts emitted sources are exactly `["/repo/f0","/repo/f1","/repo/d0"]`, and `entry_indices_are_a_single_monotonic_space_across_both_tables` asserts `[0,1,2,3]` gapless with disjoint file `[0,1]` / dir `[2,3]` ranges. The integration test `directory_and_file_entries_get_distinct_indices_and_envelope_major_is_unchanged` runs a real `patina apply --yes` subprocess over copy-mode entries (a fully-implemented `copy_tree` path), decodes the actual COMMIT, and asserts distinct file indices, one shared index across the directory's two leaves, no collision, files-precede-directory, and envelope major still `2`. Mutation checks hold. Evidence file present with scoped per-slice runner output, test names match the diff, red/green differ — no fabrication pattern matched. Roll call complete: CHK-022 `judgment-only`/deferred is correct (no `when` gate in T-002), CHK-001/002/003 owned by completed T-001.
</review>

<review date="2026-06-01T21:14:58Z" model="claude-opus-4-8[1m]/high" persona="security" verdict="pass" round="2">
T-002 round 2 is a pure import-convention fix over the round-1 (security-passed) state: `use crate::config::ManagedEntry;` added to the import block and `resolve_entry`'s parameter switched from the inline `crate::config::ManagedEntry` path to the imported name — both resolve to the same type, no behavioral change. No security-relevant surface moved. `resolve_entry` relocates the existing `canonicalize` + `expand_tilde` source/target handling verbatim — no new traversal vector. The journal `entry: u32` wire format and version envelope are unchanged (DEC-009; `commit_record.rs` integration test asserts COMMIT major stays `2`). The `entry_index.saturating_add(1)` overflow edge only collapses entries past `u32::MAX` declarations from the user's own local `patina.toml`, no trust boundary crossed; mirrors the pre-existing `op_index` pattern — not a defect. No authn/authz boundary, secret, injection vector, sensitive-data log, crypto parameter, or new dependency is touched (`cargo deny` clean). The `when` gate and source-kind check are correctly deferred to T-005/T-006 rather than faked.
</review>

<review date="2026-06-01T21:14:58Z" model="claude-sonnet-4-6[1m]/medium" persona="style" verdict="pass" round="2">
Round-1 blocker resolved. `use crate::config::ManagedEntry;` is present at line 38 of `patina-core/src/apply/engine.rs`, placed in alphabetical order after `HookEvent` and before `parse_module_config`, consistent with the file's sorted `crate::config::*` import grouping. The `resolve_entry` signature now uses `entry: &ManagedEntry` (no inline path). No new style issues introduced: the `SymlinkTree` arm additions in `engine.rs`, `diff.rs`, and `apply.rs` follow the surrounding match-arm style; the new `SymlinkTreeNotImplemented` variant in `apply/mod.rs` carries a doc comment on the struct field; import ordering is clean throughout.
</review>

<review date="2026-06-01T21:14:58Z" model="claude-opus-4-8[1m]/high" persona="correctness" verdict="pass" round="2">
T-002 round-2 is correct. The single substantive change since the round-1 correctness pass is the style-blocker fix in `patina-core/src/apply/engine.rs` — adding `use crate::config::ManagedEntry;` and changing `resolve_entry`'s parameter from the inline `&crate::config::ManagedEntry` to `&ManagedEntry`; the inline path and the imported name resolve to the identical type, so the edit is behavior-preserving with zero correctness surface. The round-1 logic is intact and sound: `assemble_plan_operations` increments one `u32` once per `ResolvedEntry` over `file_entries.chain(directory_entries)`, yielding a gapless `0..N` space with files strictly before directories and no `[[file]]`/`[[directory]]` index collision; `execute` reads the plan-assigned `op.entry_index` for rollback grouping while keeping the distinct per-record `op_index` progress cursor. The `saturating_add(1)` collapse edge is unreachable (>4e9 entries), and `SymlinkTree`'s symlink-op-shape mapping still errors `SymlinkTreeNotImplemented` at materialize. No logic, Option/Result, boundary, concurrency, or resource-leak defect at or above the reporting bar.
</review>
