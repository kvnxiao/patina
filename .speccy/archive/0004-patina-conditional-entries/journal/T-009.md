---
spec: SPEC-0004
task: T-009
generated_at: 2026-06-01T23:01:36Z
---

<implementer date="2026-06-01T23:01:36Z" model="claude-opus-4-8[1m]/high" round="1">
- Reuse survey: Mapped the task area (covered REQ-004; suggested files
  `patina-core/src/profile.rs`, `patina-core/src/apply/engine.rs`,
  `patina-cli/tests/auto_match_predicates.rs`; immediate neighbours
  `patina-core/src/template/mod.rs::Engine::eval_when` +
  `template/strict.rs`, `patina-core/src/variables/mod.rs::Resolver`,
  `patina-core/src/variables/builtins.rs`, `patina-core/src/apply/hooks.rs::should_run`,
  `patina-cli/tests/common/mod.rs`, `patina-cli/tests/conditional_entries.rs`).
  Decisions:
  - reuse-as-is — `template::Engine::eval_when` (the shared `when`
    evaluator hooks already use; routed auto_match through it, no new
    evaluator); `variables::Resolver::new` (a no-layers `Resolver` *is*
    the built-ins-only context DEC-006 requires — no new constructor);
    the `Fixture`/`code` test harness in `tests/common/mod.rs`;
    `current_os_family()` helper pattern copied from
    `tests/conditional_entries.rs`. The CLI error-chain renderer
    (`cli::resolve_exit_code`) and `ExitCode::from_error_chain` already
    surface a wrapped source's `Display` to stderr and map an
    `EngineError` to a non-zero code — reused unchanged.
  - extend — `profile::resolve` grew an `engine: &Engine` parameter and
    now evaluates each rule's `when` via `eval_when` against a
    built-ins-only `Resolver`; `Builtins::profile` changed from
    `String` to `Option<String>` so `patina.profile` is *undefined*
    (not defined-empty) until `Resolver::with_profile` resolves it —
    this is what makes CHK-021 error instead of silently non-matching;
    `Resolver::with_profile` now stores `Some(name)`.
  - write-fresh — `ProfileError::Predicate { predicate, source }`
    (replaces the deleted `UnsupportedPredicate`); the integration suite
    `patina-cli/tests/auto_match_predicates.rs` and its `write_root`
    helper (grepped `tests/common/mod.rs` for an existing root-manifest
    overwriter — `Fixture` only writes the default root in `new()`, so a
    local helper was needed). New top-level symbols: `ProfileError::Predicate`
    (extends the existing enum), `write_root` (test helper) — each
    accounted for above.
- Completed: Routed `[[auto_match]]` `when` evaluation through the shared
  `MiniJinja` engine and deleted the narrow evaluator (REQ-004 / DEC-006).
  In `patina-core/src/profile.rs`: removed `evaluate_predicate`,
  `parse_string_literal`, and `ProfileError::UnsupportedPredicate`; added
  `ProfileError::Predicate` wrapping `template::TemplateError` via
  `#[source]` so the undefined-variable name reaches the CLI error chain;
  `resolve` now takes `engine: &Engine`, builds one built-ins-only
  `Resolver::new(builtins.clone())`, and evaluates each rule with
  `engine.eval_when`. Rewrote the module docs (narrow-grammar section →
  shared-engine section) and replaced the four narrow-grammar unit tests
  (`predicate_rejects_unsupported_shape`, `predicate_rejects_non_patina_lhs`,
  `predicate_rejects_unquoted_rhs`, `missing_builtin_compares_as_empty_string`)
  with engine-path tests (`auto_match_inequality_predicate_now_selects`,
  `auto_match_or_predicate_now_selects`,
  `auto_match_referencing_patina_profile_errors`). In
  `patina-core/src/variables/builtins.rs`: `profile` field →
  `Option<String>` (`None` = unresolved → `patina.profile` undefined),
  with `current`/`for_tests` setting `None` and `get` returning the
  option directly; updated the module doc and the
  `profile_starts_unresolved` test. In `patina-core/src/variables/mod.rs`:
  `with_profile` stores `Some(name)`. In `patina-core/src/apply/engine.rs`:
  construct the shared `Engine` *before* `resolve_profile` and pass it in;
  removed the now-duplicate later `Engine::new()`. Updated the four
  `resolve` call sites in `patina-core/tests/profile_resolution.rs` to
  thread an `Engine`. Added CLI integration tests CHK-008/009/020/021 in
  `patina-cli/tests/auto_match_predicates.rs`.
- Undone: Nothing in T-009's scope is deferred. The task notes that hook
  `when` undefined-access already errors uniformly (DEC-010) — confirmed:
  `apply/hooks.rs::should_run` already routes through `Engine::eval_when`
  and `should_run_surfaces_undefined_variable_error` asserts it; grepped
  `apply/hooks.rs` and `patina-cli/tests` for any reliance on
  undefined-in-`when` silently evaluating false and found none, so no hook
  test or fixture needed fixing.
- Hygiene checks: `just test` (= `cargo test --workspace --locked`) exited
  0 — all workspace suites pass, including the new
  `auto_match_predicates` (4 tests), the rewritten `profile::tests` unit
  tests, and `profile_resolution`/`variables` integration tests.
  `just lint` exited 0 — the project's CI-order suite: nightly
  `fmt --all --check`, clippy `--workspace --all-targets --all-features
  --locked -- -D warnings` (cross-compiled to the Linux + Windows targets),
  `RUSTDOCFLAGS=-D warnings cargo doc`, and `cargo deny check`
  (`advisories ok, bans ok, licenses ok, sources ok`). No new dependency
  was added.
- Evidence: paper trail at
  `.speccy/specs/0004-patina-conditional-entries/evidence/T-009.md`.
  Roll call for the four CHKs under REQ-004:
  - CHK-008 (auto_match OS rule resolves profile `p`): demonstrated →
    evidence Scenario 1; test `auto_match_rule_on_os_resolves_its_profile`
    asserts `--json` `profile == "p"`. (Parity floor — guards a rewrite
    regression rather than flipping a pre-edit bug.)
  - CHK-009 (`!=` predicate materializes target, no `UnsupportedPredicate`):
    demonstrated → evidence Scenario 2; test
    `file_inequality_predicate_materializes_target`.
  - CHK-020 (misspelled `patina.oss` fails non-zero, stderr names it,
    target not dropped): demonstrated → evidence Scenario 3; test
    `file_misspelled_builtin_fails_and_names_the_variable`.
  - CHK-021 (`patina.profile` reference errors naming the variable):
    demonstrated → evidence Scenario 4; CLI test
    `auto_match_referencing_patina_profile_fails_and_names_it` plus unit
    test `profile::tests::auto_match_referencing_patina_profile_errors`.
  No CHK in T-009's scope is `judgment-only`; every CHK is scriptable.
- Discovered issues: `Builtins::profile` had been a defined empty
  `String` until `with_profile` ran, so `patina.profile` resolved as a
  *defined* empty string. Under the shared engine that would make
  `patina.profile == 'work'` a defined `false` (silent non-match) — the
  exact DEC-010 footgun CHK-021 forbids. Fixing it required changing the
  field to `Option<String>` (unresolved = `None` = undefined) rather than
  a profile.rs-local workaround, since the undefined-access semantics live
  in the resolver/engine layer. This is a model correction (the field now
  honestly represents resolved-or-not), not added complexity; no other
  caller depended on the defined-empty behaviour (verified by grep: the
  only `patina.profile` assertions, in `variables.rs` and
  `variables/mod.rs`, run *after* `with_profile` and still see `Some`).
- Procedural compliance: This entry lands directly in `journal/T-009.md`
  per the journal-file schema; no `<implementer-note>` was written into
  TASKS.md. The TASKS.md `state` for T-009 flips from `in-progress` to
  `in-review` as the final step of this turn. No shipped `skills/` bodies
  were edited.
</implementer>

<review date="2026-06-01T23:06:01Z" model="claude-opus-4-8[1m]/high" persona="business" verdict="pass" round="1">
REQ-004/DEC-006/DEC-010 are faithfully delivered. The narrow `[[auto_match]]` evaluator and `ProfileError::UnsupportedPredicate` are gone from production code; auto-match routes through the shared `Engine::eval_when` against a built-ins-only resolver. The four CLI tests in `patina-cli/tests/auto_match_predicates.rs` map exactly to the SPEC scenarios and pass: CHK-008 (parity, `--json` profile == "p"), CHK-009 (`!=` materializes, no `UnsupportedPredicate`), CHK-020 (misspelled `patina.oss` exits non-zero, stderr names it, target not created), CHK-021 (`patina.profile` errors naming it). I traced the load-bearing DEC-010 behavior change end-to-end: `patina.oss == 'windows'` produces an undefined comparison result (`strict.rs` tracker + `coerce_when_result` at `template/mod.rs:183`), converted to a typed `UndefinedVariable` error rather than a silent defined-false — the exact footgun the SPEC forbids. `patina.profile` is now `Option<String>` (None = unresolved), making CHK-021 error during profile resolution instead of silently matching the empty string. The CHK-021 stderr contract holds via the error chain `EngineError::Profile` → `ProfileError::Predicate` (`#[source]`) → `TemplateError::UndefinedVariable`. The DEC-010 hook-uniformity leg is regression-free. No non-goal scope snuck in; the empty Open Questions block was not silently resolved.
</review>

<review date="2026-06-01T23:06:01Z" model="claude-opus-4-8[1m]/xhigh" persona="tests" verdict="pass" round="1">
All four CHKs are exercised by tests that drive the real code paths, not mocks.
- CHK-008: `auto_match_rule_on_os_resolves_its_profile` drives the real `patina apply --json` over a tempdir repo and asserts `doc["profile"] == "p"`; `Fixture::run` does `env_remove("PATINA_PROFILE")` so resolution genuinely falls through to the auto_match rule. Real subprocess, real JSON.
- CHK-009: `file_inequality_predicate_materializes_target` asserts exit 0 AND `~/.zshrc` exists for a `!=` predicate the deleted narrow evaluator rejected — a positive materialization assertion.
- CHK-020: `file_misspelled_builtin_fails_and_names_the_variable` asserts non-zero exit AND `stderr.contains("patina.oss")` AND target NOT created. Verified the mechanism: `patina.oss` is tracked undefined and `coerce_when_result` errors rather than coercing to false.
- CHK-021: `auto_match_referencing_patina_profile_fails_and_names_it` + unit test assert non-zero / `stderr.contains("patina.profile")` / typed `ProfileError::Predicate`. The `Builtins::profile: Option<String>` change makes this honest — `get("patina.profile")` returns `None` while unresolved.
Deletion/replacement confirmed: the four narrow-grammar unit tests are gone, replaced by engine-path tests driving real `resolve(..., &engine)`. The hook grep claim holds: `should_run` already routes through `Engine::eval_when` and `should_run_surfaces_undefined_variable_error` asserts it; no CLI/hook test relies on silent-false-on-undefined. I independently re-ran `cargo test -p patina-cli --test auto_match_predicates` (4 passed) and `cargo test -p patina-core --lib profile::` (14 passed). Roll call names all four covered CHKs as `demonstrated`.
</review>

<review date="2026-06-01T23:06:01Z" model="claude-opus-4-8[1m]/high" persona="security" verdict="pass" round="1">
Routing `[[auto_match]]` `when` through `Engine::eval_when` under a builtins-only `Resolver::new(builtins.clone())` (no `with_*` layers) is sound. During profile resolution `Resolver::get` walks five empty user layers then falls to `Builtins::get`, so a manifest predicate cannot reference any user-supplied variable (CLI/per-machine/per-profile/per-module/repo-shared) — an out-of-scope reference resolves to `None` → undefined → typed `ProfileError::Predicate` naming the variable (fail-closed, never a silent mis-select). The `Builtins::profile: Option<String>` change correctly makes `patina.profile` undefined during resolution (`builtins.rs:86`), closing the DEC-010 defined-empty-string footgun; `with_profile` only sets `Some(..)` after `resolve_profile` returns, so there is no stale-read path. No new evaluation surface beyond the existing hook/entry `when`: same `build_context`/`eval_when`, `Enumerator::Empty` blocks layer-internals leakage via `{% for %}`, `when` does no `{{ }}` emit, and `patina.env.*` is pre-existing shared behavior. Error messages name variable names, not values. No new dependency. No auth boundary, injection vector, or race introduced.
</review>

<review date="2026-06-01T23:06:01Z" model="claude-sonnet-4-6[1m]/medium" persona="style" verdict="pass" round="1">
All style invariants hold. The deleted `evaluate_predicate`, `parse_string_literal`, and `ProfileError::UnsupportedPredicate` symbols have no orphan references remaining — the single mention in `auto_match_predicates.rs:80` is a comment explaining what no longer happens. The `Option<String>` change to `Builtins::profile` is handled idiomatically at every read site: `Builtins::get` returns the option directly, `Resolver::with_profile` stores `Some(profile.into())`, and the test assertion correctly checks `is_none()`. The new `ProfileError::Predicate` variant follows the existing `thiserror` struct-variant pattern with a `#[source]` field and a justifying doc comment. Module docs in `profile.rs` no longer describe the narrow evaluator or any `T-008` forward reference. Import ordering in `profile.rs` (crate:: → external) matches the established project convention. The `#![expect(clippy::expect_used, reason = "...")]` annotation in `auto_match_predicates.rs` is the exact pattern used by every sibling integration test file. Reuse survey claims are honest.
</review>

<review date="2026-06-01T23:06:01Z" model="claude-opus-4-8[1m]/high" persona="correctness" verdict="pass" round="1">
T-009 is correct. (1) `profile::resolve` evaluates each `[[auto_match]]` `when` via `engine.eval_when` against `Resolver::new(builtins.clone())` with no `with_profile`/user layers — a genuine builtins-only context per DEC-006. (2) The `Builtins::profile: String → Option<String>` migration is handled at every read site: `Builtins::get` returns the Option verbatim so `None` is undefined (not defined-empty), `Resolver::with_profile` stores `Some(name)`, and `build_planning_context` (apply/engine.rs:390-391) calls `.with_profile(profile.name.clone())` AFTER `resolve_profile`, so `patina.profile` resolves normally in the apply/template path while being undefined during profile resolution — exactly what makes CHK-021 error instead of silently non-matching. The other `profile`-named reads (`resolved.profile`, `rule.profile`, `Resolution.name`) are unrelated `String` fields, unaffected. (3) DEC-010 uniformity falls out of routing through `Engine::eval_when`, whose `coerce_when_result` fails closed on an undefined result; hooks already route through it and no hook test/fixture relied on silent-false-on-undefined. The shared `Engine` is constructed once and hoisted before profile resolution with the prior duplicate removed — no double-construction or use-after-move. All 20 profile unit tests and the 4 new CLI integration tests pass locally.
</review>
