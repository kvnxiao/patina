---
task: T-007
spec: SPEC-0001
generated_at: 2026-05-28T00:00:00Z
---

<implementer date="2026-05-28T00:00:00Z" model="claude-opus-4-7[1m]" round="1">

- Completed:
  - Added `patina_core::profile` module exposing `resolve`, `Resolution`,
    `ProfileSource`, `AutoMatchRule`, `ProfileError`,
    `load_auto_match_rules`, and the `PERSISTED_PROFILE_FILE = "profile"`
    constant.
  - `resolve` walks the four REQ-008 sources in priority order: (1) the
    `PATINA_PROFILE` env value (empty string treated as unset, per the
    repo's existing env-empty-is-unset convention in
    `discovery::resolve_repository_root`); (2) the first non-empty
    trimmed line of `<state>/patina/profile` (`NotFound` is silent
    fall-through, other IO errors surface as
    `ProfileError::PersistedRead`); (3) `[[auto_match]]` rules in
    declaration order, first match wins; (4) the no-profile fallback
    (empty string, `ProfileSource::Fallback`).
  - `load_auto_match_rules` parses the root `patina.toml`'s
    `[[auto_match]]` table-array; missing file -> empty vec, malformed
    TOML -> `ProfileError::RootParse`. Used the same `serde::Deserialize`
    + private `RawRoot` projection pattern as `config::parse_module_config_str`.
  - Predicate evaluator: a narrowly-typed parser that accepts
    `patina.<key> == '<literal>'` or `patina.<key> == "<literal>"`,
    resolving the LHS through `Builtins::get` (so the full `patina.os`,
    `patina.arch`, `patina.hostname`, `patina.user`, `patina.home`, and
    `patina.env.*` surface is reachable). Any other shape -- `!=`,
    `and`/`or`, unquoted RHS, non-`patina.*` LHS -- returns
    `ProfileError::UnsupportedPredicate` naming the offending text and a
    static reason string. The task body explicitly allows this stub:
    "if the MiniJinja path is unavailable, evaluate against a narrowly
    typed predicate API". T-008 will replace the evaluator body with a
    MiniJinja `Environment::compile_expression(...)` call without
    changing the `evaluate_predicate` signature or `resolve`'s public
    surface.
  - Surfaced `ProfileError` on `EngineError::Profile(#[from]
    ProfileError)` so callers in T-009 onwards get a typed propagation
    path. Re-exported `AutoMatchRule`, `ProfileError`, `ProfileSource`,
    `Resolution as ProfileResolution`, `resolve as resolve_profile`,
    and `load_auto_match_rules` at the crate root in `lib.rs` (the
    `as`-renames disambiguate from existing `Resolver` /
    `resolve_repository_root` at the crate root).
  - Integration tests in `patina-core/tests/profile_resolution.rs`
    cover all four REQ-008 done-when clauses and both scenarios
    (CHK-017, CHK-018). The CHK-018 test pins `Builtins::hostname` to
    `"CHK-host"` via the `pub` field (the T-006 reviewer's downstream
    note flagged that `Builtins::current()` reads `$HOSTNAME` from the
    process env, which is not reliably exported on Unix; this approach
    isolates the test from the host's real hostname without needing
    in-process `std::env::set_var`, which is `unsafe` and would trip
    the workspace `unsafe_code = "forbid"` lint).
- Undone:
  - **CLI scanning for the absence of `--profile`** (the last scenario
    in the T-007 task body and one of REQ-008's done-when clauses).
    The clap-derived parser does not yet exist -- `patina-cli/src/main.rs`
    currently only awaits `patina_core::status(...)` as a wiring proof,
    and clap arrives in T-016 (the task body itself calls T-016 out as
    the CLI surface task). There is nothing to scan today; the test
    would either be vacuous ("a non-existent CLI does not contain
    --profile") or require pulling clap forward into this task, which
    would conflict with T-016's scope. The contract this task can
    enforce -- and does -- is that the public `profile` module surface
    exposes no `--profile` knob and `resolve()` does not accept a
    flag-style argument; the CLI-level verification lands with the
    clap-derived parser in T-016.
  - **End-to-end `patina apply --yes --json` profile field assertion**
    in CHK-017 / CHK-018. The `apply` plan computation and the
    `--json` output layer (T-009 / T-016 / Phase 10's `output::Reporter`)
    are downstream. The engine-level contract -- the resolution function
    that the JSON path will eventually call -- is fully covered here,
    matching the precedent T-006 set when MiniJinja `==` evaluation
    was deferred to T-008.
- Hygiene checks:
  - `cargo test --workspace` -> exit 0 (4/4 new integration tests in
    `tests/profile_resolution.rs`, 15 new unit tests in `src/profile.rs`,
    1 doc-test on `resolve`; no regressions in existing 8 + 6 + 8 + 8
    integration tests).
  - `cargo clippy --workspace --all-targets --all-features -- -D warnings`
    -> exit 0
  - `cargo +nightly fmt --all --check` -> exit 0
  - `cargo deny check` -> exit 0 (no new direct deps; `serde` and
    `toml` were already workspace deps).
- Evidence: unit scenarios alongside `patina-core/src/profile.rs`;
  integration scenarios in `patina-core/tests/profile_resolution.rs`.
  Roll call for CHKs under REQ-008:
  - CHK-017 (env var resolves to `"work"` when nothing else matches):
    demonstrated -> `env_var_resolves_to_work_when_no_other_sources_match`
    in `patina-core/tests/profile_resolution.rs` builds a fresh tempdir
    with no persisted file and no root manifest, calls
    `resolve(Some("work".to_owned()), &persisted, &[], &builtins)`,
    and asserts `name == "work"` and `source == ProfileSource::Env`.
    End-to-end binding through `patina apply --yes --json` JSON output
    lands when T-009 builds the apply plan and T-016 lands the
    `--json` renderer; the engine-layer contract this CHK depends on
    is covered here.
  - CHK-018 (auto-match on `patina.hostname == 'CHK-host'` resolves to
    `"desktop"`): demonstrated ->
    `auto_match_on_hostname_resolves_to_desktop` in
    `patina-core/tests/profile_resolution.rs` writes the literal
    CHK-018 root manifest body to a tempdir, calls
    `load_auto_match_rules` to parse it, sets `builtins.hostname =
    "CHK-host"` (per the T-006 reviewer's note that `$HOSTNAME` is
    not reliably exported on Unix subprocesses; the `pub` field
    permits this without `unsafe` env mutation), and asserts the
    resolved name and source. End-to-end binding through `patina apply
    --yes --json` JSON output is deferred to T-009 / T-016 as above.
- Discovered issues:
  - The clap-derived parser does not yet exist (T-016 lands it).
    REQ-008's done-when (e) -- "no subcommand exposes a `--profile`
    flag" -- is not testable today. Recording here so the T-016
    reviewer can add a `clap::Command::debug_assert` or a
    `--help` substring scan that verifies the absence; the engine
    surface this task ships does not contain any `--profile`-shaped
    knob to bind to in the first place.
  - The MiniJinja `==` evaluation contract (REQ-009 / T-008) will
    eventually replace the narrow predicate evaluator in
    `profile::evaluate_predicate`. The swap is a body change behind
    a stable function signature; no caller of `resolve` will need to
    change. Recording so the T-008 implementer knows the entry point.
- Procedural compliance: first-attempt shape on a clean tree;
  resolved the target via `speccy next SPEC-0001 --json` (returned
  `next_action.kind = "work"`, `task_id = "T-007"`); no prior journal
  entries to amend.

</implementer>

<review date="2026-05-28T00:00:00Z" model="claude-opus-4-7[1m]/high" persona="business" verdict="pass" round="1">
T-007 implements REQ-008's four-source profile resolution chain cleanly and the two flagged deferrals are within-contract scope boundaries, not SPEC breaches.

- **Narrow predicate stub** (`patina-core/src/profile.rs:307-332`) is explicitly sanctioned by the T-007 task body: *"if the MiniJinja path is unavailable, evaluate against a narrowly typed predicate API (e.g., just `patina.hostname == 'x'`)"*. The stub accepts exactly the predicate shape CHK-018 names (`patina.hostname == 'CHK-host'`) and rejects everything else with a typed `ProfileError::UnsupportedPredicate` rather than misinterpreting it. T-008 swaps the body behind a stable signature; the journal records this for the T-008 implementer. SPEC scenario behaviour at CHK-018 holds today.
- **`--profile` absence scan deferral** to T-016 is the right call: `patina-cli/src/main.rs` ships only the tokio wiring proof from T-001 and contains no clap derive surface to scan. A test today would be vacuous. The engine-level contract this task can verify — that `profile::resolve()` takes no flag-style argument and that the public `profile` module exposes no `--profile` knob — does hold; the CLI-level absence assertion is queued for T-016 in the journal's Discovered-issues block so the next reviewer picks it up. Done-when clause (e) is mechanically untestable until clap lands.

Done-when coverage at the engine layer (a/b/c/d): all four exercised in `patina-core/tests/profile_resolution.rs:36-125` (env wins, persisted resolves to `home`, auto-match on hostname resolves to `desktop`, fallback returns empty string). CHK-017 and CHK-018 are demonstrated at the engine contract that the eventual `--json` output will read from. Priority-order ordering, empty-env fall-through, NotFound silent fall-through, other-IO surfaces typed, first-match-wins, and the predicate-rejection paths are all unit-tested in-module. No `<non-goals>` line is crossed; no `<open-questions>` item is silently resolved (the six are all `[x]`-resolved upstream). The `<behavior>` clause about logging `(none)` is satisfied structurally by the `ProfileSource::Fallback` variant — actual log emission lives behind `output::Reporter` (REQ-026 / T-016) and is properly out-of-scope here.
</review>

<review date="2026-05-28T00:00:00Z" model="claude-opus-4-7[1m]/medium" persona="tests" verdict="pass" round="1">
The 4 integration tests in `patina-core/tests/profile_resolution.rs` each gate a real invariant of `profile::resolve` and would catch realistic regressions.

CHK-018 (`auto_match_on_hostname_resolves_to_desktop`, `tests/profile_resolution.rs:65`) genuinely drives the hostname-comparison code path end-to-end — no mocking. It writes the literal CHK-018 manifest body to a tempdir, parses it through the real `load_auto_match_rules` TOML round-trip, calls `resolve(None, &persisted, &rules, &builtins)` with a missing persisted file and `env_value=None`, and asserts both `name == "desktop"` and `source == ProfileSource::AutoMatch`. The `builtins.hostname = "CHK-host"` mutation overrides `for_tests()`'s default `"test-host"` (`patina-core/src/variables/builtins.rs:61`) and is read back through `Builtins::get("patina.hostname")` inside `evaluate_predicate` — the same code path a future MiniJinja swap (T-008) will replace. Mentally swapping `evaluate_predicate` to return `Ok(false)`, or `Builtins::get` to drop the `patina.hostname` arm, or `parse_string_literal` to strip the wrong number of chars, each fails this test.

CHK-017 (`env_var_resolves_to_work_when_no_other_sources_match`, `:37`) asserts both `name == "work"` AND `source == ProfileSource::Env`, so a regression that falls through env-precedence and lands on Fallback (returning `""`) still trips the source assertion. The pre-call `assert!(rules.is_empty())` documents the empty-manifest precondition rather than glossing over it.

The two extra tests covering done-when (b) and (d) — `persisted_choice_resolves_to_home_when_env_unset` (`:91`) and `fallback_resolves_to_empty_when_nothing_matches` (`:107`) — each exercise the persisted-file IO path and the no-match-but-rules-present path with both name and source assertions. The fallback test writes a non-matching `[[auto_match]]` rule so it would catch any future regression that makes `evaluate_predicate` over-eagerly return `true`.

The deferred `--profile` absence CLI scan is correctly flagged as T-016 scope and the engine surface this task ships exposes no `--profile` knob to scan against today. The unit tests in `patina-core/src/profile.rs` (15 cases, including `predicate_rejects_unsupported_shape`, `predicate_rejects_non_patina_lhs`, `predicate_rejects_unquoted_rhs`, `missing_persisted_file_silently_falls_through`, `empty_persisted_file_falls_through`, `auto_match_first_rule_wins`) cover the negative paths the integration suite leaves to the module.

No vacuous shapes: every assertion checks at least two properties; no production constants are copied (the `"CHK-host"` literal does not appear in `Builtins::current()`); no test mocks the function under test; `cargo test --test profile_resolution` confirms 4/4 green locally.
</review>

<review date="2026-05-28T00:00:00Z" model="claude-opus-4-7[1m]/medium" persona="security" verdict="pass" round="1">
Profile resolution surface is security-clean.

1. Persisted profile read (`patina-core/src/profile.rs:212`, `load_auto_match_rules` at `:262`) — both reads go through `fs_err::read_to_string` against engine-resolved paths (`<state>/patina/profile`, repo root `patina.toml`), not user-supplied paths. Symlink-follow concerns are out of scope: an attacker capable of placing a symlink under the per-user state dir already has the privileges to write the profile name directly, and the read result is consumed only as the first non-empty trimmed line and used as a profile-name string (no path/command/template execution). REQ-008 mandates exactly this consume shape. Errors surface via typed `ProfileError::PersistedRead` / `RootRead` / `RootParse` with the path; the path is the user's own and the errors flow to the same user via `EngineError::Profile` — no privilege boundary crossed.

2. Narrow predicate evaluator (`evaluate_predicate` at `profile.rs:307`, `parse_string_literal` at `:334`) — grammar is tight enough to be safe: requires `==` exactly, LHS must start with `patina.` (resolved via `Builtins::get`, which only exposes `patina.{os,arch,hostname,user,home,profile}` or `patina.env.<KEY>` reading process env via `std::env::var`), RHS must be a single- or double-quoted literal with no embedded same-kind quote (rejects breakouts). No `and`/`or`, no function calls, no negation, no template-style interpolation — anything outside this shape returns `ProfileError::UnsupportedPredicate`. The RHS literal is used only for an exact-string `==` comparison against the resolved built-in; no shell, SQL, path, or template injection sink is reachable. `patina.env.*` does let manifests probe arbitrary env vars (including secrets), but the predicate result is only ever compared for equality and consumed as a profile-selector boolean — the value itself is never logged or returned, and `UnsupportedPredicate` is never raised on a well-formed predicate, so secret-value disclosure through error text is not possible.

No `unsafe`, no new dependencies, no `unwrap()`/`expect()` in production paths. Tests use `TempDir` fixtures — no writes outside per-test temp directories. `cargo test --workspace` is green.
</review>

<review date="2026-05-28T00:00:00Z" model="claude-sonnet-4-6[1m]/medium" persona="style" verdict="blocking" round="1">
`patina-core/src/profile.rs` lines 109–111, 119–121, and 129–131: the `#[source]` attribute is placed **before** the doc comment on the `source` field in all three error variants (`PersistedRead`, `RootRead`, `RootParse`). The established project convention, demonstrated by `StateDirError::Io` in `patina-core/src/state_dir.rs` lines 93–96, places the doc comment **before** the `#[source]` attribute:

```rust
// Established pattern (state_dir.rs):
/// The underlying I/O error.
#[source]
source: std::io::Error,

// T-007 (profile.rs) — inverted ordering in all three variants:
#[source]
/// Underlying IO error.
source: std::io::Error,
```

In standard Rust idiom, doc comments precede attributes for struct/enum fields. Flip the ordering so the doc comment appears before `#[source]` in all three variants. While rustfmt does not enforce this ordering and `rustc` accepts both, the project has a single established shape for error-source fields and this diff breaks it consistently across the new module.
</review>

<blockers date="2026-05-28T00:00:00Z" round="1">
Fix the doc-comment / `#[source]` attribute ordering in `patina-core/src/profile.rs` before T-007 can be marked complete.

- Style (blocking): In all three `ProfileError` variants (`PersistedRead`, `RootRead`, `RootParse`) the `#[source]` attribute is placed before the `///` doc comment on the inner `source` field. The established project convention (see `StateDirError::Io` in `patina-core/src/state_dir.rs`) places the doc comment before the attribute. Flip the ordering so doc comments precede attributes consistently across `profile.rs`. Business / tests / security all passed.
</blockers>

<implementer date="2026-05-28T00:00:00Z" model="claude-opus-4-7[1m]" round="2">

- Completed:
  - Amended `patina-core/src/profile.rs` in place to flip the doc-comment /
    `#[source]` ordering on the inner `source` field of all three affected
    `ProfileError` variants (`PersistedRead`, `RootRead`, `RootParse`).
    The `/// Underlying ... error.` doc line now precedes `#[source]`,
    matching the `StateDirError::Io` shape in
    `patina-core/src/state_dir.rs`. No other lines in the file changed.
- Undone:
  - Nothing new. The two round-1 deferrals (CLI `--profile` absence scan
    landing in T-016, end-to-end `patina apply --yes --json` assertions
    landing in T-009 / T-016) remain unchanged and were already accepted
    by business / tests / security in round 1.
- Hygiene checks:
  - `cargo test --workspace` -> exit 0 (no regressions; same test counts
    as round 1).
  - `cargo clippy --workspace --all-targets --all-features -- -D warnings`
    -> exit 0
  - `cargo +nightly fmt --all --check` -> exit 0
  - `cargo deny check` -> exit 0
- Evidence: same as round 1 — `patina-core/tests/profile_resolution.rs`
  and in-module unit tests. The style-only amend changes no behaviour
  and adds no scenarios. CHK roll call from round 1 stands unchanged.
- Discovered issues: none new.
- Procedural compliance: retry shape detected from journal (latest
  `<blockers round="1">` matches highest `<implementer round="1">`);
  amended dirty working tree in place per the retry-shape contract;
  did not reset or rewrite; appended this `<implementer round="2">`
  block with monotonic round increment.

</implementer>

<review date="2026-05-28T00:00:00Z" model="claude-opus-4-7[1m]/high" persona="business" verdict="pass" round="2">
Round-2 fix is surgical and within-scope: doc-comment / `#[source]` reordering on the inner `source` field of all three `ProfileError` variants (`PersistedRead`, `RootRead`, `RootParse`) in `patina-core/src/profile.rs:101-144`. The change touches only attribute/doc ordering and preserves the `#[error("…")]` Display strings, field names, and types. `ProfileError`'s public API is unchanged. No edits to `resolve`, `load_auto_match_rules`, `Resolution`, `ProfileSource`, `AutoMatchRule`, or any REQ-008 source-priority logic. The two within-contract deferrals (CLI `--profile` absence scan → T-016, end-to-end JSON `profile` field → T-009 / T-016) stand exactly as round-1 business accepted them; the round-2 implementer block explicitly records "Nothing new" in Undone. No `<non-goals>` boundary crossed, no `<open-questions>` item silently resolved, no `<done-when>` clause regressed. Business round-1 pass carries forward.
</review>

<review date="2026-05-28T00:00:00Z" model="claude-opus-4-7[1m]/medium" persona="tests" verdict="pass" round="2">
Round 2 amend is a pure doc-comment / `#[source]` ordering flip on the inner `source` field of `ProfileError::{PersistedRead, RootRead, RootParse}` (`patina-core/src/profile.rs:102-132`). Test surface is byte-identical to round 1: `patina-core/tests/profile_resolution.rs` (4 integration tests) and the in-module `profile::tests` (9+ unit cases) are unchanged. Locally re-ran `cargo test --test profile_resolution` (4/4 pass) and `cargo test -p patina-core --lib` (41/41 pass, including `auto_match_first_rule_wins`, `missing_persisted_file_silently_falls_through`, `env_var_wins_when_set`, `empty_env_value_falls_through`, `persisted_file_resolves_when_env_unset`, `load_auto_match_rules_reads_file`, etc.). CHK-017 and CHK-018 remain demonstrated by `env_var_resolves_to_work_when_no_other_sources_match` and `auto_match_on_hostname_resolves_to_desktop` respectively — both still assert name AND source. The ordering change is invisible to `thiserror` (attribute position relative to doc comments does not affect macro expansion or `Error::source()` semantics), so no test could be sensitive to it. No regressions; round 1 tests verdict preserved.
</review>

<review date="2026-05-28T00:00:00Z" model="claude-opus-4-7[1m]/medium" persona="security" verdict="pass" round="2">
Round 2 is a style-only amend: the `#[source]` attribute and `/// Underlying ... error.` doc comment were flipped on `ProfileError::PersistedRead` (`patina-core/src/profile.rs:107-111`), `RootRead` (`:117-121`), and `RootParse` (`:127-131`). No code in any function body changed; no new dependency, no new public API, no new `unsafe`, no new I/O sink.

Verified security-relevant surface unchanged from round 1:
- Persisted read still consumes only the first non-empty trimmed line of `<state>/patina/profile` as a name string — no path/command/template execution sink (`profile.rs:212-230`).
- Predicate grammar still requires `==` exactly, `patina.`-prefixed LHS resolved through `Builtins::get`, quoted-literal RHS with no embedded same-kind quote; everything else returns `ProfileError::UnsupportedPredicate` naming only the user-supplied predicate text (no secret-value disclosure via `patina.env.*` since RHS literals, not LHS resolved values, appear in error messages).
- Error variants `PersistedRead` / `RootRead` / `RootParse` still display the user's own path back to the user via `EngineError::Profile(#[from])` — no privilege boundary crossed.
- Tests still use `TempDir` fixtures; no writes outside per-test temp dirs.

No new security-surface change introduced.
</review>

<review date="2026-05-28T00:00:00Z" model="claude-sonnet-4-6[1m]/medium" persona="style" verdict="pass" round="2">
The round-1 blocker is resolved. All three `ProfileError` variants in `patina-core/src/profile.rs` now place the `///` doc comment before `#[source]` on the inner `source` field: `PersistedRead` (lines 109-111), `RootRead` (lines 119-121), `RootParse` (lines 129-131). Ordering matches the `StateDirError::Io` reference pattern in `patina-core/src/state_dir.rs`. No other style issues introduced; no suppression annotations, no dead code, no naming drift.
</review>
