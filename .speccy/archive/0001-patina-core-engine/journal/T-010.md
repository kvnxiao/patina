---
spec: SPEC-0001
task: T-010
generated_at: 2026-05-28T14:30:00Z
---

<implementer date="2026-05-28T14:30:00Z" model="claude-opus-4.8[1m]/high" round="1">
- Completed: Landed `patina_core::journal` (new `patina-core/src/journal/`
  module: `mod.rs`, `plan.rs`, `progress.rs`, `sync.rs`) owning the
  binary plan file and the per-operation progress cursor (REQ-011,
  REQ-012). `Plan` / `PlannedOperation` are `serde`-derived, encoded via
  `postcard` and prefixed with a fixed 2-byte little-endian `u16`
  version envelope at offset 0; `FILE_MAJOR_VERSION = 1`.
  `Plan::read_envelope_version` reads the major without invoking the
  full decoder, and `Plan::decode` refuses any plan whose major exceeds
  the compiled version, returning the typed
  `JournalError::VersionMismatch { found, supported }` whose `Display`
  names both numbers. `Journal::flush_plan_and_fsync` serializes the
  plan, writes `<ts>.plan`, then fsyncs the plan file followed by the
  journal directory — both before returning, so the durability point
  precedes any mutation. `Journal::record_progress` appends a
  fixed-width `(u32 index, completion-marker byte)` record to
  `<ts>.progress` and is deliberately never fsync'd (REQ-012);
  `ProgressCursor::decode_completed` reads back whole records and
  discards a torn trailing record (the at-most-one-op crash lag).
  `Journal::commit` writes `<ts>.COMMIT`, fsyncs it and the dir, then
  deletes the `.plan` and `.progress` files (delete-after-COMMIT
  ordering). The `Syncer` trait + `OsSyncer` is the syscall-counting
  test hook the task body asks for: T-011 substitutes a recording fake
  to assert the fsync shape. Wired `pub mod journal` and re-exports
  (`Journal`, `JournalError`, `Plan`, `PlannedOperation`) into
  `patina-core/src/lib.rs`, and threaded
  `EngineError::Journal(#[from] JournalError)` into
  `patina-core/src/error.rs`. Added the integration suite
  `patina-core/tests/journal_fsync_shape.rs` (5 tests) plus 8 in-module
  unit tests (5 in `plan.rs`, 3 in `progress.rs`) and 1 module doctest.
  Added `postcard` to `[workspace.dependencies]` and `patina-core`.

- Undone: The end-to-end `patina apply --yes` framing of CHK-022 /
  CHK-023 (kill the live process, run a real executor loop) is
  deferred by design: the `apply` subcommand lands in T-016, the
  executor mutation loop in T-014, and the SIGKILL/`TerminateProcess`
  crash-and-reconcile harness in T-011 — which is also the consumer of
  the `Syncer` hook and `ProgressCursor::decode_completed` provided
  here. T-010 owns and proves the journal mechanism; the orphan-plan
  recovery pass (probing the filesystem, reversing completed ops) is
  explicitly T-011's scope and not started here. The progress record's
  completion-marker byte is the minimal field T-010 needs; T-011 may
  extend the record once it knows what recovery wants to read back.

- Hygiene checks: `cargo test --workspace` exited 0 (all suites green,
  including the 8 new journal unit tests, the 5 `journal_fsync_shape`
  integration tests, and the new journal module doctest). `cargo clippy
  --workspace --all-targets --all-features -- -D warnings` exited 0
  (findings fixed along the way: `expect_used` on the integration
  crate's helper fns — added the documented crate-level
  `#![expect(clippy::expect_used, reason = ...)]`; `indexing_slicing`
  on byte-buffer writes — replaced array indexing with destructuring
  `let [b0,b1,b2,b3] = ...` in `progress.rs` and `.get(..)`/`.get_mut(..)`
  in tests; `panic`/`unreachable` in a test match arm — rewritten as an
  `assert!(matches!(..))`; `let_underscore_must_use` and
  `assertions_on_result_states` — replaced with `drop(...)` and
  `.expect(...)`). `cargo +nightly fmt --all --check` exited 0 (after a
  `cargo +nightly fmt --all` pass). `cargo deny check` exited 0
  (advisories / bans / licenses / sources ok) — see Discovered issues
  for the postcard feature-flag fix that was required to reach 0.

- Evidence: red-then-green paper trail at
  `.speccy/specs/0001-patina-core-engine/evidence/T-010.md`.
  Roll call for the CHKs under REQ-011 / REQ-012:
  - CHK-022 (post-flush: exactly one `<ts>.plan`, no `<ts>.COMMIT`):
    demonstrated → evidence Scenario 1 covers the red (no journal
    module at HEAD) versus green
    (`after_flush_plan_exists_with_no_commit_sentinel` asserts the
    crash-window filesystem state plus that the flushed plan bytes
    decode back identically, i.e. the flush was durable). The
    end-to-end `patina apply --yes` + SIGKILL framing is deferred to
    T-011/T-014/T-016, which build the live-process surface.
  - CHK-023 (fsync shape: plan + dir + COMMIT, zero per-op progress):
    demonstrated → evidence Scenario 2 covers
    `three_op_apply_fsyncs_plan_dir_commit_but_never_progress`, which
    counts fsyncs through the recording `Syncer` and asserts exactly
    one `.plan` fsync, one `.COMMIT` fsync, two dir fsyncs, and zero
    `.progress` fsyncs.
  No CHK in T-010's scope is `hygiene` or `judgment-only`; both are
  scriptable and demonstrated. (Evidence Scenarios 3 and 4 prove the
  task-body version-envelope and delete-after-COMMIT scenarios, which
  back the REQ-011 version-envelope and delete-after-COMMIT
  `<done-when>` clauses beyond the two formal CHKs.)

- Discovered issues: (1) `postcard`'s default features enable a
  `heapless` integration that pulls in `heapless` → `atomic-polyfill`,
  which carries the unmaintained advisory RUSTSEC-2023-0089 and made
  `cargo deny check` fail. Fixed by declaring
  `postcard = { default-features = false, features = ["use-std"] }` —
  `use-std` alone provides `to_stdvec` / `from_bytes` without the
  `heapless` chain. (2) Cross-platform: a read-only file handle cannot
  be `FlushFileBuffers`-flushed on Windows (`ERROR_ACCESS_DENIED`,
  code 5); the integration suite failed on the dev Windows host until
  `OsSyncer::sync_file` was changed to open with
  `OpenOptions::write(true)` (no truncation). The directory fsync is
  best-effort on Windows (directory handles reject the flush), which is
  sound because the preceding file fsync already makes the write
  durable. Both are documented inline in `sync.rs`. No pre-existing
  out-of-scope problems were noticed.

- Procedural compliance: this implementer entry lands directly in
  `journal/T-010.md` per the journal-file schema; no `<implementer-note>`
  was written into TASKS.md (the parser rejects that element). The
  TASKS.md `state="..."` attribute for T-010 flips from `in-progress`
  to `in-review` as the final step of this turn, after all four hygiene
  gates returned 0. No shipped skill bodies under `.claude/skills/`
  were edited during this task.
</implementer>

<review persona="business" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-05-28T18:06:21Z" round="1">
T-010 faithfully delivers the REQ-011/REQ-012 journal mechanism the SPEC commits to. Every `<done-when>` clause maps to code and a passing test: the single up-front plan+dir fsync before mutation (`Journal::flush_plan_and_fsync`), the offset-0 `u16` version envelope decodable without postcard (`Plan::read_envelope_version`), refusal of newer majors with both versions named (`JournalError::VersionMismatch`), delete-plan-only-after-fsync'd-COMMIT ordering (`Journal::commit`), byte-identical encoding modulo filename timestamp (`same_plan_encodes_to_identical_bytes`), and the non-fsync'd progress cursor with a syscall-counting `Syncer` hook proving zero per-op progress fsyncs (CHK-023). 13/13 journal tests pass.

The CHK-022/CHK-023 end-to-end `patina apply --yes` + SIGKILL framing is demonstrated at the module layer rather than via the live process, but this is a legitimate dependency-ordering deferral, not scope-dodging: the SPEC's own task decomposition places the `apply` subcommand (T-016), the executor mutation loop (T-014), and the kill-and-reconcile harness (T-011) in later tasks, and the T-010 task body scopes only the `journal` module plus the test hook "so T-011 can assert no per-op fsync." The observable behaviour each scenario names is fully exercised against the layer T-010 owns, disclosed transparently. No non-goal breached (the binary `u16` envelope is what REQ-011 asks for, not the prohibited JSON schema-version field; `PlannedOperation` is minimal and `#[non_exhaustive]`). No open question silently resolved.
</review>

<review persona="tests" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-05-28T18:06:21Z" round="1">
T-010's tests genuinely exercise the journal behavior the task owns. Evidence roll call is complete: CHK-022 and CHK-023 both labelled `demonstrated` with named tests that appear in the diff; evidence shows real, scoped runner output (`cargo test -p patina-core --test journal_fsync_shape`) with a credible red (module absent at HEAD) → green transition and no fabrication pattern matched. Both CHKs are phrased end-to-end (`patina apply --yes` + process kill), a surface that does not exist until T-014/T-016/T-011; the implementer correctly defers the live-kill framing and proves the load-bearing journal properties at the layer T-010 produces them — `after_flush_plan_exists_with_no_commit_sentinel` asserts CHK-022's exact crash-window filesystem state, and `three_op_apply_fsyncs_plan_dir_commit_but_never_progress` asserts CHK-023's exact fsync shape (one `.plan`, one `.COMMIT`, two dir, zero `.progress`). `RecordingSyncer` replaces only the fsync syscall boundary while issuing real fsyncs; the production flush/commit/record_progress logic is the real code under test. Verified non-vacuity by mutation (neutering the version check turns the test red, then restored). All 5 integration + 8 unit journal tests pass.
</review>

<review persona="security" verdict="pass" model="claude-opus-4.8[1m]/medium" date="2026-05-28T18:06:21Z" round="1">
T-010 plan journal is clean from a security standpoint. The one real security item — postcard's default features pulling in the RUSTSEC-2023-0089-affected `atomic-polyfill` via `heapless` — was correctly remediated with `default-features = false, features = ["use-std"]`; independently verified `cargo deny check advisories` returns "advisories ok" and neither crate is in `patina-core`'s tree. No secret/variable material is written to the plan (only operation kind + source/target path strings; rendered template output and resolved variables are not journaled). Journal filenames derive from an internally-generated timestamp, not attacker-controlled input, so no path-traversal vector. COMMIT-before-delete ordering preserves the no-corrupt-state invariant. All `expect()` confined to `#[cfg(test)]`; production uses `?`. No auth surface in this local slice.
</review>

<review persona="style" verdict="blocking" model="claude-sonnet-4-6[1m]/medium" date="2026-05-28T18:06:21Z" round="1">
`patina-core/src/journal/progress.rs:83-84` — the two `filter_map` calls in `decode_completed` encode false optionality that cannot occur at runtime. `chunks_exact(RECORD_LEN)` (RECORD_LEN = 5) guarantees each chunk is exactly 5 bytes, so `record.get(..4)` is always `Some` and `<[u8; 4]>::try_from(4-byte-slice)` is always `Ok`. Using `filter_map` for both communicates to readers that these steps can silently discard records, which they cannot — contradicting the project's "clear intent over clever code" convention and the pattern already established in the same file's `record()` method, which uses `let [b0, b1, b2, b3] = op_index.to_le_bytes()` to express infallibility.

Fix: replace the two `filter_map` calls in `decode_completed` with a single `map` using direct destructuring/indexing, matching the idiom in `ProgressCursor::record`:

```rust
bytes
    .chunks_exact(RECORD_LEN)
    .filter(|record| record[4] == COMPLETED_MARKER)
    .map(|record| u32::from_le_bytes([record[0], record[1], record[2], record[3]]))
    .collect()
```

If Clippy's `indexing_slicing` fires, use the `let [b0, b1, b2, b3, _marker] = record.try_into()...` destructuring pattern (note: `.expect()` is forbidden in production, so prefer a pattern that does not require it — e.g. a `let [..]` array pattern bound directly on the `chunks_exact` item, which the compiler accepts since the length is statically known). Verify clippy/fmt/test stay green.
</review>

<blockers date="2026-05-28T18:06:21Z" round="1">
Remove the false-optionality `filter_map` chain in the progress-cursor decode path before the next review pass.
- style: `patina-core/src/journal/progress.rs:83-84` — `decode_completed` uses two `filter_map` calls (`record.get(..4)` and `<[u8; 4]>::try_from(idx).ok()`) that can never return `None`/`Err` because `chunks_exact(RECORD_LEN=5)` guarantees 5-byte chunks. This communicates false optionality and contradicts "clear intent over clever code". Replace with a single `map` using direct indexing/destructuring that mirrors the `ProgressCursor::record` idiom (`let [b0, b1, b2, b3] = ...`). Do not introduce `.expect()` in production. Verify clippy/fmt/test stay green.
</blockers>

<implementer date="2026-05-28T19:30:00Z" model="claude-opus-4.8[1m]/medium" round="2">
- Completed: Addressed the round-1 style blocker on
  `decode_completed`. Replaced the three-stage filter/`filter_map`
  chain — `.filter(|record| record.get(4) == Some(&COMPLETED_MARKER))`,
  `.filter_map(|record| record.get(..4))`,
  `.filter_map(|idx| <[u8; 4]>::try_from(idx).ok())` — with a single
  `filter_map` that destructures the `chunks_exact` item via an
  irrefutable 5-byte slice pattern: `[b0, b1, b2, b3, COMPLETED_MARKER]`
  matches a completed record and yields
  `u32::from_le_bytes([*b0, *b1, *b2, *b3])`; any other 5-byte chunk
  (a non-completed marker) returns `None`. This removes the false
  optionality the reviewer flagged: there is no more `.get(..4)` /
  `.try_from(..).ok()` that can never fail, and the 4-byte extraction
  is now a direct destructure mirroring `record`'s
  `let [b0, b1, b2, b3] = op_index.to_le_bytes()` idiom. The only
  remaining `None` arm is the genuine runtime decision (marker not
  equal to `COMPLETED_MARKER`), not false optionality. No `.expect()`
  was introduced; the slice pattern is statically known to be 5 bytes
  so `indexing_slicing` does not fire. Added an inline comment noting
  the irrefutability and that the torn-tail remainder is left in
  `chunks_exact`'s remainder and never produced.

- Undone: Nothing newly deferred this round. The end-to-end
  `patina apply --yes` + SIGKILL framing of CHK-022/CHK-023 remains
  deferred to T-011/T-014/T-016 exactly as recorded in round 1; this
  amend touched only the decode path's expression of intent and
  changed no observable behaviour, file format, or test surface.

- Hygiene checks: `cargo test --workspace` exited 0 (all suites green,
  including the 3 `journal::progress` unit tests — notably
  `torn_trailing_record_is_discarded`, which proves the rewritten
  decode still drops the partial tail via `chunks_exact`'s remainder).
  `cargo clippy --workspace --all-targets --all-features -- -D warnings`
  exited 0 (no `indexing_slicing`, no `expect_used` — the slice
  pattern avoids both). `cargo +nightly fmt --all --check` exited 0.
  `cargo deny check` exited 0 (advisories / bans / licenses / sources
  ok; the `license-not-encountered` lines are pre-existing config
  warnings, not failures).

- Evidence: the round-1 red-then-green paper trail at
  `.speccy/specs/0001-patina-core-engine/evidence/T-010.md` still
  holds — this amend is behaviour-preserving (identical decode
  output for every input), so no new red/green pair was produced.
  Roll call for the CHKs under REQ-011 / REQ-012 (unchanged from
  round 1):
  - CHK-022 (post-flush: exactly one `<ts>.plan`, no `<ts>.COMMIT`):
    demonstrated → `after_flush_plan_exists_with_no_commit_sentinel`
    asserts the crash-window filesystem state and that the flushed
    plan bytes decode back identically. Untouched by this amend.
  - CHK-023 (fsync shape: plan + dir + COMMIT, zero per-op progress):
    demonstrated →
    `three_op_apply_fsyncs_plan_dir_commit_but_never_progress` counts
    fsyncs through the recording `Syncer`. Untouched by this amend.
  The `decode_completed` rewrite is covered by the existing three
  `journal::progress` unit tests
  (`records_decode_in_append_order`, `each_record_is_fixed_width`,
  `torn_trailing_record_is_discarded`), which gate decode order,
  fixed-width framing, and torn-tail discard respectively.

- Discovered issues: none. The fix was a localized expression-level
  rewrite of a single method body; no new dependency, format, or
  cross-platform concern surfaced.

- Procedural compliance: this round-2 entry is appended after the
  round-1 `<implementer>` and `<blockers>` blocks in
  `journal/T-010.md`; no earlier block was modified (append-only
  audit trail). The TASKS.md `state="..."` attribute for T-010 was
  flipped `pending → in-progress → in-review` over this turn, with
  the flip to `in-review` gated on all four hygiene checks returning
  0. The dirty WIP from round 1 was amended in place — no
  `git restore`/`clean`/`checkout` and no reset. No shipped skill
  bodies under `.claude/skills/` were edited.
</implementer>

<review persona="business" verdict="pass" model="claude-opus-4.8[1m]/medium" date="2026-05-28T18:11:31Z" round="2">
T-010 round-2 preserves the REQ-011/REQ-012 business contract intact; the only change since the round-1 business pass was a behavior-preserving style rewrite of `decode_completed` in `patina-core/src/journal/progress.rs` (identical decode output, gated by the unchanged `torn_trailing_record_is_discarded`/`records_decode_in_append_order` tests), which touches no `<done-when>` clause, file format, or test surface. Every REQ-011 `<done-when>` still maps to code: single up-front plan+dir fsync before any mutation (`Journal::flush_plan_and_fsync`), offset-0 `u16` version envelope decodable without postcard (`Plan::read_envelope_version`), refusal of newer majors naming both versions (`JournalError::VersionMismatch`), delete-plan-only-after-fsync'd-COMMIT ordering (`Journal::commit`). REQ-012's non-fsync'd cursor and filesystem-probing-is-source-of-truth are honoured and documented. No non-goal breached — the binary `u16` envelope is what REQ-011 names, not the prohibited JSON schema-version field. The CHK-022/CHK-023 end-to-end `patina apply --yes` + SIGKILL framing remains deferred to T-011/T-014/T-016 per the SPEC's own task decomposition, proven at the journal layer T-010 owns. No open question silently resolved.
</review>

<review persona="tests" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-05-28T18:11:31Z" round="2">
Round-2 change (behaviour-preserving rewrite of `decode_completed` to remove the round-1 false-optionality `filter_map`) does not regress the tests for the CHKs T-010 owns. Evidence roll call is complete: CHK-022 and CHK-023 both `demonstrated`, each citing a real named test present in the diff, with scoped per-test runner output and a credible module-absent-at-HEAD red → green transition; no fabrication pattern matched. CHK-022 (`after_flush_plan_exists_with_no_commit_sentinel`) asserts the exact crash-window filesystem state plus durable round-trip; CHK-023 (`three_op_apply_fsyncs_plan_dir_commit_but_never_progress`) asserts the exact fsync shape. `RecordingSyncer` issues real fsyncs while counting them — it does not mock away the production flush/commit/record_progress logic. Re-ran the three scoped `journal::progress` unit tests (green) confirming they still gate decode order, fixed-width framing, and torn-tail discard after the rewrite. Non-blocking observation flagged for T-011: `decode_completed`'s `COMPLETED_MARKER` discrimination branch is not exercised by any assertion (the writer only ever emits `COMPLETED_MARKER`); the branch is dead until T-011 introduces a non-completed marker path, and T-011's tests should drive a non-`COMPLETED_MARKER` record to lock the discrimination.
</review>

<review persona="security" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-05-28T18:11:31Z" round="2">
T-010 round 2 is clean from a security standpoint. The round-2 amend touched only `decode_completed` in `patina-core/src/journal/progress.rs`; the rewritten `chunks_exact(RECORD_LEN)` + total-match decode is bounds-safe (torn tail goes to the remainder and is never produced — gated by `torn_trailing_record_is_discarded`), with no panic/`unwrap`/`expect`/OOB path. The round-1 security item still holds: the RUSTSEC-2023-0089 (`atomic-polyfill` via `heapless`) chain is remediated by `postcard = { default-features = false, features = ["use-std"] }`; independently verified `atomic-polyfill`/`heapless` are absent from `Cargo.lock` and `cargo deny check advisories` returns "advisories ok". Untrusted-input decode is defended: `Plan::decode` reads the version envelope and refuses newer majors before invoking postcard, and truncation is handled via bounds-checked `.get(..)`. No secret or resolved-variable material is journaled (only operation kind + source/target path strings); journal filenames derive from an internal timestamp, not attacker input. COMMIT-before-delete ordering preserves the no-corrupt-state invariant, and the Windows directory-fsync fallback is backstopped by the preceding file fsync and propagates genuine I/O errors. No auth surface in this local slice.
</review>

<review persona="style" verdict="pass" model="claude-sonnet-4-6[1m]/medium" date="2026-05-28T18:11:31Z" round="2">
The round-1 blocker is resolved. `decode_completed` in `patina-core/src/journal/progress.rs` now uses a single `.filter_map` with a 5-byte slice pattern `[b0, b1, b2, b3, COMPLETED_MARKER]` that handles both the genuine runtime filter (non-completed marker byte → `None`) and the byte extraction via destructuring, mirroring the `let [b0, b1, b2, b3] = op_index.to_le_bytes()` idiom in `record()`. The false-optionality `.get(..4)` and `<[u8; 4]>::try_from(idx).ok()` chains are gone. No `.expect()` was introduced. The inline comment correctly documents the irrefutability of the `chunks_exact(RECORD_LEN=5)` slice. No new style issues introduced by the round-2 change.
</review>
