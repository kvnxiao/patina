---
spec: SPEC-0001
task: T-023
generated_at: 2026-05-29T04:47:32Z
---

<implementer date="2026-05-29T04:47:32Z" model="claude-opus-4-8[1m]/medium" round="1">
- Completed: Added the workspace `disallowed-macros` clippy gate
  (REQ-026). `clippy.toml` now lists `std::println`, `std::eprintln`,
  `std::print`, and `std::eprint` under `disallowed-macros` with a
  comment pointing at the `output::Reporter` rationale. Carved out the
  three legitimate test-harness IPC call sites in
  `patina-core/examples/lock_helper.rs` (the two `println!` for
  `ACQUIRED`/`RELEASED` and the `eprintln!` in `main`) with scoped
  `#[expect(clippy::disallowed_macros, reason = "...")]` blocks — these
  talk to the parent test process over stdout/stderr and are not
  user-facing CLI output, so they have no `Reporter` to route through.
  Added the integration test
  `patina-cli/tests/clippy_disallowed_macros.rs` with three cases:
  (1) the real workspace `clippy.toml` lists all four macros;
  (2) a planted `println!("hi")` in a throwaway crate (file named
  `plan.rs`, reusing the real workspace `clippy.toml`) fails
  `cargo clippy --message-format=json -- -D warnings` with a
  `clippy::disallowed_macros` diagnostic naming the file; (3) a
  non-listed `tracing`-style macro plus a scoped `#[expect]` carve-out
  pass clippy clean with zero `disallowed_macros` spans. Added `toml`
  to `patina-cli` dev-dependencies (already in the workspace dep table
  and lockfile) for the config-parse assertion.

- Undone: Nothing in T-023's scope is deferred. The `Reporter` trait
  and its `HumanReporter`/`JsonReporter` (here `StreamReporter` /
  `BufferReporter`) implementations were stood up by T-016/T-021; this
  task only adds the clippy gate per its `<task>` body.

- Hygiene checks: `cargo test --workspace` exited 0 (all suites pass,
  including the `patina-core` doc-tests that use `println!` — `clippy.toml`
  governs clippy only, not `cargo test`). `cargo clippy --workspace
  --all-targets --all-features -- -D warnings` exited 0.
  `cargo +nightly fmt --all --check` exited 0 (after running
  `cargo +nightly fmt --all` to absorb two line-wrap reformats in the
  new test). `cargo deny check` exited 0 (no advisories/bans/license/
  source failures; `toml` was already a resolved dependency).

- Evidence: red-then-green paper trail at
  `.speccy/specs/0001-patina-core-engine/evidence/T-023.md`. Roll call
  for the CHKs under REQ-026:
  - CHK-053 (workspace clippy exits 0 AND clippy.toml lists the four
    macros): demonstrated → evidence Scenario 1 covers the
    green workspace clippy run; the config-list half is gated by the
    `clippy_toml_lists_all_four_print_macros` test in
    `patina-cli/tests/clippy_disallowed_macros.rs`, which parses the
    real file and asserts each of the four entries is present.
  - CHK-054 (planted `println!` fails clippy naming the file + line):
    demonstrated → evidence Scenario 1 captures the red baseline
    (three real sites in `lock_helper.rs` flagged with file+line) and
    the permanent
    `raw_println_outside_output_module_fails_clippy_naming_the_file`
    test asserts a `disallowed_macros` diagnostic naming `src/plan.rs`.
  - CHK-055 (byte-identical stdout across two `JsonReporter`
    invocations): hygiene → this is a REQ-021 determinism property
    landed under T-021, unchanged by this task; covered by
    `patina-cli/tests/deterministic_stdout.rs` under
    `cargo test --workspace`.

- Discovered issues: The T-023 `<task>` body and REQ-026's third
  done-when bullet describe the `output` module as bearing a
  module-scoped `#[allow(clippy::disallowed_macros)]` carve-out. In
  practice the `output` module (`patina-cli/src/output/reporter.rs`)
  emits via `write_all` / `writeln!` to locked stdout/stderr handles,
  not via the four banned macros, so it needs no carve-out at all — and
  the workspace denies `allow_attributes` / `allow_attributes_without_reason`,
  so the only legal suppression form is `#[expect(..., reason = "...")]`,
  which would itself fail `-D warnings` as an unfulfilled expectation
  if placed on a module that never triggers the lint. I followed
  REQ-026's intent (the four macros are denied outside the sanctioned
  output sink) over the task body's literal `#[allow]` wording: the
  only real carve-out the workspace needs at HEAD is on the
  `lock_helper` test-harness example, which is exactly where it now
  lives. No SPEC.md edit was made (locked).

- Procedural compliance: This implementer entry lands directly in
  `journal/T-023.md` per the journal-file schema; no
  `<implementer-note>` was written into TASKS.md. The TASKS.md
  `state="..."` attribute for T-023 flips from `in-progress` to
  `in-review` as the final step of this turn. No shipped skill bodies
  under `.claude/skills/` or `.claude/agents/` required edits.
</implementer>

<review persona="business" verdict="pass" model="claude-opus-4-8[1m]/high" date="2026-05-29T04:51:47Z" round="1">
T-023 satisfies REQ-026's binding surface. All three CHK scenarios hold: CHK-053 (workspace clippy exits 0 + `clippy.toml` lists `std::println`/`std::eprintln`/`std::print`/`std::eprint` under `disallowed-macros`, verified at `clippy.toml:11-16` and gated by `clippy_toml_lists_all_four_print_macros`); CHK-054 (planted `println!` in `plan.rs` fails clippy naming the file, gated by `raw_println_outside_output_module_fails_clippy_naming_the_file` at `patina-cli/tests/clippy_disallowed_macros.rs:145`); CHK-055 (byte-identical JsonReporter stdout) correctly attributed to the REQ-021/T-021 determinism property and unchanged here. All five REQ-026 `<done-when>` bullets map to the diff.

The one deviation from the literal task-body wording — the body prescribes a module-scoped `#[allow(clippy::disallowed_macros)]` on the `output` module, which the implementer did not add — is the correct business call, not a gap. Verified at `patina-cli/src/output/reporter.rs:55-85`: `StreamReporter` emits via `write_all`/`writeln!`, never the four banned macros, so it needs no carve-out. And the workspace denies `allow_attributes`/`allow_attributes_without_reason` (`Cargo.toml:99-100`), so a literal `#[allow]` would itself break CHK-053's "clippy exits 0" promise and an `#[expect]` would fail as unfulfilled. The task-body instruction is thus internally inconsistent with the SPEC's own hard rules; honouring REQ-026's done-when over the impossible task-body carve-out is right. The implementer disclosed the deviation in the journal "Discovered issues" block and did not edit the locked SPEC. No non-goal breached (diff adds only the gate + test), no goal unserved, no open question silently decided.

Non-blocking note for a future `/speccy-amend`: the task-body `output`-module carve-out scenario (TASKS.md:1789-1794) is now dead against this implementation; it was never promoted into a CHK, so `speccy verify` is unaffected, but the task-body prose still implies `#[allow]` is the mechanism.
</review>

<review persona="tests" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-05-29T04:51:47Z" round="1">
T-023 tests genuinely exercise REQ-026's behaviour; evidence roll call is complete and credible.

- Evidence loading: `Evidence:` field present in the round-1 `<implementer>` entry, file exists at `.speccy/specs/0001-patina-core-engine/evidence/T-023.md`, roll call covers all three CHKs under REQ-026 (CHK-053 demonstrated, CHK-054 demonstrated, CHK-055 hygiene with named test). No fabrication pattern matched — the red block shows three real `disallowed_macros` errors with file+line at `lock_helper.rs` then a `Finished` + exit-0 green block (materially different, not byte-identical), and the line offsets are consistent with the `#[expect]` carve-outs being inserted between red and green.
- CHK-053: `patina-cli/tests/clippy_disallowed_macros.rs::clippy_toml_lists_all_four_print_macros` parses the *live* `clippy.toml` and asserts all four literals — not a copied constant; dropping any entry fails it. Exits-0 half shown by the green workspace run.
- CHK-054: `raw_println_outside_output_module_fails_clippy_naming_the_file` compiles a scratch crate reusing the real `clippy.toml`, plants `println!` in `src/plan.rs`, runs clippy `-D warnings`, and asserts BOTH non-success AND a `clippy::disallowed_macros` span naming `plan.rs`. Mutation-resistant: an empty/incomplete disallowed list makes clippy exit 0 and yields no span, failing both asserts. It asserts the specific lint, not any error.
- CHK-055 (hygiene): `deterministic_stdout.rs::json_apply_is_byte_identical_across_two_runs` drives the real `JsonReporter` through two `--json` applies and asserts byte-equality; correctly a REQ-021 property unchanged by this task, with a named test cite.
- Verified the implementer's documented deviation from the task body's literal `#[allow]` wording: `patina-cli/src/output/**` uses no banned print macro (grep clean), and the workspace denies `allow_attributes`/`allow_attributes_without_reason` (Cargo.toml:99-100), so `#[expect(..., reason=...)]` is the only legal carve-out form. The retired output-module-carveout sub-scenario has no behaviour left to test; the actual `#[expect]` shape is exercised by `tracing_macro_and_scoped_expect_stay_clean`. The `info!` stub there is a sound proxy — patina controls the list contents (covered elsewhere), not clippy's path resolution.
</review>

<review persona="security" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-05-29T04:51:47Z" round="1">
No security concerns. T-023 is lint-config hardening plus a test: `clippy.toml` adds four `disallowed-macros` print-family entries, three narrowly-scoped `#[expect(clippy::disallowed_macros, reason=...)]` block carve-outs in the test-only `patina-core/examples/lock_helper.rs`, and a new `patina-cli/tests/clippy_disallowed_macros.rs`. No auth/authz, network, secret, crypto, or production input-parsing surface is touched. The test's `cargo clippy` subprocess uses a fixed arg array with no shell and no attacker-controlled interpolation, and stays hermetic inside a `TempDir` with a scoped `CARGO_TARGET_DIR` (no checked-in-tree writes, no path traversal). The carve-outs are per-block, not a module-wide `#[allow]`, so the print ban that keeps user-facing output funneled through the audited `output::Reporter` is not re-opened for production code — the change tightens, not loosens, the surface where data could leak to stdout/stderr. `toml` is an existing resolved workspace dep (`0.8`), so no new dependency/CVE surface.
</review>

<review persona="style" verdict="pass" model="claude-sonnet-4-6[1m]/medium" date="2026-05-29T04:51:47Z" round="1">
All changes comply with project conventions. `clippy.toml` carries a rationale comment pointing at REQ-026 and `output::Reporter`. The three `#[expect(clippy::disallowed_macros, reason = "...")]` blocks in `lock_helper.rs` are scoped tightly around the one call site each and carry a justification referencing REQ-026, matching the "suppressions carry a justification" rule. The new test file `clippy_disallowed_macros.rs` opens with `#![expect(clippy::expect_used, reason = "...")]` — identical pattern to every sibling `tests/*.rs` file. Imports passed `cargo +nightly fmt --all --check` (implementer reports exit 0) and `group_imports = "One"` in `.rustfmt.toml` produces a single unsorted group, so the mixed-looking ordering is formatter-correct. `toml` added to `[dev-dependencies]` via `workspace = true` is consistent with workspace dep table conventions. No unused imports, no dead code, no parallel reimplementations of existing helpers.
</review>
