---
task: T-003
spec: SPEC-0001
generated_at: 2026-05-27T00:00:00Z
---

<implementer date="2026-05-27T00:00:00Z" model="claude-opus-4-7[1m]" round="1">

- Completed:
  - Added `patina_core::discovery` with two submodules: `repo` (REQ-003
    repository-root resolution) and `modules` (REQ-004 module
    enumeration). Both share a `read_manifest_head` helper that parses
    only the `[patina]` table head of a `patina.toml` (full `[[file]]`
    / `[[hook]]` schemas are T-004's scope).
  - `discovery::repo`: `resolve_repository_root()` tries `PATINA_REPO`,
    then walks up from the current working directory, then reads the
    persisted-default file under the per-machine state directory.
    The triple-fail error variant `RepoDiscoveryError::AllSourcesFailed`
    renders all three attempted sources in its Display impl so CHK-007's
    substring contract holds.
  - To avoid pre-empting T-005 (state-directory resolution) while still
    being able to attempt source 3, the OS-specific state-directory
    layout is computed inline inside `repo.rs` via per-target `cfg`s
    (XDG_STATE_HOME / HOME on Linux, HOME/Library/... on macOS,
    LOCALAPPDATA on Windows). T-005 will land a shared
    `state_dir::resolve()`; this module can switch to it without API
    churn.
  - Production entrypoint is `resolve_repository_root()` (no args,
    reads process state). A `resolve_repository_root_with(env, cwd,
    persisted)` test seam is exposed for integration tests so the
    env-var race that comes with parallel test execution is avoided.
  - `discovery::modules`: `discover_modules(root)` validates the root
    manifest declares `root = true`, walks immediate subdirectories,
    rejects depth-≥2 manifests via a recursive structural scan, and
    returns `Vec<ModuleHandle>` ordered alphabetically by directory
    name with absolute paths. Subdirectories without a `patina.toml`
    (e.g. `.git/`) are silently skipped — not every directory is a
    module.
  - `EngineError` grows two `#[from]` variants — `RepoDiscovery` and
    `ModuleDiscovery` — so the discovery types propagate cleanly
    through the engine's public API.
  - Workspace deps added: `camino` (with `serde1`), `fs-err`, `serde`
    (with `derive`), `toml`. Test dep: `tempfile`. All four are
    permissive-licensed and clear `cargo deny check`.
  - Integration tests at `patina-core/tests/repo_discovery.rs`
    (6 tests) and `patina-core/tests/module_discovery.rs` (5 tests)
    cover every CHK plus robustness cases. The tests use the
    `resolve_repository_root_with` seam to inject env/cwd/persisted
    values explicitly; one additional case exercises the persisted-
    default source positively.
- Undone:
  - The `--repo` clap flag-scan task scenario is not exercised here.
    No clap surface exists yet in `patina-cli/src/main.rs` (T-016 wires
    it). The scenario passes trivially today (no `--repo` flag exists)
    and will be re-verified naturally when T-016 introduces the
    `clap`-derived `Cli` struct. This scenario is not enumerated as a
    CHK in `speccy check SPEC-0001/T-003` (only CHK-005…CHK-009 are
    rendered), so it is judgment-only at this phase.
  - The persisted-default state-directory layout is duplicated between
    `discovery::repo` and the eventual `state_dir::resolve()` T-005
    will land. The duplication is intentional and called out in
    `discovery/mod.rs`; T-005 will collapse it.
- Hygiene checks:
  - `cargo test --workspace`: exit 0 (11 new integration tests pass;
    0 pre-existing tests still pass).
  - `cargo clippy --workspace --all-targets --all-features -- -D warnings`:
    exit 0.
  - `cargo +nightly fmt --all --check`: exit 0.
  - `cargo deny check`: exit 0 (same three pre-existing
    `license-not-encountered` warnings for `Unicode-DFS-2016`, `Zlib`,
    and `MPL-2.0` persist from baseline; non-blocking).
- Evidence: no separate evidence file written. Discovery is a pure
  library task with five CHKs all backed by direct integration tests
  under `patina-core/tests/`. Roll call for CHKs under REQ-003 and
  REQ-004:
  - CHK-005 (PATINA_REPO resolves repo root from unrelated CWD):
    demonstrated → `env_var_resolves_repository_root` in
    `patina-core/tests/repo_discovery.rs` constructs a tempdir with a
    root manifest, sets `env_value=Some(repo)`, sets `cwd` to an
    unrelated tempdir, asserts resolution equals the canonicalized
    repo path.
  - CHK-006 (walk-up from T/zsh/ finds T/patina.toml):
    demonstrated → `walk_up_finds_root_from_subdirectory` in
    `repo_discovery.rs` creates `T/patina.toml` (root) plus
    `T/zsh/`, passes `cwd=T/zsh`, asserts resolution equals T.
  - CHK-007 (triple-fail error names PATINA_REPO, walk-up, persisted
    default): demonstrated →
    `all_sources_failing_names_each_source` in `repo_discovery.rs`
    asserts the rendered error string contains all three required
    substrings and matches `RepoDiscoveryError::AllSourcesFailed`.
  - CHK-008 (discovers {nvim, zsh} alphabetically with absolute
    paths): demonstrated →
    `discovers_modules_alphabetically_with_absolute_paths` in
    `patina-core/tests/module_discovery.rs` constructs the
    SPEC-described layout and asserts both the ordered name vec and
    that each module path is absolute and rooted under the
    canonicalized tempdir.
  - CHK-009 (depth-2 manifest rejected with `maximum module depth`
    phrase naming the offending path): demonstrated →
    `rejects_manifest_at_depth_two` in `module_discovery.rs`
    constructs `T/zsh/plugins/patina.toml`, asserts the error
    matches `MaximumModuleDepth` and its Display contains both the
    offending path (in either POSIX or Windows separator form) and
    the phrase `maximum module depth`.
- Discovered issues:
  - The task body's 4th scenario (clap `--help` output contains no
    `--repo`) cannot be empirically verified at this phase because
    T-016 has not landed clap. It is not enumerated as a CHK by
    `speccy check`. Logged as judgment-only above; T-016's
    implementation should preserve the no-`--repo` invariant.
  - The `result_large_err` clippy lint required boxing
    `toml::de::Error` inside the `Parse` variants of both
    `ManifestHeadError` and `ModuleDiscoveryError`. Cheap to fix; no
    behaviour change.
  - The `expect_used` lint fires on integration tests under
    `tests/*.rs` because `allow-expect-in-tests = true` only covers
    items inside `#[cfg(test)]` modules — integration-test crates
    sit outside that scope. Resolved with a file-level
    `#![expect(clippy::expect_used, reason = "...")]` on each
    test file. The deeper fix (a shared `tests/common/` harness
    returning `Result`) belongs to a later test-harness task, not
    here.
- Procedural compliance: clean working tree at entry
  (`git status --porcelain` empty); first-attempt shape (no prior
  T-003 journal). State flipped `pending` → `in-progress` →
  `in-review` with all four hygiene gates green before the
  `in-review` flip. No edits to locked SPEC.md. Journal file is a
  sibling of SPEC.md / TASKS.md as required.

</implementer>

<review date="2026-05-28T03:30:31Z" model="claude-opus-4-7[1m]/medium" persona="business" verdict="pass" round="1">
T-003's discovery layer faithfully delivers REQ-003 and REQ-004 against every CHK rendered by `speccy check`.

REQ-003 coverage:
- CHK-005 (PATINA_REPO from unrelated CWD): `env_var_resolves_repository_root` in `patina-core/tests/repo_discovery.rs`.
- CHK-006 (walk-up from T/zsh): `walk_up_finds_root_from_subdirectory`.
- CHK-007 (triple-fail names PATINA_REPO / walk-up / persisted default): `all_sources_failing_names_each_source` plus the `AllSourcesFailed` `Display` template in `patina-core/src/discovery/repo.rs:35-48` that hard-codes the three required substrings.
- Plus the persisted-default source-3 path is positively exercised by `persisted_default_is_consulted_when_other_sources_fail` (not in CHK list but reinforces the SPEC's third-source contract).

REQ-004 coverage:
- CHK-008 (`{nvim, zsh}` alphabetical with absolute paths): `discovers_modules_alphabetically_with_absolute_paths` in `patina-core/tests/module_discovery.rs`.
- CHK-009 (`maximum module depth` rejection naming offending path): `rejects_manifest_at_depth_two`, with Windows-separator tolerance.
- Non-CHK `<done-when>` items also covered: `rejects_non_root_manifest_declaring_root_true`, `rejects_root_manifest_missing_root_key`.

Scope discipline checks:
- Top-level `<non-goals>` "no `--repo <path>` global flag" is respected — the diff introduces no `--repo` argument anywhere. The `--help` no-`--repo` task scenario is not empirically verifiable yet because the clap surface lives in T-016; the implementer correctly flagged this in the journal and the invariant holds trivially today (no flag parser exists to scan).
- No SPEC-0002 surface (`init`/`add`/`promote`/`doctor`) sneaks in. No nested-module support. No merge modes.
- All 16 SPEC open questions are already resolved per `## Open Questions`; the diff does not silently re-decide any.
- User-story 1 (fresh-laptop developer) directly depends on this discovery layer; the implementation routes through `PATINA_REPO` → walk-up → persisted default exactly as the user expects.

Edge-case sanity:
- Empty `PATINA_REPO` is treated as unset (REQ-003's plain-language intent), with a dedicated robustness test.
- Non-module subdirectories (`.git/`, scratch) are silently skipped — a reasonable judgment-only call that does not conflict with REQ-004's `<done-when>` (the SPEC only mandates rejecting depth-≥2 manifests and root-key-misuse cases, not classifying directory-without-manifest as an error).
- Intentional duplication of the state-dir layout between `discovery::repo` and the future `state_dir::resolve()` is explicitly called out in the journal and earmarked for T-005 to collapse — not a drift signal.

No blockers from a business-intent standpoint.
</review>

<review date="2026-05-28T03:30:31Z" model="claude-opus-4-7[1m]/medium" persona="tests" verdict="blocking" round="1">
Tests themselves are sound: all 5 covered CHKs (CHK-005 through CHK-009) are exercised by real integration tests in `patina-core/tests/repo_discovery.rs` and `patina-core/tests/module_discovery.rs` that drive `resolve_repository_root_with` and `discover_modules` against real tempdirs with real `patina.toml` fixtures. Re-ran both test files locally — 11 tests pass, no mocks of the system under test, assertions are tight (variant matching plus rendered-Display substring checks for CHK-007/CHK-009, alphabetical ordering and absolute-path assertions for CHK-008). The two task-scenarios beyond the CHK set (non-root declaring `root = true`; root missing `root = true`) also have proper variant + substring assertions.

Blocker is procedural, not substantive: the journal's `Evidence:` field says "no separate evidence file written" and no file exists at `.speccy/specs/0001-patina-core-engine/evidence/T-003.md`, while T-001 established the convention with `evidence/T-001.md`. Per the persona's evidence-loading contract step 3 ("Treat the absence of the `Evidence:` field, or the absence of the file at the referenced path, as `verdict="blocking"`"), this must be flipped back. The CHK roll call inside the journal is intact and labels every CHK as `demonstrated` with a specific test name and file path, so the fix is mechanical: write `.speccy/specs/0001-patina-core-engine/evidence/T-003.md` containing the existing roll call body (and ideally captured `cargo test --test repo_discovery --test module_discovery` output so the paper trail is self-contained), update the journal's `Evidence:` field to point at that path, and re-flip to `in-review`. No test rewrites required.
</review>

<review date="2026-05-28T03:30:31Z" model="claude-opus-4-7[1m]/medium" persona="security" verdict="pass" round="1">
Discovery is a pure library surface with no auth boundary, no network, no
secrets, no telemetry, and no untrusted-input channel — the threat model is
"user's own dotfiles repo on user's own machine."

Things checked and cleared:
- `PATINA_REPO`, `XDG_STATE_HOME`, `HOME`, `LOCALAPPDATA` are all read as
  path strings; empty-string values are filtered explicitly
  (`patina-core/src/discovery/repo.rs:95`, `:212`, `:216`, `:222`, `:232`).
  Paths are used as paths, never as shell or template input — no injection
  vector.
- The persisted-default file at `<state>/patina/default_repo` is read,
  trimmed, parsed as a path, and gated through `validate_root()`
  (`patina-core/src/discovery/repo.rs:243-251`). A maliciously-edited
  default cannot escalate beyond "point Patina at a directory the user can
  already read"; downstream apply still requires a valid root manifest.
- TOML head parse is the well-vetted `toml` crate against a minimal
  `ManifestHead` projection (`patina-core/src/discovery/mod.rs:35-46`); no
  arbitrary-deserialization surface.
- No panics in production paths: every `?` returns a typed error variant,
  `unwrap`/`expect` are confined to the test crates with a file-level
  `#![expect(clippy::expect_used, ...)]` per the implementer notes.
- Non-UTF-8 cwd / non-UTF-8 child paths surface as typed errors
  (`CwdNotUtf8`, `ModuleDiscoveryError::Io { InvalidData }`), not panics.
- Walk-up terminates deterministically at filesystem root via
  `dir.parent()` returning `None` (`repo.rs:193`).

Noted-but-out-of-scope (robustness, not security; user owns the data; not
blocking):
- `check_no_deep_manifests` in `patina-core/src/discovery/modules.rs:183`
  recurses without a hard depth cap and does not check
  `file_type.is_symlink()` before descending. A symlink cycle or a
  pathologically deep tree (`node_modules`, etc.) in the user's own repo
  could blow the stack or do a lot of unnecessary IO. SPEC-0001 doesn't
  require depth-cap or symlink-cycle guards in discovery, and the threat
  model is single-user trusted input, so this doesn't rise to a security
  blocker — flagging for the implementer's awareness in case a later task
  (T-009 / lexical-canonicalize) wants to harden it.

No blocking security issues for T-003.
</review>

<review date="2026-05-28T03:30:31Z" model="claude-sonnet-4-6[1m]/medium" persona="style" verdict="pass" round="1">
All conventions from AGENTS.md and the `.claude/rules/rust/` rule files are satisfied.

- `camino::Utf8Path`/`Utf8PathBuf` used throughout; `fs-err` used over `std::fs` at every call site.
- `thiserror` for library-layer errors; no `anyhow` in `patina-core`.
- No `unwrap()`, no `expect()` in production paths; `?` propagation used consistently.
- `#![expect(clippy::expect_used, reason = "...")]` in both integration test files carries a proper `reason` string as required by `allow_attributes_without_reason = "deny"`.
- No bare `#[allow(...)]` attributes introduced.
- Imports are one-per-line matching `imports_granularity = "Item"` from `.rustfmt.toml`; all new files have trailing newlines (byte-probed: `0a`).
- Dead-code check: both `use camino::Utf8Path` imports are used in helper function signatures; no unused imports.
- `fs_err` called by crate-path prefix in tests (no `use fs_err;` needed, valid Rust).
- The "every public function has a doc comment with at least one `# Examples`" guidance is aspirational (`missing_docs = "warn"` only) and the pre-existing T-001 public functions also omit examples — T-003 is consistent with the established codebase pattern.
</review>

<blockers date="2026-05-28T03:30:31Z" round="1">
Write the per-task evidence file at `.speccy/specs/0001-patina-core-engine/evidence/T-003.md` and update the round-1 `<implementer>` block's `Evidence:` field to point at it. The CHK roll call (CHK-005 through CHK-009) is already complete and accurate inside the journal — copy it verbatim into the evidence file and ideally capture the `cargo test --test repo_discovery --test module_discovery` runner output as the paper trail so the file is self-contained. No test rewrites required.
- tests (blocking): journal's `Evidence:` field reads "no separate evidence file written" with no file present at the canonical `evidence/T-003.md` path; this violates the evidence-loading contract that treats a missing referenced path as `verdict="blocking"`. T-001 established the precedent at `evidence/T-001.md`.
</blockers>

<implementer date="2026-05-27T00:00:00Z" model="claude-opus-4-7[1m]" round="2">

- Completed:
  - Wrote `.speccy/specs/0001-patina-core-engine/evidence/T-003.md`
    containing the round-1 CHK roll call verbatim plus the captured
    `cargo test --test repo_discovery --test module_discovery`
    runner output, matching the precedent set by `evidence/T-001.md`.
    No production-code changes this round; the round-1 implementation
    of `patina_core::discovery` (REQ-003 + REQ-004) is unchanged.
- Undone: nothing for T-003. The clap `--help` no-`--repo` scenario
  remains judgment-only until T-016, as noted in round 1.
- Hygiene checks:
  - `cargo test --workspace`: exit 0.
  - `cargo clippy --workspace --all-targets --all-features -- -D warnings`:
    exit 0.
  - `cargo +nightly fmt --all --check`: exit 0.
  - `cargo deny check`: exit 0 (same pre-existing
    `license-not-encountered` baseline warnings persist; non-blocking).
- Evidence: paper trail at
  `.speccy/specs/0001-patina-core-engine/evidence/T-003.md`.
  Roll call for CHKs under REQ-003 and REQ-004:
  - CHK-005 (PATINA_REPO resolves repo root from unrelated CWD):
    demonstrated → `env_var_resolves_repository_root` in
    `patina-core/tests/repo_discovery.rs`.
  - CHK-006 (walk-up from T/zsh/ finds T/patina.toml):
    demonstrated → `walk_up_finds_root_from_subdirectory` in
    `patina-core/tests/repo_discovery.rs`.
  - CHK-007 (triple-fail error names PATINA_REPO, walk-up, persisted
    default): demonstrated → `all_sources_failing_names_each_source`
    in `patina-core/tests/repo_discovery.rs`.
  - CHK-008 (discovers {nvim, zsh} alphabetically with absolute
    paths): demonstrated →
    `discovers_modules_alphabetically_with_absolute_paths` in
    `patina-core/tests/module_discovery.rs`.
  - CHK-009 (depth-2 manifest rejected with `maximum module depth`
    phrase naming the offending path): demonstrated →
    `rejects_manifest_at_depth_two` in
    `patina-core/tests/module_discovery.rs`.
- Discovered issues: none new this round.
- Procedural compliance: retry shape detected via journal
  (`<implementer round="1">` plus `<blockers round="1">`); amended
  WIP in place rather than resetting; did not edit the round-1
  `<implementer>` block; round number monotonically incremented from
  1 to 2. State flipped `pending` → `in-progress` → `in-review` with
  all four hygiene gates green before the `in-review` flip. No edits
  to locked SPEC.md. Evidence file is a sibling of SPEC.md /
  TASKS.md under `evidence/`, matching T-001's precedent.

</implementer>

<review date="2026-05-28T03:35:42Z" model="claude-opus-4-7[1m]/medium" persona="business" verdict="pass" round="2">
Round-2 retry is procedural-only — implementer wrote `.speccy/specs/0001-patina-core-engine/evidence/T-003.md` with the round-1 CHK roll call verbatim plus captured `cargo test --test repo_discovery --test module_discovery` runner output (11/11 passing). No production-code changes; the round-1 `patina_core::discovery` implementation covering REQ-003 (PATINA_REPO → walk-up → persisted-default ordering) and REQ-004 (flat depth-1 module enumeration with the three typed failure modes) is unchanged from the round-1 pass.

SPEC-vs-evidence cross-check still holds:
- CHK-005 / CHK-006 / CHK-007 (REQ-003) tied to `env_var_resolves_repository_root`, `walk_up_finds_root_from_subdirectory`, `all_sources_failing_names_each_source` in `patina-core/tests/repo_discovery.rs`.
- CHK-008 / CHK-009 (REQ-004) tied to `discovers_modules_alphabetically_with_absolute_paths` and `rejects_manifest_at_depth_two` in `patina-core/tests/module_discovery.rs`.
- Extra `<done-when>` items beyond the CHK set (non-root with `root = true`; root missing `root = true`) covered by `rejects_non_root_manifest_declaring_root_true` and `rejects_root_manifest_missing_root_key`.
- `<non-goals>` "no `--repo <path>` global flag" — diff introduces no `--repo` argument; the clap `--help` scan task scenario is correctly flagged as judgment-only until T-016 lands the parser and is not in the rendered CHK set. Evidence file's explicit note on this is honest and accurate.
- Open questions and user stories: untouched; the diff does not silently re-decide anything.

No business-intent blockers.
</review>

<review date="2026-05-28T03:35:42Z" model="claude-opus-4-7[1m]/medium" persona="tests" verdict="pass" round="2">
Round 2 cleanly resolves the round 1 procedural blocker. The evidence file at `.speccy/specs/0001-patina-core-engine/evidence/T-003.md` is now present, carries a complete CHK roll call (CHK-005 through CHK-009 all labelled `demonstrated` with specific test names and file paths), and includes captured `cargo test --test repo_discovery --test module_discovery` runner output that is structurally consistent with real Rust test framework emission (Windows-style separators matching the host, "Finished `test` profile" header, per-binary execution sections, per-test status lines, summary line with passed/failed/ignored/measured/filtered counts).

Fabrication checks all clear:
- Every test name in the evidence runner output appears verbatim in the actual test files (`patina-core/tests/repo_discovery.rs`; `patina-core/tests/module_discovery.rs`).
- Test counts match exactly: 6 in repo_discovery.rs, 5 in module_discovery.rs.
- Invocation is properly scoped to the slice (`--test repo_discovery --test module_discovery`), not the workspace-wide hygiene run.
- The runner output is plausibly noisy — not the suspiciously-clean prose summary that fabricated evidence tends to produce.

The underlying tests themselves remain sound from round 1: every CHK is exercised by a real integration test that drives `resolve_repository_root_with` / `discover_modules` against real tempdir fixtures with real `patina.toml` content. Assertions are tight (variant matching via `matches!` plus rendered `Display` substring checks for CHK-007 and CHK-009; ordered name vector plus absolute-path assertions for CHK-008). No mocking of the system under test. The judgment-only label on the clap `--help` no-`--repo` scenario is correct — that scenario genuinely cannot be exercised until T-016 lands the clap surface.
</review>

<review date="2026-05-28T03:35:42Z" model="claude-opus-4-7[1m]/medium" persona="security" verdict="pass" round="2">
No production code changed between rounds; round 2 only added `.speccy/specs/0001-patina-core-engine/evidence/T-003.md` and appended an `<implementer round="2">` journal block. The security surface of `patina_core::discovery` (REQ-003 + REQ-004) is identical to round 1, which already passed this persona.

Re-confirmed against the unchanged round-1 implementation:
- Env vars (`PATINA_REPO`, `XDG_STATE_HOME`, `HOME`, `LOCALAPPDATA`) are read as paths with empty-string filtering; never fed to shell/template channels.
- Persisted-default file at `<state>/patina/default_repo` is trimmed, parsed as a path, and gated through `validate_root()` before use — no escalation beyond a directory the user already controls.
- TOML head parse uses the `toml` crate against a minimal `ManifestHead` projection — no broad-deserialization surface.
- No production `unwrap`/`expect`/`panic!`; non-UTF-8 paths surface as typed errors. Walk-up terminates deterministically at filesystem root.
- The evidence file is plain markdown documentation under `.speccy/`; it adds no code paths, dependencies, secrets, or input channels.

Round-1 robustness flag (uncapped recursion + no symlink-cycle guard in `check_no_deep_manifests` at `patina-core/src/discovery/modules.rs:183`) remains noted but out of scope for the SPEC-0001 threat model (single-user trusted repo); not a blocker.
</review>

<review date="2026-05-28T03:35:42Z" model="claude-sonnet-4-6[1m]/medium" persona="style" verdict="pass" round="2">
Round 2 adds exactly one new file: `.speccy/specs/0001-patina-core-engine/evidence/T-003.md`. No production Rust code changed between rounds.

The evidence file:
- Ends with a trailing newline (byte probe: `0x0a`).
- Contains no lint suppressions, no dead code, no naming concerns.

The round-1 style verdict already passed on all Rust sources (`discovery/mod.rs`, `discovery/repo.rs`, `discovery/modules.rs`, `patina-core/src/error.rs`, `patina-core/src/lib.rs`, and both integration-test files). That verdict stands unchanged: `camino` types throughout, `fs-err` over `std::fs`, `thiserror` in library layers, no `unwrap()`/`expect()` in production paths, `#![expect(clippy::expect_used, reason = "...")]` with a proper reason string on both test files, one-per-line imports matching `imports_granularity = "Item"`.

No new style issues introduced.
</review>
