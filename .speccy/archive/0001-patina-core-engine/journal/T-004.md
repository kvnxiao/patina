---
task: T-004
spec: SPEC-0001
generated_at: 2026-05-27T00:00:00Z
---

<implementer date="2026-05-27T00:00:00Z" model="claude-opus-4-7[1m]" round="1">

- Completed:
  - Added `patina_core::config` with two submodules: `file_entry`
    (REQ-005 `[[file]]` schema) and `hook_entry` (REQ-006 `[[hook]]`
    schema). The module exposes `ModuleConfig`, `parse_module_config`
    (read-from-disk), and `parse_module_config_str` (in-memory; used
    by every test in this task).
  - `config::file_entry`: `FileEntry` carries `source`, `targets`
    (always plural internally), and `mode: FileMode`. `FileMode` has
    five variants — `Symlink` (default), `SymlinkDir`, `Copy`,
    `CopyTree`, `TemplateRender` (implicit; derived from `.tmpl`
    suffix). `FileEntryError` has four variants: `TargetAndTargets`,
    `TargetMissing`, `TargetsEmpty`, `UnsupportedMode`,
    `ImplicitTemplateModeDeclared`. Each `Display` template hard-codes
    the substring contract its owning CHK requires.
  - `config::hook_entry`: `HookEntry` carries `event: HookEvent`
    (`PreApply | PostApply`), `command`, optional `shell` and `when`
    (stored verbatim — T-008 / T-015 handle them later), and
    `must_succeed` defaulting to `true` via `unwrap_or(true)` in
    `HookEntry::from_raw`. `HookEntryError::UnsupportedEvent` lists
    both accepted values in its Display template.
  - `ModuleConfig.variables` captures the raw `[variables]` table as
    `Option<toml::value::Table>` so T-006 has a clean handoff without
    a second TOML pass. This task does not enforce the reserved
    `patina.*` namespace rule — that is T-006's job per REQ-007.
  - `RawModule` projects the manifest with `_patina: Option<toml::Value>`
    so the root manifest's `[patina]` table parses without erroring.
    `[[file]]` and `[[hook]]` table arrays default to empty so a
    minimal manifest (e.g. an empty `[variables]` module) parses
    cleanly.
  - `EngineError` grows a `ConfigParse` `#[from]` variant so the
    parser surface is reachable through the engine's public API.
  - Two new integration test files added:
    `patina-core/tests/config_file_entry.rs` (11 tests covering every
    REQ-005 CHK whose proof is parser-bounded, plus
    `variables_table_is_preserved_for_t006` for the T-006 handoff
    and `parses_symlink_dir_and_copy_tree_modes` for the remaining
    declarable modes) and `patina-core/tests/config_hook_entry.rs`
    (7 tests covering both REQ-006 CHKs plus
    `rejects_on_drift_event_listing_accepted_values` for the v1.0
    non-goal twin and `preserves_shell_verbatim` for parse-time
    rule 3).
- Undone:
  - CHKs requiring `patina apply` to actually materialize files —
    CHK-010, CHK-011, CHK-041 (end-to-end), CHK-042 (end-to-end),
    CHK-043 (end-to-end), CHK-044 — are judgment-only at this phase
    because they exercise the file-mode executors (T-014), the
    MiniJinja env (T-008), and the apply pipeline that drives variable
    resolution / canonicalization / executor dispatch. T-004's parser
    correctness is the prerequisite each depends on and is
    demonstrated here; the end-to-end behaviour will be demonstrated
    by the executor task that owns it. Roll call below labels each
    CHK accordingly.
- Hygiene checks:
  - `cargo test --workspace`: exit 0 (18 new integration tests pass;
    11 prior tests still pass — total 29).
  - `cargo clippy --workspace --all-targets --all-features -- -D warnings`:
    exit 0.
  - `cargo +nightly fmt --all --check`: exit 0.
  - `cargo deny check`: exit 0 (same three pre-existing
    `license-not-encountered` baseline warnings for `Unicode-DFS-2016`,
    `Zlib`, and `MPL-2.0`; non-blocking).
- Evidence: paper trail at
  `.speccy/specs/0001-patina-core-engine/evidence/T-004.md`.
  Roll call for CHKs under REQ-005 and REQ-006:
  - CHK-010 (`$HOME/.zshrc` symlink readlink equals canonical
    `T/zsh/zshrc`): judgment-only → end-to-end `patina apply` scenario
    requiring the file-mode executor (T-014). Parser prerequisite
    (`mode = "symlink"` resolves to `FileMode::Symlink`, single-target
    string becomes one-element vec) demonstrated by
    `parses_single_target_explicit_symlink_mode` in
    `patina-core/tests/config_file_entry.rs`.
  - CHK-011 (`gitconfig.tmpl` renders with variables resolved):
    judgment-only → end-to-end template-render pipeline requiring
    MiniJinja (T-008), variable resolution (T-006), and the apply
    executor (T-014). Parser prerequisites (`.tmpl` source resolves
    to `FileMode::TemplateRender`; `.tmpl` source with explicit
    `mode` is rejected) demonstrated by
    `tmpl_source_resolves_to_template_render_mode` and
    `rejects_tmpl_source_with_explicit_mode` in
    `patina-core/tests/config_file_entry.rs`.
  - CHK-012 (`mode = "merge-json"` rejected, naming the four
    accepted modes): demonstrated →
    `rejects_merge_json_mode_listing_accepted_values` in
    `patina-core/tests/config_file_entry.rs`.
  - CHK-041 (no `mode` field defaults to `symlink`): demonstrated at
    the parser level →
    `defaults_to_symlink_when_mode_omitted` in
    `patina-core/tests/config_file_entry.rs`. End-to-end readlink
    check is T-014's responsibility.
  - CHK-042 (multi-target `targets = [...]` with `mode = "symlink"`):
    judgment-only for end-to-end. Parser-side multi-target handling
    demonstrated by `parses_targets_array_with_copy_mode` (same
    shape, different mode) in
    `patina-core/tests/config_file_entry.rs`.
  - CHK-043 (multi-target `targets = [...]` with `mode = "copy"`):
    demonstrated at the parser level →
    `parses_targets_array_with_copy_mode` in
    `patina-core/tests/config_file_entry.rs`. End-to-end byte-content
    equality is T-014's responsibility.
  - CHK-044 (multi-target `.tmpl` source renders to each target):
    judgment-only → requires T-008 + T-014. Parser prerequisite
    (`.tmpl` source resolves to `FileMode::TemplateRender`) covered
    by `tmpl_source_resolves_to_template_render_mode`.
  - CHK-045 (both `target` and `targets` declared → typed error
    naming both keys and `exactly one`): demonstrated →
    `rejects_target_and_targets_both_set` in
    `patina-core/tests/config_file_entry.rs`.
  - CHK-046 (neither `target` nor `targets` declared → typed error
    naming both keys and `missing`): demonstrated →
    `rejects_neither_target_nor_targets` in
    `patina-core/tests/config_file_entry.rs`.
  - CHK-047 (`targets = []` → typed error naming `targets` and
    `non-empty`): demonstrated → `rejects_empty_targets_array` in
    `patina-core/tests/config_file_entry.rs`.
  - CHK-013 (`event = "on_change"` rejected, naming `pre_apply` and
    `post_apply`): demonstrated →
    `rejects_on_change_event_listing_accepted_values` in
    `patina-core/tests/config_hook_entry.rs`.
  - CHK-014 (`must_succeed` defaults to `true` when omitted):
    demonstrated → `must_succeed_defaults_to_true_per_chk_014` in
    `patina-core/tests/config_hook_entry.rs`.
- Discovered issues:
  - The thiserror crate treats any field named `source` as
    `#[source]` regardless of `#[error(...)]` interpolation, which
    breaks for plain `String` fields. Renamed the
    `ImplicitTemplateModeDeclared` variant's path field to
    `source_path` to dodge the magic; no behaviour change. (The
    `Io { source: io::Error }` and `Toml { source: Box<toml::de::Error> }`
    variants use the magic correctly.)
  - The `indexing_slicing` clippy lint fires on integration tests
    that read `config.files[0]` after asserting `.len() == 1`.
    Resolved with a file-level
    `#![expect(clippy::indexing_slicing, reason = "...")]` on both
    new test files, consistent with how the prior tests carry
    `#![expect(clippy::expect_used, ...)]`.
  - The `RawModule._patina` field is captured as
    `Option<toml::Value>` and intentionally unused (prefixed with
    `_` to suppress dead-code warnings). It exists only so the root
    manifest's `[patina]` table parses without an unknown-field
    error; the discovery layer (T-003) is the consumer of that
    table head.
- Procedural compliance: clean working tree at entry
  (`git status --porcelain` empty); first-attempt shape (no prior
  T-004 journal). State flipped `pending` → `in-progress` →
  `in-review` with all four hygiene gates green before the
  `in-review` flip. No edits to locked SPEC.md. Journal and evidence
  files are siblings of SPEC.md / TASKS.md as required.

</implementer>

<review date="2026-05-28T03:47:06Z" model="claude-opus-4-7[1m]/medium" persona="business" verdict="pass" round="1">
T-004 satisfies REQ-005 / REQ-006 to the parse boundary the task scope mandates. All parse-bounded CHKs (CHK-012, 013, 014, 045, 046, 047) are demonstrated by passing integration tests whose substring assertions match the SPEC's Display contracts verbatim, and every task scenario in T-004's `<task-scenarios>` block has a corresponding test. The `FileMode` enum carries the five required variants with `Symlink` as the documented default; the implicit-template rule rejects `.tmpl` sources that declare any `mode`; `must_succeed` defaults to `true` via `unwrap_or(true)` per REQ-006. The implementer's judgment-only classification of CHK-010 / CHK-011 / CHK-041 / CHK-042 / CHK-043 / CHK-044 is correct — these scenarios run `patina apply` end-to-end and depend on T-008 (MiniJinja) plus T-014 (executors), which are out of scope for a parser task. The `[variables]` table is preserved raw on `ModuleConfig.variables` so T-006 has a clean handoff with no second TOML pass, matching the task body. No SPEC non-goal (`merge-json`, `on_change`, `on_drift`) was silently admitted; all are tested rejection paths. No open question was resolved silently. Scope discipline is clean — the diff touches only the new `patina-core/src/config/` module, two test files, and minimal additions to `error.rs` / `lib.rs` for the `ConfigParse` `#[from]` variant and re-exports.
</review>

<review date="2026-05-28T03:47:06Z" model="claude-opus-4-7[1m]/high" persona="tests" verdict="pass" round="1">
T-004's test suite faithfully exercises every parser-bounded scenario in the task body and every parser-bounded CHK under REQ-005/REQ-006. The 18 integration tests across `patina-core/tests/config_file_entry.rs` and `patina-core/tests/config_hook_entry.rs` drive the real `parse_module_config_str` API against real TOML text — no mocking of the system under test.

Negative-path tests (CHK-012, CHK-013, CHK-045, CHK-046, CHK-047, scenario 8) assert both the substring contract on the rendered Display *and* match against the specific typed-error variant via `matches!`, so they would catch a regression that renamed the variant or dropped a substring from the error template. Positive-path tests assert structural properties of the parsed `FileEntry`/`HookEntry` (mode discriminant, targets vec contents, must_succeed=true, when stored verbatim) — an obviously-wrong implementation (e.g., defaulting to `Copy`, defaulting `must_succeed` to `false`, dropping the `.tmpl` rule) would fail at least one of these.

The CHK roll call is complete and properly disciplined: CHK-010, CHK-011, CHK-042, CHK-044 are correctly labelled `judgment-only` because they exercise the end-to-end apply pipeline that lives in T-008 (MiniJinja env) and T-014 (file-mode executors); CHK-041 and CHK-043 are split into demonstrated parser-side + judgment-only end-to-end, which matches the task's "parsing and validation only" scope statement. Each judgment-only entry names the owning downstream task and cites the parser prerequisite that is demonstrated here.

Evidence file at `.speccy/specs/0001-patina-core-engine/evidence/T-004.md` shows real cargo runner output with framework-emitted structural artifacts (test names, `Finished test profile` line, summary counts, timing). Evidence command (`cargo test --test config_file_entry --test config_hook_entry`) is properly scoped to the slice rather than the full workspace hygiene run. Test names in the evidence match test names in the diff. Re-ran locally: 18 passed, 0 failed.

No vacuous test shapes detected.
</review>

<review date="2026-05-28T03:47:06Z" model="claude-opus-4-7[1m]/medium" persona="security" verdict="pass" round="1">
Parser-only slice with no security-sensitive side effects active at this layer. No filesystem writes, no symlink creation, no shell execution, no template evaluation — all correctly deferred to T-008/T-014/T-015 and labelled as such in the journal. TOML deserialization goes through `serde`/`toml` with no custom unsafe sinks. Errors echo user input back into `Display` strings, which is appropriate for a CLI config parser (no auth boundary crossed). `HookEntry.command`/`shell`/`when` are stored verbatim as untrusted `String`s — correct for parse-time; downstream T-015 owns PATH validation and shell-argv handling, and T-008 owns MiniJinja sandboxing. No new dependencies introduced beyond the existing workspace set, so no `deny.toml` surface change. No `unwrap`/`expect`/`panic` in `patina-core/src/config/{mod,file_entry,hook_entry}.rs`. The path-traversal concern on `FileEntry.source` (`..`-escape outside the module dir) is genuinely an executor-layer responsibility (T-014 must canonicalize and bound-check), not a T-004 omission.
</review>

<review date="2026-05-28T03:47:06Z" model="claude-sonnet-4-6[1m]/medium" persona="style" verdict="blocking" round="1">
`patina-core/tests/config_file_entry.rs` lines 83, 104, 123, 141, 164 use `patina_core::ConfigParseError` inline via full path inside `matches!` patterns, without a `use` import at the top of the file. `patina-core/tests/config_hook_entry.rs` line 73 does the same. Meanwhile, both files import `FileMode` / `HookEvent` with `use` at the top (lines 8, 8 respectively), and `ConfigParseError` is also re-exported from the crate root at `patina_core::ConfigParseError`.

The prior test files (`tests/repo_discovery.rs`, `tests/module_discovery.rs`) consistently bring all referenced types in via `use` at the top — there are no inline full-path references in `matches!` patterns. The T-004 tests split the same concern across two styles in the same files: `use patina_core::FileMode` at the top, but `patina_core::ConfigParseError::FileEntry(...)` inline. Add `use patina_core::ConfigParseError;` to both test files and drop the `patina_core::` prefix from the `matches!` patterns to match the project's import-at-the-top convention.
</review>

<blockers date="2026-05-28T03:47:06Z" round="1">
Group the inline `patina_core::ConfigParseError` references in the two new integration test files into a `use` import at the top, matching the import-at-the-top convention established by the prior `tests/repo_discovery.rs` and `tests/module_discovery.rs`. Mechanical fix: add `use patina_core::ConfigParseError;` to `patina-core/tests/config_file_entry.rs` and `patina-core/tests/config_hook_entry.rs`, then drop the `patina_core::` prefix from each `matches!` pattern. No production code changes required; no test logic changes.
- style (blocking): inline `patina_core::ConfigParseError::FileEntry(...)` paths inside `matches!` patterns split the import style — `FileMode` / `HookEvent` are imported at the top while `ConfigParseError` is referenced via full path. Bring them in line with the project convention.
</blockers>

<implementer date="2026-05-27T00:00:00Z" model="claude-opus-4-7[1m]" round="2">

- Completed:
  - Addressed the round-1 style blocker by adding
    `use patina_core::ConfigParseError;` to the top of both
    `patina-core/tests/config_file_entry.rs` and
    `patina-core/tests/config_hook_entry.rs`, alongside the
    existing `FileMode` / `HookEvent` imports.
  - Dropped the `patina_core::` prefix from every `ConfigParseError`
    reference inside `matches!` patterns (five sites in
    `config_file_entry.rs`, one site in `config_hook_entry.rs`).
    Import style is now consistent with `tests/repo_discovery.rs`
    and `tests/module_discovery.rs` — every referenced type is
    imported at the top.
  - The five-line collapsed `matches!` form for
    `ImplicitTemplateModeDeclared` now fits on one body line under
    rustfmt's width, mirroring the other variants.
- Undone:
  - Same downstream-CHK roll call as round 1 — CHK-010, CHK-011,
    CHK-041 (end-to-end), CHK-042 (end-to-end), CHK-043
    (end-to-end), CHK-044 remain judgment-only at the parser
    boundary. No new undone items.
- Hygiene checks:
  - `cargo test --workspace`: exit 0 (all 29 prior tests still
    pass; no test logic changed).
  - `cargo clippy --workspace --all-targets --all-features -- -D warnings`:
    exit 0.
  - `cargo +nightly fmt --all --check`: exit 0.
  - `cargo deny check`: exit 0 (same three pre-existing
    `license-not-encountered` baseline warnings; non-blocking).
- Evidence: paper trail unchanged from round 1 at
  `.speccy/specs/0001-patina-core-engine/evidence/T-004.md`. The
  round-2 fix is import-style mechanics with zero behavioural delta
  — every CHK roll-call entry from round 1 still applies verbatim,
  same demonstrating test names, same judgment-only downstream
  ownership.
- Discovered issues: none — the fix was strictly mechanical.
- Procedural compliance: retry-shape entry per the speccy-work
  recipe (journal carried round-1 `<implementer>` + round-1
  `<blockers>`; dirty tree from prior pass amended in place; no
  `git restore` / `git clean`). State flipped `pending` →
  `in-progress` → `in-review` with all four hygiene gates green
  before the `in-review` flip. No edits to the locked SPEC.md or
  to the round-1 `<implementer>` block.

</implementer>

<review date="2026-05-28T03:51:28Z" model="claude-opus-4-7[1m]/medium" persona="business" verdict="pass" round="2">
Round 2 is a strictly mechanical import-style cleanup (added `use patina_core::ConfigParseError;` to both test files; dropped the `patina_core::` prefix from each `matches!` pattern). Zero behaviour delta. The round-1 business pass on REQ-005/REQ-006 still holds verbatim: every parser-bounded CHK (CHK-012, 013, 014, 045, 046, 047) is demonstrated by passing integration tests whose substring assertions match the SPEC's `Display` contracts; CHK-010/011/041(e2e)/042(e2e)/043(e2e)/044 are correctly deferred as judgment-only to the executor tasks T-008/T-014 with the parser prerequisites demonstrated here. No SPEC non-goal (`merge-json`, `on_change`, `on_drift`) was silently admitted. No open question was silently resolved. Scope discipline remains clean.
</review>

<review date="2026-05-28T03:51:28Z" model="claude-opus-4-7[1m]/medium" persona="tests" verdict="pass" round="2">
Round 2 is a pure import-style mechanical fix with zero test-logic delta. Both `patina-core/tests/config_file_entry.rs` and `patina-core/tests/config_hook_entry.rs` now carry `use patina_core::ConfigParseError;` at the top alongside the existing `FileMode`/`HookEvent` imports, and every `matches!` pattern drops the `patina_core::` prefix. No fixtures, assertions, or judgment-only labels changed. The tests-persona substantive review from round 1 still applies: 18 integration tests drive the real `parse_module_config_str` API against real TOML text with no mocking, negative paths match both substring contracts and typed-error variants, the CHK roll call correctly defers CHK-010/011/042/044 end-to-end materialization to T-008/T-014. Evidence file unchanged. No vacuous test shapes.
</review>

<review date="2026-05-28T03:51:28Z" model="claude-opus-4-7[1m]/low" persona="security" verdict="pass" round="2">
Round 2 delta is a mechanical test-only style fix: add `use patina_core::ConfigParseError;` at the top of `patina-core/tests/config_file_entry.rs` and `patina-core/tests/config_hook_entry.rs`, then drop the `patina_core::` prefix from `matches!` patterns. No production code, no test logic, no new dependencies, no new sinks. The round-1 security analysis carries forward verbatim: parser-only slice with no filesystem writes, no symlink creation, no shell execution, no template evaluation; all sensitive surfaces correctly deferred to T-008 / T-014 / T-015. `cargo deny check` baseline unchanged. No `unwrap`/`expect`/`panic` introduced. Nothing to block on.
</review>

<review date="2026-05-28T03:51:28Z" model="claude-sonnet-4-6[1m]/medium" persona="style" verdict="pass" round="2">
Round-1 blocker resolved. Both `patina-core/tests/config_file_entry.rs:8` and `patina-core/tests/config_hook_entry.rs:8` now carry `use patina_core::ConfigParseError;` at the top, alongside the existing `FileMode` / `HookEvent` imports. A grep of both files for `patina_core::ConfigParseError` returns only the two `use` lines — no residual full-path inline references in `matches!` patterns. Import style is consistent with the project's import-at-the-top convention and with the prior test files. No other style issues introduced by the round-2 mechanical fix.
</review>
