---
spec: SPEC-0001
task: T-011
generated_at: 2026-05-28T16:00:00Z
---

<implementer date="2026-05-28T16:00:00Z" model="claude-opus-4.8[1m]/high" round="1">
- Completed: Landed the `recovery` and `probe` submodules under
  `patina_core::journal` (REQ-013, backward-only crash convergence per
  DEC-011). New files: `patina-core/src/journal/recovery.rs`,
  `patina-core/src/journal/probe.rs`; wired into
  `patina-core/src/journal/mod.rs` and re-exported `recover_orphans`,
  `RecoveryReport`, `mirror_backup_path`, `Probe`, `classify_target`,
  and the new `ROLLED_BACK_SUFFIX` constant; surfaced `recover_orphans`
  and `RecoveryReport` from the crate root in `patina-core/src/lib.rs`.
  `recover_orphans(journal_dir, backups_dir)` scans the journal dir for
  orphan plans (a `.plan` file with neither a `.COMMIT` nor the new
  `.ROLLED_BACK` sentinel), decodes each via T-010's version-envelope
  check, then for each operation probes the filesystem and consults the
  per-apply backup directory to reverse it backward: a target with a
  backup is restored from the original bytes (overwrite case); a target
  with no backup is deleted if present (fresh-creation case). The orphan
  `.plan` and `.progress` files are removed only after every reversal
  succeeds. `probe.rs` owns `mirror_backup_path` (the
  `backups/TS/mirrored-target` layout that T-012 will write and
  recovery is the first reader of), `classify_target` (present/absent via
  `symlink_metadata`, so a dangling symlink counts as present), and
  `operation_target`. Recovery is backward-only and idempotent (second
  pass finds no orphan; in-pass restores rewrite identical bytes and
  deletes of absent targets are no-ops); the advisory progress cursor is
  ignored for the reversal decision.
- Undone: Nothing within REQ-013's scope. The reversal is intentionally
  coarse (overwrite→restore, fresh→delete) rather than pre-state-hash
  probed, because T-010's `PlannedOperation` does not yet carry per-op
  pre-state hashes and the SPEC done-when only requires backups + inverse
  ops; `Probe` is `#[non_exhaustive]` so a finer probe can layer in when
  later tasks enrich the plan record. The end-to-end `patina apply`
  wiring that invokes `recover_orphans` on startup lands with the apply
  subcommand (T-016); the symlink-specific inverse (delete new link,
  restore original) reduces here to the same restore/delete dichotomy and
  will be exercised once T-014's symlink executor and T-012's backup
  writer exist.
- Hygiene checks:
  - `cargo test --workspace`: exit 0.
  - `cargo clippy --workspace --all-targets --all-features -- -D warnings`:
    exit 0.
  - `cargo +nightly fmt --all --check`: exit 0.
  - `cargo deny check`: exit 0 (advisories ok, bans ok, licenses ok,
    sources ok).
- Evidence: paper trail at
  `.speccy/specs/0001-patina-core-engine/evidence/T-011.md`.
  Roll call for CHKs under REQ-013:
  - CHK-024 (3-of-5 completed ops reversed from backups, orphan plan +
    progress removed, new apply proceeds from scratch): demonstrated →
    evidence Scenario 1 (`restores_overwritten_targets_and_clears_orphan_files`
    in `patina-core/tests/recovery_crash.rs`) stages the exact crash scene
    (5-op orphan plan, 3 overwrites with backups, no COMMIT) and asserts
    the 3 targets restore to pre-apply bytes, the fresh target is removed,
    and both orphan files are gone. The remaining REQ-013 done-when
    bullets and the probe-over-cursor behavior are covered by the four
    sibling tests (interrupted-before-any-op, idempotence, backward-only,
    lying-cursor) plus the `probe.rs`/`recovery.rs` unit tests.
- Discovered issues: none affecting other tasks. Note for the reviewer:
  `mirror_backup_path` is defined in T-011 because recovery is the first
  consumer of the backup layout; T-012 (REQ-014, pending) must write
  backups through this same mapping so the two halves agree. The
  `ROLLED_BACK_SUFFIX` constant is introduced here (recovery must skip
  rolled-back plans) and will be written by T-018's rollback path.
- Procedural compliance: no `unwrap`/`expect`/`panic`/`todo` in
  production paths (test helpers use `.expect` under the
  `allow-expect-in-tests` carve-out and the `#![expect(clippy::expect_used)]`
  crate attribute in the integration test); all fs access via `fs-err`;
  `camino` UTF-8 path types throughout; typed `JournalError` returns only.
  No new dependencies. `SPEC.md` not edited. `TASKS.md` touched only to
  flip T-011 `pending → in-progress → in-review`. Journal is append-only.
</implementer>

<review persona="business" verdict="pass" model="claude-opus-4-8[1m]/high" date="2026-05-28T18:25:37Z" round="1">
T-011 satisfies REQ-013's observable behavior for the layer it owns. CHK-024 and all four REQ-013 `<done-when>` bullets (interrupted-before-any-op, N-of-M reversal from backups, idempotence, backward-only) are demonstrated by `patina-core/tests/recovery_crash.rs`, and the probe-over-cursor (REQ-012) edge case is covered. `recover_orphans` correctly drives reversal off the backup directory + filesystem probe rather than the advisory cursor, honoring DEC-011's backward-only commitment. No non-goal scope crept in; `mirror_backup_path` and `ROLLED_BACK_SUFFIX` are justified forward-declarations for T-012/T-018. Full `patina-core` suite is green on a clean build (an apparent intermittent failure was a `cargo test` loop racing the build lock, gone once the test binary is pre-built).

Non-blocking observation for the orchestrator / T-016 reviewer: CHK-024's literal end-to-end phrasing ("when `patina apply --yes` is next invoked... before any new mutation occurs") is not yet exercised through a real `apply` invocation — `recover_orphans` is exported (`patina-core/src/lib.rs:41`) but not wired into an apply-startup path, which does not exist until T-016. This is consistent with the task decomposition and was disclosed in the journal/evidence. Ensure T-016 actually invokes `recover_orphans` on startup so the user-facing contract closes.
</review>

<review persona="tests" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-05-28T18:25:37Z" round="1">
CHK-024 is genuinely demonstrated. `restores_overwritten_targets_and_clears_orphan_files` in `patina-core/tests/recovery_crash.rs` stages the exact crash scene (5-op orphan plan, 3 overwrites with backups, 1 fresh-created, 1 unstarted, lying cursor [0,1,2]), drives the real `recover_orphans` entry point with no mocking, and asserts pre-apply bytes are restored, the fresh target deleted, and both orphan files removed. Sibling tests cover the negative/boundary paths (interrupted-before-any-op, idempotence, backward-only-never-forward, probe-over-cursor) and probe/recovery unit tests cover the mirror-path and orphan-classification helpers. Verified the tests bite: mutating `reverse_operation` to skip the backup restore fails 4 of 5 integration tests including the CHK-024 test. The probe-over-cursor test is non-vacuous because the cursor is never consulted in the reversal path. Evidence roll call labels CHK-024 `demonstrated` against a named test in the diff. Minor non-blocking note: the evidence file describes red/green in prose rather than pasting captured runner output, but the red-phase claims hold up under independent mutation testing.
</review>

<review persona="security" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-05-28T18:25:37Z" round="1">
No security blockers in T-011 (crash-recovery reversal, REQ-013). Key surfaces: path traversal in the backup mirror is correctly neutralized — `mirror_components` (`probe.rs:105`) drops `RootDir`/`CurDir`/`ParentDir` and folds the platform `Prefix` to alphanumerics, so an attacker-influenced target path in the decoded plan cannot escape `<backups>/<ts>/` (tested by `mirror_strips_dot_and_parent_segments`). Symlink-safe restore: `restore_from_backup` unlinks any existing entry before `fs_err::copy`, so a leftover/partial symlink at the target is removed (not followed). Symlink/dir removal uses `symlink_metadata`, so a symlink (including dangling or symlink-to-dir) is removed as a link rather than followed; `remove_dir_all` only fires for a real directory. Untrusted-input bound: orphan plans are decoded through T-010's version envelope, refusing newer/malformed plans. No secret handling, logging, auth, or network surface in this single-user local-fs slice.
</review>

<review persona="style" verdict="blocking" model="claude-sonnet-4-6[1m]/medium" date="2026-05-28T18:25:37Z" round="1">
`patina-core/src/journal/recovery.rs:236-242` introduces a private `remove_if_present(path: &Utf8PathBuf)` that is functionally identical to the `remove_if_present(path: &Utf8Path)` already present in `patina-core/src/journal/mod.rs:233-238`. Both match `NotFound` to `Ok(())` and wrap everything else in `JournalError::Filesystem`.

Two violations in one: (1) Duplicate helper — the recovery module invented a parallel copy rather than reusing the existing one; the project's code-quality rules require reusing existing helpers. (2) Parameter type regression — the duplicate uses `&Utf8PathBuf` rather than `&Utf8Path`, fighting the project convention (`.claude/rules/rust/rust-code-quality.md` "Choosing Function Parameter Types": prefer `&Utf8Path` for read-only access); the existing `mod.rs` version correctly takes `&Utf8Path`.

Fix: delete the duplicate in `recovery.rs`, make `remove_if_present` in `mod.rs` visible to the `recovery` sibling (promote to `pub(super)` or `pub(crate)`), and call `super::remove_if_present` from `recovery.rs` at both call sites.
</review>

<blockers date="2026-05-28T18:25:37Z" round="1">
Remove the duplicated filesystem helper in the recovery module before the next review pass.
- style: `patina-core/src/journal/recovery.rs:236-242` defines a private `remove_if_present(path: &Utf8PathBuf)` that duplicates `remove_if_present(path: &Utf8Path)` already in `patina-core/src/journal/mod.rs:233-238`, and regresses the parameter type from `&Utf8Path` to `&Utf8PathBuf`. Delete the recovery-module copy, promote the `mod.rs` helper to `pub(super)`/`pub(crate)`, and call `super::remove_if_present` from both `recovery.rs` call sites. Verify clippy/fmt/test stay green.
</blockers>

<implementer date="2026-05-28T19:10:00Z" model="claude-opus-4.8[1m]/high" round="2">
- Completed: Addressed the round-1 style blocker by collapsing the
  duplicated filesystem helper. Deleted the private
  `remove_if_present(path: &Utf8PathBuf)` from
  `patina-core/src/journal/recovery.rs`, promoted the existing
  `remove_if_present(path: &Utf8Path)` in `patina-core/src/journal/mod.rs`
  to `pub(super)` (updating its doc comment to note the `recovery` sibling
  is now a second caller), and routed both recovery call sites in
  `reverse_orphan` through `super::remove_if_present`. The single canonical
  helper now keeps the project-preferred `&Utf8Path` read-only parameter
  type. Removed the now-unused top-level `use camino::Utf8PathBuf;` import
  from `recovery.rs` (it was consumed only by the deleted helper) and moved
  it into the `#[cfg(test)] mod tests` block, which still references
  `Utf8PathBuf` for the `Dirs` fixture fields.
- Undone: Nothing new. T-011's REQ-013 scope is unchanged from round 1; this
  round is a pure deduplication with no behavioral delta. The same
  forward-declaration deferrals noted in round 1 (apply-startup wiring in
  T-016, backup writer in T-012, rollback sentinel writer in T-018) still
  stand.
- Hygiene checks:
  - `cargo test --workspace`: exit 0.
  - `cargo clippy --workspace --all-targets --all-features -- -D warnings`:
    exit 0.
  - `cargo +nightly fmt --all --check`: exit 0.
  - `cargo deny check`: exit 0 (advisories ok, bans ok, licenses ok,
    sources ok).
- Evidence: paper trail at
  `.speccy/specs/0001-patina-core-engine/evidence/T-011.md`. This round
  changed no observable behavior, so the round-1 evidence stands unchanged.
  Roll call for CHKs under REQ-013:
  - CHK-024 (3-of-5 completed ops reversed from backups, orphan plan +
    progress removed, new apply proceeds from scratch): demonstrated →
    `restores_overwritten_targets_and_clears_orphan_files` in
    `patina-core/tests/recovery_crash.rs` still stages the full crash scene
    and asserts restore/delete/cleanup; the orphan-file cleanup it asserts
    now flows through the shared `super::remove_if_present` helper, and the
    test remains green after the dedup.
- Discovered issues: none.
- Procedural compliance: surgical change confined to the blocker —
  deleted-helper / promoted-visibility / re-routed-call-sites, plus the
  unavoidable import relocation the deletion forced. No `unwrap`/`expect`/
  `panic`/`todo` in production paths; typed `JournalError` returns
  preserved; `fs-err` and `camino` conventions intact. No new dependencies.
  `SPEC.md` not edited. `TASKS.md` touched only to flip T-011
  `pending → in-progress → in-review`. Journal is append-only — this block
  is appended after the round-1 blockers.
</implementer>

<review persona="business" verdict="pass" model="claude-opus-4.8[1m]/high" date="2026-05-28T18:30:58Z" round="2">
T-011 round 2 is a pure deduplication addressing the round-1 style blocker, with no behavioral delta to REQ-013. The recovery module's duplicate `remove_if_present` is deleted; both orphan-cleanup call sites in `recovery.rs:173-174` now route through the canonical `pub(super)` helper in `mod.rs:234` (`&Utf8Path`). CHK-024's observable contract remains demonstrated by `restores_overwritten_targets_and_clears_orphan_files` — stages the 5-op crash scene with 3 backed-up overwrites and a lying cursor [0,1,2], asserts the 3 targets restore to pre-apply bytes, the fresh target is removed, and both orphan plan/progress files are cleared. All four REQ-013 `<done-when>` bullets plus the REQ-012 probe-over-cursor edge case stay covered; no non-goal scope crept in. The round-1 non-blocking note still stands: CHK-024's literal end-to-end `patina apply` startup wiring lands in T-016 and must invoke `recover_orphans` to close the user-facing contract — correctly deferred per the task decomposition.
</review>

<review persona="tests" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-05-28T18:30:58Z" round="2">
Round-2 dedup is behavior-neutral and does not regress test coverage. CHK-024 remains genuinely demonstrated by `restores_overwritten_targets_and_clears_orphan_files` (`patina-core/tests/recovery_crash.rs:135`), which drives the real `recover_orphans` entry point (no mocking) against a staged 5-op crash scene and asserts pre-apply restore, fresh-target deletion, and orphan plan/progress removal. The round-2 change only collapsed the duplicate `remove_if_present` helper and routed both orphan-cleanup call sites through `super::remove_if_present` (`recovery.rs:173-174`) — no test or assertion changed. Verified the tests still bite through the deduped path: mutating `reverse_orphan` to skip cleanup fails 4 of 5 integration tests including the CHK-024 cleanup assertions. Negative/boundary paths (interrupted-before-any-op, idempotence byte-stability, backward-only-never-forward, probe-over-cursor) have specific assertions. Evidence field present on both implementer rounds, file exists, and the CHK-024 roll-call label cites a named test that exists in the diff. Suite green (5/5).
</review>

<review persona="security" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-05-28T18:30:58Z" round="2">
Round-2 dedup is security-neutral and the round-1 security clearance still holds. The deleted `remove_if_present` duplicate in `recovery.rs` was byte-identical (`NotFound → Ok(())`, else `JournalError::Filesystem`) to the now-`pub(super)` canonical in `mod.rs:234`; collapsing them introduces no new behavior, and `remove_file` unlinks a symlink rather than following it, so the orphan-cleanup path has no symlink-follow risk. Unchanged and still sound: path-traversal containment in `mirror_components` (`probe.rs:105-128`, drops `..`/root, folds prefix to alphanumerics), symlink-safe replace in `restore_from_backup`/`remove_target` via `symlink_metadata`, and version-envelope-gated plan decode. No new deps; no secret/log/auth/network surface in this single-user local-fs slice.
</review>

<review persona="style" verdict="pass" model="claude-sonnet-4-6[1m]/medium" date="2026-05-28T18:30:58Z" round="2">
Round-1 blocker addressed correctly. The duplicate `remove_if_present(path: &Utf8PathBuf)` in `patina-core/src/journal/recovery.rs` is gone. The canonical helper in `mod.rs` (line 234) was promoted to `pub(super)` with its doc comment updated to note the `recovery` sibling as a second caller. Both call sites in `reverse_orphan` (lines 173–174) now route through `super::remove_if_present`. The `use camino::Utf8PathBuf` import was correctly relocated into the `#[cfg(test)] mod tests` block where it is still needed. No new style violations introduced.
</review>
