---
spec: SPEC-0001
task: T-027
generated_at: 2026-05-30T00:00:00Z
---

<implementer date="2026-05-30T00:00:00Z" model="claude-opus-4-8[1m]/low" round="1">
- Completed: Added the REQ-030 lock-acquisition policy to the engine
  apply path, keeping the default `Blocking` path byte-for-byte the
  pre-amendment behaviour. In `patina-core/src/lock.rs`: added a
  `try_acquire(path, kind)` primitive that makes exactly one
  non-blocking `try_lock_*` attempt and surfaces contention as a new
  `LockError::Contended { kind, path }` variant (the enum is already
  `#[non_exhaustive]`); factored the lock-file open into a shared
  `open_lock_file` helper reused by both `acquire` and `try_acquire`
  so there is no duplicated open logic. In
  `patina-core/src/apply/engine.rs`: added a `LockPolicy` enum with
  `Blocking` (derived `#[default]`), `NonBlocking`, and
  `Held(LockGuard)`; `execute` now takes `policy: LockPolicy` by value
  and resolves the guard before any filesystem mutation —
  `Blocking` acquires exclusive with `exclusive_timeout()` exactly as
  today, `NonBlocking` calls `try_acquire` and returns the typed
  contention error early (before the plan flush, so zero mutation),
  and `Held(guard)` reuses the caller's guard without acquiring.
  Re-exported `LockPolicy` from `patina-core/src/lib.rs`; the library
  `apply()` entry point passes `LockPolicy::default()`. CLI call sites
  in `patina-cli/src/cmd/apply.rs` pass `LockPolicy::Blocking` at both
  the human and `--json` `execute_plan` sites so `patina apply`
  behaviour and exit codes are unchanged. `run_rollback`
  (`patina-cli/src/cmd/rollback.rs`) is untouched, per the task scope.
  Added two unit tests in a `lock_policy_tests` module in `engine.rs`
  driving `execute` in-process against a minimal empty-operation
  `ResolvedPlan` over a tempdir state directory.

- Undone: The `NonBlocking` watcher call-site wiring (SPEC-0003) and
  the `Held` `remove` / `promote` call-site wiring (SPEC-0002) are
  explicitly out of this task's scope and are owned by those SPECs.
  Nothing from T-027's scope is deferred.

- Hygiene checks: `cargo test --workspace` → exit 0 (the
  pre-existing `lock_concurrency::blocked_exclusive_exits_with_lock_timeout_code`
  timing test flaked once under parallel CPU load, then passed on a
  parallel retry, serialized, and in isolation — see Discovered
  issues). `cargo clippy --workspace --all-targets --all-features --
  -D warnings` → exit 0. `cargo +nightly fmt --all --check` → exit 0.
  `cargo deny check` → exit 0 (no new dependencies).

- Evidence: paper trail at
  `.speccy/specs/0001-patina-core-engine/evidence/T-027.md`. Roll
  call for the CHKs under REQ-030:
  - CHK-065 (NonBlocking apply on a contended lock returns the typed
    contention error and writes no `<ts>.plan` / `<ts>.COMMIT`):
    demonstrated → evidence Scenario 1
    (`non_blocking_apply_on_contended_lock_errors_and_writes_no_journal`).
  - CHK-066 (Held policy applies with the caller's guard without
    timing out and leaves a `<ts>.COMMIT`): demonstrated → evidence
    Scenario 2 (`held_policy_applies_with_callers_guard_and_commits`).
  - CHK-067 (default `Blocking` policy preserves REQ-021
    byte-identical stdout across two `patina apply --yes` runs):
    hygiene → the existing `patina-cli/tests/deterministic_stdout.rs`
    suite drives `patina apply --yes` end-to-end on the `Blocking`
    path and asserts byte-identical stdout across consecutive runs; it
    runs under `cargo test --workspace`. This task wired
    `LockPolicy::Blocking` into that path without changing acquire
    semantics, so the existing suite is the proof the default is
    unchanged.

- Discovered issues:
  `patina-core/tests/lock_concurrency.rs::blocked_exclusive_exits_with_lock_timeout_code`
  is a load-dependent timing flake: it spawns a holder process, sleeps
  a fixed 150ms to let it acquire, then spawns a contender with a
  200ms cap. Under CPU contention (full parallel `cargo test
  --workspace`) the holder can fail to acquire within 150ms, so the
  contender wins the lock and exits 0 instead of the expected 4. It
  passes in isolation, passes serialized (`--test-threads=1`), and
  passed on a parallel retry. It is pre-existing and unrelated to
  T-027 (this task never touches the blocking `acquire` path or the
  spawned helper binary). Flagged for a future hardening of that
  test's timing assumption; not fixed here as out of scope.

- Procedural compliance: this entry lands directly in
  `journal/T-027.md`; no `<implementer-note>` was written into
  TASKS.md. The TASKS.md `state="..."` attribute for T-027 flips from
  `in-progress` to `in-review` as the final step of this turn. No
  shipped skill bodies under `.claude/skills/` were edited.
</implementer>

<review date="2026-05-30T05:05:09Z" model="claude-opus-4-8[1m]/medium" persona="business" verdict="pass" round="1">
T-027 satisfies REQ-030 end-to-end. All three scenarios map cleanly to passing tests: CHK-065 (NonBlocking contention → typed `LockError::Contended` with zero journal writes, verified at `patina-core/src/apply/engine.rs` `non_blocking_apply_...`), CHK-066 (Held policy completes against the caller's own guard with a `.COMMIT`, `held_policy_...`), and CHK-067 (default Blocking byte-identical stdout, covered by the passing `patina-cli/tests/deterministic_stdout.rs`). The default path is byte-for-byte the pre-amendment `acquire_lock(..., exclusive_timeout())`; the CLI passes `LockPolicy::Blocking` at both `execute_plan` sites and `apply()` defaults to it. The new `Contended` variant is correctly kept distinct from `Timeout` — `patina-cli/src/exit_code.rs:70` maps only `Timeout` to exit 4, so the REQ-022/REQ-023 exit-code contract is preserved. No new user-facing flag (matches REQ-030's explicit exclusion); `run_rollback`, the watcher NonBlocking wiring, and remove/promote Held wiring are correctly deferred to the downstream SPECs rather than silently pulled in. No open question was implicitly resolved.
</review>

<review date="2026-05-30T05:05:09Z" model="claude-opus-4-8[1m]/medium" persona="tests" verdict="pass" round="1">
T-027 (REQ-030) tests genuinely exercise the lock-policy behaviour. The two demonstrated unit tests in `patina-core/src/apply/engine.rs` (`lock_policy_tests`) drive the real `execute` path against real `fs2` advisory locks. Mutation-verified: rewriting the `Held` arm to re-acquire makes `held_policy_applies_with_callers_guard_and_commits` fail with a self-deadlock `Timeout` (the exact bug REQ-030 prevents), and rewriting `NonBlocking` to a blocking acquire makes `non_blocking_apply_on_contended_lock_errors_and_writes_no_journal` fail because it asserts the specific `LockError::Contended { kind: Exclusive }` variant plus zero `.plan`/`.COMMIT` writes — not any error. CHK-067's `hygiene` label cites `patina-cli/tests/deterministic_stdout.rs`, which drives `patina apply --yes` (human + `--json`) end-to-end on the now-`Blocking` path with byte-identical stdout assertions. Evidence roll call covers all three CHKs (065/066 demonstrated, 067 hygiene with a specific cite); runner output is authentic and test names match the diff; no fabrication patterns matched.
</review>

<review date="2026-05-30T05:05:09Z" model="claude-opus-4-8[1m]/medium" persona="security" verdict="pass" round="1">
REQ-030 lock-policy amendment is security-clean. The `NonBlocking` path (`try_acquire` -> `LockError::Contended`) returns early at `patina-core/src/apply/engine.rs:348`, before `flush_plan_and_fsync` (`:375`) and any executor/backup mutation, so the zero-mutation contention guarantee holds (CHK-065). `Held` reuses the caller's guard with no second `flock`/`LockFileEx`, avoiding self-deadlock (CHK-066). Contention vs. genuine I/O failure is correctly disambiguated via the existing `try_lock`/`is_contended` path in `patina-core/src/lock.rs:330`; I/O faults still map to `LockError::Io`. No new input parsing, endpoints, dependencies, secret handling, or sensitive-data logging; the new `Contended` Display discloses only the lock path, which the pre-existing `Timeout` variant already exposes. `LockGuard` is non-`Clone` and passed by value into the `Held` variant, so guards cannot be aliased. Default `Blocking` path is byte-for-byte unchanged (CHK-067 covered by the existing `deterministic_stdout.rs` suite). One non-blocking observation, out of scope for this task: `recover_orphans` (`:337`) still runs before lock acquisition, as it did pre-amendment — the amendment did not introduce or move this, so it is not a T-027 regression.
</review>

<review date="2026-05-30T05:05:09Z" model="claude-sonnet-4-6[1m]/medium" persona="style" verdict="blocking" round="1">
`patina-core/src/apply/engine.rs:596` names the new test module `mod lock_policy_tests {`. Every other test module in `patina-core/src/` uses `mod tests {` uniformly — all 20 existing instances across `lock.rs`, `journal/`, `apply/`, `status/`, `template/`, `paths.rs`, etc. use exactly `mod tests`. There is no pre-existing `mod tests` in `engine.rs` that would force a disambiguating name, so the correct name per project convention is `mod tests`. Rename `lock_policy_tests` to `tests` to match the established pattern.
</review>

<review date="2026-05-30T05:05:09Z" model="claude-opus-4-8[1m]/low" persona="correctness" verdict="pass" round="1">
REQ-030 lock-policy wiring is logically sound. The guard is resolved before any filesystem mutation in `patina-core/src/apply/engine.rs:342`; `NonBlocking` contention short-circuits via `?` ahead of `flush_plan_and_fsync` (engine.rs:375), so the zero-mutation guarantee holds (CHK-065, verified by the passing in-process test). `try_acquire` (lock.rs:281) reuses the same `try_lock`/`is_contended` helpers as the blocking path, mapping contention to the new `LockError::Contended` and other failures to `LockError::Io`; `EngineError::Lock` carries `#[from] LockError` so propagation is correct. `Held(guard)` consumes the caller's guard without re-acquiring, so no self-deadlock (CHK-066, verified by the passing test). The `open_lock_file` extraction preserves the original `OpenOptions` and error mapping, leaving the default `Blocking` path byte-for-byte unchanged (CHK-067). Both new tests pass under `cargo test -p patina-core`. No correctness defects at or above the reporting threshold.
</review>

<review date="2026-05-30T05:05:09Z" model="claude-opus-4-8[1m]/medium" persona="architecture" verdict="pass" round="1">
The REQ-030 `LockPolicy` API shape, layering, and Held-guard model are architecturally sound and will not force downstream rework on the load-bearing paths. Layering is clean: lock primitives stay in `patina-core/src/lock.rs` (`acquire`/`try_acquire`/the new `open_lock_file` helper); `LockPolicy` lives in `patina-core/src/apply/engine.rs:90` and is re-exported via `lib.rs:39`; CLI and library `apply()` pass the policy explicitly. No cross-layer reach, no new module cycle. Reuse rule honored: `open_lock_file` de-dupes the lock-file open. No premature abstraction: three concrete variants each with a named consumer (Blocking=CLI, NonBlocking=SPEC-0003 watcher, Held=SPEC-0002 remove/promote). Guard correctly kept off the `Clone` `ApplyRequest`. Decisions honored: DEC-005 and REQ-023's exclusive-for-mutation default preserved byte-for-byte. Cross-SPEC contract matches the downstream amendments verbatim. Two non-blocking concerns handed to SPEC-0002 decomposition: (1) by-value `Held` drops the guard at `execute` return, not command end — workable because the re-apply COMMIT is the terminal mutation in the non-purge flow, but SPEC-0002 `remove --purge` must sequence the purge-delete before the re-apply or re-acquire; (2) `Held` does not assert `guard.kind() == Exclusive` — a shared guard would silently violate REQ-023; a debug-assert or typed reject is cheap insurance before two external callers depend on it.
</review>

<blockers date="2026-05-30T05:05:09Z" round="1">
Rename the new test module to match project convention before the next implementer pass.
- style: `patina-core/src/apply/engine.rs:596` declares `mod lock_policy_tests {`, but all 20 existing test modules in `patina-core/src/` use `mod tests {`. There is no pre-existing `mod tests` in `engine.rs` forcing a disambiguating name, so rename `lock_policy_tests` → `tests`.
</blockers>

<implementer date="2026-05-30T18:00:00Z" model="claude-opus-4-8[1m]/low" round="2">
- Completed: Addressed the round-1 style blocker. Renamed the test
  module in `patina-core/src/apply/engine.rs` from
  `mod lock_policy_tests` to `mod tests`, matching the uniform
  `mod tests` convention used by all 20 existing test modules under
  `patina-core/src/`. No `mod tests` pre-existed in `engine.rs`, so no
  disambiguation was needed. This is the only change this round; the
  two test bodies and all production code are untouched.

- Undone: Nothing in T-027's scope is deferred.

- Hygiene checks: `cargo test --workspace` → exit 0 (run with
  `--test-threads=1`; under full parallelism the pre-existing
  load-dependent timing flake
  `lock_concurrency::blocked_exclusive_exits_with_lock_timeout_code`
  tripped once exactly as documented in round 1, then passed
  serialized — this test never touches the blocking `acquire` path or
  this task's code). `cargo clippy --workspace --all-targets
  --all-features -- -D warnings` → exit 0. `cargo +nightly fmt --all
  --check` → exit 0. `cargo deny check` → exit 0 (no dependency
  change).

- Evidence: paper trail at
  `.speccy/specs/0001-patina-core-engine/evidence/T-027.md` (unchanged
  from round 1; the rename does not alter test behaviour). Roll call
  for the CHKs under REQ-030:
  - CHK-065 (NonBlocking apply on a contended lock returns the typed
    contention error and writes no `<ts>.plan` / `<ts>.COMMIT`):
    demonstrated → evidence Scenario 1
    (`non_blocking_apply_on_contended_lock_errors_and_writes_no_journal`,
    now in `mod tests`).
  - CHK-066 (Held policy applies with the caller's guard without
    timing out and leaves a `<ts>.COMMIT`): demonstrated → evidence
    Scenario 2 (`held_policy_applies_with_callers_guard_and_commits`,
    now in `mod tests`).
  - CHK-067 (default `Blocking` policy preserves REQ-021
    byte-identical stdout): hygiene → the existing
    `patina-cli/tests/deterministic_stdout.rs` suite drives `patina
    apply --yes` end-to-end on the `Blocking` path; unchanged by this
    rename.

- Discovered issues: same pre-existing timing flake noted in round 1
  (`lock_concurrency::blocked_exclusive_exits_with_lock_timeout_code`);
  unrelated to T-027 and untouched by this rename.

- Procedural compliance: this round-2 entry is appended after the
  round-1 `<implementer>` and `<blockers>` blocks; no earlier block
  was modified. The TASKS.md `state` for T-027 flips from
  `in-progress` to `in-review` as the final step. No shipped skill
  bodies under `.claude/skills/` were edited.
</implementer>

<review date="2026-05-30T05:08:00Z" model="claude-opus-4-8[1m]/medium" persona="business" verdict="pass" round="2">
T-027 satisfies REQ-030 end-to-end and the round-2 rename (`mod lock_policy_tests` → `mod tests` at `patina-core/src/apply/engine.rs:596`) is behavior-neutral, so all three scenarios still map cleanly. CHK-065: NonBlocking contention returns the typed `LockError::Contended { kind: Exclusive }` and short-circuits before `flush_plan_and_fsync`, with the test asserting zero `.plan`/`.COMMIT` writes. CHK-066: `Held(guard) => guard` reuses the caller's exclusive guard with no second acquire, test asserts a successful `.COMMIT`. CHK-067: default `Blocking` preserves byte-identical stdout via `patina-cli/tests/deterministic_stdout.rs`. REQ-022/REQ-023 exit-code contract preserved — `Contended` is distinct from `Timeout`, and `patina-cli/src/exit_code.rs:70` maps only `Timeout` to exit 4. No new user-facing flag; `run_rollback` untouched; watcher NonBlocking (SPEC-0003) and remove/promote Held (SPEC-0002) wiring correctly deferred. No open question silently resolved.
</review>

<review date="2026-05-30T05:08:00Z" model="claude-opus-4-8[1m]/medium" persona="tests" verdict="pass" round="2">
Round-2 rename `lock_policy_tests` → `tests` is clean and the two demonstrated tests still gate REQ-030 behaviour. `cargo test -p patina-core --lib apply::engine::tests` compiles and passes both `non_blocking_apply_on_contended_lock_errors_and_writes_no_journal` and `held_policy_applies_with_callers_guard_and_commits`. Only `engine.rs:596` declares `mod tests` — no collision. Assertions are unchanged and remain specific: CHK-065 asserts the exact `Err(EngineError::Lock(LockError::Contended { kind: LockKind::Exclusive, .. }))` plus zero `.plan`/`.COMMIT`; CHK-066 asserts `ApplyResult::Applied`, present `.COMMIT`, absent `.plan`. CHK-067 is correctly `hygiene`-labelled with a scoped cite. Non-blocking nit: the evidence narrative at line 12 still says the tests "live in the `lock_policy_tests` module" — stale prose after the rename; the round-2 journal roll call correctly names `mod tests`.
</review>

<review date="2026-05-30T05:08:00Z" model="claude-opus-4-8[1m]/medium" persona="security" verdict="pass" round="2">
Round-2 change is a test-only module rename; production code and test bodies are byte-identical to the round-1 pass. The REQ-030 lock-policy amendment remains security-clean: `NonBlocking` returns `LockError::Contended` before any filesystem mutation; contention vs. I/O fault is correctly disambiguated via `try_lock`/`is_contended` at `patina-core/src/lock.rs:282-296`; `Held` consumes the non-`Clone` `LockGuard` by value with no re-acquire; the `Contended` Display leaks only the lock path, matching the pre-existing `Timeout` variant; no new dependencies, input parsing, endpoints, secret handling, or auth boundaries. No blockers.
</review>

<review date="2026-05-30T05:08:00Z" model="claude-sonnet-4-6[1m]/medium" persona="style" verdict="pass" round="2">
Round-1 blocker resolved: `patina-core/src/apply/engine.rs:596` now reads `mod tests {`, matching the uniform `mod tests` convention used by all other test modules in `patina-core/src/`. The rename is the only change in round 2; no production code was altered and no new style issues are introduced.
</review>

<review date="2026-05-30T05:08:00Z" model="claude-opus-4-8[1m]/medium" persona="correctness" verdict="pass" round="2">
REQ-030 lock-policy wiring remains logically sound; the round-2 delta is a test-only module rename with no change to production control flow. Guard is resolved before any mutation (engine.rs:342); `NonBlocking` short-circuits via `?` ahead of `flush_plan_and_fsync` (engine.rs:375), preserving zero-mutation on contention (CHK-065); `Held(guard)` consumes the caller's guard without re-acquiring, so no self-deadlock (CHK-066); `try_acquire` (lock.rs:281) maps contention to `LockError::Contended` and other failures to `LockError::Io`, and `EngineError::Lock` carries `#[from] LockError` for correct propagation. `open_lock_file` preserves the original `OpenOptions`, leaving the default `Blocking` path unchanged (CHK-067). No module collision (single `mod tests`); both unit tests pass. No correctness defect at or above the reporting threshold.
</review>
