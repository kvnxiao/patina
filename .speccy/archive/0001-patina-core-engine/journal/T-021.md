---
spec: SPEC-0001
task: T-021
generated_at: 2026-05-29T09:00:00Z
---

<implementer date="2026-05-29T09:00:00Z" model="claude-opus-4-8[1m]/medium" round="1">
- Completed: Made user-facing `patina apply` stdout byte-deterministic across
  consecutive applies (REQ-021) and locked the property with a regression
  test. This was an audit-confirms-clean plus add-the-test task: the
  foundations laid by earlier tasks already satisfy REQ-021, so no production
  source needed to change. Audit findings: (1) the JSON envelope
  (`patina-cli/src/cmd/apply.rs::json_envelope`) and the diff renderer
  (`patina-cli/src/output/diff.rs`) are pure functions of the `ResolvedPlan`
  and on-disk state — no timestamp, PID, or random ID feeds either; (2)
  collection ordering that surfaces on stdout is already deterministic —
  `discover_modules` sorts module handles alphabetically by name
  (`patina-core/src/discovery/modules.rs:177`), `walk_files` sorts copy-tree
  entries (`patina-core/src/apply/mod.rs:279`), and per-target plan rows
  preserve input declaration order (a stable function of inputs, not
  alphabetised, exactly as the SPEC scenario requires); (3) the only
  `jiff::Timestamp::now()` call on a non-test/non-journal path is
  `cmd::apply::current_timestamp()`, which keys the journal `<ts>` filename
  only and never reaches stdout — the SPEC's explicitly-permitted timestamp
  location, already documented by that function's doc comment. New test file
  `patina-cli/tests/deterministic_stdout.rs` (3 cases): JSON byte-identity
  across two consecutive applies (CHK-035), human-mode byte-identity, and
  multi-target input-order preservation.
- Undone: Nothing in scope. No production code changed because the audit found
  REQ-021 already satisfied; the task's deliverable is the regression test
  that gates it. No non-goal was touched.
- Hygiene checks: `cargo test --workspace` exited 0 (all suites pass, including
  the new `patina-cli/tests/deterministic_stdout.rs`, 3 cases). `cargo clippy
  --workspace --all-targets --all-features -- -D warnings` exited 0. `cargo
  +nightly fmt --all --check` exited 0 (after `fmt` split the two trailing
  `assert_eq!` value arguments onto separate lines). `cargo deny check` exited
  0 (no new dependencies — the test uses `camino`, `fs-err`, `tempfile`,
  `serde_json`, and `std` only; pre-existing unmatched-license-allowance
  warnings are non-fatal).
- Evidence: paper trail at
  `.speccy/specs/0001-patina-core-engine/evidence/T-021.md`. Roll call for the
  CHKs under REQ-021:
  - CHK-035 (clean state, two consecutive `--yes --json` applies →
    `diff -u out1 out2` empty): demonstrated → evidence Scenario 1, mirrored by
    `deterministic_stdout.rs::json_apply_is_byte_identical_across_two_runs`.
  REQ-021 attaches exactly one `<scenario>` (CHK-035). The other `<done-when>`
  / `<behavior>` bullets are additional demonstrated coverage (not separate
  CHK ids): human-mode byte-identity (Scenario 2,
  `human_apply_is_byte_identical_across_two_runs`) and the grep-clean
  no-non-deterministic-input property (the grep audit + the input-order test
  `multi_target_rows_preserve_input_declaration_order`, Scenario 3). No CHK in
  T-021's scope is `hygiene`- or `judgment-only`-labelled.
- Discovered issues: While drafting the test I confirmed a subtlety worth
  recording: the property REQ-021 guards is stdout stability *as a function of
  inputs*, not invariance across a state transition. A from-scratch apply and
  an immediately-following no-op apply legitimately differ (human diff shows
  `+ indent = 2` insertion vs `  indent = 2` context line), because the diff
  renderer compares against the target's current on-disk content. The test
  therefore primes the repo with one apply, then asserts byte-identity across
  two *measured* applies that both observe the converged state — which is the
  CHK-035 "clean state … unchanged repository" shape. No defect found; the
  renderer and envelope were already deterministic.
- Procedural compliance: First-attempt pass on a clean working tree; this
  implementer entry is created directly in `journal/T-021.md` per the
  journal-file schema (no `<implementer-note>` written into TASKS.md). The
  TASKS.md `state` attribute for T-021 flips `pending → in-progress →
  in-review` this turn. No shipped skill bodies under `.claude/skills/` were
  edited.
</implementer>

<review persona="business" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-05-29T10:00:00Z" round="1">
T-021 faithfully delivers REQ-021. The audit-confirms-clean claim is verified against the working tree: only `patina-cli/tests/deterministic_stdout.rs` is added, no production source changed. The JSON envelope (`patina-cli/src/cmd/apply.rs:144`) is a pure function of `ResolvedPlan` (repo_root/profile/plan/result, no timestamp/PID/random), and the sole `jiff::Timestamp::now()` (`apply.rs:267`) keys only the journal filename — the SPEC's explicitly-permitted location — never stdout. All three `<done-when>` bullets (JSON byte-identity = CHK-035, human byte-identity, grep-clean no-non-deterministic-input) plus the multi-target input-order behavior are covered by the three passing regression tests. The priming-apply test design correctly implements CHK-035's "unchanged repository" precondition and matches the SPEC's own gloss that "the second is effectively a no-op"; it locks the right property (stdout stability as a function of identical inputs). No scope creep, no non-goal touched, no open question silently resolved.
</review>

<review persona="tests" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-05-29T10:00:00Z" round="1">
The regression test genuinely gates REQ-021 / CHK-035 and is not vacuous. `patina-cli/tests/deterministic_stdout.rs:139` asserts raw `first.stdout == second.stdout` (`Vec<u8>` byte-identity) across two real `patina apply --yes --json` invocations of the built `CARGO_BIN_EXE_patina` binary — a true cross-process byte-comparison. The priming-apply-then-compare-runs-2-and-3 structure correctly establishes CHK-035's "unchanged repository" precondition. Catch-power independently confirmed: planting a `jiff::Timestamp::now()` into `json_envelope` turned `json_apply_is_byte_identical_across_two_runs` red (the two stdout buffers diverged on the injected `ts` field), then restored. A PID, random nonce, or HashMap-seeded reordering would diverge the same way. `multi_target_rows_preserve_input_declaration_order` asserts `.codex` precedes `.claude` for a deliberately non-alphabetical `targets` array — meaningful because the envelope emits rows in `op.targets` iteration order. Independently verified the audit-confirms-clean claim (only journal-keyed `now()` and lock-wait `Instant::now()`, both off the stdout path). Evidence file present, CHK-035 roll call complete, all three named tests appear in the diff, evidence command per-test scoped. All 3 tests pass.
</review>

<review persona="security" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-05-29T10:00:00Z" round="1">
Test-only change confirmed against the working tree: the sole T-021 deliverable is the new integration test `patina-cli/tests/deterministic_stdout.rs`; no production source modified. Test fixtures fully isolate the host: `HOME`, `USERPROFILE`, `XDG_STATE_HOME`, `LOCALAPPDATA` redirected to tempdir paths and `PATINA_PROFILE` removed before each `apply` spawn, so a filesystem-mutating `patina apply` never touches the developer's real `$HOME` or state dir. No secrets/credentials in fixtures or assertions; manifest bodies are inert literals; no injection vector. Verified the implementer's audit claim that no non-deterministic input reaches stdout (only `now()` is the journal-keying call, off the stdout path). The determinism property this test locks is mildly security-positive — it guards against wall-clock/PID/host data leaking into `--json` output consumed by CI scripts. No authn/authz, crypto, or secret-handling surface in scope. No blockers.
</review>

<review persona="style" verdict="blocking" model="claude-sonnet-4-6[1m]/medium" date="2026-05-29T10:00:00Z" round="1">
`patina-cli/tests/deterministic_stdout.rs` duplicates the `Fixture` struct and the `code()` helper verbatim from `patina-cli/tests/apply_cli.rs`. The project's `rust-let-types-carry-invariants.md` rule (under "Reuse over reinvent") is explicit: "A parallel copy drifts" and calling the existing one rather than introducing a parallel implementation is a write-time step.

The duplication is not cosmetic. The copy in `deterministic_stdout.rs` already silently dropped the explanatory inline comment on `LOCALAPPDATA` isolation (why parallel tests must never share a state tree) and trimmed one sentence from the `module()` doc — exactly the drift the rule anticipates. `exit_codes.rs` has its own copy too, so the codebase already has three copies of the same `Fixture`/`code()` pair.

Fix: extract the shared fixture into `patina-cli/tests/common/mod.rs` (or `tests/common.rs`), declare it with `mod common;` in each affected integration-test file, and import `common::Fixture`/`common::code`. All three existing files (`apply_cli.rs`, `exit_codes.rs`, `deterministic_stdout.rs`) should then delegate to that single module. Affected line ranges in the new file: `Fixture` struct at lines 27–80, `code()` at lines 83–85.
</review>

<blockers date="2026-05-29T10:00:00Z" round="1">
One blocking verdict (style); business, tests, security passed. Test-fixture duplication to resolve before the next pass:
- `patina-cli/tests/deterministic_stdout.rs` copies the `Fixture` struct (lines ~27–80) and `code()` helper (lines ~83–85) verbatim from `apply_cli.rs`; `exit_codes.rs` also carries its own copy, so there are now three copies (rust-let-types-carry-invariants.md "Reuse over reinvent"). Extract the shared fixture into a single `patina-cli/tests/common/mod.rs` module (`Fixture` + `code()`, with the full `LOCALAPPDATA`-isolation doc comment preserved), and migrate all three integration-test files (`apply_cli.rs`, `exit_codes.rs`, `deterministic_stdout.rs`) to `mod common;` + `use common::{Fixture, code};`, deleting their local copies. Keep the existing test behaviour byte-for-byte; re-run the full hygiene suite (test / clippy -D warnings / nightly fmt --check / deny check).
</blockers>

<implementer date="2026-05-29T12:00:00Z" model="claude-opus-4-8[1m]/medium" round="2">
- Completed: Resolved the round-1 style blocker by extracting the duplicated
  test fixture into one shared module. New file
  `patina-cli/tests/common/mod.rs` holds the single canonical `Fixture`
  (`new`, `module`, `state_root`, `apply_with_env`, `apply`) and the `code()`
  helper, preserving verbatim the full `LOCALAPPDATA`-isolation doc comment
  from the original `apply_cli.rs` copy (the richest of the three) and the
  "optional source file" `module()` doc. Migrated all three integration-test
  files to `mod common;` + `use common::Fixture; use common::code;`, deleting
  each file's local `Fixture`/`code()` copy: `apply_cli.rs`, `exit_codes.rs`,
  and `deterministic_stdout.rs`. `exit_codes.rs` keeps only its file-specific
  `hook_module()` free fn; its former `apply_with_env`/`state_root` were the
  superset adopted into common, so no behaviour was lost. All test bodies,
  assertions, fixtures, and manifest literals are byte-for-byte unchanged.
- Undone: Nothing in scope. The amend was confined to the three test files
  plus the new common module; no production source touched, no test semantics
  altered.
- Hygiene checks: `cargo test --workspace` exited 0 (all suites pass; the
  three migrated crates run green: apply_cli 8, exit_codes 5,
  deterministic_stdout 3). `cargo clippy --workspace --all-targets
  --all-features -- -D warnings` exited 0 (after two attribute corrections,
  below). `cargo +nightly fmt --all --check` exited 0. `cargo deny check`
  exited 0 (only the pre-existing unmatched-`Zlib`-license-allowance warning,
  non-fatal; no new dependencies).
- Evidence: paper trail at
  `.speccy/specs/0001-patina-core-engine/evidence/T-021.md` (round-1 evidence
  unchanged; the property under test is identical). Roll call for the CHKs
  under REQ-021:
  - CHK-035 (clean state, two consecutive `--yes --json` applies ->
    byte-identical stdout): demonstrated -> still gated by
    `deterministic_stdout.rs::json_apply_is_byte_identical_across_two_runs`,
    now sourcing its `Fixture`/`code` from `common`. The test ran green
    post-migration. No other CHK is in T-021's scope.
- Discovered issues: Two clippy `unfulfilled-lint-expectations` surfaced from
  the migration and were fixed at write time, not after a review bounce.
  (1) `apply_cli.rs`'s file-level `#![expect(clippy::expect_used)]` became
  unfulfilled once its only remaining `.expect()` calls lived inside `#[test]`
  fns (covered by `allow-expect-in-tests`); removed the now-redundant
  attribute. (2) The shared `common/mod.rs` carries `dead_code` as
  `#![allow(...)]` rather than `#![expect(...)]`: because each including crate
  uses a different subset of the fixture helpers, no single `expect` is
  fulfilled in every crate (the crate using all helpers reports it
  unfulfilled). The reason string documents this. `exit_codes.rs` keeps its
  `expect_used` attribute: its `hook_module` free fn still uses `.expect()`
  outside a `#[test]` fn, so the expectation remains fulfilled there.
- Procedural compliance: Retry pass on the prior round's uncommitted WIP,
  amended in place (no `git restore`/`clean`/`checkout`; the round-1
  `deterministic_stdout.rs` was edited, not rewritten from scratch). This
  `<implementer round="2">` block is appended after the round-1 blockers; no
  earlier journal content was modified. TASKS.md T-021 `state` flips
  `pending -> in-progress -> in-review` this turn. No shipped skill bodies
  under `.claude/skills/` were edited.
</implementer>

<review persona="business" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-05-29T13:00:00Z" round="2">
Round-2 delta is a test-only fixture de-duplication that fully resolves the round-1 style blocker without disturbing REQ-021 coverage. No production source changed (`git diff --name-only HEAD` = TASKS.md + the two migrated test files only; untracked is `tests/common/mod.rs`, `tests/deterministic_stdout.rs`, and speccy artifacts). CHK-035 stays gated by `patina-cli/tests/deterministic_stdout.rs` (`json_apply_is_byte_identical_across_two_runs`), whose body is byte-for-byte unchanged — only its `Fixture`/`code` source moved to `common`. The human-mode and multi-target-input-order proofs are intact, and the migrated `common::Fixture` is the faithful superset (verbatim `LOCALAPPDATA`-isolation doc comment; `apply()` delegates to `apply_with_env(args, &[])`, identical behaviour). All three affected crates pass. No scope creep, no non-goal touched. Minor non-blocking nit: the round-2 journal entry says "apply_cli 8" but the crate runs 7 — a self-report miscount, coverage unaffected.
</review>

<review persona="tests" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-05-29T13:00:00Z" round="2">
Round-2 shared-fixture migration preserves test behaviour byte-for-byte. The working-tree diff on `apply_cli.rs` and `exit_codes.rs` removes only the duplicated `Fixture`/`code` definitions and substitutes `mod common;` + `use common::{Fixture, code};`; every `#[test]` body is untouched and no assertion was weakened or dropped. `common/mod.rs` is a true superset of both files' prior fixtures (the `state_root`/`apply_with_env` helpers `exit_codes.rs` relied on are preserved verbatim; `apply` delegates to `apply_with_env(args, &[])`). `deterministic_stdout.rs` keeps its real `first.stdout == second.stdout` byte-identity assertion across two real `--yes --json` applies of `CARGO_BIN_EXE_patina`. Independently re-confirmed catch-power post-migration by planting a `SystemTime::now()` field into `json_envelope`: `json_apply_is_byte_identical_across_two_runs` went red on diverging stdout buffers, then green after revert (production file back to clean diff). All three suites compile and pass (apply_cli 7, deterministic_stdout 3, exit_codes 5). Evidence per-step compliant; CHK-035 roll-call `demonstrated` and tied to a named test in the diff; no fabrication patterns. "apply_cli 8" is a harmless prose miscount.
</review>

<review persona="security" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-05-29T13:00:00Z" round="2">
Round-2 test-only de-duplication is security-clean; host isolation survived the extraction. Confirmed no production source changed (`git diff HEAD` and untracked scan outside `patina-cli/tests`/`.speccy` both empty). The extracted `patina-cli/tests/common/mod.rs::apply_with_env` preserves the complete host-isolation set exactly once: `HOME`, `USERPROFILE`, `XDG_STATE_HOME`, `LOCALAPPDATA` redirected to per-fixture tempdir paths, plus `env_remove("PATINA_PROFILE")` — so a filesystem-mutating `patina apply` never touches the developer's real `$HOME`, state dir, journal, lock, or backup tree. The `LOCALAPPDATA`-isolation comment is preserved verbatim. `Fixture::state_root` resolves from the fixture's own isolated env values, preventing concurrent-test collisions. Collapsing three divergent copies into one canonical source is mildly security-positive (removes the drift surface where one copy could silently lose an isolation env var). No secrets in fixtures; no authn/authz, crypto, or injection surface. Verified isolation works: 15/15 green across the three migrated suites. No blockers.
</review>

<review persona="style" verdict="pass" model="claude-sonnet-4-6[1m]/medium" date="2026-05-29T13:00:00Z" round="2">
Round-1 blocker resolved correctly. `patina-cli/tests/common/mod.rs` holds the single canonical `Fixture` + `code()`, with the full `LOCALAPPDATA`-isolation doc comment preserved verbatim. All three files (`apply_cli.rs`, `exit_codes.rs`, `deterministic_stdout.rs`) use `mod common;` + `use common::{Fixture, code};` with their local copies deleted.

The two write-time attribute choices are both sound: (1) `apply_cli.rs` dropping `#![expect(clippy::expect_used)]` is correct — every remaining `.expect()` there is inside a `#[test]` fn, covered by `allow-expect-in-tests`, and the helpers that justified the expectation now live in `common`. (2) `common/mod.rs` using `#![allow(dead_code)]` rather than `#![expect(dead_code)]` is the correct call: each integration-test file is its own crate compilation unit, `state_root`/`apply_with_env` are live in `exit_codes.rs`'s unit but dead in the others, so `#![expect(dead_code)]` would trigger `unfulfilled_lint_expectations` in the `exit_codes` compilation; `#![allow]` with a justification string is the right tool. No naming drift, no dead imports, no parallel reimplementation, no doc/code mismatch.
</review>
