---
spec: SPEC-0001
task: T-026
generated_at: 2026-05-29T00:00:00Z
---

<implementer date="2026-05-29T00:00:00Z" model="claude-opus-4-8[1m]/medium" round="1">
- Completed: Widened the committed apply record per REQ-029 as one atomic
  change so the workspace compiles and CI stays green.
  `ExpectedTarget::Content` (`patina-core/src/journal/record.rs`) now carries
  `source: String` (canonical absolute source) and `hash: [u8; 32]` (a
  `blake3` digest) in place of `fingerprint: u64`; added a `source()`
  accessor returning the materialization source for either variant (the
  recorded `link_target` for `Symlink`, the new `source` for `Content`), and
  documented `Symlink.link_target` as the symlink's canonical source.
  Replaced `fingerprint_bytes(&[u8]) -> u64` with `content_hash(&[u8]) ->
  [u8; 32]` (`*blake3::hash(bytes).as_bytes()`), used by both write and read
  sides; re-exported it from `journal/mod.rs` and the crate root
  (`patina-core/src/lib.rs`). Bumped the shared `FILE_MAJOR_VERSION` `1`→`2`
  in `patina-core/src/journal/plan.rs` (the envelope rule already refuses a
  newer major, so no other version-handling code changed). Write side
  (`patina-core/src/apply/engine.rs` `build_apply_record`) captures
  `record.source` and `content_hash(&bytes)` for content targets. Read side
  (`patina-core/src/status/classify.rs`) compares a freshly computed
  `content_hash` against the recorded `hash`; CLEAN/DRIFTED/MISSING/ORPHANED
  logic unchanged. Added `blake3 = "1.5"` to `[workspace.dependencies]`
  (`Cargo.toml`) and referenced it from `patina-core/Cargo.toml`. Updated the
  mechanical ripple sites that build/destructure the changed variants:
  in-module tests in `record.rs` and `classify.rs`, and the rollback test in
  `rollback/mod.rs` (`rollback/replay.rs` and `journal/recovery.rs` only
  referenced these via `source`/`entry` accessors or doc-comment prose, so
  needed no field changes). Added integration coverage in
  `patina-cli/tests/commit_record.rs` driving `patina apply --yes` and
  decoding the COMMIT record.

- Undone: Nothing in T-026's scope is deferred. T-010 and T-017 stay
  `completed` per the 2026-05-29 amendment — their REQ-011/012 and REQ-018
  semantics are unchanged and this task's review covers the files it touches.

- Hygiene checks: `cargo test --workspace` exited 0 (all suites pass,
  including the five new `commit_record` integration tests and the new
  `content_hash_*` / `source_accessor_*` unit tests). `cargo clippy
  --workspace --all-targets --all-features -- -D warnings` exited 0 (the new
  integration-test crate carries `#![expect(clippy::expect_used)]`,
  `#![expect(clippy::panic)]`, and `#![expect(clippy::indexing_slicing)]`
  inner attributes matching the sibling `status_cli.rs` / `executor_modes.rs`
  convention). `cargo +nightly fmt --all --check` exited 0. `cargo deny
  check` exited 0 — `blake3`'s license expression cleared the existing
  allowlist (advisories/bans/licenses/sources all ok); no `deny.toml` edit
  required.

- Evidence: red-then-green paper trail at
  `.speccy/specs/0001-patina-core-engine/evidence/T-026.md`. Roll call for
  the CHKs under REQ-029:
  - CHK-062 (copy target records canonical source + 32-byte `blake3` hash):
    demonstrated → evidence Scenario 1 covers a copy-mode apply whose decoded
    COMMIT entry names the canonical source and a `hash` equal to
    `content_hash` (blake3) of the source bytes.
  - CHK-063 (two unchanged applies record byte-identical `blake3` hash):
    demonstrated → evidence Scenario 2 extracts the `hash` from two
    consecutive COMMIT records and asserts byte-equality.
  - CHK-064 (COMMIT envelope major reads `2`): demonstrated → evidence
    Scenario 3 reads the COMMIT file's first two bytes as a little-endian
    `u16` and asserts `== 2`.
  Two REQ-029 done-when items beyond the three rendered CHKs are also
  covered by tests in `commit_record.rs`:
  `symlink_target_records_link_target_as_source` (the symlink source is the
  canonical link target) and `status_uses_recorded_blake3_for_drift` (the
  read side reports `clean` with no edit and `drifted` after an external
  edit, confirming it compares the recorded `blake3`). The
  newer-major-refusal done-when item is unchanged behaviour already covered
  by `journal_fsync_shape.rs` against the bumped `FILE_MAJOR_VERSION`. The
  `content_hash_is_blake3_of_the_bytes` unit test in `record.rs` pins the
  helper to blake3 by re-deriving the digest (not copying a constant), so a
  silent swap to another hash is caught. No CHK in scope is `judgment-only`.

- Discovered issues: None. The pre-existing module doc comment in
  `journal/recovery.rs` uses the word "fingerprint" in prose unrelated to the
  content-hash mechanism (it describes the crash-recovery signature); left
  unchanged as out of scope.

- Procedural compliance: This implementer entry lands directly in
  `journal/T-026.md` per the journal-file schema; no `<implementer-note>` was
  written into TASKS.md. The TASKS.md `state="..."` attribute for T-026 flips
  from `in-progress` to `in-review` as the final step of this turn. No
  shipped skill bodies under `.claude/skills/` were edited.
</implementer>

<review persona="business" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-05-29T07:20:11Z" round="1">
T-026 (REQ-029) delivers exactly what the SPEC promises. Every `<done-when>` item maps to the diff and is gated by a real test: content targets record a canonical `source` + 32-byte `blake3` hash (`patina-core/src/apply/engine.rs:421-431`), symlink targets record the link target as source via the `source()` accessor (`patina-core/src/journal/record.rs:98-104`), the shared `FILE_MAJOR_VERSION` is bumped to `2` so both `.plan` and `.COMMIT` carry `2` at offset 0 (`patina-core/src/journal/plan.rs:28`), the newer-major refusal is preserved, and `status` classifies content by freshly computed `blake3` vs the recorded hash with CLEAN/DRIFTED behaviour unchanged (`patina-core/src/status/classify.rs:94-99`). CHK-062/063/064 plus the two extra done-when items (symlink source provenance, status drift) are covered by five passing integration tests in `patina-cli/tests/commit_record.rs` that assert load-bearing properties (the hash test re-derives the blake3 digest rather than copying a constant; the drift test flips clean→drifted on an external edit). Verified locally: `cargo test --workspace --locked`, `cargo clippy --workspace --all-targets --locked -- -D warnings`, and `cargo deny check` all exit 0 (deny's license warnings are pre-existing unmatched-allowance notices, not failures; `blake3` cleared without an allowlist edit). The change is surgical (10 files, the exact set the task names) with no scope creep into the SPEC non-goals — no `gc`/`--repo`/merge-mode surface added, and the rollback edit is a mechanical test-fixture update leaving rollback logic untouched. The diff reflects the latest 2026-05-29 Changelog intent (blake3 + per-target source), not the superseded `u64` fingerprint, and it serves the named cross-SPEC consumers (SPEC-0002 remove/promote target→source map, SPEC-0003 drift cache and watcher source paths from the post-commit-truth COMMIT record). All sixteen-plus open questions in the SPEC are resolved; none are silently decided here.
</review>

<review persona="tests" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-05-29T07:20:11Z" round="1">
T-026 tests faithfully exercise all three covered scenarios against the real code path. CHK-062 (`copy_target_records_canonical_source_and_blake3_hash`), CHK-063 (`two_applies_record_byte_identical_hash`), and CHK-064 (`commit_envelope_major_is_two`) each drive a real `patina apply --yes` subprocess and decode the on-disk `<ts>.COMMIT` record — no mocking of the system under test. The source assertion independently re-canonicalizes the fixture path (`canonical(&source)`) and compares byte-for-byte against `entry.source()`, so a wrong/empty recorded source fails. CHK-064 reads the raw envelope u16 at offset 0 from disk (not the `FILE_MAJOR_VERSION` constant), so it would fail if the bump to `2` were reverted. The blake3 binding is pinned non-circularly: the integration test asserts the engine fed the source bytes through `content_hash`, while the in-module `content_hash_is_blake3_of_the_bytes` unit test re-derives `*blake3::hash(b"payload").as_bytes()` rather than copying a constant — catching a silent hash-function swap. The read-side negative path (`status_uses_recorded_blake3_for_drift`) asserts specific `clean`→`drifted` state transitions, not catch-any. Evidence roll call at `.speccy/specs/0001-patina-core-engine/evidence/T-026.md` is complete for REQ-029's CHK-062/063/064 (all `demonstrated` with named tests); every cited test name appears in the diff (`patina-cli/tests/commit_record.rs` and the `record.rs` unit tests). The red phase is a legitimate compile-failure red (the pre-edit `Content` variant lacked `source`/`[u8;32]`), and the evidence anchors on the scoped per-test crate, distinct from the `cargo test --workspace` hygiene gate. No vacuous-test shapes found.
</review>

<review persona="security" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-05-29T07:20:11Z" round="1">
No security issues. blake3 (resolved 1.8.5) is a sound, maintained cryptographic hash with no misconfigurable parameters and clears `cargo deny`; it strictly strengthens the prior non-cryptographic fingerprint used for local drift detection. The new `source: String` records a canonical absolute path into the local COMMIT record at the same trust boundary as existing `target`/`link_target` fields and is not surfaced in any status output, log, or error message, so it introduces no new sensitive-data-exposure or injection/traversal vector. The `FILE_MAJOR_VERSION` 1→2 bump correctly fences older binaries off the widened format. Reviewed `patina-core/src/journal/record.rs`, `patina-core/src/apply/engine.rs`, `patina-core/src/status/classify.rs`, and `patina-cli/tests/commit_record.rs`.
</review>

<review persona="style" verdict="pass" model="claude-sonnet-4-6[1m]" date="2026-05-29T07:20:11Z" round="1">
All changed files are consistent with the project's style conventions. `record.rs` has the new `source: String` and `hash: [u8; 32]` fields, the `content_hash` helper calls `blake3::hash(...)` directly (no dead `std::hash` imports), and the `source()` accessor follows the existing `target()` / `entry()` accessor pattern. `engine.rs` imports `crate::journal::content_hash` and `classify.rs` imports the same symbol, so the write and read sides share one hash implementation. The `blake3` dependency is declared once in `[workspace.dependencies]` and referenced from `patina-core/Cargo.toml` per the workspace rules. All `#![expect]` suppressions in the new `commit_record.rs` integration test carry `reason` fields, satisfying `allow_attributes_without_reason = "deny"`. The three separate `#![expect]` blocks differ cosmetically from the sibling `status_cli.rs`'s single combined block, but Clippy accepts both forms and the divergence is not lint-triggering. No dead imports, no leftover `fingerprint_bytes` calls in production code, no parallel helper reinvented.
</review>
