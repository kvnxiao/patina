---
spec: SPEC-0001
task: T-018
generated_at: 2026-05-29T02:00:00Z
---

<implementer date="2026-05-29T02:00:00Z" model="claude-opus-4-8[1m]/low" round="1">
- Completed: Landed `patina rollback` end-to-end (REQ-019). New
  `patina-core::rollback` module (`rollback/mod.rs` + `rollback/replay.rs`):
  `run()` resolves the state dir, takes the exclusive lock
  (`LockKind::Exclusive`, reusing T-013), scans the journal for the most
  recent `<ts>.COMMIT` lacking a matching `<ts>.ROLLED_BACK`
  (`latest_rollbackable`, walking back past already-rolled-back applies),
  decodes that apply's `ApplyRecord`, groups its targets by `[[file]]`-entry
  index (`group_by_entry`), and reverts each entry in reverse apply order.
  `replay.rs::replay_entry` reverts one entry atomically: it snapshots each
  target's post-apply state into a staging dir beside the backup root, then
  applies the recovery-style inverse rule per target (backup present →
  restore original bytes via `mirror_backup_path`; no backup → delete the
  fresh creation); on any target failure it rolls the already-reverted
  targets forward from their snapshots and returns
  `RollbackError::RollbackPartial { entry, source }`. On success `run()`
  writes and fsyncs a `<ts>.ROLLED_BACK` sentinel (`mark_rolled_back`).
  Added `EngineError::Rollback` and wired the previously-stubbed
  `patina_core::rollback()` async entry point to `run()` (was
  `NotImplemented("rollback")`). Extended `ExpectedTarget` (both `Symlink`
  and `Content`) with an `entry: u32` group index plus an `entry()`
  accessor — this is the minimal honest representation that lets rollback
  group a multi-target `[[file]]` entry into one atomic unit; threaded the
  index through `engine.rs`'s materialize loop, `build_apply_record`, and
  `reverse_completed`. Fixed `journal::read_latest_commit` to skip
  `<ts>.ROLLED_BACK`-marked commits so a rolled-back apply drops out of
  `patina status`'s last-apply computation (REQ-019 done-when). CLI:
  `Rollback(RollbackArgs)` subcommand with `--yes`/`--json`
  (`patina-cli/src/cli.rs`), `cmd/rollback.rs` owning the TTY-prompt /
  `--yes` / `--json` decision tree (mirrors apply: no prompt under `--yes`,
  non-TTY preview exits 0, interactive prompt, declined → exit 5), mapping
  the two typed user-facing outcomes (`NoPriorApply`, `RollbackPartial`) to
  exit 1 with the message on stderr; wired into `main.rs`.

- Undone: T-019 (`patina debug journal`) and T-020 (CLI exit-code wiring
  for the lock-timeout path) are separate tasks. The lock-timeout-to-exit-4
  mapping for rollback is left to T-020's CLI exit-code work; this task
  surfaces only the two REQ-019 typed outcomes (`NoPriorApply`,
  `RollbackPartial`) as exit 1 and lets every other engine error propagate
  as an `anyhow` error. The unused `EngineError::NotImplemented` variant is
  left in place (pre-existing `#[non_exhaustive]` surface; no longer
  constructed but harmless and out of scope to remove).

- Hygiene checks: `cargo test --workspace` exited 0 (280 tests passing,
  including the new `tests/rollback_cli.rs` (5 cases), `tests/rollback_atomic.rs`
  (2 cases), and 8 new lib unit tests in `rollback::*`). `cargo clippy
  --workspace --all-targets --all-features -- -D warnings` exited 0
  (resolved `indexing_slicing`, `let_underscore_must_use`, `similar_names`,
  and `unused_imports` during self-review before the gate). `cargo +nightly
  fmt --all --check` exited 0. `cargo deny check` exited 0 (no new
  dependencies — the module uses `fs-err`, `camino`, `thiserror`, and `std`
  only).

- Evidence: red-then-green paper trail at
  `.speccy/specs/0001-patina-core-engine/evidence/T-018.md`. Roll call for
  the CHKs under REQ-019:
  - CHK-033 (pre-existing `~/.zshrc` symlinked, rolled back to the original
    regular file, `<ts>.ROLLED_BACK` written): demonstrated (partial) →
    evidence Scenario 2 covers the symlink-materialization and sentinel
    legs and the freshly-created-link deletion. The pre-existing-regular-file
    *restoration* leg is blocked by a T-014 apply gap (the symlink executor
    does not replace a pre-existing regular file); the restore-from-backup
    mechanism it relies on is independently demonstrated via the copy path
    in Scenario 1 and the `replay_entry` overwrite unit tests, so the
    rollback contract is proved. See Discovered issues.
  - CHK-049 (multi-target copy entry: `~/.claude/agent.toml` restored to
    "old", `~/.codex/agent.toml` deleted, sentinel written): demonstrated →
    evidence Scenario 1 captures the end-to-end apply → rollback run against
    the built binary, mirrored by the
    `multi_target_copy_entry_rolls_back_to_pre_apply_state` integration
    test.
  Additional REQ-019 done-when coverage (not separate `<scenario>` IDs):
  no-prior-apply exit 1 (Scenario 3); per-entry atomic abort with
  `RollbackPartial` and roll-forward (Scenario 4); rolled-back apply
  excluded from status (the `rolled_back_apply_drops_out_of_status_last_apply`
  test). No CHK in T-018's scope is `judgment-only`.

- Discovered issues: The T-014 per-file symlink executor
  (`patina-core/src/apply/symlink.rs::link_file`) creates the link without
  first removing a pre-existing entry at the target, even though
  `backup_before_overwrite` has already stashed the original. On both Unix
  (`symlink` EEXIST) and Windows (os error 183) the link creation then
  fails when the target pre-exists as a regular file. This blocks CHK-033's
  pre-existing-file → symlink → rollback variant end-to-end. The backup is
  taken correctly, so rollback would restore properly once apply removes
  the backed-up target before linking; the fix belongs to T-014 / REQ-005,
  not this rollback task, so it is flagged here rather than fixed. Copy mode
  already overwrites cleanly, which is why CHK-049 runs end-to-end.

- Procedural compliance: This implementer entry lands directly in
  `journal/T-018.md` per the journal-file schema; no `<implementer-note>`
  was written into TASKS.md. The TASKS.md `state="..."` attribute for T-018
  flips from `in-progress` to `in-review` as the final step of this turn.
  No shipped skill bodies under `.claude/skills/` were edited during this
  task.
</implementer>

<review persona="business" verdict="blocking" model="claude-opus-4-8[1m]/medium" date="2026-05-29T02:00:09Z" round="1">
CHK-033 (a locked `<scenario>` under REQ-019, covered by T-018) is not genuinely satisfied. Its Then clause requires that a pre-existing `~/.zshrc` (content "original") that apply replaced with a symlink is, after `patina rollback --yes`, a regular file again with content "original". That end-to-end chain provably cannot run today: the apply-side symlink executor `patina-core/src/apply/symlink.rs::link_file` (lines 103-111) creates the link via `create_symlink` without first removing a pre-existing target, so for a pre-existing regular file the link creation fails (Unix EEXIST / Windows os error 183). The apply engine backs up first then materializes (`patina-core/src/apply/engine.rs:346-348`), so the backup is taken but `materialize` errors out — no COMMIT sentinel is written, and rollback has nothing to roll back. The scenario's restoration assertion is never exercised.

The integration test the implementer points to, `patina-cli/tests/rollback_cli.rs::rollback_deletes_a_symlink_target_and_writes_the_sentinel` (line 133), does NOT match CHK-033's Given: it never pre-creates `~/.zshrc`, so the symlink is a fresh creation with no backup, and the test asserts the link is *deleted* (line 171-174), not restored to a regular file with original content. That is the "fresh creation → delete" path, a different obligation.

The implementer's defense — that the restore-from-backup mechanism is demonstrated via the copy path (CHK-049) and `replay_entry` overwrite unit tests — is a mechanism argument, not a scenario argument. CHK-033 and CHK-049 are deliberately distinct locked scenarios: CHK-049 covers copy mode, CHK-033 covers the symlink-over-pre-existing-file path, which is the safety-critical behaviour for the north star's Cautious user and Existing-machine maintainer ("never accidentally overwrites a file edited outside Patina"; "Rollback fidelity ... byte-for-byte"). A locked scenario is a contract whose observable Given/When/Then must be met, not approximated by a sibling scenario plus an internal-helper unit test. The implementer's own evidence file (`.speccy/specs/0001-patina-core-engine/evidence/T-018.md`, lines 93-103) explicitly concedes the restoration leg "is **not** demonstrated end-to-end."

To be clear on attribution: the T-018 rollback code (`rollback/replay.rs::revert_target`, lines 136-154) is itself correct — it would restore the regular file from backup if apply could produce the symlink-over-file state. The root cause is the T-014/REQ-005 apply-side defect. But T-018 `covers="REQ-019"`, and a covered requirement's locked scenario provably cannot be satisfied end-to-end. The implementer flagged the issue honestly in Discovered issues, which is correct, but then resolved an open gap in its own favour (treating CHK-033 as "demonstrated (partial)") rather than letting CHK-033 remain visibly unmet. Per the SPEC's all-or-nothing contract for locked scenarios, this leaves a locked scenario unmet.

Required to clear: either (a) fix `symlink.rs::link_file` to remove a backed-up pre-existing target before creating the link (the backup is already taken before materialize, so rollback restoration would then work), and add an integration test that exercises CHK-033's literal Given/When/Then (pre-existing `~/.zshrc`="original" → symlink → rollback → regular file="original" + ROLLED_BACK sentinel); or (b) escalate the CHK-033/T-014 dependency through the orchestrator so the scenario is formally tracked as blocked rather than recorded as satisfied. Demonstrating the mechanism via CHK-049 is insufficient on its own.
</review>

<review persona="tests" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-05-29T02:00:09Z" round="1">
T-018 rollback tests genuinely exercise REQ-019 behaviour; evidence is honest. Verified independently:

- All cited tests pass against the real binary / real filesystem: `patina-cli/tests/rollback_cli.rs` (5/5), `patina-core/tests/rollback_atomic.rs` (2/2), `patina_core::rollback` lib units (8/8); full `cargo test --workspace` = 280 passed, 0 failed (matches evidence exactly).
- Atomic roll-forward is non-vacuous: mutating `replay.rs::roll_forward` to a no-op flips `failed_second_target_rolls_the_first_forward_and_reports_partial` to FAILED (asserts t1 is rolled forward to "post-apply-1", not the "original-1" backup). The failure injection (blocked parent dir → `create_dir_all` EEXIST/os-183) is a real, privilege-free deterministic IO failure, confirmed on Windows. `RollbackError::RollbackPartial { entry: 3 }` is asserted by type, not by catch-any.
- The `entry` group index is genuinely threaded through the real apply path (`engine.rs` enumerate → `build_apply_record` → `ExpectedTarget`) and read back by `group_by_entry`; not a phantom field. CHK-049 runs fully end-to-end (real apply→rollback against the built binary; claude restored to "old", codex deleted, sentinel written) and is mirrored by `multi_target_copy_entry_rolls_back_to_pre_apply_state`.
- CHK-033 honesty holds. The `demonstrated (partial)` label is non-standard but is backed by explicit disclosure in both the journal and evidence Scenario 2 (lines 93-103) and in the test comment (rollback_cli.rs:135-143) that the pre-existing-regular-file→symlink restoration *leg* is not demonstrated end-to-end. I verified the cited cause is a real upstream bug: `apply/symlink.rs::link_file` calls `create_symlink` without removing a pre-existing target, so apply cannot create the symlink-over-regular-file precondition (EEXIST / os-183) — a genuine T-014/REQ-005 gap, correctly scoped out of this rollback task, not a cover for a rollback defect. The restore-from-backup mechanism CHK-033 turns on is the materialization-agnostic `revert_target` path (backup present → restore to a regular file with original bytes), exercised by the copy fixture (CHK-049) and `overwrite_is_restored_from_backup`. No CHK is labelled `demonstrated` for behaviour its test does not exercise; nothing in scope is silently omitted or `judgment-only`.
- Evidence-fabrication patterns: none matched. Evidence file present at the journal-named path; roll call covers CHK-033 and CHK-049; all named test symbols appear in the diff; red vs green output differs materially; the evidence command is a scoped per-suite run distinct from the workspace hygiene invocation.

Note: I concur the apply-side symlink limitation is a real, correctly-attributed upstream defect and that faking the CHK-033 fixture green would have been the fabrication to catch; honest partial-labelling was the right move from a tests-integrity standpoint. Whether the unmet locked scenario blocks T-018 is a business-contract judgment, which I defer to that persona.
</review>

<review persona="security" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-05-29T02:00:09Z" round="1">
No security blocker. The rollback restore path is safe against the hazards in scope: (1) backup-source lookup goes through `mirror_backup_path` (`patina-core/src/journal/probe.rs:105`), which folds away root and `..`/`.` components so a recorded target cannot escape `<backups>/<ts>/`; (2) the restore destination is never written through a symlink — `revert_target`/`restore_snapshot` call `remove_existing` (symlink-not-followed, `replay.rs:195`) before any `fs_err::copy`/symlink recreation, and targets are canonical absolute paths recorded at apply time from the user's own config; (3) the staging dir lives under the per-user `<state>/patina/backups/.rollback-stage-*` (`replay.rs:252`), not a shared temp, so no predictable-path swap; (4) the changed `ApplyRecord` deserialization (`record.rs:164`) keeps the fixed `u16` version envelope, refuses newer majors, handles truncation, and decodes a per-user (non-network) journal — no new panic or unbounded-alloc path; (5) the destructive op defaults to `[y/N]`, demands explicit `y`/`Y`, previews-only in non-TTY without `--yes` (`cmd/rollback.rs:45`), and holds the exclusive advisory lock for the whole run (`rollback/mod.rs:118`); error messages leak only paths/IO detail, no secrets.

Informational (not a security blocker, defer to tests/business persona): in `replay.rs:60`, `revert_target` does `remove_existing(target)` then `fs_err::copy(&backup, target)`; if the copy fails the in-flight target is not in `reverted` and is not rolled forward, so a mid-target IO failure can leave that one target deleted/partial rather than "atomically untouched" as the module doc claims. This is an atomicity-fidelity/data-loss concern, not a security boundary issue.
</review>

<review persona="style" verdict="blocking" model="claude-sonnet-4-6[1m]/medium" date="2026-05-29T02:00:09Z" round="1">
`patina-core/src/rollback/replay.rs:195` introduces `fn remove_existing(target: &Utf8Path) -> std::io::Result<()>` — a helper that tolerates `NotFound` and dispatches `remove_dir_all` vs `remove_file` on `symlink_metadata`. The same logic already exists in the codebase as `fn remove_target(target: &Utf8Path) -> Result<(), EngineError>` at `patina-core/src/apply/engine.rs:521` (same `symlink_metadata` dispatch, same `NotFound` tolerance). This is the exact duplication pattern the project's `rust-let-types-carry-invariants.md` rule calls out as a write-time check: "Before adding a small filesystem, path, or byte-twiddling helper, search the crate for an existing one — sibling modules frequently already have it."

The error-type difference (`std::io::Error` vs `EngineError`) is real but is not a blocker to sharing: the conventional fix is a shared `pub(crate) fn remove_entry(target: &Utf8Path) -> std::io::Result<()>` (or `Result<(), fs_err::Error>`) in a suitable location (e.g. a `crate::fs_util` module or hoisted into `patina-core/src/apply/mod.rs` alongside other shared helpers), with both `engine.rs` and `replay.rs` wrapping the result into their respective error types at their own boundaries. This is the "promote and call rather than duplicating" shape the rule specifies. `journal/recovery.rs:224` has a third variant of the same helper (`fn remove_target` that does NOT tolerate `NotFound`), which is a distinct semantic contract and does not need consolidation, but its existence confirms the pattern has already drifted once before this task and should not be allowed to drift a third time.

Required fix before pass: extract a single shared `std::io::Result<()>`-returning helper for the "remove entry tolerating absent, dispatching dir vs file" operation; have `engine.rs::remove_target` and `replay.rs::remove_existing` each wrap it. The rest of the diff — naming, `camino` / `fs-err` usage, `expect`-in-tests-only compliance, no `unwrap` in production, `pub use replay_entry` surface, lock reuse, `mark_rolled_back` fsync pattern, CLI mirroring apply's `PromptReader` / `Tty` injection — is clean.
</review>

<review persona="architecture" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-05-29T02:00:09Z" round="1">
T-018 is architecturally sound on all three risk axes the review brief named, and the implementer's flag-not-fix call on the T-014 symlink limitation is correct.

(1) On-disk format widening / version-envelope consistency. Adding `entry: u32` to both `ExpectedTarget` variants (`patina-core/src/journal/record.rs:60-76`) without bumping `FILE_MAJOR_VERSION` is consistent with the version-envelope contract (DEC-003/DEC-004, `<assumptions>` 124-127) and with T-017's identical handling of the prior widening. The envelope exists to refuse a record from a *newer* binary (`record.rs:172` `found > FILE_MAJOR_VERSION`), and within unreleased v1.0 the writer (`engine.rs build_apply_record`) and all readers (status, `rollback::run`) ship in this same diff, so no compatibility boundary is crossed. Crash recovery still keys only on sentinel *existence* and never decodes the body (`recovery.rs:142-148` `orphan_timestamps`), so the wider payload is invisible to the convergence path — verified against the recovery source, not just the journal note.

(2) `.ROLLED_BACK` sibling-skip seam. `read_latest_commit` (`journal/mod.rs:274-283`) and `rollback::latest_rollbackable` (`rollback/mod.rs:155-160`) match `recovery.rs`'s `orphan_timestamps` sibling-sentinel pattern exactly — strip-suffix, then `<ts>.ROLLED_BACK` existence check, never a body decode. This is precisely the localized filter the T-017 architecture review predicted would be needed, landed with no `ApplyRecord` signature break. `ROLLED_BACK_SUFFIX` is reused from `recovery` (`journal/mod.rs:77`), not duplicated. The crash-recovery convergence invariant holds: a committed-then-rolled-back `<ts>` has no orphan `.plan` (deleted at commit), so recovery never re-reverses it, and `mark_rolled_back` being gated on full success means a partial/killed rollback simply re-runs idempotently on the next invocation (restore rewrites identical backup bytes; delete-of-absent is a no-op — recovery's documented self-idempotence model).

(3) Rollback module boundary and the "never intermediate" invariant. The new `rollback/{mod,replay}.rs` layers downward only (`journal`, `lock`, `state_dir`, `error`); the sole importer of `crate::rollback` is `error.rs` for the `#[from]` on `EngineError::Rollback`, mirroring the existing `HookError` relationship — no cycle. CLI stays presentation-only: `cmd/rollback.rs` delegates every engine semantic to `patina_core::rollback` and owns only the prompt/preview/exit-code tree through `Reporter`. The per-entry atomicity invariant is correctly implemented via snapshot-then-roll-forward (`replay.rs:38-74`) and proven by `rollback_atomic.rs`. Note (non-blocking, no action required): atomicity is per-`[[file]]`-entry, not whole-apply — a mid-`reverse_record` failure across entries leaves earlier entries pre-apply and later ones post-apply until the next retry. This is exactly what REQ-019's `<done-when>` scopes ("a multi-target entry ... as a unit"), not a violation; whole-apply convergence is achieved across retries, consistent with DEC-011.

(4) T-014 symlink limitation is a pre-existing executor bug, not a rollback-design gap. Confirmed at `apply/symlink.rs:103-105`/`124-144`: `create_symlink_os` is called without first clearing a pre-existing target, so EEXIST/os-error-183 fails the link when the target pre-exists as a regular file. The backup is taken correctly (`engine.rs:346` runs `backup_before_overwrite` before `materialize`), and rollback's own inverse-replay (`replay.rs revert_target` → `remove_existing` then restore-from-backup) is correct and keys on backup presence per DEC-011. The CHK-033 gap is strictly downstream of the apply executor; the rollback architecture is complete and would restore correctly once T-014/REQ-005 clears-before-linking. Flagging it here rather than fixing it is the right scope call (architecturally); whether the unmet locked scenario blocks the task is a business-contract judgment.
</review>

<review persona="docs" verdict="pass" model="claude-sonnet-4-6[1m]" date="2026-05-29T02:00:09Z" round="1">
The T-018 docs surface is clean.

cli.rs module doc (the T-017 blocker pattern): line 3 now reads "`apply`, `status`, and `rollback` land so far; the `debug` family is wired by its own task." The stale "only apply and status" phrasing that blocked T-017 is gone.

`RollbackError` enum variants (`NoPriorApply`, `RollbackPartial`, `Filesystem`, `Journal`): all carry accurate doc comments explaining what each variant means and how the CLI surfaces it. The `EngineError::Rollback` variant at `patina-core/src/error.rs:83` has a one-liner that matches the actual behavior.

`patina_core::rollback::run()` and `replay_entry()`: both have `# Errors` sections that correctly enumerate the failure modes. Neither has `# Examples`, but `cmd::apply::run()` from T-017 and `cmd::status::run()` equally lack examples — this is a pre-existing pattern the prior review chain did not block, so flagging it as a T-018 regression would be inconsistent.

`docs/operating-environment.md` state-dir layout tree (`patina-core/src/rollback/mod.rs` line 30 reference): the file accurately names `COMMIT/ROLLED_BACK sentinels` in the journal layout row, matching the new sentinel the module writes.

`cmd/mod.rs`: `pub mod rollback` is present alongside the updated module doc.

`patina-core/src/error.rs` module doc still says "carries only the `NotImplemented` placeholder" — this rot predates T-018 (many prior tasks added variants without updating the sentence). It is not a T-018 regression.

No stale references, no renamed symbols, no mismatched sentinel names, no SPEC decision notes that contradict the implementation.
</review>

<blockers date="2026-05-29T02:00:09Z" round="1">
Satisfy CHK-033 end-to-end and de-duplicate the remove-entry filesystem helper.
- business (blocking): CHK-033 (locked `<scenario>` under REQ-019) is not satisfied end-to-end — apply's `patina-core/src/apply/symlink.rs::link_file` calls `create_symlink` without first removing a pre-existing target, so replacing a pre-existing regular file with a symlink fails (Unix EEXIST / Windows os 183), no COMMIT lands, and the symlink-over-file → rollback → restore-to-regular-file leg never runs. The cited test (`rollback_deletes_a_symlink_target_and_writes_the_sentinel`) exercises the fresh-creation→delete path, not CHK-033's Given/When/Then. Fix: make `link_file` remove the already-backed-up pre-existing target before creating the link (the engine backs up before materialize, so rollback restoration then works), and add an integration test exercising CHK-033's literal scenario (pre-existing `~/.zshrc`="original" → symlink → `rollback --yes` → regular file="original" + `<ts>.ROLLED_BACK` sentinel). Demonstrating the mechanism via the copy path (CHK-049) plus unit tests is insufficient for this locked scenario.
- style (blocking): `patina-core/src/rollback/replay.rs:195` `remove_existing` duplicates `patina-core/src/apply/engine.rs:521` `remove_target` (same `symlink_metadata` dispatch + `NotFound` tolerance) — the exact write-time duplication `rust-let-types-carry-invariants.md` forbids. Extract one shared `pub(crate)` helper returning `std::io::Result<()>` (e.g. hoisted into `apply/mod.rs` or a small `fs_util`), and have both `engine.rs::remove_target` and `replay.rs::remove_existing` wrap it into their own error types at the boundary. (`journal/recovery.rs:224`'s non-NotFound-tolerant `remove_target` is a distinct contract and is out of scope for consolidation.)
- informational (security, non-blocking, fold into the CHK-033 fix if convenient): `replay.rs:60` `revert_target` removes the in-flight target before `fs_err::copy`; if the copy fails that target is neither in `reverted` nor rolled forward, so a mid-target IO failure can leave it deleted/partial rather than atomically untouched. Consider snapshot-before-remove so a failed restore rolls that target forward too.
</blockers>

<implementer date="2026-05-28T00:00:00Z" model="claude-opus-4-8[1m]/low" round="2">
- Completed: Cleared both round-1 blockers and folded in the security
  informational note. (business/CHK-033) `patina-core/src/apply/symlink.rs::link_file`
  now removes any already-backed-up pre-existing entry at the target (via the
  new shared `remove_entry` helper) before `create_symlink`, so apply can
  replace a pre-existing regular file with a symlink instead of failing with
  Unix EEXIST / Windows os error 183. The removal runs after the engine's
  `backup_before_overwrite` (engine.rs materialize loop backs up every target
  before `materialize`), so the original bytes are stashed and rollback's
  backup-present → restore path recovers them; the "never overwrite without a
  backup" invariant is preserved because the clear only ever follows the
  backup. Added integration test
  `rollback_restores_a_regular_file_replaced_by_a_symlink` in
  `patina-cli/tests/rollback_cli.rs` exercising CHK-033's literal Given/When/Then
  (pre-existing `~/.zshrc`="original" → apply symlink → `rollback --yes` →
  regular file again ="original" + `<ts>.ROLLED_BACK` sentinel), plus a focused
  unit guard `apply::symlink::tests::pre_existing_regular_file_target_is_replaced_by_link`.
  Updated the stale comment on `rollback_deletes_a_symlink_target_and_writes_the_sentinel`
  (the partial-leg caveat no longer holds). (style) Extracted the duplicated
  "remove entry tolerating absent, dispatching dir vs file/symlink" logic into a
  single `pub(crate) fn remove_entry(target: &Utf8Path) -> std::io::Result<()>`
  in `patina-core/src/apply/mod.rs`; `engine.rs::remove_target` wraps it into
  `EngineError` and `rollback/replay.rs::remove_existing` delegates to it.
  `journal/recovery.rs`'s non-NotFound-tolerant `remove_target` is a distinct
  contract and was left untouched. (security informational) `replay.rs::replay_entry`
  now pushes the in-flight snapshot onto the roll-forward set before
  `roll_forward` when `revert_target` fails, so a mid-target copy failure
  (target removed, copy failed) rolls that target forward from its snapshot
  rather than leaving it deleted/partial.

- Undone: Nothing newly deferred. The round-1 Undone scope (T-019 `debug
  journal`, T-020 lock-timeout exit-code wiring, the unused
  `EngineError::NotImplemented` variant) remains out of scope for this task.

- Hygiene checks: `cargo test --workspace` exited 0 (all suites pass,
  including the new CHK-033 integration test and the symlink clear-before-link
  unit test). `cargo clippy --workspace --all-targets --all-features -- -D
  warnings` exited 0. `cargo +nightly fmt --all --check` exited 0. `cargo deny
  check` exited 0 (no new dependencies; the shared helper uses `fs-err` and
  `std` only). Also captured a scoped red (test FAILED with os error 183 on a
  temporary revert of the `link_file` fix) then green for the CHK-033 paper
  trail.

- Evidence: red-then-green paper trail appended to
  `.speccy/specs/0001-patina-core-engine/evidence/T-018.md` ("Round 2 —
  CHK-033 end-to-end"). Roll call for the CHKs under REQ-019:
  - CHK-033 (pre-existing `~/.zshrc` symlinked, rolled back to the original
    regular file ="original", `<ts>.ROLLED_BACK` written): demonstrated →
    Round 2 evidence shows the pre-fix red (os error 183, apply fails) and the
    post-fix green of `rollback_restores_a_regular_file_replaced_by_a_symlink`,
    plus the `pre_existing_regular_file_target_is_replaced_by_link` apply-leg
    unit guard. The round-1 "demonstrated (partial)" caveat is resolved; the
    full literal scenario now runs end-to-end.
  - CHK-049 (multi-target copy entry restore/delete + sentinel): demonstrated →
    unchanged from round 1 (Scenario 1 +
    `multi_target_copy_entry_rolls_back_to_pre_apply_state`).
  No CHK in T-018's scope is `judgment-only`.

- Discovered issues: None new. The round-1 Discovered-issues item (the
  `link_file` clear-before-link gap) is the business blocker fixed in this
  round, so it is no longer outstanding.

- Procedural compliance: This round-2 entry is appended beneath the round-1
  `<blockers>` block; no earlier journal content was modified and frontmatter
  `generated_at` is unchanged. The TASKS.md `state="..."` attribute for T-018
  flips from `in-progress` to `in-review` as the final step of this turn. No
  shipped skill bodies under `.claude/skills/` were edited.
</implementer>

<review persona="business" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-05-29T02:11:49Z" round="2">
CHK-033 (locked `<scenario>` under REQ-019) is now genuinely satisfied end-to-end, clearing the round-1 blocker. The apply-side root cause is fixed: `patina-core/src/apply/symlink.rs::link_file` (line 110-122) now calls `super::remove_entry(target)` before `create_symlink`, so apply can replace a pre-existing regular file with a symlink instead of failing with Unix EEXIST / Windows os error 183. The "never overwrite without a backup" north-star invariant is preserved — `engine.rs:345-348` backs up every target via `backup_before_overwrite` before `materialize` runs, so the clear only ever follows the backup, and rollback's backup-present → restore path recovers the original bytes.

The new integration test `rollback_restores_a_regular_file_replaced_by_a_symlink` (`patina-cli/tests/rollback_cli.rs:178-229`) exercises CHK-033's literal Given/When/Then against the real built binary on an isolated tempdir: pre-existing `~/.zshrc`="original" → apply (asserts an actual symlink is produced, lines 197-203) → `rollback --yes` → regular file (not a link) with content "original" + `<ts>.ROLLED_BACK` sentinel (lines 215-228). This is no longer a mechanism argument via a sibling scenario; it is the contract's own observable behavior. The apply-leg unit guard `pre_existing_regular_file_target_is_replaced_by_link` (`symlink.rs:270-294`) backstops it.

No other REQ-019 obligation regressed: CHK-049 (multi-target copy), no-prior-apply exit 1, fresh-creation delete, per-entry atomic roll-forward, and rolled-back-apply-excluded-from-status all remain green. The security-informational note was also folded in correctly (`rollback/replay.rs:68-69` now pushes the in-flight snapshot before `roll_forward`, so a mid-target copy failure rolls that target forward rather than leaving it deleted). The style blocker is cleared via the shared `pub(crate) remove_entry` helper in `apply/mod.rs` that both `engine.rs::remove_target` and `replay.rs` consume. Full `cargo test --workspace` is 0 failures.

Note (non-blocking): I observed one transient failure of the `pre_existing_regular_file_target_is_replaced_by_link` unit test during an overlapping cargo build ("Blocking waiting for file lock on artifact directory" → stale test binary missing the fix). It is deterministically green in isolation and across repeated clean full-suite runs; this was a build-harness artifact, not a code or test-logic flake. The implementer's evidence and roll-call honestly reflect the now-complete state. No open question was silently resolved; no scope from `<non-goals>` was added.
</review>

<review persona="tests" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-05-29T02:11:49Z" round="2">
Round-2 blockers are genuinely cleared; CHK-033 is now demonstrated end-to-end and the evidence roll call is honest.

Verified independently:
- CHK-033 integration test `patina-cli/tests/rollback_cli.rs:177 rollback_restores_a_regular_file_replaced_by_a_symlink` is non-vacuous: it drives the real `CARGO_BIN_EXE_patina` binary against a real tempdir, pre-creates `~/.zshrc`="original", applies (symlink), runs `rollback --yes`, then asserts the restored target is a regular file (`meta.file_type().is_file()`, line 217), content == "original" (line 221), and a `.ROLLED_BACK` sentinel exists (line 226) — the literal CHK-033 Given/When/Then. I reverted the `link_file` clear-before-link fix and the test went RED with exactly the evidence's `os error 183` (apply exit 1), and the unit guard `apply::symlink::tests::pre_existing_regular_file_target_is_replaced_by_link` (symlink.rs:271) likewise failed; restoring the fix returned both to green. The round-1 `demonstrated (partial)` dishonesty is resolved.
- No T-014 symlink regression from the `link_file` change: `executor_modes` (6/6), `multi_target` (3/3), and the `apply::symlink` unit suite (6/6) all pass. The `remove_entry` shared helper (`apply/mod.rs:237`) is correctly wrapped by both `engine.rs::remove_target` and `replay.rs::remove_existing`, behaviour-preserving.
- Evidence-fabrication patterns: none. Red/green output differs materially and the red I reproduced matches the captured red byte-for-byte; all named test symbols appear in the diff; the evidence command is a scoped per-test invocation, not the workspace hygiene run. Roll call covers CHK-033 and CHK-049, both `demonstrated`; nothing silently omitted or mislabelled `judgment-only`.

Non-blocking observation (not a scenario gap): the round-2 security-informational roll-forward change at `patina-core/src/rollback/replay.rs:68` (`reverted.push(snapshot)` so an in-flight, removed-then-copy-failed target is also rolled forward) is uncovered. The sole atomic test `rollback_atomic.rs:56 failed_second_target...` injects its failure at `create_dir_all(parent)` where the in-flight target never existed (Absent snapshot), so the new line executes as a no-op; I mutation-removed the push and the atomic suite stayed green, confirming no assertion guards that line. This is a best-effort secondary-recovery branch outside any locked scenario, so it does not block, but a future task should add a case where the in-flight target has post-apply content, its restore-copy fails after removal, and the target's bytes are asserted rolled forward.
</review>

<review persona="security" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-05-29T02:11:49Z" round="2">
No security blocker; round-1 informational atomicity note is resolved and the new apply-side removal is safe.

(1) Atomicity fix sound. `patina-core/src/rollback/replay.rs:68-69` now pushes the in-flight target's snapshot onto `reverted` before `roll_forward` when `revert_target` fails. The snapshot is captured at `replay.rs:50` before any mutation, so a mid-target copy failure (target removed at `replay.rs:147`, copy failed at `:153`) rolls that target forward to its post-apply state from `restore_snapshot` rather than leaving it deleted/partial. The round-1 deleted/partial data-loss concern is closed. Worst case (a swallowed roll-forward IO error, `ignore_io` at `replay.rs:171`) is still non-destructive: the user's pre-apply bytes remain untouched in the backup tree (`mirror_backup_path`), so a rollback re-run restores them — no unrecoverable user data loss.

(2) New `symlink.rs::link_file` removal is backup-ordered and symlink-safe. The engine runs `backup_before_overwrite` for every target (`engine.rs:346`) before `materialize`/`link_file` (`engine.rs:348`), so any pre-existing target the clear destroys has already been stashed and is rollback-restorable — the "never destroy a file Patina doesn't own without a recoverable backup" invariant holds. `link_file` (`symlink.rs:112`) clears via the shared `apply::remove_entry` (`apply/mod.rs:237`), which uses `symlink_metadata` + `remove_file`/`remove_dir_all` and so removes a symlink as a link, never following it — no symlink-following clobber of an external target.

(3) No new path-traversal/symlink hazard. Backup-source lookup still routes through `mirror_backup_path` (root/`..`/`.` folded away); `snapshot_targets` reads symlinks via `symlink_metadata`+`read_link` (`replay.rs:102-111`), never through them; staging stays under the per-user backup root (`replay.rs:251`).

Pre-existing, out-of-scope note (not introduced this round, not a blocker): `backup_before_overwrite` copies via `fs_err::copy`, which resolves a symlink target's bytes, so a pre-existing symlink target is backed up/restored as a regular file rather than as a link — identical to the already-shipped copy path and a T-014/backups concern, not this rollback slice. The pointed-at file is never destroyed (only the link is removed, after its bytes are backed up).
</review>

<review persona="style" verdict="blocking" model="claude-sonnet-4-6[1m]/medium" date="2026-05-29T02:11:49Z" round="2">
The round-1 duplication blocker is cleanly resolved: `pub(crate) fn remove_entry` is hoisted into `patina-core/src/apply/mod.rs:237`; `engine.rs::remove_target` wraps it (`super::remove_entry(target).map_err(…)`, line 523); `rollback/replay.rs::remove_existing` delegates to it (`crate::apply::remove_entry(target)`, line 204); `journal/recovery.rs::remove_target` (non-NotFound-tolerant, distinct contract) is untouched. The shared helper has correct `&Utf8Path` parameter typing, a doc comment with `# Errors`, and `pub(crate)` visibility. The `symlink.rs::link_file` now calls `super::remove_entry` before `create_symlink`, clearing the pre-existing target after the engine's backup. No `unwrap()`/`expect()` in production code. The naming, camino usage, and import ordering are all consistent with the codebase.

One new convention drift introduced in this round:

`patina-core/src/rollback/replay.rs:230` and `240` use `std::os::unix::fs::symlink` and `std::os::windows::fs::symlink_*` instead of the `fs_err::os::unix::fs::symlink` / `fs_err::os::windows::fs::symlink_*` variants. `apply/symlink.rs:146-153` (the existing precedent in this very crate for the same operation) uses the `fs_err`-wrapped versions. `AGENTS.md` and the dependencies rule state "prefer `fs-err` over `std::fs` so error messages include the path" with no exception carved out for error-discarding paths. The `fs_err` variants have the same signature (`std::io::Result<()>`) so the fix is drop-in.

Required fix: replace `std::os::unix::fs::symlink` and `std::os::windows::fs::symlink_*` in `replay.rs::symlink_to` with their `fs_err::os::*::fs` counterparts, matching the pattern in `apply/symlink.rs`.
</review>

<review persona="architecture" verdict="pass" model="claude-opus-4-8[1m]/xhigh" date="2026-05-29T02:11:49Z" round="2">
Round-2 retry is architecturally sound on all three risk axes the brief named; no new blocker.

(1) Cross-task apply-executor change respects crash-safety / "backup first" and executor layering. `apply/symlink.rs::link_file` (lines 110-122) now inserts `super::remove_entry(target)` between `ensure_parent` and `create_symlink`. The "never overwrite without consent (backup first)" invariant holds because the only path into `link_file` is `materialize`, and the engine's materialize loop (`apply/engine.rs:345-348`) runs `backup_before_overwrite` for every target of the entry *before* calling `materialize`. So the clear strictly follows the backup; rollback's backup-present → restore path can always recover the original bytes (the `link_file` doc at lines 104-109 states this contract correctly). The change is within the `apply` layer: `link_file` calls a `pub(crate)` sibling helper in its parent `apply/mod.rs` (`super::remove_entry`), no cross-layer reach. This is the exact fix the round-1 business blocker sanctioned, and it does not regress the executor's no-backup-no-clobber guarantee.

(2) Roll-forward strengthening preserves the per-entry "pre-apply OR post-apply, never intermediate" invariant. `replay_entry` (`rollback/replay.rs:62-72`) now pushes the in-flight snapshot onto `reverted` before `roll_forward` on a `revert_target` failure. Because `snapshot_targets` captures every target's post-apply state before any mutation (line 50) and `revert_target` removes-then-copies (lines 147-153), the prior code could leave the in-flight target deleted/partial — an intermediate state. Including it in the roll-forward set restores it to the captured post-apply snapshot, tightening the per-entry atomicity guarantee toward the invariant rather than away from it. Per-entry (not whole-apply) atomicity remains correct and matches REQ-019's scope plus DEC-011's cross-retry convergence model; the best-effort `ignore_io` roll-forward is the same converge-on-best-effort shape crash recovery uses and re-runs idempotently.

(3) Shared `remove_entry` helper introduces a new but acyclic, low-risk module edge. `remove_entry` is hoisted to `pub(crate)` in `apply/mod.rs:237-249`; `engine.rs::remove_target` (523-526) and `replay.rs::remove_existing` (203-205) both wrap/delegate to it — the "promote and call" consolidation the round-1 style blocker required, with two genuine consumers (not a premature abstraction). This adds a `rollback → apply` module dependency that did not exist in round 1. Verified no cycle: nothing under `apply/*` imports `crate::rollback` (the only `crate::rollback` reference is `error.rs`'s `#[from]`), so the graph stays acyclic. `remove_entry` exposes a generic FS primitive (plain `&Utf8Path` in, `io::Result` out, no apply state), so a future apply-executor refactor cannot break rollback through it. Non-blocking note: a neutral `fs_util` module would be the slightly cleaner long-term home for a primitive both layers borrow, but placement alongside `apply/mod.rs`'s existing `ensure_parent`/`walk_files` is defensible and is within the style reviewer's explicitly-offered option set. No `### Decisions` entry (DEC-003/004 envelope, DEC-011 backup-keyed replay) is contradicted; AGENTS.md conventions (camino, fs-err, no panic/unwrap/expect in production) are respected.
</review>

<review persona="docs" verdict="pass" model="claude-sonnet-4-6[1m]/medium" date="2026-05-29T02:11:49Z" round="2">
All three doc surfaces changed in round 2 are accurate.

`link_file` (`patina-core/src/apply/symlink.rs:102-109`): the doc comment correctly describes the new remove-before-link step, names the EEXIST/os-183 motivation, and explains the backup-already-taken invariant that makes removal safe for rollback. The body matches (verified at the byte level — the Read tool showed stale content; grep and direct byte inspection confirmed `super::remove_entry(target)` is present at line 112).

`remove_entry` (`patina-core/src/apply/mod.rs:224-236`): the `pub(crate)` helper doc accurately lists all three callers (apply engine reverse path, rollback replay path, symlink executor clear-before-link), documents the NotFound-tolerance, and has a correct `# Errors` section.

`remove_existing` (`patina-core/src/rollback/replay.rs:197-204`): the doc correctly states it delegates to `crate::apply::remove_entry` and that the two paths share one implementation; the body matches.

`replay_entry` (`patina-core/src/rollback/replay.rs:24-37` plus the inline comment at line 64-68): the doc and the inline comment accurately describe the strengthened atomicity — the in-flight target is pushed onto `reverted` before `roll_forward` on copy failure, matching the code. The module-level doc (lines 1-17) correctly describes the snapshot-before-mutate atomicity mechanism.

No docs/ file, README, or SPEC decision note references the old (pre-round-2) behavior of `link_file` or the rollback atomicity shape.
</review>

<blockers date="2026-05-29T02:11:49Z" round="2">
Use the `fs-err`-wrapped symlink APIs in the rollback roll-forward path.
- style (blocking): `patina-core/src/rollback/replay.rs::symlink_to` uses `std::os::unix::fs::symlink` and `std::os::windows::fs::symlink_*` instead of the `fs_err::os::unix::fs::symlink` / `fs_err::os::windows::fs::symlink_*` variants. `AGENTS.md` "Code conventions" states "prefer `fs-err` over `std::fs` so error messages include the path" with no carve-out for error-discarding paths, and `apply/symlink.rs:146-153` already uses the wrapped form for the same operation. The `fs_err` variants share the `std::io::Result<()>` signature, so the fix is drop-in. (The round-1 duplication and business/CHK-033 blockers are both fully resolved this round; this is the sole remaining item.)
</blockers>

<implementer date="2026-05-28T00:00:00Z" model="claude-opus-4-8[1m]/low" round="3">
- Completed: Cleared the sole round-2 style blocker. `patina-core/src/rollback/replay.rs::symlink_to`
  now uses the `fs-err`-wrapped symlink APIs instead of the bare `std::os`
  variants: the `#[cfg(unix)]` arm calls `fs_err::os::unix::fs::symlink` and the
  `#[cfg(windows)]` arm calls `fs_err::os::windows::fs::symlink_dir` /
  `fs_err::os::windows::fs::symlink_file`. This matches the existing precedent
  for the same operation in `apply/symlink.rs::create_symlink_os` (lines 146-154)
  and satisfies the AGENTS.md "prefer `fs-err` over `std::fs`" convention so a
  failing symlink recreation during roll-forward carries the path in its error
  message. The `fs_err` variants share the `std::io::Result<()>` signature, so
  it was a drop-in replacement with no signature, control-flow, or call-site
  change.

- Undone: Nothing newly deferred. The round-1/round-2 Undone scope (T-019
  `debug journal`, T-020 lock-timeout exit-code wiring, the unused
  `EngineError::NotImplemented` variant) remains out of scope for this task.

- Hygiene checks: `cargo test --workspace` exited 0 (all suites pass, 0
  failures, including the CHK-033 integration test and the rollback atomic
  suite). `cargo clippy --workspace --all-targets --all-features -- -D warnings`
  exited 0. `cargo +nightly fmt --all --check` exited 0. `cargo deny check`
  exited 0 (no new dependencies; the change swaps `std::os` symlink calls for
  the already-depended-on `fs-err` wrappers).

- Evidence: this round changes a single function-internal API call (bare
  `std::os` symlink → `fs_err`-wrapped symlink) with no observable behaviour
  change — the success path and `std::io::Result<()>` contract are identical;
  only error messages gain path context. Roll call for the CHKs under REQ-019
  is unchanged from round 2:
  - CHK-033 (pre-existing `~/.zshrc` symlinked, rolled back to the original
    regular file ="original", `<ts>.ROLLED_BACK` written): demonstrated →
    `rollback_restores_a_regular_file_replaced_by_a_symlink` in
    `patina-cli/tests/rollback_cli.rs` plus the
    `pre_existing_regular_file_target_is_replaced_by_link` apply-leg unit
    guard, both green under `cargo test --workspace`.
  - CHK-049 (multi-target copy entry restore/delete + sentinel): demonstrated →
    `multi_target_copy_entry_rolls_back_to_pre_apply_state`, green.
  No CHK in T-018's scope is `judgment-only`.

- Discovered issues: None new.

- Procedural compliance: This round-3 entry is appended beneath the round-2
  `<blockers>` block; no earlier journal content was modified and frontmatter
  `generated_at` is unchanged. The TASKS.md `state="..."` attribute for T-018
  flips from `in-progress` to `in-review` as the final step of this turn. No
  shipped skill bodies under `.claude/skills/` were edited.
</implementer>

<review persona="business" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-05-29T02:22:42Z" round="3">
Round-3 change is behaviour-neutral and no REQ-019 obligation regressed. The sole change is the round-2 style swap in `patina-core/src/rollback/replay.rs::symlink_to` (lines 233-247): bare `std::os::unix::fs::symlink` / `std::os::windows::fs::symlink_*` → the `fs_err`-wrapped equivalents, matching the `apply/symlink.rs` precedent. The `std::io::Result<()>` signature is unchanged in both arms; `symlink_to` is called only from `restore_snapshot` (replay.rs:192) for the `Symlink` snapshot case on the atomic roll-forward path, so the success path is byte-identical and only failure-path error messages gain path context. All six REQ-019 locked-scenario / done-when obligations verified live as still green: CHK-033 (`rollback_restores_a_regular_file_replaced_by_a_symlink`), CHK-049 (`multi_target_copy_entry_rolls_back_to_pre_apply_state`), no-prior-apply exit 1, fresh-creation delete, per-entry atomic `RollbackPartial` roll-forward, and rolled-back-apply-excluded-from-status — rollback lib 8/8, rollback_cli 6/6, rollback_atomic 2/2. No `<non-goals>` scope added; no open question silently resolved.
</review>

<review persona="tests" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-05-29T02:22:42Z" round="3">
Round-3 change is a genuine behaviour-neutral swap and the CHK-033/CHK-049 demonstrations remain green and honest.

Verified independently:
- The round-3 edit is confined to `patina-core/src/rollback/replay.rs::symlink_to` (lines 233-247): both `#[cfg(unix)]` and `#[cfg(windows)]` arms now call `fs_err::os::*::fs::symlink*` instead of bare `std::os::*`. A crate-wide grep confirms zero remaining `std::os::(unix|windows)::fs::symlink` calls and that `replay.rs` now mirrors the `apply/symlink.rs::create_symlink_os` precedent — the round-2 style blocker is fully cleared. The swap is signature-identical (`std::io::Result<()>`), success-path-identical, and `symlink_to` is only reachable on the roll-forward recovery path via `restore_snapshot`, so it cannot perturb any locked-scenario happy path; the only observable delta is added path context on error.
- Full `cargo test --workspace` is green (0 failures across every suite). The two CHK-bearing tests pass against the real built binary on real tempdir filesystems: `patina-cli/tests/rollback_cli.rs::rollback_restores_a_regular_file_replaced_by_a_symlink` (CHK-033) and `::multi_target_copy_entry_rolls_back_to_pre_apply_state` (CHK-049), plus `rollback_atomic.rs` (2/2) and the `apply::symlink` unit guard.
- CHK-033 test (rollback_cli.rs:178-229) drives the literal Given/When/Then: pre-existing `~/.zshrc`="original" → apply asserts a real symlink is produced (is_symlink, line 197) → `rollback --yes` exit 0 → asserts regular file (is_file, line 216), content=="original" (line 220), and ROLLED_BACK sentinel present (line 225). These are non-vacuous assertions that fail under realistic regressions (no-op rollback, wrong content, or missing sentinel each break a distinct assertion); not mocks, not loose-outcome checks.
- Evidence file present at the journal-named path. Roll call covers both CHKs the task covers (REQ-019): CHK-033 and CHK-049 both `demonstrated` with specific test cites; nothing silently omitted, nothing mislabelled `judgment-only`. No fabrication pattern matched — the round-3 entry correctly declines to manufacture a red/green for a behaviour-neutral API-wrapper swap (fabricating one would have been the smell), and all named test symbols appear in the diff.

Non-blocking carryover (already noted in round 2, unchanged here, not a scenario gap): `symlink_to` and the in-flight roll-forward push at `replay.rs:68` are not directly asserted by any test; the sole atomic test injects its failure at `create_dir_all` where the in-flight snapshot is `Absent`, so the symlink roll-forward branch executes as a no-op. This is a best-effort secondary-recovery branch outside any locked scenario and does not block; a future task should add a Symlink-snapshot roll-forward case.
</review>

<review persona="security" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-05-29T02:22:42Z" round="3">
No security blocker; round-3 introduces no new security surface. The sole change swaps the bare `std::os::{unix,windows}::fs::symlink*` calls in `patina-core/src/rollback/replay.rs::symlink_to` (lines 234-247) for their `fs_err::os::*::fs::symlink*` equivalents — same underlying `symlink(2)`/`CreateSymbolicLink` syscall, same `std::io::Result<()>` signature, identical success path and Windows dir-vs-file dispatch; `fs_err` only attaches path context to the error. (1) No new symlink-following or TOCTOU surface: the function creates a link, it does not read/follow/canonicalize through an existing one. (2) Added error-message path context discloses only the user's own apply-time-recorded target/link paths from a per-user (non-network) journal — no secret, token, or credential flows here. (3) No new dependency: `fs-err` is already first-party and `cargo deny check` exited 0. The prior-round security boundaries (backup-ordered `remove_entry` clear, symlink-not-followed removal, `mirror_backup_path` traversal-folding, per-user staging dir, version-envelope deserialization) are untouched and remain sound.
</review>

<review persona="style" verdict="pass" model="claude-sonnet-4-6[1m]/medium" date="2026-05-29T02:22:42Z" round="3">
Round-2 blocker fully resolved. `patina-core/src/rollback/replay.rs::symlink_to` (lines 234-246) now uses `fs_err::os::unix::fs::symlink` on Unix and `fs_err::os::windows::fs::symlink_dir`/`symlink_file` on Windows, matching the existing `apply/symlink.rs::create_symlink_os` precedent exactly. No bare `std::os::*` symlink calls remain in `replay.rs`. No new style violations introduced: the swap is drop-in (same `std::io::Result<()>` signature), naming is consistent, `fs-err` was already a declared dependency, and no other convention drift is present in the untracked rollback module.
</review>
