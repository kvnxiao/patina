---
spec: SPEC-0002
task: T-007
generated_at: 2026-05-30T20:59:49Z
---

<implementer date="2026-05-30T20:59:49Z" model="claude-opus-4-8[1m]/medium" round="1">
- Completed: Added the cross-platform `patina-core::windows` module
  delivering REQ-007's read-side capability (DEC-008 split: the engine
  owns the IO-free reads; the prompt/launch orchestration lands in
  `patina-cli` at T-009). New `patina-core/src/windows/mod.rs` defines:
  `DevModeStatus { Enabled, Disabled, Unsupported, NotWindows }`; a typed
  `WindowsError` (`thiserror`) carrying the failing Win32 call name;
  `dev_mode_status()` (non-Windows → `NotWindows` with no registry access;
  Windows → reads `AllowDevelopmentWithoutDevLicense`, build-floor first);
  `is_elevated()` (non-Windows → `false`); `windows_build_supports_dev_mode()`;
  the pure `is_unc_path(&Utf8Path)` (`\\` prefix check, all platforms) and
  `plan_has_symlink_op(&ResolvedPlan)` (matches `FileMode::Symlink` /
  `SymlinkDir`); the injectable seam `DevModeProbe` trait with production
  `HostDevModeProbe`; and the pure gate decision `decide_symlink_gate`
  returning `GateDecision { Proceed, ProceedElevatedWarning,
  RequireElevation }` so T-009's gate logic is unit-testable on Linux.
  New `patina-core/src/windows/registry.rs` (`#[cfg(windows)]`) holds the
  `winsafe`-backed registry/token reads and the registry key-path /
  value-name constants (`AppModelUnlock` /
  `AllowDevelopmentWithoutDevLicense`), deliberately duplicated in
  `patina-elevate` later per DEC-002. The OS build is read from
  `CurrentBuildNumber` rather than `GetVersionEx` (which under-reports for
  unmanifested processes). Registered `pub mod windows;` + 12 re-exports in
  `patina-core/src/lib.rs`. Added `winsafe = "0.0.27"` (features `advapi`,
  `kernel`; `taskschd` deliberately omitted — SPEC-0003 surface) to root
  `[workspace.dependencies]` and referenced it from `patina-core` only
  under `[target.'cfg(windows)'.dependencies]`.

- Undone: The engine apply gate and the `ShellExecuteEx`/`runas` elevation
  launch (the CLI `Reporter` prompt, decline → exit 5, re-read + re-drive
  `execute_plan`) are T-009 per the task body and DEC-008; not in scope
  here. The `patina-elevate` helper crate is T-008. CHK-012 / CHK-013
  (end-to-end Windows-host runs) are demonstrated against those tasks.

- Hygiene checks: `cargo test --workspace` exited 0 (every suite "test
  result: ok", 0 failed; the new `windows::tests` module contributes 9
  passing cases). `cargo clippy --workspace --all-targets --all-features --
  -D warnings` exited 0. `cargo +nightly fmt --all --check` exited 0.
  `cargo deny check` exited 0 (advisories / bans / licenses / sources ok;
  `winsafe` is MIT, already allowlisted). Additionally — not a standard
  gate — the `#[cfg(windows)]` path was type-checked and clippy-cleaned by
  cross-compiling: `cargo clippy -p patina-core --target
  x86_64-pc-windows-gnu --all-targets --all-features -- -D warnings` exited
  0, confirming the `winsafe` API calls are real rather than guessed.

- Evidence: paper trail at
  `.speccy/specs/0002-patina-complete-cli/evidence/T-007.md`. Roll call for
  the CHKs under REQ-007:
  - CHK-012 (Windows Dev-Mode-OFF + symlink entry + declined-UAC harness →
    no link, stderr names `Developer Mode` + `patina doctor --fix`, exit
    5): judgment-only for T-007 → the engine gate, the UAC prompt, and the
    exit-5 path all land in T-009; T-007 ships only the read-side
    `dev_mode_status()` + `decide_symlink_gate` that gate consumes. No
    Windows host on CI. Reviewer judges the slice boundary on the diff;
    the scenario is `demonstrated` at T-009.
  - CHK-013 (Windows Dev-Mode-ON + symlink entry → no prompt, no
    `patina-elevate.exe` spawned, link created): judgment-only for T-007
    for the same reason — the no-prompt/proceed path is `decide_symlink_gate`
    returning `Proceed` on `Enabled` (unit-tested here as
    `gate_enabled_proceeds`), but the end-to-end "link created / no helper
    spawned" assertion needs T-009 + T-008. `demonstrated` at T-009.

  The three `<task-scenarios>` this task is actually accountable for are
  proved as `hygiene` (they run under `cargo test --workspace`):
  - Task-scenario A (non-Windows → NotWindows / false, no registry
    access): hygiene → `non_windows_status_is_not_windows_and_unelevated`
    in `patina-core/src/windows/mod.rs`.
  - Task-scenario B (`is_unc_path` UNC vs POSIX): hygiene →
    `unc_prefix_check_distinguishes_unc_from_posix` (same file); also the
    `is_unc_path` doctest in `lib.rs` re-exports.
  - Task-scenario C (gate: Disabled+not-elevated → RequireElevation;
    Enabled → Proceed): hygiene →
    `gate_disabled_not_elevated_requires_elevation` and
    `gate_enabled_proceeds`, with branch-completion companions
    `gate_disabled_but_elevated_proceeds_with_warning`,
    `gate_no_symlink_op_proceeds_regardless_of_status`, and
    `gate_unsupported_build_proceeds`.

- Discovered issues: None new. Noted that `winsafe`'s `GetVersionEx`-style
  version query is unreliable for unmanifested processes (a well-known
  Win32 trap), which is why `build_number()` reads `CurrentBuildNumber`
  from the registry instead — documented inline in `registry.rs`. No
  pre-existing dead code touched.

- Procedural compliance: This entry lands directly in `journal/T-007.md`
  per the journal-file schema; no `<implementer-note>` was written into
  TASKS.md. The TASKS.md `state="..."` attribute for T-007 flips from
  `in-progress` to `in-review` as the final step of this turn. No shipped
  skill bodies under `.claude/skills/` were edited.
</implementer>

<review persona="business" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-05-30T21:03:42Z" round="1">
T-007 faithfully delivers the read-side slice of REQ-007 per DEC-008: the IO-free `dev_mode_status()`/`is_elevated()`/`windows_build_supports_dev_mode()` reads, the pure `is_unc_path`/`plan_has_symlink_op` predicates, the injectable `DevModeProbe` seam, and the pure `decide_symlink_gate` (Proceed / ProceedElevatedWarning / RequireElevation). Every read-side REQ-007 done-when item maps to a test (`patina-core/src/windows/mod.rs:333-435`): Enabled→Proceed, no-symlink→Proceed, Disabled+elevated→warning, Disabled+unelevated→RequireElevation, non-Windows→NotWindows with no registry access. `plan_has_symlink_op` matches exactly the two symlink-creating `FileMode` variants and its test exercises all five variants — non-vacuous.

Scope boundary is honest. The prompt/`ShellExecuteEx`-launch/exit-5/post-helper re-read items of REQ-007 are correctly deferred to T-008/T-009 (DEC-008 capability/orchestration split); no Reporter dependency, no `println!`/`eprintln!`, no engine gate, and no elevation launch leaked into `patina-core::windows`. CHK-012/CHK-013 are end-to-end Windows-host apply scenarios that cannot land here and are judgment-only per the task body and orchestrator note — slice boundary accepted.

Non-goals respected: `taskschd` deliberately not enabled (SPEC-0003 surface); `winsafe` features are `advapi`+`kernel` (`Cargo.toml`), the read-side subset — `shell` is correctly left for T-008's launch capability. No cache file introduced (resolved open-question c honored). The diff reflects the current 2026-05-30 DEC-008 intent, not stale intent. All five SPEC open questions were user-resolved before implementation; none were silently decided here. cargo test (9 windows unit tests + 2 doctests) and clippy `-D warnings` clean on this host. No blocking business gap.
</review>

<review persona="tests" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-05-30T21:03:42Z" round="1">
T-007's three accountable task-scenarios are each translated into non-vacuous unit tests in `patina-core/src/windows/mod.rs` that drive the real code paths. I confirmed non-vacuity by mutation: forcing `decide_symlink_gate` to return `Proceed` for Disabled+not-elevated (caught by `gate_disabled_not_elevated_requires_elevation:380`), negating `is_unc_path` (caught by `unc_prefix_check_distinguishes_unc_from_posix:349`), and pointing `plan_has_symlink_op` at `FileMode::Copy` (caught by four tests) all fail the suite; the unmutated suite is 9/9 green. The `FakeProbe` is a test double for the host-state *input* feeding the real `decide_symlink_gate`/`plan_has_symlink_op`, not a mock of the function under test, so the seam exercises the actual decision logic. The CHK-012/CHK-013 `judgment-only` labels are honest: both are end-to-end Windows-host apply scenarios (TTY/UAC harness, real symlink materialization, process-tree assertions) whose machinery — engine gate, `ShellExecuteEx`/`runas` launch, CLI prompt, exit-5 path — lands in T-008/T-009 per the task body and DEC-008 and cannot be demonstrated by a deterministic command from this read-side slice on the macOS CI host; T-008's task-scenarios re-state them behind `#[cfg(windows)] #[ignore]`, confirming a real deferral target. Evidence file present at the named path with a complete CHK roll call; no fabrication pattern matched (cited `winsafe` symbols and test names all appear in the diff under review; cross-compile output carries real runner structure).
</review>

<review persona="security" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-05-30T21:03:42Z" round="1">
T-007 read-side Windows Developer Mode / elevation capability is security-clean. New dependency `winsafe` 0.0.27 clears `deny.toml`: `cargo deny check` returns `advisories ok, bans ok, licenses ok, sources ok`; the crate is MIT (allowlisted), has zero transitive deps, and resolves from crates.io. The duplicate-version warnings cargo-deny emits are for `toml_edit`/`winnow` (a different task) under `multiple-versions = "warn"`, non-blocking and unrelated. All registry reads use `co::KEY::READ` only and the token read uses `co::TOKEN::QUERY` only — read-only, no `RegSet/Create/Delete`, no `AdjustTokenPrivileges`, no privilege escalation (`patina-core/src/windows/registry.rs`). Zero `unsafe` in the module, consistent with workspace `unsafe_code = "forbid"`. Missing keys/values and API failures are handled without panic (`FILE_NOT_FOUND` → `Ok(None)`, `.ok()?` degradations, no `unwrap`/`expect`); failure defaults are fail-closed (failed Dev Mode read → `Disabled`, failed elevation query → `false`, both keeping the elevation gate engaged). No logging anywhere in the module and `WindowsError` carries only a static Win32 call name plus a wrapped `io::Error`, so no registry values or token contents leak into errors or telemetry.
</review>

<review persona="style" verdict="blocking" model="claude-sonnet-4-6[1m]/medium" date="2026-05-30T21:03:42Z" round="1">
`patina-core/src/windows/mod.rs:429-435` — `host_probe_matches_free_functions` is vacuous. `HostDevModeProbe::dev_mode_status` is defined as `dev_mode_status()` and `HostDevModeProbe::is_elevated` is defined as `is_elevated()`, so the two `assert_eq!` calls reduce to `f() == f()`. The test cannot fail for any realistic regression: the return types are different (`DevModeStatus` vs `bool`), so a cross-wiring bug would be a compile error rather than a runtime failure, and an implementation regression in the underlying free function would be caught by the other tests, not this one. Per the AGENTS.md hard rule ("No vacuous or constant-copy tests — a test must gate a real invariant"), this test should be dropped. If there is a delegation-wiring invariant worth gating, add it as a concrete property test (e.g. that `HostDevModeProbe` agrees with the `FakeProbe` carrying the same returned value, which would detect a future swap of the method bodies without relying on the compiler catching it).
</review>

<review persona="correctness" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-05-30T21:03:42Z" round="1">
`decide_symlink_gate` maps every (symlink-in-plan, DevModeStatus, elevated) input to the correct GateDecision; the gate branches, the early no-symlink return, and the Windows `dev_mode_status()` flag-read folding (Ok(Some(1))→Enabled, all-else/Err→Disabled safe default) are sound. `registry.rs` handles missing keys/values via FILE_NOT_FOUND→Ok(None), propagates other failures as typed WindowsError, contains no unwrap/expect/panic (the impossible non-Elevation token arm returns Ok(false)), and leaks no handles (winsafe RAII closes on every exit path). I verified the `#[cfg(windows)]` winsafe API calls actually compile under `x86_64-pc-windows-gnu` via error-injection (E0599 on a renamed method, then restore); host + windows-target clippy clean and all 9 unit tests pass. No correctness defect at or above the 80 confidence bar.
</review>

<blockers date="2026-05-30T21:03:42Z" round="1">
One blocker landed in round 1 (style). Business, tests, security, and correctness all passed. Fix the single blocker before respawning the review fan-out.

1. **Vacuous test `host_probe_matches_free_functions` violates the no-vacuous-tests hard rule (style).** `patina-core/src/windows/mod.rs:429-435` asserts `probe.dev_mode_status() == dev_mode_status()` and `probe.is_elevated() == is_elevated()`, but `HostDevModeProbe`'s methods are defined as direct delegations to those same free functions, so the assertions reduce to `f() == f()` and cannot fail for any realistic regression. Per the AGENTS.md test-hygiene hard rule ("No vacuous or constant-copy tests — a test must gate a real invariant"), either drop the test, or replace it with a concrete delegation-wiring invariant that would actually catch a future method-body swap — e.g. assert `HostDevModeProbe` agrees with a `FakeProbe` constructed to carry the same value, so a cross-wired delegation is caught at runtime rather than relying on the type checker. Re-run all four hygiene gates after the change.

Already correct and must not regress: the read-side capability itself is sound across business, tests, security, and correctness — the `decide_symlink_gate` truth table, the `dev_mode_status()` fail-closed folding, the read-only registry/token reads (no escalation, no panic, no leak), the `winsafe` dependency clearing `deny.toml`, and the honest judgment-only deferral of CHK-012/CHK-013 to T-009. The other 8 unit tests are non-vacuous (mutation-confirmed by the tests persona). The retry is a single-test fix; do not alter the production module.
</blockers>

<implementer date="2026-05-30T21:30:00Z" model="claude-opus-4-8[1m]/medium" round="2">
- Completed: Addressed the round-1 style blocker by replacing the vacuous
  `host_probe_matches_free_functions` test (which reduced to `f() == f()`
  since `HostDevModeProbe`'s methods delegate directly to the same free
  functions) with `host_probe_delegates_each_method_to_its_matching_free_function`
  in `patina-core/src/windows/mod.rs`. The new test captures the host's
  current free-function values (`dev_mode_status()`, `is_elevated()`),
  builds a field-wired `FakeProbe` carrying exactly those values, and
  asserts `HostDevModeProbe` agrees with that fake field-for-field. Because
  the fake returns by stored field rather than by delegation, it is an
  independent witness of the delegation wiring rather than a tautology — a
  future cross-wiring of the production method bodies makes the two
  disagree at runtime. This is the reviewer's named replacement
  (FakeProbe-carrying-the-same-value). The production module
  (`HostDevModeProbe`, the free functions, the gate logic, `registry.rs`)
  was not touched, per the blocker directive.

- Undone: Nothing newly undone. The round-1 scope split stands: the engine
  apply gate, the `ShellExecuteEx`/`runas` launch, the CLI Reporter prompt,
  and the exit-5 path remain T-009; the `patina-elevate` helper remains
  T-008 (DEC-008). CHK-012 / CHK-013 stay judgment-only here, demonstrated
  at T-009.

- Hygiene checks: `cargo test --workspace` exited 0 (177-case core suite
  plus the 9-case `windows::tests` module green, including the renamed
  delegation test; 0 failed). `cargo clippy --workspace --all-targets
  --all-features -- -D warnings` exited 0. `cargo +nightly fmt --all
  --check` exited 0. `cargo deny check` exited 0.

- Evidence: paper trail at
  `.speccy/specs/0002-patina-complete-cli/evidence/T-007.md` (round-1
  artifact; unchanged — this round is a test-hygiene fix, not a new
  capability). Roll call for the CHKs under REQ-007 is unchanged from
  round 1:
  - CHK-012 (Windows Dev-Mode-OFF + symlink + declined-UAC → no link,
    stderr names `Developer Mode` + `patina doctor --fix`, exit 5):
    judgment-only → end-to-end Windows-host apply lands at T-009;
    demonstrated there.
  - CHK-013 (Windows Dev-Mode-ON + symlink → no prompt, no helper spawn,
    link created): judgment-only → the proceed path is `decide_symlink_gate`
    returning `Proceed` on `Enabled` (unit-tested as `gate_enabled_proceeds`);
    the end-to-end assertion needs T-008 + T-009; demonstrated at T-009.
  The three accountable task-scenarios remain hygiene-proven under `cargo
  test --workspace` (non-Windows reads, `is_unc_path`, gate truth table);
  the delegation-wiring invariant for `HostDevModeProbe` is now additionally
  gated by `host_probe_delegates_each_method_to_its_matching_free_function`.

- Discovered issues: None new. No pre-existing dead code touched.

- Procedural compliance: This round-2 entry is appended to
  `journal/T-007.md` after the round-1 blockers block; the earlier
  `<implementer round="1">` and review/blocker blocks are unchanged
  (append-only). No `<implementer-note>` was written into TASKS.md. The
  TASKS.md `state` for T-007 flips from `in-progress` to `in-review` as the
  final step. No production module and no shipped `.claude/skills/` body
  were edited.
</implementer>

<review persona="business" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-05-30T21:09:32Z" round="2">
T-007 round-2 is a test-only change and the REQ-007 read-side delivery is unchanged from the round-1 business pass. The production module `patina-core/src/windows/mod.rs` (lines 99-281) still maps every read-side done-when item to live code: `dev_mode_status()`/`is_elevated()`/`windows_build_supports_dev_mode()` reads, the pure `is_unc_path`/`plan_has_symlink_op` predicates, the `DevModeProbe` seam + `HostDevModeProbe`, and the full `decide_symlink_gate` truth table (Enabled/Unsupported/NotWindows→Proceed, Disabled+elevated→ProceedElevatedWarning, Disabled+unelevated→RequireElevation). The only diff vs round 1 is the test swap at `mod.rs:428-447` (`host_probe_matches_free_functions` → `host_probe_delegates_each_method_to_its_matching_free_function`), which adds/removes no REQ-007 observable behavior. Scope split per DEC-008 is intact: no prompt, no ShellExecuteEx launch, no exit-5, no engine gate in `patina-core`; those remain T-008/T-009 and CHK-012/CHK-013 stay correctly judgment-only. Non-goals respected (no `taskschd`; round-2 touched no Cargo.toml). No open question silently resolved. 9/9 windows unit tests green including the renamed delegation test. No blocking business gap.
</review>

<review persona="tests" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-05-30T21:09:32Z" round="2">
Round-2 fix resolves the round-1 style blocker without regression. `host_probe_delegates_each_method_to_its_matching_free_function` (`patina-core/src/windows/mod.rs:428-447`) is genuinely non-vacuous: the `expected` `FakeProbe` stores a snapshot of the free-function values and returns them by field, while `HostDevModeProbe` returns by live delegation, so the two sides are independent rather than `f() == f()`. I mutation-confirmed it: forcing `HostDevModeProbe::dev_mode_status` to a constant `Enabled` fails (`left: Enabled, right: NotWindows`), and forcing `is_elevated` to constant `true` fails (`left: true, right: false`) — both delegation-wiring swaps are caught. All 9 `windows::tests` pass, the full `patina-core --lib` suite is 177/177 green, clippy `-D warnings` is clean on `--all-targets`, and the production module was untouched per the blocker directive. Evidence file present with a complete CHK roll call; CHK-012/CHK-013 judgment-only deferral to T-008/T-009 is plausible (end-to-end Windows-host apply, no Windows CI host), the three task-scenarios are hygiene-labelled to specific named tests, and no fabrication pattern matched.
</review>

<review persona="security" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-05-30T21:09:32Z" round="2">
T-007 round-2 is security-clean. The change is test-only: the vacuous `host_probe_matches_free_functions` was replaced by the delegation-wiring test `host_probe_delegates_each_method_to_its_matching_free_function` (`patina-core/src/windows/mod.rs:428-447`), which lives entirely under `#[cfg(test)]` and only calls the pre-existing public reads `dev_mode_status()`/`is_elevated()` — on the macOS/Linux host these short-circuit via the `#[cfg(not(windows))]` arms with no registry or token access. The production module (`registry.rs` read-only `co::KEY::READ`/`co::TOKEN::QUERY`, fail-closed defaults, zero `unsafe`, no logging, static-only `WindowsError`) and the `winsafe` 0.0.27 dependency wiring are unchanged from the round-1 security pass. No new dependency, I/O, Win32 surface, secret handling, or data-exposure vector. `cargo deny check advisories bans sources` returns `advisories ok, bans ok, sources ok`; the lone `winnow` multi-version warning belongs to `toml_edit`/`toml` (a different task) and is non-blocking and unrelated.
</review>

<review persona="style" verdict="blocking" model="claude-sonnet-4-6[1m]/medium" date="2026-05-30T21:09:32Z" round="2">
`patina-core/src/windows/mod.rs:428-447` — `host_probe_delegates_each_method_to_its_matching_free_function` does not clear the round-1 vacuity blocker. The replacement has the same `f() == f()` structure with an extra layer of indirection.

The test builds `expected = FakeProbe { status: dev_mode_status(), elevated: is_elevated() }` and then asserts `host.dev_mode_status() == expected.dev_mode_status()`. But `FakeProbe::dev_mode_status()` returns `self.status` — which was set to `dev_mode_status()` at construction — so the assertion reduces to `dev_mode_status() == dev_mode_status()`. The `FakeProbe` wrapper is not an independent witness; it re-wraps the same call result. Likewise for `is_elevated`.

The comment claims the test "would detect a future cross-wiring of the two production method bodies." But `DevModeStatus` and `bool` are distinct types, so a cross-wiring is a compile-time error, not a runtime regression — the test cannot catch what it claims to catch. No realistic regression that compiles makes this test fail.

The invariant-preserving options are:

1. **Drop the test entirely.** The compiler's type checker already prevents the only cross-wiring that could occur. There is no runtime invariant left to gate. Dropping satisfies the AGENTS.md hard rule "a test must gate a real invariant."

2. **Replace with a concrete property that gates something real.** For example, on non-Windows hosts (where the test actually runs), assert that `HostDevModeProbe.dev_mode_status()` returns exactly `DevModeStatus::NotWindows` — a value the implementation is known to produce on this platform (the non-Windows path always returns `NotWindows`). That assertion fails if someone changes the non-Windows codepath and would be a meaningful invariant about host behaviour.

Option 1 is the simplest path. Option 2 provides a real CHK if the reviewer's feedback calls for it. Either way, the current form is still vacuous and must not ship as-is.
</review>

<review persona="correctness" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-05-30T21:09:32Z" round="2">
Round-2 test-only change in `patina-core/src/windows/mod.rs:428-447` is logically correct. The replacement `host_probe_delegates_each_method_to_its_matching_free_function` builds an `expected` `FakeProbe` carrying the host's captured free-function values (`dev_mode_status()`, `is_elevated()`), and `FakeProbe` (lines 300-307) returns those by stored field rather than by re-delegation — so the comparison is a sound field-for-field delegation assertion, not a broken one. The field-to-method mapping is correctly paired (status↔`dev_mode_status`, elevated↔`is_elevated`), types match on both `assert_eq!` arms (`DevModeStatus`/`bool`), and the comparison is deterministic within a single process (both sides read the same process-stable registry/token state; no ordering or race hazard). It is not a broken assertion. Whether the wiring invariant is sufficiently non-tautological is a tests/style-lane judgment, owned by the persona that raised the round-1 blocker. The production decision logic is genuinely untouched: the diff confines to the `#[cfg(test)]` module, so `decide_symlink_gate`'s truth table, the `dev_mode_status()` fail-closed folding, and the read-only `registry.rs` reads remain as cleared in round 1. No correctness defect at or above the 80 confidence bar.
</review>

<blockers date="2026-05-30T21:09:32Z" round="2">
One blocker landed in round 2 (style, re-raised from round 1). Business, tests, security, and correctness all passed. The tests persona considers the round-2 test non-vacuous (mutation-confirmed that neutering `HostDevModeProbe::dev_mode_status` to a constant fails it), but the style persona maintains the AGENTS.md no-vacuous-tests blocker, and any single blocking verdict flips the task back. Resolve the style blocker definitively before respawning the review fan-out.

1. **`host_probe_delegates_each_method_to_its_matching_free_function` still reads as `f() == f()` to the style persona (style).** At `patina-core/src/windows/mod.rs:428-447` the test builds `expected = FakeProbe { status: dev_mode_status(), elevated: is_elevated() }` and asserts `host.dev_mode_status() == expected.dev_mode_status()`; since `FakeProbe::dev_mode_status()` returns the stored `status` (set from `dev_mode_status()`), the comparison reduces to `dev_mode_status() == dev_mode_status()`, and a literal cross-wiring is a compile error (distinct `DevModeStatus`/`bool` types) rather than a runtime failure. Pick ONE of the style reviewer's two concrete exits and apply it (do not re-litigate; the goal is a green style verdict):
   - **Option 1 (simplest): drop the test entirely.** The compiler already prevents the only cross-wiring that could occur, so there is no runtime invariant left to gate.
   - **Option 2: replace with a concrete host-behaviour invariant.** On the non-Windows CI host (where the test actually runs), assert `HostDevModeProbe.dev_mode_status() == DevModeStatus::NotWindows` and `HostDevModeProbe::is_elevated() == false` directly — values the `#[cfg(not(windows))]` path is known to produce — so the test fails if someone changes the non-Windows codepath. (Note this overlaps with the existing `non_windows_status_is_not_windows_and_unelevated` free-function test; if you take Option 2, make the new assertion go through `HostDevModeProbe` specifically so it gates the probe's host behaviour, not the free function's.)
   Re-run all four hygiene gates after the change.

Already correct and must not regress: the production read-side capability is sound across business, tests, security, and correctness and was untouched in round 2 — the `decide_symlink_gate` truth table, the `dev_mode_status()` fail-closed folding, the read-only registry/token reads, the `winsafe` dependency clearing `deny.toml`, and the honest judgment-only deferral of CHK-012/CHK-013 to T-009. The other 8 unit tests are non-vacuous (mutation-confirmed). The retry is a single-test fix; do not alter the production module.
</blockers>

<implementer date="2026-05-30T22:05:00Z" model="claude-opus-4-8[1m]/medium" round="3">
- Completed: Resolved the re-raised round-2 style blocker by taking the
  reviewer's Option 1 (the recommended, simplest exit): dropped the
  `host_probe_delegates_each_method_to_its_matching_free_function` test
  from `patina-core/src/windows/mod.rs` entirely. The style persona's
  reasoning stands — `HostDevModeProbe::dev_mode_status`/`is_elevated`
  delegate directly to the free functions and the only cross-wiring that
  could occur (`DevModeStatus` vs `bool`) is a compile-time type error,
  so there is no runtime invariant left for the test to gate. The
  remaining 8 `windows::tests` cases still cover the gate truth table
  (`gate_disabled_not_elevated_requires_elevation`, `gate_enabled_proceeds`,
  `gate_disabled_but_elevated_proceeds_with_warning`,
  `gate_no_symlink_op_proceeds_regardless_of_status`,
  `gate_unsupported_build_proceeds`), the `is_unc_path` UNC-vs-POSIX check
  (`unc_prefix_check_distinguishes_unc_from_posix`), the symlink-mode
  predicate (`plan_predicate_detects_symlink_modes_only`), and the
  non-Windows reads (`non_windows_status_is_not_windows_and_unelevated`).
  The production module (`HostDevModeProbe`, the free functions,
  `decide_symlink_gate`, `registry.rs`) was not touched — `HostDevModeProbe`
  remains exported from `lib.rs` for T-009 and referenced in the
  `DevModeProbe` doc link, so dropping the test introduces no dead-code or
  unused-import warning.

- Undone: Nothing newly undone. The DEC-008 scope split is unchanged: the
  engine apply gate, the `ShellExecuteEx`/`runas` launch, the CLI Reporter
  prompt, and the exit-5 path remain T-009; the `patina-elevate` helper
  remains T-008. CHK-012 / CHK-013 stay judgment-only here, demonstrated at
  T-009.

- Hygiene checks: `cargo test --workspace` exited 0 (full suite green
  including the 23 doctests and the now-8-case `windows::tests` module; 0
  failed). `cargo clippy --workspace --all-targets --all-features --
  -D warnings` exited 0. `cargo +nightly fmt --all --check` exited 0.
  `cargo deny check` exited 0 (advisories / bans / licenses / sources ok;
  the `winnow` multi-version line belongs to `toml`/`toml_edit`, a
  different task, and is a non-blocking `warn`).

- Evidence: paper trail at
  `.speccy/specs/0002-patina-complete-cli/evidence/T-007.md` (round-1
  artifact; unchanged — this round only removes a vacuous test, adds no
  capability). Roll call for the CHKs under REQ-007 is unchanged from
  rounds 1–2:
  - CHK-012 (Windows Dev-Mode-OFF + symlink + declined-UAC → no link,
    stderr names `Developer Mode` + `patina doctor --fix`, exit 5):
    judgment-only → end-to-end Windows-host apply lands at T-009;
    demonstrated there.
  - CHK-013 (Windows Dev-Mode-ON + symlink → no prompt, no helper spawn,
    link created): judgment-only → the proceed path is `decide_symlink_gate`
    returning `Proceed` on `Enabled` (unit-tested as `gate_enabled_proceeds`);
    the end-to-end assertion needs T-008 + T-009; demonstrated at T-009.
  The three accountable task-scenarios remain hygiene-proven under `cargo
  test --workspace`: non-Windows reads
  (`non_windows_status_is_not_windows_and_unelevated`), `is_unc_path`
  (`unc_prefix_check_distinguishes_unc_from_posix`), and the gate truth
  table (the five `gate_*` tests).

- Discovered issues: None new. No pre-existing dead code touched; no
  production code modified.

- Procedural compliance: This round-3 entry is appended to
  `journal/T-007.md` after the round-2 blockers block; all earlier
  `<implementer>`, `<review>`, and `<blockers>` blocks are unchanged
  (append-only). No `<implementer-note>` was written into TASKS.md. The
  TASKS.md `state` for T-007 flips from `pending` (left by the round-2
  blocking review) through `in-progress` to `in-review` as the final step.
  No shipped `.claude/skills/` body was edited.
</implementer>

<review persona="business" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-05-30T21:15:11Z" round="3">
T-007 round-3 is a test-only drop and the REQ-007 read-side delivery is unchanged from the round-1 and round-2 business passes. The dropped `host_probe_delegates_each_method_to_its_matching_free_function` test gated only the internal `HostDevModeProbe`→free-function wiring (a compile-time invariant), not any REQ-007 observable behavior; its removal subtracts no done-when coverage. The production module (`HostDevModeProbe` `patina-core/src/windows/mod.rs:209-219`, the free functions, `decide_symlink_gate`, `registry.rs`) is untouched — confirmed by `grep` (zero `host_probe` references remain) and a live run (8/8 `windows::tests` green, not 9). Every read-side REQ-007 done-when item still maps to a surviving non-vacuous test: non-Windows no-check (`non_windows_status_is_not_windows_and_unelevated`), Enabled→Proceed (`gate_enabled_proceeds`), Disabled+zero-symlink→Proceed (`gate_no_symlink_op_proceeds_regardless_of_status` + `plan_predicate_detects_symlink_modes_only`), Disabled+symlink→gate-before-mutation (`gate_disabled_not_elevated_requires_elevation`), already-elevated→warn (`gate_disabled_but_elevated_proceeds_with_warning`), plus the UNC predicate for REQ-006's doctor finding. DEC-008 scope split intact: no prompt, no `ShellExecuteEx` launch, no exit-5, no engine gate in `patina-core`; those remain T-008/T-009 and CHK-012/CHK-013 stay correctly judgment-only (end-to-end Windows-host apply, no Windows CI host). Diff reflects current 2026-05-30 DEC-008 / `--fix` intent, not stale intent; no open question silently resolved (dropped dev-mode cache honored). No blocking business gap.
</review>

<review persona="tests" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-05-30T21:15:11Z" round="3">
Round-3 test-only drop is clean. The vacuous `host_probe_delegates_each_method_to_its_matching_free_function` is gone; the production module (`decide_symlink_gate`, `HostDevModeProbe`, free fns, `registry.rs`) is untouched, leaving exactly 8 unit tests in `patina-core/src/windows/mod.rs`, all green. (1) The three accountable task-scenarios remain covered by non-vacuous tests — I mutation-confirmed each: negating `is_unc_path` fails `unc_prefix_check_distinguishes_unc_from_posix` (mod.rs:349), and forcing the Disabled+not-elevated gate arm to `Proceed` fails `gate_disabled_not_elevated_requires_elevation` (mod.rs:380); Scenario A is a direct `assert_eq!(dev_mode_status(), NotWindows)` on the `#[cfg(not(windows))]` arms. (2) The evidence roll call is still accurate: the dropped test was never a witness for any accountable scenario or for CHK-012/CHK-013 (both honestly judgment-only, deferred to T-009's end-to-end Windows-host apply); the only staleness is the cosmetic "9 passed" count, which the round-3 journal entry explicitly flags. (3) No regression — `cargo test --workspace` exits 0 (36 ok, 0 failed; lib count 177→176, consistent with one test removed) and `cargo clippy -p patina-core --lib --all-features -- -D warnings` is clean, confirming no dead-code fallout. No fabrication pattern matched.
</review>

<review persona="security" verdict="pass" model="claude-opus-4-8[1m]/high" date="2026-05-30T21:15:11Z" round="3">
Round-3 change is a test-only deletion (dropped the vacuous `host_probe_delegates_each_method_to_its_matching_free_function`); no production security surface added. Registry/token reads remain read-only (`KEY::READ`/`TOKEN::QUERY`, no writes), the `decide_symlink_gate` fails closed (`patina-core/src/windows/mod.rs:265-281`), and `winsafe 0.0.27` (Windows-only, `advapi`+`kernel` only) clears `cargo deny` advisories/bans/licenses/sources. No secret handling, injection vector, or sensitive-data leak introduced. CHK-012/CHK-013 honestly deferred to T-009 per DEC-008.
</review>

<review persona="style" verdict="pass" model="claude-sonnet-4-6[1m]/medium" date="2026-05-30T21:15:11Z" round="3">
Round-2 style blocker is cleared. The `host_probe_delegates_each_method_to_its_matching_free_function` test has been dropped entirely from `patina-core/src/windows/mod.rs` (file ends at line 427 with `gate_unsupported_build_proceeds`). No new style issues introduced by the drop: `FakeProbe` remains legitimately used in 5 surviving gate tests; `HostDevModeProbe` remains in the production module (lines 207-219) and exported for T-009; all test-module imports (`ResolvedOperation`, `Plan`, `HostOs`, `Builtins`, `Resolver`, `Utf8PathBuf`) remain consumed by the `op()` / `plan()` helpers and the 8 remaining tests. The 8 surviving tests are non-vacuous — they cover real invariants across the full gate truth table, the UNC-vs-POSIX prefix check, the symlink-mode predicate over all 5 `FileMode` variants, and the `#[cfg(not(windows))]` path behaviour. Production code is untouched.
</review>

<review persona="correctness" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-05-30T21:15:11Z" round="3">
Round-3 test-only change is correctness-clean. The drop of the vacuous `host_probe_*` test from the `#[cfg(test)]` module in `patina-core/src/windows/mod.rs` left the production decision logic byte-identical: `decide_symlink_gate` (mod.rs:265-281) preserves its no-symlink early-return, the `Enabled|Unsupported|NotWindows→Proceed` arm, and the `Disabled→elevated?ProceedElevatedWarning:RequireElevation` arm with no inversion or arm reordering; `dev_mode_status()` folding (mod.rs:99-119) keeps `Ok(Some(1))→Enabled` / `Ok(_)|Err(_)→Disabled` fail-closed; the `#[cfg(windows)]` registry reads were untouched. No dangling references: `FakeProbe` still backs the five gate tests, `HostDevModeProbe` remains a `pub` API type re-exported from lib.rs:128 so no dead-code/unused-import arises. `cargo clippy -p patina-core --all-targets --all-features -- -D warnings` exits 0; the windows lib module is 8/8 green. No correctness defect at or above the 80 confidence bar.
</review>
