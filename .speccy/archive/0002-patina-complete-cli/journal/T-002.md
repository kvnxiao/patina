---
spec: SPEC-0002
task: T-002
generated_at: 2026-05-30T08:15:00Z
---

<implementer date="2026-05-30T08:15:00Z" model="claude-opus-4-8[1m]/medium" round="1">
- Completed: Added the public persisted `default_repo` pointer
  write/existence surface to
  `patina-core/src/discovery/repo.rs`, reusing the existing
  `PERSISTED_DEFAULT_FILENAME` constant and parameterizing on an
  explicit `state_dir` (no environment re-resolution inside the
  functions, so the CLI and integration harness can target an
  isolated tempdir). Three functions: `default_repo_pointer_path(state_dir: &Utf8Path) -> Utf8PathBuf`
  (joins the filename), `persisted_default_present(state_dir: &Utf8Path) -> bool`
  (plain `Utf8Path::exists` for `doctor`'s missing-pointer finding),
  and `write_persisted_default(state_dir: &Utf8Path, repo: &Utf8Path) -> Result<(), RepoDiscoveryError>`
  (writes `repo` as one UTF-8 line plus trailing newline via `fs-err`,
  matching the format the existing private `read_persisted_default`
  trims). Added a typed `RepoDiscoveryError::WritePersistedDefault { path, source }`
  variant (the enum is already `#[non_exhaustive]`, so this is
  additive). Re-exported the three functions and
  `PERSISTED_DEFAULT_FILENAME` from `patina-core/src/discovery/mod.rs`
  and `patina-core/src/lib.rs` next to the existing `discovery`
  re-exports. The write does not canonicalize — per the task and
  REQ-001, callers (T-003 `init`) canonicalize via
  `crate::paths::canonicalize` before calling. Two integration tests
  added to the existing `patina-core/tests/repo_discovery.rs`:
  `write_persisted_default_round_trips_through_read_path` (writes,
  then resolves back through `resolve_repository_root_with`'s source-3
  read path to the canonical repo) and
  `persisted_default_present_is_false_without_pointer`. Three doctests
  on the new public functions exercise the path-join and absent-pointer
  contracts.

- Undone: T-002 is the library writer only. The `patina init` command
  itself (`Command::Init`, `cmd/init.rs`, CLI dispatch, the harness
  generalization, and the `tests/init_cli.rs` end-to-end crate) is
  T-003's scope per its `<task>` element in TASKS.md, and `doctor`'s
  consumption of `persisted_default_present` is a later command task.
  Nothing in T-002's stated scope is deferred.

- Hygiene checks: `cargo test --workspace` exited 0 (includes the two
  new integration tests and three new doctests, all passing).
  `cargo clippy --workspace --all-targets --all-features -- -D warnings`
  exited 0 (zero new warnings). `cargo +nightly fmt --all --check`
  exited 0 (after sorting the new test-crate import block
  alphabetically). `cargo deny check` exited 0 (no new dependency —
  `fs-err` and `camino` were already crate dependencies).

- Evidence: red-then-green paper trail at
  `.speccy/specs/0002-patina-complete-cli/evidence/T-002.md`.
  Roll call for the CHKs under the covered REQ-001:
  - CHK-001 (`patina init T` end-to-end: manifest + `default_repo`
    contents + exit 0): judgment-only for T-002 → this CHK exercises
    the `patina init` command, which T-003 delivers. T-002 supplies
    only the `default_repo` writer the command calls; the writer's
    own round-trip is proven by evidence Scenario 1
    (`write_persisted_default_round_trips_through_read_path`), but the
    end-to-end CLI assertion cannot be scripted from this task.
    reviewer-business judges the library/command split on the diff.
  - CHK-002 (`patina init` on an existing manifest refuses, exits 1,
    state untouched): judgment-only for T-002 → entirely a
    command-level refusal path delivered by T-003; T-002's writer is
    not even reached on that path. reviewer-business judges on the
    diff.
  T-002's task-scenario behaviour (the writer round-trip and the
  absent-pointer existence check) is fully demonstrated by evidence
  Scenarios 1 and 2; the two REQ-001 CHKs are command-level and land
  with T-003.

- Discovered issues: `patina-core/src/discovery/mod.rs` carries a
  module-doc note saying "T-005 will land a shared
  `state_dir::resolve()` helper; until then the persisted-default path
  is computed OS-specifically inside `repo`." `state_dir::compute_root`
  already exists and the private `persisted_default_repo_path` already
  routes through it, so that note reads as partially stale. Left
  untouched — it is pre-existing prose outside this task's scope and
  the locked-SPEC / surgical-change rules counsel against drive-by doc
  edits. Flagged for whoever lands the `state_dir::resolve()` consumer.

- Procedural compliance: This implementer entry lands directly in
  `journal/T-002.md` per the journal-file schema; no
  `<implementer-note>` was written into TASKS.md. The TASKS.md
  `state="..."` attribute for T-002 flips from `in-progress` to
  `in-review` as the final step of this turn. No shipped skill bodies
  under `.claude/skills/` were edited.
</implementer>

<review persona="business" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-05-30T08:30:00Z" round="1">
T-002 (library `default_repo` writer surface, covers REQ-001) faithfully delivers its scoped slice of REQ-001. Both task-level `<task-scenarios>` are covered by passing tests: `write_persisted_default` round-trips through the real `resolve_repository_root_with` source-3 read path (`patina-core/tests/repo_discovery.rs:127`), and `persisted_default_present` returns false for an empty state dir. The writer's one-UTF-8-line-plus-newline format matches what the private `read_persisted_default` trims/validates (`patina-core/src/discovery/repo.rs:237` vs `:315`), so the write/read contract holds.

The two REQ-001 CHKs (CHK-001, CHK-002) are end-to-end `patina init` command scenarios correctly deferred to T-003 per its `<task>` body; T-002 cannot script them and the journal/evidence flag them as judgment-only honestly. No scope creep, no non-goal touched, no open question silently resolved. The deliberate no-canonicalize choice in the writer is consistent with REQ-001 prose ("callers canonicalize") and the canonical-path done-when is verifiable at T-003/CHK-001.

Non-blocking nit (not a defect in shipped behavior): the `persisted_default_present` doc comment at `patina-core/src/discovery/repo.rs:192` cites "REQ-008" for doctor's missing-pointer finding; that finding is REQ-005 (`DOC-NO-DEFAULT-REPO`) / REQ-006 (`--fix`). REQ-008 is the `patina-elevate.exe` crate. Worth fixing whenever this file is next touched, but it does not gate the merge.
</review>

<review persona="tests" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-05-30T08:30:00Z" round="1">
T-002's two task-scenarios are translated into real executable tests that drive the actual code paths, not mocks. `write_persisted_default_round_trips_through_read_path` (`patina-core/tests/repo_discovery.rs:127`) writes via the real `write_persisted_default`, then both reads the file directly (`contents.trim() == repo.as_str()`) and drives the real source-3 read path through `resolve_repository_root_with(..., Some(pointer))` → `read_persisted_default` → `validate_root`/canonicalize, so a no-op or wrong-path writer would fail the `pointer.exists()`, content, or `resolved == repo` assertions. `persisted_default_present_is_false_without_pointer` (`repo_discovery.rs:158`) asserts the false polarity; the true polarity is asserted inside the round-trip test, so an always-true or always-false implementation is caught. Three doctests on the new public functions pass (two with assertions, `write_persisted_default` correctly `no_run`). I independently reran both integration tests (8 passed) and the doctests (21 passed). Evidence at `.speccy/specs/0002-patina-complete-cli/evidence/T-002.md` is a genuine red-then-green trail: red is a real `E0432 unresolved import` compile failure (correct shape for a new-API task), green is scoped per-test runner output materially different from the red phase and distinct from the `cargo test --workspace` hygiene run; both evidence test names appear in the diff. Roll call covers REQ-001's CHK-001 and CHK-002, both labeled `judgment-only` — plausible, since both are `patina init` command end-to-end scenarios that T-003 (still pending) delivers and T-002's writer cannot exercise; deferred to reviewer-business per contract, not mine to block. Test naming and tempdir-canonicalization conventions match the surrounding `repo_discovery.rs` style.
</review>

<review persona="security" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-05-30T08:30:00Z" round="1">
T-002 adds a persisted `default_repo` pointer writer/existence surface to `patina-core::discovery::repo` with no security-relevant exposure. The written content is a filesystem repo path (not a secret); the `WritePersistedDefault` error carries only the path and underlying `io::Error`, no credential leak, and there are no `tracing`/log sinks in the diff. `default_repo_pointer_path` joins a fixed constant filename onto the caller-supplied `state_dir`, so no untrusted component drives the path and there is no traversal. The read-back path (`read_persisted_default` → `validate_root`) still validates the pointer points at a real `[patina].root = true` directory, so a tampered pointer cannot escalate. Advisory-lock serialization (REQ-009) correctly lives in the T-003 command layer per the documented engine/CLI split, not in this parameterized library function — out of scope for this slice. No auth boundary, injection vector, or sensitive-data exposure introduced.
</review>

<review persona="style" verdict="pass" model="claude-sonnet-4-6[1m]" date="2026-05-30T08:30:00Z" round="1">
All three new functions (`default_repo_pointer_path`, `persisted_default_present`, `write_persisted_default`) follow the project's conventions exactly: `&Utf8Path` parameter types, `fs-err` I/O, `std::io::Error` source fields consistent with every existing variant in the file, `#[must_use]` with custom messages, complete `# Errors` / `# Examples` sections, and no `unwrap()` or `expect()` in production paths. Import additions in both `mod.rs` and `lib.rs` are accepted by `rustfmt` and `clippy --D warnings` exits clean. The two integration tests gate real invariants (round-trip through the existing read path; existence false without a file) and use `.expect()` only on fixture setup, consistent with the file's existing `#![expect(clippy::expect_used)]` gate. No dead code, no parallel reimplementations, no style drift found.
</review>

<review persona="correctness" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-05-30T08:30:00Z" round="1">
T-002's persisted `default_repo` writer slice is correct. The write/read round-trip is sound: `write_persisted_default` emits `format!("{repo}\n")` and the existing `read_persisted_default` `.trim()`s it back to exactly `repo` (verified by passing `write_persisted_default_round_trips_through_read_path` and confirmed against `patina-core/src/discovery/repo.rs:237-248` vs `:315-323`). `default_repo_pointer_path` and `persisted_default_present` are linear, branch-free, with no edge-case mishandling. The single fallible path maps `std::io::Error` into the typed `RepoDiscoveryError::WritePersistedDefault` variant carrying the path — no `unwrap`/`expect`, no swallowed error, no skipped cleanup, no leaked handle. No off-by-one, control-flow, concurrency, or resource-leak defect at or above the reporting threshold. Tests and doctests pass.
</review>
