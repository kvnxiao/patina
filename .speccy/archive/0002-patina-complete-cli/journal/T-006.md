---
spec: SPEC-0002
task: T-006
generated_at: 2026-05-30T20:39:53Z
---

<implementer date="2026-05-30T20:39:53Z" model="claude-opus-4-8[1m]/medium" round="1">
- Completed: Landed `patina promote <target>` end-to-end (REQ-004, REQ-009,
  REQ-010). Added a `Promote` variant + `PromoteArgs` (positional `target`,
  `--json`, `--yes`) to `patina-cli/src/cli.rs`; dispatched the arm with the
  TTY-detection wiring in `patina-cli/src/main.rs`; registered `pub mod
  promote;` in `patina-cli/src/cmd/mod.rs`. New command module
  `patina-cli/src/cmd/promote.rs`: under ONE exclusive guard it `read_latest_commit`s,
  locates the `ExpectedTarget` whose `manage_key` matches the canonicalized
  target, refuses (exit 1) on an `ExpectedTarget::Symlink` and on an
  `ExpectedTarget::Content` whose `source()` ends in `.tmpl`, then copies the
  target's current bytes via `fs-err` into the journaled repository source and
  re-journals so the fresh `<ts>.COMMIT` records `content_hash(new bytes)`. The
  refusal checks run before the prompt, so a refused promote never mutates.
  Per the task's "factor any shared `pub(crate)` helper" directive, I extracted
  the lock-acquisition and Held-policy re-apply that `remove` (T-005) and
  `promote` share into a new `patina-cli/src/cmd/managed.rs`
  (`acquire_state_and_lock`, `rejournal`) and refactored `remove.rs` to call it
  rather than duplicate the lock path, engine-error mapping, and re-plan/
  re-execute sequence. Tests: new `patina-cli/tests/promote_cli.rs` (5
  integration tests) plus unit tests in `promote.rs` for the refusal-shape
  decision, the confirm gate, and JSON-envelope determinism.

- Undone: Nothing in T-006's scope is deferred. T-007 (Windows dev-mode
  detection module) and the remaining SPEC-0002 commands (`doctor`, docs)
  remain their own tasks, untouched here.

- Hygiene checks: `cargo test --workspace` exited 0 (all suites pass, including
  the 5 new `promote_cli` integration tests and the 7 new `cmd::promote`
  unit tests, and the refactored `remove_cli` / `cmd::remove` suites still
  green). `cargo clippy --workspace --all-targets --all-features -- -D warnings`
  exited 0. `cargo +nightly fmt --all --check` exited 0 (only the pre-existing
  benign `float_literal_trailing_zero` unknown-config warnings). `cargo deny
  check` exited 0 (no new dependencies — `promote` and `managed` reuse existing
  `patina-core` / `fs-err` / `serde_json` surfaces).

- Evidence: red-then-green paper trail at
  `.speccy/specs/0002-patina-complete-cli/evidence/T-006.md`. Roll call for the
  CHKs under REQ-004 / REQ-009:
  - CHK-007 (copy-mode promote rewrites source, re-journals new hash, status
    CLEAN): demonstrated → evidence Scenario 1 covers the external-edit →
    promote → source-bytes / journal-hash / `status --json` CLEAN run versus
    the pre-edit unrecognized-subcommand red.
  - CHK-008 (template-rendered target refused, names `.tmpl` and `template`,
    exit 1, nothing mutated): demonstrated → evidence Scenario 2.
  - CHK-016 (concurrent `add` blocks on a held exclusive lock): hygiene → owned
    and demonstrated by T-004's `add_serializes_behind_a_held_exclusive_lock`;
    appears in T-006's `speccy check` render only because it shares REQ-009. It
    re-runs under `cargo test --workspace`. T-006's own REQ-009 surface (one
    held exclusive lock, re-apply under `LockPolicy::Held`) is exercised by the
    Scenario 1 re-journal, which would deadlock if the command self-contended.
  Beyond the covered CHKs, the SPEC's third `promote` done-when scenario
  (symlink-target refusal) is demonstrated by evidence Scenario 3, and the
  unmanaged-path exit-1 and `--json` determinism behaviours by Scenarios 4–5.

- Discovered issues: None new. The shared-helper extraction into
  `cmd/managed.rs` is the cross-command refactor the T-006 task body
  anticipated; `remove.rs` now depends on it, which is intentional and covered
  by the still-green `remove_cli` suite.

- Procedural compliance: This implementer entry lands directly in
  `journal/T-006.md` per the journal-file schema; no `<implementer-note>` was
  written into TASKS.md. The TASKS.md `state="..."` attribute for T-006 flips
  from `in-progress` to `in-review` as the final step of this turn. No shipped
  skill bodies under `.claude/skills/` required edits during this task.
</implementer>

<review persona="business" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-05-30T20:43:19Z" round="1">
T-006 (`patina promote`) faithfully delivers REQ-004 and REQ-009. Every done-when bullet maps to code and a passing test: copy-mode promote rewrites the repo source and re-journals the blake3 hash with a subsequent CLEAN status (CHK-007, `promote.rs:104-112` + `promote_cli.rs:67`); template-source refusal names the `.tmpl` path and "template" at exit 1 mutating nothing (CHK-008, `promote.rs:136-145`); symlink-target refusal exits 1 mutating nothing (`promote.rs:127-135`); copy-tree resolves the single leaf `ExpectedTarget` via `manage_key`; the exclusive lock is taken once and the re-apply runs under `LockPolicy::Held` with no self-contention (`managed.rs:41-65`). Refusals fire before the prompt and before any write, satisfying the "no file mutated" guarantee. The `cmd/managed.rs` extraction is the shared-helper factoring the task body explicitly directed, not scope creep; `remove_cli` (T-005) and the full unit suite stay green after the refactor. Changelog intent (blake3, not SHA) is reflected in the test. No non-goal breached, no open question silently resolved. All 5 promote integration tests and 51 cli unit tests pass.
</review>

<review persona="tests" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-05-30T20:43:19Z" round="1">
Tests genuinely exercise the scenarios for SPEC-0002/T-006; evidence roll call is complete and non-fabricated.

- Evidence (`.speccy/specs/0002-patina-complete-cli/evidence/T-006.md`) present and well-formed: roll call covers all CHKs under the covered REQs — CHK-007 demonstrated (Scenario 1), CHK-008 demonstrated (Scenario 2), CHK-016 hygiene with a real cite (`add_serializes_behind_a_held_exclusive_lock` confirmed at `patina-cli/tests/add_cli.rs:264`; CHK-016 legitimately belongs to T-004 and only appears in T-006's render via shared REQ-009). Red/green are materially different (clap exit-2 `unrecognized subcommand 'promote'` vs `test ... ok`); the red command is scoped to the `promote_cli` crate, not the full hygiene run; all five test names and the unit test in the evidence appear verbatim in the diff; cited panic line numbers (`:77/:157/:204/:236/:263`) align with the actual assert sites.
- Integration tests spawn the real `patina` binary against isolated tempdir fixtures (`patina-cli/tests/promote_cli.rs` via `common::Fixture`) — no mocking of the system under test.
- CHK-007 proven non-vacuous by a mutation test: neutering `rejournal` in `patina-cli/src/cmd/managed.rs` (skip the re-apply) makes `promote_copy_target_rewrites_source_and_rejournals` fail precisely on the journal-hash assertion (`promote_cli.rs:95`), confirming the test gates the `LockPolicy::Held` re-journal, not just the source byte-write. Restored after.
- Negative paths carry specific, non-catch-all assertions: template-refusal asserts stderr contains both `gitconfig.tmpl` and `template` plus byte-unchanged source; symlink-refusal asserts `~/.zshrc` and `symbolic-link` plus still-a-symlink and unchanged source; unmanaged asserts `~/.bashrc` and `not managed` plus unchanged file. Refusals occur before the prompt and before any `fs_err::write` in `cmd/promote.rs::run`, so "no file mutated" is meaningful.
- REQ-010 determinism covered by both the pure-function unit test (`success_envelope_is_deterministic`) and the integration JSON test; envelope carries no timestamps/PIDs.
- All 5 integration + 7 unit tests pass against the real implementation.
</review>

<review persona="security" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-05-30T20:43:19Z" round="1">
`patina promote` is sound from a security standpoint. The user-supplied target is used only to match a trusted journaled `ExpectedTarget`; reads/writes go to the engine-recorded `target()`/`source()` paths, not the raw argument, so there is no path-traversal or arbitrary-write vector (`patina-cli/src/cmd/promote.rs:69-107`). Symlink and template targets are refused before any mutation, the `#[non_exhaustive]` wildcard fails closed, and consent gating precedes all filesystem writes (`promote.rs:85-104`, `:121-159`). Single held exclusive lock closes the lookup→re-journal TOCTOU window (`patina-cli/src/cmd/managed.rs:41-65`). File contents are never logged — only paths appear in output. No new dependencies.
</review>

<review persona="style" verdict="blocking" model="claude-sonnet-4-6[1m]/medium" date="2026-05-30T20:43:19Z" round="1">
`patina-cli/src/cmd/remove.rs:60` and `patina-cli/src/cmd/promote.rs:51` each declare an identical private constant:

```rust
const TEMPLATE_SUFFIX: &str = ".tmpl";
```

The `managed.rs` module was introduced precisely as the shared scaffolding for these two commands. This constant is used by both: `remove.rs` uses it to detect template sources that need re-rendering; `promote.rs` uses it to refuse promotion of template targets. It belongs in `managed.rs` as a `pub(crate)` constant, not duplicated in two sibling modules.

Per `.claude/rules/rust/rust-let-types-carry-invariants.md` (Reuse over reinvent):

> Before adding a new helper, type, or utility, check whether one already exists… Call the existing one rather than introducing a parallel implementation.

Move `TEMPLATE_SUFFIX` to `managed.rs` as `pub(crate) const TEMPLATE_SUFFIX: &str = ".tmpl";` and have both `remove.rs` and `promote.rs` reference `crate::cmd::managed::TEMPLATE_SUFFIX` (or via `use`). This is the mechanical deduplication the extraction was supposed to accomplish.
</review>

<review persona="correctness" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-05-30T20:43:19Z" round="1">
No correctness defects found. Refusal guards (symlink, `.tmpl`) fire before the prompt and before any read/write, and the `#[non_exhaustive]` catch-all refuses rather than panics. The copy logic reads `expected.target()` and writes `expected.source()` (correct direction, correct canonical paths). The re-journal moves the single held exclusive guard into `execute_plan(..., LockPolicy::Held(guard))`, which engine.rs:352 reuses without re-acquiring, so there is no self-deadlock; the fresh COMMIT re-plans against the modified source and records the new content hash. The remove.rs refactor to `acquire_state_and_lock`/`rejournal` is behavior-preserving (identical lock and re-plan/re-execute logic; only genuinely-unused imports dropped). Build, clippy-relevant compile, promote_cli (5), remove_cli (4), and cmd unit tests (51) all pass.
</review>

<blockers date="2026-05-30T20:43:19Z" round="1">
One blocker landed in round 1 (style). Business, tests, security, and correctness all passed. Fix the single blocker before respawning the review fan-out.

1. **Duplicated `TEMPLATE_SUFFIX = ".tmpl"` constant violates the reuse-over-reinvent rule (style).** `patina-cli/src/cmd/remove.rs:60` and `patina-cli/src/cmd/promote.rs:51` each declare an identical private `const TEMPLATE_SUFFIX: &str = ".tmpl";`. The `cmd/managed.rs` module is the shared scaffolding for exactly these two commands, so the constant belongs there. Per `.claude/rules/rust/rust-let-types-carry-invariants.md` ("Reuse over reinvent"), move it to `managed.rs` as `pub(crate) const TEMPLATE_SUFFIX: &str = ".tmpl";` and have both `remove.rs` and `promote.rs` reference `crate::cmd::managed::TEMPLATE_SUFFIX`. Re-run all four hygiene gates after the dedup.

Already correct and must not regress: the round-1 `promote` behaviour is sound across business, tests, security, and correctness — the single held exclusive lock reused via `LockPolicy::Held`, the symlink/`.tmpl` refusal guards firing before any mutation, the copy-current-bytes-into-repo-source logic, the re-journal recording the new blake3 hash with a subsequent CLEAN status, and the behaviour-preserving `remove.rs` refactor onto `managed.rs`. CHK-007, CHK-008, and the negative/`--json` scenarios are non-vacuous and pass. The retry is a one-constant dedup: move `TEMPLATE_SUFFIX` into `managed.rs` and reference it from both call sites; do not alter behaviour.
</blockers>

<implementer date="2026-05-30T21:05:00Z" model="claude-opus-4-8[1m]/medium" round="2">
- Completed: Addressed the single round-1 style blocker by deduplicating the
  `TEMPLATE_SUFFIX` constant. Moved the declaration into the shared
  `patina-cli/src/cmd/managed.rs` module as
  `pub(crate) const TEMPLATE_SUFFIX: &str = ".tmpl";` (with a doc comment
  naming both consuming commands), and deleted the two identical private
  `const TEMPLATE_SUFFIX: &str = ".tmpl";` declarations from
  `patina-cli/src/cmd/remove.rs` and `patina-cli/src/cmd/promote.rs`. Both
  call sites now reference the shared constant via
  `use crate::cmd::managed::TEMPLATE_SUFFIX;` — the `ends_with(TEMPLATE_SUFFIX)`
  checks in `remove.rs` (template re-render path) and `promote.rs` (template
  refusal guard) are unchanged. Pure refactor: no behaviour, control flow, or
  output changed.

- Undone: Nothing in T-006's scope is deferred.

- Hygiene checks: `cargo test --workspace` exited 0. `cargo clippy --workspace
  --all-targets --all-features -- -D warnings` exited 0. `cargo +nightly fmt
  --all --check` exited 0. `cargo deny check` exited 0.

- Evidence: round-1 paper trail at
  `.speccy/specs/0002-patina-complete-cli/evidence/T-006.md` stands unchanged —
  this round altered no behaviour and added no test, so the CHK roll call is
  unchanged from round 1:
  - CHK-007 (copy-mode promote rewrites source, re-journals new hash, status
    CLEAN): demonstrated → evidence Scenario 1; still green under
    `cargo test --workspace`.
  - CHK-008 (template-rendered target refused, names `.tmpl` and `template`,
    exit 1, nothing mutated): demonstrated → evidence Scenario 2; the refusal
    guard now reads the shared `managed::TEMPLATE_SUFFIX` and the
    template-refusal integration test stays green.
  - CHK-016 (concurrent `add` blocks on a held exclusive lock): hygiene →
    owned by T-004, re-runs under `cargo test --workspace`; untouched here.
  The dedup is a compile-time reference change only; the existing red/green
  pairs continue to gate the behaviour.

- Discovered issues: None.

- Procedural compliance: Retry amended the prior pass's uncommitted WIP in
  place — no `git restore`/`clean`/`checkout`, no rewrite from scratch. This
  `round="2"` block is appended after the round-1 blockers; earlier blocks are
  unmodified. TASKS.md T-006 `state` flips `pending` → `in-progress` →
  `in-review` this turn. No `<implementer-note>` was written into TASKS.md; no
  shipped `.claude/skills/` body was edited.
</implementer>

<review persona="business" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-05-30T20:48:29Z" round="2">
Round-2 refactor preserves the REQ-004/REQ-009 behaviour the round-1 business pass validated. The single style blocker (duplicated `const TEMPLATE_SUFFIX = ".tmpl"`) is resolved: the constant now lives once in `patina-cli/src/cmd/managed.rs:34` as `pub(crate)` and both call sites import it (`promote.rs:36`, `remove.rs:39`), used unchanged at `promote.rs:134` and `remove.rs:148`. The change moves three shared items (`TEMPLATE_SUFFIX`, `acquire_state_and_lock`, `rejournal`) into the `managed.rs` scaffolding — which is exactly the `pub(crate)` shared-helper factoring the T-006 task body directed ("factor any shared `pub(crate)` helper in `cmd/` rather than duplicating it"), not scope creep. No observable behaviour changed: `promote` still refuses symlink and `.tmpl` targets before any prompt or write (`promote.rs:89`, `:124-143`), copies target bytes into the journaled source (`promote.rs:100-105`), and re-journals under `LockPolicy::Held` reusing the single held exclusive lock (`promote.rs:110` → `managed.rs:65-71`). CHK-007 and CHK-008 hold: all 5 promote_cli + 4 remove_cli integration tests and the cmd::promote/cmd::remove unit suites pass; clippy `-D warnings` clean. No non-goal breached, no open question silently resolved.
</review>

<review persona="tests" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-05-30T20:48:29Z" round="2">
Round-2 refactor preserves CHK-007/CHK-008 coverage; the deduplicated `TEMPLATE_SUFFIX` guard is still exercised by live tests.

The round-1 style blocker is resolved: exactly one `pub(crate) const TEMPLATE_SUFFIX: &str = ".tmpl";` now lives in `patina-cli/src/cmd/managed.rs:34`, and both consumers reference it (`promote.rs:36`/`promote.rs:134` template-refusal guard, `remove.rs:39`/`remove.rs:148` template re-render path). No duplicate declaration survives. The change is a pure compile-time reference move — no test or behaviour altered — so the round-1 evidence roll call legitimately stands unchanged.

Evidence (`.speccy/specs/0002-patina-complete-cli/evidence/T-006.md`) present and well-formed; roll call covers CHK-007 (demonstrated, Scenario 1), CHK-008 (demonstrated, Scenario 2), CHK-016 (hygiene, cites `add_serializes_behind_a_held_exclusive_lock`, T-004-owned via shared REQ-009). Red/green are materially different (clap exit-2 `unrecognized subcommand 'promote'` vs `test ... ok`); the red command is scoped to the `promote_cli` crate, not the workspace hygiene run; all five test names appear in the diff. No fabrication pattern matches.

Verified green against the real implementation after the refactor: `promote_cli` (5/5, including CHK-007 `promote_copy_target_rewrites_source_and_rejournals` and CHK-008 `promote_template_target_refuses`), `remove_cli` (4/4, no regression from the shared-constant move), and `cmd::promote` unit suite (7/7, including `refuse_unpromotable_refuses_template_targets` which drives the deduplicated guard, and `success_envelope_is_deterministic` for REQ-010). Negative paths carry specific assertions (template refusal asserts both `gitconfig.tmpl` and `template`; symlink refusal asserts target name and `symbolic-link`; unmanaged asserts `not managed`), not catch-all error matches, and each verifies the source/target is unmutated.
</review>

<review persona="security" verdict="pass" model="claude-opus-4-8[1m]/high" date="2026-05-30T20:48:29Z" round="2">
Round-2 change is a pure compile-time constant relocation with no new security surface. `TEMPLATE_SUFFIX = ".tmpl"` moved into `patina-cli/src/cmd/managed.rs:34` as `pub(crate)`; both `remove.rs` and `promote.rs` now `use crate::cmd::managed::TEMPLATE_SUFFIX` (promote.rs:36, used at promote.rs:134). Value is byte-identical and the `ends_with(TEMPLATE_SUFFIX)` template-refusal guard fires unchanged before any mutation. No new dependency, no path/IO/auth/logging change, `pub(crate)` scope stays within the CLI crate. Round-1 security guarantees (trusted-journaled-target matching, single held exclusive lock closing the lookup→re-journal TOCTOU, refusals-before-writes, paths-not-contents in output) are intact.
</review>

<review persona="style" verdict="pass" model="claude-sonnet-4-6[1m]/medium" date="2026-05-30T20:48:29Z" round="2">
Round-1 blocker is fully cleared. `managed.rs:34` is the single source of truth: `pub(crate) const TEMPLATE_SUFFIX: &str = ".tmpl";` with a doc comment naming both consumers. Both `remove.rs:39` and `promote.rs:36` reference it via `use crate::cmd::managed::TEMPLATE_SUFFIX;`; no duplicate private `const` declaration remains anywhere in the crate. No new style issues introduced.
</review>

<review persona="correctness" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-05-30T20:48:29Z" round="2">
Round-2 `TEMPLATE_SUFFIX` relocation is a behaviour-preserving refactor. The constant now lives once in `patina-cli/src/cmd/managed.rs:34` as `pub(crate) const TEMPLATE_SUFFIX: &str = ".tmpl";` with the identical value `".tmpl"`; both prior private duplicates in `remove.rs` and `promote.rs` are removed and replaced with `use crate::cmd::managed::TEMPLATE_SUFFIX;`. Both call sites are unchanged in semantics — `promote.rs:134` `source.ends_with(TEMPLATE_SUFFIX)` (template-refusal guard) and `remove.rs:148` `source.as_str().ends_with(TEMPLATE_SUFFIX)` (template re-render path) — same value, same `ends_with` comparison, same control flow. `patina-cli` builds clean. No logic, boundary, error-handling, or control-flow change; the round-1 correctness guarantees are unaffected.
</review>
