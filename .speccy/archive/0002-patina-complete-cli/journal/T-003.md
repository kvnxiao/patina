---
spec: SPEC-0002
task: T-003
generated_at: 2026-05-30T09:10:00Z
---

<implementer date="2026-05-30T09:10:00Z" model="claude-opus-4-8[1m]/medium" round="1">
- Completed: Landed `patina init` (REQ-001, REQ-010) and generalized
  the integration harness. Added an `Init` variant + `InitArgs` to the
  `Command` enum in `patina-cli/src/cli.rs` (optional positional
  `path: Option<Utf8PathBuf>`, `--json`, `--yes`), parsing-only like the
  sibling args structs. Created `patina-cli/src/cmd/init.rs` with
  `pub async fn run(args: &InitArgs, reporter: &mut impl Reporter) -> Result<i32>`,
  registered `pub mod init;` in `cmd/mod.rs`, and dispatched the
  `Command::Init` arm in `main.rs`. Behaviour: resolve the target dir
  (positional or CWD, created via `fs_err::create_dir_all`); if a
  `patina.toml` already exists, refuse via `refuse_existing` before
  taking the lock or mutating anything (exit 1); otherwise acquire the
  exclusive advisory lock at `state_dir::resolve()?.join("lock")` via
  `acquire_lock(.., LockKind::Exclusive, exclusive_timeout())` (REQ-009),
  re-check the manifest under the lock, write
  `scaffold_root_manifest(<rfc3339-now>)` (T-001) to
  `<target>/patina.toml`, then `write_persisted_default(&state, &canonical)`
  (T-002) with the `canonicalize_path`-canonicalized target. Output
  (REQ-010): human path prints an "Initialized" line then the
  `Next: run \`patina add <path>\` to register an existing dotfile.`
  hint as the final stdout line; `--json` emits a deterministic
  `{ "initialized", "default_repo" }` document on stdout (no timestamp).
  The lock error is mapped through `EngineError::from` so a contention
  timeout reaches the exit-code-4 mapping in `ExitCode::from_error_chain`.
  Harness: added a generic `Fixture::run(&self, args, extra) -> Output`
  to `patina-cli/tests/common/mod.rs` carrying the same
  `PATINA_REPO`/`HOME`/`USERPROFILE`/`XDG_STATE_HOME`/`LOCALAPPDATA`
  isolation, and made `apply_with_env` delegate to it (prepending
  `apply`), so the existing apply suites keep working unchanged. New
  integration crate `patina-cli/tests/init_cli.rs` with four end-to-end
  tests plus five `cmd::init` unit tests.

- Undone: Nothing in T-003's stated scope is deferred. `patina add`
  (T-004) and the later command tasks consume the same generalized
  `Fixture::run` harness but are out of this task's scope. CHK-018
  (`patina doctor --json` determinism) belongs to the `doctor` command
  task, not `init`.

- Hygiene checks: `cargo test --workspace` exited 0 (33 test binaries
  ok, including the new `init_cli` integration crate and the
  `cmd::init` unit tests). `cargo clippy --workspace --all-targets
  --all-features -- -D warnings` exited 0 (resolved a doc-lazy-continuation,
  a match-single-pattern, and an unused-async by adopting the same
  `#[expect(clippy::unused_async, ..)]` the sibling commands use; added
  `#![expect(clippy::expect_used, ..)]` to the new test crate, matching
  `common/mod.rs`). `cargo +nightly fmt --all --check` exited 0.
  `cargo deny check` exited 0 (no new dependency — `jiff`, `serde_json`,
  `fs-err`, `camino` were already crate dependencies).

- Evidence: red-then-green paper trail at
  `.speccy/specs/0002-patina-complete-cli/evidence/T-003.md`.
  Roll call for the CHKs under the covered REQ-001 and REQ-010:
  - CHK-001 (`patina init T`: manifest `root = true` + `default_repo`
    canonical path + exit 0): demonstrated → evidence Scenario 1
    (`init_scaffolds_manifest_pointer_and_hint`), red exit 2 → green
    exit 0 with all three artefacts asserted.
  - CHK-002 (`patina init T` on an existing manifest: byte-identical
    file, stderr `already exists` + path, exit 1): demonstrated →
    evidence Scenario 2 (`init_refuses_when_manifest_exists`), red exit
    2 → green exit 1 with the state directory left untouched.
  - CHK-017 (`patina init T --json` second/third runs byte-identical;
    success vs failure differ): demonstrated → evidence Scenarios 3 and
    4 (`init_json_failure_is_byte_stable_across_reruns`,
    `init_json_success_emits_deterministic_schema`). The two failing
    `--json` runs emit a byte-identical typed-error document; the
    success document carries only the deterministic canonical/pointer
    paths.
  - CHK-018 (`patina doctor --json` determinism): judgment-only for
    T-003 → this CHK exercises `doctor`, a later command task, not
    `init`. No `init` artefact proves it; reviewer-business judges the
    command split on the diff.

- Discovered issues: None new. The harness generalization touches the
  shared `common/mod.rs`; `apply_with_env`/`apply` keep their exact
  prior signatures and delegate, so the existing apply integration
  crates compile and pass unchanged.

- Procedural compliance: This implementer entry lands directly in
  `journal/T-003.md` per the journal-file schema; no
  `<implementer-note>` was written into TASKS.md. The TASKS.md
  `state="..."` attribute for T-003 flips from `in-progress` to
  `in-review` as the final step of this turn. No shipped skill bodies
  under `.claude/skills/` were edited. The locked `SPEC.md` was not
  touched.
</implementer>

<review persona="business" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-05-30T09:45:00Z" round="1">
T-003 (`patina init` + harness generalization) faithfully delivers REQ-001 and the init-owned slice of REQ-010. Every REQ-001 `<done-when>` bullet maps to observable behavior proven by `patina-cli/tests/init_cli.rs` (CHK-001/CHK-002/CHK-017, all green): root `patina.toml` with `[patina] root = true` + `created_at`, canonical absolute `default_repo` pointer, exit-1 typed refusal naming the existing path with the state dir left untouched, positional-path dir creation, and the final-line `patina add` hint. REQ-010 stdout/stderr split and byte-stable `--json` (success and failure documents) hold. CHK-018 (`doctor --json`) is correctly deferred to T-010, which explicitly owns it — REQ-010 is co-covered across tasks. No non-goal scope crept in; `--yes` is parse-only-for-parity per the task note; no open question silently resolved. The shared-harness generalization is behavior-preserving — `apply_cli`/`status_cli`/`rollback_cli`/`deterministic_stdout` all still pass. Serves the fresh-laptop first-time-setup user story directly. Files: `patina-cli/src/cmd/init.rs`, `patina-cli/src/cli.rs`, `patina-cli/tests/init_cli.rs`, `patina-cli/tests/common/mod.rs`.
</review>

<review persona="tests" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-05-30T09:45:00Z" round="1">
Tests faithfully exercise the real behaviour for every demonstrated CHK under REQ-001/REQ-010.

CHK-001/CHK-002 (`patina-cli/tests/init_cli.rs:22,67`) and the `--json` determinism tests (`:108,147`) all spawn the real `patina` binary end-to-end via `Fixture::run` -- no mocking of the system under test. CHK-001 asserts root=true (independent TOML parse), the `default_repo` pointer (independently canonicalized via `fs_err::canonicalize`, matching production's `paths::canonicalize` existing-path branch), and the hint as the final stdout line. CHK-002 is a specific negative path: exit 1, byte-identical manifest preservation, stderr contains both `already exists` and the manifest path, and the state dir untouched (no pointer) -- it would fail if the impl overwrote the file or wrote the pointer on refusal. CHK-017's failure polarity asserts byte-identical stdout across two runs plus the typed `error=already_exists`/`path` schema; the success-schema test pins that no timestamp leaks into stdout.

Evidence at `.speccy/specs/0002-patina-complete-cli/evidence/T-003.md` shows a genuine red→green transition: the red phase removed the `Init` subcommand so clap exits 2, with panic locations (`init_cli.rs:27,76,121,152`) mapping to real assertion lines, flipping to `ok` green. Roll call covers all four CHKs; CHK-018 (`doctor`) is correctly `judgment-only` (no `init` artifact can prove it; deferred to the later doctor task). No fabrication pattern matches; the red capture is scoped to the `init_cli` crate, distinct from the full-suite hygiene run.

Minor (non-blocking) note: the CHK-017 scenario names a third byte-for-byte repetition and a success-vs-failure diff; the test runs two failing reps and pins the two schemas separately rather than an explicit `assert_ne!`/third run. The byte-stability invariant and distinct-schema contract are both adequately covered by the same code paths, so this is acceptable.
</review>

<review persona="security" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-05-30T09:45:00Z" round="1">
No security defects in `patina init` (T-003). The command crosses no auth/privilege boundary, handles no secrets, and runs entirely with the invoking user's privileges against their own paths.

Reviewed angles, all clear:
- TOCTOU: the pre-lock `manifest_path.exists()` fast-fail is followed by a re-check under the exclusive lock before the write (`patina-cli/src/cmd/init.rs:66-83`), closing the race window so a concurrent `init` cannot be clobbered. Lock is acquired before any file mutation (REQ-009), reusing the SPEC-0001 `acquire_lock`/`resolve_state_dir` primitives rather than re-deriving them.
- No-overwrite invariant: an existing `patina.toml` is refused without truncation; the integration test asserts byte-identical preservation and that the `default_repo` pointer is never written on refusal (`patina-cli/tests/init_cli.rs:82-100`).
- Output/data exposure: JSON envelopes go through `serde_json::json!` (properly escaped); only user-known paths and a config-resident `created_at` timestamp are emitted; no secrets, no stdout timestamp/PID leakage (determinism preserved per CHK-017).
- No injection surface: no shell, SQL, template, or deserialization of untrusted input on the path. `canonicalize_path` is applied before persisting the pointer.

Non-blocking observation (not a defect): `resolve_target` runs `fs_err::create_dir_all` on the positional path before the lock is taken. Creating an empty directory is benign and idempotent, crosses no trust boundary, and is not a mutation of any user-owned file, so it does not warrant the lock-first ordering the manifest write requires.
</review>

<review persona="style" verdict="pass" model="claude-sonnet-4-6[1m]/medium" date="2026-05-30T09:45:00Z" round="1">
All changed files (`patina-cli/src/cli.rs`, `patina-cli/src/cmd/init.rs`, `patina-cli/src/cmd/mod.rs`, `patina-cli/src/main.rs`, `patina-cli/tests/common/mod.rs`, `patina-cli/tests/init_cli.rs`) are consistent with project conventions.

- Import ordering matches sibling commands (crate-local uses, then `anyhow`, then `patina_core`, alphabetical within each group).
- The `#[expect(clippy::unused_async, reason = "...")]` annotation carries a justification and matches the `patina-core/src/lib.rs` pattern for synchronous async stubs.
- `unwrap_or_else(|_| "{}".to_owned())` on `serde_json::to_string_pretty` matches the pattern used in `apply.rs`, `rollback.rs`, and `status.rs`.
- `BufferReporter` is imported locally inside each `#[cfg(test)]` function, consistent with `rollback.rs` and `status.rs`.
- `#![expect(clippy::expect_used, reason = "...")]` in `init_cli.rs` matches the pattern in `rollback_cli.rs`.
- `Fixture::apply_with_env` correctly delegates to the new `Fixture::run`, preserving the existing call-site contract.
- Unit tests gate real invariants: format interpolation, schema determinism, error routing to stdout vs stderr, timestamp parseability.
- No dead imports, no bare suppressions, no panics, no `std::fs` or `std::path::Path` where project conventions require `fs-err`/`camino`.

Non-blocking: the inline JSON build in `refuse_existing` is slightly inconsistent with a single named `json_envelope` helper, but two distinct success/error shapes make a one-off acceptable and readable.
</review>

<review persona="correctness" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-05-30T09:45:00Z" round="1">
No correctness defects found in T-003 (`patina init`). Control flow is sound: the pre-lock manifest check is an optimization returning `Ok(1)`, and the authoritative re-check under the exclusive lock (`patina-cli/src/cmd/init.rs:81`) correctly closes the concurrent-init TOCTOU window. The lock guard binds to `_guard` (not `_`), so it is held for the full mutating body rather than dropped immediately; both refusal paths return before lock acquisition, so no cleanup is skipped on early return. Exit-code mapping is correct on every path: refusal → `Ok(1)` → exit 1; lock timeout → `EngineError::Lock(Timeout)` preserved through `.context()` in the `anyhow` chain so `from_error_chain` maps it to exit 4; other engine errors → `_ => Generic` (exit 1), with no double-printing on the `Ok(1)` paths. `canonicalize_path(&target)` runs after the manifest write creates the directory, so it takes the existing-path branch and matches the test's independent `fs_err::canonicalize` (macOS `/var`→`/private/var` symlink resolution). Write/read pointer format is consistent (`write_persisted_default` emits `{repo}\n`; `read_persisted_default` trims). All 4 integration tests and 5 unit tests pass.
</review>
