---
spec: SPEC-0005
task: T-004
generated_at: 2026-06-02T19:41:07Z
---

<implementer date="2026-06-02T19:41:07Z" model="claude-opus-4-8[1m]" round="1">
- Reuse survey: Mapped the task area (covered REQ-001 / REQ-002,
  suggested files `patina-core/src/apply/engine.rs` and
  `patina-core/src/apply/mod.rs`, and their immediate neighbours
  `apply/classify.rs`, `apply/template.rs`, `journal/plan.rs`,
  `status/classify.rs`). Decisions:
  reuse-as-is — `crate::apply::classify::classify_leaf` (the T-003
  per-leaf classifier) for every leaf comparison; `crate::apply::walk_files`
  for tree-leaf enumeration (the same sorted walk the `copy_tree` /
  `tree_symlink` executors use); `Engine::render` (`template/mod.rs:145`)
  to render a template target's comparison bytes once at plan time;
  `PlannedOperation::{symlink,render,copy}` and
  `PlannedOperation::disposition()` (T-002's constructors / accessor).
  Extend — `ResolvedOperation` grows a `dispositions: Vec<TargetDisposition>`
  carrier; `ResolvedEntry` grows a parallel `dispositions` field;
  `planned_operation` and `resolve_entry` take the classification inputs;
  `assemble_plan_operations` threads the aggregate onto the durable op.
  Added `EngineError::Classify` to the existing transparent-subsystem-error
  enum rather than a parallel error path. Write-fresh — three new top-level
  symbols: `TargetDisposition` and `LeafDisposition` (the in-memory carriers
  DEC-007 requires; searched the crate for an existing per-target/per-leaf
  disposition carrier and found none, only the wire-level `Disposition` enum)
  and `classify_entry` / `classify_target` (the entry-level classifier that
  renders-once and folds tree leaves into the DEC-007 aggregate; grepped
  `apply` for an existing entry-level classify seam and found only the
  per-leaf `classify_leaf` and the status managed-set walk, neither of which
  produces per-entry dispositions).
- Completed: Wired plan-time classification end-to-end. `resolve_entry`
  (`engine.rs`) now takes `engine` / `resolver` and, after resolving the
  source and targets, calls the new `classify_entry`, which renders a
  template source once and routes each declared target through
  `classify_target`. Single-target modes classify the target directly into a
  `TargetDisposition { aggregate, leaves: [] }`; tree modes (`copy-tree`,
  `symlink-tree`) short-circuit an absent target directory to a `Create`
  aggregate, otherwise walk the source leaves with `walk_files`, classify each
  leaf at its mirrored target path via `classify_leaf`, carry the per-leaf
  `LeafDisposition`s, and fold them into the DEC-007 aggregate (`Unchanged`
  iff every leaf is `Unchanged`, else `Update`). `ResolvedEntry` and
  `ResolvedOperation` grew the per-target `dispositions` carrier;
  `assemble_plan_operations` zips `targets` with `dispositions` and passes the
  aggregate to `planned_operation`, so the durable `PlannedOperation` carries
  the real disposition (REQ-002) and the in-memory `ResolvedOperation` carries
  the full per-target/per-leaf classification for T-005's write-skip and
  T-009/T-010's reporting (REQ-001). Promoted `classify_leaf`'s `ClassifyError`
  to `pub`, re-exported it from `apply` (`pub use classify::ClassifyError`),
  and added `EngineError::Classify(#[from] ...)`. Removed the now-obsolete
  `dead_code` `expect` on `classify_leaf`. Updated the two test fixtures that
  build these types (`engine.rs` `resolved_entry` helper, `windows/mod.rs`
  `op` helper) to populate aligned placeholder dispositions. Added five
  tempdir/synthetic unit tests in `engine.rs` covering CHK-001/002/003, the
  absent-tree-target `Create` aggregate, the all-clean-tree `Unchanged`
  aggregate, and the durable-plan threading.
- Undone: The commit-record per-leaf disposition is still seeded with
  `PLACEHOLDER_DISPOSITION` in `build_apply_record` — that is REQ-002's
  commit-record arm and is explicitly T-005's scope (record real per-leaf
  dispositions when reworking `build_apply_record`), so the placeholder
  constant is intentionally retained for that one remaining site. The execute
  write-skip that consumes `ResolvedOperation::dispositions` is T-005; the
  diff / `--json` reporting that consume the per-leaf dispositions are
  T-009 / T-010. Nothing in T-004's own scope is deferred.
- Hygiene checks: `just lint` exited 0 — nightly `fmt --all --check` (exit 0),
  `clippy --workspace --all-targets --all-features --locked` on both
  `x86_64-unknown-linux-gnu` and `x86_64-pc-windows-gnu` cross targets
  (exit 0, the macOS target is skipped off a Mac per the justfile), docs with
  `RUSTDOCFLAGS="-D warnings" cargo doc` (exit 0), and `cargo deny check`
  (advisories ok, bans ok, licenses ok, sources ok — the duplicate-crate /
  unmatched-license-allowance warnings are pre-existing transitive noise, no
  new dependency added). `just test` exited 0 (workspace + doctests all green,
  including the five new T-004 cases). Per AGENTS.md the project's gate is
  `just check` (= `just lint` + `just test`), run here instead of ad-hoc
  cargo invocations so the docs gate is not dropped.
- Evidence: red-then-green paper trail at
  `.speccy/specs/0005-patina-skip-if-satisfied/evidence/T-004.md`.
  Roll call for the CHKs under T-004's covered REQs (REQ-001 / REQ-002):
  - CHK-001 (satisfied symlink/copy/template all classify Unchanged):
    demonstrated → evidence Scenario 1 covers the pre-edit placeholder-Create
    baseline versus the post-edit `Unchanged` classification.
  - CHK-002 (mutated copy → Update, deleted symlink → Create, template stays
    Unchanged): demonstrated → evidence Scenario 2.
  - CHK-003 (copy-tree one drifted leaf → that leaf Update, others Unchanged,
    aggregate Update): demonstrated → evidence Scenario 3 (with the absent-tree
    `Create` and all-clean `Unchanged` aggregate edge cases proven alongside).
  REQ-002's CHK-004 / CHK-005 (plan and apply-record encode/decode round-trip)
  were demonstrated under T-002, which added and round-tripped the wire field;
  T-004's REQ-002 contribution is threading the *real* classified disposition
  onto the durable `PlannedOperation`, covered as demonstrated by evidence
  Scenario 4 (`assemble_plan_threads_disposition_onto_durable_operation`). No
  CHK in T-004's scope is `judgment-only`; classification is fully scriptable.
- Discovered issues: None. The `PLACEHOLDER_DISPOSITION` constant remains in
  use at exactly one site (`build_apply_record`) by design until T-005; its
  doc comment was updated to say so rather than leaving the stale T-002 wording
  that implied T-004 would remove it entirely.
- Procedural compliance: This implementer entry lands directly in
  `journal/T-004.md` per the journal-file schema; no `<implementer-note>` was
  written into TASKS.md. The TASKS.md `state="..."` attribute for T-004 flips
  from `in-progress` to `in-review` as the final step of this turn. No shipped
  skill bodies under `.claude/skills/` required edits during this task.
</implementer>

<review date="2026-06-02T15:45:40-04:00" model="claude-opus-4-8[1m]/xhigh" persona="business" verdict="pass" round="1">
T-004 delivers exactly its scoped slice of REQ-001 and REQ-002. Plan-time
classification is wired end-to-end: `gate_and_resolve_entry` → `resolve_entry` →
`classify_entry`/`classify_target` (`patina-core/src/apply/engine.rs`) classifies
every surviving target against live filesystem state (DEC-006: live read, no
cache), the real per-op aggregate threads onto the durable `PlannedOperation` via
`planned_operation`/`assemble_plan_operations`, and the full per-target/per-leaf
`TargetDisposition`/`LeafDisposition` rides on the in-memory `ResolvedOperation`
for downstream tasks. REQ-001 done-when bullets all map (symlink/copy/template
Create/Update/Unchanged and the tree single-drifted-leaf case via CHK-001/002/003
tempdir tests), and "Unchanged coincides with status Clean / no third definition"
holds because T-004 reuses T-003's `classify_leaf`. REQ-002's in-memory and
durable-plan bullets are met and asserted by
`assemble_plan_threads_disposition_onto_durable_operation`. The one deferral —
`build_apply_record` still seeds `ExpectedTarget` with `PLACEHOLDER_DISPOSITION` —
is explicitly assigned to T-005 by the task body, DEC-007, and T-005's body. No
non-goal scope crept in; no open question silently resolved.
</review>

<review date="2026-06-02T15:45:40-04:00" model="claude-opus-4-8[1m]/high" persona="tests" verdict="pass" round="1">
T-004's plan-time classification tests genuinely exercise the real
`classify_entry`/`classify_target`/`classify_leaf` code path against real tempdir
filesystem state — no mocking of the system under test, no empty bodies, no
loose-outcome assertions. CHK-001 → `satisfied_symlink_copy_and_template_all_classify_unchanged`;
CHK-002 → `mutated_copy_is_update_and_deleted_symlink_is_create`; CHK-003 →
`copy_tree_with_one_drifted_leaf_aggregates_to_update` (asserts both the per-leaf
split and the durable aggregate `Update`), plus absent-tree-Create and all-clean
edge cases. CHK-004/CHK-005 round-trip are correctly attributed in the roll call
to T-002's committed tests; T-004's REQ-002 contribution is covered by
`assemble_plan_threads_disposition_onto_durable_operation`. Mutation check:
disabling the `all_unchanged = false` drift line made the tree-aggregate test fail
(`left: Unchanged, right: Update`), confirming non-vacuity; file restored. Evidence
file present and sound: roll call labels all five CHKs `demonstrated`, red phase is
a real `error[E0425]: cannot find function classify_entry`, green is real scoped
`cargo test` output, all six test names appear verbatim in the diff.
</review>

<review date="2026-06-02T15:45:40-04:00" model="claude-opus-4-8[1m]/medium" persona="security" verdict="pass" round="1">
No security issues in T-004. The change is a plan-time filesystem classifier with
no new trust boundary, network, auth, or secret-handling surface. `ClassifyError::Io`
reports the source path and underlying IO error but never file contents or hashes;
surfaced transparently via `EngineError::Classify`. Paths are not secret in a local
dotfile manager. Content equality reuses the existing blake3 `content_hash` through
the shared `content_matches`/`symlink_matches` seams — no new crypto primitive.
`classify_leaf` uses `symlink_metadata` (no link-following) and operates on
canonicalized sources joined with `walk_files` leaves; targets resolve by declared
location, so no new path-traversal vector. Template rendering at plan time reuses
the existing `Engine::render`/`Resolver` (MiniJinja strict-undefined); rendered
bytes are only hashed for comparison, never logged or written during planning. No
dependency added. The plan-vs-execute TOCTOU is inherent to the apply model,
explicitly accepted, and gates no authorization decision.
</review>

<review date="2026-06-02T15:45:40-04:00" model="claude-sonnet-4-6[1m]" persona="style" verdict="blocking" round="1">
`patina-core/src/lib.rs` is missing the re-export for `ClassifyError`.

Every error type that appears as an `EngineError` variant source is re-exported
from `lib.rs` so callers can name the type without digging into module internals
(`pub use apply::ExecutorError;`, `pub use apply::HookError;`,
`pub use backups::BackupError;`, ...). The new
`EngineError::Classify(#[from] crate::apply::ClassifyError)` variant added in
`patina-core/src/error.rs` follows the same `#[from]` pattern, but `ClassifyError`
is absent from `lib.rs`. It is only re-exported at `patina-core::apply::ClassifyError`
(via `apply/mod.rs`), not at the crate root `patina_core::ClassifyError` that every
other variant's inner type exposes. A caller pattern-matching on
`EngineError::Classify(err)` must reach into `patina_core::apply::ClassifyError`
rather than the flat `patina_core::ClassifyError`.

Fix: add `pub use apply::ClassifyError;` to `patina-core/src/lib.rs` alongside
`pub use apply::ExecutorError;` and `pub use apply::HookError;`.
</review>

<review date="2026-06-02T15:45:40-04:00" model="claude-opus-4-8[1m]/high" persona="correctness" verdict="pass" round="1">
T-004 plan-time disposition wiring is correct. The DEC-007 aggregate fold in
`classify_target` (`patina-core/src/apply/engine.rs`) checks absent-target → Create
first, then Unchanged-iff-all-leaves-unchanged else Update, matching the spec; tree
leaves reuse the executor's `walk_files` sorted enumeration classified at the
mirrored `target.join(relative)` path. `classify_entry` builds `dispositions`
strictly parallel to `targets`, so the `zip` in `assemble_plan_operations` cannot
silently truncate. Template comparison bytes are rendered through the same
incremental per-module `resolver`/`engine.render` the executor uses, so no spurious
Unchanged/Update arises from a render-context mismatch. Classification and render
failures propagate as typed `EngineError` variants during planning before any
mutation; `entry_index` uses `saturating_add`. No logic, boundary, Option/Result,
concurrency, or resource defect found at the ≥80 bar.
</review>

<blockers date="2026-06-02T15:45:40-04:00" round="1">
Re-export `ClassifyError` from the crate root before the next implementer pass.
- style (blocking): `patina-core/src/lib.rs` is missing `pub use apply::ClassifyError;`. The new `EngineError::Classify(#[from] crate::apply::ClassifyError)` variant breaks the established convention — every other `EngineError` variant's inner type (`ExecutorError`, `HookError`, `BackupError`, etc.) is re-exported at `patina_core::<Type>`, but `ClassifyError` is reachable only at `patina_core::apply::ClassifyError`. Add `pub use apply::ClassifyError;` to `lib.rs` alongside the sibling error re-exports.
</blockers>

<implementer date="2026-06-02T16:05:00-04:00" model="claude-opus-4-8[1m]/high" round="2">
- Reuse survey: Retry addressing a single non-reuse style blocker, so no
  full area-map re-survey is owed (round-1 mapped the area). Per-symbol
  floor: this round adds no new top-level symbol — only one `pub use`
  re-export line. The re-exported type, `ClassifyError`, is the existing
  T-003 type already re-exported at `apply::ClassifyError`
  (`apply/mod.rs:53`, `pub use classify::ClassifyError;`); this round
  surfaces it at the crate root alongside the sibling
  `pub use apply::ExecutorError;` / `pub use apply::HookError;`. Nothing
  written fresh; nothing extended beyond the re-export list.
- Completed: Addressed the round-1 style blocker. Added
  `pub use apply::ClassifyError;` to `patina-core/src/lib.rs` (alphabetically
  before `CompletionRecord`, matching the existing sorted re-export block),
  so `EngineError::Classify(#[from] crate::apply::ClassifyError)` now follows
  the crate convention — its inner type is reachable at the flat
  `patina_core::ClassifyError` like every other `EngineError` variant source
  (`ExecutorError`, `HookError`, `BackupError`). No other change this round;
  the round-1 implementation is otherwise unchanged.
- Undone: Nothing in T-004's scope is deferred. The single round-1 deferral
  (`build_apply_record` still seeds `PLACEHOLDER_DISPOSITION`) remains T-005
  scope as documented in round 1, DEC-007, and T-005's body.
- Hygiene checks: `just lint` exited 0 — nightly `fmt --all --check`, clippy
  (`--workspace --all-targets --all-features --locked`) cross-compiled to
  `x86_64-unknown-linux-gnu` and `x86_64-pc-windows-gnu` (macOS skipped off a
  Mac per the justfile), docs (`RUSTDOCFLAGS="-D warnings" cargo doc`), and
  `cargo deny check` (advisories/bans/licenses/sources all ok; the
  duplicate-crate warnings are pre-existing transitive noise, no new dep
  added). `just test` exited 0 (workspace + doctests green, including the five
  round-1 T-004 cases). Per AGENTS.md the gate is `just check`
  (= `just lint` + `just test`), run here rather than ad-hoc cargo so the
  docs gate is not dropped.
- Evidence: red-then-green paper trail at
  `.speccy/specs/0005-patina-skip-if-satisfied/evidence/T-004.md` (round-1,
  unchanged). The round-2 fix is a re-export visibility correction with no new
  behavioural surface, so it adds no scenario; its proof is the green `just
  lint` (the convention the style persona flagged) plus the unchanged
  round-1 test suite. CHK roll call for T-004's covered REQs is unchanged from
  round 1:
  - CHK-001 (satisfied symlink/copy/template all classify Unchanged):
    demonstrated → evidence Scenario 1.
  - CHK-002 (mutated copy → Update, deleted symlink → Create, template stays
    Unchanged): demonstrated → evidence Scenario 2.
  - CHK-003 (copy-tree one drifted leaf → that leaf Update, others Unchanged,
    aggregate Update): demonstrated → evidence Scenario 3.
  - CHK-004 / CHK-005 (plan and apply-record wire round-trip): demonstrated
    under T-002; T-004's REQ-002 contribution (real disposition threaded onto
    the durable `PlannedOperation`) demonstrated by evidence Scenario 4
    (`assemble_plan_threads_disposition_onto_durable_operation`).
  No CHK in T-004's scope is judgment-only.
- Discovered issues: None.
- Procedural compliance: This round-2 entry is appended after the round-1
  `<implementer>` and `<blockers round="1">` blocks; earlier blocks are
  unmodified. The TASKS.md `state` for T-004 flips `pending` →
  `in-progress` → `in-review` this turn. No `<implementer-note>` written into
  TASKS.md. No shipped skill bodies under `.claude/skills/` were edited.
</implementer>

<review date="2026-06-02T15:53:23-04:00" model="claude-opus-4-8[1m]/medium" persona="business" verdict="pass" round="2">
T-004 delivers exactly its scoped slice of REQ-001 and REQ-002, and round-2
cleanly addressed the sole round-1 blocker (the style re-export) with no
behavioral change. REQ-001 `<done-when>` all map: plan-time classification is
wired live end-to-end (`gate_and_resolve_entry` invoked from the plan path →
`resolve_entry` → `classify_entry`/`classify_target`), covering symlink/copy/
template Create/Update/Unchanged and the tree single-drifted-leaf case, asserted
by CHK-001/002/003 tempdir tests; "Unchanged coincides with status Clean, no
third definition" holds via the shared `symlink_matches`/`content_matches` seams
(DEC-006 live reads, no cache). REQ-002: the real per-op aggregate threads onto
the durable `PlannedOperation` (asserted by
`assemble_plan_threads_disposition_onto_durable_operation`) and the full
per-target/per-leaf carrier rides on the in-memory `ResolvedOperation`. The
single deferral — `build_apply_record` still seeds `PLACEHOLDER_DISPOSITION` —
is explicitly T-005's commit-record arm per the task body, DEC-007, and T-005's
body. No non-goal scope crept in; no open question silently decided.
</review>

<review date="2026-06-02T15:53:23-04:00" model="claude-opus-4-8[1m]/high" persona="tests" verdict="pass" round="2">
T-004's plan-time classification tests genuinely exercise the real
`classify_entry`/`classify_target`/`classify_leaf` paths against live tempdir
state — no mocking of the system under test, no loose-outcome assertions. CHK-001
→ `satisfied_symlink_copy_and_template_all_classify_unchanged`; CHK-002 →
`mutated_copy_is_update_and_deleted_symlink_is_create`; CHK-003 →
`copy_tree_with_one_drifted_leaf_aggregates_to_update` (asserts both per-leaf
split and durable aggregate `Update`), plus absent-tree-`Create` and all-clean
edge cases. The Unchanged⇔Clean tie is real (shared `status::classify` seams, no
second definition). CHK-004/CHK-005 correctly attributed to T-002's committed
wire round-trip tests; T-004's REQ-002 contribution is
`assemble_plan_threads_disposition_onto_durable_operation`, which drives real
`assemble_plan_operations` and would have been the placeholder `Create`. Mutation
checks: forcing the aggregate fold to always `Unchanged` failed the drifted-tree
test; forcing the copy content-match to always-true failed four tests across both
layers. Files restored; apply suite green (74 passed). Evidence sound: roll call
labels CHK-001/002/003 `demonstrated`, red is a real `error[E0425]`, green is
scoped per-test output, all six test names appear in the diff. The round-2 fix
(`pub use apply::ClassifyError;`) is a re-export visibility correction with no
behavioral surface, so correctly adds no scenario.
</review>

<review date="2026-06-02T15:53:23-04:00" model="claude-opus-4-8[1m]/high" persona="security" verdict="pass" round="2">
No security issues in T-004 round 2. The round-2 change is solely the
style-blocker fix: `pub use apply::ClassifyError;` at the crate root
(`patina-core/src/lib.rs:34`), promoting `ClassifyError` to `pub`, plus a
doc-comment touch-up. Widening that surface exposes only an error enum whose
single variant (`ClassifyError::Io`) carries a source path and underlying IO
error — never file contents, rendered bytes, or hashes; paths are not secret in a
local dotfile manager. No behavior change. The plan-time classifier remains
clean: blake3 `content_hash` reuse via the shared seams (no new crypto primitive);
`symlink_metadata` (no link-following) for existence; tree leaves enumerated via
`walk_files` under the declared source/target (no new traversal vector); template
bytes rendered once through the existing `Engine::render`/`Resolver` and only
hashed, never logged/written during planning; render/IO failures surface as
transparent typed `EngineError` variants before any mutation. No new dependency
(`cargo deny check` clean). The plan-vs-execute TOCTOU is inherent, accepted, and
gates no authorization decision.
</review>

<review date="2026-06-02T15:53:23-04:00" model="claude-sonnet-4-6[1m]/medium" persona="style" verdict="pass" round="2">
Round-1 blocker resolved: `pub use apply::ClassifyError;` is present in
`patina-core/src/lib.rs` at line 34, alphabetically ordered before
`CompletionRecord`, matching the flat crate-root re-export convention for all
`EngineError` variant inner types. No new style regressions in round 2: the T-003
`#[cfg_attr(not(test), expect(dead_code, ...))]` on `classify_leaf` is correctly
removed now that it is called from production; `pub use classify::ClassifyError;`
in `apply/mod.rs` is alphabetically placed; the `EngineError::Classify(#[from]
...)` variant carries a doc comment matching siblings; `TargetDisposition` and
`LeafDisposition` carry `#[non_exhaustive]` + `#[derive(Debug, Clone)]` matching
the `ResolvedOperation`/`ResolvedPlan` pattern; `classify_entry`/`classify_target`
are private free functions matching neighbors; the `PLACEHOLDER_DISPOSITION` doc
comment was updated to reflect the narrowed remaining scope. No new dependencies,
no unjustified suppressions, no reinvented helpers.
</review>

<review date="2026-06-02T15:53:23-04:00" model="claude-opus-4-8[1m]/medium" persona="correctness" verdict="pass" round="2">
T-004 round-2 is correct. The round-2 delta is a single inert re-export (`pub use
apply::ClassifyError;` in `patina-core/src/lib.rs:34`) with no control-flow
impact; it compiles clean under the docs/clippy gates a conflicting re-export
would have failed. The round-1 classification logic re-verified end-to-end: the
DEC-007 aggregate fold in `classify_target` short-circuits absent-target →
`Create`, then folds `Unchanged` iff every leaf is `Unchanged` else `Update`; an
absent source-leaf target correctly classifies `Create` (≠ `Unchanged`) and flips
the aggregate to `Update` on an existing tree dir. The empty-tree edge (zero
leaves, existing dir → `Unchanged`) is benign. `classify_entry` builds
`dispositions` strictly parallel to `targets`, so the `zip` in
`assemble_plan_operations` cannot truncate. The `SymlinkTree` per-leaf
`symlink_matches(leaf_target, leaf_source)` comparison matches what `tree_symlink`
actually materializes (same `walk_files` order). Classification/render failures
propagate as typed `EngineError` variants before any mutation. No logic, boundary,
Option/Result, concurrency, or resource defect found at the ≥80 bar.
</review>
