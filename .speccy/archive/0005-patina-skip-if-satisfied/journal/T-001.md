---
spec: SPEC-0005
task: T-001
generated_at: 2026-06-02T19:10:00Z
---

<implementer date="2026-06-02T19:10:00Z" model="claude-opus-4-8[1m]" round="1">
- Reuse survey: Mapped the task area (covered REQ-013, suggested files
  `patina-core/src/journal/{disposition.rs,mod.rs,plan.rs,record.rs}`,
  `patina-core/src/lib.rs`, `AGENTS.md`, and their immediate
  neighbours). Decisions: write-fresh — the `Disposition` enum and its
  `label` method; grepped the workspace for `enum Disposition` /
  `Disposition` / a Create-Update-Unchanged label mapping and found
  none (the `Disposition` word matches in `status/classify.rs`,
  `lock.rs`, etc. are coincidental `Status`/`Clean` text, not a
  reusable type). The `label()` shape follows the existing
  `enum -> &'static str` convention already on `LockKind::label`,
  `LifecycleResult::label` per `rust-let-types-carry-invariants.md`
  (single mapping site on the type). Reuse-as-is — the version-envelope
  reset reuses the existing `version_envelope::{encode_with_envelope,
  decode_envelope,read_envelope_version}` codec and the existing
  `FILE_MAJOR_VERSION` constant with no new helper; the new tests reuse
  the existing `sample()` / `record()` fixtures in `plan.rs` /
  `record.rs`. No new top-level symbol beyond the `Disposition` enum and
  its `label` method.
- Completed: Added `patina-core/src/journal/disposition.rs` with
  `Disposition { Create, Update, Unchanged }` deriving
  `Debug, Clone, Copy, PartialEq, Eq, Serialize, Deserialize`, plus a
  single `label(self) -> &'static str` mapping to `"create"` /
  `"update"` / `"unchanged"`. Declared `mod disposition;` and
  re-exported `pub use disposition::Disposition;` in
  `patina-core/src/journal/mod.rs`, and re-exported `Disposition` from
  `patina-core/src/lib.rs` next to `ExpectedTarget`. Reset
  `FILE_MAJOR_VERSION` from `2` to `1` in
  `patina-core/src/journal/plan.rs`. Updated the stale `record.rs`
  module doc that claimed the shared major "is bumped to 2" to reference
  `FILE_MAJOR_VERSION` and the pre-release no-bump policy. Recorded
  DEC-005 in `AGENTS.md` under Code conventions as an "On-disk format
  version (pre-release no-bump policy)" bullet. Added the CHK-017 /
  CHK-018 scenario tests to both `plan.rs` and `record.rs`
  (`envelope_major_byte_reads_as_one`,
  `major_two_buffer_is_refused_not_misdecoded`) plus an
  `on_disk_major_is_held_at_one` pin on the constant.
- Undone: Threading `disposition` onto `PlannedOperation` /
  `ExpectedTarget` and the round-trip-of-dispositions half of CHK-017
  is T-002's scope per its `<task>` element; T-001 lands the type and
  the version reset only. Nothing from T-001's own scope is deferred.
- Hygiene checks: `cargo test --workspace --locked` (via `just test`)
  exited 0 — all unit, integration, and doctests green including the
  four new tests and the constant pin. `cargo clippy --workspace
  --all-targets --all-features --locked -- -D warnings` (via
  `just lint`, host + cross `x86_64-unknown-linux-gnu` /
  `x86_64-pc-windows-gnu`) exited 0 with zero new warnings.
  `cargo +nightly fmt --all --check` exited 0. `cargo doc
  -D warnings` exited 0 (new doc examples on `Disposition` /
  `label` compile and run as doctests). `cargo deny check` exited 0
  (no new dependencies; the `wit-bindgen` duplicate is a pre-existing
  transitive warning, `bans ok`).
- Evidence: paper trail at
  `.speccy/specs/0005-patina-skip-if-satisfied/evidence/T-001.md`.
  Roll call for the CHKs under REQ-013:
  - CHK-017 (envelope major reads as 1; dispositions round-trip):
    demonstrated → evidence Scenario 1 covers the major-byte-reads-as-1
    half via `envelope_major_byte_reads_as_one` on both `Plan` and
    `ApplyRecord` and the `on_disk_major_is_held_at_one` pin. The
    "round-trips the dispositions" half is deferred to T-002, which
    threads the disposition fields onto the wire types and adds the
    round-trip-of-dispositions coverage.
  - CHK-018 (major-2 buffer is refused, not misdecoded): demonstrated →
    evidence Scenario 2 covers the red/green via
    `major_two_buffer_is_refused_not_misdecoded` on both `Plan` and
    `ApplyRecord`, asserting `VersionMismatch { found: 2, supported: 1 }`.
- Discovered issues: Resetting the supported major to `1` invalidated
  three tests that depended on the old `2`: `commit_record.rs`'s
  `commit_envelope_major_is_two` and the major assertion in
  `directory_and_file_entries_..._envelope_major_is_unchanged`
  hard-coded literal `2`; and two `assert_ne!(DRIFT_CACHE_MAJOR_VERSION,
  FILE_MAJOR_VERSION)` tests (in `drift_cache.rs` unit tests and
  `tests/drift_cache.rs`) silently relied on the journal major differing
  from the drift-cache major (`1`). Per the task's "update any test that
  hard-codes the literal 2" instruction and test-hygiene rule #2
  (no fragile constant-relationship tests): the `commit_record.rs`
  asserts now compare against the imported `FILE_MAJOR_VERSION` symbol
  (renamed `commit_envelope_major_matches_supported`), and the two
  `assert_ne!` lines were removed — each test's substantive invariant
  (the cache decodes against its own major regardless of the journal's)
  is fully proven by the surviving envelope-byte + decode assertions,
  with the now-unused `FILE_MAJOR_VERSION` imports dropped.
- Procedural compliance: This implementer entry lands directly in
  `journal/T-001.md` per the journal-file schema; no
  `<implementer-note>` was written into TASKS.md. The TASKS.md
  `state="..."` attribute for T-001 flips from `in-progress` to
  `in-review` as the final step of this turn. No shipped skill bodies
  under `.claude/skills/` were edited.
</implementer>

<review date="2026-06-02T14:57:00-04:00" model="claude-opus-4-8[1m]/high" persona="business" verdict="pass" round="1">
T-001 faithfully delivers its slice of REQ-013. The `Disposition { Create,
Update, Unchanged }` type with a single `label()` mapping site, the
`FILE_MAJOR_VERSION` reset 2→1, the major-2-buffer rejection tests on both
`Plan` and `ApplyRecord` (CHK-018, asserting `VersionMismatch { found: 2,
supported: 1 }`), the `1`-byte pin, and the DEC-005 pre-release no-bump policy
in `AGENTS.md` all map cleanly to the three task scenarios and the REQ-013
done-when items. The one business-risk item — the "decoding round-trips the
dispositions" half of CHK-017 being absent — is a sanctioned scope split, not a
dropped requirement: T-001's task body explicitly states the disposition fields
ride the wire in T-002, and T-002 `covers="REQ-013"` with the round-trip
coverage in its own scenarios. No non-goal was breached (no CLI flag, no
classification cache, no reap-policy change), no empty Open Question was
silently decided, and the consequential test updates are in-scope fallout of
the version reset that the task body anticipated.
</review>

<review date="2026-06-02T14:57:00-04:00" model="claude-opus-4-8[1m]/medium" persona="tests" verdict="pass" round="1">
Tests genuinely exercise the real code paths for both covered scenarios.
CHK-018 (`major_two_buffer_is_refused_not_misdecoded` in `plan.rs` and
`record.rs`) encodes a valid payload through the real envelope, flips the
leading u16 to 2, and asserts the specific `JournalError::VersionMismatch {
found: 2, supported: 1 }` variant — not "any error" — driving the real `decode`
→ `decode_envelope` path; reverting `FILE_MAJOR_VERSION` to 2 makes `2 > 2`
false and the test catches the resulting `Ok`. CHK-017's "major byte reads as
1" half is observed at the actual wire byte via `envelope_major_byte_reads_as_one`,
paired with the `on_disk_major_is_held_at_one` constant pin. The
`Disposition::label` test asserts each variant maps to a distinct word at the
single mapping site. CHK-017's round-trip half is legitimately deferred to T-002.
The `commit_record.rs` / `drift_cache.rs` test edits are correctly driven by the
major reset and align with test-hygiene rule #2 (literal-`2` asserts re-pointed
at the `FILE_MAJOR_VERSION` symbol; the now-degenerate `assert_ne!` guards
removed while behavioral decode-against-own-major assertions survive). Evidence
file present. Minor non-blocking note: the evidence green section cites the
full-suite `cargo test --workspace --locked` rather than a scoped per-test
invocation, but the named tests appear in the diff and the red reasoning is
concrete, so no fabrication pattern matches.
</review>

<review date="2026-06-02T14:57:00-04:00" model="claude-opus-4-8[1m]/medium" persona="security" verdict="pass" round="1">
No security concerns. T-001 introduces only a data-free `Disposition` enum
whose `label()` returns `&'static str` literals — no user input, paths, bytes,
secrets, logging, or crypto surface. The `FILE_MAJOR_VERSION` reset from 2 to 1
strengthens rather than weakens the format-confusion boundary: `decode_envelope`
refuses `found > supported` (`patina-core/src/version_envelope.rs:142-151`), so
a major-2 buffer is now correctly rejected with `VersionMismatch { found: 2,
supported: 1 }` rather than misdecoded, and CHK-018 tests pin this on both
`Plan` and `ApplyRecord`. No new dependencies (deny.toml unaffected). The
`postcard` variant-ordering hazard is correctly documented on the type.
</review>

<review date="2026-06-02T14:57:00-04:00" model="claude-sonnet-4-6[1m]" persona="style" verdict="pass" round="1">
The diff is style-clean across all changed files. `disposition.rs`: naming,
derives, `#[must_use]` on `label`, and doc examples follow project conventions;
the module-level doc cross-links `PlannedOperation` and `ExpectedTarget`
correctly; no dead code. The new `plan.rs` / `record.rs` tests gate real
behavior; `on_disk_major_is_held_at_one` is a policy pin for DEC-005, not a
constant-copy-of-itself anti-pattern. The replaced `assert_ne!` drift-cache
guards correctly pin the coupling behaviour rather than a coincidental value
difference. `commit_record.rs` assertions updated to use the
`FILE_MAJOR_VERSION` symbol; function renamed to
`commit_envelope_major_matches_supported`. The `AGENTS.md` DEC-005 bullet
accurately describes the implemented policy. Orphan `FILE_MAJOR_VERSION` imports
dropped where the `assert_ne!` was removed. Reuse survey present and verified.
</review>

<review date="2026-06-02T14:57:00-04:00" model="claude-opus-4-8[1m]/high" persona="correctness" verdict="pass" round="1">
No correctness defects found. The `FILE_MAJOR_VERSION` reset to 1 propagates
through `Plan`/`ApplyRecord` encode and decode via the symbol (no stray
literal-2 sites remain outside descriptive comments); `decode_envelope`'s
`found > supported` check short-circuits before body decode, so the CHK-018
major-2 buffers are refused with `VersionMismatch { found: 2, supported: 1 }`
rather than misdecoded. `Disposition::label` is an exhaustive three-arm match
with distinct words and no swapped/shared arm. The removed
`assert_ne!(DRIFT_CACHE_MAJOR_VERSION, FILE_MAJOR_VERSION)` lines were a genuine
fix — both constants are now 1, so the inequality would have failed at runtime;
the surviving decode-against-own-major assertions still cover the
format-independence invariant.
</review>
