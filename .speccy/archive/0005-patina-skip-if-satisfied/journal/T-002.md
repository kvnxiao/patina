---
spec: SPEC-0005
task: T-002
generated_at: 2026-06-02T20:05:00Z
---

<implementer date="2026-06-02T20:05:00Z" model="claude-opus-4-8[1m]" round="1">
- Reuse survey: Mapped the task area (covered REQ-002 / REQ-013;
  suggested files `patina-core/src/journal/plan.rs`,
  `patina-core/src/journal/record.rs`,
  `patina-core/src/apply/engine.rs`, `patina-core/src/journal/probe.rs`,
  `patina-core/src/rollback/mod.rs`; and their immediate neighbours
  that construct the two wire types). Decisions: reuse-as-is — the
  T-001 `Disposition` enum and its `Disposition::Create` placeholder
  (`patina-core/src/journal/disposition.rs`, re-exported at
  `patina_core::Disposition` and `crate::journal::Disposition`); the
  existing `PlannedOperation::{symlink,render,copy}` constructors and
  the `ExpectedTarget::{target,source,entry}` accessors, which I grew
  rather than replaced. Extend — added a `disposition` field to every
  `PlannedOperation` and `ExpectedTarget` variant; extended the three
  constructors to take a `disposition` parameter; threaded the
  placeholder through `planned_operation` (engine.rs) and
  `build_apply_record` (engine.rs). Write-fresh — a `disposition()`
  accessor on each of `PlannedOperation` and `ExpectedTarget`
  (grepped `rg 'fn disposition'` crate-wide first: no match, so the
  absence is auditable), and a single `PLACEHOLDER_DISPOSITION`
  const in engine.rs documenting the T-004/T-005 follow-up instead of
  scattering bare literals. New top-level symbols: the two
  `disposition()` accessors and the `PLACEHOLDER_DISPOSITION` const;
  each named above against the search/decision that justifies it.
- Completed: Added `disposition: Disposition` to all three
  `PlannedOperation` variants (`patina-core/src/journal/plan.rs`) and
  both `ExpectedTarget` variants
  (`patina-core/src/journal/record.rs`), with a `disposition()`
  accessor on each type. Extended the `symlink`/`render`/`copy`
  constructors to take the disposition. Threaded the placeholder
  `Disposition::Create` through `planned_operation` and the two
  `ExpectedTarget` pushes in `build_apply_record`
  (`patina-core/src/apply/engine.rs`), via a documented
  `PLACEHOLDER_DISPOSITION` const so the swap point for T-004/T-005 is
  one named site. Updated the `describe` match arms in
  `journal/render.rs` to `..` the new field, and every in-tree
  constructor/struct-literal/doctest that builds these types: the
  `plan.rs`/`record.rs` fixtures (now carrying mixed
  Create/Update/Unchanged dispositions per CHK-004/005), `probe.rs`,
  `recovery.rs`, `render.rs` and `journal/mod.rs` doctests,
  `rollback/mod.rs`, `status/classify.rs`,
  `watch/subscriptions.rs` (helpers + doc example), and the
  integration tests `journal_fsync_shape.rs`, `recovery_crash.rs`,
  `debug_journal_cli.rs`, plus the `debug.rs`/`promote.rs` test
  helpers. Added two round-trip tests:
  `per_op_dispositions_round_trip` (plan, CHK-004) and
  `per_leaf_dispositions_round_trip_at_major_one` (record, CHK-005 /
  CHK-017), and extended the `target_accessor` test to exercise the
  new `disposition()` accessor.
- Undone: No real classification — every disposition is the
  `Disposition::Create` placeholder, per the task's explicit
  field-only, behavior-neutral scope. T-003 builds the plan-time
  classifier; T-004 replaces the placeholder with real classification
  on the operation and durable plan; T-005 records real per-leaf
  dispositions in the commit and drives the write-skip. Recovery
  (`recovery.rs`) and rollback (`rollback/replay.rs`) still key on
  backup presence; the disposition-aware arms are T-007 / T-008.
- Hygiene checks: ran the project's canonical `just check` (=
  `just lint` + `just test`), which exited 0. It covers the four
  standard gates plus the docs gate: `cargo +nightly fmt --all
  --check` exited 0 (also confirmed directly: exit 0); clippy
  (`--workspace --all-targets --all-features --locked -- -D
  warnings`, cross-compiled to the linux and windows targets via
  `lint-clippy`) exited 0 after fixing one
  `redundant_closure_for_method_calls` finding (switched
  `.map(|op| op.disposition())` to `.map(PlannedOperation::disposition)`);
  the docs gate (`RUSTDOCFLAGS=-D warnings cargo doc`) exited 0;
  `cargo test --workspace --locked` (incl. doctests) exited 0; and
  `cargo deny check` exited 0 (confirmed directly: exit 0; no new
  dependencies — `Disposition` is first-party).
- Evidence: red-then-green paper trail at
  `.speccy/specs/0005-patina-skip-if-satisfied/evidence/T-002.md`.
  Roll call for the four CHKs under REQ-002 / REQ-013:
  - CHK-004 (Plan one Create/Update/Unchanged round-trips): demonstrated
    → evidence Scenario 1 covers the pre-edit "field does not exist"
    compile failure versus the post-edit
    `per_op_dispositions_round_trip` test asserting decoded
    dispositions equal `[Create, Update, Unchanged]`.
  - CHK-005 (ApplyRecord one Create/Update/Unchanged round-trips):
    demonstrated → evidence Scenario 2 covers the
    `per_leaf_dispositions_round_trip_at_major_one` test asserting the
    decoded record's per-target dispositions equal the originals.
  - CHK-017 (envelope major byte reads 1, dispositions round-trip):
    demonstrated → evidence Scenario 2 also asserts
    `read_envelope_version(&bytes) == 1` on the
    disposition-carrying record.
  - CHK-018 (major-2 buffer refused, not misdecoded): hygiene →
    covered unchanged by the pre-existing
    `major_two_buffer_is_refused_not_misdecoded` tests in `plan.rs`
    and `record.rs` (T-001), which run under `cargo test --workspace`;
    this task did not alter the envelope-refusal path and the tests
    stay green.
  No CHK in T-002's scope is `judgment-only`.
- Discovered issues: None new. The disposition placeholder is an
  intentional, task-scoped stand-in (not a defect) and is documented
  at its single `PLACEHOLDER_DISPOSITION` definition site for the
  next implementer (T-004/T-005).
- Procedural compliance: This entry lands directly in
  `journal/T-002.md` per the journal-file schema; no
  `<implementer-note>` was written into TASKS.md. The TASKS.md
  `state="..."` attribute for T-002 flips from `in-progress` to
  `in-review` as the final step of this turn. No shipped skill bodies
  under `.claude/skills/` required edits during this task.
</implementer>

<review date="2026-06-02T15:16:36-04:00" model="claude-opus-4-8[1m]/medium" persona="business" verdict="pass" round="1">
T-002 (REQ-002, REQ-013) delivers what the SPEC scopes for this field-only
slice. The `disposition` field is threaded onto both durable wire types —
`PlannedOperation` (`patina-core/src/journal/plan.rs`) and `ExpectedTarget`
(`patina-core/src/journal/record.rs`) — with accessors, and the two new
round-trip tests assert per-target dispositions directly via those accessors:
`per_op_dispositions_round_trip` (CHK-004) and
`per_leaf_dispositions_round_trip_at_major_one` (CHK-005/CHK-017, including
`read_envelope_version == 1`). Both pass; `cargo build --workspace
--all-targets` is clean, confirming every construction call site was threaded.
Scope is honestly bounded: the in-memory `ResolvedOperation` carrier and real
classification are correctly deferred to T-004 per DEC-007 (not silently
dropped); `engine.rs` uses only the documented `PLACEHOLDER_DISPOSITION` const
with no classification logic; recovery/rollback behavior arms and
`status::classify` are untouched (T-005/T-007/T-008 scope preserved). No
non-goal breached and no open question resolved.
</review>

<review date="2026-06-02T15:16:36-04:00" model="claude-opus-4-8[1m]/high" persona="tests" verdict="pass" round="1">
T-002's two task-scenarios are each backed by a real postcard encode/decode
round-trip test that would fail under a wrong implementation.
`per_op_dispositions_round_trip` (`patina-core/src/journal/plan.rs`, CHK-004)
and `per_leaf_dispositions_round_trip_at_major_one`
(`patina-core/src/journal/record.rs`, CHK-005/CHK-017) decode post-round-trip
bytes and assert the per-target disposition vector equals `[Create, Update,
Unchanged]`, with the latter also pinning `read_envelope_version == 1`;
`Disposition` derives genuine `PartialEq` across three distinct variants so the
assertions are not loose. CHK-018 is correctly labelled hygiene — the
`major_two_buffer_is_refused_not_misdecoded` tests in both files predate T-002,
are unchanged by this diff, and now exercise the refusal path against the
disposition-carrying fixtures. Evidence file present, roll call covers all four
CHKs with specific test cites, red-phase compile errors (E0061/E0063) match the
pre-edit types given the diff, and the evidence command is scoped per-test. No
fabrication pattern matched.
</review>

<review date="2026-06-02T15:16:36-04:00" model="claude-opus-4-8[1m]/medium" persona="security" verdict="pass" round="1">
No security issues. Field-only, behavior-neutral threading of a closed
3-variant `Disposition` enum onto the `PlannedOperation`/`ExpectedTarget` wire
types behind the unchanged version envelope (held at major 1; `decode_envelope`
still refuses `found > supported` at `patina-core/src/version_envelope.rs:144`,
covered by the `major_two_buffer_is_refused_not_misdecoded` tests in `plan.rs`
and `record.rs`). No new dependencies, no new external-input parsing, no
auth/authz/secret/crypto/logging surface. The `Disposition::Create` placeholder
does not yet drive any recovery/rollback destructive decision (those arms remain
backup-presence-keyed, deferred to T-007/T-008), so no disposition-driven
leave/skip/delete path is reachable from a corrupted journal byte in this slice.
</review>

<review date="2026-06-02T15:16:36-04:00" model="claude-sonnet-4-6[1m]" persona="style" verdict="pass" round="1">
T-002 is clean against the project's style conventions. Import additions are
alphabetically ordered and pass the nightly fmt gate. The new `disposition()`
accessor on both `PlannedOperation` and `ExpectedTarget` carries `#[must_use =
"..."]` with a justification string, consistent with existing accessors in the
same files. The `PLACEHOLDER_DISPOSITION` const is non-pub and carries a full
doc comment naming the T-004/T-005 handoff sites. The `..` wildcard in
`render.rs::describe` is the idiomatic way to ignore the new field. The two new
round-trip tests assert per-field values rather than relying solely on
`PartialEq` on the aggregate struct, satisfying the test-hygiene rule against
loose-outcome assertions. No dead imports or unused symbols introduced. Reuse
survey is present and credible.
</review>

<review date="2026-06-02T15:16:36-04:00" model="claude-opus-4-8[1m]/high" persona="correctness" verdict="pass" round="1">
No correctness defects found. The `disposition` field is threaded onto every
`PlannedOperation`/`ExpectedTarget` variant; both `disposition()` accessors
cover all variants via OR-patterns with no arm misrouting; `describe()` and
`planned_operation()` preserve their original variant→label and FileMode→op
mappings after reformatting. The `FILE_MAJOR_VERSION` 2→1 reset is consistent
across plan.rs and record.rs (shared const), and the CHK-018 major-2 injection
tests correctly assert `VersionMismatch { found: 2, supported: 1 }` against the
offset-zero little-endian envelope. Round-trip tests exercise all three
Create/Update/Unchanged variants and pass; the full workspace test compile
succeeds, confirming all construction sites updated. The uniform
`PLACEHOLDER_DISPOSITION = Create` is the task's intended behavior-neutral
stand-in (T-004/T-005 own real classification), not a defect.
</review>
