---
spec: SPEC-0005
task: T-010
generated_at: 2026-06-03T17:30:00Z
---

<implementer date="2026-06-03T17:30:00Z" model="claude-opus-4-8[1m]/high" round="1">
- Reuse survey: Mapped the task area (covered REQ-011 / REQ-012; suggested
  files `patina-cli/src/cmd/apply.rs`, `patina-cli/tests/skip_if_satisfied.rs`,
  `patina-cli/tests/deterministic_stdout.rs`; plus the disposition carriers
  `ResolvedOperation.dispositions` / `TargetDisposition` / `LeafDisposition`
  in `patina-core/src/apply/engine.rs` that T-004 populates, the
  `Disposition::label` method from T-001, and the sibling T-009 diff renderer
  `patina-cli/src/output/diff.rs` which already does per-leaf routing). `rg`
  for existing `apply --json` plan-shape assertions across `patina-cli/tests`
  (`"plan"`, `--json`) confirmed only `status --json` and the multi-target /
  byte-identical apply tests touch the apply envelope; none assert a
  per-tree-row shape, so expanding tree modes to per-leaf rows is non-breaking.
  Decisions:
  reuse-as-is — `Disposition::label()` (T-001's single enum→word mapping site,
  re-exported `Disposition`) for the `state` value, called via
  `disposition.aggregate.label()` / `leaf.disposition.label()`; the
  `op.dispositions` carrier (parallel to `op.targets`) read directly, the same
  data T-009's diff renderer consumes — no re-classification of the live
  filesystem in the CLI; `mode_label` (existing) for the row's `mode`; the
  `Fixture` harness in `patina-cli/tests/common/mod.rs` reused verbatim for both
  new tests. Extend — `json_envelope`'s plan-building closure was widened from
  `op.targets.iter().map(...)` to zip `op.targets` with `op.dispositions` and
  fan out per row via the new `plan_rows` helper; the per-leaf routing shape
  (single-target → one row keyed by aggregate; tree → one row per
  `LeafDisposition` keyed by `relative`) mirrors `diff.rs` `render` exactly so
  the two output surfaces agree on what an entry expands to. Write-fresh — one
  new top-level production fn, `plan_rows`
  (`patina-cli/src/cmd/apply.rs`): `rg 'fn plan_rows'` across the workspace
  returned no prior symbol; it is the apply-JSON analogue of `diff.rs`'s
  per-leaf branch, kept local to `apply.rs` rather than shared because the two
  emit different artifacts (JSON value vs rendered diff block) over the same
  routing. Two new test fns and one test helper (`plan_rows` / `state_for` in
  the test crate) are owed and recorded below; `rg` found no existing
  `apply --json` `state` test to extend.
- Completed: REQ-011 — every `--json` plan row now carries a `state` field
  equal to the target's `Disposition` label (`create` / `update` /
  `unchanged`), derived purely from the plan-time disposition via the T-001
  `label()` method (`patina-cli/src/cmd/apply.rs` `json_envelope` + new
  `plan_rows`). Tree modes (DEC-003 / DEC-007) list per-leaf rows, each keyed
  by the leaf path under the declared source/target and carrying that leaf's
  per-leaf `state`. REQ-012 — a fully-satisfied `apply --json` already flows
  through the same `json_envelope` (the no-op short-circuit is a filesystem
  property in the engine, not a CLI shape change), so it emits the standard
  `repo_root` / `profile` / `plan` / `result` envelope with every plan entry
  `state: "unchanged"` — the realization of "zero change counts" against an
  envelope that reports state per entry. Re-exported `TargetDisposition` from
  `patina-core/src/lib.rs` (next to `ResolvedOperation` / `ResolvedPlan`) so
  the CLI can name the `plan_rows` parameter — the type is genuinely part of
  the resolved-plan surface the CLI consumes.
- Undone: nothing in REQ-011 / REQ-012 scope. No production behaviour outside
  the JSON envelope was touched; the human diff (T-009), execute write-skip
  (T-005), and no-op short-circuit (T-006) are unchanged.
- Hygiene checks: `cargo +nightly fmt --all --check` exit 0; `cargo clippy
  --workspace --all-targets --all-features --locked -- -D warnings` exit 0 (run
  via `just lint` for both the `x86_64-unknown-linux-gnu` and
  `x86_64-pc-windows-gnu` cross targets; aarch64-apple-darwin skipped off a Mac
  per the justfile, CI's macos-latest leg is the backstop); `cargo doc`
  (`RUSTDOCFLAGS="-D warnings" --workspace --no-deps --all-features`, via
  `just lint`) exit 0; `cargo deny check` exit 0 (advisories ok, bans ok,
  licenses ok, sources ok — no new dependency). `cargo test --workspace
  --locked` (via `just test`) exit 0. Ran `just check` (= `just lint` +
  `just test`) as the canonical gate; all four gates exited 0.
- Evidence: paper trail at
  `.speccy/specs/0005-patina-skip-if-satisfied/evidence/T-010.md`.
  Roll call for CHKs under REQ-011 / REQ-012:
  - CHK-015 (a mixed plan's `--json` entries carry `state` create / update /
    unchanged matching their targets, second run byte-identical):
    demonstrated → evidence `### Scenario 1 — Mixed plan stamps create /
    update / unchanged per entry (CHK-015)` covers the red (drop the `state`
    field → `state_for` `.expect` fails) vs green (each row's `state` equals
    its classified disposition; two re-applies over the satisfied state are
    byte-identical) pair, backed by the test
    `json_plan_entries_carry_their_disposition_state`
    (`patina-cli/tests/skip_if_satisfied.rs`).
  - CHK-016 (a fully-satisfied `--json` apply emits the standard envelope,
    zero change counts, every entry `state: unchanged`): demonstrated →
    evidence `### Scenario 2 — Fully-satisfied no-op emits the standard
    all-unchanged envelope (CHK-016)` covers the red (drop the field →
    `Some("unchanged")` vs `None`) vs green (standard four-key envelope via the
    `plan_rows` test helper's shape assertion, non-empty plan, all entries
    `unchanged`) pair, backed by the test
    `fully_satisfied_json_emits_standard_envelope_all_unchanged`
    (`patina-cli/tests/skip_if_satisfied.rs`).
- Discovered issues: none in production. Two write-time clippy/docs bounces
  caught and fixed before the gate passed — the test helper `state_for` first
  used `.map(..).unwrap_or_else(|| panic!(..))`, which trips
  `clippy::map_unwrap_or` and `clippy::panic` (the `tests/*.rs` integration
  crate is production-lint scope; only the file-level `expect_used` allow
  covers the `tests/*.rs` helpers, not a bare `panic!`), reworked to end the
  Option chain in `.expect(..)`; and the `json_envelope` doc-comment intra-doc
  links to `Disposition` failed `cargo doc -D warnings` because `Disposition`
  is not imported in `apply.rs`, fixed by fully-qualifying the links
  (`[`Disposition`](patina_core::Disposition)`).
- Procedural compliance: flipped T-010 `pending` → `in-progress` → (this block)
  `in-review` in TASKS.md; no `SPEC.md` edit; no other task touched; journal is
  append-only (this is the sole round-1 block). The only `patina-core` change is
  the additive `pub use` of `TargetDisposition` (no behaviour change); no new
  dependency added, so `deny.toml` is untouched.
</implementer>

<review persona="business" verdict="pass" model="claude-opus-4-8[1m]/high" date="2026-06-03T05:26:12Z" round="1">
T-010 faithfully delivers REQ-011 and REQ-012. REQ-011: every `--json` plan row carries a `state` field equal to the target's plan-time `Disposition` label (`create`/`update`/`unchanged`) via the reused T-001 `label()` method; tree modes fan out per-leaf rows matching the T-009 diff renderer's routing (DEC-003/DEC-007). REQ-012: the fully-satisfied no-op flows through the same `json_envelope` and emits the standard four-key envelope (`repo_root`/`profile`/`plan`/`result` = `"applied"`, not a special-cased shape per DEC-004) with every plan entry `unchanged`. "Zero change counts" is correctly realized through all-`unchanged` entries since the envelope carries no separate numeric counter. Both new tests (`patina-cli/tests/skip_if_satisfied.rs:631`, `:714`) shell out to the real CLI and assert the observable contract including the byte-identical re-run over satisfied state (REQ-011 done-when reading of CHK-015's "second run is byte-identical"); both pass. No non-goal scope crept in (no force-rewrite flag, no cache reuse). No open questions silently resolved (SPEC has none open). Relevant files: `patina-cli/src/cmd/apply.rs`, `patina-cli/tests/skip_if_satisfied.rs`, `patina-core/src/lib.rs`.
</review>

<review persona="tests" verdict="pass" model="claude-opus-4-8[1m]/high" date="2026-06-03T05:26:12Z" round="1">
T-010 tests genuinely exercise the covered scenarios against the real binary. `patina-cli/tests/skip_if_satisfied.rs:json_plan_entries_carry_their_disposition_state` (CHK-015) pre-stages a fixture that classifies one Create / one Update / one Unchanged target, runs `apply --json` as a subprocess, and asserts each plan entry's `state` equals its real plan-time disposition label (sourced from `disposition.aggregate.label()` → `Disposition::label`, the canonical mapping), then checks two converged re-runs are byte-identical for REQ-012. `fully_satisfied_json_emits_standard_envelope_all_unchanged` (CHK-016) converges the repo and asserts the standard four-key envelope plus every entry `unchanged`; "zero change counts" is faithfully realized as all-entries-`unchanged` since `json_envelope` has no separate counter field. Both drive the production `json_envelope`/`plan_rows` path with no mocking of the system under test, and the `state_for(...).expect(...)` helper fails loudly on a missing/mis-keyed `state`, so a wrong implementation is caught. Evidence file present at the named path; red (`FAILED. 0 passed; 2 failed` + `state_for` panic) and green (`ok. 2 passed`) are materially different; cited test names and the `state_for` symbol all appear in the diff; scoped per-test commands, not the full hygiene suite. Roll call covers both CHKs under REQ-011/REQ-012, both `demonstrated` with specific test cites.
</review>

<review persona="security" verdict="pass" model="claude-opus-4-8[1m]/high" date="2026-06-03T05:26:12Z" round="1">
No security concerns. T-010's in-scope WIP adds a `state` field to each `apply --json` plan row, sourced purely from the plan-time `Disposition::label()` (a closed enum of the static strings `create`/`update`/`unchanged`) and re-exports `TargetDisposition` from `patina-core`. No new sensitive surface: the only added values are the three static state words and, in tree mode, `leaf.relative` joined onto the already-emitted source/target — repo-relative paths the user declared themselves and which the human diff (T-009) already exposes. All output goes through `serde_json::json!`, which escapes correctly, so there is no injection vector; values come from infallible `Utf8Path::as_str()`. No auth/authz boundary, secret handling, crypto, or authorization race is touched; classification reads existing plan-time carriers (no new filesystem read, no added TOCTOU window). No new dependency (`deny.toml` untouched per journal and diff). Production code introduces no `unwrap`/`panic`; the `panic!`/`expect` usages are confined to `#[cfg(test)]`. Reviewed `patina-cli/src/cmd/apply.rs` (`json_envelope`/`plan_rows`) and `patina-core/src/lib.rs`.
</review>

<review persona="style" verdict="pass" model="claude-sonnet-4-6[1m]/medium" date="2026-06-03T05:26:12Z" round="1">
All changed code is clean against project conventions. No `unwrap()` introduced in production (the pre-existing `unwrap_or_else` at line 316 of `apply.rs` predates this task). The new private `plan_rows` production fn is documented; `TargetDisposition` re-export is additive and used. Test helpers `plan_rows` and `state_for` in `skip_if_satisfied.rs` differ meaningfully in signature from the similarly-named helpers in `commit_record.rs`, `status_cli.rs`, and `symlink_tree_orphans.rs` (different JSON shapes, different parameter types), so no reuse drift. The `!rows.is_empty()` guard is non-vacuous: it prevents the `for row in &rows` loop from passing vacuously on an empty plan. No dead imports, no naming drift, and no suppression annotations introduced.
</review>

<review persona="correctness" verdict="pass" model="claude-opus-4-8[1m]/high" date="2026-06-03T05:26:12Z" round="1">
No correctness defects found in T-010. The new `json_envelope` zip over `op.targets`/`op.dispositions` is safe — both vectors are constructed one-per-target in `classify_entry` and moved onto `ResolvedOperation` in lockstep (`patina-core/src/apply/engine.rs`), so the `.zip()` truncates nothing. `plan_rows` (`patina-cli/src/cmd/apply.rs`) routes single-target vs tree modes on the same `leaves.is_empty()` predicate the T-009 diff renderer uses, including the tree-Create empty-leaves case, so the two output surfaces agree on entry expansion. Determinism holds: leaf order is `walk_files`-sorted, operations iterate in plan order, and the `state` field is a pure function of the plan-time disposition with no timestamps/PIDs/absolute paths. The fully-satisfied no-op still flows through `json_envelope` from the in-memory `ResolvedPlan` (the short-circuit is engine-side), emitting the standard four-key envelope with every entry `unchanged` per REQ-012.
</review>
