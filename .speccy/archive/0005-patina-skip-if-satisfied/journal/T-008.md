---
spec: SPEC-0005
task: T-008
generated_at: 2026-06-03T00:00:00Z
---

<implementer date="2026-06-03T00:00:00Z" model="claude-opus-4-8[1m]/high" round="1">
- Reuse survey: Mapped the task area (covered REQ-006; suggested files
  `patina-core/src/rollback/replay.rs`, `patina-core/src/rollback/mod.rs`,
  `patina-cli/tests/skip_if_satisfied.rs`; plus the `Disposition` enum and
  the `ExpectedTarget::disposition()` accessor in
  `patina-core/src/journal/{disposition.rs,record.rs}`, and the parallel
  T-007 `reverse_operation` early-arm in
  `patina-core/src/journal/recovery.rs`). Decisions:
  reuse-as-is — `Disposition` / `Disposition::Unchanged` and
  `ExpectedTarget::{target,entry,disposition}()` accessors threaded through
  unchanged; `mirror_backup_path` and the `crate::fsx` restore/delete
  helpers in `revert_target` untouched (tree-leaf Update still restores from
  the whole-tree backup at the leaf mirror path, DEC-007); the existing
  snapshot/roll-forward atomic region reused verbatim for the Create/Update
  targets. Extend — `EntryGroup.targets` grew from `Vec<&str>` to
  `Vec<RevertTarget>`; `group_by_entry` now pairs each target with its
  recorded disposition; `replay_entry`'s signature changed from `&[&str]`
  to `&[RevertTarget<'_>]`. Pattern reuse — the early "leave Unchanged" arm
  mirrors T-007's `reverse_operation` guard, implemented here at the
  `replay_entry` level (Unchanged targets are filtered out of the
  snapshot/revert set entirely so `revert_target` never sees them).
  Write-fresh — `RevertTarget<'a>` (a target-path + disposition pair). Grep
  before writing: `rg 'struct RevertTarget'` and `rg 'fn .*revert'` across
  `patina-core` returned no existing pairing type or helper, so the new
  struct is owed and recorded here; it is `pub` and re-exported from the
  `rollback` module beside `replay_entry` since the integration test
  `tests/rollback_atomic.rs` constructs it.
- Completed: REQ-006 — rollback now leaves commit-recorded `Unchanged`
  targets in place. `replay_entry` (`patina-core/src/rollback/replay.rs`)
  takes `&[RevertTarget<'_>]` and filters out `Disposition::Unchanged`
  targets before snapshotting, returning `Ok(())` early for an
  all-Unchanged entry; `Create` (no backup → delete) and `Update`
  (backup → restore) flow through `revert_target` exactly as before.
  `group_by_entry` / `EntryGroup` (`patina-core/src/rollback/mod.rs`)
  thread each target's `ExpectedTarget::disposition()` to `replay_entry`.
  `RevertTarget` is re-exported via `pub use replay::RevertTarget`.
- Undone: nothing in REQ-006 scope. The diff/`--json` reporting (REQ-010/
  REQ-011/REQ-012) is T-009/T-010, untouched here.
- Hygiene checks: `cargo test --workspace --locked` exit 0;
  `cargo clippy --workspace --all-targets --all-features --locked -- -D warnings`
  (via `just lint`) exit 0; `cargo +nightly fmt --all --check` (via
  `just lint`) exit 0; `cargo deny check` (via `just lint`) exit 0
  (`advisories ok, bans ok, licenses ok, sources ok`). Ran `just lint` and
  `just test` as the canonical gate; both exited 0.
- Evidence: roll call for CHKs under REQ-006:
  - CHK-010 (apply → rollback over a mixed Create/Update/Unchanged commit
    leaves Unchanged byte-identical, deletes Create, restores Update to
    pre-apply bytes): demonstrated → e2e test
    `rollback_leaves_unchanged_deletes_create_and_restores_update` in
    `patina-cli/tests/skip_if_satisfied.rs` drives a real `patina apply
    --yes` then `patina rollback --yes`, pre-staging the live filesystem so
    the single commit records one of each disposition, and asserts all
    three post-rollback outcomes. Reinforced by `patina-core` unit tests in
    `rollback::replay::tests`: `unchanged_target_is_left_in_place` (an
    Unchanged target with no backup is not deleted) and
    `mixed_entry_reverts_create_and_update_but_leaves_unchanged` (one entry,
    all three dispositions). The pre-existing
    `fresh_creation_is_deleted` / `overwrite_is_restored_from_backup` /
    `multi_target_entry_reverts_every_target` and the
    `tests/rollback_atomic.rs` atomicity tests gate the unchanged Create/
    Update behaviour through the new signature.
- Discovered issues: the e2e test initially passed a no-op because a
  non-interactive `patina rollback` without `--yes` only previews and
  performs no mutation (`patina-cli/src/cmd/rollback.rs`, the
  `(false, Tty::NonInteractive)` arm). Fixed by invoking
  `rollback --yes`; documented inline in the test so the requirement is
  explicit. No production-code issues found.
- Procedural compliance: flipped T-008 `pending` → `in-progress` → (this
  block) `in-review` in TASKS.md; no `SPEC.md` edit; no other task touched;
  journal is append-only (this is the sole round-1 block).
</implementer>

<review persona="business" verdict="pass" model="claude-opus-4-8[1m]/high" date="2026-06-03T04:50:41Z" round="1">
T-008 faithfully delivers REQ-006's observable contract. `patina rollback` now reads each commit-recorded per-leaf disposition (`ExpectedTarget::disposition()`) through `group_by_entry` → `replay_entry` and leaves `Unchanged` targets in place while deleting `Create` and restoring `Update` from backup. All three `<done-when>` clauses and the `<behavior>` Given/When/Then are covered: the e2e `rollback_leaves_unchanged_deletes_create_and_restores_update` (`patina-cli/tests/skip_if_satisfied.rs`) drives a real mixed `apply --yes` → `rollback --yes` and asserts Unchanged byte-identical, Create absent, Update restored to pre-apply bytes (CHK-010); reinforced by unit tests `unchanged_target_is_left_in_place` and `mixed_entry_reverts_create_and_update_but_leaves_unchanged` (`patina-core/src/rollback/replay.rs`). All five replay tests + two rollback_atomic tests pass locally.

This advances the SPEC `<goals>` rollback-fidelity commitment ("a target the apply did not touch is never deleted or restored on the way back") and serves the cautious-user story. No scope sneak: the diff is confined to the rollback path and its tests; no T-009/T-010 reporting leaked in, no non-goal (force-rewrite flag, cache, reap-policy change) touched, and the SPEC's "Open Questions: None" was not silently re-decided.

Non-blocking observation (out of business scope, flagged for the style/tests personas if they care): REQ-006 and the task body phrase the third arm as living *inside* `revert_target`, but the implementer placed the Unchanged guard one level up in `replay_entry` (filtering Unchanged out before the snapshot/roll-forward set). The observable behavior is identical — arguably stronger, since Unchanged targets cannot be touched even by a partial-failure roll-forward — and the choice is documented in the T-008 journal reuse survey. The business contract (Unchanged left, Create deleted, Update restored) is fully met.
</review>

<review persona="tests" verdict="blocking" model="claude-opus-4-8[1m]/high" date="2026-06-03T04:50:41Z" round="1">
Missing evidence file. The T-008 journal `<implementer>` block's `Evidence:` field names no per-task evidence file path and no file exists at `.speccy/specs/0005-patina-skip-if-satisfied/evidence/T-008.md`. Every sibling task in this spec (T-001 through T-007) produced a red-then-green paper-trail file at `.speccy/specs/0005-patina-skip-if-satisfied/evidence/T-NNN.md` whose `### Scenario N` blocks back its journal's `demonstrated` labels; T-008 instead inlines a roll call directly in the journal and points to no file. Per the evidence-loading contract (steps 1-3), absence of the referenced evidence file is a blocking condition — the red-then-green file is the reviewer's primary input alongside the diff, and CHK-010 is labelled `demonstrated`, which must be backed by exactly one `### Scenario N` block in that file. Resolution: write `.speccy/specs/0005-patina-skip-if-satisfied/evidence/T-008.md` capturing the red baseline (pre-change `replay_entry`/`revert_target`: a no-backup Unchanged target hits the no-backup→delete arm at `patina-core/src/rollback/replay.rs:203-206` and is deleted) versus the green result for the CHK-010 scenario, and point the journal `Evidence:` field at that path.

Note for the implementer (not a separate blocker): the implementation and tests themselves are substantively sound and exercise real code paths — `replay_entry` filters `Disposition::Unchanged` out of the revert set ahead of the no-backup→delete branch; `unchanged_target_is_left_in_place` would go red if that filter were removed; `mixed_entry_reverts_create_and_update_but_leaves_unchanged` guards that a Create-with-no-backup is still deleted (the over-broad-filter regression); and `rollback_leaves_unchanged_deletes_create_and_restores_update` in `patina-cli/tests/skip_if_satisfied.rs` drives a real `apply --yes`/`rollback --yes` and asserts all three CHK-010 outcomes. The only thing standing between this task and a pass is the absent evidence file.
</review>

<review persona="security" verdict="pass" model="claude-opus-4-8[1m]/high" date="2026-06-03T04:50:41Z" round="1">
No security issues in the T-008 rollback slice. The new `disposition` field is a mandatory postcard enum (fail-closed: malformed/legacy records are rejected by the version envelope rather than silently decoding as `Unchanged`), so a `Create`/`Update` target can never be mis-skipped from rollback. The change only narrows the atomic revert set (excluding no-backup `Unchanged` targets so they are left in place instead of deleted) — no new path-traversal, injection, deletion, secret-handling, auth, or TOCTOU surface. Verified by `patina-core/src/rollback/replay.rs` (`unchanged_target_is_left_in_place`, `mixed_entry_reverts_create_and_update_but_leaves_unchanged`), `patina-core/src/journal/record.rs` (`major_two_buffer_is_refused_not_misdecoded`), and the e2e `rollback_leaves_unchanged_deletes_create_and_restores_update` in `patina-cli/tests/skip_if_satisfied.rs`; all pass against the dirty working tree.
</review>

<review persona="style" verdict="blocking" model="claude-sonnet-4-6[1m]/medium" date="2026-06-03T04:50:41Z" round="1">
Two module-level doc comments describe the rollback inverse-operation rule as having exactly two branches, but the new `Unchanged`-disposition arm makes it three.

`patina-core/src/rollback/mod.rs` lines 10-23 ("Inverse-operation rule" section): states "driven by backup *presence*" and enumerates only backup-present → restore and no-backup → delete. It also claims this "mirrors crash recovery" — but crash recovery was already updated in T-007 to enumerate a third branch (Unchanged → leave in place), so the claim is now doubly wrong. The `Unchanged` disposition arm introduced in this task is entirely absent from this section.

`patina-core/src/rollback/replay.rs` lines 4-7 (module-level preamble): says "a target with a backup is restored from it … a target with no backup is deleted (the apply created it fresh)." The `Unchanged` case — filtered out of the snapshot/roll-forward region before either branch is reached — is not mentioned.

The `replay_entry` function doc at lines 38-60 was correctly updated to describe all three outcomes, but the file-level and module-level preambles that introduce the same algorithm were not. This is the identical doc-drift pattern that blocked T-007 in round 1 (`recovery.rs` module doc omitted the new arm). CLAUDE.md's hard rule: "Never let docs drift. If your change alters observable behavior … the corresponding docs change in the same PR."

Fix: update the "Inverse-operation rule" section in `mod.rs` and the module preamble in `replay.rs` to enumerate all three reversal outcomes in evaluation order — disposition == `Unchanged` (leave in place, do not consult backup); backup present → restore; no backup → delete.

No other findings: `RevertTarget` struct and field docs are accurate; the `group_by_entry` and `EntryGroup` doc comments are accurate; import ordering is clean; the two new unit tests (`unchanged_target_is_left_in_place`, `mixed_entry_reverts_create_and_update_but_leaves_unchanged`) gate real invariants; the `create`/`update` test helper functions are correct, unsuppressed, and duplicate-free.
</review>

<review persona="correctness" verdict="pass" model="claude-opus-4-8[1m]/high" date="2026-06-03T04:50:41Z" round="1">
No correctness defects found in SPEC-0005/T-008. The `Unchanged`-skip in `replay_entry` (`patina-core/src/rollback/replay.rs:67-77`) filters to `Create`/`Update` before snapshotting; the `!= Disposition::Unchanged` condition keeps exactly the two mutating variants (verified against the 3-variant enum). All-Unchanged entries return `Ok(())` before any stage dir is created (no resource leak), and `snapshot_targets` enumerates the compacted `to_revert` slice so staged-file indices stay dense and collision-free. The atomic roll-forward set is correctly narrowed to Create/Update only — Unchanged targets are never snapshotted nor passed to `revert_target`, so the prior no-backup-→-delete rule can no longer mistakenly delete an Unchanged target. Disposition plumbing through `group_by_entry`/`EntryGroup` (`mod.rs`) reads `ExpectedTarget::disposition()` from the commit record and preserves reverse-apply ordering. Unit tests (`unchanged_target_is_left_in_place`, `mixed_entry_reverts_create_and_update_but_leaves_unchanged`), the atomic tests (`rollback_atomic.rs`), and the e2e `rollback_leaves_unchanged_deletes_create_and_restores_update` all pass against the real engine.
</review>

<blockers date="2026-06-03T04:50:41Z" round="1">
Two blockers before the next implementer pass: write the missing per-task evidence file, and fix module-level doc drift in the rollback module.
- tests: No evidence file exists at `.speccy/specs/0005-patina-skip-if-satisfied/evidence/T-008.md`. Every sibling task (T-001..T-007) produced a red-then-green paper-trail file there whose `### Scenario N` blocks back its journal `demonstrated` labels; T-008 inlines a roll call in the journal and names no file path. CHK-010 is labelled `demonstrated` and must be backed by exactly one `### Scenario N` block in that file. Write `evidence/T-008.md` capturing the red baseline (pre-change `replay_entry`/`revert_target`: a no-backup Unchanged target hits the no-backup→delete arm and is deleted) versus the green result for CHK-010, and point the journal `Evidence:` field at that path.
- style: Two module-level doc comments describe rollback as having exactly two reversal branches, but the new `Unchanged` arm makes it three. `patina-core/src/rollback/mod.rs` (lines 10-23, "Inverse-operation rule") enumerates only backup-present→restore and no-backup→delete, and its "mirrors crash recovery" claim is now stale since T-007 already added the third arm to recovery. `patina-core/src/rollback/replay.rs` (lines 4-7, module preamble) says only "backup → restore … no backup → delete." Update both to enumerate all three outcomes in evaluation order: disposition == Unchanged (leave in place, do not consult backup); backup present → restore; no backup → delete. The `replay_entry` function doc at lines 38-60 is already correct.
</blockers>

<implementer date="2026-06-03T18:30:00Z" model="claude-opus-4-8[1m]/high" round="2">
- Reuse survey: retry round addressing two non-reuse blockers (a missing
  evidence file and module-level doc drift); no new top-level symbol
  introduced, so the round-1 area-map stands and the per-symbol floor is
  vacuous this round. The fix touches only doc comments
  (`patina-core/src/rollback/{mod.rs,replay.rs}` module-level preambles)
  and adds the per-task evidence file; no production logic, signatures, or
  tests changed.
- Completed: resolved both round-1 blockers.
  - tests blocker: wrote the missing paper-trail file
    `.speccy/specs/0005-patina-skip-if-satisfied/evidence/T-008.md` with a
    single `### Scenario 1` block backing CHK-010's `demonstrated` label —
    red baseline (pre-T-008 `revert_target` no-backup→delete arm at
    `replay.rs:202-206` deletes an Unchanged target; reproducible red by
    removing the `Disposition::Unchanged` filter, which fails
    `unchanged_target_is_left_in_place`) versus green (the filter excludes
    Unchanged from the snapshot/revert set; e2e
    `rollback_leaves_unchanged_deletes_create_and_restores_update` asserts
    all three outcomes). The journal `Evidence:` roll call below now points
    at that file path.
  - style blocker: updated the two stale module-level doc preambles to
    enumerate all three reversal outcomes in evaluation order (Unchanged →
    leave in place, do not consult backup; backup present → restore; no
    backup → delete). `mod.rs` "Inverse-operation rule" section reworded
    so the "mirrors crash recovery" framing no longer implies a
    backup-presence-only, two-branch rule; `replay.rs` module preamble
    likewise extended. The `replay_entry` function doc was already correct
    and is untouched.
- Undone: nothing in REQ-006 scope. No production behaviour changed this
  round; the round-1 implementation already passed business/security/
  correctness review.
- Hygiene checks: `just lint` exit 0 (nightly `fmt --all --check` clean;
  `clippy --workspace --all-targets --all-features --locked -- -D warnings`
  clean; `cargo doc -D warnings` clean — relevant since this round edits
  doc comments; `cargo deny check`: advisories ok, bans ok, licenses ok,
  sources ok). `just test` (`cargo test --workspace --locked`) exit 0.
  Spot-checked the touched paths: `rollback::replay::tests` 5 passed;
  `rollback_leaves_unchanged_deletes_create_and_restores_update` passed.
- Evidence: paper trail at
  `.speccy/specs/0005-patina-skip-if-satisfied/evidence/T-008.md`.
  Roll call for CHKs under REQ-006:
  - CHK-010 (apply → rollback over a mixed Create/Update/Unchanged commit
    leaves Unchanged byte-identical, deletes Create, restores Update to
    pre-apply bytes): demonstrated → evidence `### Scenario 1 — Mixed
    Create/Update/Unchanged rollback leaves Unchanged byte-identical
    (CHK-010)` covers the red (no-backup→delete deletes Unchanged) vs green
    (disposition filter leaves it in place) pair, backed by e2e
    `rollback_leaves_unchanged_deletes_create_and_restores_update`
    (`patina-cli/tests/skip_if_satisfied.rs`) and unit tests
    `unchanged_target_is_left_in_place` /
    `mixed_entry_reverts_create_and_update_but_leaves_unchanged`
    (`patina-core/src/rollback/replay.rs`).
- Discovered issues: none this round. The round-1 production code and tests
  were affirmed sound by the business/security/correctness personas; both
  blockers were documentation/audit-trail only.
- Procedural compliance: flipped T-008 `pending` → `in-progress` → (this
  block) `in-review` in TASKS.md; amended the existing dirty WIP in place
  (no `git restore`/`clean`/`checkout`); no `SPEC.md` edit; no other task
  touched; journal is append-only (this round-2 block follows the round-1
  block and the round-1 blockers verbatim).
</implementer>

<review persona="business" verdict="pass" model="claude-opus-4-8[1m]/high" date="2026-06-03T04:58:22Z" round="2">
T-008 (round 2) delivers REQ-006's observable contract in full. `patina rollback` reads each commit-recorded per-target disposition (`ExpectedTarget::disposition()`) through `group_by_entry` → `replay_entry` (`patina-core/src/rollback/replay.rs`, `patina-core/src/rollback/mod.rs`) and leaves `Unchanged` targets in place while deleting `Create` and restoring `Update` from backup. All three `<done-when>` clauses and the `<behavior>` Given/When/Then are covered: the e2e `rollback_leaves_unchanged_deletes_create_and_restores_update` (`patina-cli/tests/skip_if_satisfied.rs`) drives a real mixed `apply --yes` → `rollback --yes` over a single commit pre-staged to record one of each disposition, and asserts Unchanged byte-identical, Create absent, Update restored to pre-apply bytes (CHK-010); reinforced by unit tests `unchanged_target_is_left_in_place` and `mixed_entry_reverts_create_and_update_but_leaves_unchanged`.

This advances the SPEC `<goals>` rollback-fidelity commitment ("a target the apply did not touch is never deleted or restored on the way back") and serves the cautious-user story. No scope sneak: the round-2 diff is confined to the rollback path, its tests, two module-level doc preambles, and the new evidence file. No non-goal touched (no force-rewrite flag, no classification cache, no reap-policy change, no merge-modes), and the SPEC's "Open Questions: None" was not silently re-decided. The Changelog shows no intent shift the diff contradicts; DEC-007's tree-leaf whole-tree-backup restore is honored in the `replay_entry` doc and behavior.

Round-1 → round-2: both round-1 blockers (tests: missing `evidence/T-008.md`; style: two stale module-doc preambles enumerating only two reversal branches) were documentation/audit-trail, not business-contract — and both are now resolved (evidence file present with a single `### Scenario 1` block backing CHK-010; `mod.rs` "Inverse-operation rule" and `replay.rs` preamble now enumerate all three outcomes in evaluation order). No production behavior changed this round, so the round-1 business pass holds.

Non-blocking observation (out of business scope, unchanged from round 1): REQ-006 and the task body phrase the third arm as living inside `revert_target`, but the Unchanged guard sits one level up in `replay_entry` (filtering Unchanged out before snapshot/roll-forward). Observable behavior is identical and arguably stronger; the business contract (Unchanged left, Create deleted, Update restored) is fully met.
</review>

<review persona="tests" verdict="pass" model="claude-opus-4-8[1m]/high" date="2026-06-03T04:58:22Z" round="2">
Round-1 tests blocker (missing evidence file) is resolved. The journal round-2 `<implementer>` `Evidence:` field names `.speccy/specs/0005-patina-skip-if-satisfied/evidence/T-008.md`, the file exists, and its roll call covers REQ-006's sole CHK-010 as `demonstrated`, backed by exactly one `### Scenario 1` block — contract steps 1-5 satisfied.

No fabrication patterns: the red half (`assertion failed: target.exists()`, `FAILED. 0 passed; 1 failed`, exit 101) and green half (`5 passed; 0 failed`, exit 0) differ materially; every test named in the evidence appears in the diff; the red baseline is honestly framed as a counterfactual (filter removed) for a net-new feature; and the evidence commands are scoped (`-p patina-core --lib rollback::replay::tests`, `-p patina-cli --test skip_if_satisfied <name>`), not the workspace-wide hygiene run.

CHK-010 is exercised against the real code path. `rollback_leaves_unchanged_deletes_create_and_restores_update` (`patina-cli/tests/skip_if_satisfied.rs`) spawns the real `patina` binary for `apply --yes` then `rollback --yes` and asserts all three clauses with specific bytes (Unchanged stays `unchanged-bytes`, Create `create_out` absent, Update restored to `update-pre-apply`); it correctly uses `--yes` so it cannot pass on a non-interactive preview no-op. Unit tests pin both regression directions: with `Disposition` being a 3-variant enum, the `!= Disposition::Unchanged` filter (`replay.rs:71-77`) keeps exactly Create/Update, so removing it sends the no-backup Unchanged target into the delete arm at `replay.rs:205-208` and fails `unchanged_target_is_left_in_place`; `mixed_entry_reverts_create_and_update_but_leaves_unchanged` guards the inverse (an over-broad filter swallowing Create-with-no-backup fails `assert!(!created.exists())`). The style-blocker doc edits in `mod.rs`/`replay.rs` are documentation-only and touch no test or production logic. Tests are deterministic with per-fixture env isolation.
</review>

<review persona="security" verdict="pass" model="claude-opus-4-8[1m]/high" date="2026-06-03T04:58:22Z" round="2">
No security issues in T-008 (round 2). The round-2 amendment touched only the two module-level doc preambles (`patina-core/src/rollback/{mod.rs,replay.rs}`) and added the evidence file `.speccy/specs/0005-patina-skip-if-satisfied/evidence/T-008.md`; no production logic, signatures, or trust boundaries changed, so the round-1 security pass stands.

Re-verified the rollback-skip surface: `replay_entry` (`patina-core/src/rollback/replay.rs:67-77`) excludes a target from the snapshot/revert set only when its commit-recorded disposition is exactly `Disposition::Unchanged` (predicate `t.disposition != Disposition::Unchanged`), and `group_by_entry` (`patina-core/src/rollback/mod.rs:178-182`) sources that disposition directly from the persisted `ExpectedTarget::disposition()` with no default-to-Unchanged fallback. This is fail-closed: a `Create`/`Update` target can never be mis-skipped, and a malformed/legacy record is rejected by the version envelope rather than silently decoding as `Unchanged`. The change strictly narrows the destructive set (no-backup `Unchanged` targets are left in place instead of deleted) — no new path-traversal, deletion, injection, secret-handling, auth, or TOCTOU surface. Guarded by `unchanged_target_is_left_in_place`, `mixed_entry_reverts_create_and_update_but_leaves_unchanged`, and the e2e `rollback_leaves_unchanged_deletes_create_and_restores_update`.
</review>

<review persona="style" verdict="pass" model="claude-sonnet-4-6[1m]/medium" date="2026-06-03T04:58:22Z" round="2">
Both round-1 doc-drift blockers are resolved. `patina-core/src/rollback/mod.rs` lines 10-27 ("Inverse-operation rule") now enumerates all three reversal outcomes in evaluation order (Unchanged → leave in place; backup present → restore; no backup → delete), and the "mirrors crash recovery" framing is consistent with T-007's update to `recovery.rs`. `patina-core/src/rollback/replay.rs` lines 1-10 (module preamble) likewise names all three outcomes with correct sequencing. The round-2 diff itself is minimal: one import (`use crate::journal::Disposition`) and three `disposition: Disposition::Update` field additions in test fixtures — all used, all consistent with surrounding test style. No new style issues.
</review>

<review persona="correctness" verdict="pass" model="claude-opus-4-8[1m]/high" date="2026-06-03T04:58:22Z" round="2">
No correctness defects in SPEC-0005/T-008 round 2. The round-2 deltas are documentation-only (the `mod.rs` "Inverse-operation rule" preamble and the `replay.rs` module/file preamble, both now enumerating the three reversal outcomes) plus a mechanical test-shape closure update in `group_by_entry`'s unit test that extracts `t.target` for the assertion — no production control flow changed from the round-1 implementation that already passed correctness review. Reconfirmed the load-bearing logic: `replay_entry` (`patina-core/src/rollback/replay.rs`) filters `disposition != Disposition::Unchanged` into a compacted `to_revert` slice and returns `Ok(())` early when it is empty before any stage dir is created (no leak), and `snapshot_targets` enumerates that dense slice so staged indices stay collision-free; the `Disposition` enum (`patina-core/src/journal/disposition.rs:34-42`) has exactly three variants, so the `!= Unchanged` predicate keeps precisely Create and Update — Unchanged targets are never snapshotted nor reach the no-backup→delete branch, so they cannot be erroneously deleted. Disposition plumbing through `group_by_entry`/`EntryGroup` (`mod.rs`) reads `ExpectedTarget::disposition()` and feeds `replay_entry` in preserved reverse-apply order (`.rev()` at `mod.rs:155-156`). `just lint`/`just test` reported green including `cargo doc -D warnings` (relevant since this round edits doc comments).
</review>
