---
spec: SPEC-0005
task: T-003
generated_at: 2026-06-02T19:23:50Z
---

<implementer date="2026-06-02T19:23:50Z" model="claude-opus-4-8[1m]/high" round="1">
- Reuse survey: Mapped the task area — covered REQ-001, the suggested
  files (`patina-core/src/status/classify.rs`, a new sibling under
  `patina-core/src/apply/`, `patina-core/src/journal/record.rs` for
  `content_hash`), and their immediate neighbours (`apply/mod.rs`
  module tree, `apply/template.rs` for the render shape, `config`'s
  `FileMode`, the `Disposition` enum from T-001). Decisions:
  - reuse-as-is — `journal::content_hash` (`record.rs:139`) for the
    `blake3` hash, `journal::read_symlink_target` (`record.rs:225`) and
    `paths::simplified_str` for the link compare, the `Disposition` and
    `FileMode` enums. No new primitive written for any of these.
  - extend — factored the symlink and content comparisons **out** of
    `status::classify::classify`'s two match arms into two `pub(crate)`
    seams in the same module (`symlink_matches`, `content_matches`) and
    made `classify` call them, so the existing status path keeps one
    definition of "matches" and the new plan-time classifier shares it
    (the REQ-001 "no third definition" mandate, per the grep-first reuse
    rule in `rust-let-types-carry-invariants.md`). Promoted
    `status::mod`'s `mod classify;` to `pub(crate) mod classify;` so the
    seams are reachable from `apply`.
  - write-fresh — `apply::classify::classify_leaf` (the per-leaf
    Create/Update/Unchanged decision) and `apply::classify::ClassifyError`
    (a typed read failure for a copy source). Searched `apply/` and the
    crate for an existing per-leaf disposition classifier
    (`rg 'fn classify' patina-core/src`, `rg Disposition`): none existed —
    T-002 only threaded a `Disposition::Create` placeholder, it computed
    nothing. New top-level symbols introduced this round:
    `classify_leaf`, `ClassifyError`, `symlink_matches`, `content_matches`
    — accounted for above (two reuse-extends of the status seam, two
    write-fresh in apply).
- Completed: Added `patina-core/src/apply/classify.rs` (registered as
  `pub(crate) mod classify;` in `apply/mod.rs`). `classify_leaf(mode,
  source, target, rendered)` short-circuits to `Create` when the target
  is absent (`symlink_metadata(target).is_err()`, matching how
  `status::classify` decides existence so a dangling link counts as
  present), then dispatches per `FileMode`: symlink family →
  `symlink_matches(target, source)`; copy / copy-tree → read+hash the
  source and `content_matches(target, &hash)`; template → `content_matches`
  against the caller-supplied freshly-rendered bytes (rendered once by the
  caller per the accepted double-render in assumptions). A match yields
  `Unchanged`, a mismatch `Update`. Refactored
  `patina-core/src/status/classify.rs`: extracted `symlink_matches` /
  `content_matches` (`pub(crate)`) and rewrote `classify` to call them;
  behaviour is unchanged (the 9 pre-existing status tests still pass).
  13 new unit tests cover each mode × {Create, Update, Unchanged}, the
  symlink-replaced-by-regular-file Update case, the unreadable-source
  error, and two Clean⇔Unchanged tie tests that drive both classifiers on
  one live state.
- Undone: Wiring `classify_leaf` into `plan` / `assemble_plan_operations`
  and populating real dispositions on `ResolvedOperation` /
  `PlannedOperation` is deliberately left to T-004 per its `<task>`
  element (T-003's scope is the classifier and its unit matrix only).
  Because the engine does not call it yet, `classify_leaf` carries a
  `#[cfg_attr(not(test), expect(dead_code, reason = "...T-004..."))]` with
  a justification naming the consuming task; it is removed when T-004
  adds the call. No execute-time write-skip, no tree aggregate, no
  reporting — all later tasks.
- Hygiene checks: ran the project's canonical gate `just check`
  (= `just lint` + `just test`), which exited 0. Constituent gates and
  their exit codes: `cargo +nightly fmt --all --check` → 0;
  `cargo clippy --workspace --all-targets --all-features --locked` →
  0 on both cross targets `x86_64-unknown-linux-gnu` and
  `x86_64-pc-windows-gnu` (the `#[cfg(windows)]` test symlink helper
  lints clean on the windows target); `RUSTDOCFLAGS=-D warnings cargo
  doc --workspace --no-deps --all-features` → 0; `cargo deny check` →
  0 (no new dependencies); `cargo test --workspace --locked` → all
  passed, including the 13 new `apply::classify::tests` cases. Scoped
  run `cargo test -p patina-core --lib classify` → 23 passed, 0 failed.
- Evidence: paper trail at
  `.speccy/specs/0005-patina-skip-if-satisfied/evidence/T-003.md`.
  T-003 is a pure-function library task with no CLI surface (the
  classifier is not yet wired into a command), so its CHKs are proved
  by the unit-test matrix under the standard `cargo test` hygiene suite
  rather than by red/green CLI scenarios. Roll call for the CHKs under
  REQ-001:
  - CHK-001 (symlink target Unchanged when pointing at source, Update
    elsewhere, Create when absent): hygiene → `symlink_pointing_at_source_is_unchanged`,
    `symlink_pointing_elsewhere_is_update`, `symlink_target_absent_is_create`,
    and `symlink_target_is_regular_file_is_update` in
    `patina-core/src/apply/classify.rs`, run under `cargo test --workspace`.
  - CHK-002 (copy/template target Unchanged when bytes hash-equal,
    Update when differing, Create when absent): hygiene → `copy_matching_bytes_is_unchanged`,
    `copy_differing_bytes_is_update`, `copy_target_absent_is_create`,
    `template_matching_rendered_output_is_unchanged`,
    `template_differing_output_is_update`, `template_target_absent_is_create`
    in the same file.
  - CHK-003 (per-leaf tree classification — one drifted leaf Update, the
    rest Unchanged): hygiene → `copy_tree_leaf_classifies_like_copy`
    asserts a copy-tree leaf is a single regular-file comparison
    classified independently; the engine drives this per leaf in T-004.
    The Clean⇔Unchanged shared-primitive tie (REQ-001's "no third
    definition") is additionally covered by
    `state_status_calls_clean_classifies_unchanged_symlink` /
    `...content`. No CHK in T-003's scope is `demonstrated` (no CLI
    behaviour to red/green) or `judgment-only`.
- Discovered issues: none. The `status::classify` extraction is a clean
  factor-out with no behavioural change; no pre-existing problems were
  noticed in the touched area.
- Procedural compliance: this entry lands directly in
  `journal/T-003.md` per the journal-file schema; no `<implementer-note>`
  was written into TASKS.md. The TASKS.md `state="..."` attribute for
  T-003 flips from `in-progress` to `in-review` as the final step of this
  turn. No shipped skill bodies under `.claude/skills/` were edited.
</implementer>

<review date="2026-06-02T15:27:55-04:00" model="claude-opus-4-8[1m]/high" persona="business" verdict="pass" round="1">
T-003 (REQ-001) delivers what the SPEC promises. The plan-time `classify_leaf`
(`patina-core/src/apply/classify.rs`) maps every mode to Create/Update/Unchanged
matching all four REQ-001 `<done-when>` bullets, and the load-bearing "no third
definition of matches" mandate is genuinely honored: `status::classify` was
refactored to route through shared `pub(crate)` `symlink_matches`/`content_matches`
seams (`patina-core/src/status/classify.rs`) that `classify_leaf` reuses, with the
existence check (`symlink_metadata(target).is_err()`) mirroring status's own. The
two Clean⇔Unchanged tie tests drive both classifiers on identical live state.
Scope is disciplined per the task split — plan-path wiring is correctly deferred
to T-004 (where the SPEC's "when the plan is computed" CHK-001/002/003 integration
assertions land), not silently resolved here. No non-goal scope crept in (no force
flag, no cache, no reap-policy change; reads live state per DEC-006), and the
per-leaf tree classification reflects the current DEC-007 intent.
</review>

<review date="2026-06-02T15:27:55-04:00" model="claude-opus-4-8[1m]/high" persona="tests" verdict="pass" round="1">
T-003's classifier and its unit matrix are well-tested against real behavior. All
three `<task-scenarios>` map to executable tests that drive `classify_leaf` over
real `TempDir`/`fs_err`/symlink state (no mocks of the system under test):
symlink/copy/template × {Create, Update, Unchanged} in
`patina-core/src/apply/classify.rs`. The negative path
`copy_unreadable_source_surfaces_io_error` asserts the specific `ClassifyError::Io`
variant, not any error. The REQ-001 "no third definition of matches" mandate is
enforced by the tie tests `state_status_calls_clean_classifies_unchanged_{symlink,
content}`, which drive both `status::classify::classify` (asserting `Clean`) and
`classify_leaf` (asserting `Unchanged`) on the same live state; grep confirms
`symlink_matches`/`content_matches` have exactly one definition shared by both call
sites. A mutation forcing `classify_leaf` to always return `Unchanged` fails 5
tests, confirming non-vacuity. Evidence file is genuine: scoped run reproduced (23
passed), all named tests exist in the diff. Roll call covers every CHK under
REQ-001 with specific test-name cites; deferral of engine-level "when the plan is
computed" assertions to T-004 matches the explicit task split.
</review>

<review date="2026-06-02T15:27:55-04:00" model="claude-opus-4-8[1m]/high" persona="security" verdict="pass" round="1">
T-003 plan-time leaf classifier is a read-only, pure-function library slice with no
security regressions. Integrity primitive unchanged: equality is decided by blake3
`content_hash` for content/template and the verbatim `simplified_str` link-target
compare for symlinks — the exact primitives `status` already uses (REQ-001, no
third definition); blake3's collision resistance means a drifted target cannot be
crafted to hash-equal the source and suppress a future write. No symlink-follow
hazard: existence is probed with `symlink_metadata` (not `metadata`), and the
classifier never follows or mutates through a link — actual write-skip is deferred
to T-005, so no classify-then-mutate TOCTOU is introduced. No sensitive-data
exposure: `ClassifyError::Io` discloses only a local source path consistent with
the `fs-err` convention; no secrets/tokens/credentials handled. No new dependencies
(deny check = 0), no auth/network/rate-limit surface. Non-blocking robustness note:
`rendered.unwrap_or(&[])` would, on a missing template render, only mis-bias toward
`Update`/`Unchanged` — fail-safe direction, worst case a redundant write, never an
unsafe skip.
</review>

<review date="2026-06-02T15:27:55-04:00" model="claude-sonnet-4-6[1m]" persona="style" verdict="pass" round="1">
All style conventions are satisfied. Import ordering in `apply/classify.rs` follows
the project's one-per-line pattern matching sibling modules. The
`#[cfg_attr(not(test), expect(dead_code, reason = "...T-004..."))]` suppression
carries the required justification naming the consuming task. `pub(crate)`
visibility on `classify_leaf`, `ClassifyError`, `symlink_matches`, and
`content_matches` is correct and consistent with the surrounding codebase.
`#[non_exhaustive]` on `ClassifyError` matches `ExecutorError`'s shape. All public
items carry doc comments including `# Errors`. Reuse survey is honest —
`content_hash` is confirmed at `record.rs:139` and `symlink_matches`/
`content_matches` are genuinely new seams factored from `status::classify` rather
than re-invented. No dead imports, unused variables, or parallel helper duplication
issues beyond what the module boundary makes unavoidable.
</review>

<review date="2026-06-02T15:27:55-04:00" model="claude-opus-4-8[1m]/high" persona="correctness" verdict="pass" round="1">
No correctness defects found. The `status::classify` extraction into
`symlink_matches`/`content_matches` is a faithful, behavior-preserving refactor
(original per-arm Clean/Drifted logic reproduced exactly; the 9 pre-existing status
tests pass). `apply::classify::classify_leaf` has sound control flow: the
`symlink_metadata(target).is_err()` Create short-circuit matches status's existence
check (dangling links correctly treated as present), the per-mode dispatch routes
through the shared seams so Unchanged coincides with status Clean (verified by the
two tie tests), the copy-source read failure surfaces a typed `ClassifyError::Io`
rather than a swallowed error, and the final `unchanged ? Unchanged : Update`
mapping has no inverted condition (Create handled before the match). Passing
`source.as_str()` as the symlink `desired` is correct — `ExpectedTarget::Symlink.
link_target` is defined as the source path. The template arm's
`rendered.unwrap_or(&[])` fallback errs conservatively and has no production caller
yet. No off-by-one, boundary, concurrency, or resource-leak issues; all 23 scoped
tests pass.
</review>
