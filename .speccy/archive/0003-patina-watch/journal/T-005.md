---
spec: SPEC-0003
task: T-005
generated_at: 2026-05-31T00:00:00Z
---

<implementer date="2026-05-31T22:40:00Z" model="claude-opus-4-8[1m]/high" round="1">
- Reuse survey: Bounded to the task area (covered REQ-007 CHK-018 CLI
  slice; suggested files `patina-cli/src/cli.rs`,
  `patina-cli/src/cmd/debug.rs`, `patina-cli/tests/debug_drift_cache_cli.rs`;
  neighbour the parallel `DebugCommand::Journal` / `run_journal` surface
  and the `debug_journal_cli.rs` test harness). Decisions:
  - Reuse-as-is: `patina_core::{load_drift_cache_file, render_drift_cache}`
    (the T-004 surface this task exists to expose) called directly in the
    new `run_drift_cache`; `Reporter::{diff, warn}` and
    `ExitCode::{Success, Generic}` exactly as `run_journal` uses them;
    `DriftCacheError`'s own `Display` for the failure line (its
    `VersionMismatch` arm already names both majors). In tests:
    `DriftCache::{new, encode}`, `DriftEntry::new`,
    `DRIFT_CACHE_MAJOR_VERSION` (all T-004 public surface), the
    `BufferReporter` unit harness, and the `invoke`/`code`/`write_*`
    fixture-and-spawn shape plus the `u16::MAX` envelope-patch trick —
    all reused as a pattern from the shipped `debug_journal_cli.rs`.
  - Extend: `DebugCommand` (grew a `DriftCache(DebugDriftCacheArgs)`
    variant rather than a parallel enum) and `cmd/debug.rs::run` (grew a
    `DriftCache` match arm). Both extend the existing namespace in place.
  - Write-fresh: `DebugDriftCacheArgs` (single `path: Utf8PathBuf`
    positional, mirroring `DebugJournalArgs`) and the `run_drift_cache`
    free fn. `rg 'DriftCache|drift-cache' patina-cli/src` confirmed no
    pre-existing CLI drift-cache surface before writing them.
- Completed: Added `DebugCommand::DriftCache(DebugDriftCacheArgs)` and the
  `DebugDriftCacheArgs` struct to `patina-cli/src/cli.rs`; added the
  `DriftCache` arm to `cmd/debug.rs::run` dispatching to a new
  `run_drift_cache` that calls `load_drift_cache_file` + `render_drift_cache`
  and emits via the `Reporter`, with exit-code mapping parallel to
  `run_journal` (0 on decode+render, 1 on any failure). Added 3 unit tests
  in `cmd/debug.rs` and 4 integration tests in the new
  `patina-cli/tests/debug_drift_cache_cli.rs`. Updated the `debug.rs`
  module doc to cover both subcommands.
  - One deliberate divergence from `run_journal`: `render_drift_cache`
    takes only `&cache` (the cache carries its own `journal_ts`), unlike
    `render_plan(&plan, &ts)`. And `run_drift_cache` warns `"{path}: {err}"`
    rather than the bare `err.to_string()` `run_journal` uses, because the
    drift cache's `DriftCacheError::Filesystem` is a `#[from] std::io::Error`
    whose `Display` is `"drift-cache filesystem operation failed"` and does
    NOT carry the path (empirically verified with a transient probe — see
    Discovered issues). Prefixing the caller-held `args.path` at this
    control-flow layer satisfies task-scenario 2's "names the path" contract
    without editing the locked T-004 format module.
- Undone: Nothing in T-005's scope. The producer of real `drift.cache`
  files — the watcher event loop and drift-detection upsert — is T-008 /
  T-010 and out of scope here; tests synthesize cache bytes via the public
  `DriftCache::encode`, exactly as `debug_journal_cli.rs` synthesizes plan
  bytes. No docs change: `docs/USER_GUIDE.md` documents `debug journal`
  because journals already exist, but no command yet writes a drift cache,
  so documenting `debug drift-cache` ahead of its producer would describe
  behaviour a user cannot exercise; the user-facing watch docs land with
  the watch tasks (consistent with T-001..T-004 touching no docs). The
  `debug` group is `hide = true` (developer post-mortem tooling).
- Hygiene checks: `just check` exited 0 (project standard gate; runs the
  docs gate the bare cargo commands omit). Component exit codes:
  `cargo +nightly fmt --all --check` 0;
  `cargo clippy --workspace --all-targets --all-features -- -D warnings` 0;
  `RUSTDOCFLAGS="-D warnings" cargo doc --workspace --no-deps --all-features` 0;
  `cargo deny check` 0 (no new dependency — the change is pure CLI wiring
  over existing patina-core surface); `cargo test --workspace --locked` 0.
- Evidence: paper trail at
  `.speccy/specs/0003-patina-watch/evidence/T-005.md`. Roll call for the
  CHKs under REQ-007 (T-005 owns only the CLI slice; the other two CHKs are
  earlier/later tasks):
  - CHK-018 (`patina debug drift-cache <path>` prints version, timestamp,
    path, both hashes; exit 0): demonstrated → evidence Scenario 1
    (red: `unrecognized subcommand` / exit 2; green:
    `decodes_a_drift_cache_and_prints_version_timestamp_path_and_hashes`
    asserts the `version:`/timestamp/`.gitconfig`/both-hex substrings + exit
    0, plus the unit `renders_a_valid_drift_cache_to_stdout_and_exits_zero`).
    The two error scenarios (missing path → exit 1 naming the path; newer
    major → exit 1 naming both versions) are T-005's own task-scenarios
    2 & 3, also demonstrated (evidence Scenarios 2 & 3).
  - CHK-011 (drift detected → cache entry + exactly one notification):
    judgment-only for T-005 → out of scope; drift detection + notify land
    in T-010. T-005 adds no watcher.
  - CHK-012 (`patina status --json` reports DRIFTED from the live re-hash,
    not the cache): judgment-only for T-005 → out of scope; status coupling
    is unchanged by this CLI-only task.
- Discovered issues: The `DriftCacheError::Filesystem` variant
  (`#[from] std::io::Error`) drops the path: a transient probe showed
  `load_drift_cache_file("/no/such/drift.cache")` Displays as
  `drift-cache filesystem operation failed` with no path, because `?`
  converts the fs-err error into a bare `io::Error` that loses fs-err's
  path-wrapped message. T-005 works around it at the CLI layer (prefixing
  `args.path`) rather than modifying the locked T-004 module. A future task
  that wants the path inside the typed core error (e.g. for non-CLI callers)
  could store the `Utf8PathBuf` on the `Filesystem` variant; flagged, not
  fixed here, as it is out of T-005's CLI-wiring scope and would touch the
  locked SPEC-0003 format module.
- Procedural compliance: Entry lands directly in `journal/T-005.md` per the
  journal-file schema; no `<implementer-note>` written into TASKS.md. The
  TASKS.md `state` for T-005 flips `pending` → `in-progress` → `in-review`
  across this turn. No shipped `.claude/skills/` bodies required edits.
</implementer>

<review date="2026-05-31T22:07:48Z" model="claude-opus-4-8[1m]/high" persona="business" verdict="pass" round="1">
CHK-018 is genuinely satisfied. `patina debug drift-cache <path>` decodes via T-004's `load_drift_cache_file` + `render_drift_cache` and the renderer emits all required substrings — `drift-cache version:`, `journal timestamp: <ts>`, the target path, and both hashes as lower-case hex — with exit 0. The integration test `decodes_a_drift_cache_and_prints_version_timestamp_path_and_hashes` asserts exactly the CHK-018 contract and passes. The two error task-scenarios (missing path → exit 1 naming the path; newer major → exit 1 naming both versions) are also covered and pass. UX mirrors `debug journal`: same `DebugCommand` extension, same single-positional args shape, same `Reporter::diff`/`warn` routing, same exit-code mapping, same `hide=true` group. The one deliberate divergence (`run_drift_cache` prefixes `{path}:` because T-004's `DriftCacheError::Filesystem` `#[from] io::Error` lacks the path, whereas `PlanRenderError` carries it) preserves observable parity and is documented, not silently fixed. No scope creep, no non-goal violations; open question (c) was author-resolved with CHK-018 as its named scenario. Synthesizing cache bytes via the public `DriftCache::encode` is the correct decode-surface seam (matches `debug_journal_cli.rs`).
</review>

<review date="2026-05-31T22:07:48Z" model="claude-opus-4-8[1m]/high" persona="tests" verdict="pass" round="1">
CHK-018 is genuinely exercised end-to-end via the real binary: `decodes_a_drift_cache_and_prints_version_timestamp_path_and_hashes` spawns `CARGO_BIN_EXE_patina`, writes a real cache via `DriftCache::encode`, and asserts exit 0 plus `version:`, the bound timestamp, `.gitconfig`, and both hex hashes — substrings confirmed actually emitted by the real `render_drift_cache`. Negative paths carry specific assertions: missing path → exit 1 + path on stderr + empty stdout; newer-major → exit 1 + `65535` + supported major + the word `version`. Ran all 3 unit + 4 integration tests (green) and confirmed via mutation that each assertion bites: swapping the error exit code to Success fails 4 tests; replacing the path-prefixed `warn` with naive `err.to_string()` reproduces the exact documented red state and fails the path-naming tests; suppressing the render fails the CHK-018 positive test. Evidence file exists with scoped red→green capture; cited test names appear in the diff; roll call covers REQ-007 (CHK-018 demonstrated, CHK-011/CHK-012 judgment-only — watcher drift-detection and `status` coupling land in T-010, correctly out of scope). No fabrication, no vacuous assertions. The CLI maps every `DriftCacheError` through one Err arm, so a dedicated corrupt-body CLI test would exercise the same control-flow already covered (format-level decode failures are tested in T-004) — acceptable scoping.
</review>

<review date="2026-05-31T22:07:48Z" model="claude-opus-4-8[1m]/high" persona="security" verdict="pass" round="1">
T-005 (`patina debug drift-cache <path>`) is secure. The untrusted-byte decode path is panic-free end to end: `split_envelope` bounds-checks via `split_first_chunk::<2>().ok_or(...)` with no indexing, the body deserialize is `postcard::from_bytes(...).map_err(...)` which returns `Err` (never panics) on malformed/truncated input, and `render_drift_cache` uses infallible `writeln!`-into-String plus fixed-`[u8;32]` hex encoding with no `unwrap`/`expect` on any production path. Exit-code mapping is correct: `Ok` → 0, every `Err` arm (Filesystem/Truncated/VersionMismatch/Decode) → 1, with nothing rendered to stdout on failure. Error messages disclose nothing beyond the path the caller already supplied (`"{}: {err}"`); the typed `DriftCacheError` Display strings carry only version numbers, byte counts, and postcard reasons — no file contents, env, or foreign paths. No new dependency, no injection sink (output routed through `Reporter`), no auth/crypto/secret surface. No blockers.
</review>

<review date="2026-05-31T22:07:48Z" model="claude-sonnet-4-6[1m]/medium" persona="style" verdict="pass" round="1">
All style checks pass. `DebugCommand::DriftCache` / `DebugDriftCacheArgs` in `cli.rs` mirror `Journal` / `DebugJournalArgs` exactly (same derives, same `#[arg(value_name = "path")]` on `Utf8PathBuf`). Imports in `debug.rs` use separate `use` lines per item, matching convention. `run_drift_cache` routes output through `reporter.diff`/`reporter.warn`, no stray `println!`/`eprintln!`. The `"{path}: {err}"` divergence from `run_journal`'s bare `err.to_string()` is the correct CLI-layer workaround — `DriftCacheError::Filesystem` is `#[from] std::io::Error` and the `fs-err`→`io::Error` conversion strips the path; the comment explains this without suppression annotations. Tests use `.expect()` (test code only), no `unwrap()`. No dead imports, unused variables, or parallel reimplementations. `debug_help_names_drift_cache_subcommand` gates a real CLI-shape invariant (clap variant → kebab-case subcommand). Reuse survey present; named symbols all exist and are called.
</review>

<review date="2026-05-31T22:07:48Z" model="claude-opus-4-8[1m]/medium" persona="correctness" verdict="pass" round="1">
T-005 is correct. `run_drift_cache` (`patina-cli/src/cmd/debug.rs:66-85`) maps Ok→0 / any-Err→1 via `ExitCode::{Success=0, Generic=1}`, exactly paralleling `run_journal`; all `DriftCacheError` variants (Filesystem/Decode/Truncated/VersionMismatch) funnel through the single Err arm so no decode/version/IO failure can leak through as success. It calls T-004's `load_drift_cache_file` + `render_drift_cache` and renders to stdout via `reporter.diff` on success, errors to stderr via `reporter.warn`. Dispatch is reachable (`main.rs:69` → `cmd::debug::run` → new `DriftCache` arm). The `format!("{}: {err}", args.path)` path-prefix correctly compensates for the path-dropping `Filesystem(#[from] io::Error)` variant. All 6 unit and 4 integration tests pass after a fresh build (an initial failure traced to a stale CARGO_BIN_EXE artifact, not a code defect). No correctness findings at or above the confidence bar.
</review>
