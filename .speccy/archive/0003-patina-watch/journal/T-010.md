---
spec: SPEC-0003
task: T-010
generated_at: 2026-05-31T20:30:00Z
---

<implementer date="2026-05-31T20:30:00Z" model="claude-opus-4-8[1m]/high" round="1">
- Reuse survey: Mapped the task area (covered REQ-007 / DEC-003 /
  DEC-004 / DEC-008 / DEC-013, the suggested files `watch/drift.rs`,
  `watch/mod.rs`, `Cargo.toml`, `patina-core/Cargo.toml`,
  `watch_foreground_cli.rs`, plus the T-004 `drift_cache.rs`, the
  journal `record.rs`, and the T-007/T-009 `subscriptions.rs` they lean
  on). Decisions:
  - reuse-as-is — the whole T-004 drift-cache format and IO:
    `DriftCache`, `DriftEntry::new`, `write_drift_cache` (already
    tempfile-then-rename atomic), `load_drift_cache_file`; the journal's
    `content_hash` (no parallel `blake3` call); `fs_err::read` for live
    bytes. The `patina debug drift-cache` decode/render surface and its
    CLI wiring were already shipped by T-004 — T-010 only populates the
    cache, so CHK-018 needed no new code.
  - extend — `subscriptions.rs`: added a `content_targets` field to the
    existing `WatchSet` (and a `ContentTarget` struct) plus the content
    arm in `compute_watch_set`, reusing the existing `record.targets`
    iteration rather than re-deriving the record in the loop;
    `watch/mod.rs`: added the content-target `else` arm to the T-009
    select-loop and threaded a `journal_ts` alongside `watch_set`
    through the loop and `rescan`.
  - write-fresh — `watch/drift.rs`. Searched the crate with
    `rg 'trait.*Sink|fn .*drift|NotificationSink'` before writing;
    nothing existed. New top-level symbols: `drift::NotificationSink`
    (trait, mandated by DEC-013), `drift::NotifySink` (production
    `notify-rust` impl), `drift::DriftOutcome` (mirrors
    `reapply::ReapplyOutcome`), `drift::handle_target_events`, the consts
    `NOTIFICATION_TITLE` / `RATE_LIMIT_WINDOW_SECS` / `DRIFT_CACHE_FILENAME`,
    `subscriptions::ContentTarget`. Per the reuse rule's byte-twiddling
    clause I did NOT write a second hex helper: I promoted the existing
    private `drift_cache::hex_encode` to `pub(super)` and call it for the
    `drift` event's hash fields, so the log and `debug drift-cache` agree.
- Completed: Landed the drift-detection handler end-to-end (REQ-007).
  `drift::handle_target_events` iterates the committed content targets
  whose path the debounced batch touched, reads the live bytes, hashes
  via `content_hash`, and compares to the recorded `ExpectedTarget::Content`
  hash (now carried into `WatchSet.content_targets`). On divergence it
  logs a `drift` warn event (`drift_path` / `drift_expected_hash` /
  `drift_actual_hash` — underscore field names, matching the watcher's
  existing `re_apply_*` events; tracing's dotted-field macro grammar is
  ambiguous with `%`-sigil value exprs in this version, the same call
  recorded in T-009's journal), upserts the `drift.cache` (keyed on the
  target path so a repeated edit replaces rather than duplicates the
  entry; all of a batch's divergences coalesce to one atomic write), and
  notifies through the `NotificationSink` gated by a per-target 60s rate
  limit (DEC-004) keyed on the cache's own `detected_at_unix` — the cache
  is the single rate-limit ledger, so the limit survives across batches.
  The `mod.rs` loop routes a content-target-only batch here (NOT to
  re-apply, which would rewrite the target and re-trigger). A symlink
  target never reaches the handler (it is absent from `content_targets`,
  DEC-008). The handler never auto-syncs (DEC-003) — observe + notify
  only. Added `notify-rust = "4.17"` to the workspace + `patina-core`;
  the production `NotifySink` swallows a daemon-unreachable error as a
  `drift_notify_failed` warn so a headless host never crashes the watcher.
- Undone: Nothing in REQ-007 is deferred. The per-OS service install
  (T-011+) is out of this task's scope; the foreground watcher is the
  engine it will supervise.
- Hygiene checks:
  - `cargo test --workspace --locked` → exit 0
  - `cargo clippy --workspace --all-targets --all-features -- -D warnings` → exit 0
  - `cargo +nightly fmt --all --check` → exit 0
  - `cargo deny check` → exit 0 (also run at the dep-add gate per DEC-013,
    before building on `notify-rust`; advisories/bans/licenses/sources ok)
  - (also `RUSTDOCFLAGS="-D warnings" cargo doc --workspace --no-deps
    --all-features` → exit 0, the `just lint` docs gate)
- Evidence: paper trail at `.speccy/specs/0003-patina-watch/evidence/T-010.md`.
  Roll call for CHKs under REQ-007:
  - CHK-011 (divergent target → one notification + cache entry with
    H1/H2): demonstrated → evidence Scenario 1. The notification count
    is asserted against the capture sink in
    `watch::drift::tests::divergent_target_notifies_once_and_records_h1_h2`
    (DEC-013); the on-disk cache + drift log are asserted end-to-end in
    `foreground::an_external_target_edit_logs_drift_and_populates_the_cache`.
  - CHK-012 (`patina status --json` reports `.gitconfig` DRIFTED from the
    live re-hash, not the cache; aggregate `drifted >= 1`): demonstrated
    → evidence Scenario 3. Asserted in the end-to-end foreground test by
    parsing the status JSON; the live-re-hash independence is backed by
    the existing `edited_copy_target_reports_drifted` (DRIFTED with no
    watcher running). This is fundamentally a SPEC-0001 surface; T-010
    confirms the cache does not feed it.
  - CHK-018 (`patina debug drift-cache <path>` decodes the populated
    cache, printing version / journal timestamp / `.gitconfig` / both
    hashes, exit 0): hygiene → the decode/render/CLI surface shipped in
    T-004 and is covered by `patina-cli`'s `debug_drift_cache_cli.rs`
    (`decodes_a_drift_cache_and_prints_version_timestamp_path_and_hashes`)
    and `drift_cache.rs` unit tests, which run under the project test
    command. T-010 adds the producer that writes the cache those tests
    decode; no new decode behaviour to demonstrate here.
  - DEC-004 rate-limit (not its own CHK but a REQ-007 `<done-when>` /
    `<behavior>` bullet): demonstrated → evidence Scenario 2
    (`second_detection_within_the_window_is_rate_limited` +
    `detection_past_the_window_notifies_again`).
  - DEC-008 symlink-not-watched (REQ-007 `<done-when>` / `<behavior>`):
    demonstrated → evidence Scenario 4
    (`watch_set_separates_sources_from_content_targets`).
- Discovered issues: none. The drift-cache atomic-write, version
  envelope, and decode surface were already complete from T-004, so this
  task was purely the producer + dispatch + sink. No SPEC ambiguity
  surfaced; the `journal_ts` the cache binds to is sourced from the
  committed record's `last_apply.at` (the only journal timestamp the
  loop already holds), which the cache renders as-is.
- Procedural compliance: TASKS.md state flipped pending → in-progress →
  in-review by editing the `state` attribute (no other hand-edits). No
  locked-SPEC edit. Journal is append-only (this is round 1, file
  created with the three-field frontmatter). `cargo deny check` was run
  at the dependency-add gate before code was built on `notify-rust`
  (DEC-013). No `unwrap`/`expect`/panic in production; the `NotifySink`
  daemon-error and the cache-write error are both logged-and-swallowed
  so a transient failure never crashes the watcher.
</implementer>

<review persona="business" verdict="blocking" model="claude-opus-4-8[1m]/high" date="2026-06-01T02:47:58Z" round="1">
REQ-007's done-when bullet "The drift cache survives watcher restarts; the next `patina apply` clears it when its journal becomes the new truth" is not delivered. The persist-across-restarts half is satisfied (the cache is a file re-read by `load_drift_cache_file` on the next batch), but the "next `patina apply` clears it" half is unimplemented anywhere in the tree — `grep` finds zero drift-cache references in `patina-core/src/apply/`, the journal commit path, or `patina-cli/src/cmd/apply.rs`.

T-010 is the only task covering REQ-007's runtime drift behavior (T-001/T-004/T-005 cover envelope + format + decode), so this bullet has no other task home; if T-010 doesn't deliver it, REQ-007 ships incomplete. The handler at `patina-core/src/watch/drift.rs` only *rebinds* `cache.journal_ts` to the new committed `at` and upserts entries keyed on target path — it never clears entries belonging to a superseded journal. Observable consequence: after a re-apply commits a new journal, stale entries from the prior journal era persist on disk and are silently re-labeled with the new `journal_ts`, so `patina debug drift-cache` (CHK-018's support surface) shows entries mis-bound to a journal they were never measured against.

This is the easy-to-miss pattern the SPEC warns about: CHK-011/CHK-012/CHK-018 all pass, but none exercises the cache-clear-on-apply lifecycle, so the green suite does not cover the dropped bullet. It does not corrupt `patina status` (status uses the live re-hash, REQ-018, and never reads the cache — correct), but it leaves a named done-when unmet and contradicts the journal's "Undone: Nothing in REQ-007 is deferred."

Required before pass: either implement the clear so a new committed journal drops superseded drift-cache entries per the done-when, OR — if the intent is that this lifecycle bullet belongs to a separate apply-pipeline task — surface that to a human and amend coverage so REQ-007's done-when is explicitly homed, rather than silently leaving it unimplemented. Everything else maps cleanly: CHK-011, CHK-012, CHK-018, DEC-003 (no auto-sync), DEC-004 (per-target 60s rate limit), DEC-008 (symlink absent from `content_targets`), DEC-013 (sink trait + capture sink + `cargo deny` gate). No non-goal scope creep and no open question silently resolved.
</review>

<review persona="tests" verdict="pass" model="claude-opus-4-8[1m]/high" date="2026-06-01T02:47:58Z" round="1">
T-010 drift-detection tests genuinely exercise REQ-007. Evidence roll call is complete: CHK-011/CHK-012 `demonstrated`, CHK-018 `hygiene` with a verified cite (`decodes_a_drift_cache_and_prints_version_timestamp_path_and_hashes` in `patina-cli/tests/debug_drift_cache_cli.rs`). All test names in the evidence appear in the diff; red (T-009 no-op `else` arm) and green outputs are materially different; scoped per-test cites, not the full-suite run.

CHK-011: `patina-core/src/watch/drift.rs` `divergent_target_notifies_once_and_records_h1_h2` drives the real `handle_target_events` (real `blake3` `content_hash`, real `write_drift_cache`, real cache decoded from disk) and asserts `expected==H1, actual==H2, count==1` — the `CaptureSink` is an injected DEC-013 sink, not a mock of the SUT. End-to-end `patina-cli/tests/watch_foreground_cli.rs` confirms the on-disk + log surface against a live watcher subprocess and asserts the negative path (`re_apply` count == 0, a content edit must not re-apply). CHK-012 parses real `status --json` for `state=="drifted"` and `drifted >= 1`. DEC-004 rate limit is covered with a deterministic injected `now_unix` (30s suppressed, 61s re-notifies — a sliding window, not a one-shot), no time flake. DEC-008 verified at `subscriptions.rs`. Negative/edge paths (clean, not-in-batch, unreadable-after-delete, per-target coalesced write) all have specific assertions that would fail under an obviously-broken reimplementation.
</review>

<review persona="security" verdict="pass" model="claude-opus-4-8[1m]/high" date="2026-06-01T02:47:58Z" round="1">
No security blockers. The drift handler logs only the target path and one-way `blake3` hex digests (`patina-core/src/watch/drift.rs`) and notifies with the path only — no file-content or plaintext leak in logs or notifications. Read failures, notification-daemon failures, and cache-write failures are all logged-and-swallowed so no panic/crash path is introduced. The `notify-rust 4.17` dependency and its transitive DBus/WinRT stack clear the DEC-013 gate: `cargo deny check` returns `advisories ok, bans ok, licenses ok, sources ok`. Symlink targets are correctly excluded from `content_targets` (only the `ExpectedTarget::Content` arm is collected in `subscriptions.rs`), so the handler never re-hashes a link to an out-of-bounds read. Targets derive from the trusted committed journal record — no new path-traversal or injection vector. The read-then-hash race is detection-only and never feeds an authorization decision (`status` re-derives DRIFTED from its own live re-hash).
</review>

<review persona="style" verdict="pass" model="claude-sonnet-4-6[1m]/medium" date="2026-06-01T02:47:58Z" round="1">
The diff is well-aligned with the project's conventions. All new public items in `drift.rs` and `subscriptions.rs` carry doc comments. Naming follows the existing `re_apply_*`/`drift_*` underscore-field convention. Error paths log-and-swallow without panics. The `hex_encode` promotion to `pub(super)` and reuse rather than duplication is exactly the pattern `rust-let-types-carry-invariants.md` prescribes. The `let _outcomes` and `let _outcome` discard pattern is consistent with T-009's existing `reapply` arm. No dead code, no suppression annotations, and no reinvented helpers detected.
</review>

<review persona="correctness" verdict="pass" model="claude-opus-4-8[1m]/high" date="2026-06-01T02:47:58Z" round="1">
T-010 drift handler is correct. Reviewed `patina-core/src/watch/drift.rs` (handler, rate-limit, upsert), the `watch/mod.rs` batch-routing `else` arm, the `subscriptions.rs` `content_targets` derivation, and the `drift_cache.rs` `hex_encode` promotion.

Control flow checks out: content-target path matching uses the same `Utf8PathBuf::from(target)` string in both `compute_subscriptions` and `compute_watch_set.content_targets`, so a `notify`-reported path reliably hits the handler. Rate-limit arithmetic uses `now_unix.saturating_sub(detected_at_unix) < 60` — no overflow, boundary consistent (+60s notifies; +61s test confirms; +30s test confirms suppression). Symlink targets correctly excluded (DEC-008). The Result-swallowing paths are intentional best-effort-no-crash. Empty-record / no-commit case yields empty `content_targets` and a no-op handler. The 17 drift unit tests pass and `clippy -D warnings` is clean.

Two narrow non-blocking observations (below bar): (1) the sliding rate-limit window refreshes `detected_at_unix` on every divergence so a continuous edit stream can suppress indefinitely — more conservative than DEC-004, never violates it; (2) `load`'s `unwrap_or_else` swallows a newer-major version error and a subsequent write would downgrade a future-format cache, but the drift cache is ephemeral, no newer major exists in v1.0, worst case one extra notification.
</review>

<blockers date="2026-06-01T02:47:58Z" round="1">
Implement the drift-cache clear-on-new-journal half of REQ-007's done-when ("the next `patina apply` clears it when its journal becomes the new truth"); it is currently unimplemented and T-010 is its only task home in this SPEC.
- business (blocking): The handler at `watch/drift.rs` rebinds `cache.journal_ts` to the new committed journal `at` but never clears entries measured against the superseded journal, so stale entries persist on disk and are silently re-labeled with the new `journal_ts` — `patina debug drift-cache` then shows entries mis-bound to a journal they were never measured against. `grep` confirms zero drift-cache references in the apply engine / journal-commit path, and no other SPEC-0003 task (T-001/T-004/T-005) homes this lifecycle bullet. Required: when the handler observes the committed `journal_ts` has advanced past the cache's recorded `journal_ts`, clear the stale entries (do not merely rebind the timestamp) before upserting new divergences, and add a test asserting that a journal-timestamp advance drops prior-era entries. The natural seam is the existing `journal_ts` rebind in `handle_target_events`; if you conclude the clear genuinely belongs to the SPEC-0001 apply engine (standalone `patina apply` with no watcher running) rather than the watcher's drift handler, do NOT silently leave it unimplemented — stop and surface that the done-when needs a `/speccy-amend` to home it in a separate apply-pipeline task, and correct the round-1 "Undone: Nothing in REQ-007 is deferred" claim.
</blockers>

<implementer date="2026-06-01T02:54:04Z" model="claude-opus-4-8[1m]/high" round="2">
- Reuse survey: This retry addresses a single non-reuse blocker (a
  missing lifecycle clear), so the full round-1 area-map is not re-run.
  No new top-level symbol was introduced — the fix is an in-place
  amend of the existing `handle_target_events` rebind block plus two
  new `#[cfg(test)]` functions — so the per-symbol floor adds nothing
  to account for. I considered and rejected homing the clear in the
  SPEC-0001 apply engine: REQ-007's done-when binds the clear to "the
  next `patina apply` … when its journal becomes the new truth", and
  the watcher already observes that truth-advance via the `journal_ts`
  it threads from the committed record (the rescan path updates it on
  every new `.COMMIT`). The drift handler is the existing seam the
  blocker named, so no `/speccy-amend` was needed.
- Completed (this round's amend): Implemented the clear-on-new-journal
  half of REQ-007's done-when in `patina-core/src/watch/drift.rs`. The
  round-1 code unconditionally rebound `cache.journal_ts` to the
  current timestamp, silently re-labeling superseded entries. Replaced
  that with a guarded clear: when the loaded cache's `journal_ts` no
  longer equals the journal timestamp the watcher is now reading
  against, the cache's `entries` are cleared (dropping the prior era's
  entries and their rate-limit ledger) before binding the new
  timestamp; a cache already bound to the current journal is left
  intact so the per-target rate limit still survives across batches
  within one journal era (DEC-004). A fresh/empty cache is constructed
  bound to the current timestamp, so the guard is a no-op there and
  nothing is cleared spuriously. No new dependency, no new public
  symbol, no signature change — the loop in `watch/mod.rs` already
  threads the current `journal_ts` (sourced from the committed record's
  `last_apply.at`, refreshed by `rescan` on each new `.COMMIT`), so the
  producer needed no plumbing change. Updated the module doc comment
  with a "Clear-on-new-journal" section so the prose matches the code.
- Undone: Nothing in REQ-007 is deferred. (This corrects the round-1
  "Undone: Nothing in REQ-007 is deferred" claim, which was wrong: the
  clear-on-new-journal done-when was in fact unimplemented in round 1.
  It is now implemented and tested.) The per-OS service install
  (T-011+) remains out of this task's scope.
- Hygiene checks (via `just lint` + `just test`, CI's exact commands):
  - `cargo test --workspace --locked` → exit 0 (227 lib + all
    integration suites pass; the 9 `watch::drift` unit tests include
    the two added this round)
  - `cargo clippy --workspace --all-targets --all-features -- -D warnings` → exit 0
  - `cargo +nightly fmt --all --check` → exit 0 (after applying
    `cargo +nightly fmt --all`, which reflowed the new doc comment)
  - `cargo deny check` → exit 0 (advisories ok, bans ok, licenses ok,
    sources ok — re-run; no dependency change this round)
  - (also `RUSTDOCFLAGS="-D warnings" cargo doc …` → exit 0, the
    `just lint` docs gate)
- Evidence: paper trail at `.speccy/specs/0003-patina-watch/evidence/T-010.md`
  (round-1 roll call still current). Round-2 delta for the previously
  unmet REQ-007 `<done-when>` bullet:
  - REQ-007 `<done-when>` "the next `patina apply` clears it when its
    journal becomes the new truth": demonstrated → new unit test
    `watch::drift::tests::a_journal_timestamp_advance_clears_prior_era_entries`
    seeds a drift entry under journal era `20260101T000000Z`, then runs
    a second batch under era `20260202T000000Z` against a different
    target and asserts the cache rebinds to the new era and holds
    exactly the new-era entry (the superseded entry is gone). Pre-fix
    this assertion fails with `entries.len() == 2` (stale entry
    persists, re-labeled); post-fix it is 1. Boundary guard against
    over-clearing: `watch::drift::tests::same_journal_era_preserves_the_rate_limit_ledger`
    runs two batches under the *same* `journal_ts` 30s apart and
    asserts the second is `RateLimited` with one notification — proving
    the clear does not fire within a journal era and DEC-004's ledger
    still survives.
  - CHK-011 / CHK-012 / CHK-018, DEC-004 rate-limit, DEC-008
    symlink-not-watched: unchanged from round 1 and still green; this
    round touched only the journal-era clear path.
- Discovered issues: none. The blocker's diagnosis was exact and the
  named seam (`handle_target_events`'s `journal_ts` rebind) was the
  right one; no SPEC ambiguity surfaced.
- Procedural compliance: TASKS.md `state` flipped pending →
  in-progress → in-review by editing the attribute only (no other
  hand-edit). The prior pass's WIP was amended in place — no
  `git restore`/`clean`/`checkout`, no rewrite-from-scratch, no state
  reset. No locked-SPEC edit. Journal is append-only: this round-2
  block was appended after the round-1 blockers; no earlier block was
  modified. `round="2"` increments the round-1 `round="1"` by exactly
  1. No `unwrap`/`expect`/panic in production. No new dependency, so
  no new `cargo deny` gate beyond the standard hygiene run.
</implementer>

<review persona="business" verdict="pass" model="claude-opus-4-8[1m]/high" date="2026-06-01T02:55:09Z" round="2">
Round-1 blocker is resolved. REQ-007's done-when "the next `patina apply` clears it when its journal becomes the new truth" is now implemented: `patina-core/src/watch/drift.rs` clears `cache.entries` (not merely rebinds the timestamp) when the loaded cache's `journal_ts` no longer matches the journal the watcher is reading against, then binds the new timestamp before upserting this batch's divergences. `journal_ts` is sourced from the committed record's `last_apply.at` and refreshed by `rescan` on each new `.COMMIT` (`watch/mod.rs`), so the clear fires on a real apply, matching the done-when's "when its journal becomes the new truth" semantics. The required test `a_journal_timestamp_advance_clears_prior_era_entries` asserts a journal-era advance drops the prior-era entry (would fail pre-fix with `len==2`, passes post-fix with `len==1`), and `same_journal_era_preserves_the_rate_limit_ledger` guards against over-clearing so DEC-004's per-target rate-limit ledger still survives within one journal era. The implementer also explicitly considered and rejected homing the clear in the SPEC-0001 apply engine with a documented rationale and corrected the wrong round-1 "Undone: Nothing deferred" claim, so no done-when was silently left unhomed. All other REQ-007 mappings (CHK-011/CHK-012/CHK-018, DEC-003/DEC-004/DEC-008/DEC-013) remain green end-to-end. No non-goal scope creep, no silently-resolved open question.
</review>

<review persona="tests" verdict="pass" model="claude-opus-4-8[1m]/high" date="2026-06-01T02:55:09Z" round="2">
Round-2 clear-on-new-journal behavior is genuinely gated. The round-1 blocker required (a) clearing superseded drift-cache entries on a journal-timestamp advance rather than merely rebinding the timestamp, and (b) a test asserting the advance drops prior-era entries. Both are delivered.

(a) `patina-core/src/watch/drift.rs` replaces the round-1 unconditional rebind with a guarded `cache.entries.clear()` + rebind fired only when `cache.journal_ts != journal_ts`. (b) `watch::drift::tests::a_journal_timestamp_advance_clears_prior_era_entries` seeds an `old.conf` entry under era `20260101T000000Z`, then runs a second batch against `new.conf` under era `20260202T000000Z` and asserts the cache rebinds and holds exactly one entry (the new-era target only, stale entry gone).

I verified this is not a vacuous test: I temporarily reverted the implementation to the round-1 unconditional rebind and the test failed with `left: 2, right: 1` — exactly the red-state the journal claims — then restored the file and confirmed it passes. The boundary test `same_journal_era_preserves_the_rate_limit_ledger` guards the opposite failure (always-clearing would break DEC-004's per-target rate limit across batches within one era); it asserts the second same-era detection is `RateLimited` with `count==1`, which an always-clear bug would fail. The real-loop seam matches the unit test: `watch/mod.rs` re-sources `journal_ts` from the rescanned `record.last_apply.at` on a new `.COMMIT` before dispatching the content-target batch to `handle_target_events`.

Evidence protocol satisfied: `Evidence:` field present on both implementer blocks; CHK roll call complete (CHK-011/CHK-012 demonstrated, CHK-018 hygiene with a verified cite `decodes_a_drift_cache_and_prints_version_timestamp_path_and_hashes`); all 9 drift unit tests pass; red/green materially different and reproduced empirically. Non-blocking note: the round-2 red/green paper trail lives in the journal's `<implementer round="2">` block rather than being appended as a new session to the evidence file — a hygiene gap, not a coverage gap.
</review>

<review persona="security" verdict="pass" model="claude-opus-4-8[1m]/high" date="2026-06-01T02:55:09Z" round="2">
No security blockers in the round-2 clear-on-new-journal change. The delta is the guarded entry-clear at `patina-core/src/watch/drift.rs` plus two `#[cfg(test)]` functions. It introduces no new sensitive-data exposure (the clear path logs nothing; the `drift` warn event still emits only the target path and one-way blake3 hex digests; the notification body names only the path), no new path construction, file read, deserialization, or injection vector, and no new dependency. The `journal_ts` driving the equality guard is sourced from the trusted committed journal record (`record…last_apply.at`, refreshed by rescan on each new `.COMMIT`), not attacker-controlled input, so the guard is not a spoofable boundary. Clearing the rate-limit ledger on a genuine journal advance is desktop-notification throttling, not an authorization throttle; the worst case is one extra benign notification per target after a re-apply. If anything, the change reduces stale-data exposure in the `patina debug drift-cache` view. Round-1 security pass remains valid.
</review>

<review persona="style" verdict="pass" model="claude-sonnet-4-6[1m]/medium" date="2026-06-01T02:55:09Z" round="2">
Round-2 diff is clean. The clear-on-new-journal fix is three lines in `handle_target_events` (`if cache.journal_ts != journal_ts { cache.entries.clear(); journal_ts.clone_into(...) }`), a matching "Clear-on-new-journal" doc section, and two new `#[cfg(test)]` functions. All consistent with the surrounding style.
- Naming: `a_journal_timestamp_advance_clears_prior_era_entries` and `same_journal_era_preserves_the_rate_limit_ledger` follow the existing verb-noun test-naming pattern.
- `.expect("descriptive message")` only in test functions — correct per `allow-expect-in-tests = true`.
- `clone_into` is the idiomatic form; `pub(super) hex_encode` reuse is already established from round 1.
- No suppression annotations, no dead imports, no reinvented helpers detected in the delta.
- The `let_chains` form matches the existing pattern in `patina-core/src/config/file_entry.rs`.
- Doc comment accurately describes the new guard behaviour and explains the "why" (prior entries measured against a superseded apply are stale and must not be re-labeled).
</review>

<review persona="correctness" verdict="pass" model="claude-opus-4-8[1m]/high" date="2026-06-01T02:55:09Z" round="2">
Round-2 clear-on-new-journal logic in `patina-core/src/watch/drift.rs` is correct. The guard `if cache.journal_ts != journal_ts { entries.clear(); journal_ts.clone_into(&mut cache.journal_ts); }` does a value comparison (`String` vs `&str`), so a same-era load leaves entries intact (DEC-004 rate-limit ledger preserved) and a new-era load drops the prior era's entries before rebinding. Ordering is right: the clear runs before the per-target `upsert_entry` loop, so this batch's divergences are added after stale entries are gone, never clobbered. The fresh/empty-cache path constructs the cache via `DriftCache::new(journal_ts, ...)` so the guard is a no-op there — nothing cleared spuriously. The rate-limit check (`recently_notified`) reads the post-clear cache, so the first divergence of a new era correctly always notifies while same-era suppression survives. The empty-`journal_ts` no-apply-yet case is harmless (`content_targets` empty → zero-iteration no-op). `journal_ts` is sourced from the committed record and refreshed by `rescan` on each new `.COMMIT`, and both `journal_ts` and `content_targets` are updated together, so no era/expectations skew is introduced. The two added tests assert the load-bearing boundaries (new-era drop → `entries.len()==1` with only the new target; same-era → `RateLimited`). No correctness defect at or above the reporting bar.
</review>
