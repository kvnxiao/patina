---
spec: SPEC-0003
task: T-007
generated_at: 2026-05-31T22:50:05Z
---

<implementer date="2026-05-31T22:50:05Z" model="claude-opus-4-8[1m]" round="1">
- Reuse survey: Mapped the task area — covered REQ-005 (CHK-009), the
  suggested files `patina-core/src/watch/subscriptions.rs` (new) and
  `patina-core/src/watch/mod.rs`, plus the record surface
  (`patina-core/src/journal/record.rs`) and the `watch/` neighbours
  (`drift_cache.rs`, `logging.rs`). Decisions:
  - reuse-as-is — `ExpectedTarget::source()` and the `ExpectedTarget`
    enum's `Symlink` / `Content` variants
    (`patina-core/src/journal/record.rs:96`,`50`): the canonical
    source-path accessor and the symlink-vs-content discriminant the
    REQ-005 / DEC-008 split keys on. No new accessor needed.
  - reuse-as-is — `ApplyRecord` (`record.rs:137`, re-exported at crate
    root) as the function input, and the `state.join("journal")`
    journal-directory convention already used at
    `patina-cli/src/cmd/promote.rs:75` and `cmd/remove.rs:85`. I joined
    `"journal"` onto the passed state dir to match, rather than inventing
    a new layout constant.
  - reuse-as-is — the `tracing::info!` field shape with
    `target: "patina_core"` and a trailing message string, mirroring the
    existing `tracing::debug!("fs_write_retry")` site in
    `patina-core/src/apply/retry.rs:73`.
  - write-fresh — `compute_subscriptions`: grepped the crate
    (`rg 'fn compute_subscriptions|subscription' patina-core/src`) before
    writing; nothing computes a watch path set, so this is genuinely new.
  New top-level symbols introduced and their accounting:
  `compute_subscriptions` (new — the search above came up empty; it is the
  pure record→path-set mapping the task asks for). No new types, errors,
  or constants — the function reuses `ApplyRecord`, `ExpectedTarget`,
  `Utf8Path`, and `Utf8PathBuf` end-to-end.

- Completed: Landed the pure subscription-set computation for REQ-005.
  Created `patina-core/src/watch/subscriptions.rs` exposing
  `compute_subscriptions(record: &ApplyRecord, state_dir: &Utf8Path)
  -> Vec<Utf8PathBuf>` (`#[must_use]`). For each recorded target it pushes
  the canonical `source()` and, only for `ExpectedTarget::Content`, the
  target path; symlink target paths are deliberately never pushed
  (DEC-008). It then appends `state_dir.join("journal")` (the
  journal-rescan subscription, REQ-005 `<done-when>`). Order is apply
  order with first-occurrence de-duplication via a small local
  `push_unique` closure. The computed set is emitted as a
  `watch_subscriptions` `tracing::info!` event (`target: "patina_core"`,
  fields `count` and a tab-joined `paths`) so the T-008 foreground watcher
  and CHK-009 can read it back from the log. Registered
  `pub mod subscriptions;` in `watch/mod.rs` (and updated its module doc)
  and re-exported `compute_subscriptions` from `patina-core/src/lib.rs`
  alongside the other `watch::` re-exports. Tests: five lib unit tests in
  `subscriptions.rs` — the two `<task-scenarios>`
  (`two_symlinks_one_content_yields_five_subscriptions`,
  `lone_symlink_omits_target_path`) plus three supporting
  (`content_target_subscribes_both_source_and_target`,
  `duplicate_source_paths_collapse`, `empty_record_yields_only_journal_dir`).

- Undone: No `notify` wiring, no debouncer, no journal-rescan-on-new-COMMIT
  loop — all deferred to T-008 (event loop, hands the set to the
  debouncer) and T-009 (re-reads the latest commit and recomputes the set
  on a journal-directory event, which is what CHK-017 inspects), per their
  `<task>` elements. This task is the pure mapping only, as scoped.

- Hygiene checks: `cargo test --workspace --locked` exit 0;
  `cargo clippy --workspace --all-targets --all-features -- -D warnings`
  exit 0; `cargo +nightly fmt --all --check` exit 0; `cargo deny check`
  exit 0 (advisories ok, bans ok, licenses ok, sources ok). The docs gate
  (`RUSTDOCFLAGS="-D warnings" cargo doc --workspace --no-deps`) also
  passed via `just lint`. No dependencies added.

- Evidence: paper trail at
  `.speccy/specs/0003-patina-watch/evidence/T-007.md`. Roll call for CHKs
  under REQ-005:
  - CHK-009 (running watcher's subscription list is exactly four watched
    paths plus the journal dir): demonstrated (path-set arithmetic) →
    evidence Scenario 1; unit test
    `two_symlinks_one_content_yields_five_subscriptions` proves the
    record→set mapping yields exactly the three sources + one content
    target + journal dir = five, with neither symlink target path present.
    The *running-watcher / log-inspection* surface of CHK-009 is
    judgment-deferred to T-008 (event loop + stderr layer); this task
    emits the `watch_subscriptions` info event T-008's harness reads.
  - CHK-017 (journal-rescan event on a new `.COMMIT` re-reads the latest
    commit and updates the subscription set): judgment-only for this task
    → deferred to T-009, which owns the live re-read loop; T-007 provides
    only the recompute function it will call.

- Discovered issues: none. The pre-existing duplicate-`winnow` `cargo deny`
  warning is unchanged by this task (no dependency added) and `cargo deny
  check` still exits 0.

- Procedural compliance: did not edit the locked `SPEC.md`; `TASKS.md`
  touched only to flip T-007's `state` attribute `pending` →
  `in-progress` → `in-review` (the latter as the final step of this turn).
  No shipped `.claude/` skill bodies edited. Journal is the sole home for
  this handoff prose; no `<implementer-note>` inlined in `TASKS.md`.
</implementer>

<review persona="business" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-05-31T22:52:49Z" round="1">
T-007 faithfully delivers the REQ-005 pure record→path-set mapping. `compute_subscriptions` (`patina-core/src/watch/subscriptions.rs:78`) watches every target's `source()`, adds content target paths, omits symlink target paths (DEC-008 / REQ-005 final `<done-when>` bullet), and appends `state_dir.join("journal")` as the journal-rescan subscription — matching SPEC-0001 REQ-016 layout and the `promote.rs:75`/`remove.rs:85` convention. Both rendered `<task-scenarios>` are covered by passing, non-vacuous unit tests (full ordered-vector + explicit symlink-target-absence assertions). The running-watcher log-inspection surface of CHK-009 and the live journal-rescan loop of CHK-017 are explicitly and correctly deferred to T-008 and T-009 (which `cover` them), not silently dropped. No scope creep, no dependency added, no open question silently resolved, no changelog-signalled intent drift.
</review>

<review persona="tests" verdict="pass" model="claude-opus-4-8[1m]/high" date="2026-05-31T22:52:49Z" round="1">
T-007's tests genuinely exercise the real `compute_subscriptions` (no mocks) and gate REQ-005/CHK-009's path-set arithmetic. Both `<task-scenarios>` map to executable unit tests with exact-ordered-vector assertions: `two_symlinks_one_content_yields_five_subscriptions` (patina-core/src/watch/subscriptions.rs:143) and `lone_symlink_omits_target_path` (subscriptions.rs:174). The load-bearing DEC-008 negative path (symlink target paths must never be subscribed) has explicit `!subs.contains(...)` assertions; I empirically confirmed via a mutation (pushing symlink target paths) that three tests fail and the originals pass again after restore, so the suite would catch an obviously-wrong reimplementation. Dedup, content-both-sides, and empty-record edges are each pinned. Evidence file at .speccy/specs/0003-patina-watch/evidence/T-007.md is honest: red phase shows the symbol was absent (rg exit 1), green phase cites the real test names present in the diff with a scoped `cargo test -p patina-core --lib watch::subscriptions` invocation (distinct from the workspace-wide hygiene run), and red/green are materially different — no fabrication pattern matched. Roll call covers CHK-009 (demonstrated, with the running-watcher log surface legitimately deferred to T-008's event loop) and CHK-017 (judgment-only, deferred to T-009's live journal-rescan loop); both deferrals are plausible since this slice is the pure record→path-set mapping with no `notify` wiring.
</review>

<review persona="security" verdict="pass" model="claude-opus-4-8[1m]/high" date="2026-05-31T22:52:49Z" round="1">
T-007 passes security review. The slice is a pure, side-effect-free path-set computation (`patina-core/src/watch/subscriptions.rs`) with no auth boundary, injection vector, crypto, secret handling, or race condition.

- Log exposure: the `watch_subscriptions` `tracing::info!` event (`subscriptions.rs:99-104`) emits filesystem paths only — source paths, content target paths, and `<state>/patina/journal`. The blake3 `expected_hash`/`actual_hash` digests on `ExpectedTarget::Content` are deliberately excluded (only `source()`/`target` strings flow into the set). No secret, token, or file-content plaintext leaks. Home-dir paths at `info` level are standard operational logging.
- No injection: input is an already-deserialized, Patina-written `&ApplyRecord` from the trusted committed journal; the only path join is `state_dir.join("journal")` with a fixed literal segment (no attacker-controlled traversal).
- DEC-008 boundary honored: symlink target paths are never subscribed; only their sources are.
- No new dependencies introduced by this task (the committed `Cargo.toml` deltas belong to T-006; no uncommitted manifest changes).
</review>

<review persona="style" verdict="blocking" model="claude-sonnet-4-6[1m]/medium" date="2026-05-31T22:52:49Z" round="1">
`patina-core/src/watch/subscriptions.rs:165` contains a vacuous assertion that violates the project's test hygiene rules (AGENTS.md, "Test hygiene" section, rule #5):

```rust
assert_eq!(subs.len(), 5);
```

This line appears immediately after an `assert_eq!` on lines 155-164 that compares `subs` against a vector literal containing exactly five elements. If the vector equality assertion passes, `subs.len() == 5` is trivially guaranteed and the redundant assertion "cannot fail in any interesting way." This matches the AGENTS.md prohibition: "Loose-outcome assertions any input passes. Assertions so permissive that any input satisfies them ... gate nothing." Remove the line.

The rest of the diff — the `compute_subscriptions` function, its `#[must_use]` annotation and message, the module-level doc comment, the `watch/mod.rs` registration, the `lib.rs` re-export, and the four other unit tests — all conform to project conventions.
</review>

<review persona="correctness" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-05-31T22:52:49Z" round="1">
T-007 `compute_subscriptions` is a correct pure record→path-set mapping. Control flow is straightforward and defect-free: per-target it pushes `source()` (defined for both `ExpectedTarget` variants) and, only for `Content`, the target path; symlink target paths are correctly omitted (DEC-008); the `<state>/patina/journal` rescan path is appended last. First-occurrence de-dup via `push_unique` is sound, empty-record and lone-symlink boundaries are handled, and there are no panics, narrowing casts, early returns bypassing cleanup, or resource/concurrency concerns. CHK-009 arithmetic checks out (3 sources + 1 content target + journal dir = 5) and the 5 lib tests plus the doctest pass. `patina-core/src/watch/subscriptions.rs:78`.
</review>

<blockers date="2026-05-31T22:52:49Z" round="1">
Remove the vacuous redundant length assertion before the next implementer pass.
- style: `patina-core/src/watch/subscriptions.rs:165` has `assert_eq!(subs.len(), 5)` immediately after a full ordered-vector equality assertion (lines 155-164) over a five-element literal. The length check is trivially implied by the vector equality and cannot fail independently — it violates AGENTS.md test-hygiene rule #5 (loose-outcome assertions that gate nothing). Delete the line. No other persona raised a blocker.
</blockers>

<implementer date="2026-05-31T23:10:00Z" model="claude-opus-4-8[1m]/medium" round="2">
- Reuse survey: The round-1 blocker is a test-hygiene fix, not a
  reuse-related one, so the full area-map is not re-run (round-agnostic
  per-symbol floor still applies). This amend introduces no new top-level
  symbol — it only deletes one line from an existing test — so there is no
  per-symbol accounting owed.

- Completed: Addressed the sole round-1 `style` blocker. Deleted the
  vacuous `assert_eq!(subs.len(), 5)` at
  `patina-core/src/watch/subscriptions.rs` (the line that previously sat
  immediately after the full ordered-vector equality assertion in
  `two_symlinks_one_content_yields_five_subscriptions`). The preceding
  `assert_eq!(subs, vec![...])` over the five-element literal already
  pins both the exact contents and the length, so removing the redundant
  length check loses no coverage while satisfying AGENTS.md test-hygiene
  rule #5. No production code touched; no other test altered.

- Undone: Nothing newly deferred. The same T-008 / T-009 scope boundaries
  from round 1 stand (no `notify` wiring, no debouncer, no live
  journal-rescan loop — those tasks `cover` the running-watcher and
  re-read surfaces).

- Hygiene checks: `cargo test --workspace --locked` exit 0;
  `cargo clippy --workspace --all-targets --all-features -- -D warnings`
  exit 0; `cargo +nightly fmt --all --check` exit 0; `cargo deny check`
  exit 0 (advisories ok, bans ok, licenses ok, sources ok — the
  pre-existing duplicate-`winnow` warning is unchanged and non-fatal).
  The docs gate also passed under `just lint`. Scoped re-run
  `cargo test -p patina-core --lib watch::subscriptions` shows all five
  subscription unit tests green.

- Evidence: paper trail at
  `.speccy/specs/0003-patina-watch/evidence/T-007.md` (round-1 red/green
  pair unchanged; this round is a test-only hygiene amend). Roll call for
  CHKs under REQ-005:
  - CHK-009 (running watcher's subscription list is exactly four watched
    paths plus the journal dir): demonstrated → unit test
    `two_symlinks_one_content_yields_five_subscriptions` still proves the
    record→set mapping yields exactly the three sources + one content
    target + journal dir, with neither symlink target path present; the
    removed line was redundant, the load-bearing ordered-vector assertion
    remains. The running-watcher / log-inspection surface stays
    judgment-deferred to T-008.
  - CHK-017 (journal-rescan event re-reads the latest commit and updates
    the subscription set): judgment-only for this task → deferred to
    T-009, which owns the live re-read loop.

- Discovered issues: none.

- Procedural compliance: did not edit the locked `SPEC.md`; `TASKS.md`
  touched only to flip T-007's `state` `pending` → `in-progress` →
  `in-review`. Amended the prior pass's WIP in place (no `git restore` /
  `clean` / `checkout`); did not rewrite the file from scratch. No shipped
  `.claude/` skill bodies edited. Journal is the sole home for this
  handoff prose.
</implementer>

<review persona="business" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-05-31T22:58:42Z" round="2">
T-007 faithfully delivers the in-scope REQ-005 record→path-set mapping. `compute_subscriptions` (`patina-core/src/watch/subscriptions.rs:78`) subscribes every target's canonical `source()`, adds content target paths, omits symlink target paths (DEC-008), and appends `state_dir.join("journal")` as the journal-rescan subscription required by REQ-005's fourth `<done-when>` bullet. This matches the 2026-05-29 changelog intent (read the committed record via REQ-029's `source()`, not the deleted `.plan`) and the open-question (b) resolution. Both rendered `<task-scenarios>` map to passing, non-vacuous unit tests with exact ordered-vector assertions plus explicit DEC-008 negative-presence checks. The running-watcher log-inspection surface of CHK-009 and the live journal-rescan loop of CHK-017 are genuinely (not silently) deferred to T-008 and T-009. No scope creep beyond the pure mapping, no dependency added, no open question silently decided. The sole round-1 blocker (the vacuous `assert_eq!(subs.len(), 5)`) is removed; the round-2 amend is test-only and the load-bearing ordered-vector assertion remains intact.
</review>

<review persona="tests" verdict="pass" model="claude-opus-4-8[1m]/high" date="2026-05-31T22:58:42Z" round="2">
T-007 round-2 passes tests review. The sole round-1 blocker (vacuous `assert_eq!(subs.len(), 5)` at the old line 165) is genuinely removed — no residual `subs.len()` assertion remains, and the load-bearing ordered-vector equality plus the two `!subs.contains(...)` DEC-008 negative assertions in `two_symlinks_one_content_yields_five_subscriptions` (`patina-core/src/watch/subscriptions.rs:143`) survive. Both `<task-scenarios>` map to executable unit tests that drive the real `compute_subscriptions` against genuine `ApplyRecord`/`ExpectedTarget` types (no mocks). I empirically mutated the impl to also subscribe symlink target paths (the exact DEC-008 violation CHK-009 forbids) and confirmed three tests fail — both scenario tests plus `duplicate_source_paths_collapse` — then restored and re-confirmed all five green, so the suite catches an obviously-wrong reimplementation. Evidence at `.speccy/specs/0003-patina-watch/evidence/T-007.md` is honest and unfabricated: red phase (symbol absent) corroborated by the diff, green phase cites test names present in the diff via a scoped per-slice invocation distinct from the workspace hygiene run, and red/green are materially different. Roll call covers CHK-009 (demonstrated; running-watcher log surface plausibly deferred to T-008) and CHK-017 (judgment-only; plausibly deferred to T-009's live re-read loop since this slice has no `notify` wiring).
</review>

<review persona="security" verdict="pass" model="claude-opus-4-8[1m]/high" date="2026-05-31T22:58:42Z" round="2">
T-007 round 2 passes security review. The slice remains a pure, side-effect-free record→path-set mapping (`patina-core/src/watch/subscriptions.rs`) with no auth boundary, injection vector, crypto, secret handling, or race condition; the round-2 delta is a single test-line deletion that introduces no new surface.

- Log exposure: the `watch_subscriptions` `tracing::info!` event (`subscriptions.rs:99-104`) emits filesystem path strings only — sources, content target paths, and `<state>/patina/journal`. The `blake3` `hash` field on `ExpectedTarget::Content` is destructured to `..` (`subscriptions.rs:90`) and never flows into the set, so no digest, token, or file-content plaintext leaks. Home-dir paths at `info` level are standard operational logging.
- No injection/traversal: input is an already-deserialized `&ApplyRecord` from the trusted committed journal; the only path join is `state_dir.join("journal")` with a fixed literal segment (`subscriptions.rs:97`) — no attacker-controlled traversal.
- DEC-008 boundary honored: symlink target paths are never subscribed (`subscriptions.rs:86-92`); only their sources are watched.
- No new dependencies introduced by this task.
</review>

<review persona="style" verdict="pass" model="claude-sonnet-4-6[1m]/medium" date="2026-05-31T22:58:42Z" round="2">
The round-1 blocker is resolved. `patina-core/src/watch/subscriptions.rs:165` (the vacuous `assert_eq!(subs.len(), 5)` immediately after the full ordered-vector equality assertion) has been deleted. The surviving assertions in `two_symlinks_one_content_yields_five_subscriptions` are the ordered-vector `assert_eq!` (which pins both contents and length) and two `assert!(!subs.contains(...))` negative-presence checks for the symlink target paths — all gate real invariants. No other code was changed. Conventions throughout the file remain correct.
</review>

<review persona="correctness" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-05-31T22:58:42Z" round="2">
T-007 `compute_subscriptions` (`patina-core/src/watch/subscriptions.rs:78`) is a correct, infallible record→path-set mapping. Per target it pushes `source()` (valid for both `ExpectedTarget` variants per `record.rs:96-100`) and only the `Content` target path; symlink target paths are correctly omitted (DEC-008). Journal-rescan path appended last; first-occurrence dedup via `push_unique` is sound; empty-record, lone-symlink, and duplicate-source boundaries all handled. No inverted/reordered match arms, no narrowing casts, no panics, no `Option`/`Result` mishandling, no early-return cleanup bypass, no concurrency or resource concerns. The round-1 style blocker (redundant `assert_eq!(subs.len(), 5)`) is removed with no correctness impact — the surviving ordered-vector assertion still pins contents and length. Five unit tests and the doctest pass.
</review>
