---
spec: SPEC-0003
task: T-013
generated_at: 2026-06-01T04:06:00Z
---

<implementer date="2026-06-01T04:06:00Z" model="claude-opus-4-8[1m]" round="1">
- Reuse survey: Mapped the task area (covered REQ-001 / REQ-003,
  suggested files `service/scheduled_task.rs`, `service/mod.rs`,
  `patina-core/Cargo.toml`, `tests/watch_service_cli.rs`, plus the
  sibling backends `service/launchd.rs` / `service/systemd.rs`, the
  `state_dir::HostOs` neighbour, and the existing `windows/registry.rs`
  winsafe pattern). Decisions:
  - reuse-as-is — the entire T-011 trait surface: `ServiceBackend`,
    `ServiceError` (`AlreadyInstalled` / `Supervisor` / `ResolveBinary`),
    `ServiceStatus`, `LifecycleResult`, the `FOREGROUND_ARGS` const,
    `canonical_binary_path`, and `recover_log_counters`;
    `state_dir::HostOs::Windows` for the factory dispatch; the winsafe
    `co` / `prelude` import style and the `co::HRESULT` / `co::CLSCTX`
    surface mirrored from `windows/registry.rs` and `windows/elevate.rs`
    (the latter is the existing winsafe COM/`CoCreateInstance` precedent
    in this crate, though it uses `ShellExecuteEx` rather than COM
    objects).
  - extend — `service/mod.rs`: added `#[cfg(windows)] pub mod
    scheduled_task;` and a `#[cfg(windows)] HostOs::Windows` factory arm
    (Windows no longer falls through to the `_ => unsupported` arm);
    updated the module + factory + `current()` doc prose that previously
    said Windows returns the unsupported stub. `patina-core/Cargo.toml`:
    added `"taskschd"` to the existing Windows `winsafe` feature list
    (was `["shell"]`), with the rationale comment extended.
    `tests/watch_service_cli.rs`: doc-comment-only update to the
    `not_installed_or_unsupported` helper (Windows now hits the real
    backend's not-installed no-op, not the unsupported stub) — no test
    behaviour change.
  - write-fresh — `ScheduledTaskBackend` and its lifecycle methods, the
    `render_task_xml` / `xml_escape` / `connect_service` / `task_exists`
    / `register_task` / `delete_task` / `query_task_status` /
    `is_not_found` / `supervisor_err` / `schtasks` free fns, the
    `TaskStatus` struct, and the `TASK_NAME` / `TASK_FOLDER` /
    `SCHED_S_TASK_HAS_NOT_RUN` consts, all in `scheduled_task.rs`.
    Searched the crate (`rg 'ScheduledTaskBackend|render_task_xml|fn
    is_not_found|TASK_NAME'`) — all empty before this task. `xml_escape`
    is NOT reused from `launchd::xml_escape` despite identical bodies:
    each is `#[cfg]`-gated to its own target OS (launchd is
    `target_os = "macos"`, this is `windows`), so a shared `pub(super)`
    helper would have to compile and be reachable on the OS whose backend
    is excluded — the same reason `systemd.rs` did not share it; the
    duplication is noted in the `xml_escape` doc comment. New top-level
    symbols all accounted for above. No `unsafe` (the workspace forbids
    it; `HRESULT::from_raw` is `unsafe`, so `is_not_found` takes the raw
    `u32` from `err.raw()` instead of an `HRESULT`).
- Completed: Landed the Windows per-user Scheduled Task backend behind
  the T-011 trait. Enabled `"taskschd"` on `patina-core`'s Windows
  `winsafe` dependency (was `["shell"]`, now `["shell", "taskschd"]`).
  `service/scheduled_task.rs` (`#[cfg(windows)]`): `install` registers a
  per-user task named `Patina Watcher` from Task Scheduler 2.0 XML
  (`render_task_xml`: an `OnLogon` `<LogonTrigger>`, a
  `<RunLevel>LeastPrivilege</RunLevel>` non-elevated principal, and an
  `<Exec>` action of the canonical binary + `watch --foreground`) via
  `ITaskService::Connect` → `NewTask` → `put_XmlText` →
  `ITaskFolder::RegisterTaskDefinition` under
  `TASK_LOGON::INTERACTIVE_TOKEN` in the current user's folder (`\`), all
  HKCU-scoped and non-elevated; `start` / `stop` drive `schtasks /run` /
  `/end` (this winsafe version exposes no `IRegisteredTask::Run`, so the
  liveness-changing actions shell out, mirroring how systemd shells to
  `systemctl`); `restart` is stop-then-start; `uninstall` best-effort
  ends then `ITaskFolder::DeleteTask`; `status` reads the registered
  task's `get_State` (RUNNING → running), `get_LastTaskResult` (sentinel
  `SCHED_S_TASK_HAS_NOT_RUN` → None), and `get_LastRunTime` (epoch → None)
  and recovers the log counters via `recover_log_counters`. `install` on
  an already-registered task returns `AlreadyInstalled` (exit 1);
  lifecycle calls on a not-installed task are `NotInstalled` no-ops. The
  factory in `mod.rs` routes `HostOs::Windows` to this backend. Because
  the module is `#[cfg(windows)]`, the macOS/Linux build excludes it
  entirely (task scenario 3).
- Undone: The live COM round-trip — `ITaskService::Connect` /
  `RegisterTaskDefinition` actually registering a task, `DeleteTask`
  removing it, and the `get_State` / `get_LastTaskResult` liveness reads
  — is not driven end-to-end from a test, for the same CI-isolation
  reason the T-011 launchd and T-012 systemd live registrations are not
  (it escapes the fixture tempdir and needs a real Windows Task
  Scheduler). T-014 (docs) remains for its own turn.
- Hygiene checks: `just test` (= `cargo test --workspace --locked`)
  exited 0 (all host suites green, including the 5 `watch_service_cli`
  integration tests). `just lint` exited 0, covering all four CI lint
  jobs: `cargo +nightly fmt --all --check` exited 0; `cargo clippy
  --workspace --all-targets --all-features -- -D warnings` exited 0;
  `RUSTDOCFLAGS="-D warnings" cargo doc --workspace --no-deps
  --all-features` exited 0; `cargo deny check` exited 0 (no new
  top-level dependency — `winsafe` was already in the Windows graph;
  this turn only enables its `taskschd` feature, which pulls
  `oleaut`/`ole`/`user` transitively and clears advisories / bans /
  licenses / sources). The `#[cfg(windows)]` module (5
  `scheduled_task::tests::*` unit tests) was additionally compiled,
  borrow-checked, and lint-checked clean via `cargo clippy --target
  x86_64-pc-windows-gnu --all-targets --all-features -- -D warnings`
  (zero `scheduled_task` findings); the Windows CI leg runs the
  assertions. The one remaining Windows-target finding,
  `apply/retry.rs:71 indexing may panic`, is pre-existing at `HEAD`
  (T-003 code) and outside T-013's scope.
- Evidence: paper trail at
  `.speccy/specs/0003-patina-watch/evidence/T-013.md`. Roll call for the
  CHKs under REQ-001 / REQ-003 (CHK-001 / CHK-002 / CHK-005 / CHK-006 are
  the macOS / Linux backends' scope, owned by T-011 / T-012):
  - CHK-003 (Windows install: HKCU-scoped `Patina Watcher` task exists
    with an `OnLogon` trigger and `RunLevel = Limited`, JSON `result =
    installed`): demonstrated + judgment-only. The task descriptor the
    live `RegisterTaskDefinition` consumes — the `<LogonTrigger>`, the
    `<RunLevel>LeastPrivilege</RunLevel>` (the schema spelling of
    `RunLevel = Limited` / non-elevated), and the `<Exec>` command +
    `watch --foreground` arguments — is demonstrated → evidence Scenario
    1's `render_task_xml_*` unit tests (compiled + lint-checked clean on
    the Windows target; run on the Windows CI leg). The live
    `ITaskService::Connect` / `RegisterTaskDefinition` COM round-trip and
    the `OnLogon`-trigger-exists query are NOT scriptable in CI (escape
    tempdir isolation, need a real Task Scheduler), so reviewer-business
    / reviewer-security judge the registration wiring on the diff.
  - Task scenario 3 (non-Windows build excludes the `taskschd`-gated
    code): demonstrated → evidence Scenario 2 (host `cargo check` + the
    full host hygiene gate pass with the `#[cfg(windows)]` module
    compiled out).
  - REQ-003 status object shape on Windows: hygiene + judgment-only. The
    not-found classification (`is_not_found`) and the sentinel mapping
    are gated by `is_not_found_recognizes_the_task_not_found_hresults`
    (a pure `u32` test, compiled on the Windows target); the live
    `get_State` / `get_LastTaskResult` reads are judged on the diff.
- Discovered issues: This winsafe version (0.0.27) exposes no
  `IRegisteredTask::Run` method, so `start` shells out to `schtasks /run`
  (and `stop` to `schtasks /end`) for the liveness-changing actions while
  the registration / deletion / query go through the `taskschd` COM API
  directly — flagged so the reviewer does not expect a pure-COM `Run`.
  Separately, the Windows-target clippy run surfaces a pre-existing
  `apply/retry.rs:71 indexing may panic` finding (T-003 code, present at
  `HEAD`); left untouched as out-of-scope per the surgical-change rule,
  and it does not fire on the host gate where that windows-only path is
  not compiled.
- Procedural compliance: This implementer entry lands directly in
  `journal/T-013.md` (created this turn with the three-field
  frontmatter); no `<implementer-note>` was written into TASKS.md. The
  TASKS.md `state="..."` attribute for T-013 flips `pending` →
  `in-progress` → `in-review` over this turn, with `in-review` set as
  the final step. No shipped skill bodies under `.claude/skills/` were
  edited.
</implementer>

<review persona="business" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-06-01T04:07:13Z" round="1">
T-013 satisfies REQ-001/REQ-003 done-when for the Windows Scheduled Task backend. Install registers a per-user `Patina Watcher` task via the `taskschd` COM API (`RegisterTaskDefinition` in folder `\` under `INTERACTIVE_TOKEN`, non-elevated) with an `OnLogon` `<LogonTrigger>`, `<RunLevel>LeastPrivilege</RunLevel>` (the correct XML spelling of TASK_RUNLEVEL_LUA = "Limited"/non-elevated, verified against MS docs), and an `<Exec>` of the canonical binary + `watch --foreground` — covering CHK-003. Already-installed exits 1 via `AlreadyInstalled`; lifecycle calls on a not-installed task return `NotInstalled` (CLI maps to exit 1 with the install hint); `status` reads `get_State`/`get_LastTaskResult`/`get_LastRunTime` and recovers log counters; `uninstall` ends-then-`DeleteTask`.

The flagged winsafe-0.0.27 workaround drops no required behavior: `IRegisteredTask` in 0.0.27 exposes no `Run()` method, so `start`→`schtasks /run` / `stop`→`schtasks /end` is forced. REQ-003's done-when names the task's "Run"/"End" actions (which `schtasks /run`//`/end` invoke), not a specific COM method, so the contract is met; this mirrors the systemd backend's `systemctl` shell-out precedent. No DEC governs the COM-vs-shell mechanism, the task stays HKCU-scoped/non-elevated in `patina-core` per REQ-001, and no open question was silently resolved. No scope creep; the pre-existing `apply/retry.rs:71` Windows finding was correctly left untouched.

Non-blocking note: `stop` shells to `schtasks /end` even though `IRegisteredTask::Stop()` exists in 0.0.27 — a defensible symmetry choice with `start`, still satisfying the "End action" contract.
</review>

<review persona="tests" verdict="blocking" model="claude-opus-4-8[1m]/xhigh" date="2026-06-01T04:07:13Z" round="1">
The Task Scheduler XML render is well-tested; the `get_State` / last-result state mapping is not, and the roll call miscites an unrelated test as covering it.

What is solid: `render_task_xml_declares_the_logon_trigger_run_level_and_exec_action` asserts the exact `<LogonTrigger>`, `<RunLevel>LeastPrivilege</RunLevel>`, `<Command>` = canonical binary, and `<Arguments>` = `watch --foreground` that CHK-003 / REQ-001's done-when name — non-vacuous. The well-formedness, escaping, and `is_not_found_recognizes_the_task_not_found_hresults` tests are likewise non-vacuous. Task scenario 3 (non-Windows build excludes the cfg-gated code) is demonstrated by the host hygiene gate. Evidence file is structurally sound, the cross-compile-only execution caveat is stated honestly, and every test name in the evidence appears in the diff.

The blocker: the status state-mapping in `query_task_status` (`scheduled_task.rs:255-273`) is untested, and it is exactly the surface this review must scrutinize. Three pure value predicates are fused inline into the live-COM read: `state == co::TASK_STATE::RUNNING` (liveness), `result != SCHED_S_TASK_HAS_NOT_RUN` (the never-run → `None` exit-code mapping), and `date != 0.0` (the epoch → `None` last-fired mapping). These map directly onto CHK-006's contract ("`last_exit_code` is the supervisor's most recent recorded value or `null` if never run"). Both sibling backends gate exactly this mapping with a dedicated unit test — `parse_launchctl_print_reports_none_exit_code_when_never_run` and `parse_systemctl_show_reports_none_when_never_run` — by extracting the supervisor-output → status mapping into a pure free function. T-011/T-012 thereby put this mapping on the demonstrated side of the CI line; T-013 inlined it and pushed it to judgment-only.

The "unsafe in CI" justification applies only to the COM round-trip (`Connect`/`RegisterTaskDefinition`/live `get_State`), which is legitimately non-scriptable. But the value-mapping of the read's result needs no live Task Scheduler: it is pure logic over `(TASK_STATE, i32, f64)`, demonstrably extractable exactly as the siblings extracted theirs. Drop the `result != SCHED_S_TASK_HAS_NOT_RUN` filter (never-run task now reports `Some(266499)` instead of `null`) or flip the `RUNNING` comparison — no test in this diff fails. The evidence roll call labels "REQ-003 status object shape on Windows" as `hygiene` and cites `is_not_found_recognizes_the_task_not_found_hresults`, but that test covers not-found classification, not the state mapping.

Required: extract the `(get_State, get_LastTaskResult, get_LastRunTime)` → status mapping into a pure free function (mirroring `parse_launchctl_print` / `parse_systemctl_show`) and add unit tests for the running, stopped, and never-run cases — at minimum the `SCHED_S_TASK_HAS_NOT_RUN` → `None` exit-code mapping (CHK-006) and the `TASK_STATE::RUNNING` → running liveness mapping.
</review>

<review persona="security" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-06-01T04:07:13Z" round="1">
Windows Scheduled Task backend is clean across all three scrutiny axes.

(1) XML injection: `render_task_xml` escapes both the `<Command>` binary path and the `<Arguments>` token string via `xml_escape` (all five metacharacters, `&` first to avoid double-escape), identical to the launchd sibling (`patina-core/src/watch/service/scheduled_task.rs`). Both interpolated values are non-attacker-controlled anyway (`canonical_binary_path()` from `current_exe`; hardcoded `FOREGROUND_ARGS`), and the escape holds regardless. The `render_task_xml_escapes_special_characters_in_the_binary_path` test asserts a raw `<c>` cannot survive as an element.

(2) `schtasks` construction: `Command::new("schtasks").args(args)` with an argv array, no shell; task name is the hardcoded `TASK_NAME` const passed as a discrete `/tn` token, so no flag/argument injection is reachable.

(3) COM privilege surface: `RegisterTaskDefinition` uses `TASK_LOGON::INTERACTIVE_TOKEN` with no stored password, XML principal is `InteractiveToken` + `RunLevel = LeastPrivilege`, registered in the current user's `\` folder under `Connect(None,None,None,None)` — HKCU-scoped and non-elevated, matching CHK-003. No elevation, no stored credentials. No `unsafe`, no secret handling, no sensitive data in error/log strings, and no new top-level dependency (the `taskschd` winsafe feature was deny-cleared per the journal).
</review>

<review persona="style" verdict="pass" model="claude-sonnet-4-6[1m]/medium" date="2026-06-01T04:07:13Z" round="1">
The `scheduled_task.rs` backend and its associated `mod.rs` / `Cargo.toml` / test-file changes are clean. Reuse survey verified: `xml_escape` is intentionally duplicated (not shared as `pub(super)`) because each backend is `#[cfg]`-gated to its own target OS; the doc comment explains this explicitly — the rationale is sound and the duplication is minimal. The `schtasks` command-runner parallels `launchctl`/`systemctl` in the sibling files; no reinvention. `ignore_fmt` from `launchd.rs` is rightly absent because `render_task_xml` uses `format!()` not a `write!` loop. `ScheduledTaskBackend` follows the `LaunchdBackend`/`SystemdBackend` pattern exactly. No `unwrap`, no `println!`, no `allow` suppressions, imports ordered consistently, public items have doc comments, tests assert real invariants.

Minor aside (non-blocking): `mod.rs` uses `#[cfg(windows)]` while the adjacent macOS/Linux attributes use `#[cfg(target_os = "macos")]` / `#[cfg(target_os = "linux")]`. The short form is idiomatic and functionally identical, but breaks local symmetry. Not worth a retry round.
</review>

<review persona="correctness" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-06-01T04:07:13Z" round="1">
No correctness defect found in the Windows Scheduled Task backend (`patina-core/src/watch/service/scheduled_task.rs`) or the `mod.rs`/`Cargo.toml`/test diffs.
- COM lifecycle is sound: `connect_service` returns `(CoUninitializeGuard, ITaskService)` and all four call sites bind the guard to a named `_guard` so it outlives every derived interface pointer; a bare `_` drop trap was avoided. `CoInitializeEx -> HrResult<CoUninitializeGuard>` confirmed for winsafe 0.0.27.
- Status reads type-check and map correctly: `get_State -> TASK_STATE` vs `RUNNING`, `get_LastTaskResult -> i32` filtered against `SCHED_S_TASK_HAS_NOT_RUN: i32` then lossless `i64::from`, `get_LastRunTime -> f64` epoch-sentinel `!= 0.0`. Read-error → None/false on the informational status path is documented (CHK-006), not a logic bug.
- Install `AlreadyInstalled` guard (with `TASK_CREATION::CREATE` no-clobber backstop), uninstall/start/stop/restart `NotInstalled` no-ops, and `restart = stop?;start?` all match the launchd/systemd sibling contract. `task_exists` correctly converts FILE/PATH_NOT_FOUND HRESULTs to `Ok(false)` and propagates other failures.
- `schtasks` shell-out maps spawn failure and non-zero exit to `ServiceError::Supervisor`; success returns `Ok(output)`. No unwrap/expect/panic/indexing anywhere in the file.
- The flagged `apply/retry.rs:71 indexing may panic` is genuinely out of scope and not a real defect: the `attempt >= BACKOFF_SCHEDULE_MS.len()` guard at line 66 returns early before the index at line 71, the code is T-003 (present at HEAD, untouched by this diff), and `scheduled_task.rs` does not reference `retry.rs`, so T-013 does not newly exercise that path.
</review>

<review persona="architecture" verdict="pass" model="claude-opus-4-8[1m]/high" date="2026-06-01T04:07:13Z" round="1">
T-013 lands the third `ServiceBackend` concrete (Windows `ScheduledTaskBackend`) with no trait churn: the shared `ServiceBackend` / `ServiceError` / `ServiceStatus` / `LifecycleResult` surface in `patina-core/src/watch/service/mod.rs` is byte-unchanged (diff adds only the `#[cfg(windows)] HostOs::Windows` factory arm and doc prose). COM-isms are fully contained in `scheduled_task.rs`; the shared mod.rs is winsafe/`co::`-free and the `taskschd` feature is scoped to `[target.'cfg(windows)'.dependencies]`. The factory arm slots between the Linux systemd-guard arm and the `_` fallback without disturbing the macOS/Linux arms. `HostOs` has three variants only (non-macOS/Windows folds into `Linux`), so the unsupported stub is now reached solely by non-systemd `HostOs::Linux` hosts per DEC-010 — exactly the intended narrowing. DEC-010 and DEC-012 honored; REQ-001 HKCU/non-elevated/`LeastPrivilege` satisfied, correctly keeping the backend in `patina-core` rather than `patina-elevate`. The `xml_escape` duplication and the `schtasks` shell-out for start/stop are both justified (cfg-disjoint modules; winsafe exposes no `IRegisteredTask::Run`) and parallel the systemd sibling, not premature abstraction. No new module cycle, no cross-layer call, no contradicted decision.
</review>

<blockers date="2026-06-01T04:07:13Z" round="1">
One blocking finding to clear: the Windows status state-mapping is inlined and untested, unlike its two sibling backends which extracted the mapping into a pure tested function.
- tests (blocking): The `query_task_status` state-mapping in `scheduled_task.rs` fuses three pure value predicates inline into the live-COM read — `state == co::TASK_STATE::RUNNING` (liveness), `result != SCHED_S_TASK_HAS_NOT_RUN` (never-run → `None` exit-code, CHK-006), and `date != 0.0` (epoch → `None` last-fired) — none of which is gated by a deterministic test. Dropping the `SCHED_S_TASK_HAS_NOT_RUN` filter (never-run reports `Some(266499)` instead of `null`) or flipping the `RUNNING` comparison leaves the entire suite green. Both sibling backends (T-011 `parse_launchctl_print`, T-012 `parse_systemctl_show`) extract this supervisor-output→status mapping into a pure free function with running/stopped/never-run unit tests, putting CHK-006's mapping on the demonstrated side of the CI line; T-013 pushed it to judgment-only, and the evidence roll call miscites `is_not_found_recognizes_the_task_not_found_hresults` (not-found classification, not state mapping) for "REQ-003 status object shape on Windows". The live COM round-trip (`Connect`/`RegisterTaskDefinition`/live `get_State`) legitimately stays judgment-only. Required: extract the `(get_State, get_LastTaskResult, get_LastRunTime)` → status mapping into a pure free function (mirroring the siblings) and add unit tests for at least the never-run `SCHED_S_TASK_HAS_NOT_RUN` → `None` exit-code mapping (CHK-006) and the `TASK_STATE::RUNNING` → running liveness mapping; re-label the status-shape clause in the evidence with a cite that actually covers the mapping.
</blockers>

<implementer date="2026-06-01T05:10:00Z" model="claude-opus-4-8[1m]/medium" round="2">
- Reuse survey: This retry addresses a reuse-related tests blocker, so
  the relevant area was re-surveyed against the two sibling backends.
  - extend — `scheduled_task.rs::query_task_status`: the inline
    `(get_State, get_LastTaskResult, get_LastRunTime)` → status mapping
    is lifted out; `query_task_status` now reads the three COM
    properties (degrading a per-property read error to that property's
    never-run sentinel via `unwrap_or`) and delegates the value-mapping
    to the new pure free fn.
  - write-fresh — `map_task_status(state, result, last_run) ->
    TaskReadout` and the `TaskReadout` struct. Per-symbol accounting:
    both mirror the sibling extractions `systemd::parse_systemctl_show`
    / `launchd::parse_launchctl_print` (and their `SystemctlShow` /
    `LaunchctlPrint` structs) — confirmed by reading both siblings
    (`rg 'fn parse_systemctl_show|struct SystemctlShow'`). `TaskReadout`
    is distinct from the existing `TaskStatus` because `TaskStatus`
    additionally carries the `installed` flag set by the not-found arm
    of `query_task_status`, which is not part of the pure mapping.
- Completed: Addressed the round-1 tests blocker. Extracted the Windows
  status state-mapping out of the live-COM read in `query_task_status`
  into the pure free fn `map_task_status(co::TASK_STATE, i32, f64) ->
  TaskReadout` (mirroring `parse_launchctl_print` /
  `parse_systemctl_show`), and added three deterministic unit tests:
  `map_task_status_reports_none_exit_code_when_never_run` (CHK-006: the
  `SCHED_S_TASK_HAS_NOT_RUN` + epoch sentinels map exit code and
  last-fired to `None`), `map_task_status_reports_running_for_the_running_state`
  (`TASK_STATE::RUNNING` → running, recorded code/timestamp surfaced),
  and `map_task_status_treats_a_non_running_state_as_stopped`
  (`READY` → stopped, non-zero exit code still surfaced). Dropping the
  sentinel filter or flipping the `RUNNING` comparison now fails a test.
  Re-labelled the evidence: added Scenario 3 ("status state-mapping")
  with a red/green pair citing the three `map_task_status_*` tests for
  CHK-006, replacing the round-1 miscite of
  `is_not_found_recognizes_the_task_not_found_hresults` (which covers
  not-found classification, not the state mapping).
- Undone: Nothing new. The live COM round-trip
  (`Connect` / `RegisterTaskDefinition` / live `get_State` /
  `DeleteTask`) stays judgment-only for the same CI-isolation reason as
  round 1 and the launchd / systemd siblings. T-014 (docs) remains for
  its own turn.
- Hygiene checks: `just test` (= `cargo test --workspace --locked`)
  exited 0. `just lint` exited 0, covering all four CI lint jobs:
  `cargo +nightly fmt --all --check` exited 0; `cargo clippy --workspace
  --all-targets --all-features -- -D warnings` exited 0;
  `RUSTDOCFLAGS="-D warnings" cargo doc --workspace --no-deps
  --all-features` exited 0; `cargo deny check` exited 0. The
  `#[cfg(windows)]` module (now eight `scheduled_task::tests::*` unit
  tests) was additionally compiled, borrow-checked, and lint-checked
  clean via `cargo clippy --target x86_64-pc-windows-gnu --all-targets
  --all-features -- -D warnings` (zero `scheduled_task` findings; the
  Windows CI leg runs the assertions). The pre-existing
  `apply/retry.rs:71 indexing may panic` Windows-target finding (T-003
  code, present at `HEAD`) remains the only `-D warnings` finding on
  that target and is out of T-013's scope.
- Evidence: paper trail at
  `.speccy/specs/0003-patina-watch/evidence/T-013.md` (Scenario 3 added
  this round). Roll call for the CHKs under REQ-001 / REQ-003 (CHK-001 /
  CHK-002 / CHK-005 are the macOS / Linux backends' scope, T-011 /
  T-012):
  - CHK-003 (Windows install: HKCU-scoped `Patina Watcher` task with an
    `OnLogon` trigger and `RunLevel = Limited`, JSON `result =
    installed`): demonstrated + judgment-only. The task descriptor is
    demonstrated → evidence Scenario 1's `render_task_xml_*` tests; the
    live `RegisterTaskDefinition` COM round-trip is judged on the diff.
  - CHK-006 (status `last_exit_code` is the supervisor's most recent
    recorded value or `null` if never run): demonstrated → evidence
    Scenario 3's `map_task_status_reports_none_exit_code_when_never_run`
    (never-run → `None`) plus the running / stopped mapping tests. The
    live `get_LastTaskResult` read itself is judgment-only.
  - Task scenario 3 (non-Windows build excludes the `taskschd`-gated
    code): demonstrated → evidence Scenario 2 (host hygiene gate passes
    with the `#[cfg(windows)]` module compiled out).
- Discovered issues: None new this round. The winsafe-0.0.27 missing
  `IRegisteredTask::Run` (start/stop shell to `schtasks`) and the
  pre-existing `apply/retry.rs:71` Windows finding both stand as flagged
  in round 1.
- Procedural compliance: This round-2 entry is appended after the
  round-1 `<implementer>` and `<blockers round="1">` blocks; no earlier
  block was modified. The prior pass's uncommitted WIP was amended in
  place (no `git restore` / `clean` / `checkout`). TASKS.md `state` for
  T-013 flips `pending` → `in-progress` → `in-review` this turn. No
  shipped skill bodies under `.claude/skills/` were edited.
</implementer>

<review persona="tests" verdict="pass" model="claude-opus-4-8[1m]/high" date="2026-06-01T04:16:44Z" round="2">
Round-1 tests blocker resolved. `map_task_status` (`patina-core/src/watch/service/scheduled_task.rs`) is a genuine pure free fn live-called by `query_task_status` (not a dead parallel copy) — the inline mapping is gone, the three COM reads degrade to sentinels then delegate. The three new tests gate the exact mutations the blocker named: `map_task_status_reports_none_exit_code_when_never_run` (asserts `last_exit_code == None` and `last_fired_at == None` for the `SCHED_S_TASK_HAS_NOT_RUN`/epoch sentinels — dropping the sentinel filter yields `Some(266499)` and fails; CHK-006); `map_task_status_reports_running_for_the_running_state` + `..treats_a_non_running_state_as_stopped` both fail if the `RUNNING` comparison is flipped. Shape mirrors the siblings' `parse_launchctl_print`/`parse_systemctl_show` and their `*_reports_none_*_when_never_run` tests; `TaskReadout` is correctly distinct from `TaskStatus` (the latter carries the `installed` flag from the not-found arm). Evidence Scenario 3 now cites the three `map_task_status_*` tests for CHK-006, replacing the round-1 miscite of `is_not_found_recognizes_the_task_not_found_hresults` (remaining `is_not_found` references correctly describe not-found classification only). Red/green halves are materially different; the cross-target clippy execution caveat is stated honestly; no fabrication pattern matches. Roll call covers every in-scope CHK (CHK-003, CHK-006, task scenario 3) with valid labels and specific cites.
</review>
