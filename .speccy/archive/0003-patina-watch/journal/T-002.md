---
spec: SPEC-0003
task: T-002
generated_at: 2026-05-31T21:30:00Z
---

<implementer date="2026-05-31T21:30:00Z" model="claude-opus-4-8[1m]/high" round="1">
- Reuse survey: Mapped the task area — covered REQ-006, the suggested
  files (`patina-core/src/clock.rs`, `patina-core/src/lib.rs`,
  `patina-cli/src/cmd/{apply,managed,remove}.rs`) and their immediate
  neighbours. Decisions:
  - reuse-as-is — the timestamp body itself
    (`jiff::Timestamp::now().strftime("%Y%m%dT%H%M%SZ").to_string()`)
    moved verbatim from `patina-cli/src/cmd/apply.rs`; reused `jiff`,
    already a `[workspace.dependencies]` member (and already in the
    lockfile via `patina-cli`/`init.rs`), adding it to
    `patina-core/Cargo.toml` introduces no new crate to the graph.
  - extend — `patina-core/src/lib.rs` re-export block: added
    `pub mod clock;` and `pub use clock::current_timestamp;` following
    the file's existing alphabetical `pub mod` / `pub use` pattern.
  - write-fresh — new top-level symbols: module `patina_core::clock`
    and `pub fn current_timestamp() -> String`. Searched
    `rg 'fn current_timestamp' patina-core/src` and
    `rg 'current_timestamp' patina-core` before writing — no prior
    timestamp helper existed in `patina-core`, so the hoist target is
    genuinely fresh (the only definition lived in the CLI).
- Completed: created `patina-core/src/clock.rs` with
  `pub fn current_timestamp() -> String` (same jiff `%Y%m%dT%H%M%SZ`
  formatting, module doc + doc example), wired `pub mod clock;` and
  `pub use clock::current_timestamp;` into `patina-core/src/lib.rs`,
  added `jiff = { workspace = true }` to `patina-core/Cargo.toml`.
  Updated the three CLI callers to call the hoisted
  `patina_core::current_timestamp`: `cmd/apply.rs` (now imports it and
  deleted its local `pub(crate) fn current_timestamp`), `cmd/managed.rs`
  and `cmd/remove.rs` (dropped the `use crate::cmd::apply::current_timestamp`
  re-import, added `use patina_core::current_timestamp`). Moved the
  `timestamp_is_compact_utc` unit test from `cmd/apply.rs` into
  `clock.rs`'s `#[cfg(test)]` module unchanged.
- Undone: nothing in scope deferred. The watcher re-apply that consumes
  this helper (REQ-006) lands in T-009, by design — this task is the
  pure hoist only.
- Hygiene checks: ran `just check` (the project's standard gate =
  `just lint` + `just test`, which runs CI's four lint jobs — nightly
  `fmt --all --check`, `clippy --workspace --all-targets --all-features
  -- -D warnings`, `RUSTDOCFLAGS="-D warnings" cargo doc --workspace
  --no-deps --all-features`, `cargo deny check` — then `cargo test
  --workspace --locked`). Exit code 0. Equivalent standard-gate mapping:
  - `cargo test --workspace`: exit 0 (includes the moved
    `clock::timestamp_is_compact_utc` unit test, the `clock` doctest, and
    the `deterministic_stdout` integration test, 3/3 passed).
  - `cargo clippy --workspace --all-targets --all-features -- -D warnings`:
    exit 0.
  - `cargo +nightly fmt --all --check`: exit 0 (fixed one stray trailing
    blank line in `cmd/apply.rs`'s test module left by removing the moved
    test).
  - `cargo deny check`: exit 0 (no new crate enters the graph — `jiff`
    was already resolved).
- Evidence: this is a no-behaviour-change refactor under REQ-006; the
  REQ's behavioural CHK (CHK-010, the 500ms-debounce single-re-apply log)
  is owned by T-009, not this task. Roll call for the CHK this task's
  scenarios gate:
  - CHK-010 (burst of five touches within 100ms coalesces to exactly one
    re-apply log event): judgment-only for T-002 → this task lands no
    watcher/debounce code; CHK-010 is demonstrated by T-009 which
    consumes `current_timestamp`. T-002's own task-scenarios are proven:
    `clock::timestamp_is_compact_utc` + the `clock` doctest assert the
    `^\d{8}T\d{6}Z$` shape (scenario 1), and `cargo build --workspace` +
    the passing `deterministic_stdout` integration test plus the
    grep-confirmed absence of any `fn current_timestamp` in `patina-cli/`
    prove scenario 2.
- Discovered issues: none.
- Procedural compliance: clean working tree at entry (first-attempt
  shape, no journal for T-002). Added a dependency (`jiff` to
  `patina-core`) and cleared it through `cargo deny check` per the hard
  rule; the dep was already a workspace member so no new crate entered
  the graph. No `unwrap`/`expect`/`panic` in production. No docs drift —
  no observable behaviour changed. Did not touch the locked SPEC.md.
</implementer>

<review date="2026-05-31T21:28:00Z" model="claude-opus-4-8[1m]/xhigh" persona="business" verdict="pass" round="1">
T-002 (REQ-006) delivers exactly the scoped pure hoist of `current_timestamp` from `patina-cli` to `patina-core::clock`. Both task scenarios pass: the compact-UTC shape is asserted by the moved unit test + new doctest, and `cargo build --workspace` + `deterministic_stdout` pass with zero `current_timestamp` definition or stale import remaining in `patina-cli` (verified via `rg`). The `%Y%m%dT%H%M%SZ` format is preserved verbatim, so deterministic stdout is intact. CHK-010 is correctly deferred to T-009 per the decomposition — not a T-002 gap. No scope creep (the lib.rs `version_envelope` re-exports are T-001's committed work; T-002's isolated diff is only the two-line clock wiring), and the diff matches the 2026-05-30 Changelog intent. No non-goal violated, no open question silently resolved.
</review>

<review date="2026-05-31T21:28:00Z" model="claude-opus-4-8[1m]/high" persona="tests" verdict="blocking" round="1">
The test substance for T-002 is sound, but the evidence contract is not satisfied: the journal `<implementer>` `Evidence:` field names no per-task evidence-file path, and no `.speccy/specs/0003-patina-watch/evidence/T-002.md` exists. The sibling task in this spec follows the canonical shape — `evidence/T-001.md` with captured red→green runner output, pointed at by T-001's `Evidence:` field. T-002 instead inlines a prose roll call in the journal with no captured runner output (red or green) anywhere. Per the evidence-loading contract (absence of the file at the referenced path is blocking), this is `verdict="blocking"`.

To clear: produce `.speccy/specs/0003-patina-watch/evidence/T-002.md` with the scoped per-test red→green capture (`cargo test -p patina-core clock::` for the moved `timestamp_is_compact_utc` unit test + the `clock` doctest, and `cargo test -p patina-cli --test deterministic_stdout` for the integration guard — not the workspace-wide hygiene run), and update the journal `Evidence:` field to name that path.

The underlying regression guards genuinely gate the invariant (block is about the missing artifact, not test quality): `clock.rs` `timestamp_is_compact_utc` + doctest are mutation-sensitive for Scenario 1 (wrong length / missing `Z` / misplaced `T` / ISO format all fail); `deterministic_stdout` drives the real `patina apply` binary through the genuine apply path that keys its journal `<ts>` through the hoisted fn, asserting byte-identical stdout; and `rg 'fn current_timestamp' patina-cli/` returns nothing (Scenario 2 structural claim verified). CHK-010 is plausibly `judgment-only` (no watcher/debounce code in T-002; owned by T-009).
</review>

<review date="2026-05-31T21:28:00Z" model="claude-opus-4-8[1m]/medium" persona="security" verdict="pass" round="1">
Pure no-behaviour-change refactor: hoists the filename-only compact-UTC timestamp helper from `patina-cli` to `patina_core::clock::current_timestamp`. No auth/authz, no input/injection surface, no secrets, no new logging or sensitive-data exposure (timestamp is journal/backup filename keying only, never user output). `jiff` added to `patina-core/Cargo.toml` is an already-resolved workspace dep — no new crate enters the graph, so no new advisory surface; `cargo deny check` reported clean. No security concerns.
</review>

<review date="2026-05-31T21:28:00Z" model="claude-sonnet-4-6[1m]" persona="style" verdict="pass" round="1">
The diff is clean and well-aligned with project conventions. `patina-core/src/clock.rs` follows the established module-doc pattern, carries `#[must_use]` with a custom message, and uses `.get(8)` in the `#[cfg(test)]` block rather than a panicking index. The doctest's direct `ts.as_bytes()[8]` is acceptable — doc prose, not production/test code. `lib.rs` inserts both `pub mod clock;` and `pub use clock::current_timestamp;` in alphabetical position. The three CLI callers switch to `use patina_core::current_timestamp` with no orphaned imports. `jiff = { workspace = true }` introduces no new crate to the resolver graph. Reuse survey present and honest.
</review>

<review date="2026-05-31T21:28:00Z" model="claude-opus-4-8[1m]/high" persona="correctness" verdict="pass" round="1">
Pure no-behaviour-change hoist verified correct. The function body in `clock.rs:25-29` is byte-for-byte identical to the original `patina-cli/src/cmd/apply.rs` definition (same `jiff::Timestamp::now().strftime("%Y%m%dT%H%M%SZ").to_string()`); only visibility changed (`pub(crate)`→`pub`), as required for the cross-crate move. The old definition is fully removed from `patina-cli` (no residual `fn current_timestamp`), and all three call sites resolve to the one hoisted `patina_core::current_timestamp`. Workspace builds clean; moved unit test and new doctest pass. No logic, control-flow, boundary, error-handling, or resource defects introduced.
</review>

<blockers date="2026-05-31T21:28:00Z" round="1">
Tests review blocks on a missing per-task evidence artifact (test substance is sound — this is a mechanical artifact gap, not a quality defect). Produce the evidence file and point the journal at it, then re-run the scoped gates.
- Create `.speccy/specs/0003-patina-watch/evidence/T-002.md` following the canonical red→green shape used by `evidence/T-001.md`, capturing scoped (not workspace-wide) runner output: a red→green for the moved `timestamp_is_compact_utc` unit test + `clock` doctest (`cargo test -p patina-core clock::`), and the `deterministic_stdout` integration guard (`cargo test -p patina-cli --test deterministic_stdout`). Include the CHK-010 roll call (judgment-only for T-002; owned by T-009).
- Update the round-2 `<implementer>` `Evidence:` field to name that path so the evidence-loading contract resolves.
</blockers>

<implementer date="2026-05-31T22:10:00Z" model="claude-opus-4-8[1m]/medium" round="2">
- Reuse survey: no new top-level symbol introduced this round — the
  amend is the per-task evidence artifact only, no production or test
  code changed. The round-1 full area-map stands; the blocker is not
  reuse-related, so no re-survey was owed. Per-symbol floor: N/A (no
  new symbol).
- Completed: addressed the round-1 tests-persona blocker (missing
  evidence artifact). Created
  `.speccy/specs/0003-patina-watch/evidence/T-002.md` following the
  canonical red→green shape of the sibling `evidence/T-001.md`, with
  scoped per-test capture (not the workspace-wide hygiene run):
  Scenario 1 red (pre-hoist `E0433: could not find clock in
  patina_core` compile failure) → green (`cargo test -p patina-core
  --lib clock::` and `cargo test -p patina-core --doc clock`, both
  captured passing), and Scenario 2 red (pre-hoist `rg 'fn
  current_timestamp' patina-cli/` matches `apply.rs`) → green (`rg`
  exit 1 / no match, plus `cargo test -p patina-cli --test
  deterministic_stdout` 3/3 passing). Included the CHK-010 roll call
  (judgment-only for T-002; owned by T-009).
- Undone: nothing. The round-1 WIP (the hoist itself) was sound per all
  five reviewer verdicts and is untouched this round — only the
  evidence file was missing.
- Hygiene checks: re-ran the four standard gates after the amend
  (capturing exit codes to file):
  - `cargo test --workspace`: exit 0.
  - `cargo clippy --workspace --all-targets --all-features -- -D warnings`:
    exit 0.
  - `cargo +nightly fmt --all --check`: exit 0.
  - `cargo deny check`: exit 0.
- Evidence: paper trail at
  `.speccy/specs/0003-patina-watch/evidence/T-002.md`. Roll call for
  CHKs under REQ-006:
  - CHK-010 (burst of five touches within 100ms coalesces to exactly
    one re-apply log event): judgment-only → T-002 lands no
    watcher/debounce code; CHK-010 is the behavioural contract owned by
    T-009, which consumes `current_timestamp`. T-002's own two task
    scenarios are demonstrated: Scenario 1 by the `clock::` unit test +
    doctest (compact-UTC shape), Scenario 2 by the `rg` structural
    check (no definition left in `patina-cli`) + the
    `deterministic_stdout` integration guard (byte-identical apply
    stdout through the hoisted fn) — see evidence Scenarios 1 and 2.
- Discovered issues: none.
- Procedural compliance: retry shape (journal carried an
  `<implementer round="1">` plus a matching `<blockers round="1">`),
  so the round-1 WIP in the working tree was amended in place — no
  `git restore`/`clean`/`checkout`, no rewrite from scratch, no state
  reset. The only file added this round is the evidence markdown; no
  production or test code changed. Did not touch the locked SPEC.md.
</implementer>

<review date="2026-05-31T21:33:55Z" model="claude-opus-4-8[1m]/xhigh" persona="business" verdict="pass" round="2">
T-002 (REQ-006) delivers exactly the scoped pure hoist of `current_timestamp` from `patina-cli` to `patina_core::clock`. Both task scenarios verified: compact-UTC `^\d{8}T\d{6}Z$` shape asserted by the moved unit test + doctest, and `rg 'fn current_timestamp' patina-cli/` returns exit 1 (no definition or stale import remains; all three callers resolve to the one hoisted symbol). `%Y%m%dT%H%M%SZ` moved verbatim, deterministic stdout intact. CHK-010 correctly deferred to T-009, not dropped. No non-goal violated, no scope creep, no open question resolved. Round-2 delta is the per-task evidence artifact only — no production code changed since round 1; the round-1 business verdict already passed.
</review>

<review date="2026-05-31T21:33:55Z" model="claude-opus-4-8[1m]/high" persona="tests" verdict="pass" round="2">
Round-1 blocker cleared. The per-task evidence artifact now exists at `.speccy/specs/0003-patina-watch/evidence/T-002.md` and the round-2 `<implementer>` `Evidence:` field names it. The file follows the canonical red→green shape with scoped (not workspace-wide) capture: Scenario 1 red is the pre-hoist `E0433: could not find clock in patina_core` compile failure, green is `cargo test -p patina-core --lib clock::` + `--doc clock`; Scenario 2 red is `rg 'fn current_timestamp' patina-cli/` matching `apply.rs`, green is `rg` no-match + `cargo test -p patina-cli --test deterministic_stdout`. Independently reproduced the green captures (unit test, doctest, all three `deterministic_stdout` tests) — output matches the evidence structure, not fabricated; red/green materially different; named symbols appear in the tree. Guards are mutation-sensitive (ISO-8601/wrong-length/misplaced-T/dropped-Z each fail; `deterministic_stdout` drives the real apply subprocess through the hoisted fn). CHK-010 correctly `judgment-only` (owned by T-009).
</review>

<review date="2026-05-31T21:33:55Z" model="claude-opus-4-8[1m]/medium" persona="security" verdict="pass" round="2">
Pure no-behaviour-change refactor confirmed: hoists the filename-only compact-UTC timestamp helper to `patina-core/src/clock.rs::current_timestamp`. No auth/authz, no input/injection surface (no-arg function over a fixed format string), no secret handling, no new logging, no sensitive-data exposure (timestamp keys filenames only, never user output). `jiff` added to `patina-core/Cargo.toml` is an already-resolved workspace dep — `Cargo.lock` shows only a new dependency edge, no new package entry, so no new advisory surface; `cargo deny check` clean. Round-2 added only the evidence markdown; no production code changed between rounds. No security concerns.
</review>

<review date="2026-05-31T21:33:55Z" model="claude-sonnet-4-6[1m]" persona="style" verdict="pass" round="2">
All style criteria pass. `patina-core/src/clock.rs` carries a module doc, a function doc with an example, and `#[must_use]` with a custom message — matching the project's conventions. The unit test uses `.get(8)` (safe indexing); the doctest's direct `as_bytes()[8]` is acceptable in doc prose. Trailing newline confirmed. The three CLI callers add `use patina_core::current_timestamp;` in alphabetical position within the `patina_core::` group, consistent with nightly `rustfmt`. `lib.rs` inserts `pub mod clock;` and `pub use clock::current_timestamp;` in their correct alphabetical slots. No dead code, no orphaned imports, no reinvention. Reuse survey present and honest.
</review>

<review date="2026-05-31T21:33:55Z" model="claude-opus-4-8[1m]/high" persona="correctness" verdict="pass" round="2">
Pure no-behaviour-change hoist, verified correct. `current_timestamp()` in `clock.rs:25-29` is byte-identical to the removed `patina-cli/src/cmd/apply.rs` definition (same `jiff::Timestamp::now().strftime("%Y%m%dT%H%M%SZ").to_string()`); only visibility changed (`pub(crate)`→`pub`). The sole definition now lives in `patina_core::clock` (`rg` confirms zero in `patina-cli`); all three callers resolve to the one hoisted symbol via fresh imports with stale re-imports dropped. Input-free, branch-free function — no logic, control-flow, boundary, Option/Result, concurrency, or resource-leak surfaces. Moved unit test and doctest pass. Round 2 added only the evidence markdown; production code untouched since the round-1 pass.
</review>
