---
spec: SPEC-0003
task: T-001
generated_at: 2026-05-31T20:30:00Z
---

<implementer date="2026-05-31T20:30:00Z" model="claude-opus-4-8[1m]/high" round="1">
- Reuse survey: Mapped the task area — covered REQ-007, suggested files
  (`patina-core/src/version_envelope.rs`, `journal/plan.rs`,
  `journal/record.rs`, `journal/mod.rs`, `lib.rs`,
  `tests/version_envelope.rs`) and their immediate neighbours
  (`journal/render.rs`, the `JournalError` enum). Decisions:
  reuse-as-is — kept the journal's `FILE_MAJOR_VERSION: u16 = 2`
  exactly where it lives in `journal/plan.rs` (the journal versions
  independently), and reused the existing `JournalError::{Truncated,
  VersionMismatch}` variants as the journal's unchanged public error
  vocabulary. Extend — `Plan::{encode,decode,read_envelope_version}`
  and `ApplyRecord::{encode,decode}` now delegate to the shared codec
  rather than each owning a private offset-0 LE-u16 implementation;
  `JournalError` grows a `From<EnvelopeError>` impl (no new public
  variant) so the journal maps the shared codec's two arms onto its
  own. Write-fresh — the `version_envelope` module: `ENVELOPE_LEN`,
  `encode_with_envelope`, `decode_envelope`, `read_envelope_version`,
  and `EnvelopeError`. Searched `rg 'fn encode_with_envelope|fn
  decode_envelope|enum EnvelopeError|mod version_envelope'` across the
  workspace — no pre-existing shared envelope helper; only the two
  inlined copies the task names. New top-level symbols introduced: the
  `version_envelope` module and its five public items above, plus the
  `From<EnvelopeError> for JournalError` impl — each is the extraction
  the task mandates, none is a parallel re-implementation.
- Completed: Created `patina-core/src/version_envelope.rs` — a
  format-agnostic envelope codec exposing `pub const ENVELOPE_LEN:
  usize = 2`, `encode_with_envelope(major: u16, body: &[u8]) -> Vec<u8>`
  (prepends the LE `u16`), `decode_envelope(bytes, supported_major) ->
  Result<&[u8], EnvelopeError>` (returns the post-envelope body slice or
  refuses a newer major), a `read_envelope_version` reader, and a
  `#[non_exhaustive]` `thiserror` `EnvelopeError { Truncated { got,
  need }, VersionMismatch { found, supported } }` modelling exactly the
  two arms the journal already distinguished. Wired `pub mod
  version_envelope;` into `patina-core/src/lib.rs` and re-exported
  `ENVELOPE_LEN`, `EnvelopeError`, `decode_envelope`,
  `encode_with_envelope`. Re-implemented `Plan::{encode,decode}` and
  `Plan::read_envelope_version` (`journal/plan.rs`) and
  `ApplyRecord::{encode,decode}` (`journal/record.rs`) over the helper,
  dropping their two private `ENVELOPE_LEN` consts and the duplicated
  offset-0 decode. Added `impl From<EnvelopeError> for JournalError`
  (`journal/mod.rs`) mapping `Truncated`→`Truncated` and
  `VersionMismatch`→`VersionMismatch`, so the journal's public error
  vocabulary is unchanged. Added integration test
  `patina-core/tests/version_envelope.rs` covering the three
  task-scenarios. No drift-cache code landed (per the task's explicit
  scope boundary).
- Undone: All SPEC-0003 watcher surface — the drift cache (T-004), the
  `patina debug drift-cache` subcommand (T-005), and the drift-detection
  loop (T-010) that the REQ-007 CHKs (CHK-011/012/018) acceptance-test
  end-to-end — is deliberately deferred to those tasks. T-001 lands only
  the shared helper and the journal refactor, so the new helper is
  ready for T-004 to build its own `DRIFT_CACHE_MAJOR_VERSION` envelope
  on. Nothing in T-001's own scope is deferred.
- Hygiene checks: `just test` (= `cargo test --workspace --locked`)
  exited 0 — 42 `test result: ok` blocks, zero failures, including the
  three new `version_envelope` integration tests, the seven new
  `version_envelope::tests::*` unit tests, and every pre-existing
  journal envelope test (`journal::plan::tests::*`,
  `journal::record::tests::*`, `commit_record.rs`, `debug_journal`)
  passing unchanged. `just lint` exited 0, covering all four gates in
  CI's order: `cargo +nightly fmt --all --check` (0), `cargo clippy
  --workspace --all-targets --all-features -- -D warnings` (0),
  `RUSTDOCFLAGS=-D warnings cargo doc --workspace --no-deps
  --all-features` (0), and `cargo deny check` (0 — `advisories ok, bans
  ok, licenses ok, sources ok`; no dependency added, so deny was clean).
- Evidence: red-then-green paper trail at
  `.speccy/specs/0003-patina-watch/evidence/T-001.md`. Roll call for the
  CHKs under REQ-007:
  - CHK-011 (watcher writes drift.cache entry + one notification):
    judgment-only for this task → the behaviour is delivered by T-010
    (drift loop) on top of T-004's cache format; T-001 lands neither
    watcher nor cache. No T-001 artifact proves it; it is out of this
    task's scope and is acceptance-tested when T-010 lands.
  - CHK-012 (`patina status --json` reports `drifted` from live
    re-hash): judgment-only for this task → `status` classification is
    SPEC-0001 REQ-018 behaviour exercised by T-010's scenario, not
    touched here.
  - CHK-018 (`patina debug drift-cache <path>` decodes + exits 0):
    judgment-only for this task → delivered by T-005's subcommand on
    top of T-004's `render_drift_cache`; not in T-001's scope.
  T-001's own deliverable (the shared envelope codec + journal refactor)
  is proved by the three `### Scenario N` blocks in the evidence file,
  mapping to the task's three `<task-scenarios>`: Scenario 1 (helper
  round-trip), Scenario 2 (newer-major refusal naming both versions),
  Scenario 3 (journal regression suite passes unchanged). These are the
  task-scenario acceptance criteria; the REQ-007 CHKs are downstream
  end-to-end checks the later watcher tasks own.
- Discovered issues: None. The two envelope copies the task names were
  the only duplication in the area; both are now collapsed onto the
  shared codec. The journal's `read_envelope_version` is retained as a
  thin `pub` wrapper because it is part of the journal's existing public
  surface; it now forwards to the shared reader.
- Procedural compliance: This entry lands directly in
  `journal/T-001.md` per the journal-file schema; no `<implementer-note>`
  was written into TASKS.md. The TASKS.md `state` attribute for T-001
  flips `pending` → `in-progress` → `in-review` across this turn, ending
  at `in-review`. No shipped skill bodies under `.claude/skills/`
  required edits during this task.
</implementer>

<review date="2026-05-31T21:11:19Z" model="claude-opus-4-8[1m]/high" persona="business" verdict="pass" round="1">
T-001 delivers exactly the slice of REQ-007 it is scoped to — the shared `pub version_envelope` helper extraction plus the journal refactor over it — and nothing more. The 2026-05-30 Changelog amendment (item 2) is reflected: a single format-agnostic codec with the major version passed as a parameter so the drift cache (T-004) can carry its own `DRIFT_CACHE_MAJOR_VERSION` independent of the journal's `FILE_MAJOR_VERSION` (kept at 2, untouched). No-behaviour-change contract holds via `From<EnvelopeError> for JournalError`; all journal regression tests pass unchanged. CHK-011/012/018 are correctly out of T-001's scope (T-004/T-005/T-010 own them) and the roll call attributes each rather than silently dropping it. No non-goal scope crept in; no open question silently resolved.
</review>

<review date="2026-05-31T21:11:19Z" model="claude-opus-4-8[1m]/high" persona="tests" verdict="pass" round="1">
T-001's three `<task-scenarios>` (helper round-trip, newer-major refusal naming both `found`/`supported`, journal regression suite unchanged) each map to a real, mutation-sensitive test. Independently reproduced the green output of `tests/version_envelope.rs` (3 tests, names matching evidence) and the journal envelope tests. Two mutations confirm the tests catch a broken impl: disabling the version check fails the refusal test; corrupting the body offset fails the round-trip test. Evidence roll call accounts for all three REQ-007 CHKs as judgment-only and the labels are plausible (no drift.cache, no debug subcommand, no watcher in the tree — they land in T-004/T-005/T-010). No fabrication: red is a genuine E0432 missing-module compile failure, green is real scoped cargo output, evidence command is per-test-binary scoped not the full-suite hygiene run.
</review>

<review date="2026-05-31T21:11:19Z" model="claude-opus-4-8[1m]/high" persona="security" verdict="pass" round="1">
Pure internal extraction of a version-envelope codec; no new attack surface. The `decode_envelope` path parses untrusted on-disk bytes and is panic-free: bounds-checks via `bytes.get(..ENVELOPE_LEN)` / `bytes.get(ENVELOPE_LEN..)` before reading, refuses newer majors before invoking the body decoder, and `copy_from_slice` runs only on a slice already proven `ENVELOPE_LEN` long. No `unwrap`/`expect`/`panic` in production. Error messages disclose only version numbers and byte counts. Behaviour is a 1:1 parity refactor (journal error mapping preserved, regression tests pass). No new dependencies, no crypto/auth/secret/logging/IPC surface. The unbounded `postcard::from_bytes` body is the deserialization boundary owned downstream and is neither introduced nor worsened here.
</review>

<review date="2026-05-31T21:11:19Z" model="claude-sonnet-4-6[1m]/medium" persona="style" verdict="blocking" round="1">
Two blocking findings:

1. `version_envelope.rs:142` — infallible step expressed as fallible (`rust-let-types-carry-invariants.md`). After `read_envelope_version(bytes)?` succeeds, the buffer is statically proven ≥ `ENVELOPE_LEN`; the comment even says "the body slice is total." Yet the next line uses `.ok_or(EnvelopeError::Truncated { … })` — a fallible API for a provably infallible operation, the exact anti-pattern the rule names a review blocker. Fix: `&bytes[ENVELOPE_LEN..]` directly (or `bytes.get(ENVELOPE_LEN..).expect("proven above")` if a panic-free guarantee is wanted).

2. `tests/version_envelope.rs:41-47` — vacuous constant-copy test (`envelope_sits_at_offset_zero_as_little_endian_u16`). Frames with `FILE_MAJOR_VERSION` and asserts the leading two bytes equal `FILE_MAJOR_VERSION.to_le_bytes()` — copies the production constant into the assertion and tests that `u16::to_le_bytes` round-trips with itself; cannot fail in any regression that still passes the Scenario 1 round-trip. AGENTS.md test hygiene prohibits copying production constants into tests. Either derive a real property (assert against the hardcoded literal `[2, 0]` to catch a BE swap) or drop the test (the offset-zero contract is already verified structurally by round-trip and version-mismatch tests).
</review>

<review date="2026-05-31T21:11:19Z" model="claude-opus-4-8[1m]/high" persona="correctness" verdict="pass" round="1">
No correctness defects found. The extraction is behaviour-preserving: `encode_with_envelope` produces byte-identical output to the inlined LE-u16 + body, and `decode_envelope` preserves the original error ordering (truncation → strict `found > supported` version-mismatch → body slice). The `From<EnvelopeError> for JournalError` impl maps both arms with exactly-matching field shapes. The strict-`>` comparison correctly admits equal and older majors and refuses only newer ones (no off-by-one). The unreachable `bytes.get(ENVELOPE_LEN..)` fallback is a style/infallible-as-fallible note, not a correctness bug, and returns the correct error type regardless. Full `patina-core` suite passes with zero failures.
</review>

<blockers date="2026-05-31T21:11:19Z" round="1">
Style review blocks on two project-rule violations; address both before the next implementer pass, then re-run `just lint` + `just test`.
- `version_envelope.rs` `decode_envelope`: the body-slice extraction after `read_envelope_version(bytes)?` uses `.ok_or(EnvelopeError::Truncated { … })` for a provably-infallible step (buffer already proven ≥ `ENVELOPE_LEN`). Replace with a direct `&bytes[ENVELOPE_LEN..]` (or `.expect("proven above")`) per `rust-let-types-carry-invariants.md` — do not express an infallible step as fallible.
- `tests/version_envelope.rs` `envelope_sits_at_offset_zero_as_little_endian_u16`: vacuous constant-copy test (asserts `FILE_MAJOR_VERSION.to_le_bytes()` against an encode of `FILE_MAJOR_VERSION`). Either assert against the hardcoded byte literal `[2, 0]` so a BE swap would fail, or drop the test — the offset-zero contract is already covered by the round-trip and version-mismatch scenarios.
</blockers>

<implementer date="2026-05-31T22:15:00Z" model="claude-opus-4-8[1m]/medium" round="2">
- Reuse survey: This is a non-reuse-blocker retry, so the round-1
  area-map is not re-run. Per the round-agnostic per-symbol floor, the
  only new top-level symbol this round introduces is the private
  `split_envelope(bytes: &[u8]) -> Result<(u16, &[u8]), EnvelopeError>`
  helper in `version_envelope.rs`. It is an internal extraction of logic
  that already lived (twice) inside `read_envelope_version` and
  `decode_envelope`, not a parallel re-implementation: searched
  `rg 'fn split_envelope|split_first_chunk' patina-core` — the codec is
  the only `split_first_chunk` user and no prior `split_envelope`
  existed. Both public functions now delegate to it, collapsing the two
  copies of the offset/length logic onto one bounds check.
- Completed: Addressed both round-1 style blockers by amending the
  round-1 WIP in place (no rewrite-from-scratch, no `git restore`).
  Blocker 1 (infallible-step-as-fallible): replaced the dead
  `bytes.get(ENVELOPE_LEN..).ok_or(Truncated…)` arm in `decode_envelope`
  with a single `split_first_chunk::<ENVELOPE_LEN>()` in the new private
  `split_envelope` helper, which destructures the fixed-size version
  array and the body tail together. The body slice now falls out of the
  same bounds check `read_envelope_version` already performed — no
  second lookup, no fallible-looking arm, and no `clippy::indexing_slicing`
  violation (a direct `&bytes[ENVELOPE_LEN..]` would have tripped that
  lint; `split_first_chunk` is the panic-free, infallible-by-pattern
  form the rule prescribes). `read_envelope_version` and
  `decode_envelope` both now read through `split_envelope`. Blocker 2
  (vacuous constant-copy test): deleted
  `tests/version_envelope.rs::envelope_sits_at_offset_zero_as_little_endian_u16`,
  which copied `FILE_MAJOR_VERSION` into both frame and assertion. The
  offset-zero/little-endian contract is still gated by the unit test
  `envelope_is_the_literal_leading_le_u16` (frames major `7`, asserts
  against the `7u16.to_le_bytes()` literal — no production constant) and
  by the round-trip and version-mismatch scenarios. The
  `FILE_MAJOR_VERSION` import remains used by Scenario 2.
- Undone: Nothing in T-001's scope is deferred this round. The wider
  SPEC-0003 watcher surface (T-004 drift cache, T-005 debug subcommand,
  T-010 drift loop) remains out of scope as in round 1.
- Hygiene checks: `just test` (= `cargo test --workspace --locked`)
  exited 0 — 42 `test result: ok` blocks, zero failures, including the
  now-2-test `tests/version_envelope.rs` and every unchanged journal
  envelope test. `just lint` exited 0 across all four gates in CI order:
  `cargo +nightly fmt --all --check` (0), `cargo clippy --workspace
  --all-targets --all-features -- -D warnings` (0 — `indexing_slicing`
  clean), `RUSTDOCFLAGS=-D warnings cargo doc --workspace --no-deps
  --all-features` (0), `cargo deny check` (0 — advisories/bans/licenses/
  sources ok; no dependency added).
- Evidence: amend paper trail appended as session
  `2026-05-31-T001-attempt-2` in
  `.speccy/specs/0003-patina-watch/evidence/T-001.md`, one block per
  blocker with the green gate output. Roll call for the CHKs under
  REQ-007 is unchanged from round 1 — CHK-011, CHK-012, CHK-018 remain
  judgment-only for T-001 (the drift cache, debug subcommand, and
  watcher loop that end-to-end exercise them land in T-004/T-005/T-010;
  no T-001 artifact proves them and none is in scope). T-001's own
  deliverable is proved by the three `### Scenario N` blocks from round
  1, all still green after the amend.
- Discovered issues: The naive blocker-1 fix (`&bytes[ENVELOPE_LEN..]`)
  the blocker suggested as one option trips `clippy::indexing_slicing`,
  which this project lints at `-D warnings`; the `split_first_chunk`
  form satisfies both the let-types-carry-invariants rule and the lint.
  No other issues.
- Procedural compliance: This entry is appended after the round-1
  `</implementer>`, `<review>` blocks, and `<blockers round="1">` in
  `journal/T-001.md`; earlier blocks are untouched (append-only). TASKS.md
  T-001 `state` flips `pending` → `in-review` this turn (the blocker had
  reset it to `pending`); no `<implementer-note>` was inlined into
  TASKS.md. No shipped `.claude/skills/` bodies were edited.
</implementer>

<review date="2026-05-31T21:19:22Z" model="claude-opus-4-8[1m]/high" persona="business" verdict="pass" round="2">
Round-2 amend is confined to the two round-1 style blockers (the private `split_envelope` helper refactor and deletion of the vacuous constant-copy test); neither touches any business-relevant surface. The 2026-05-30 Changelog (item 2) intent is intact: a single format-agnostic `pub version_envelope` codec with the major version passed as a parameter, so T-004 can carry its own `DRIFT_CACHE_MAJOR_VERSION` independent of the journal's `FILE_MAJOR_VERSION` (kept at 2, untouched). REQ-007 `<done-when>` envelope bullet satisfied. No-behaviour-change contract holds via `From<EnvelopeError> for JournalError`; all journal/core regression tests + doctests pass unchanged. Scenario 2's "name both versions" preserved. CHK-011/012/018 remain correctly out of scope (T-004/T-005/T-010 own them); roll call re-attributes each. No non-goal scope crept in.
</review>

<review date="2026-05-31T21:19:22Z" model="claude-opus-4-8[1m]/high" persona="tests" verdict="pass" round="2">
The round-1 style blocker that dropped `envelope_sits_at_offset_zero_as_little_endian_u16` (constant-copy tautology) and replaced its coverage with `envelope_is_the_literal_leading_le_u16` (`version_envelope.rs:165`) genuinely preserves the offset-zero LE invariant: mutating `encode_with_envelope` to `to_be_bytes()` fails the replacement test, so it is mutation-sensitive, and it asserts against the literal `7u16.to_le_bytes()` rather than the production `FILE_MAJOR_VERSION`, removing the tautology while keeping a real BE-swap guard. Offset-zero contract further covered by round-trip, version-mismatch (unit + integration), and the journal's own `envelope_carries_current_major_at_offset_zero`. All three `<task-scenarios>` map to real, mutation-sensitive tests; journal regression suite (14 tests) green after the `split_envelope`/`From` refactor. Evidence roll call accounts for all three REQ-007 CHKs as judgment-only; confirmed no drift-cache/watcher/debug code in tree. No fabrication.
</review>

<review date="2026-05-31T21:19:22Z" model="claude-opus-4-8[1m]/high" persona="security" verdict="pass" round="2">
Round-2 change is panic-free and introduces no new attack surface. The `decode_envelope` / `read_envelope_version` untrusted-bytes path now routes through the single `split_envelope` helper, whose only bounds check is `bytes.split_first_chunk::<ENVELOPE_LEN>()` — a total std method returning `None` (mapped to `Truncated` via `.ok_or(...)?`) on a short buffer, never panicking. Empty and 1-byte inputs rejected before the version is read (`truncated_buffer_is_rejected_before_decode` covers `[]` and `[0u8]`). `u16::from_le_bytes(*envelope)` runs on a statically-sized `[u8; 2]`; the body tail falls out of the same destructure with no second index, so no `unwrap`/`expect`/`panic`/`indexing_slicing` path. Newer-major refusal fires before `postcard::from_bytes`. Error messages disclose only version numbers and byte counts. The style blocker's alternate `&bytes[ENVELOPE_LEN..]` would have added an indexing-panic surface; the chosen `split_first_chunk` form is the safer fix.
</review>

<review date="2026-05-31T21:19:22Z" model="claude-sonnet-4-6[1m]/medium" persona="style" verdict="pass" round="2">
Both round-1 blockers are genuinely resolved. Blocker 1 (infallible-step-as-fallible): the new private `split_envelope` helper at `version_envelope.rs:94` uses `bytes.split_first_chunk::<ENVELOPE_LEN>()` — a single bounds check destructuring the fixed-size envelope array and body tail together; `decode_envelope` gets the body from the same destructure as the version read, no second fallible-looking extraction. `rust-let-types-carry-invariants.md` satisfied. Blocker 2 (vacuous constant-copy test): deleted; replacement `envelope_is_the_literal_leading_le_u16` frames with literal `7` and asserts against `7u16.to_le_bytes()` — no production constant copied, catches a BE regression. AGENTS.md test hygiene met. No new violations: all `.expect()` inside `#[cfg(test)]`; production panic-free; public items retain doc comments; private helper correctly needs none; no unused imports or dead code.
</review>

<review date="2026-05-31T21:19:22Z" model="claude-opus-4-8[1m]/high" persona="correctness" verdict="pass" round="2">
No correctness defects in the round-2 change. The `split_first_chunk::<ENVELOPE_LEN>()` rewrite in `split_envelope` (`version_envelope.rs:94`) is byte-for-byte behaviour-preserving versus round 1: same offset-0 LE-`u16` major (`u16::from_le_bytes(*envelope)`) and same `&bytes[ENVELOPE_LEN..]` body tail from one bounds check, with the `None` arm producing identical `Truncated { got, need }`. Truncation still strictly precedes the `found > supported_major` version-mismatch check; the round-1 dead second body-slice arm is removed but was provably unreachable, so no observable change. Empty-body round-trips to `b""`. Strict `>` admits equal/older, refuses only newer — no off-by-one. `From<EnvelopeError> for JournalError` maps both arms field-for-field; `Plan`/`ApplyRecord` delegate with `FILE_MAJOR_VERSION = 2` untouched. Full suite green, journal envelope regression tests pass unchanged.
</review>
