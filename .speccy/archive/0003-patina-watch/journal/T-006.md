---
spec: SPEC-0003
task: T-006
generated_at: 2026-05-31T22:39:26Z
---

<implementer date="2026-05-31T22:39:26Z" model="claude-opus-4-8[1m]" round="1">
- Reuse survey: Mapped the task area — covered REQ-009, suggested files
  `patina-core/src/watch/logging.rs` (new), `patina-core/src/state_dir.rs`,
  `Cargo.toml`, `patina-core/Cargo.toml`, `patina-core/tests/watch_logging.rs`,
  and the `watch/` module neighbours (`mod.rs`, `drift_cache.rs`).
  Decisions:
  - write-fresh — the lazy `logs/` creation: I deliberately did NOT extend
    `state_dir::create_tree` / `create_dir_idempotent` (the only sibling
    dir-creators; `rg 'fn create_dir|create_dir_all' patina-core/src`),
    because REQ-009 and the task body are explicit that `resolve` must NOT
    create `logs/` — the watcher owns it. I call `fs_err::create_dir_all`
    directly (the project's filesystem convention, the same primitive
    `create_dir_idempotent` wraps) inside `build_file_appender`.
  - write-fresh — the rolling appender: `tracing-appender` is a new
    dependency; searched the crate for any existing rotating-writer or
    appender helper and found none (`rg 'appender|rolling|RollingFile'
    patina-core/src` empty pre-edit).
  - write-fresh — `LoggingError`: searched `watch/` for a shareable error
    type; only `drift_cache::DriftCacheError` exists and its variants are
    drift-cache-specific, so a new `thiserror` enum modelled on
    `state_dir::StateDirError`'s `{path, source}` shape is appropriate.
  New top-level symbols introduced and their accounting: `LoggingError`
  (new — no existing error fits, modelled on `StateDirError`),
  `FileAppender` (new — bundles the `NonBlocking` writer + `WorkerGuard`
  the T-008 subscriber needs; no existing handle type), `build_file_appender`
  (new — the search above came up empty), plus the three module-private
  consts (`LOGS_DIR`, `FILENAME_PREFIX`, `MAX_LOG_FILES`).

- Completed: Landed the watcher's structured-log substrate (REQ-009).
  Added `tracing-appender = "0.2"` to root `[workspace.dependencies]` and
  `tracing-appender = { workspace = true }` to `patina-core/Cargo.toml`.
  Created `patina-core/src/watch/logging.rs` exposing
  `build_file_appender(&Utf8Path) -> Result<FileAppender, LoggingError>`,
  which lazily creates `<state>/patina/logs/` via `fs_err::create_dir_all`
  (idempotent) and builds a daily-rotating, keep-7
  `RollingFileAppender` (`Builder::new().rotation(Rotation::DAILY)
  .filename_prefix("watch.log").max_log_files(7)`), wraps it in
  `tracing_appender::non_blocking`, and returns the `NonBlocking` writer
  plus its `WorkerGuard` in a `#[must_use]` `FileAppender` struct for the
  watcher to hold for its process lifetime. Registered the module in
  `watch/mod.rs` (and updated its module doc) and re-exported
  `FileAppender` / `LoggingError` / `build_file_appender` from
  `patina-core/src/lib.rs` alongside the `drift_cache` re-exports. Updated
  the `state_dir.rs` module doc to note `logs/` is created by the watcher,
  not by `resolve`. Tests: two lib unit tests in `logging.rs` (creates-dir
  + writes-a-line; idempotent over an existing dir) and three integration
  tests in `patina-core/tests/watch_logging.rs` (resolve does NOT create
  `logs/`; first init creates `logs/` and writes a rotating file;
  idempotent over an existing `logs/`).

- Undone: The watcher event loop, re-apply dispatch, and the stderr layer
  that compose this file appender into a live `tracing` subscriber and
  emit the `re_apply` events are deliberately deferred to T-008
  (`patina watch --foreground`) per its `<task>` element — the task body
  scopes T-006 to the sink only ("keep this function free of `notify`
  wiring" is T-007's analogue; T-006 is the appender). `tracing-subscriber`
  is intentionally NOT added here — T-008 adds it when it builds the
  layered subscriber. Nothing in T-006's own scope is deferred.

- Hygiene checks: `cargo test --workspace --locked` exited 0 (all
  workspace tests + doctests pass, including the 2 new lib unit tests and
  3 new integration tests). `cargo clippy --workspace --all-targets
  --all-features -- -D warnings` exited 0 (fixed two `indexing_slicing`
  hits in the new tests by switching `vec[0]` to `.first().expect(..)`).
  `cargo +nightly fmt --all --check` exited 0. `cargo deny check` exited 0
  (the new `tracing-appender` dependency clears advisories/bans/licenses/
  sources; pre-existing duplicate-crate warnings are unchanged and covered
  by `multiple_crate_versions = "allow"`).

- Evidence: red-then-green paper trail at
  `.speccy/specs/0003-patina-watch/evidence/T-006.md`. Roll call for the
  CHK under REQ-009:
  - CHK-014 (foreground watcher emits exactly three info-level `re_apply`
    events with distinct `re_apply.id` on stderr): judgment-only for this
    task → deferred to T-008. CHK-014 inspects the watcher's *stderr* for
    `re_apply` events, which requires the T-008 event loop, re-apply
    dispatch, and the stderr `tracing` layer — none of which exist yet.
    T-006 lands only the file sink those events will be written into; it
    cannot demonstrate CHK-014. The evidence file's three `### Scenario`
    blocks (demonstrated) instead prove the task's own three
    `<task-scenarios>`: `resolve` does not create `logs/`,
    `build_file_appender` lazily creates `logs/` and writes a daily-rotating
    file, and `cargo deny check` passes with the new dependency. CHK-014 is
    carried here as deferred so reviewer-tests sees the gap is intentional
    and owned by T-008, not glossed.

- Discovered issues: None. The `state_dir` module already correctly
  avoided creating `logs/` before this task; T-006 only adds the doc note
  pinning that as intentional and the watcher-side creator.

- Procedural compliance: This implementer entry lands directly in
  `journal/T-006.md` per the journal-file schema; no `<implementer-note>`
  was written into TASKS.md (the parser rejects that element). The TASKS.md
  `state="..."` attribute for T-006 flips from `in-progress` to `in-review`
  as the final step of this turn. No shipped skill bodies under `.claude/`
  required edits during this task.
</implementer>

<review persona="business" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-05-31T22:43:40Z" round="1">
T-006 faithfully delivers the slice of REQ-009 / DEC-009 it scopes: lazy `<state>/patina/logs/` creation, daily-rotating keep-7 `tracing-appender` sink, and the guarantee that `state_dir::resolve` does NOT create `logs/`. Verified in working tree: `build_file_appender` lazily creates the dir (`patina-core/src/watch/logging.rs:109-130`, `Rotation::DAILY` + `max_log_files(7)`); `resolve` creates only root/journal/backups with a doc note pinning `logs/` ownership (`patina-core/src/state_dir.rs:188-190`, diff adds the note at lines 23-26); `tracing-appender` added to both Cargo.tomls; `cargo deny check` clears (advisories/bans/licenses/sources ok); all 5 task tests pass (2 lib unit, 3 integration). No non-goal scope crept in (no ring buffer, no metrics subcommand, no HTTP endpoint). CHK-014 (three info-level `re_apply` events on watcher stderr) is correctly deferred to T-008/T-009 — it requires the event loop, re-apply dispatch, and stderr layer that this task explicitly does not own; the deferral is recorded in the journal, not glossed. The task's own three `<task-scenarios>` are all covered. The stray `jiff` line in the committed `patina-core/Cargo.toml` diff belongs to T-002, not this task's working-tree changes — out of scope.
</review>

<review persona="tests" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-05-31T22:43:40Z" round="1">
T-006's three task-scenarios are each driven by a real test, not a mock. `patina-core/tests/watch_logging.rs` and the `logging.rs` lib unit tests exercise the actual `tracing-appender` non-blocking writer end-to-end (write line → flush → assert exactly one `watch.log`-prefixed file containing the line); two independent mutations I applied (wrong filename prefix; decoy sink) both make the test fail at a precise assertion, confirming it is load-bearing. The negative scenario (`resolve` must NOT create `logs/`) is real: `create_tree` provably only creates `journal/`+`backups/`, so `resolve_creates_journal_and_backups_but_not_logs:48` would fail if a `logs/` write were added. `cargo deny check` exits 0 (Scenario 3). Evidence at `.speccy/specs/0003-patina-watch/evidence/T-006.md` is present, names tests that appear verbatim in the diff, shows a materially different red (E0425 / `rg` exit 1) vs green transition, and uses scoped per-slice commands rather than the full hygiene run. CHK-014 (REQ-009's only scenario) is labelled `judgment-only` — correctly so: it requires the T-008 foreground stderr layer and T-009 `re_apply` dispatch, neither of which exists in this file-sink-only slice; not mine to block on and the deferral is honest.
</review>

<review persona="security" verdict="pass" model="claude-opus-4-8[1m]/high" date="2026-05-31T22:43:40Z" round="1">
T-006 lands only the watcher's log sink (`patina-core/src/watch/logging.rs`) and is clean from a security standpoint.

- New dependency `tracing-appender 0.2.5` (tokio-rs maintained, MIT) clears `cargo deny check` on all four gates (advisories, bans, sources, licenses ok); only pre-existing benign winnow duplicate / unmatched-license-allowance warnings remain.
- No injection or path-traversal vector: `logs/` is a fixed-constant join onto a caller-resolved `&Utf8Path`, with a hardcoded filename prefix; no user-controlled path component.
- Directory creation via `fs_err::create_dir_all` matches the existing sibling convention (`journal/`, `backups/` in `state_dir.rs:189-190`), which likewise sets no explicit mode and relies on the already-private per-machine state root + umask — no permissions regression introduced.
- No log records are emitted in this slice, so no sensitive-data-exposure surface here. The `drift.path`/`expected_hash`/`actual_hash` content that T-009/T-010 will write to this sink is where log-content scrutiny belongs; that is correctly out of scope for T-006.
- `WorkerGuard` is correctly surfaced behind `#[must_use]` so buffered lines are not silently dropped (reliability, not security, but handled well).

No auth boundary, secret handling, or crypto in this task.
</review>

<review persona="style" verdict="pass" model="claude-sonnet-4-6[1m]/medium" date="2026-05-31T22:43:40Z" round="1">
All style checks pass. The implementation follows established project conventions throughout:

- `LoggingError` variants follow the `{path, source}` struct shape of `StateDirError` and `DriftCacheError` siblings.
- `FileAppender` is `#[must_use]` with a descriptive message, matching `LockGuard` in `lock.rs`.
- The `#![expect(clippy::expect_used, reason = "...")]` suppression in `watch_logging.rs` is byte-identical to the pattern in `backup_retention.rs`, `backups.rs`, and other sibling integration test files — suppression carries its required justification.
- No `unwrap()` in production code; `.expect()` in tests only.
- `tracing-appender` wired into both root `[workspace.dependencies]` and `patina-core/Cargo.toml` as `workspace = true`, consistent with the workspace dependency pattern.
- Flat re-exports in `lib.rs` (`FileAppender`, `LoggingError`, `build_file_appender`) follow the same pattern as the `watch::drift_cache` re-exports landed in T-004/T-005.
- The module-level `# Examples` block in `logging.rs` covering `build_file_appender` matches how `drift_cache.rs` handles its primary public functions.
- Reuse survey is present and honest: `fs_err::create_dir_all` is the correct primitive (same one `create_dir_idempotent` wraps), new error type is justified, no reinvention found.
</review>

<review persona="correctness" verdict="pass" model="claude-opus-4-8[1m]/high" date="2026-05-31T22:43:40Z" round="1">
No correctness defects found in T-006's logging sink at the >=80 confidence bar.

`build_file_appender` (`patina-core/src/watch/logging.rs:109`) is linear and sound: `create_dir_all` is correctly idempotent (pre-existing `logs/` is not an error), both `?` short-circuits map to the right `LoggingError` variant (`CreateLogDir` / `Appender`) and neither bypasses an acquired guard/lock since no resource is held before either fallible step, and `non_blocking` returns `(writer, guard)` with both moved into the returned `FileAppender` — no dropped `WorkerGuard`, no leak, and `#[must_use]` guards caller misuse. Unit and integration tests pass.

Noted but below the blocking bar: `max_log_files(7)` (`logging.rs:121`) maps REQ-009's "keep 7 most recent" literally, while tracing-appender 0.2.5 documents that retention "can sometimes dip below the maximum" and suggests `m+1` to guarantee `m`. This is a transient under-retention window at rotation boundaries, not wrong results / loss / crash, and the literal "keep 7 -> 7" reading is defensible; it is a spec-intent question for the business lane rather than a correctness defect.
</review>
