---
spec: SPEC-0004
task: T-012
generated_at: 2026-06-01T23:38:03Z
---

<implementer date="2026-06-01T23:38:03Z" model="claude-opus-4-8[1m]/high" round="1">
- Reuse survey: Mapped the task area — the live docs surface (`docs/USER_GUIDE.md`,
  `docs/ARCHITECTURE.md`, `docs/OPERATING_ENVIRONMENT.md`, `README.md`) and the
  four locked-SPEC requirement bodies named in the task (SPEC-0001 REQ-005 /
  REQ-008 / REQ-009, SPEC-0002 REQ-002). This is a docs/prose task: no production
  Rust changed, so the three-tier code survey is vacuous (no new top-level symbol).
  To keep the documented schema honest I sourced the ground-truth vocabulary from
  the shipped code rather than from the SPEC prose: the user-facing mode names and
  the kind-typed split from `patina-core/src/config/file_entry.rs` (module docs +
  `FileMode` / `EntryKind`), the `--symlink-tree` `add` flag from
  `patina-cli/src/cli.rs` (`AddArgs`) and `patina-cli/src/cmd/add.rs`, the variable
  layering from `patina-core/src/config/root.rs` and the precedence comment in
  `patina-core/src/apply/engine.rs` (`CLI > per-machine > per-profile > per-module >
  repo-shared > built-ins`), and the unified `when` engine + undefined-variable
  behaviour from the SPEC's CHK-006 / CHK-020 / CHK-021 scenarios. reuse-as-is /
  extend / write-fresh: not applicable (no symbols introduced).
- Completed: Refreshed the docs so they no longer describe the old single
  `[[file]]` table or the `symlink-dir` / `copy-tree` mode names, and added the
  cross-SPEC superseded-by notes.
  - `docs/USER_GUIDE.md` "Declaring dotfiles": rewrote the schema section to
    document the `[[file]]` / `[[directory]]` kind-typed table-array split, the
    collapsed shared mode names (`symlink` default / `copy`, plus `symlink-tree`
    for directories), and the implicit `.tmpl` render; added a `[[directory]]`
    `symlink-tree` example. Added a new "Conditional entries with `when`"
    subsection (per-entry `when`, the one unified MiniJinja predicate engine
    across templates / `[[auto_match]]` / entries, strict-undefined errors at
    every `when` site including the `patina.oss` typo case, and the
    `patina.profile`-undefined-during-resolution caveat). Added a "Variables"
    subsection documenting the repo-shared `[variables]` + per-profile
    `[profiles.<name>.variables]` layering and the full precedence chain.
    Fixed the `add` / `remove` command-table rows and the drift-notifications
    "copy-tree files" phrasing.
  - `docs/ARCHITECTURE.md`: replaced "the five file modes" with the
    `[[file]]` / `[[directory]]` entry-kinds-and-modes phrasing.
  - `README.md`: updated the `add` command-table row to name both entry kinds.
  - Superseded-by notes (locked-SPEC cosmetic cross-references, no requirement
    semantics changed): SPEC-0001 REQ-005 (schema + the missing-source timing
    promotion to plan time), REQ-008 (auto_match evaluator), REQ-009 (the `when`
    engine extension); SPEC-0002 REQ-002 (`add` now emits `[[file]]` or
    `[[directory]]` by source kind). Each points to the relevant SPEC-0004 REQ.
- Undone: Nothing in T-012's scope is deferred. Historical/changelog references
  to `symlink-dir` / `copy-tree` inside SPEC-0001/0002 changelog rows and the
  original (now annotated) requirement prose are deliberately left intact — the
  task scenario permits occurrences "in changelog/historical or superseded-by
  context".
- Hygiene checks: ran the project's standard gate via `just check`
  (`just lint` + `just test`), which runs CI's gates in CI's order and stops at
  first failure.
  - `just lint` (nightly `fmt --all --check`; clippy
    `--workspace --all-targets --all-features --locked -- -D warnings`;
    `RUSTDOCFLAGS="-D warnings" cargo doc --workspace --no-deps --all-features`;
    `cargo deny check`): exit 0. The `cargo doc -D warnings` docs gate — the one
    the task specifically calls for to catch broken intra-doc links — passed; I
    re-ran `just lint-docs` standalone to confirm (exit 0, no broken links).
  - `just test` (`cargo test --workspace --locked`): exit 0; all unit /
    integration / doctest suites passed.
- Evidence: no separate `evidence/T-012.md` paper trail — this is a docs-only
  task with no red/green behavioural pair to capture; the proof is the docs gate
  plus the grep-based scenario checks. T-012 has no covered CHKs of its own
  (its `<task-scenarios>` are doc-content assertions, not SPEC CHK ids). Roll call
  against the four task scenarios:
  - Scenario 1 (no live `symlink-dir` / `copy-tree` outside historical/
    superseded context): demonstrated → `rg "symlink-dir|copy-tree"` across
    `docs/` + `README.md` returns one hit, the superseded-by parenthetical in
    `docs/USER_GUIDE.md:77`, which is exactly the permitted superseded context.
  - Scenario 2 (schema reference documents both table-arrays, collapsed modes +
    `symlink-tree`, per-entry `when`, both variable layers): demonstrated →
    the rewritten `docs/USER_GUIDE.md` "Declaring dotfiles" + "Conditional
    entries with `when`" + "Variables" subsections cover all four.
  - Scenario 3 (affected SPEC-0001/0002 requirement bodies each carry a
    superseded-by note to SPEC-0004): demonstrated → REQ-005, REQ-008, REQ-009
    in SPEC-0001 and REQ-002 in SPEC-0002 each open with a blockquote pointer.
  - Scenario 4 (`just lint` `cargo doc -D warnings` passes, no broken intra-doc
    links): hygiene → `just lint-docs` exit 0, captured above.
- Discovered issues: none new. The SPEC-0001 / SPEC-0002 changelog rows still
  use the old mode spellings in their historical narrative; that is correct and
  in-scope-to-leave (changelog is an append-only historical record).
- Procedural compliance: this entry lands directly in `journal/T-012.md` per the
  journal-file schema; no `<implementer-note>` was written into TASKS.md. The
  TASKS.md `state` for T-012 flips `in-progress` → `in-review` as the final step
  of this turn. The locked SPEC-0001 / SPEC-0002 `SPEC.md` edits are limited to
  cosmetic superseded-by cross-references in requirement bodies that do not change
  any `<requirement>` / `<done-when>` / `<scenario>` semantics — permitted under
  the locked-SPEC exception and explicitly directed by the T-012 task body. No
  shipped `skills/` bodies required edits.
</implementer>

<review date="2026-06-01T23:42:05Z" model="claude-opus-4-8[1m]/medium" persona="business" verdict="pass" round="1">
T-012 docs + cross-SPEC superseded-by task satisfies all four task-scenarios and respects the locked-SPEC boundary.
- Scenario 1: `rg "symlink-dir|copy-tree"` across `docs/` + `README.md` returns one hit, `docs/USER_GUIDE.md:77`, the explicit "no longer accepted" superseded-context note — the permitted exception.
- Scenario 2: `docs/USER_GUIDE.md` documents both `[[file]]` and `[[directory]]` table-arrays, the collapsed `symlink`/`copy` modes plus `symlink-tree`, per-entry `when` (incl. the strict-undefined typo error path and the `patina.profile`-during-resolution caveat), and the repo-shared `[variables]` + per-profile `[profiles.<name>.variables]` layers. The documented precedence chain matches SPEC-0004 REQ-005 exactly.
- Scenario 3: SPEC-0001 REQ-005/008/009 and SPEC-0002 REQ-002 each carry an accurate superseded-by/extended-by blockquote pointing to the right SPEC-0004 REQ.
- Scenario 4: `just lint-docs` (`cargo doc -D warnings`) exit 0, no broken intra-doc links.
- Locked-SPEC boundary (critical check): `git diff --numstat` shows 28/0 insertions/deletions in SPEC-0001 and 7/0 in SPEC-0002 — zero deletions. Every hunk inserts a `>` note immediately after a `### REQ-NNN:` heading; no requirement/done-when/scenario content is altered. Cosmetic cross-references only.
No excluded scope snuck in; no silently-resolved open questions.
</review>

<review date="2026-06-01T23:42:05Z" model="claude-sonnet-4-6[1m]/medium" persona="docs" verdict="blocking" round="1">
`docs/ARCHITECTURE.md` line 121-122, "Apply phases" section, bullet 1 still reads:
    paths, and produce an ordered list of operations across the five file
    modes.
The implementer's journal states this phrase was replaced with the `[[file]]` / `[[directory]]` entry-kinds-and-modes phrasing, but the file was not updated at that location. "Five file modes" is inaccurate against the shipped schema: the user-facing collapsed mode names are now three (`symlink` / `symlink-tree` / `copy`), the executor-facing `FileMode` enum has six variants, and entries are kind-typed across two table-arrays. The engine-layer module doc at line 43-44 of the same file WAS correctly updated to say "the `[[file]]` / `[[directory]]` entry kinds and their materialization modes," making the stale phrase in the Apply Phases section internally inconsistent.
All other items pass: USER_GUIDE.md correctly documents the split, collapsed modes plus `symlink-tree`, per-entry `when`, and the variable precedence chain. README.md `add` row names both entry kinds. The single `symlink-dir`/`copy-tree` occurrence in `docs/USER_GUIDE.md:77` is in the permitted superseded context. SPEC-0001 REQ-005/008/009 and SPEC-0002 REQ-002 each carry the required superseded-by blockquote.
Fix: update `docs/ARCHITECTURE.md` line 121-122 to remove "the five file modes" and replace with phrasing consistent with the `[[file]]` / `[[directory]]` kind-typed entry model.
</review>

<review date="2026-06-01T23:42:05Z" model="claude-opus-4-8[1m]/high" persona="tests" verdict="pass" round="1">
T-012 is docs-only and correctly added no tests. Working-tree diff touches only `docs/USER_GUIDE.md`, `docs/ARCHITECTURE.md`, `README.md`, and the two locked SPEC bodies — zero `.rs`/test files. No forbidden substring-grep-the-docs test for `symlink-dir`/`copy-tree` was introduced; Scenario 1 was verified via a manual `rg` check (one permitted superseded-context hit), not a brittle prose-assertion test. The pre-existing `patina-cli/tests/docs_structure.rs` is a CommonMark structural-anchor test (the sanctioned shape), and the USER_GUIDE rewrite kept its required H2 headings (the new subsections are `###`). Hygiene confirmed green: `just lint-docs` (`cargo doc -D warnings`) exit 0 with no broken intra-doc links, and `just test` (full workspace suite) exit 0 — the existing behavioral suite is what proves the documented schema matches real behavior.
</review>

<review date="2026-06-01T23:42:05Z" model="claude-opus-4-8[1m]/high" persona="security" verdict="pass" round="1">
No security surface. T-012 is docs-only: prose rewrites in USER_GUIDE.md/ARCHITECTURE.md/README.md and additive superseded-by blockquotes in the locked SPEC-0001/SPEC-0002 requirement bodies. No production code, dependency, or security-relevant config changed. The new TOML examples demonstrate no insecure pattern (no secrets, injection, or path-traversal). The documented strict-undefined `when` semantics are fail-closed. The superseded-by edits are purely additive forward-pointers; none deletes or weakens an existing requirement, and the missing-source plan-time promotion note explicitly retains the materialize-time existence check as a TOCTOU backstop.
</review>

<review date="2026-06-01T23:42:05Z" model="claude-sonnet-4-6[1m]/medium" persona="style" verdict="pass" round="1">
All style criteria pass. No new diagrams were introduced (ARCHITECTURE.md already uses Mermaid; USER_GUIDE.md has none) — no Mermaid-vs-ASCII drift. All new fenced blocks use `toml` or `sh` correctly. All three new TOML examples validated as syntactically correct. The four superseded-by notes use the same `> **Bold lead sentence.**` blockquote pattern; lead-verb variation ("Superseded in part by", "Extended by") is semantically accurate. New schema vocabulary used throughout; the only `symlink-dir`/`copy-tree` occurrence in `docs/` is the permitted historical parenthetical at USER_GUIDE.md:77. No dead content or naming drift. Reuse survey present and correctly scoped to a docs-only task.
</review>

<review date="2026-06-01T23:42:05Z" model="claude-opus-4-8[1m]/high" persona="correctness" verdict="pass" round="1">
All TOML/CLI examples and behavioral claims in the doc diff are valid against the shipped code. Verified: the `[[directory]]` `mode = "symlink-tree"` example parses (`file_entry.rs:240`); the documented per-table mode allowlists match the `from_raw_file`/`from_raw_directory` resolvers exactly; the `when = "patina.os == 'windows'"` example is a real evaluable predicate and `patina.os`/`patina.hostname` are genuine built-ins; the strict-undefined `patina.oss`-typo-errors claim matches `template/strict.rs`; the `patina.profile`-undefined-during-resolution caveat matches `builtins.rs`/`profile.rs`; and the documented precedence chain, read lowest-to-highest, reverses to the resolver's fixed order `CLI > per-machine > per-profile > per-module > repo-shared > built-ins` (`variables/mod.rs:122-123`). The `add` row claims match `cmd/add.rs`. No correctness defects.
</review>

<blockers date="2026-06-01T23:42:05Z" round="1">
Fix the stale "five file modes" phrase in ARCHITECTURE.md before the next review pass.
- docs (blocking): `docs/ARCHITECTURE.md:121-122` ("Apply phases" section, bullet 1) still reads "produce an ordered list of operations across the five file modes." This is stale against the shipped kind-typed schema (collapsed user-facing modes are three: `symlink`/`symlink-tree`/`copy`; entries are split across `[[file]]`/`[[directory]]`), and it is internally inconsistent with line 43-44 of the same file which was correctly updated. Replace "the five file modes" with phrasing consistent with the `[[file]]`/`[[directory]]` kind-typed entry model and their materialization modes. Re-run `just lint` to confirm the docs gate stays green. (business, tests, security, style, correctness all passed.)
</blockers>

<implementer date="2026-06-01T23:58:00Z" model="claude-opus-4-8[1m]/high" round="2">
- Reuse survey: No new top-level symbol introduced (docs-only amend), so the
  per-symbol floor is vacuous. The round-1 blocker was not reuse-related, so the
  full area-map is not re-run per round semantics.
- Completed: Amended the prior WIP in place — addressed the single round-1 docs
  blocker. `docs/ARCHITECTURE.md` "Apply phases" bullet 1 (formerly line 121-122)
  now reads "produce an ordered list of operations across the `[[file]]` /
  `[[directory]]` entry kinds and their materialization modes," matching the
  already-correct module-doc phrasing at line 43-44 and the shipped kind-typed
  schema. Swept the touched docs for surviving old-taxonomy phrasing
  (`rg "five file modes|symlink-dir|copy-tree|five modes|the five"` across
  `docs/*.md` + `README.md`): the only remaining hit is the permitted
  superseded-context parenthetical at `docs/USER_GUIDE.md:77`. No other stale
  phrasing survives.
- Undone: Nothing in scope deferred. The historical `symlink-dir`/`copy-tree`
  references in SPEC-0001/0002 changelog/requirement prose remain intentionally
  intact (permitted historical/superseded context, unchanged from round 1).
- Hygiene checks: per the blocker's scoping, re-ran the full `just lint` to keep
  the `cargo doc -D warnings` docs gate green.
  - `just lint` (nightly `fmt --all --check`; clippy
    `--workspace --all-targets --all-features --locked` cross-compiled to the
    linux + windows-gnu targets; `RUSTDOCFLAGS="-D warnings" cargo doc`;
    `cargo deny check`): exit 0. The `cargo doc -D warnings` docs gate passed
    with no broken intra-doc links; `cargo deny check` reported "advisories ok,
    bans ok, licenses ok, sources ok" (the printed warnings are pre-existing
    unmatched-license-allowance / duplicate-lockfile-entry advisories unrelated
    to this prose edit).
  - `just test` (`cargo test --workspace --locked`): not re-run this round — a
    one-line prose edit to `docs/ARCHITECTURE.md` changes no compiled artifact
    or test fixture, and the round-1 entry already recorded `just test` exit 0;
    the blocker scoped the re-run to `just lint` for the docs gate.
- Evidence: docs-only amend, no red/green behavioural pair to capture; T-012 has
  no covered CHKs of its own (its `<task-scenarios>` are doc-content assertions).
  Roll call against the round-1 blocker and the affected task scenario:
  - Round-1 docs blocker (stale "five file modes" in ARCHITECTURE.md): demonstrated
    → the phrase is replaced with the kind-typed phrasing; `rg` sweep confirms no
    surviving live occurrence in the touched docs.
  - Scenario 4 (`just lint` `cargo doc -D warnings` passes, no broken intra-doc
    links): hygiene → full `just lint` exit 0 this round.
- Discovered issues: none new.
- Procedural compliance: retry-mode amend — edited the existing dirty WIP in
  place (no `git restore`/`clean`/`checkout`, no rewrite-from-scratch, no state
  reset). This entry appends after the round-1 `<blockers>`; round monotonically
  increments 1 → 2. TASKS.md `state` for T-012 flips `pending` → `in-progress`
  → `in-review` this turn. No locked-SPEC edits this round.
</implementer>

<review date="2026-06-01T23:46:09Z" model="claude-sonnet-4-6[1m]/medium" persona="docs" verdict="pass" round="2">
Round-1 blocker resolved. `docs/ARCHITECTURE.md` lines 121-123 now read "produce an ordered list of operations across the `[[file]]` / `[[directory]]` entry kinds and their materialization modes" — matching the already-correct module-doc phrasing at lines 43-44 exactly. Sweep confirms no surviving stale taxonomy: `rg "five file modes|five modes|symlink-dir|copy-tree|the five"` across `docs/` and `README.md` returns exactly one hit — `docs/USER_GUIDE.md:77` — the permitted superseded-context parenthetical. All four task scenarios still hold (Scenario 1: only the permitted historical note; Scenario 2: USER_GUIDE documents both table-arrays, collapsed modes plus `symlink-tree`, per-entry `when`, both variable layers; Scenario 3: SPEC-0001 REQ-005/008/009 and SPEC-0002 REQ-002 carry the superseded-by blockquote; Scenario 4: `cargo doc -D warnings` exit 0). No new stale phrases introduced.
</review>

<review date="2026-06-01T23:46:09Z" model="claude-opus-4-8[1m]/medium" persona="business" verdict="pass" round="2">
T-012 round-2 retry satisfies all four task-scenarios and respects the locked-SPEC cosmetic boundary; the sole round-1 docs blocker is resolved. Blocker fix: `docs/ARCHITECTURE.md:121-123` "Apply phases" bullet now reads kind-typed `[[file]]` / `[[directory]]` phrasing, internally consistent with the module-doc at line 43-44 and the shipped schema. No scope regression: working tree touches only the five expected files (two docs, README, two locked SPEC bodies) plus orchestrator-owned TASKS.md/journal. Scenario 1: one permitted `symlink-dir`/`copy-tree` hit (superseded context at `docs/USER_GUIDE.md:77`); README clean. Scenario 2: USER_GUIDE documents both table-arrays, collapsed modes + `symlink-tree`, per-entry `when`, and both variable layers; precedence chain matches SPEC-0004 REQ-005. Scenario 3: SPEC-0001 REQ-005/008/009 and SPEC-0002 REQ-002 each carry an accurate pointer to SPEC-0004. Scenario 4: full `just lint` (`cargo doc -D warnings`) exit 0. Locked-SPEC boundary: `git diff --numstat` is additive-only, no requirement semantics changed.
</review>

<review date="2026-06-01T23:46:09Z" model="claude-opus-4-8[1m]/high" persona="tests" verdict="pass" round="2">
Round-2 retry remains correctly docs-only and adds no vacuous substring-prose tests. Working-tree diff touches only `docs/ARCHITECTURE.md`, `docs/USER_GUIDE.md`, `README.md`, the two locked SPEC bodies, and TASKS.md — zero `.rs`/test files. The one-line fix at `docs/ARCHITECTURE.md:121` ("five file modes" → `[[file]]`/`[[directory]]` kind-typed phrasing) resolves the round-1 docs blocker and aligns with the already-correct module-doc at line 43. No test (committed or pending) asserts `symlink-dir`/`copy-tree`/`five file modes`, so the forbidden prose-grep shape was avoided; Scenario 1 is verified by manual `rg`, with the lone surviving occurrence the permitted superseded-context note at `docs/USER_GUIDE.md:77`. I independently re-ran the task-mandated docs gate `RUSTDOCFLAGS="-D warnings" cargo doc --workspace --no-deps --all-features` → exit 0, no broken intra-doc links (cached rebuild confirms no Rust source touched). Evidence roll call valid: T-012 has no covered CHKs of its own; its doc-content/hygiene task-scenarios are labelled `demonstrated`/`hygiene` with the cited `just lint-docs` gate.
</review>

<review date="2026-06-01T23:46:09Z" model="claude-opus-4-8[1m]/high" persona="security" verdict="pass" round="2">
No security surface (docs-only). The round-2 change is the targeted fix for the round-1 docs blocker: prose edits in `docs/ARCHITECTURE.md` (lines 42-43 and 121-123) replacing "the five file modes" with "the `[[file]]` / `[[directory]]` entry kinds and their materialization modes." No production code, dependency, configuration, or security-relevant artifact changed; no new TOML examples, secrets, injection, or path-traversal patterns introduced. The fail-closed strict-undefined `when` semantics and TOCTOU materialize-time existence backstop noted in round 1 are unaffected. Verdict unchanged from round 1: pass.
</review>

<review date="2026-06-01T23:46:09Z" model="claude-sonnet-4-6[1m]/medium" persona="style" verdict="pass" round="2">
Round-2 one-line docs fix is clean. Both sites in `docs/ARCHITECTURE.md` now carry the identical phrasing "`[[file]]` / `[[directory]]` entry kinds and their materialization modes" (lines 43-44 and 121-123), resolving the internal inconsistency flagged in round 1. The inline-code style for TOML table-array names is consistent with the rest of the file. Prose flows grammatically in both the module-doc bullet and the numbered Apply-phases list. No naming drift, dead content, or suppression annotations introduced.
</review>

<review date="2026-06-01T23:46:09Z" model="claude-opus-4-8[1m]/high" persona="correctness" verdict="pass" round="2">
Round-2 one-line docs fix in `docs/ARCHITECTURE.md:121-123` is correct against the shipped code. The stale "the five file modes" phrase is replaced with "`[[file]]` / `[[directory]]` entry kinds and their materialization modes," which matches the ground truth: `EntryKind` has exactly two variants (`File`, `Directory`) and the apply planner dispatches on `FileMode` to emit `PlannedOperation`s (`patina-core/src/apply/engine.rs:705-717`), so the "ordered list of operations across the ... materialization modes" claim holds. The new phrasing is now internally consistent with the already-correct module-doc at `docs/ARCHITECTURE.md:43-44`. No doc example or behavioral claim is rendered incorrect; the surviving `symlink-dir`/`copy-tree` strings are confined to `FileMode` variant doc comments describing prior behavior and the permitted superseded-context note at `docs/USER_GUIDE.md:77`. No correctness defects.
</review>
