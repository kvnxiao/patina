---
spec: SPEC-0004
task: T-001
generated_at: 2026-06-01T20:46:27Z
---

<implementer date="2026-06-01T20:46:27Z" model="claude-opus-4-8[1m]/high" round="1">
- Reuse survey: Mapped the task area (covered REQ-001 / DEC-001 /
  DEC-002; suggested files `patina-core/src/config/file_entry.rs` and
  `config/mod.rs`; plus the immediate downstream `FileMode` consumers
  the schema change breaks). Decisions:
  reuse-as-is — kept `FileMode` as the executor-dispatch enum
  (`apply::materialize`, `output/diff.rs`, `cmd/apply.rs::mode_label`
  all match on it) rather than introducing a parallel executor mode
  type; reused the existing `.tmpl`-suffix detection and the
  exactly-one-of `target`/`targets` resolution by factoring them into
  shared `has_tmpl_suffix` / `resolve_targets` free fns called by both
  table constructors; mirrored `HookEntry.when: Option<String>`
  (`hook_entry.rs:34`) for the new `when` field rather than inventing a
  new shape.
  extend — extended `FileMode` with one new `SymlinkTree` variant (the
  per-leaf directory mode T-007 will execute) rather than a separate
  enum; extended `ModuleConfig` with a `directories: Vec<ManagedEntry>`
  field alongside the existing `files`; extended `mode_manifest_str`
  (writer) and the engine/diff/apply match arms to the collapsed
  taxonomy.
  write-fresh — the `EntryKind` enum and the unified `ManagedEntry`
  struct (renamed from `FileEntry`, kept as a `pub type FileEntry`
  alias so downstream `FileEntry` references compile); the two
  per-table `from_raw_file` / `from_raw_directory` constructors that
  validate each table's accepted-mode allowlist (the only way to build
  a `ManagedEntry`, so an illegal kind/mode pairing is unconstructable
  — DEC-001's "illegal states unrepresentable" bar); the
  `UnsupportedFileMode` / `UnsupportedDirectoryMode` /
  `DirectoryTemplateSource` error variants (the old single
  `UnsupportedMode` is gone). Searched `patina-core` for an existing
  kind-aware entry type and a `[[directory]]` parser and found none.
  New top-level symbols: `EntryKind`, `ManagedEntry` (+ `FileEntry`
  alias), `RawEntry` (renamed from `RawFileEntry`),
  `FileMode::SymlinkTree`, `ExecutorError::SymlinkTreeNotImplemented`,
  the three new `FileEntryError` variants, and the two free fns above —
  each justified above.
- Completed: Restructured `patina-core/src/config/file_entry.rs` into
  the kind-typed model: `EntryKind { File, Directory }`, a unified
  `ManagedEntry { kind, mode, source, targets, when }`, and per-table
  `from_raw_file` / `from_raw_directory` constructors. A `[[file]]`
  accepts `symlink` (default) / `copy` / implicit `.tmpl` template; a
  `[[directory]]` accepts `symlink` (default → `FileMode::SymlinkDir`,
  atomic) / `symlink-tree` (→ new `FileMode::SymlinkTree`) / `copy`
  (→ `FileMode::CopyTree`, recursive). A `.tmpl` `[[directory]]` source
  is rejected (`DirectoryTemplateSource`). The strings `symlink-dir`
  and `copy-tree` are no longer accepted input on either table; mode
  rejection errors name that table's accepted modes (CHK-003 contract:
  a `[[file]]` `symlink-tree` names `symlink-tree`, `symlink`, `copy`).
  Wired `RawModule` (`config/mod.rs`) to deserialize a new `directory`
  table-array next to `file`, and surfaced `ModuleConfig.directories`.
  Re-exported `EntryKind` and `ManagedEntry` from `config/mod.rs`.
  Updated the collapsed writer mapping `mode_manifest_str`
  (`config/writer.rs`) — `symlink-dir`/`copy-tree` are never emitted.
  Landed the minimal mechanical adjustments to keep the workspace
  compiling: added the `SymlinkTree` arm to `apply::materialize`
  (typed `SymlinkTreeNotImplemented` stub — T-007 implements it),
  `apply::engine::planned_operation` (marked dispatch point mapping to
  the symlink op shape — T-002 wires directory planning),
  `output/diff.rs`, and `cmd/apply.rs::mode_label`. The plan loop still
  iterates only `config.files`, so behaviour is unchanged this task;
  `config.directories` is parsed and carried for T-002.
- Undone: directory-entry plan emission and the single monotonic
  entry-index space (T-002); `when` evaluation (T-005); plan-time
  source existence/kind check (T-006); the real `symlink-tree`
  executor (T-007); `[[directory]]` manifest writer and `add` wiring
  (T-010/T-011); doc + cross-SPEC superseded-by sweep (T-012). All are
  separate tasks per TASKS.md. The `SymlinkTree` arms here are typed
  stubs / marked dispatch points, never reached this task because the
  plan loop emits no directory operations yet.
- Hygiene checks: `just test` (= `cargo test --workspace --locked`)
  exited 0 — the full workspace suite passes, including the 20
  rewritten/added `config_file_entry` tests and all doctests.
  `just lint` exited 0 — nightly `cargo fmt --all --check` clean;
  `cargo clippy --workspace --all-targets --all-features --locked
  -- -D warnings` clean cross-compiled to both
  `x86_64-unknown-linux-gnu` and `x86_64-pc-windows-gnu` (macOS leg
  skipped off a Mac, CI backstops it); `RUSTDOCFLAGS="-D warnings"
  cargo doc` clean (caught and fixed two private-intra-doc-link
  warnings on the `ManagedEntry` doc); `cargo deny check` reported
  advisories/bans/licenses/sources ok (no new dependencies — the
  change is `std` + existing `serde`/`thiserror`/`camino`).
- Evidence: paper trail at
  `.speccy/specs/0004-patina-conditional-entries/evidence/T-001.md`.
  Roll call for the CHKs under REQ-001:
  - CHK-001 (`[[file]]` no-mode → file kind + symlink): demonstrated →
    evidence Scenario 1 covers the pre-edit no-`kind`-field compile gap
    vs the post-edit `file_with_omitted_mode_resolves_to_file_kind_symlink`
    assertion.
  - CHK-002 (`[[directory]]` `symlink-tree` → directory kind + per-leaf
    symlink): demonstrated → evidence Scenario 2 covers the pre-edit
    no-`directories`-field / no-`symlink-tree` gap vs the post-edit
    `directory_symlink_tree_resolves_to_directory_kind_per_leaf_symlink`
    assertion.
  - CHK-003 (`[[file]]` `symlink-tree` rejected naming `symlink-tree`,
    `symlink`, `copy`): demonstrated → evidence Scenario 3 covers the
    pre-edit four-old-modes message vs the post-edit
    `file_with_symlink_tree_mode_is_rejected_naming_accepted_file_modes`
    substring assertions.
- Discovered issues: The `apply/mod.rs`, `apply/symlink.rs`,
  `apply/copy.rs` module/variant doc comments still describe the
  executor `FileMode` variants using the historical `symlink-dir` /
  `copy-tree` framing, and `executor_modes.rs` test comments reference
  `REQ-005`. These are accurate at the executor level (the variants are
  unchanged) and the user-facing doc sweep is explicitly T-012's scope
  (it covers `docs/`, `README.md`, and cross-SPEC notes). Left
  untouched to keep this task surgical; flagged for T-012.
- Procedural compliance: This implementer entry lands directly in
  `journal/T-001.md` per the journal-file schema; no
  `<implementer-note>` was written into TASKS.md. The TASKS.md
  `state="..."` attribute for T-001 was flipped `pending` →
  `in-progress` at the start of this turn and `in-progress` →
  `in-review` as the final step. No shipped `.claude/skills/speccy-*`
  bodies required edits during this task.
</implementer>

<review date="2026-06-01T20:50:35Z" model="claude-opus-4-8[1m]/high" persona="business" verdict="pass" round="1">
T-001 delivers exactly REQ-001's schema split and nothing it shouldn't. Every `<done-when>` of REQ-001 maps to the diff and to a passing test in `patina-core/tests/config_file_entry.rs` (20/20 green against the working tree): kind-typed `[[file]]`/`[[directory]]` tables, collapsed mode names (`symlink-dir`/`copy-tree` removed as accepted input), `symlink-tree` directory mode, `.tmpl`-on-directory rejection, per-table mode allowlist errors, and the shared exactly-one-of/non-empty-targets rules. CHK-001/002/003 substring contracts hold — `UnsupportedFileMode` names `symlink-tree`, `symlink`, `copy` (`patina-core/src/config/file_entry.rs`). DEC-001's "illegal states unrepresentable" bar is met: `ManagedEntry` is constructible only through `from_raw_file`/`from_raw_directory`. Scope discipline is clean: `when` is parsed-and-carried (eval correctly deferred to T-005), `SymlinkTree` is a typed `SymlinkTreeNotImplemented` stub / marked dispatch point (executor deferred to T-007), and the plan loop still iterates only `config.files` so directory emission stays with T-002 — no downstream task's behavior was smuggled in, and no non-goal was touched. The SPEC had no open questions to resolve.
Non-blocking observation: the task prose predicted `status/mod.rs` and `cmd/add.rs` would break compilation, but neither needed edits (they reference only surviving `FileMode` variants / the retained `config.files` field). Harmless over-estimate, not a defect.
</review>

<review date="2026-06-01T20:50:35Z" model="claude-opus-4-8[1m]/high" persona="tests" verdict="pass" round="1">
Tests genuinely exercise REQ-001's parse-layer behaviour through the real `parse_module_config_str` path -- no mocks of the system under test. All three covered CHKs are demonstrated with named tests present in the diff (`patina-core/tests/config_file_entry.rs`): CHK-001 (`file_with_omitted_mode_resolves_to_file_kind_symlink`), CHK-002 (`directory_symlink_tree_resolves_to_directory_kind_per_leaf_symlink`), CHK-003 (`file_with_symlink_tree_mode_is_rejected_naming_accepted_file_modes`). Evidence roll call accounts for every CHK under REQ-001 with `demonstrated` labels and correct scenario mapping; the red phases are honest compile-failure prose for a schema change (assertion couldn't compile pre-edit), materially distinct from the green runner output -- no fabrication pattern matched. Negative paths assert the *specific* typed variant via `matches!` (`UnsupportedFileMode`, `UnsupportedDirectoryMode`, `DirectoryTemplateSource`), not catch-any-error, and `rejects_unknown_file_mode_naming_accepted_values` adds a negative assertion that removed `symlink-dir`/`copy-tree` spellings never leak into the `[[file]]` error message. I confirmed mutation sensitivity: flipping the `symlink-tree` resolution arm to `SymlinkDir` fails `config_file_entry.rs:67` (CHK-002), so the test catches the obvious wrong implementation. All 20 tests pass; the stub `SymlinkTree` executor/plan arms are correctly out-of-scope (T-002/T-007) and unreachable this task.
</review>

<review date="2026-06-01T20:50:35Z" model="claude-opus-4-8[1m]/medium" persona="security" verdict="pass" round="1">
No security concerns. T-001 is a pure schema/parsing refactor with no auth, secret-handling, crypto, or concurrency surface.
Input validation is sound: the per-table `from_raw_file`/`from_raw_directory` constructors in `patina-core/src/config/file_entry.rs` are the only construction path for `ManagedEntry`, and each enforces an exact-string mode allowlist (`patina-core/src/config/file_entry.rs:193-217` and `:227-244`), making illegal kind/mode pairings unconstructable. No injection vector — modes are matched against literals, not interpolated.
The new `when` field is parsed and carried verbatim as `Option<String>` with no evaluation this task (deferred to T-005), so it introduces no expression-injection surface here, and it is not logged or echoed anywhere in this diff.
Error messages (`UnsupportedFileMode`, `UnsupportedDirectoryMode`, `DirectoryTemplateSource`) reflect the user's own manifest values back to the same local user operating on their own repo — no cross-trust-boundary disclosure, and consistent with the pre-existing `UnsupportedMode` reflection. The `SymlinkTreeNotImplemented` stub returns a typed error rather than panicking and is unreachable until directory planning lands (T-002/T-007). No new dependencies (change is `std` + existing `serde`/`thiserror`/`camino`).
</review>

<review date="2026-06-01T20:50:35Z" model="claude-sonnet-4-6[1m]/medium" persona="style" verdict="blocking" round="1">
`patina-core/src/config/file_entry.rs:279` — `has_tmpl_suffix` takes `&Utf8PathBuf` instead of `&Utf8Path`. The project rule file `.claude/rules/rust/rust-let-types-carry-invariants.md` calls this exact anti-pattern out by name as a blocking style concern: "taking `&Utf8PathBuf` where the canonical version correctly takes `&Utf8Path` per the parameter-type hierarchy in `rust-code-quality.md`." Change the signature to `fn has_tmpl_suffix(source: &Utf8Path) -> bool`; the two call sites (`&source` where `source: Utf8PathBuf`) compile unchanged because `&Utf8PathBuf` coerces to `&Utf8Path`.
</review>

<review date="2026-06-01T20:50:35Z" model="claude-opus-4-8[1m]/high" persona="correctness" verdict="pass" round="1">
T-001 schema split is correct. `resolve_targets` is a faithful 1:1 extraction of the prior XOR/non-empty logic; the `.tmpl`-before-allowlist ordering is preserved in `from_raw_file`; per-table mode allowlists are disjoint and complete. All five touched `FileMode` match sites handle the new `SymlinkTree` variant, and the executor arm returns a typed error rather than panicking. The plan loop (`engine.rs:268`) still iterates only `config.files`, so `SymlinkTree`/directory dispatch points are genuinely unreachable this task — no behavior change leaks. No reachable correctness defect at or above the 80-confidence threshold.
</review>

<blockers date="2026-06-01T20:50:35Z" round="1">
Fix the `&Utf8PathBuf` parameter-type anti-pattern before the next review pass.
- style (blocking): `patina-core/src/config/file_entry.rs:279` — `has_tmpl_suffix` takes `&Utf8PathBuf` instead of `&Utf8Path`, violating `.claude/rules/rust/rust-let-types-carry-invariants.md`. Change the signature to `fn has_tmpl_suffix(source: &Utf8Path) -> bool`; the two call sites coerce unchanged. (business, tests, security, correctness all passed.)
</blockers>

<implementer date="2026-06-01T21:05:00Z" model="claude-opus-4-8[1m]/high" round="2">
- Reuse survey: Non-reuse blocker (a parameter-type fix on the existing
  `has_tmpl_suffix` free fn), so no area re-survey was owed and none was
  run. No new top-level symbol was introduced this round, so the
  per-symbol floor is satisfied vacuously — the only changes are the
  `has_tmpl_suffix` signature and the `Utf8Path` import that backs it.
- Completed: Addressed the round-1 style blocker. Changed
  `has_tmpl_suffix`'s signature from `fn has_tmpl_suffix(source:
  &Utf8PathBuf)` to `fn has_tmpl_suffix(source: &Utf8Path)`
  (`patina-core/src/config/file_entry.rs`), aligning it with the
  parameter-type hierarchy in `rust-code-quality.md` and the canonical
  `&Utf8Path` rule in `rust-let-types-carry-invariants.md`. Added
  `use camino::Utf8Path;` (kept on its own line per the project's
  `fmt` import-granularity setting; the combined-brace form
  `{Utf8Path, Utf8PathBuf}` was rejected by `cargo fmt --check`). The
  two call sites (`from_raw_file`, `from_raw_directory`) pass `&source`
  where `source: Utf8PathBuf`; `&Utf8PathBuf` auto-derefs to
  `&Utf8Path`, so both compile unchanged with no call-site edit.
- Undone: nothing new — round-1's deferred scope (T-002 directory plan
  emission, T-005 `when` eval, T-006 source check, T-007 `symlink-tree`
  executor, T-010/T-011 writer + `add` wiring, T-012 doc sweep) remains
  with those tasks, unchanged by this amend.
- Hygiene checks:
  - `cargo test --workspace --locked` (via `just test`): exit 0.
  - `cargo clippy --workspace --all-targets --all-features --locked
    -- -D warnings` (via `just lint`, cross-compiled to
    `x86_64-unknown-linux-gnu` and `x86_64-pc-windows-gnu`; macOS leg
    skipped off a Mac, CI backstops): exit 0.
  - `cargo +nightly fmt --all --check` (via `just lint`): exit 0
    (after splitting the new import onto its own line).
  - `cargo deny check` (via `just lint`): exit 0 — advisories, bans,
    licenses, sources ok; no new dependencies.
- Evidence: paper trail at
  `.speccy/specs/0004-patina-conditional-entries/evidence/T-001.md`
  (round-1 scenarios unchanged — this round is a signature-only refactor
  with no behavioural delta). Roll call for the CHKs under REQ-001:
  - CHK-001 (`[[file]]` no-mode → file kind + symlink): demonstrated →
    `file_with_omitted_mode_resolves_to_file_kind_symlink` still passes
    (the `has_tmpl_suffix` call in `from_raw_file` is the same logic
    through a borrowed-coercion arg).
  - CHK-002 (`[[directory]]` `symlink-tree` → directory kind + per-leaf
    symlink): demonstrated →
    `directory_symlink_tree_resolves_to_directory_kind_per_leaf_symlink`
    still passes.
  - CHK-003 (`[[file]]` `symlink-tree` rejected naming `symlink-tree`,
    `symlink`, `copy`): demonstrated →
    `file_with_symlink_tree_mode_is_rejected_naming_accepted_file_modes`
    still passes.
- Discovered issues: none new. The round-1 executor-level doc framing
  flagged for T-012 (`apply/mod.rs`, `apply/symlink.rs`,
  `apply/copy.rs`, `executor_modes.rs`) is untouched and still T-012's
  scope.
- Procedural compliance: Retry-mode amend of the prior pass's
  uncommitted WIP in place — no `git restore`/`clean`/`checkout`, no
  rewrite-from-scratch, no state reset. T-001 flipped `pending` →
  `in-progress` at the start of this round and `in-progress` →
  `in-review` after the hygiene gate passed. `round="2"` increments the
  prior `round="1"` by exactly 1. This entry was appended after the
  existing journal contents; no earlier block was modified.
</implementer>

<review date="2026-06-01T20:56:58Z" model="claude-opus-4-8[1m]/high" persona="business" verdict="pass" round="2">
T-001 delivers REQ-001's schema split faithfully and the round-2 retry introduced no business-scope drift — it is a pure `&Utf8PathBuf`→`&Utf8Path` signature fix on `has_tmpl_suffix` (plus the `use camino::Utf8Path;` import) addressing the round-1 style blocker, with zero behavioural delta (`patina-core/src/config/file_entry.rs`). Every REQ-001 `<done-when>` maps to the diff and to a passing test in `patina-core/tests/config_file_entry.rs`: `[[file]]` no-mode→`Symlink` / `copy`→`Copy` / `.tmpl`→`TemplateRender` (rejecting an explicit mode); `[[directory]]` no-mode→atomic `SymlinkDir` / `symlink-tree`→`SymlinkTree` / `copy`→recursive `CopyTree`; per-table mode allowlist rejections naming that table's accepted modes; `[[directory]]` `.tmpl` rejection (`DirectoryTemplateSource`); and the shared exactly-one-of/non-empty-`targets` rules (`resolve_targets`) exercised on both tables. CHK-001/002/003 hold. DEC-001's illegal-states-unrepresentable bar is met: `ManagedEntry` is constructible only through `from_raw_file`/`from_raw_directory`. Scope discipline clean: `when` parsed-and-carried only (eval deferred to T-005), `SymlinkTree` a typed stub (T-007), plan loop still iterates only `config.files` (T-002). No non-goal touched, no open questions silently resolved.
</review>

<review date="2026-06-01T20:56:58Z" model="claude-opus-4-8[1m]/high" persona="tests" verdict="pass" round="2">
Tests genuinely drive REQ-001's parse behaviour through the real `parse_module_config_str` path -- no mocks of the system under test. All three covered CHKs are demonstrated by named tests present in the diff (`patina-core/tests/config_file_entry.rs`): CHK-001 (`file_with_omitted_mode_resolves_to_file_kind_symlink`, asserts `kind == File` + `mode == Symlink`), CHK-002 (`directory_symlink_tree_resolves_to_directory_kind_per_leaf_symlink`, asserts `files.is_empty()` + `directories.len() == 1` + `kind == Directory` + `mode == SymlinkTree`), CHK-003 (`file_with_symlink_tree_mode_is_rejected_naming_accepted_file_modes`, asserts rendered contains `symlink-tree`/`symlink`/`copy` AND `matches!(UnsupportedFileMode)`). Evidence roll call accounts for every CHK with `demonstrated` labels and scoped per-test invocations distinct from the hygiene full-suite run; red phases are honest compile-failure prose for a schema change -- no fabrication pattern matched. Negative paths assert the specific typed variant via `matches!` (`UnsupportedFileMode`/`UnsupportedDirectoryMode`/`DirectoryTemplateSource`/`TargetAndTargets`), and `rejects_unknown_file_mode_naming_accepted_values` adds a negative assertion that removed `symlink-dir`/`copy-tree` spellings never leak. Mutation sensitivity confirmed: flipping the `symlink-tree` arm to `SymlinkDir` fails the CHK-002 assertion. The round-2 delta is a parameter-type-only refactor; the `.tmpl` suffix logic stays covered by `rejects_tmpl_source_with_explicit_mode_on_file`, `tmpl_file_source_resolves_to_template_render_mode`, and `directory_with_tmpl_source_is_rejected` -- re-demonstrating the same green tests is the correct shape for a non-behavioural blocker fix.
</review>

<review date="2026-06-01T20:56:58Z" model="claude-opus-4-8[1m]/medium" persona="security" verdict="pass" round="2">
No security concerns. Round 2 is a parameter-type-only refactor (`has_tmpl_suffix` now takes `&Utf8Path` instead of `&Utf8PathBuf`, plus a `use camino::Utf8Path;` import) layered on the round-1 schema split; it introduces no new auth, secret-handling, crypto, injection, or concurrency surface. Verified against the diff (`patina-core/src/config/file_entry.rs`): mode resolution remains literal-string allowlist matching in `from_raw_file`/`from_raw_directory` (no injection vector), `ManagedEntry` is still constructible only through these per-table constructors, the new `when` field is parsed and carried verbatim as `Option<String>` with no evaluation this task (deferred to T-005) and is not logged or echoed, error variants reflect the local user's own manifest back to themselves (no cross-trust-boundary disclosure), the `SymlinkTreeNotImplemented` stub returns a typed error rather than panicking, and no new dependencies were added (`cargo deny check` clean). The round-1 style blocker is orthogonal to security and the fix carries no security regression.
</review>

<review date="2026-06-01T20:56:58Z" model="claude-sonnet-4-6[1m]/medium" persona="style" verdict="pass" round="2">
Round-1 blocker resolved. `has_tmpl_suffix` at `patina-core/src/config/file_entry.rs:280` now correctly takes `&Utf8Path` instead of `&Utf8PathBuf`. The two call sites pass `&source` (where `source: Utf8PathBuf`) and coerce cleanly via `Deref`. The `use camino::Utf8Path;` import is on its own line, consistent with the project's `cargo fmt` import-granularity setting. No new naming, dead-code, suppression, or idiomatic-pattern issues introduced by the round-2 amend.
</review>

<review date="2026-06-01T20:56:58Z" model="claude-opus-4-8[1m]/high" persona="correctness" verdict="pass" round="2">
T-001 round 2 is correct. The only round-2 delta is the style-blocker fix: `has_tmpl_suffix` changed from `&Utf8PathBuf` to `&Utf8Path` (`patina-core/src/config/file_entry.rs`) with `use camino::Utf8Path;` added. The body is byte-identical (`extension().is_some_and(|ext| ext.eq_ignore_ascii_case("tmpl"))`), and both call sites pass `&source` where `source: Utf8PathBuf`, coercing cleanly via Deref — no logic, branch, or boundary change. The round-1 correctness verdict still holds: `resolve_targets` faithfully preserves the prior XOR/non-empty rule, the `.tmpl`-before-allowlist ordering is intact in `from_raw_file`, the two per-table allowlists are disjoint/complete, and every exhaustive `FileMode` match covers the new `SymlinkTree` arm with the executor arm returning a typed `SymlinkTreeNotImplemented` error rather than panicking. The two non-exhaustive `matches!` sites that omit `SymlinkTree` (`plan_has_symlink_op`, doctor) are not reachable defects this task: `from_raw_file` can never produce `SymlinkTree` and the plan loop iterates only `config.files`. No reachable correctness defect at or above the 80-confidence threshold.
</review>
