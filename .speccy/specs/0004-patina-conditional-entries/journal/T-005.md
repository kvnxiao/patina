---
spec: SPEC-0004
task: T-005
generated_at: 2026-06-01T21:45:07Z
---

<implementer date="2026-06-01T21:45:07Z" model="claude-opus-4-8[1m]/high" round="1">
- Reuse survey: Mapped the task area (covered REQ-003 / REQ-009 / DEC-004;
  suggested files `patina-core/src/apply/engine.rs` and
  `patina-cli/tests/conditional_entries.rs`; plus the upstream surfaces the
  gate plugs into — `ManagedEntry.when` from T-001, the T-002
  `assemble_plan_operations` / `ResolvedEntry` index machinery, and the
  shared predicate evaluator). Decisions: reuse-as-is —
  `Engine::eval_when(expr, resolver)` (`template/mod.rs:162`), the exact path
  `apply::hooks::should_run` uses, so no second predicate evaluator is
  introduced (REQ-004 / DEC-006); the existing
  `EngineError::Template(#[from] crate::template::TemplateError)` variant
  (`error.rs:59`), so `eval_when`'s error threads up through `?` with no new
  error variant; `resolve_entry` (the T-002 per-entry canonicalizer)
  unchanged; `ManagedEntry.when` (`config/file_entry.rs:95`) which T-001
  already parses and carries; the `Fixture` harness
  (`patina-cli/tests/common/mod.rs`) and the priming-then-measure
  byte-identical pattern from `deterministic_stdout.rs`. extend — the two
  declared-order buckets in `plan()` became `Vec<Option<ResolvedEntry>>`
  and `assemble_plan_operations` now advances the `entry_index` for every
  slot but emits only for `Some`, so a `when`-false entry keeps its index
  (REQ-009); no new field on any struct. write-fresh —
  `gate_and_resolve_entry` (the per-entry `when`-then-canonicalize gate) and
  the new CLI test module `conditional_entries.rs`. Searched the crate
  (`rg "fn .*when"`, `rg "eval_when"`) for an existing per-entry gate helper
  and found only `apply::hooks::should_run` (a hook-shaped gate over a
  `ResolvedHook`, not a `ManagedEntry`), so the managed-entry gate is
  genuinely new. New top-level symbol: `gate_and_resolve_entry` — justified
  above (no existing managed-entry `when` gate; it wraps the reused
  `eval_when` + `resolve_entry`).
- Completed: Inserted the REQ-009 `when` gate at the top of each managed
  entry's per-entry body in `apply::plan` (`patina-core/src/apply/engine.rs`).
  `plan()` constructs one `Engine` (cheap, `Arc`-shared environment) and
  threads it plus the planning `Resolver` into the new
  `gate_and_resolve_entry`, which evaluates `entry.when` via `eval_when`
  **first** and, on false, returns `Ok(None)` without ever calling
  `resolve_entry` — so a `when`-false entry is never canonicalized (REQ-009
  order: (1) `when`, (2) canonicalize, (3) [kind/existence — T-006]). The
  two declared-order buckets became `Vec<Option<ResolvedEntry>>`; a `None`
  slot keeps the entry's position so `assemble_plan_operations` advances the
  `u32` `entry_index` for every declared entry while emitting a
  `PlannedOperation`/`ResolvedOperation` only for `Some` — a `when`-false
  entry occupies its index but contributes no operation and (via the
  `PlannedOperation`-driven diff) no diff line (DEC-004). The gate is
  per-entry, above the target loop in `resolve_entry`, so a multi-target
  entry's `when` gates all targets together (REQ-003). Updated the
  `assemble_plan_operations` doc comment to describe the `None`-slot index
  behaviour. Added one engine unit test and four CLI integration tests (see
  Evidence).
- Undone: The plan-time source existence-and-kind check (step 3 of REQ-009)
  is T-006 — it slots into `gate_and_resolve_entry`'s `Some` branch after
  canonicalization; CHK-022 (a `when`-false, wrong-OS, absent-source
  `[[directory]]` exits 0 with no kind/missing-source error) becomes
  satisfiable once T-006 adds that check (T-005 supplies the gate that
  ensures the absent source is never reached, but the exit-0-with-no-error
  contract needs T-006's check to be the thing skipped). The `when`-flip
  reaping (CHK-019) rides on the status managed-set becoming `when`-aware
  (T-008); T-005 asserts only that a `when`-false entry plans nothing. The
  `symlink-tree` executor (T-007) is untouched.
- Hygiene checks:
  - `just test` (= `cargo test --workspace --locked`): exit 0 — full
    workspace suite plus doctests pass, including the new engine unit test
    `when_false_entry_occupies_its_index_but_emits_no_operation` and the
    four new `conditional_entries` CLI tests; the two T-002 engine unit
    tests pass after the `Vec<Option<_>>` signature change.
  - `just lint` clippy (`cargo clippy --workspace --all-targets
    --all-features --locked -- -D warnings`, cross-compiled to
    `x86_64-unknown-linux-gnu` and `x86_64-pc-windows-gnu`; macOS leg
    skipped off a Mac with CI as backstop): exit 0. Clippy initially flagged
    `clippy::collapsible_if` on the gate (folded to a let-chain `if let
    Some(expr) = entry.when.as_deref() && !engine.eval_when(...)?`) and
    `clippy::unnecessary_wraps` on the test `resolved_entry` helper (kept it
    returning a bare `ResolvedEntry`; the call sites wrap `Some(...)` and the
    one `None` slot is written inline); both fixed and re-run green.
  - `just lint` fmt (`cargo +nightly fmt --all --check`): exit 0 (ran
    `cargo +nightly fmt --all` to apply the wrapping rustfmt wanted, then
    re-checked).
  - `just lint` docs (`RUSTDOCFLAGS="-D warnings" cargo doc`): exit 0 — no
    broken intra-doc links from the new `gate_and_resolve_entry` references.
  - `just lint` `cargo deny check`: advisories / bans / licenses / sources
    all ok (pre-existing transitive `wit-bindgen` duplicate-crate warnings
    only; no dependency added — the change is `std` + existing crates).
- Evidence: paper trail at
  `.speccy/specs/0004-patina-conditional-entries/evidence/T-005.md`.
  T-005 covers REQ-003 and REQ-009. Roll call for the CHKs under those REQs:
  - CHK-006 (a `when`-false entry creates no target and plans zero
    operations): demonstrated → evidence
    `when_false_entry_creates_no_target_and_plans_zero_operations` drives the
    real `patina apply --json --yes` over a `[[file]]` with
    `when = "patina.os == 'definitely-not-this-os'"`; asserts exit 0, the
    `~/.zshrc` target is not created, and no `--json` plan row mentions
    `.zshrc`. Red/green: without the gate the entry plans and materializes,
    failing both the `!exists` and plan-array assertions.
  - CHK-007 (a `when`-true entry materializes; the second run is
    byte-identical): demonstrated → evidence
    `when_true_entry_materializes_and_second_run_is_byte_identical` uses a
    copy-mode `[[file]]` whose `when` equals `patina.os == '<current OS>'`;
    after a priming apply it asserts the target exists and two further
    `apply --yes` runs emit byte-identical stdout (REQ-021 parity). Copy
    mode isolates the parity property — a symlink's plan diff renders its
    link target differently fresh-vs-converged (an orthogonal quirk
    `deterministic_stdout.rs` also sidesteps).
  - REQ-003 multi-target `<done-when>` (the `when` gates all targets
    together): demonstrated → `multi_target_false_when_plans_none_of_its_targets`
    (neither `~/.codex/agent.toml` nor `~/.claude/agent.toml` is created) and
    `multi_target_true_when_plans_all_of_its_targets` (both are), pinning the
    gate as per-entry above the target loop.
  - REQ-009 index `<done-when>` (a `when`-false entry occupies its index but
    emits nothing): demonstrated → unit test
    `when_false_entry_occupies_its_index_but_emits_no_operation` asserts a
    `[Some(f0), None, Some(f2)]` + `[Some(d0)]` sequence emits only f0/f2/d0
    and yields indices `[0, 2, 3]` (the gap at 1 proves no renumbering).
  - CHK-022 (only `<scenario>` under REQ-009 needing the kind/existence
    check): judgment-only / deferred → requires T-006's plan-time source
    check; T-005 supplies the gate but not the check whose skipping CHK-022
    asserts. Flagged so reviewer-tests does not expect it green here.
  - CHK-019 (the `when`-flip reaping leg under REQ-003): judgment-only /
    deferred → rides on the T-008 `when`-aware status managed-set; T-005
    asserts only the plan-nothing property.
- Discovered issues: none new. The `plan()` module-level docstring
  (`engine.rs:9-12`) and `assemble_plan_operations`' neighbours still phrase
  things in terms of `[[file]]` entries (a pre-`[[directory]]`-split
  artefact flagged in earlier journals for the T-012 docs sweep); not made
  worse here and left untouched to keep this change surgical.
- Procedural compliance: This round-1 `<implementer>` block creates
  `journal/T-005.md` with three-field frontmatter per the journal schema;
  no `<implementer-note>` was written into TASKS.md. The TASKS.md
  `state="..."` attribute for T-005 was flipped `pending` → `in-progress`
  at the start of this turn and `in-progress` → `in-review` after all four
  hygiene gates passed. The locked `SPEC.md` was not edited. No shipped
  `.claude/skills/speccy-*` bodies required edits during this task.
</implementer>

<review date="2026-06-01T21:48:48Z" model="claude-opus-4-8[1m]/high" persona="business" verdict="pass" round="1">
T-005 (REQ-003 + REQ-009) delivers what the SPEC promises. The `when` gate (`gate_and_resolve_entry`, `patina-core/src/apply/engine.rs`) evaluates `entry.when` via the shared `Engine::eval_when` before `resolve_entry` canonicalizes the source, so a gated-off entry is never canonicalized (REQ-009 ordering) and a `when`-false entry pushes nothing into the `operations`/`resolved_ops` vecs — no operation, no diff line (DEC-004). The `None`-slot design preserves the single monotonic entry-index space: `when_false_entry_occupies_its_index_but_emits_no_operation` proves indices stay `[0,2,3]` rather than renumbering. Requirement-to-`<done-when>` mapping for T-005's owned items checks out: CHK-006 (false `when` creates no target, zero plan ops), CHK-007 (true `when` materializes + byte-identical second run, REQ-021 parity), and the REQ-003 multi-target `<done-when>` (gate per-entry above the target loop). All 4 CLI tests and the engine unit test pass. Correctly-scoped deferrals: CHK-019 (when-flip reaping) → T-008, CHK-022 (only `<scenario>` under REQ-009) → T-006, both assigned to those tasks and called out in the journal. No non-goal violated (no module-level `when`, no second evaluator), no wire-format change, Open Questions empty.
</review>

<review date="2026-06-01T21:48:48Z" model="claude-opus-4-8[1m]/high" persona="tests" verdict="pass" round="1">
T-005 tests genuinely exercise the REQ-003/REQ-009 `when`-gate behaviour. CHK-007 (`conditional_entries.rs:104-125`) primes convergence then asserts `first.stdout == second.stdout` byte-for-byte on raw `Vec<u8>` across two real `patina apply --yes` subprocess runs over per-tempdir-isolated env — a leaked timestamp/PID/run-id would fail it. CHK-006 (`conditional_entries.rs:38-66`) drives the real `apply --json --yes` and asserts both that the `~/.zshrc` target is not created and that the `--json` plan array carries no `.zshrc` row, so a `when`-false entry verifiably plans zero operations. All four CLI tests invoke `env!("CARGO_BIN_EXE_patina")` as a subprocess (no mocks); the unit test drives the real `assemble_plan_operations`. The when-true predicate is non-tautological: `current_os_family()` uses `std::env::consts::OS`, which `normalized_os` returns unchanged for the three supported platforms, so `patina.os ==` evaluates true through the real `Engine::eval_when`. Multi-target false/true tests assert each target individually. Evidence roll call covers all CHKs under REQ-003/REQ-009 (CHK-006/CHK-007 + done-whens demonstrated; CHK-022 deferred to T-006 and CHK-019 deferred to T-008 — both confirmed against the diff). No fabrication pattern matched.
</review>

<review date="2026-06-01T21:48:48Z" model="claude-opus-4-8[1m]/high" persona="security" verdict="pass" round="1">
T-005 routes a managed entry's user-supplied `when` string through `Engine::eval_when` (MiniJinja `compile_expression().eval()`) at `patina-core/src/apply/engine.rs` (`gate_and_resolve_entry`). This is the exact pre-existing evaluator and `Resolver`-backed `SemiStrict` context already used on `main` for hook `when` predicates (`apply/hooks.rs::should_run`) and `.tmpl` rendering — no new functions/filters are registered, the expression grammar is sandboxed (no code/FS/process access), and no new variable namespace is exposed. The only ambient-data reach, `patina.env.*` reading `std::env::var` at access time, predates this task and is unchanged. Patina is a single-user CLI acting on the invoking user's own manifest, files, and environment, so no privilege/trust boundary is crossed. The new code logs neither the expression nor evaluated values; failure errors name only the undefined variable name, not its value. Evaluating `when` before canonicalizing paths slightly reduces surface. No injection vectors and no dependency added (deny check clean). No security concerns beyond the established surface.
</review>

<review date="2026-06-01T21:48:48Z" model="claude-sonnet-4-6[1m]/medium" persona="style" verdict="pass" round="1">
All style criteria pass. The T-005 diff is clean:
- `gate_and_resolve_entry` and `resolve_entry` both take `&Utf8Path` (not `&Utf8PathBuf`), satisfying the `rust-let-types-carry-invariants.md` parameter-type rule.
- No new imports are introduced in `engine.rs`; `Engine`, `ManagedEntry`, and `Resolver` were already in scope.
- `conditional_entries.rs` follows the established `use common::Fixture; use common::code;` import pattern exactly as siblings `deterministic_stdout.rs` and `variable_layers.rs`.
- No `#[allow]` suppressions, no dead imports, no unused parameters.
- `current_os_family()` returning `std::env::consts::OS` as `&'static str` is correct; the doc comment's claim that it matches `normalized_os`'s output for the three supported platforms is accurate.
- The reuse survey is honest: `Engine::eval_when` is genuinely reused (same path as `apply::hooks::should_run`), no second predicate evaluator introduced.
- Tests gate real invariants (operation absence, target non-creation, index gap, byte-identical stdout) — none vacuous.
</review>

<review date="2026-06-01T21:48:48Z" model="claude-opus-4-8[1m]/high" persona="correctness" verdict="pass" round="1">
T-005 correctly enforces the REQ-009 per-entry ordering and the entry-index invariant; no correctness defect at or above the reporting threshold. Verified in `patina-core/src/apply/engine.rs`:
- `gate_and_resolve_entry` uses the let-chain `if let Some(expr) = entry.when.as_deref() && !engine.eval_when(expr, resolver)?` and returns `Ok(None)` in the false branch *before* `resolve_entry` is reached, so a `when`-false entry is never canonicalized (step 1 before step 2). Left-to-right short-circuit means `eval_when` is invoked only when `entry.when` is `Some`.
- A no-`when` entry skips the `if let` and falls through to `Ok(Some(resolve_entry(...)))`, so it always plans.
- The gate returns one `Option<ResolvedEntry>` per entry and `resolve_entry` builds the full `targets` vec, so the gate is per-entry above the target loop — a multi-target entry gates all targets together.
- `assemble_plan_operations` advances `entry_index` for every slot, so a dropped entry occupies its index but emits no operation (proven by the `[0, 2, 3]` gap unit test). `saturating_add` preserves the prior clamp; no off-by-one.
- `execute` reads `op.entry_index` from the plan-time `ResolvedOperation` rather than re-deriving; the `ResolvedPlan.operations: Vec<ResolvedOperation>` vs the durable `Plan` `Vec<PlannedOperation>` are distinct and consistently wired.
- Undefined variable in `when` errors (fail-closed) rather than silently dropping the entry. No determinism regression: dropped entries contribute nothing to the plan/diff and bucket fill order is preserved.
</review>
