# Evidence for SPEC-0002 T-004

Red-then-green paper trail for `patina add <path>` (REQ-002, REQ-009,
REQ-010). The red phase runs the new `patina-cli/tests/add_cli.rs` crate
against a binary with the `Add` subcommand absent; the green phase runs it
against the implemented command. The scenarios back the `demonstrated` CHK
labels in `journal/T-004.md`.

## Red capture method

The tracked CLI wiring edits (`cli.rs`, `main.rs`, `cmd/mod.rs`) were
`git stash`ed and the untracked `cmd/add.rs` was moved aside, while the new
`tests/add_cli.rs` crate was kept present, then the crate was run. Every
behavioural test fails at its exit-code assertion: the pre-change `patina`
has no `add` subcommand, so clap rejects the argument and the process exits
`2`, not the `0`/`1` the tests expect. (The two-mode-flags test also exits
2 in both phases, but for different reasons — see Scenario 3.)

```
test result: FAILED. 0 passed; 7 failed; 0 ignored; 0 measured; 0 filtered out
```

## Session 2026-05-30-T004-attempt-1

### Scenario 1 — `patina add ~/.zshrc --module zsh --symlink --yes` stages the file, writes the entry, leaves the target (CHK-003)

`add_moves_file_writes_entry_and_leaves_target`.

<red>
Pre-edit: the `Add` subcommand does not exist, so `patina add ...` is a
clap usage error (exit 2). The first assertion (exit 0) fails:

```
thread 'add_moves_file_writes_entry_and_leaves_target' panicked at patina-cli/tests/add_cli.rs:40:5:
assertion `left == right` failed: add must exit 0; stderr: ...
  left: 2
 right: 0
```

The move / manifest / untouched-target assertions are never reached.
</red>

<green>
Post-edit: `patina add ~/.zshrc --module zsh --symlink --yes` copies the
target's bytes into `<repo>/zsh/zshrc` (a regular file with content `foo`,
not a symlink), appends a `[[file]]` entry to `<repo>/zsh/patina.toml` with
`source = "zshrc"`, `target = "~/.zshrc"`, `mode = "symlink"`, and leaves
`~/.zshrc` as a regular file still holding `foo` (apply has not run). All
of these are asserted and the test exits 0.

The convergence half (REQ-002's "the system is functional" intent) is now
an automated regression guard — see Scenario 2 (CHK-004).

```
test add_moves_file_writes_entry_and_leaves_target ... ok
```
</green>

### Scenario 2 — `add` then `apply --yes` materializes the target as a symlink to the canonical staged source (CHK-004)

`add_then_apply_materializes_target_as_symlink`.

<red>
Pre-edit: the `Add` subcommand does not exist, so the leading `patina add
~/.zshrc ...` is a clap usage error (exit 2). The first assertion (add must
exit 0) fails, and `apply` is never reached:

```
thread 'add_then_apply_materializes_target_as_symlink' panicked at patina-cli/tests/add_cli.rs:
assertion `left == right` failed: add must exit 0; stderr: ...
  left: 2
 right: 0
```
</red>

<green>
Post-edit: `patina add ~/.zshrc --module zsh --symlink --yes` stages the
entry (target still a regular file, asserted not-yet-a-symlink), then
`patina apply --yes` materializes `~/.zshrc` as a symbolic link. The test
asserts the link resolves (via `read_link` + `canonicalize`) to the
canonical path of `<repo>/zsh/zshrc` — proving `add` wrote a correct,
applyable `source`/`target` relationship, not merely correct manifest text.
A subtly wrong staged entry would pass CHK-003 yet fail this assertion.

```
test add_then_apply_materializes_target_as_symlink ... ok
```
</green>

### Scenario 3 — two mode flags are a clap usage error naming the conflict (REQ-002 behaviour)

`add_two_mode_flags_is_a_usage_error`.

<red>
Pre-edit: `patina add ~/.zshrc --symlink --copy` exits 2 because clap does
not recognize the `add` subcommand at all — the stderr names the unknown
subcommand, not the conflicting flags, so the stderr substring assertion
(`--symlink` AND `--copy`) fails even though the exit code happens to match:

```
thread 'add_two_mode_flags_is_a_usage_error' panicked at patina-cli/tests/add_cli.rs:102:5:
stderr must name the conflicting flags, got: error: unrecognized subcommand 'add' ...
```
</red>

<green>
Post-edit: the `--symlink` / `--copy` / `--template` flags form a clap
`ArgGroup` with `multiple(false)`. Passing two produces a usage error
(exit 2) whose stderr names both conflicting flags, satisfying the exit-2
and substring assertions.

```
test add_two_mode_flags_is_a_usage_error ... ok
```
</green>

### Scenario 4 — non-TTY without `--module` exits 1 naming the missing flag (REQ-002 done-when)

`add_non_tty_without_module_exits_1`.

<red>
Pre-edit: exit 2 (unknown subcommand), not the expected 1:

```
thread 'add_non_tty_without_module_exits_1' panicked at patina-cli/tests/add_cli.rs:117:5:
  left: 2
 right: 1
```
</red>

<green>
Post-edit: `patina add ~/.zshrc --symlink --yes` in a non-TTY subprocess
exits 1, stderr names `--module`, the original `~/.zshrc` is untouched, and
no `zsh/` module directory is created (the refusal precedes any mutation).

```
test add_non_tty_without_module_exits_1 ... ok
```
</green>

### Scenario 5 — an already-managed path exits 1 naming the owning module (REQ-002 done-when)

`add_already_managed_path_exits_1`.

<red>
Pre-edit: exit 2 (unknown subcommand), not the expected 1:

```
thread 'add_already_managed_path_exits_1' panicked at patina-cli/tests/add_cli.rs:151:5:
  left: 2
 right: 1
```
</red>

<green>
Post-edit: with a pre-existing `zsh` module already managing `~/.zshrc`,
`patina add ~/.zshrc --module other --copy --yes` scans every module's
manifest, finds the matching target, exits 1, and names both "already
managed" and the owning module `zsh` on stderr; the original target is
untouched.

```
test add_already_managed_path_exits_1 ... ok
```
</green>

### Scenario 6 — `add --json --yes` emits a deterministic document (REQ-010)

`add_json_emits_deterministic_document`.

<red>
Pre-edit: exit 2 (unknown subcommand), not the expected 0:

```
thread 'add_json_emits_deterministic_document' panicked at patina-cli/tests/add_cli.rs:174:5:
  left: 2
 right: 0
```
</red>

<green>
Post-edit: `patina add ... --json --yes` exits 0 and emits a single JSON
document on stdout whose `added` / `module` / `mode` fields carry
`~/.zshrc` / `zsh` / `symlink`. The envelope carries no timestamp / PID, so
it is deterministic for a given input (REQ-010).

```
test add_json_emits_deterministic_document ... ok
```
</green>

### Scenario 7 — `add` serializes behind a held exclusive lock (CHK-016 / REQ-009)

`add_serializes_behind_a_held_exclusive_lock`.

<red>
Pre-edit: exit 2 (unknown subcommand), not the expected 0:

```
thread 'add_serializes_behind_a_held_exclusive_lock' panicked at patina-cli/tests/add_cli.rs:232:5:
  left: 2
 right: 0
```
</red>

<green>
Post-edit: the `patina-core` `lock_helper` example (process A) acquires the
exclusive lock at `<state>/lock` and holds it for 2 seconds; once A is
observed holding the lock (its `ACQUIRED` marker), `patina add` (process B)
is launched with `PATINA_LOCK_TIMEOUT_MS=30000`. B blocks for at least 1
second (a clear majority of the 2s hold) before completing successfully
(exit 0), proving it waited on A's lock rather than racing it. The staged
source file exists after the wait.

```
test add_serializes_behind_a_held_exclusive_lock ... ok
```
</green>

## Hygiene-gate evidence (catch-all for `hygiene`-labelled CHKs)

The full hygiene suite was run at the end of the turn:

- `cargo test --workspace` — exit 0 (all binaries pass, including the new
  `add_cli` integration crate and the `cmd::add` unit tests).
- `cargo clippy --workspace --all-targets --all-features -- -D warnings` —
  exit 0.
- `cargo +nightly fmt --all --check` — exit 0.
- `cargo deny check` — exit 0 (no new dependency introduced).
