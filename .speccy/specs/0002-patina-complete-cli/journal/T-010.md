---
spec: SPEC-0002
task: T-010
generated_at: 2026-05-31T00:00:00Z
---

<implementer date="2026-05-31T00:00:00Z" model="claude-opus-4-8[1m]" round="1">
- Completed: Implemented the read-only `patina doctor` path (REQ-005,
  REQ-010), the complete v1.0 finding set with a deterministic `--json`
  document. `--fix` remediation is left to T-011.
  - `patina-cli/src/cli.rs`: added the `Doctor(DoctorArgs)` subcommand
    variant and `DoctorArgs { fix, json, yes }`. `--fix` / `--yes` are
    parsed but inert on the read-only path (T-011 wires their behaviour);
    the doc comments say so.
  - `patina-cli/src/cmd/doctor.rs` (new, registered in `cmd/mod.rs`): the
    findings model — `Finding { code, level, message, path: Option<…> }`,
    a `FindingCode` enum (`WinUnc` / `WinDevMode` / `WinOsOld` /
    `NoDefaultRepo`) with a single `label()` mapping to the `DOC-*`
    strings, and a `Level { Info, Warning, Error }` with its own
    `label()`. The four v1.0 findings:
    - `DOC-WIN-UNC` (warning) — Windows + resolved repo path is UNC
      (`is_unc_path`, T-007). Carries the repo path in `path`.
    - `DOC-WIN-DEVMODE` (warning) — Windows + repo declares any
      `symlink`/`symlink-dir` `[[file]]` AND `dev_mode_status()` is
      `Disabled`; message names `Developer Mode` and
      `DEV_MODE_REGISTRY_PATH`.
    - `DOC-WIN-OSOLD` (warning) — Windows + `windows_build_supports_dev_mode()`
      is false.
    - `DOC-NO-DEFAULT-REPO` (info, not warning) — no `default_repo` pointer
      (`persisted_default_present`, T-002); message suggests `patina init`.
  - Read-only lock (REQ-009): `run` acquires `LockKind::Shared` with
    `SHARED_TIMEOUT`, mirroring `status` — a `LockError::Timeout` is
    downgraded to a stderr warning and the read proceeds; any other lock
    error is fatal.
  - Exit codes (REQ-005): `exit_code()` returns 1 iff any `Level::Error`
    finding exists, else 0. The v1.0 set has no error finding, so the
    exit-1 path is reserved for future additions (asserted in the unit
    tests).
  - Output (REQ-010): findings render to stderr (human mode: one warning
    line each, or a "no findings" line on a clean env); `--json` emits a
    single `{ "findings": [ {code, level, message, path?} ] }` document on
    stdout. The `path` key is omitted entirely when a finding has no
    associated path. The document is deterministic — built from a
    stable-ordered `Vec`, no timestamps/PIDs — so two runs are
    byte-identical (CHK-018).
  - Design seam: `compute_findings(&Inputs)` is pure over its argument;
    `gather_inputs` does the IO (repo discovery is best-effort — an
    unresolvable repo yields no repo-scoped findings rather than aborting,
    since doctor is a diagnostic). This makes the whole finding set,
    including the three Windows findings, unit-testable on the Linux/macOS
    CI by filling `Inputs` fields directly.
  - `main.rs`: dispatches `Command::Doctor` to `cmd::doctor::run` (a plain
    `fn`, not `async` — the read-only path does no `.await`).
- Undone: nothing in scope is deferred. `--fix` and its exclusive-lock /
  prompt / remediation behaviour are T-011's, and `DoctorArgs` already
  carries `--fix`/`--yes` for that task. CHK-010's full Windows host run
  ships as `#[cfg(windows)]` `#[ignore]` exactly as the task body
  prescribes.
- Hygiene checks:
  - `cargo test --workspace`: exit 0 (all suites pass; the one new
    `#[cfg(windows)]` `#[ignore]` doctor test is not compiled on the host).
  - `cargo clippy --workspace --all-targets --all-features -- -D warnings`:
    exit 0.
  - `cargo +nightly fmt --all --check`: exit 0 (the
    `float_literal_trailing_zero` warnings are pre-existing config noise).
  - `cargo deny check`: exit 0.
- Evidence: paper trail at
  `.speccy/specs/0002-patina-complete-cli/evidence/T-010.md`.
  Roll call for CHKs under REQ-005 / REQ-010:
  - CHK-010 (Windows, Dev Mode OFF + symlink repo → `DOC-WIN-DEVMODE`
    warning naming `Developer Mode` + registry path): demonstrated (logic)
    → unit test `windows_devmode_finding_requires_symlink_and_disabled`
    fills `Inputs { is_windows: true, dev_mode: Disabled,
    repo_declares_symlink: true, .. }` and asserts the warning's
    code/level/message on the CI host. The end-to-end host form is
    `judgment-only` on a non-Windows CI →
    `windows_devmode_off_with_symlink_repo_warns` (`#[cfg(windows)]`
    `#[ignore]`) encodes the integration contract; needs a real Windows
    host with Developer Mode off.
  - CHK-017 (`patina init` determinism across repeated identical runs):
    hygiene → owned by `init` (T-001); not in T-010's diff. It appears in
    this task's rendered scenario set because REQ-010 spans both commands,
    but `init`'s deterministic stdout is already covered by `init_cli.rs`
    under `cargo test`. T-010 adds the parallel `doctor` determinism proof
    (CHK-018).
  - CHK-018 (`doctor --json` byte-identical across two runs against
    unchanged state): demonstrated → integration test
    `doctor_json_is_byte_identical_across_runs` runs the real binary twice
    and asserts `first.stdout == second.stdout`; the determinism slice
    would go red on any non-deterministic JSON field.
  - DOC-NO-DEFAULT-REPO info contract (REQ-005 done-when): demonstrated →
    `missing_default_repo_reports_info_finding_and_exits_zero` (integration,
    asserts `level = info` + exit 0) and `missing_default_repo_is_info_not_warning`
    (unit). The DOC-WIN-UNC predicate is exercised cross-platform by
    `unc_predicate_distinguishes_unc_from_posix_repo_paths` plus the
    `windows_unc_repo_warns_naming_the_path` unit test.
- Discovered issues: none. The `Inputs` struct trips
  `clippy::struct_excessive_bools` (four independent host-state booleans);
  suppressed with an `#[expect]` carrying a reason that they are distinct
  facts from distinct sources, not a state machine — mirroring the existing
  `AddArgs` precedent in the same crate.
- Procedural compliance: SPEC.md untouched. TASKS.md edited only to flip
  T-010 `pending → in-progress → in-review` (no other hand-edits). No
  `unwrap`/`expect`/`panic!` in production code (the two integration-test
  `#[expect(clippy::expect_used)]` crate attributes match the sibling
  `*_cli.rs` precedent). No `println!`/`eprintln!` outside the Reporter
  layer — all output routes through the `Reporter`. The one production
  suppression (`#[expect(clippy::struct_excessive_bools)]`) carries a
  justification. No new dependency surface; `cargo deny check` passed.
</implementer>

<review persona="business" verdict="pass" model="claude-opus-4-8[1m]/xhigh" date="2026-05-31T06:08:01Z" round="1">
T-010 delivers the read-only `patina doctor` path per REQ-005 and REQ-010 with no SPEC-vs-diff gap.

Every REQ-005 `<done-when>` maps to the diff: the four-finding v1.0 set (`DOC-WIN-UNC`, `DOC-WIN-DEVMODE`, `DOC-WIN-OSOLD`, `DOC-NO-DEFAULT-REPO`) is implemented with the correct codes/levels at `patina-cli/src/cmd/doctor.rs:241-298`. The Developer Mode finding correctly gates on `repo_declares_symlink && dev_mode == Disabled` and names both "Developer Mode" and the registry path (CHK-010 contract). `DOC-NO-DEFAULT-REPO` is `Info` (not warning) and suggests `patina init`. Exit code is 0 unless an `Error` finding exists; the v1.0 set has none, so exit-1 is reserved exactly as the SPEC prescribes (`doctor.rs:303-309`).

REQ-010 is honored: the `--json` envelope `{findings:[{code,level,message,path?}]}` goes to stdout via `Reporter::json`, findings route to stderr via `Reporter::warn`, the `path` key is omitted when absent, and the document carries no timestamps/PIDs — verified byte-identical by the passing CHK-018 integration test `doctor_json_is_byte_identical_across_runs`.

Non-goals respected: no cloud-sync detection (DEC-004 docs-only stance preserved); open question (b) "cloud-sync removed entirely from v1.0" is not silently re-added. On a too-old Windows build `dev_mode_status()` returns `Unsupported` (not `Disabled`), so only the OS-too-old warning fires, avoiding a confusing redundant DevMode warning — matching SPEC intent.

`--fix`/`--yes` are parsed but inert on this read-only path, with explicit doc comments in both `cli.rs` (DoctorArgs) and `doctor.rs` stating remediation is T-011's scope. The command never branches on `args.fix`, so no user is misled into thinking a fix ran — this is the intended T-010/T-011 decomposition, not silently dropped error-path scope. CHK-017 (init determinism) appears in the rendered scenario set because REQ-010 spans both commands, but it is owned by T-003's `init_cli.rs` (confirmed present and covering the out2/out3 byte-stability contract), not T-010's diff.

All 13 doctor unit tests and 4 integration tests pass. The diff serves the "fresh-machine user wants doctor to surface misconfigurations" story without answering a different question.
</review>

<review persona="tests" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-05-31T06:08:01Z" round="1">
T-010's tests genuinely exercise the REQ-005/REQ-010 behavior; the roll call is complete and honest.

Evidence walk passes all five steps: `Evidence:` field present (journal L67-68), file exists at `.speccy/specs/0002-patina-complete-cli/evidence/T-010.md`, no fabrication pattern matched (prose paper-trail shape matches the accepted sibling T-009; every test name cited in the evidence appears in the diff and passes). Roll call covers every CHK under the covered REQs: CHK-010 (REQ-005), CHK-017 + CHK-018 (REQ-010). CHK-011 correctly belongs to REQ-006/T-011, not here.

Behavioral verification (not mock-the-SUT): `Command::Doctor` dispatches to the real `cmd::doctor::run`, which gathers real `dev_mode_status()`/`windows_build_supports_dev_mode()`/`resolve_repository_root()`/`persisted_default_present()` then calls the pure `compute_findings`. Integration tests in `doctor_cli.rs` spawn the real `patina` binary end-to-end. The pure-function seam is a legitimate testability design, not a cheat — `compute_findings` is the unit under test for the finding decision.

Mutation-checked the load-bearing assertions:
- Dropping the symlink gate on DOC-WIN-DEVMODE (`doctor.rs:260`) fails `windows_devmode_finding_absent_when_no_symlink_declared`.
- Flipping DOC-NO-DEFAULT-REPO Info→Warning (`doctor.rs:289`) fails both the unit test `missing_default_repo_is_info_not_warning` and the integration test `missing_default_repo_reports_info_finding_and_exits_zero` — the REQ-005 info-vs-warning contract is genuinely gated.

CHK-018 determinism (`doctor_cli.rs:59`) byte-compares two real-binary runs (`first.stdout == second.stdout`), not a snapshot. CHK-017's `hygiene` label cites `init_json_success_and_failure_diverge_then_failure_is_byte_stable` in `init_cli.rs:108`, which I verified covers the out1≠out2 / out2==out3 scenario exactly — accurate and re-runnable. CHK-010's host form is `judgment-only` (the `#[cfg(windows)] #[ignore]` test), correctly deferred since no Windows host exists on CI; its finding logic is separately `demonstrated` by the pure unit test asserting code/level/message. All 13 unit + 4 integration tests pass.
</review>

<review persona="security" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-05-31T06:08:01Z" round="1">
No security issues in the T-010 read-only `patina doctor` slice. The command is read-only (acquires only a `Shared` lock; `--fix`/`--yes` are inert until T-011), introduces no injection vector (findings are fixed format strings plus a resolved repo path, with all `--json` escaping delegated to `serde_json` in `patina-cli/src/cmd/doctor.rs:315-331`), and reads no secrets — the Windows registry reads in `patina-core/src/windows/registry.rs` are read-only HKLM machine-capability facts (`AllowDevelopmentWithoutDevLicense`, `CurrentBuildNumber`) and the process-token elevation flag, not credentials. The only user-identifying datum emitted is the resolved repository path in the UNC finding, which is the command's intended diagnostic output to its own operator, not a leak to an untrusted caller. No new dependency surface (no Cargo.toml change in this slice), no panics (`json_envelope` uses `unwrap_or_else`, repo discovery is best-effort), and the shared-lock-timeout downgrade is appropriate for a diagnostic with no authorization decision behind it (no TOCTOU concern). Relevant files: `patina-cli/src/cmd/doctor.rs`, `patina-cli/src/cli.rs`, `patina-core/src/windows/registry.rs`.
</review>

<review persona="style" verdict="pass" model="claude-sonnet-4-6[1m]/medium" date="2026-05-31T06:08:01Z" round="1">
The implementation is clean and follows established project conventions throughout. Key checks:

- `unwrap_or_else(|_| "{}".to_owned())` on `serde_json::to_string_pretty` matches the identical pattern used across `rollback.rs`, `status.rs`, `add.rs`, `remove.rs`, and `promote.rs` — idiomatic for this codebase.
- The `#[expect(clippy::struct_excessive_bools, reason = "...")]` suppression on `Inputs` carries a justification and mirrors the precedent on `AddArgs` in the same crate.
- `reporter.line(...)` for the "no findings" path routes to stdout (informational status), while findings go via `reporter.warn(...)` to stderr — consistent with the Reporter split documented in `output/reporter.rs` and the existing pattern in `status.rs`.
- No `unwrap()`, `expect()`, `panic!`, `println!`, or `eprintln!` appear in production code paths.
- Import ordering, naming, and error propagation style match the surrounding `cmd/` files.
- The `pub fn compute_findings` and `pub fn run` lack `# Examples` sections, but no existing `patina-cli` cmd file carries them either — not drift introduced by this diff.
</review>

<review persona="correctness" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-05-31T06:08:01Z" round="1">
No correctness defects found in the `patina doctor` read-only path (T-010).

Verified the logic and control flow in `patina-cli/src/cmd/doctor.rs`:
- All three `DOC-WIN-*` findings are correctly gated under `if inputs.is_windows` (set from `cfg!(windows)`), so they never fire on non-Windows hosts even when every trigger field is met; the `DOC-WIN-DEVMODE` predicate (`repo_declares_symlink && dev_mode == Disabled`) and the `Unsupported`/`Enabled` split versus `DOC-WIN-OSOLD` (`!build_supports_dev_mode`) are mutually consistent with no double-fire or missed branch.
- Empty-collection edges in `repository_declares_symlink` are handled (`.any()` over zero modules yields `false`; a parse failure skips that module via `is_ok_and` rather than aborting), matching the best-effort diagnostic intent.
- The shared-lock guard is bound for the whole read (`_guard`), with a correct three-arm match: `Ok` holds it, `Timeout` warns-and-proceeds (REQ-009 read-only escape hatch), any other lock error is fatal.
- Exit code returns `Generic` (1) only on a `Level::Error` finding, else `Success` (0), per REQ-005; the v1.0 set has none, so 0 in practice.
- Determinism (CHK-018) holds: no timestamps/PIDs, stable finding-order `Vec`, `serde_json::Map` is order-deterministic, and the `path` key is omitted for pathless findings. Confirmed by the byte-identical integration test.
- No `unwrap`/`expect`/`panic` on production paths; the `to_string_pretty` fallback is non-panicking.

A transient failure of two unit tests during review traced to a stale incremental-build object in my own session, not the source; a clean rebuild passes all 13 `cmd::doctor` unit tests and all 4 `doctor_cli` integration tests, with clippy clean.
</review>
