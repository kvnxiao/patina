---
spec: SPEC-0002
task: T-011
generated_at: 2026-05-31T06:16:13Z
---

<implementer date="2026-05-31T06:16:13Z" model="claude-opus-4-8[1m]" round="1">
- Completed: Implemented `patina doctor --fix` interactive remediation
  (REQ-006) with its exclusive-lock acquisition (REQ-009). In
  `patina-cli/src/cmd/doctor.rs`, `run` now takes `tty: Tty` and
  `reader: &mut impl PromptReader` and branches on `args.fix`:
  `run_report` is the unchanged T-010 read-only path (shared lock +
  escape hatch); `run_fix` is the new mutating path. `run_fix` refuses a
  non-TTY `--fix` without `--yes` up front (stderr warning naming `--yes`,
  exit 1) before any lock or mutation; otherwise it acquires the EXCLUSIVE
  lock at `<state>/lock` via `acquire_lock(.., LockKind::Exclusive,
  exclusive_timeout())`, recomputes findings under the lock, and walks
  them: `DOC-NO-DEFAULT-REPO` → `fix_default_repo` (prompt, then write the
  canonical CWD via `write_persisted_default`); `DOC-WIN-DEVMODE` →
  `fix_dev_mode` (`#[cfg(windows)]`: prompt, then drive
  `launch_elevate_helper` and map `ElevationOutcome`); non-fixable
  `DOC-WIN-UNC` / `DOC-WIN-OSOLD` surface a why-not warning. A shared
  `accept` helper mirrors the `confirm` shape in `remove.rs`/`promote.rs`.
  Each remediation that runs emits a structured `tracing::info!` event
  recording `finding`, `remediation`, and `outcome` (REQ-006). A
  non-Windows `fix_dev_mode` stub keeps the call site compiling on all
  platforms (the parity pattern `apply.rs` already uses). `main.rs`'s
  `Doctor` arm now detects the TTY and passes a `StdinReader` like the
  other prompting commands. Added `Fixture::run_in` (sets the subprocess
  CWD) to `patina-cli/tests/common/mod.rs`.

- Undone: T-012 (documenting the new commands in `docs/USER_GUIDE.md`,
  including the `doctor --fix` flow) is a separate task. Nothing in
  T-011's scope is deferred.

- Hygiene checks: `cargo test --workspace` → exit 0 (full suite green,
  including the new unit tests `accept_yes_proceeds_without_reading`,
  `accept_non_tty_without_yes_declines`, `accept_tty_reads_the_answer`,
  `fix_in_non_tty_without_yes_refuses_exit_one`, and the integration tests
  `fix_yes_writes_default_repo_from_cwd_and_exits_zero` and
  `fix_without_yes_in_non_tty_exits_one_naming_yes`). `cargo clippy
  --workspace --all-targets --all-features -- -D warnings` → exit 0.
  `cargo +nightly fmt --all --check` → exit 0. `cargo deny check` → exit 0
  (no new dependencies).

- Evidence: paper trail at
  `.speccy/specs/0002-patina-complete-cli/evidence/T-011.md`. Roll call
  for the CHKs under the covered REQs:
  - CHK-011 (REQ-006: Windows `doctor --fix` enables Developer Mode via
    the UAC flow, registry reads 1, exit 0): hygiene →
    `windows_fix_enables_dev_mode_and_exits_zero` in
    `patina-cli/tests/doctor_cli.rs`, gated `#[cfg(windows)]` `#[ignore]`
    because the macOS/Linux CI has no Windows registry or UAC dialog. The
    CI host structurally exercises the remediation wiring (`--fix`
    accepting and routing `DOC-WIN-DEVMODE` to the elevation flow) via the
    `accept_*` unit tests and the `#[cfg(windows)]` body compiling under
    `cargo clippy --all-targets`. See the evidence file's "Coverage note:
    CHK-011 on a non-Windows CI".
  - CHK-016 (REQ-009: two concurrent `patina add` serialize on the
    exclusive lock): hygiene → CHK-016's scenario concerns `patina add`,
    proved in the `add` task (T-004,
    `patina-cli/tests/add_cli.rs`); it runs under `cargo test --workspace`.
    T-011's contribution to REQ-009 is that `doctor --fix` acquires the
    same exclusive lock at `<state>/lock` before mutating — demonstrated by
    Scenario 2 (the non-TTY refusal returns before the lock, and Scenario
    1 mutates only after `run_fix` takes
    `LockKind::Exclusive`).

- Discovered issues: None. The `ScriptedReader` test helper is duplicated
  per-module (also in `remove.rs`, `promote.rs`) because this is a binary
  crate with no shared `#[cfg(test)]` util module; this matches the
  existing convention rather than introducing a new shared module within
  T-011's scope.

- Procedural compliance: This entry is the first journal block for T-011
  and creates `journal/T-011.md` with the three-field frontmatter. No
  `<implementer-note>` was written into TASKS.md. The TASKS.md
  `state="..."` attribute for T-011 flips from `in-progress` to
  `in-review` as the final step of this turn. No shipped skill bodies
  under `.claude/skills/` were edited.
</implementer>

<review persona="business" verdict="pass" model="claude-opus-4-8[1m]/high" date="2026-05-31T06:20:19Z" round="1">
T-011 (`patina doctor --fix`) satisfies REQ-006 and its REQ-009 contribution. Every REQ-006 done-when bullet maps to the diff in `patina-cli/src/cmd/doctor.rs`: `--fix` takes the EXCLUSIVE lock before mutating (REQ-009), refuses non-TTY-without-`--yes` with exit 1 naming `--yes` *before* lock/mutation, remediates `DOC-NO-DEFAULT-REPO` by writing the canonical CWD and `DOC-WIN-DEVMODE` via the T-009 UAC flow (`#[cfg(windows)]`), keeps non-fixable findings (UNC, OS-old) as warnings, and emits per-fix `tracing` events. Both non-Windows task scenarios pass as real integration tests (`fix_yes_writes_default_repo_from_cwd_and_exits_zero`, `fix_without_yes_in_non_tty_exits_one_naming_yes`); CHK-011 is correctly `#[cfg(windows)] #[ignore]`; CHK-016 is a `patina add` REQ-009 scenario covered in T-004, not regressed here. REQ-010 stderr/stdout channels verified. No non-goal breach — `--yes` is the SPEC-sanctioned auto-accept, not a smuggled `doctor --auto-fix`. `cargo test`/`clippy` green.
Non-blocking note: the `ElevationOutcome::RanButStillDisabled` arm at `patina-cli/src/cmd/doctor.rs:381` returns a hard error (names the registry path, exit 1 — matching REQ-007) but emits no `tracing` event, whereas REQ-006's done-when says "each fix that runs writes a structured trace event ... and the outcome." The enabled/declined arms the CHK-011 scenario exercises do emit it, and this is a Windows-only `#[ignore]`d path, so it does not block; recommend adding the trace event in a follow-up for full observability parity.
</review>

<review persona="tests" verdict="pass" model="claude-opus-4-8[1m]/high" date="2026-05-31T06:20:19Z" round="1">
T-011's tests genuinely exercise the REQ-006 scenarios. Both CI-runnable scenarios are driven end-to-end against the real `patina` binary (no SUT mocking): `fix_yes_writes_default_repo_from_cwd_and_exits_zero` (patina-cli/tests/doctor_cli.rs:98) spawns with CWD set to the fixture repo, asserts exit 0, reads the actual `default_repo` pointer, and asserts its trimmed contents equal `canonicalize_path(&fx.root)` — it would fail if the remediation didn't write, wrote the wrong path, or returned non-zero. The negative path `fix_without_yes_in_non_tty_exits_one_naming_yes` (doctor_cli.rs:126) asserts exit 1 + the `--yes` substring on stderr + that the pointer was NOT written, so it catches both a missing refusal and a premature mutation; assertions are specific, not catch-any-error. The `accept` decision helper has unit coverage across all three branches and `run_fix`'s non-TTY refusal is unit-tested. The `ScriptedReader` doubles the stdin seam (user keystrokes), not the system under test, so this is not the mock-the-SUT antipattern. Evidence file is present, its red/green halves differ materially, named tests appear in the diff, and the recorded command is scoped per-test rather than the full-suite hygiene run. The roll call covers both CHKs the task carries: CHK-011 is `hygiene` with a justified `#[cfg(windows)] #[ignore]` cite (`windows_fix_enables_dev_mode_and_exits_zero`) since CI has no Windows registry/UAC, and CHK-016 is `hygiene` correctly attributed to T-004's `add_serializes_behind_a_held_exclusive_lock` because that scenario is literally about `patina add`. Non-blocking observation: `doctor --fix`'s exclusive-lock acquisition (REQ-009) has no dedicated CI test that would fail if the lock were dropped, but the SPEC pins no `doctor --fix` contention scenario (CHK-016 targets `add`), so this is out of test-coverage scope.
</review>

<review persona="security" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-05-31T06:20:19Z" round="1">
No security issues in T-011's `patina doctor --fix` slice. Elevation stays privilege-separated (main process never elevates; only `patina-elevate.exe` via UAC `runas`, helper resolved from `current_exe()` sibling, fixed subcommand arg — no injection). Mutations are consent-gated: non-TTY `--fix` without `--yes` refuses before any lock or write (`patina-cli/src/cmd/doctor.rs:249`), and findings are recomputed under the EXCLUSIVE lock before remediation (no consent/mutation TOCTOU). No secret handling, no sensitive-data leak in the `tracing::info!` events (finding/remediation/outcome/canonical repo path only), no new dependencies. Test fixtures isolate HOME/state to tempdirs so `--fix` cannot touch real machine state.
</review>

<review persona="style" verdict="blocking" model="claude-sonnet-4-6[1m]/medium" date="2026-05-31T06:20:19Z" round="1">
`patina-cli/src/cmd/doctor.rs` names the per-finding consent helper `accept` (line ~408–425), while both sibling commands that perform the same "prompt-or-auto-accept-under-`--yes`" pattern use `confirm`:

- `remove.rs:233`: `fn confirm(args: &RemoveArgs, tty: Tty, reader: …, reporter: …) -> bool`
- `promote.rs:171`: `fn confirm(args: &PromoteArgs, tty: Tty, reader: …, reporter: …) -> bool`

The function body and call-site tests in `doctor.rs` also use `accept` (`accept_yes_proceeds_without_reading`, `accept_non_tty_without_yes_declines`, `accept_tty_reads_the_answer`). Rename `accept` → `confirm` in `doctor.rs` and update the three corresponding unit-test names to match the `confirm_*` naming established by the siblings. The AGENTS.md mandate is explicit: "Make the diff read as though the surrounding code's author wrote it: follow the established naming … patterns of the files you touch."
</review>

<review persona="correctness" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-05-31T06:20:19Z" round="1">
No correctness defects found in T-011's `doctor --fix` remediation slice.
Verified: the `ElevationOutcome` match in `patina-cli/src/cmd/doctor.rs:363` is exhaustive with correctly-mapped arms (no inverted/swapped branch); the `accept` match (`patina-cli/src/cmd/doctor.rs:423`) covers all `(yes, tty)` combinations without inversion; the non-TTY-without-`--yes` guard (`run_fix`, line ~255) returns exit 1 before acquiring the lock or mutating, confirmed by `fix_without_yes_in_non_tty_exits_one_naming_yes`; the exclusive lock guard is held across the whole `--fix` body and released via RAII on every exit path including `?` early returns from `fix_default_repo`/`fix_dev_mode` (no resource/lock leak); the stable finding order (UNC, DevMode, OSOld, NoDefaultRepo) makes `DOC-WIN-DEVMODE` the first prompt on the CHK-011 host, so a single auto-typed `y` drives elevation and exits 0; the sync (non-`.await`) Doctor arm in `main.rs:77` is type-consistent with the other `Result<i32>` arms. Doctor unit (17) and integration (6) tests pass locally.
</review>

<blockers date="2026-05-31T06:20:19Z" round="1">
Rename the per-finding consent helper `accept` → `confirm` to match the established sibling-command naming convention before the next implementer pass.
- style (blocking): `patina-cli/src/cmd/doctor.rs` names the consent helper `accept`, but `remove.rs:233` and `promote.rs:171` both name the identical "prompt-or-auto-accept-under-`--yes`" helper `confirm`. Rename the function `accept` → `confirm` and rename the three unit tests `accept_yes_proceeds_without_reading` / `accept_non_tty_without_yes_declines` / `accept_tty_reads_the_answer` to the matching `confirm_*` names. AGENTS.md mandates the diff follow the established naming patterns of the files it touches.
</blockers>

<implementer date="2026-05-31T07:05:00Z" model="claude-opus-4-8[1m]/medium" round="2">
- Completed: Addressed the round-1 style blocker by renaming the
  per-finding consent helper `accept` → `confirm` in
  `patina-cli/src/cmd/doctor.rs`, matching the identical helper in
  `remove.rs:233` and `promote.rs:171`. Updated both call sites
  (`fix_default_repo`, `fix_dev_mode`) and renamed the three unit tests
  to the sibling `confirm_*` convention:
  `confirm_yes_proceeds_without_reading`,
  `confirm_non_tty_without_yes_declines`, `confirm_tty_reads_the_answer`.
  The renamed doctor tests now read byte-for-byte like the `confirm_*`
  trio already present in `remove.rs` and `promote.rs`. No behaviour
  changed: the function body, signature, call sites, and the round-1
  integration tests are untouched. The remaining lowercase "accept" /
  "auto-accept" tokens in the module/function doc prose and the non-TTY
  refusal message are the English verb describing consent, not the
  function identifier, and mirror the consent-verb prose in the sibling
  commands — left as-is.

- Undone: Nothing in T-011's scope is deferred. T-012 (USER_GUIDE.md
  docs) remains a separate task.

- Hygiene checks: `cargo test --workspace` → exit 0 (full suite green;
  the renamed `confirm_*` doctor tests pass alongside the unchanged
  integration tests). `cargo clippy --workspace --all-targets
  --all-features -- -D warnings` → exit 0. `cargo +nightly fmt --all
  --check` → exit 0. `cargo deny check` → exit 0 (no dependency change).

- Evidence: paper trail unchanged at
  `.speccy/specs/0002-patina-complete-cli/evidence/T-011.md`; this round
  is a pure rename with no behavioural delta, so the round-1 red/green
  evidence still holds. Roll call for the CHKs under the covered REQs:
  - CHK-011 (REQ-006: Windows `doctor --fix` enables Developer Mode via
    the UAC flow, exit 0): hygiene →
    `windows_fix_enables_dev_mode_and_exits_zero` in
    `patina-cli/tests/doctor_cli.rs`, `#[cfg(windows)] #[ignore]` on the
    non-Windows CI; the `#[cfg(windows)]` body still compiles under
    `cargo clippy --all-targets` after the rename.
  - CHK-016 (REQ-009: two concurrent `patina add` serialize on the
    exclusive lock): hygiene → owned by T-004
    (`patina-cli/tests/add_cli.rs`); unchanged by this rename. T-011's
    REQ-009 contribution (the `doctor --fix` exclusive-lock acquisition)
    is untouched this round.

- Discovered issues: None.

- Procedural compliance: Round-2 entry appended after the existing
  round-1 `<implementer>` and `<blockers>` blocks; earlier blocks left
  intact (append-only journal). Amended the existing WIP in place — no
  `git restore`/`clean`/`checkout`, no from-scratch rewrite. TASKS.md
  `state` flipped `pending` → `in-progress` → `in-review` this turn. No
  `<implementer-note>` inlined in TASKS.md. No shipped `.claude/skills/`
  bodies edited.
</implementer>

<review persona="business" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-05-31T06:26:32Z" round="2">
T-011 (`patina doctor --fix`) still satisfies REQ-006 and its REQ-009 contribution; round-2 is a pure rename (`accept` → `confirm`, plus the three `confirm_*` unit-test names) addressing the round-1 style blocker, with no behavioral delta. Verified against the working-tree implementation: every REQ-006 done-when bullet maps to the diff in `patina-cli/src/cmd/doctor.rs` — non-TTY-without-`--yes` refuses with exit 1 naming `--yes` *before* the lock/mutation (`run_fix`, doctor.rs:249-255), `--fix` takes the EXCLUSIVE lock before mutating per REQ-009 (doctor.rs:261), `DOC-NO-DEFAULT-REPO` writes the canonical CWD (doctor.rs:301-333), `DOC-WIN-DEVMODE` drives the T-009 UAC flow and re-checks under `#[cfg(windows)]` (doctor.rs:340-386), non-fixable UNC/OS-old stay warnings (doctor.rs:280-287), and per-fix `tracing` events fire (doctor.rs:324/359/369). Both CI-runnable task scenarios pass as real binary integration tests; CHK-011 is correctly `#[cfg(windows)] #[ignore]`; CHK-016 is a `patina add` REQ-009 scenario owned by T-004, not regressed here. No non-goal breach — `--yes` is the REQ-006-sanctioned auto-accept, not a smuggled `doctor --auto-fix`, and dev-mode toggling stays consent-gated behind `confirm`. The round-1 non-blocking observation stands unchanged (the `ElevationOutcome::RanButStillDisabled` arm emits no `tracing` event) and remains a non-blocking follow-up.
</review>

<review persona="tests" verdict="pass" model="claude-opus-4-8[1m]/high" date="2026-05-31T06:26:32Z" round="2">
T-011 round-2 passes the tests review. The round-1 blocker was a pure rename (`accept` -> `confirm`), and it landed verifiably: `patina-cli/src/cmd/doctor.rs:410` now defines `fn confirm`, both call sites (`fix_default_repo:308`, `fix_dev_mode:346`) use it, and the three unit tests are renamed to `confirm_yes_proceeds_without_reading` / `confirm_non_tty_without_yes_declines` / `confirm_tty_reads_the_answer` (doctor.rs:618/632/647), now byte-for-byte matching the sibling `confirm_*` trio in `remove.rs` and `promote.rs`. No `accept` identifier remains (only the English consent verb survives in doc prose and the refusal message, correctly left to mirror the siblings). Test behaviour is unchanged and genuinely exercises the SUT: both CI-runnable scenarios drive the real binary (`fix_yes_writes_default_repo_from_cwd_and_exits_zero` at doctor_cli.rs:98 asserts the pointer's content equals `canonicalize_path(&fx.root)`; the negative path `fix_without_yes_in_non_tty_exits_one_naming_yes` at doctor_cli.rs:126 asserts exit 1 + the specific `--yes` substring + that the pointer was NOT written), and the `confirm` unit tests cover all `(yes, tty)` branches including both the `y` and `n` answers, so an inverted branch would fail. `ScriptedReader` doubles only the stdin seam, not the SUT. Evidence roll call covers both CHKs the task carries: CHK-011 is `hygiene` with a specific Windows-`#[ignore]` cite justified by the absence of a Windows registry/UAC on CI, and CHK-016 is `hygiene` correctly attributed to T-004's `patina-cli/tests/add_cli.rs`. No fabrication pattern matched; the evidence file's stale `accept_*` prose references are an honest cosmetic lag the round-2 journal openly flags as "pure rename, no behavioural delta" rather than a re-run.
</review>

<review persona="security" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-05-31T06:26:32Z" round="2">
No security issues in T-011's `patina doctor --fix` slice; round-2 is a pure `accept` → `confirm` rename addressing the style blocker with no behavioral change. Privilege separation holds (main process never elevates; only the T-009 UAC helper via `launch_elevate_helper`, fixed args, no injection); mutations stay consent-gated (non-TTY `--fix` without `--yes` refuses with exit 1 before any lock or write at `patina-cli/src/cmd/doctor.rs`, and `fix_without_yes_in_non_tty_exits_one_naming_yes` asserts no pointer write); findings recomputed under the EXCLUSIVE lock (no consent/mutation TOCTOU); `tracing` events expose only finding/remediation/outcome/canonical-repo-path (no secrets); no new dependencies; test fixtures isolate HOME/state/repo to tempdirs via `Fixture::run_in`.
</review>

<review persona="style" verdict="pass" model="claude-sonnet-4-6[1m]/medium" date="2026-05-31T06:26:32Z" round="2">
Round-1 blocker resolved. `patina-cli/src/cmd/doctor.rs` now uses `fn confirm` (line 410) and the three unit tests are named `confirm_yes_proceeds_without_reading`, `confirm_non_tty_without_yes_declines`, and `confirm_tty_reads_the_answer` — matching the convention established by `remove.rs` and `promote.rs`. No traces of the old `fn accept` / `accept_*` identifiers remain. No new style issues introduced by the rename.
</review>

<review persona="correctness" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-05-31T06:26:32Z" round="2">
No correctness defects in T-011's round-2 slice. The round-2 delta is a pure identifier rename (`accept` → `confirm`) in `patina-cli/src/cmd/doctor.rs`: function signature, body, and both call sites (`fix_default_repo` at doctor.rs:308, `fix_dev_mode` at doctor.rs:346) are logic-unchanged, and the three renamed unit tests are name-only. Re-verified the load-bearing invariants the round-1 correctness pass found, all intact: the `confirm` match (doctor.rs:423) covers every `(args.yes, tty)` pair with no inversion and `read_line().unwrap_or_default()` declines safely on `None` rather than panicking; the non-TTY-without-`--yes` guard (doctor.rs:255) returns exit 1 before lock acquisition or any mutation; the exclusive-lock `_guard` (doctor.rs:267) is RAII-held across the whole `--fix` body and released on every `?` early return; the `ElevationOutcome` match (doctor.rs:363) is exhaustive with correctly-mapped arms; and the stable finding order (UNC, DevMode, OSOld, NoDefaultRepo) makes DevMode the sole/first finding on the CHK-011 host. No remaining `accept` identifier dangles — the two surviving "accept" tokens are English prose in the doc comment and the non-TTY refusal message.
</review>
