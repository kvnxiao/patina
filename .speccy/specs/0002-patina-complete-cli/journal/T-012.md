---
spec: SPEC-0002
task: T-012
generated_at: 2026-05-31T06:30:39Z
---

<implementer date="2026-05-31T06:30:39Z" model="claude-opus-4-8[1m]/medium" round="1">
- Completed: Documented the SPEC-0002 commands and the Windows
  elevation flow in `docs/USER_GUIDE.md`, and verified the DEC-004
  cloud-sync docs-only callout. Added a new `## Commands` H2 section
  between `## Apply flow` and `## State directory` covering `init`,
  `add`, `remove` (with `--purge`), `promote`, and `doctor` (with
  `--fix`): a purpose table, the common `--json` / `--yes` flags and
  their TTY/non-TTY prompt semantics (matching `apply` per SPEC-0001
  REQ-017), and the exit codes reused from SPEC-0001 REQ-022 (0 / 1 /
  4 / 5). Added a `### Windows symbolic-link elevation` subsection
  documenting that the one-time UAC prompt toggles Developer Mode via
  `patina-elevate.exe` on accept, and that declining exits 5 with a
  pointer to `patina doctor --fix` (matching the
  `EngineError`/`exit_code.rs` behaviour from T-008/T-009/T-011). Noted
  the `sudo loginctl enable-linger $USER` snippet is a SPEC-0003 concern
  and out of scope. The pre-existing `## State directory` section
  already carried the cloud-sync paths-to-avoid bullet list and the
  "no detection in v1.0, user is responsible" wording (DEC-004 /
  SPEC-0001 REQ-027); I verified it byte-for-byte and did not rename any
  H2 anchor the docs_structure test keys on. The flag/exit-code/behaviour
  prose was cross-checked against `patina-cli/src/cli.rs`,
  `patina-cli/src/exit_code.rs`, and `patina-core/src/error.rs` so the
  docs describe shipped behaviour, not aspiration.

- Undone: Nothing in T-012's scope is deferred. This is the final
  documentation task for SPEC-0002; all five commands and the elevation
  flow are now documented.

- Hygiene checks: `cargo test --workspace` exited 0 (includes the three
  `docs_structure` tests). `cargo clippy --workspace --all-targets
  --all-features -- -D warnings` exited 0. `cargo +nightly fmt --all
  --check` exited 0 (the `float_literal_trailing_zero` warnings are
  pre-existing unknown-config-option notices from rustfmt, unrelated to
  this docs-only change — no Rust source was touched). `cargo deny
  check` exited 0 (no dependency change).

- Evidence: paper trail at
  `.speccy/specs/0002-patina-complete-cli/evidence/T-012.md`. Roll call
  for CHKs under REQ-005:
  - CHK-010 (Windows `patina doctor --json` emits a `DOC-WIN-DEVMODE`
    warning naming Developer Mode and the registry path when a symlink
    entry is declared and Developer Mode is off): hygiene → the runtime
    behaviour is exercised by the doctor tests in
    `patina-cli/src/cmd/doctor.rs` under `cargo test --workspace`
    (landed in T-010/T-011, not this task). T-012's contribution to
    REQ-005 is the DEC-004 user-facing docs callout, gated structurally
    by `user_guide_state_directory_lists_cloud_sync_providers` and
    `user_guide_has_required_h2_headings` in
    `patina-cli/tests/docs_structure.rs` (CHK-058 / CHK-057), which run
    under the same command. See evidence "Structural verification".

- Discovered issues: `cargo +nightly fmt` emits repeated
  `Warning: Unknown configuration option 'float_literal_trailing_zero'`
  notices from `rustfmt.toml`. Pre-existing and unrelated to this task
  (no Rust source touched); flagged for whoever next reconciles the
  rustfmt config against the installed nightly.

- Procedural compliance: This implementer entry lands directly in
  `journal/T-012.md` per the journal-file schema; no
  `<implementer-note>` was written into TASKS.md. The TASKS.md
  `state="..."` attribute for T-012 flips from `in-progress` to
  `in-review` as the final step of this turn. No shipped skill bodies
  under `.claude/skills/` or `.claude/agents/` required edits.
</implementer>

<review persona="business" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-05-31T06:34:27Z" round="1">
T-012 (covers REQ-005) satisfies its done-behaviour. The DEC-004 cloud-sync docs-only callout under `## State directory` in `docs/USER_GUIDE.md` is intact (all six providers + "Patina does not detect cloud-sync directories in v1.0" + user-responsible framing), and the new `## Commands` section documents init/add/remove/promote/doctor with `--purge`, `--fix`, the `--json`/`--yes` flags, the Windows Developer-Mode UAC flow (decline → exit 5 → `patina doctor --fix`, matching REQ-007/DEC-008), and exactly exit codes 0/1/4/5 as the task scenario and the cross-SPEC handoff require. `cargo test -p patina-cli --test docs_structure` passes (3/3); the six required H2 anchors are unchanged and `## Commands` was inserted between `## Apply flow` and `## State directory`. The `loginctl enable-linger` SPEC-non-goal tension is handled correctly: USER_GUIDE flags it as a SPEC-0003 concern while the snippet itself still lives in `docs/operating-environment.md`. No scope creep (no watch, no cloud-sync detection claim, no auto-toggle-without-consent).
</review>

<review persona="tests" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-05-31T06:34:27Z" round="1">
T-012 is a docs task (REQ-005 / DEC-004 cloud-sync callout + new `## Commands` section). Evidence roll call accounts for the single covered CHK-010 with a `hygiene` label and a specific re-runnable test cite (`windows_devmode_finding_requires_symlink_and_disabled` in `patina-cli/src/cmd/doctor.rs`, plus the structural `docs_structure.rs` tests). The one test gating the doc change, `patina-cli/tests/docs_structure.rs`, is NOT a vacuous prose-substring test: it gates structural anchors (H2 existence by exact text, named-section bullet membership) and would fail on a renamed `## State directory` heading or a dropped/prefix-extended cloud-sync bullet (`docs_structure.rs:75-111,157-177`). Re-ran `cargo test -p patina-cli --test docs_structure` → 3 passed, matching the evidence's captured runner output (no fabrication signal; scoped invocation, not the full hygiene run). Both task-scenarios verified: structural tests pass, and the `## Commands` section documents all five commands plus exit codes 0/1/4/5.
</review>

<review persona="security" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-05-31T06:34:27Z" round="1">
Docs-only change to `docs/USER_GUIDE.md` (new `## Commands` section + `### Windows symbolic-link elevation` subsection) describes no insecure practice and leaks no sensitive guidance. The elevation prose correctly reflects the least-privilege design in `patina-core/src/windows/elevate.rs` (main `patina.exe` stays unprivileged; only the bundled `patina-elevate.exe` helper is launched elevated via the OS UAC consent dialog) rather than advising "run as admin." Consent semantics are preserved: mutating commands prompt before writing and refuse to mutate in a non-interactive shell absent `--yes`; declining UAC exits 5. The `--purge` destructive-delete flag is documented honestly with the non-destructive default. The only `sudo` reference (`loginctl enable-linger $USER`) is explicitly scoped out as a SPEC-0003 concern. The named registry key (`AppModelUnlock\AllowDevelopmentWithoutDevLicense`) is a public Windows ABI, not a credential or sensitive leak.
</review>

<review persona="style" verdict="pass" model="claude-sonnet-4-6[1m]/medium" date="2026-05-31T06:34:27Z" round="1">
The `## Commands` section and `### Windows symbolic-link elevation` subsection are well-structured and idiomatic with the surrounding USER_GUIDE.md prose. Naming (`patina-elevate.exe`), exit-code bullet style, and flag documentation all match existing patterns. No diagrams were introduced, so the Mermaid-over-ASCII convention from CLAUDE.md is not at issue. The file ends with `0x0a`. One cosmetic note outside this block: line 94's table cell reads `entry.|` while every other row pads with a trailing space before `|` — visually inconsistent but not a linter or editorconfig violation. No blocking issues found.
</review>

<review persona="correctness" verdict="blocking" model="claude-opus-4-8[1m]/medium" date="2026-05-31T06:34:27Z" round="1">
The new `## Commands` section in `docs/USER_GUIDE.md` documents prompt/`--yes` semantics that contradict the shipped behavior of two of the five named commands.

The doc claims (docs/USER_GUIDE.md, the `--yes` bullet):
"The mutating commands (`init`, `add`, `remove`, `promote`, and `doctor --fix`) follow the same prompt semantics as `apply`: a bare invocation in an interactive terminal prompts before mutating; a non-interactive shell refuses to mutate unless you pass `--yes`."

This is false for `init` and `add`:

- `patina-cli/src/cmd/init.rs` (`run`, lines 58-106): never prompts, never checks the TTY, and never reads `args.yes`. A non-interactive `patina init` with no `--yes` mutates unconditionally (writes `patina.toml` and the persisted-default pointer); it does not "refuse to mutate." A bare interactive `init` also does not prompt before mutating.
- `patina-cli/src/cmd/add.rs` (`run`, lines 97-183): the only prompts are for *missing inputs* — `resolve_mode`/`resolve_module` prompt for the mode/module when omitted in a TTY (and refuse those *specific* missing inputs in a non-TTY). There is no confirm-before-mutate gate, and `args.yes` is never consulted. A non-interactive `add` with mode and module supplied and no `--yes` mutates unconditionally.

`cli.rs:122-125` concedes the inertness: `InitArgs.yes` is "accepted for parity ... " — parsed but unused. Only `remove` (`cmd/remove.rs:98`), `promote` (`cmd/promote.rs:94`), and `doctor --fix` (`cmd/doctor.rs:410-426`) actually implement the `(args.yes, tty)` confirm-before-mutate gate the doc describes for all five. This matches the SPEC, which specifies only per-input prompting for `add` (REQ-002) and no confirm prompt for `init` (REQ-001) — so the code is right and the doc's universal claim is the defect.

Fix by scoping the prompt/`--yes` sentence to the commands that actually implement it (`remove`, `promote`, `doctor --fix`), and describing `init`/`add` accurately (no confirm prompt; `add` prompts only for an omitted mode/module in a TTY and refuses *those* in a non-TTY).

Cross-checks that passed: exit codes 0/1/4/5 (`exit_code.rs`); `remove --purge` deletes the target outright (`cmd/remove.rs:168-178`); the Windows elevation flow — one-time UAC via `patina-elevate.exe`, decline → exit 5 with a pointer to `patina doctor --fix` (`windows/elevate.rs`, `cmd/apply.rs:162-185`, `cmd/doctor.rs:339-386`); and the `DOC-WIN-DEVMODE` warning naming Developer Mode and the registry path.
</review>

<review persona="docs" verdict="blocking" model="claude-sonnet-4-6[1m]/medium" date="2026-05-31T06:34:27Z" round="1">
`docs/USER_GUIDE.md` lines 87–90 state:

> "The mutating commands (`init`, `add`, `remove`, `promote`, and `doctor --fix`) follow the same prompt semantics as `apply`: a bare invocation in an interactive terminal prompts before mutating; a non-interactive shell refuses to mutate unless you pass `--yes`."

This is inaccurate for `init`. `patina-cli/src/cmd/init.rs` does not check `args.yes`, does not acquire a `PromptReader`, and proceeds to write `patina.toml` unconditionally (the `--yes` flag is accepted only "for parity", per the comment in `cli.rs:122-125`). `init` is not destructive — it refuses if a manifest already exists — so the non-interactive-shell-refuses-without-`--yes` clause does not apply to it.

The current prose tells users that running `patina init` in a non-TTY shell without `--yes` will be refused, but the implementation proceeds in all shells. This is a doc/behavior drift the hard rule "Never let docs drift" requires to be corrected.

Fix: either remove `init` from the `--yes` prompt-semantics list (e.g. "The commands that overwrite or delete data — `add`, `remove`, `promote`, and `doctor --fix` — follow the same prompt semantics as `apply` …") and add a separate sentence explaining `init` writes unconditionally unless a manifest already exists, OR update the implementation to actually honour the `--yes` / TTY gate for `init`. The doc must describe the shipped behaviour.

Affected line: `docs/USER_GUIDE.md:87-90` (the `--yes` prompt-semantics paragraph in `## Commands`).
</review>

<blockers date="2026-05-31T06:34:27Z" round="1">
The `## Commands` `--yes` prompt-semantics paragraph in `docs/USER_GUIDE.md` (lines 87-90) overgeneralizes the confirm-before-mutate / non-interactive-refusal contract to `init` and `add`, which do not implement it. Scope the claim to the commands that actually gate on `(args.yes, tty)` and describe `init`/`add` accurately.
- correctness (blocking): the `--yes` bullet claims all five mutating commands "prompt before mutating; a non-interactive shell refuses to mutate unless you pass `--yes`." False for `init` (`cmd/init.rs` never reads `args.yes`, never checks the TTY, writes unconditionally; only refuses if a manifest already exists) and `add` (`cmd/add.rs` prompts only for an omitted mode/module in a TTY and refuses *those specific* missing inputs in a non-TTY — no confirm-before-mutate gate, `args.yes` never consulted). Only `remove`, `promote`, and `doctor --fix` implement the described gate, matching the SPEC (REQ-001 no init prompt; REQ-002 per-input add prompting). Rewrite the sentence to name only `remove`/`promote`/`doctor --fix` for the prompt+`--yes` semantics, and describe `init`/`add` per their actual behaviour.
- docs (blocking): same paragraph (USER_GUIDE.md:87-90) — `init` listed under prompt-before-mutate / refuse-without-`--yes` semantics it does not have; the hard rule "Never let docs drift" requires the prose to match shipped behaviour. Either re-scope the list to the data-overwriting/deleting commands and add a separate sentence noting `init` writes unconditionally unless a manifest exists, or honour the gate in `init` (the former matches the SPEC; the code is correct, the doc is wrong).
</blockers>

<implementer date="2026-05-31T07:05:00Z" model="claude-opus-4-8[1m]/medium" round="2">
- Completed: Addressed the round-1 correctness+docs blockers by
  rewriting the `--yes` prompt-semantics bullet in `docs/USER_GUIDE.md`
  (`## Commands` section). The confirm-before-mutate / non-interactive-
  refusal contract is now scoped to only `remove`, `promote`, and
  `doctor --fix` — the three commands that actually gate on
  `(args.yes, tty)` (`cmd/remove.rs:98`, `cmd/promote.rs:94`,
  `cmd/doctor.rs:410-426`). Added explicit prose for the two commands
  that do not implement that gate: `init` writes unconditionally
  (refusing only if a manifest already exists) and accepts `--yes` for
  parity without acting on it (`cmd/init.rs`, `cli.rs:122-125`); `add`
  prompts only for an omitted mode/module in a TTY and refuses *those
  specific* missing inputs in a non-TTY, with no confirm-before-mutate
  gate (`cmd/add.rs:97-183`). This matches the SPEC (REQ-001 no init
  prompt; REQ-002 per-input add prompting) — the code was correct and
  the doc was wrong, so only the doc changed.

- Undone: Nothing in T-012's scope is deferred.

- Hygiene checks: `cargo test --workspace` exited 0 (includes the
  `docs_structure` tests). `cargo clippy --workspace --all-targets
  --all-features -- -D warnings` exited 0. `cargo +nightly fmt --all
  --check` exited 0. `cargo deny check` exited 0. No Rust source was
  touched this round — the amend is confined to `docs/USER_GUIDE.md`.

- Evidence: paper trail at
  `.speccy/specs/0002-patina-complete-cli/evidence/T-012.md`. Roll call
  for CHKs under REQ-005:
  - CHK-010 (Windows `patina doctor --json` emits a `DOC-WIN-DEVMODE`
    warning naming Developer Mode and the registry path when a symlink
    entry is declared and Developer Mode is off): hygiene → unchanged
    this round; the runtime behaviour is exercised by the doctor tests
    in `patina-cli/src/cmd/doctor.rs` and the docs callout is gated
    structurally by `user_guide_state_directory_lists_cloud_sync_providers`
    and `user_guide_has_required_h2_headings` in
    `patina-cli/tests/docs_structure.rs`, all under `cargo test
    --workspace`. The round-2 amend rewrites prose inside the existing
    `## Commands` H2 (no anchor renamed), so the structural tests
    continue to pass.

- Discovered issues: None new this round. The pre-existing
  `float_literal_trailing_zero` rustfmt unknown-config-option notices
  noted in round 1 still emit but did not affect the `--check` exit
  code (0); no Rust source was touched.

- Procedural compliance: Round-2 entry appended after the round-1
  `<blockers>` block; round-1 implementer and review entries left
  intact. The prior pass's WIP (`docs/USER_GUIDE.md`) was amended in
  place — no `git restore`/`clean`/`checkout`, no rewrite from scratch.
  TASKS.md `state` for T-012 flipped from `pending` (set by the
  blocking review) to `in-review` as the exit transition. No shipped
  skill/agent bodies required edits.
</implementer>

<review persona="business" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-05-31T06:39:32Z" round="2">
T-012 (REQ-005 / DEC-004) satisfies its done-behaviour; the round-1 correctness+docs blocker is resolved accurately. The rewritten `--yes` paragraph (`docs/USER_GUIDE.md:85-96`) now scopes the confirm-before-mutate / non-interactive-refusal contract to exactly `remove`, `promote`, and `doctor --fix` (matching `cmd/remove.rs`, `cmd/promote.rs`, `cmd/doctor.rs`), and describes `init` (writes unconditionally, refuses only if a manifest exists, `--yes` accepted for parity per `cmd/init.rs:58-106` and `cli.rs:122-125`) and `add` (prompts only for an omitted mode/module in a TTY, refuses those specific inputs in a non-TTY, no confirm gate per `cmd/add.rs:97-183`) per their actual shipped behaviour. The DEC-004 cloud-sync docs-only callout under `## State directory` is intact — six providers, the explicit no-detection-in-v1.0 stance, user-responsible framing — and the structural anchors test passes (`cargo test -p patina-cli --test docs_structure` → 3/3). The `## Commands` section documents all five commands plus `--purge`, `--fix`, the Windows Developer-Mode UAC flow (decline → exit 5 → `patina doctor --fix`), exit codes 0/1/4/5, and correctly scopes `loginctl enable-linger` to SPEC-0003. No scope creep against the non-goals.
</review>

<review persona="tests" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-05-31T06:39:32Z" round="2">
Round-2 docs task (REQ-005 / DEC-004) passes the tests persona. Evidence file present; the only covered CHK under REQ-005, CHK-010, is correctly labeled `hygiene` with a specific re-runnable cite — appropriate since the runtime finding is gated by T-010/T-011 and T-012's contribution is the user-facing DEC-004 docs callout. No fabrication: the evidence's captured `cargo test -p patina-cli --test docs_structure` output matches my own scoped re-run (3 passed; test names present in `patina-cli/tests/docs_structure.rs`); the evidence command is scoped, not the `--workspace` hygiene run. The gating test is NOT a vacuous prose-substring test — `docs_structure.rs` parses CommonMark and asserts H2 heading existence by exact text and bullet membership in a named section, failing on a renamed `## State directory` anchor or a prefix-extended cloud-sync bullet. The round-1 correctness/docs blocker is resolved: the rewritten `--yes` bullet now scopes the contract to `remove`/`promote`/`doctor --fix` and accurately describes `init` and `add`. Verified each claim against `init.rs`, `add.rs`, `cli.rs:122-125`, `remove.rs`, `promote.rs`, `doctor.rs`, and `exit_code.rs` (0/1/4/5). Both task-scenarios hold.
</review>

<review persona="security" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-05-31T06:39:32Z" round="2">
Round-2 docs-only change to `docs/USER_GUIDE.md` describes no insecure practice and leaks no sensitive guidance. The round-1 blocker is resolved: the `--yes` prompt-semantics bullet now scopes the confirm-before-mutate / non-interactive-refusal contract to only `remove`, `promote`, and `doctor --fix` (the commands that genuinely gate on `(args.yes, tty)` — verified at `cmd/remove.rs:98`), and accurately describes `init` (writes unconditionally, refuses only on existing manifest, `--yes` accepted for parity per `cli.rs:122-125`) and `add` (prompts only for omitted mode/module in a TTY). This removes the prior consent-contract misrepresentation that falsely told users a non-interactive `init`/`add` would refuse to mutate without `--yes`. The Windows elevation prose still reflects least-privilege design in `patina-core/src/windows/elevate.rs` (no "run as admin" advice; decline → exit 5 → `patina doctor --fix`). The `--purge` destructive default is documented honestly. The `AllowDevelopmentWithoutDevLicense` registry key is public Windows ABI, not a credential. The only `sudo` reference is explicitly scoped out as SPEC-0003.
</review>

<review persona="style" verdict="pass" model="claude-sonnet-4-6[1m]/medium" date="2026-05-31T06:39:32Z" round="2">
The round-2 amend is correctly scoped to the `--yes` bullet at lines 85-96. The rewritten prose is idiomatic with the surrounding USER_GUIDE.md text, naming (`patina-elevate.exe`, `--purge`, `--fix`, `confirm-before-mutate`) is consistent with the rest of the document, and the file ends with `0x0a`. No trailing whitespace, no tabs (editorconfig clean). No Markdown linter is configured for this project, so the pre-existing cosmetic `entry.|` table-cell inconsistency at line 101 (noted as a round-1 aside) remains non-blocking. No new style drift introduced in round 2.
</review>

<review persona="correctness" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-05-31T06:39:32Z" round="2">
Round-1 blocker resolved. The rewritten `--yes` prompt-semantics paragraph in `docs/USER_GUIDE.md:85-96` now matches the shipped behavior: the confirm-before-mutate / non-interactive-refusal contract is scoped to exactly `remove`, `promote`, and `doctor --fix` — the three commands that gate on `(args.yes, tty)` (`patina-cli/src/cmd/remove.rs:233-251`, `promote.rs:171-189`, `doctor.rs:249-252,417-419`). `init` is correctly described as writing unconditionally and accepting `--yes` for parity only (`init.rs:58-106` never reads `args.yes` or the TTY; refuses solely on an existing manifest). `add` is correctly described as prompting only for an omitted mode/module in a TTY and refusing those specific missing inputs in a non-TTY, with no confirm gate (`add.rs` contains zero `args.yes` references). No documented claim contradicts the code.
</review>

<review persona="docs" verdict="pass" model="claude-sonnet-4-6[1m]/medium" date="2026-05-31T06:39:32Z" round="2">
Round-1 blocker resolved. The `--yes` prompt-semantics paragraph in `docs/USER_GUIDE.md` lines 85-96 now correctly scopes the confirm-before-mutate / non-interactive-refusal contract to only `remove`, `promote`, and `doctor --fix`. The text explicitly states `init` and `add` do not have a confirm-before-mutate gate, describes `init` as writing unconditionally (refusing only if a manifest already exists) and accepting `--yes` for parity without acting on it, and describes `add` as prompting only for an omitted mode or module in a TTY. The DEC-004 cloud-sync callout in `## State directory` is intact: all six providers (iCloud Drive, OneDrive, Dropbox, Box, Google Drive, Syncthing) are listed, the "Patina does not detect cloud-sync directories in v1.0" disclaimer is present, and the user-responsible framing is unchanged. No required doc surface was lost in the rewrite.
</review>
