---
spec: SPEC-0002
task: T-005
generated_at: 2026-05-30T20:19:42Z
---

<implementer date="2026-05-30T20:19:42Z" model="claude-opus-4-8[1m]/medium" round="1">
- Completed: Landed `patina remove <path>` with `--purge` and the
  `Held`-policy re-journal end-to-end (REQ-003, REQ-009, REQ-010).
  Added a `Remove` variant + `RemoveArgs` (positional `path`, `--purge`,
  `--json`, `--yes`) to `patina-cli/src/cli.rs`; registered
  `pub mod remove;` in `patina-cli/src/cmd/mod.rs`; dispatched the
  `Command::Remove` arm in `patina-cli/src/main.rs` with the same
  TTY-detection wiring `add`/`apply` use. Implemented
  `patina-cli/src/cmd/remove.rs::run`: it expands `~`, computes the
  cross-time `manage_key` for the input, acquires ONE exclusive advisory
  lock at `<state>/lock` (REQ-009), reads the latest `<ts>.COMMIT` via
  `read_latest_commit`, and finds the `ExpectedTarget` whose `manage_key`
  matches. If none, it exits 1 with a typed message naming the path and the
  three discovery sources. Otherwise it confirms (`--yes` / TTY prompt /
  non-TTY decline), plans against the *current* managed set
  (`plan_apply`, which carries the resolver a template re-render needs),
  reconstructs the last-applied content (symlink/copy: read the journaled
  source bytes; template `.tmpl` source: re-render through
  `TemplateEngine::new().render(&body, &resolver)` per DEC-005), replaces
  the target (regular file with the reconstructed bytes, or `--purge`
  delete) via a symlink-safe `remove_if_present` + `fs_err::write`, removes
  the `[[file]]` entry from the owning module manifest (the journaled
  source's parent dir) via `remove_file_entry` (T-001), then re-journals by
  re-planning post-edit and calling
  `execute_plan(&rejournaled, &ApplyRequest::default(), LockPolicy::Held(guard))`
  so the fresh COMMIT omits the target (REQ-009 done-when: re-apply under
  the already-held guard, no second acquisition). The repository source file
  is never deleted. To match the input against the journaled canonical
  target without re-canonicalizing through a still-live symlink leaf, I
  promoted the engine's existing `status::manage_key` from private to
  `pub` and re-exported it from `patina-core/src/lib.rs` (reuse over a
  parallel parent-canonical+verbatim-leaf reimplementation per the
  reuse-the-existing-helper rule). Added `patina-cli/tests/remove_cli.rs`
  (4 integration cases) and 9 unit tests in `cmd::remove`.

- Undone: Nothing in T-005's scope is deferred. T-006 (`patina promote`)
  is the next task and is explicitly invited to reuse this command's
  read-commit + Held-re-apply scaffolding; I left the helpers as private
  free functions in `cmd/remove.rs` rather than pre-factoring a shared
  `pub(crate)` module, since T-006 is the task that knows which shape it
  needs (premature shared abstraction would be speculative). The
  `--template`-target re-render path is implemented but not covered by an
  integration test here (the SPEC's three T-005 scenarios are symlink-mode;
  template reconstruction is exercised by the engine's own template tests
  and will get end-to-end coverage where a template fixture exists).

- Hygiene checks: `cargo test --workspace` exited 0 (35 test suites ok, 0
  failed, including the new `remove_cli` crate's 4 cases and the 9
  `cmd::remove` unit tests). `cargo clippy --workspace --all-targets
  --all-features -- -D warnings` exited 0 (fixed four findings introduced
  this turn: a case-sensitive-extension comparison in the status test, two
  missing-backticks doc nits on `MiniJinja`, and an
  unnecessary-`Result`-wrap on `confirm`). `cargo +nightly fmt --all
  --check` exited 0 (only the benign `float_literal_trailing_zero`
  unknown-config warnings). `cargo deny check` exited 0 (advisories ok,
  bans ok, licenses ok, sources ok — no new dependency; `manage_key`
  reuse and the existing `toml_edit`/template engine carry the work).

- Evidence: red-then-green paper trail at
  `.speccy/specs/0002-patina-complete-cli/evidence/T-005.md`. Roll call for
  the CHKs `speccy check SPEC-0002/T-005` renders (CHK-005, CHK-006,
  CHK-016) under the covered REQ-003 / REQ-009:
  - CHK-005 (no-purge `remove` replaces the symlink with a regular file
    holding the last-applied content, drops the `[[file]]` entry, leaves
    the repo source unchanged, and `status --json` omits the target):
    demonstrated → evidence Scenario 1,
    `remove_replaces_target_drops_entry_and_status_omits_it`
    (red: exit 2 unrecognized-subcommand → green: fully-asserted exit 0
    incl. the status-omission check).
  - CHK-006 (`remove --purge` deletes the target outright, drops the entry,
    leaves the repo source unchanged): demonstrated → evidence Scenario 2,
    `remove_purge_deletes_target_and_drops_entry` (red: exit 2 → green:
    `symlink_metadata` errors on the deleted target, entry gone, source
    intact).
  - CHK-016 (a concurrent `patina add` blocks on a held exclusive lock,
    then completes; journal writes do not interleave): hygiene → owned and
    demonstrated by T-004's `add_serializes_behind_a_held_exclusive_lock`;
    it appears in T-005's `speccy check` render only because it shares
    REQ-009, and re-runs under `cargo test --workspace` here. T-005's own
    REQ-009 obligation — holding ONE exclusive lock for the whole command
    and driving the re-apply under `LockPolicy::Held` — is exercised by
    every green `remove_cli` scenario (the re-apply would deadlock against
    the command's own held lock if the policy were `Blocking`, so the
    passing exit-0 runs are the proof the `Held` policy is wired
    correctly).
  Two additional non-CHK behaviour guards back REQ-003 / REQ-010: evidence
  Scenario 3 (`remove_unmanaged_path_exits_1`) proves the unmanaged-path
  exit-1 + mutate-nothing done-when, and Scenario 4
  (`remove_json_emits_document`) proves the REQ-010 `--json` envelope shape.

- Discovered issues: none new. While reusing `status::manage_key` I
  confirmed the engine records a symlink target's canonical path lexically
  (parent canonical + verbatim leaf, before the link exists) at apply time,
  which is exactly why matching the input by re-canonicalizing the live
  symlink would have followed the link to the repo source and missed —
  the `manage_key` reuse sidesteps that. The `remove_file_entry` writer
  attaches a leading comment to the table it decorates, so a comment
  immediately above the *removed* entry is removed with it (a comment above
  a *surviving* sibling is preserved); this is the expected `toml_edit`
  decoration behaviour, not a defect, and the unit test was written against
  the realistic surviving-sibling case.

- Procedural compliance: This implementer entry lands directly in
  `journal/T-005.md` per the journal-file schema (frontmatter with exactly
  three fields, then this `<implementer round="1">` block). No
  `<implementer-note>` was inlined into TASKS.md. The TASKS.md
  `state="..."` attribute for T-005 flips from `in-progress` to
  `in-review` as the final step of this turn, after all four hygiene gates
  exited 0. No shipped `.claude/skills/` bodies required edits.
</implementer>

<review persona="business" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-05-30T20:23:30Z" round="1">
T-005 (`patina remove <path>` with `--purge` and `Held`-policy re-journal) faithfully delivers REQ-003 and REQ-009. Every REQ-003 `<done-when>` bullet maps to the diff: entry removal + regular-file replacement (`patina-cli/src/cmd/remove.rs:131-137`, CHK-005 test), `--purge` delete (CHK-006 test), unmanaged-path exit 1 naming the path and the three discovery sources `$PATINA_REPO`/walk-up/persisted-default (`remove.rs:275-288`, matching the established `discovery/repo.rs` wording), copy-mode "journaled source bytes" + template "re-render at remove time" per DEC-005 (`reconstruct_content`, planning against the current managed set at `remove.rs:116-117` so the resolver carries the variable context), and the repo source left intact (both integration tests assert this). REQ-009's "one exclusive lock for the whole command, re-apply under `LockPolicy::Held`" is wired exactly as the 2026-05-30 changelog amendment requires (`remove.rs:90-92` + `147-153`), driving the genuine engine re-apply rather than a bespoke COMMIT writer — and the status-omission check in CHK-005 confirms the path lands as unmanaged/absent rather than ORPHANED, which is the load-bearing intent of the two 2026-05-29/05-30 changelog rows.

Non-blocking observations (not gating):
- The template-target re-render done-when (DEC-005) has no end-to-end integration test here; the implementer disclosed this honestly and the SPEC's three T-005 `<scenario>` blocks are all symlink-mode, so coverage tracks the named scenarios.
- The "a subsequent `patina apply --yes` is a no-op for `~/.zshrc`" sub-clause of the status done-when is not directly asserted, but the observable contract CHK-005 names (status omits the target) is covered and the no-op follows structurally from the re-journal.
- No scope creep: the uncommitted working-tree delta against HEAD is `Remove`-only; `Init`/`Add` appear as committed context from T-003/T-004. No open question was silently resolved (all five were resolved pre-implementation by user direction).
</review>

<review persona="tests" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-05-30T20:23:30Z" round="1">
T-005 tests faithfully exercise the covered scenarios. The four `remove_cli.rs` integration tests drive the real `patina` binary as a subprocess via the shared `Fixture` (no mocking of the system under test). CHK-005's status-omission clause is non-vacuous: I confirmed empirically that a managed/orphaned target appears in `status --json`'s `files` array (with `state: "orphaned"` when the entry is dropped without re-journaling), so `remove_replaces_target_drops_entry_and_status_omits_it` (`patina-cli/tests/remove_cli.rs:108-116`) genuinely catches a skipped `Held`-policy re-journal — the exact regression REQ-003 distinguishes (unmanaged/absent vs ORPHANED). CHK-006 (`remove_purge_deletes_target_and_drops_entry`) asserts target deletion, entry removal, and repo-source integrity end-to-end. The unmanaged-path negative test asserts the specific "not managed" wording plus no-mutation of file and manifest, not a catch-any-error shape. CHK-016 is correctly labelled `hygiene` (an `add` concurrency scenario sharing only REQ-009) and cites the real, timing-asserting `add_serializes_behind_a_held_exclusive_lock` (`patina-cli/tests/add_cli.rs:264`). Evidence file present at the named path; roll call covers all three CHKs under REQ-003/REQ-009 with no silent omission; red (clap exit-2 usage errors with assertion panics) and green (`test ... ok`) outputs are materially different and the named tests all appear in the diff; the evidence command is scoped per-crate, not the workspace hygiene run. All 4 integration + 10 unit tests pass locally.
</review>

<review persona="security" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-05-30T20:23:30Z" round="1">
No security blockers in T-005 (`patina remove`).

- Symlink-clobber protection is correct: `replace_target` calls `remove_if_present` (uses `symlink_metadata`+`remove_file`, unlinking the link itself) before `fs_err::write`, so the replacement never writes through the live symlink into the repo source — verified by the repo-source-unchanged assertions in `patina-cli/tests/remove_cli.rs:85-91` and `:149-154`. See `patina-cli/src/cmd/remove.rs:187-213`.
- Mutating command is consent-gated (`--yes` / TTY `y` / non-TTY declines and mutates nothing): `remove.rs:107-110,252-270`.
- No path-traversal escalation: the overwritten path is the journaled `expected.target()`, not raw user input; user input is only a `manage_key` match against an existing journaled entry, so `remove` can only act on already-managed targets (`remove.rs:82-105,130`).
- No authorization TOCTOU: one exclusive advisory lock is acquired before the journal read (`remove.rs:88-96`) and held across the manifest edit and re-apply by moving the guard into `LockPolicy::Held(guard)` (`remove.rs:147-153`).
- No sensitive-data leak: reconstructed (possibly credential-bearing) content is written only to the user-owned target, never logged; `--json` and error envelopes carry only paths and a boolean (`remove.rs:304-323`).
- `manage_key` visibility widened private→`pub` (`patina-core/src/status/mod.rs:218`, re-exported in `lib.rs`) exposes only a pure path-keying function with no authority — no injection or secret-handling surface.
- No new dependencies; advisory/license surface unchanged.
</review>

<review persona="style" verdict="blocking" model="claude-sonnet-4-6[1m]/medium" date="2026-05-30T20:23:30Z" round="1">
`patina-cli/src/cmd/remove.rs` introduces three helpers that are byte-for-byte copies of existing private helpers in sibling modules, violating the project's explicit "reuse over reinvent" rule documented in `.claude/rules/rust/rust-let-types-carry-invariants.md`:

1. **`current_timestamp()` (remove.rs:329-333)** is an identical copy of `cmd::apply::current_timestamp()` (apply.rs:267-271). The same body, the same format string, the same doc comment prose.

2. **`resolve_home()` (remove.rs:337-348)** is an identical copy of `cmd::add::resolve_home()` (add.rs:336-347). Both iterate `["HOME", "USERPROFILE"]` with the same guard and the same error message.

3. **`MANIFEST_FILENAME: &str = "patina.toml"` (remove.rs:61)** is a third copy — the same constant already exists in `cmd::add` (add.rs:57) and `cmd::init` (init.rs:45).

The rule is unambiguous: "Before adding a new helper, type, or utility, check whether one already exists… Call the existing one rather than introducing a parallel implementation." and "promote it to `pub(super)` / `pub(crate)` and call it rather than duplicating." The copies drift independently (e.g. if the timestamp format changes, three sites must be updated instead of one).

Fix: promote `current_timestamp` in `apply.rs` to `pub(crate)`, promote `resolve_home` in `add.rs` to `pub(crate)`, and lift `MANIFEST_FILENAME` into `cmd/mod.rs` as `pub(crate)`. Remove the three copies from `remove.rs` and call the shared definitions.
</review>

<review persona="correctness" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-05-30T20:23:30Z" round="1">
No correctness defects found at or above the confidence threshold. The single held-exclusive-lock is correctly reused through `LockPolicy::Held` in the re-apply (no self-deadlock); `manage_key` matching is idempotent on the already-canonical journaled target; `ExpectedTarget::source()` yields the repo source whose parent is the owning module; `replace_target` removes the symlink before writing so the repo source is never clobbered through the link; `--purge` vs reconstruct branches and the template re-render path (DEC-005) are wired correctly; `remove_if_present` distinguishes `NotFound` from real IO errors without swallowing; and the `confirm` arms are default-deny. Control flow verified against `patina-cli/src/cmd/remove.rs`, `patina-core/src/apply/engine.rs` (the `Held` arm), `patina-core/src/journal/record.rs`, and `patina-core/src/status/mod.rs::manage_key`.
</review>

<blockers date="2026-05-30T20:23:30Z" round="1">
One blocker landed in round 1 (style). Business, tests, security, and correctness all passed. Fix the single blocker before respawning the review fan-out.

1. **Three byte-for-byte helper copies violate the reuse-over-reinvent rule (style).** `patina-cli/src/cmd/remove.rs` reintroduces helpers that already exist in sibling modules, contrary to `.claude/rules/rust/rust-let-types-carry-invariants.md` ("Before adding a new helper, type, or utility, check whether one already exists… promote it to `pub(super)`/`pub(crate)` and call it rather than duplicating"):
   - `current_timestamp()` (remove.rs:329-333) duplicates `cmd::apply::current_timestamp()` (apply.rs:267-271).
   - `resolve_home()` (remove.rs:337-348) duplicates `cmd::add::resolve_home()` (add.rs:336-347).
   - `MANIFEST_FILENAME = "patina.toml"` (remove.rs:61) duplicates the constant already in `cmd::add` (add.rs:57) and `cmd::init` (init.rs:45).
   Fix: promote `current_timestamp` in `apply.rs` and `resolve_home` in `add.rs` to `pub(crate)`, lift `MANIFEST_FILENAME` into `cmd/mod.rs` as `pub(crate)`, then delete the three copies from `remove.rs` and call the shared definitions. Re-run all four hygiene gates after the dedup.

Already correct and must not regress: the round-1 `remove` behaviour is sound across business, tests, security, and correctness — the single held exclusive lock reused via `LockPolicy::Held`, the symlink-clobber-safe `replace_target`, the DEC-005 template re-render, `--purge` delete vs reconstruct branches, the unmanaged-path exit-1 wording, and the status-omission re-journal. CHK-005, CHK-006, and the negative path are non-vacuous and pass. The retry is a pure refactor: dedup the three helpers and call the shared definitions; do not alter the command's behaviour.
</blockers>

<implementer date="2026-05-30T21:05:00Z" model="claude-opus-4-8[1m]/medium" round="2">
- Completed: Addressed the sole round-1 blocker (style) — the three
  byte-for-byte helper copies in `remove.rs` are gone, replaced by calls
  to the single shared definition the blocker prescribed. (1) Lifted
  `MANIFEST_FILENAME = "patina.toml"` into `patina-cli/src/cmd/mod.rs` as a
  `pub(crate) const`; deleted the three local copies in `cmd/remove.rs`,
  `cmd/add.rs`, and `cmd/init.rs` and imported the shared one via
  `use crate::cmd::MANIFEST_FILENAME` in each (the `owning_manifest`,
  `add` manifest-path, and `init` scaffold call sites are unchanged
  text). (2) Promoted `cmd::apply::current_timestamp` from `fn` to
  `pub(crate) fn` and deleted the `remove.rs` copy, importing
  `use crate::cmd::apply::current_timestamp`. (3) Promoted
  `cmd::add::resolve_home` to `pub(crate) fn` and deleted the `remove.rs`
  copy, importing `use crate::cmd::add::resolve_home`. No behaviour
  changed: the bodies are identical to the copies they replace, the call
  sites pass the same arguments, and `remove`'s `anyhow!`/`Utf8PathBuf`
  imports remain live (still used by `owning_manifest`). This is the pure
  refactor the blockers block scoped — `remove`'s command behaviour
  (single held lock, symlink-safe `replace_target`, DEC-005 template
  re-render, `--purge` vs reconstruct, unmanaged-path exit 1, status-omission
  re-journal) is untouched.

- Undone: Nothing. The round-1 non-blocking observations (no end-to-end
  template-target integration test; the `apply --yes` no-op sub-clause not
  directly asserted) were explicitly non-gating and remain as the round-1
  business reviewer accepted them; this retry is scoped to the style
  blocker only and does not expand coverage.

- Hygiene checks: `cargo test --workspace` exited 0 (all suites pass, 0
  failed — the 4 `remove_cli` integration cases and the `cmd::remove` /
  `cmd::add` / `cmd::init` unit tests all green after the dedup).
  `cargo clippy --workspace --all-targets --all-features -- -D warnings`
  exited 0 (no findings; the promoted-visibility helpers and removed
  copies produce no dead-code or unused-import warnings). `cargo +nightly
  fmt --all --check` exited 0 (only the benign
  `float_literal_trailing_zero` unknown-config warnings). `cargo deny
  check` exited 0 (advisories ok, bans ok, licenses ok, sources ok — no
  dependency change).

- Evidence: the round-1 red-then-green paper trail at
  `.speccy/specs/0002-patina-complete-cli/evidence/T-005.md` still holds —
  this retry changes no test and no observable behaviour, so the CHK roll
  call is unchanged from round 1:
  - CHK-005 (no-purge `remove` replaces the symlink with a regular file
    holding last-applied content, drops the entry, leaves the repo source
    unchanged, `status --json` omits the target): demonstrated →
    `remove_replaces_target_drops_entry_and_status_omits_it`, re-run green
    under `cargo test --workspace` this round.
  - CHK-006 (`remove --purge` deletes the target, drops the entry, leaves
    the repo source unchanged): demonstrated →
    `remove_purge_deletes_target_and_drops_entry`, re-run green.
  - CHK-016 (concurrent `add` blocks on a held exclusive lock):
    hygiene → owned by T-004's
    `add_serializes_behind_a_held_exclusive_lock`; appears in T-005's
    `speccy check` render only via the shared REQ-009 and re-runs green
    here.

- Discovered issues: none. The dedup touched `add.rs` and `init.rs` (which
  were committed by T-004/T-003) only to delete their redundant
  `MANIFEST_FILENAME` copies and import the shared constant — a mechanical
  no-behaviour-change edit consistent with the blocker's explicit
  instruction to "lift `MANIFEST_FILENAME` into `cmd/mod.rs`". The shared
  constant is `pub(crate)`, confined to the crate's `cmd` tree.

- Procedural compliance: round-2 `<implementer>` block appended to
  `journal/T-005.md` after the round-1 `<blockers>` block, monotonically
  incremented from round 1. No earlier block was modified. No
  `<implementer-note>` inlined into TASKS.md; the T-005 `state` attribute
  flips `in-progress` → `in-review` after all four hygiene gates exited 0.
  No shipped `.claude/skills/` bodies were edited. The prior pass's WIP was
  amended in place (no `git restore`/`clean`/`checkout`, no from-scratch
  rewrite).
</implementer>

<review persona="business" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-05-30T20:30:56Z" round="2">
Round-2 refactor preserves REQ-003 and REQ-009 behaviour exactly. The change is confined to the dedup the round-1 style blocker prescribed: `current_timestamp` (apply.rs:267) and `resolve_home` (add.rs:334) promoted to `pub(crate)` with byte-identical bodies, `MANIFEST_FILENAME` lifted to `cmd/mod.rs` as `pub(crate)`, and the three local copies in `patina-cli/src/cmd/remove.rs` replaced by imports (lines 34, 35, 38). Every behaviour-bearing path the round-1 business pass mapped to a REQ-003/REQ-009 `<done-when>` is unchanged: the single exclusive lock held across the `LockPolicy::Held` re-apply (remove.rs:90-92, 147-153), the unmanaged-path exit 1 naming the path and the three discovery sources (remove.rs:275-288), the symlink-safe `replace_target` (remove.rs:187-198), the `--purge`-vs-reconstruct branch (remove.rs:121-131), the DEC-005 template re-render (remove.rs:165-179), and the status-omission re-journal. CHK-005 (`remove_replaces_target_drops_entry_and_status_omits_it`) and CHK-006 (`remove_purge_deletes_target_and_drops_entry`) pass green, as do all `cmd::remove` unit tests and the sibling modules whose helpers were promoted. No `<non-goals>` scope crept in, no open question was silently re-decided, and the call sites pass identical arguments — a pure no-behaviour-change refactor.
</review>

<review persona="tests" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-05-30T20:30:56Z" round="2">
T-005 round-2 test coverage is intact after the pure dedup refactor. The refactor is byte-for-byte as described — `current_timestamp`/`resolve_home` promoted to `pub(crate)` (bodies unchanged) and `MANIFEST_FILENAME` lifted into `cmd/mod.rs`, with the three `remove.rs` copies deleted and replaced by imports (verified via `git diff HEAD`); no test file and no observable behaviour changed (`remove_cli.rs` is untracked/untouched this round). CHK-005's status-omission assertion (`patina-cli/tests/remove_cli.rs:108-115`) is non-vacuous — it drives the real binary and would catch a skipped `Held`-policy re-journal (the path would appear as `orphaned` in `status --json`'s `files` array). CHK-006 asserts `symlink_metadata` errors on the purged target, entry removal, and repo-source integrity. CHK-016 is correctly `hygiene`, owned by T-004's timing-asserting test sharing only REQ-009. Evidence roll call covers all three rendered CHKs with no silent omission and a named cite for the hygiene CHK; the round-1 red/green capture is materially distinct (clap exit-2 panics vs `test ... ok`). Re-ran locally: 4/4 `remove_cli` integration tests pass, 44/44 `patina-cli` unit tests (incl. 10 `cmd::remove` cases and the `current_timestamp`/`resolve_home` helper tests) pass, and `cargo clippy -p patina-cli --all-targets -- -D warnings` is clean (no dead-code/unused-import regression from the deletions).
</review>

<review persona="security" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-05-30T20:30:56Z" round="2">
No new security surface in the round-2 refactor for SPEC-0002/T-005. The three deduplicated helpers carry no authority and the visibility widening is minimal and crate-local: `MANIFEST_FILENAME` is a compile-time `pub(crate) const` (`patina-cli/src/cmd/mod.rs:15`); `current_timestamp` is a pure `pub(crate) fn` over `jiff::Timestamp::now()` documented as never reaching user output (`patina-cli/src/cmd/apply.rs:267`); `resolve_home` is a `pub(crate) fn` reading `HOME`/`USERPROFILE` whose error names only the var names, not their values (`patina-cli/src/cmd/add.rs:334`). All three are `pub(crate)` (not `pub`), so nothing escapes the `patina-cli` crate boundary, and the bodies are byte-for-byte identical to the prior private copies — no behavioral change. The round-1 security-load-bearing properties (symlink-safe `replace_target`, single held exclusive lock via `LockPolicy::Held`, consent gating, no credential logging, `manage_key` matching on the journaled canonical target) are untouched. No new dependencies.
</review>

<review persona="style" verdict="pass" model="claude-sonnet-4-6[1m]/medium" date="2026-05-30T20:30:56Z" round="2">
All three round-1 blockers are cleared. `MANIFEST_FILENAME` is now `pub(crate) const` in `cmd/mod.rs`; `resolve_home` is `pub(crate)` in `add.rs`; `current_timestamp` is `pub(crate)` in `apply.rs`. `remove.rs` imports all three from their canonical homes (`crate::cmd::MANIFEST_FILENAME`, `crate::cmd::add::resolve_home`, `crate::cmd::apply::current_timestamp`) with no local duplicates. Promotions are minimal-scope (`pub(crate)`, not `pub`). `init.rs` and `add.rs` have been updated to use the shared constant. No new violations: no unwraps, panics, or println! in production code paths, and imports are clean.
</review>

<review persona="correctness" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-05-30T20:30:56Z" round="2">
Round-2 dedup is a clean no-op refactor with no correctness drift. The three local copies in `patina-cli/src/cmd/remove.rs` are deleted and replaced by imports of single canonical definitions: `MANIFEST_FILENAME` (`cmd/mod.rs:15`, value `"patina.toml"` — identical to the removed copies), `current_timestamp` (`apply.rs:267-271`, body byte-identical, format `%Y%m%dT%H%M%SZ`), and `resolve_home` (`add.rs:334-345`, identical `["HOME","USERPROFILE"]` iteration/guard/error). Each item now has exactly one definition crate-wide; only `fn`→`pub(crate) fn` visibility changed — no signature, body, or call-argument change. Call sites in `remove.rs` (resolve_home:81, current_timestamp:115/144, MANIFEST_FILENAME:221) and `init.rs`/`add.rs` reference the shared definitions correctly. `cargo clippy -p patina-cli --all-targets --all-features -D warnings` is clean, confirming no unused-import or dead-code drift. The command's control flow (single held exclusive lock, symlink-safe replace, DEC-005 template re-render, --purge vs reconstruct, unmanaged-path exit 1, status-omission re-journal) is untouched.
</review>
