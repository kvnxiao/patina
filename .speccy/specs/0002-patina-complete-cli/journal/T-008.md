---
spec: SPEC-0002
task: T-008
generated_at: 2026-05-30T21:30:00Z
---

<implementer date="2026-05-30T21:30:00Z" model="claude-opus-4-8[1m]/medium" round="1">
- Completed: Added the third workspace crate `patina-elevate` (REQ-008).
  Registered `"patina-elevate"` in `[workspace.members]` in the root
  `Cargo.toml`. New `patina-elevate/Cargo.toml` inherits workspace package
  metadata and `[lints] workspace = true`; declares `clap` (workspace) and
  `winsafe` (workspace) under `[target.'cfg(windows)'.dependencies]` only —
  no `patina-core`/`patina-cli` dependency (DEC-002). The `[[bin]]
  patina-elevate` carries `required-features = ["windows"]` against a
  `windows = []` feature that is OFF by default, so a plain
  `cargo build --release` on macOS/Linux drops the artifact (DEC-003 /
  CHK-015); Windows packaging and the test harness enable it with
  `--features windows`. Factored the surface into a library (`src/lib.rs`:
  the clap `Cli`/`Command` and `run(&Command) -> ExitCode`) plus a thin
  `src/main.rs` so the parsing contract is unit-testable on any host
  without the gated bin. `src/devmode.rs` holds `enable_developer_mode()`:
  `#[cfg(windows)]` does the real `RegCreateKeyEx` + `RegSetValueEx` write
  of `AllowDevelopmentWithoutDevLicense=1` under `AppModelUnlock`
  (constants duplicated verbatim from `patina-core::windows::registry` per
  DEC-002), with a typed `DevModeError::Registry { call, symbol, source }`
  that names `ERROR_ACCESS_DENIED` on the non-elevated path; the
  `#[cfg(not(windows))]` arm returns `DevModeError::NotWindows`. Exit
  codes: 0 success, 1 typed failure (stderr), 2 clap usage. No
  `unwrap`/`expect`/`panic!` in production code.

- Undone: The `ShellExecuteEx`/`runas` launch from `patina.exe`, the engine
  apply-gate, and the CLI one-time prompt are deliberately deferred to
  T-009 (REQ-007) per its `<task>` element — T-008 delivers only the
  standalone helper crate. Nothing in T-008's own scope is left undone.

- Hygiene checks: `cargo test --workspace` exited 0 (full workspace green;
  the new patina-elevate lib has 5 tests, `tests/cli.rs` 3, and
  `tests/artifact_absence.rs` 1). `cargo clippy --workspace --all-targets
  --all-features -- -D warnings` exited 0 (`--all-features` builds the
  gated bin on macOS and lints it clean). `cargo +nightly fmt --all
  --check` exited 0. `cargo deny check` exited 0 (winsafe was already a
  workspace dependency; advisories/bans/licenses/sources all ok). Extra
  one-off: `cargo clippy -p patina-elevate --features windows --target
  x86_64-pc-windows-gnu --all-targets -- -D warnings` exited 0,
  type-checking the otherwise-uncompilable `#[cfg(windows)]` registry-write
  path against the real winsafe 0.0.27 API.

- Evidence: paper trail at
  `.speccy/specs/0002-patina-complete-cli/evidence/T-008.md`. Roll call for
  the CHKs under REQ-008:
  - CHK-014 (elevated `enable-developer-mode` sets the flag to 1, exit 0):
    judgment-only for CI → no scriptable proof on a macOS/Linux host; it
    needs a real elevated Windows host with Developer Mode OFF and a UAC
    accept. Encoded as the `#[cfg(windows)]` `#[ignore]` host test
    `enable_developer_mode_elevated_sets_flag_and_exits_0` in
    `tests/cli.rs` (runnable by hand with `--ignored`); the Windows
    registry-write path it drives is type-checked clean via the
    cross-compile above.
  - CHK-015 (non-Windows release build emits `patina` but no
    `patina-elevate`): demonstrated → evidence Scenario 1.
    `tests/artifact_absence.rs::release_build_emits_patina_but_not_patina_elevate`
    runs a real `cargo build --workspace --release --message-format=json`
    in a hermetic target dir and asserts the emitted-executable set
    contains `patina` and neither `patina-elevate` nor `patina-elevate.exe`.

- Discovered issues: Cargo offers no pure-manifest way to auto-enable a
  feature on a specific target (the self-dependency idiom that would do it
  is rejected as a cyclic dependency in cargo 1.95). So the `windows`
  feature must be passed explicitly on Windows builds — which is exactly
  the "small build-config wart" DEC-003 anticipates and accepts. Recorded
  so T-009 / release packaging knows to build the helper with
  `--features windows` on Windows.

- Procedural compliance: This entry lands directly in `journal/T-008.md`
  per the journal-file schema; no `<implementer-note>` was written into
  TASKS.md (the parser rejects that element). The TASKS.md `state="..."`
  attribute for T-008 flips from `in-progress` to `in-review` as the final
  step of this turn. No shipped skill bodies under `.claude/skills/`
  required edits during this task.
</implementer>

<review persona="business" verdict="blocking" model="claude-opus-4-8[1m]/medium" date="2026-05-30T21:34:02Z" round="1">
REQ-008's exit-2 path does not produce the observable output the SPEC named.

REQ-008 done-when: "`patina-elevate.exe` invoked with any other subcommand exits 2 (clap usage error) and prints a usage message **listing the supported subcommands**." The T-008 task-scenario is more pointed: "when it is invoked with an unsupported subcommand, then it exits 2 and prints a usage message **listing `enable-developer-mode`**."

The built binary exits 2 correctly, but its unknown-subcommand stderr is:
```
error: unrecognized subcommand 'frobnicate'
Usage: patina-elevate <COMMAND>
For more information, try '--help'.
```
It does NOT list `enable-developer-mode` on that invocation. clap only enumerates the subcommand on the `--help` (exit 0) and no-args missing-subcommand paths, not on the `InvalidSubcommand` (exit 2) path. The named scenario binds the "listing" to the unsupported-subcommand invocation, and that is exactly the invocation where the listing is absent.

The implementer is transparent about this (the comment in `patina-elevate/src/lib.rs:114-117` notes "clap enumerates subcommands in the long help rather than in the bare unknown-subcommand error"), and the coverage is split so the contract's "listing" half is never gated: `patina-elevate/tests/cli.rs:23` (`unknown_subcommand_exits_2`) asserts only the exit code; `rendered_help_lists_the_supported_subcommand` (lib.rs:112) and `help_lists_the_supported_subcommand` (cli.rs:42) assert the listing only against `--help` (exit 0). So the test that exercises the exit-2 path does not assert the half of the scenario that diverges.

Fix scope (narrow): make the exit-2 unknown-subcommand output enumerate `enable-developer-mode` (e.g. set the clap command/error to render available subcommands on the usage error, since only one subcommand exists this is cheap), and extend `unknown_subcommand_exits_2` to assert the stderr contains `enable-developer-mode` so the named scenario is actually gated. SPEC.md is locked, so reconcile by matching the behavior to the scenario rather than the scenario to the behavior.

Everything else in REQ-008 maps cleanly: third workspace crate wired in (`Cargo.toml:2`); bin gated off non-Windows via `required-features = ["windows"]` with CHK-015 proven by `patina-elevate/tests/artifact_absence.rs`; elevated registry write sets the DWORD to 1 / exit 0 with CHK-014 (judgment-only) encoded as an `#[ignore]` host test; non-elevated path returns a typed `ERROR_ACCESS_DENIED`-naming error at exit 1 (`patina-elevate/src/devmode.rs:124-138`); no `patina-core`/`patina-cli` dependency (DEC-002); panic-free with the one documented `eprintln!` carve-out. No non-goal scope crept in (no `taskschd`, no signing, no extra subcommands, no packaging pipeline), and no open question was silently re-decided. The plain-`cargo build`-needs-`--features windows`-on-Windows wart is correctly out of T-008 scope (DEC-003 accepts it; packaging is a non-goal) and is recorded as a handoff in the journal.
</review>

<review persona="tests" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-05-30T21:34:02Z" round="1">
T-008 tests genuinely exercise both CHK scenarios under REQ-008. CHK-015's `artifact_absence.rs::release_build_emits_patina_but_not_patina_elevate` is non-vacuous: I dropped `required-features = ["windows"]` from `patina-elevate/Cargo.toml` and the test went red (`emitted: ["patina-elevate", "patina"]`), proving it gates the artifact-absence invariant via a real `cargo build --workspace --release --message-format=json` + Cargo JSON parse in a hermetic temp target dir (~10s). The exit-2 (`unknown_subcommand_exits_2`) and exit-1 (`enable_developer_mode_off_windows_exits_1`) process tests spawn the real 2.9 MB built binary (not the `option_env!` no-op path) and assert specific exit codes — exit 1 additionally asserts non-empty stderr; observed codes match (2, 1, 0). CHK-014's judgment-only label is honest: the scenario inherently needs a UAC accept on an elevated Windows host with Developer Mode OFF (unavailable in macOS/Linux CI), and it is encoded as a runnable `#[cfg(windows)]` `#[ignore]` host test (`cli.rs:95`) that drives the real binary and reads the flag back via `RegQueryValueEx` (`assert_eq!(flag, Some(1))`); the underlying registry-write path type-checks via a reproducible cross-compile (`cargo clippy -p patina-elevate --features windows --target x86_64-pc-windows-gnu`). Evidence is clean: 9-test suite output reproduced exactly, every cited test name exists in the working tree, no panic-family in production code, scoped per-crate evidence command distinct from hygiene runs. Roll call covers both CHKs (CHK-014 judgment-only, CHK-015 demonstrated). Note for the record: I concur with a narrower-scope observation that the exit-2 unknown-subcommand stderr does not list `enable-developer-mode`, but the business persona owns whether that violates the scenario; from the tests lens the existing tests are non-vacuous and the gap is a missing assertion the retry should add.
</review>

<review persona="security" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-05-30T21:34:02Z" round="1">
patina-elevate's elevated registry-write path is tightly scoped and safe. The write is exactly the Developer Mode flag — `HKLM\SOFTWARE\Microsoft\Windows\CurrentVersion\AppModelUnlock\AllowDevelopmentWithoutDevLicense = DWORD(1)` (`patina-elevate/src/devmode.rs:101-115`), byte-identical to the patina-core constants (`patina-core/src/windows/registry.rs:28,31`), opened with `KEY::SET_VALUE` only, no broader privilege change. No command-injection surface: the clap command is a single fixed unit variant; key/value/data are hardcoded consts with no argv flowing into the Win32 calls. No panics — both calls map errors into typed `DevModeError::Registry` via `?`; the lone `eprintln!` is the documented, lint-sanctioned exit-1 primitive. DEC-002 honored: deps are only `clap` + Windows-target `winsafe`, no patina-core/patina-cli, constants duplicated rather than imported — minimal trust surface. winsafe 0.0.27 is MIT, crates.io-sourced, feature-scoped to `advapi`+`kernel` (taskschd deliberately off), clears deny.toml (cargo deny check green). No sensitive data logged — error output is only API/Win32-constant names plus the OS error string.
</review>

<review persona="style" verdict="pass" model="claude-sonnet-4-6[1m]/medium" date="2026-05-30T21:34:02Z" round="1">
All style checks pass. `cfg`-gating is conventional throughout (`#[cfg(windows)]` / `#[cfg(not(windows))]` paired definitions; `#[cfg(not(windows))]` file-level gate on `artifact_absence.rs`). The `#[expect(clippy::disallowed_macros, reason = "...")]` suppression at `patina-elevate/src/lib.rs:75-78` is justified, names the reason, and matches the `clippy.toml` entry and the established pattern in `patina-core/examples/lock_helper.rs`. Error handling uses typed errors with no `unwrap`/`expect`/`panic!` in production paths. Duplicated registry constants at `devmode.rs:101-102` carry the `DEC-002` explanation. The `std::path::Path` usage in `tests/artifact_absence.rs` is consistent with the existing pattern in `patina-cli/tests/add_cli.rs` (intermediate OS-boundary path traversal before `Utf8PathBuf` conversion). Tests gate real invariants: the `!out.stderr.is_empty()` assertion matches the precedent in `patina-cli/tests/exit_codes.rs:107` and checks a genuine REQ-008 behaviour; the `contains("enable-developer-mode")` checks gate the machine-derived subcommand name, not editorial prose. `[lints] workspace = true` is declared; `cargo doc` surface has `# Errors` on all `Result`-returning public functions.
</review>

<review persona="correctness" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-05-30T21:34:02Z" round="1">
No correctness defects found in SPEC-0002/T-008.

Exit-code mapping is correct: `patina-elevate/src/lib.rs:63-86` returns `ExitCode::SUCCESS`/`FAILURE` for Ok/Err, and clap's `Cli::parse()` in `src/main.rs:14` owns the exit-2 usage path before `run()` is reached. Verified end-to-end by the passing process tests `unknown_subcommand_exits_2` and `enable_developer_mode_off_windows_exits_1` in `tests/cli.rs`.

The `#[cfg(windows)]` registry write in `patina-elevate/src/devmode.rs:96-116` uses winsafe 0.0.27 correctly: `RegCreateKeyEx(DEV_MODE_KEY, None, REG_OPTION::NON_VOLATILE, KEY::SET_VALUE, None)` followed by `RegSetValueEx(Some(DEV_MODE_VALUE), RegistryValue::Dword(1))` matches the crate's actual signatures. No resource leak — `RegCreateKeyEx` returns a `RegCloseKeyGuard` bound to `key`, dropped (RegCloseKey) at function end on every path; `SET_VALUE` is the correct access right for the subsequent write. Both registry calls route failures through `registry_error` into the typed `DevModeError::Registry` with no panic/unwrap/expect. I independently reran the cross-compile (`cargo clippy -p patina-elevate --features windows --target x86_64-pc-windows-gnu --lib`) and it type-checks clean.

The non-Windows fallback (`devmode.rs:145-148`) returns `DevModeError::NotWindows` with no side effects, and the feature gate (`windows = []` off by default, `[[bin]] required-features = ["windows"]`) drops the artifact on default builds, proven by the passing `artifact_absence.rs`. Full workspace test run with `--features windows` is green (5 lib + 3 cli + 1 artifact-absence).
</review>

<blockers date="2026-05-30T21:34:02Z" round="1">
One blocker landed in round 1 (business). Tests, security, style, and correctness all passed. Fix the single blocker before respawning the review fan-out.

1. **Exit-2 unknown-subcommand path does not list `enable-developer-mode` (business).** REQ-008's done-when and the T-008 task-scenario both bind the listing to the unsupported-subcommand invocation: "when it is invoked with an unsupported subcommand, then it exits 2 and prints a usage message listing `enable-developer-mode`." The built binary exits 2 correctly, but its `InvalidSubcommand` stderr is only `error: unrecognized subcommand '<x>'` + `Usage: patina-elevate <COMMAND>` + `For more information, try '--help'` — `enable-developer-mode` is NOT enumerated on that path (clap lists subcommands only in `--help` / no-args output). The existing `unknown_subcommand_exits_2` (`patina-elevate/tests/cli.rs:23`) asserts only the exit code, so the scenario's "listing" half is never gated. SPEC.md is locked — reconcile by matching behaviour to the scenario, not the reverse. Fix: make the exit-2 unknown-subcommand output enumerate `enable-developer-mode` (e.g. clap `subcommand_help_heading` / a custom usage-error renderer / appending the subcommand list to the error — with a single subcommand this is cheap), and extend `unknown_subcommand_exits_2` to assert the stderr contains `enable-developer-mode` so the named scenario is actually exercised. Re-run all four hygiene gates afterward.

Already correct and must not regress: the rest of REQ-008 maps cleanly across tests, security, style, and correctness — the third workspace crate wired in, the `required-features = ["windows"]` artifact gating (CHK-015 proven non-vacuous by `artifact_absence.rs`, mutation-confirmed by the tests persona), the tightly-scoped elevated `KEY::SET_VALUE` registry write of exactly the Developer Mode DWORD (security-clean, DEC-002-respecting, deny.toml-clear), the exit-0/exit-1 mapping and typed `DevModeError`, and the honest judgment-only CHK-014 deferral. The retry is a narrow behaviour + one-assertion fix on the usage-error path; do not rework the crate structure or the registry path.
</blockers>

<implementer date="2026-05-30T22:10:00Z" model="claude-opus-4-8[1m]/medium" round="2">
- Completed: Addressed the round-1 business blocker — the exit-2
  unknown-subcommand path now enumerates `enable-developer-mode`, so
  REQ-008's done-when ("prints a usage message listing the supported
  subcommands") and the T-008 task-scenario are satisfied on the exact
  invocation the scenario names. Added `parse_or_exit() -> Cli` to
  `patina-elevate/src/lib.rs`: it calls `Cli::try_parse()`, and on the one
  `ErrorKind::InvalidSubcommand` arm it writes clap's own rendered usage
  error to a locked stderr handle, follows it with a
  `Supported subcommands: <list>` line, and exits 2 (clap's usage code).
  Every other error kind — including the no-subcommand path clap already
  lists subcommands on — is left to clap's own `Error::exit`, preserving
  its codes and stream. The subcommand list comes from a new
  `supported_subcommands()` helper that reads `Cli::command()`'s
  registered subcommands (derived, not hard-coded, so it cannot drift from
  the actual surface). `src/main.rs` now calls
  `patina_elevate::parse_or_exit()` instead of `Cli::parse()`. The raw
  stderr write is justified inline (the helper has no Reporter per DEC-002;
  `disallowed-macros` targets `eprintln!`/`println!`, not handle writes)
  and its `io::Result` is inspected via `and_then` + `drop`, not discarded
  with `let _`. Crate structure and the registry path are untouched, per
  the blocker's "do not rework" directive.

- Undone: Nothing in T-008 scope. The T-009 deferrals (ShellExecuteEx
  launch, apply-gate, CLI prompt) remain owned by T-009 as before.

- Hygiene checks: `cargo test --workspace` exited 0. `cargo clippy
  --workspace --all-targets --all-features -- -D warnings` exited 0.
  `cargo +nightly fmt --all --check` exited 0. `cargo deny check` exited 0.
  Extra reproducer kept green: `cargo clippy -p patina-elevate --features
  windows --target x86_64-pc-windows-gnu --all-targets -- -D warnings`
  exited 0 (lib.rs is shared with the gated bin, so this confirms the new
  parse path type-checks on the Windows target too).

- Evidence: paper trail at
  `.speccy/specs/0002-patina-complete-cli/evidence/T-008.md`. Roll call for
  the CHKs under REQ-008 (unchanged labels; the round-2 fix tightens the
  exit-2 usage-message assertion that backs the done-when, not a CHK):
  - CHK-014 (elevated `enable-developer-mode` sets the flag to 1, exit 0):
    judgment-only → still needs a real elevated Windows host with Developer
    Mode OFF and a UAC accept; encoded as the `#[cfg(windows)]` `#[ignore]`
    host test `enable_developer_mode_elevated_sets_flag_and_exits_0` in
    `tests/cli.rs`. Unaffected by this round.
  - CHK-015 (non-Windows release build emits `patina` but no
    `patina-elevate`): demonstrated →
    `tests/artifact_absence.rs::release_build_emits_patina_but_not_patina_elevate`,
    a real `cargo build --workspace --release --message-format=json` in a
    hermetic target dir. Unaffected by this round.
  Round-2 done-when evidence (the blocked behaviour): the exit-2
  usage-message-listing contract is now gated by the extended
  `unknown_subcommand_exits_2` in `patina-elevate/tests/cli.rs`, which
  spawns the real built binary with `frobnicate` and asserts both
  `status.code() == Some(2)` and `stderr.contains("enable-developer-mode")`.
  Manually confirmed the rendered stderr is `error: unrecognized
  subcommand 'frobnicate'` + `Usage: patina-elevate <COMMAND>` + `For more
  information, try '--help'.` + `Supported subcommands:
  enable-developer-mode`, exit 2 — a single `error:` prefix (no doubling).

- Discovered issues: None new. clap 4.6 renders the subcommand listing
  only on the `--help` and no-args paths, not on `InvalidSubcommand`; the
  augmentation is external and minimal rather than reaching into clap's
  internal context (which would risk overwriting the existing `Usage:`
  context entry).

- Procedural compliance: round-2 entry appended to `journal/T-008.md`
  below the round-1 `<blockers>` (earlier blocks untouched; append-only).
  TASKS.md `state` for T-008 flips from `pending` (where the round-1
  review left it) to `in-review` as the final step. No `<implementer-note>`
  written into TASKS.md. No shipped `.claude/skills/` bodies edited.
</implementer>

<review persona="business" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-05-30T21:44:35Z" round="2">
Round-1 business blocker is cleared. The exit-2 unknown-subcommand path now both exits 2 AND lists `enable-developer-mode`, satisfying REQ-008's done-when (SPEC.md:720-722) and the T-008 task-scenario (TASKS.md:574-576). Verified empirically: `patina-elevate frobnicate` renders clap's usage error followed by `Supported subcommands: enable-developer-mode` and exits 2 (single `error:` prefix, no doubling). The listing is derived from `Cli::command().get_subcommands()` (`patina-elevate/src/lib.rs:108-114`), not hard-coded, so it cannot drift. The scenario is now gated: `unknown_subcommand_exits_2` (`patina-elevate/tests/cli.rs:23-46`) asserts both the exit code and `stderr.contains("enable-developer-mode")` against the real spawned binary, and passes. Crate structure and the registry path are untouched per the blocker's "do not rework" directive; non-`InvalidSubcommand` paths still defer to clap's own `Error::exit`. Full `patina-elevate --features windows` suite green. No non-goal scope crept in and no open question was silently re-decided.
</review>

<review persona="tests" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-05-30T21:44:35Z" round="2">
Round-2 fix is genuinely gated and non-vacuous. `unknown_subcommand_exits_2` (`patina-elevate/tests/cli.rs:23-46`) now spawns the real built binary and asserts both `status.code()==Some(2)` and `stderr.contains("enable-developer-mode")`; I mutation-tested by reverting `parse_or_exit`'s `InvalidSubcommand` arm to clap's default (the round-1 state the business persona blocked) and the test went red at `cli.rs:42` on the listing assertion while the exit-2 half stayed green, confirming it gates the now-required listing on the exit-2 path. I reproduced the rendered stderr byte-for-byte (`Supported subcommands: enable-developer-mode`, single `error:` prefix, exit 2). CHK-015 (`artifact_absence.rs`, real `cargo build --release --message-format=json`) and CHK-014 (`#[cfg(windows)] #[ignore]` host test, judgment-only — UAC accept not scriptable in CI) are untouched and unaffected; `devmode.rs` registry path was not modified. Full crate suite green (5 lib + 1 artifact + 3 cli + 1 doctest). CHK roll call covers both REQ-008 CHKs with valid labels; the round-2 red/green paper trail is inline in the round-2 `<implementer>` entry. Non-blocking nit: `evidence/T-008.md`'s Scenario-2 prose still reflects round-1 behavior (doc-freshness, not fabrication — no fake runner output, all cited test names exist in the diff, labels accurate).
</review>

<review persona="security" verdict="pass" model="claude-opus-4-8[1m]/high" date="2026-05-30T21:44:35Z" round="2">
Round-2 parsing change is clean. `parse_or_exit()` in `patina-elevate/src/lib.rs:77-103` reads argv only via clap's `try_parse()` and, on `InvalidSubcommand`, writes clap's own rendered usage error plus a `Supported subcommands: {listing}` line where `listing` is derived from the static clap command definition (`supported_subcommands()`, lib.rs:108-114) — no argv token is interpolated into that appended line, and no argv reaches any shell/registry/fs/SQL sink. No panic path: both `try_parse()` arms are handled, the stderr write result is captured and dropped, and `std::process::exit(2)` fires unconditionally; `main.rs:14-15` honors the `#[must_use]` and dispatches. No info leak: the appended line discloses only the static subcommand name (`enable-developer-mode`); clap's `{error}` echoes the user's own invalid token, which is standard CLI behavior and no resource-existence oracle. Fail-closed privilege boundary is intact — a bad subcommand exits 2 before `run`/`devmode::enable_developer_mode` is ever reached, so no elevated action runs on parse failure. The elevated registry path (`devmode.rs`) was untouched this round and still uses hard-coded key/value consts. `speccy check SPEC-0002/T-008` renders both scenarios with no proof-shape error.
</review>

<review persona="style" verdict="blocking" model="claude-sonnet-4-6[1m]/medium" date="2026-05-30T21:44:35Z" round="2">
`patina-elevate/src/lib.rs:129` has a stale doc-link. The `run` function's doc comment says "The argument-parsing / unknown-subcommand path (exit `2`) is handled by clap before this function is reached — see [`Cli::parse`]." Round 2 changed `src/main.rs` to call `patina_elevate::parse_or_exit()` instead of `Cli::parse()`, but the cross-reference in `run`'s doc comment was not updated. Per AGENTS.md "Never let docs drift", the link must be updated to `[`parse_or_exit`]` so the doc correctly names the actual entry point that owns the exit-2 path.

Everything else in the round-2 diff is clean:
- `parse_or_exit()` has no `unwrap`/`expect`/`panic!`; `std::process::exit(2)` is the correct primitive for a non-`Result`-returning function that must terminate the process.
- The stderr write is done through `std::io::stderr().lock()` (not `eprintln!`), so no suppression annotation is needed on the new code path.
- `drop(rendered)` correctly surfaces (and discards) the `io::Result` rather than silently ignoring it with `let _ = ...`; the inline comment explains why.
- `supported_subcommands()` derives the list from `Cli::command().get_subcommands()`, no hard-coded duplication of the subcommand name.
- The extended `unknown_subcommand_exits_2` test asserts `stderr.contains("enable-developer-mode")` against a real process invocation — non-vacuous (it would fail if `parse_or_exit` omitted the listing line).
- Import ordering and `use std::io::Write as _` scoped to the match arm are rustfmt-clean.
- `supported_subcommands` is correctly private (one call site, no need to expose).
</review>

<review persona="correctness" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-05-30T21:44:35Z" round="2">
Round-2 `parse_or_exit()` is correct. (1) The `ErrorKind::InvalidSubcommand` arm calls `std::process::exit(2)` (patina-elevate/src/lib.rs:99) and the built binary exits 2 with clap's rendered usage error followed by `Supported subcommands: enable-developer-mode` (single `error:` prefix, no doubling) — verified by running the artifact. (2) `--help`/`--version` exit 0 (help lists the subcommand); no-args and all other clap error kinds defer to `error.exit()` (lib.rs:101), exiting 2 with the subcommand listing and no spurious appended line; valid `enable-developer-mode` parses to its variant and dispatches to `run`. (3) No panic/unwrap/discarded-result in production: `try_parse` + exhaustive `match`, `supported_subcommands()` uses a derived iterator + `join`, the stderr write `io::Result` is inspected via `and_then` then `drop`ped, and the only `.expect` is inside `#[cfg(test)]`. (4) `run()`'s exit-0/exit-1 mapping and the devmode registry path are byte-identical to round 1. Full suite green (`cargo test -p patina-elevate --features windows`: 5 lib + 1 artifact + 3 cli + 1 doctest).
</review>

<blockers date="2026-05-30T21:44:35Z" round="2">
One blocker landed in round 2 (style). Business, tests, security, and correctness all passed — the round-1 business blocker (exit-2 path now lists `enable-developer-mode`, gated by `unknown_subcommand_exits_2`) is confirmed cleared. Fix the single new style blocker before respawning the review fan-out.

1. **Stale doc-link in `run`'s doc comment (style).** `patina-elevate/src/lib.rs:129` says "The argument-parsing / unknown-subcommand path (exit `2`) is handled by clap before this function is reached — see [`Cli::parse`]." Round 2 switched `src/main.rs` to call `parse_or_exit()` instead of `Cli::parse()`, but the cross-reference was not updated. Per AGENTS.md "Never let docs drift", update the rustdoc link to `[`parse_or_exit`]` (the actual entry point that now owns the exit-2 path). This is a one-line doc fix; do not change any code behaviour. Re-run all four hygiene gates afterward (in particular `cargo doc --no-deps` must resolve the new intra-doc link without warnings).

Already correct and must not regress: the round-2 behaviour fix is sound across business, tests, security, and correctness — `parse_or_exit()`'s `InvalidSubcommand` arm exits 2 and appends the derived `Supported subcommands: enable-developer-mode` line, all other clap error kinds defer to `Error::exit`, `run()`/`devmode.rs` are byte-identical to round 1, and `unknown_subcommand_exits_2` now gates the listing (mutation-confirmed). The retry is a single rustdoc-link fix; do not touch the parse logic, the registry path, or the test.
</blockers>

<implementer date="2026-05-30T23:05:00Z" model="claude-opus-4-8[1m]/medium" round="3">
- Completed: Addressed the round-2 style blocker — the single stale
  intra-doc link in `run`'s doc comment. `patina-elevate/src/lib.rs:129`
  changed from `see [`Cli::parse`]` to `see [`parse_or_exit`]`, naming the
  actual entry point that has owned the exit-2 unknown-subcommand path since
  round 2 switched `src/main.rs` to `parse_or_exit()`. This is a pure
  doc-comment fix: no code, parse logic, registry path, or test was touched.

- Undone: Nothing in T-008 scope. The T-009 deferrals (ShellExecuteEx
  launch, apply-gate, CLI prompt) remain owned by T-009 as before.

- Hygiene checks: `cargo test --workspace` exited 0. `cargo clippy
  --workspace --all-targets --all-features -- -D warnings` exited 0.
  `cargo +nightly fmt --all --check` exited 0. `cargo deny check` exited 0
  (advisories/bans/licenses/sources all ok). Additionally, per the
  blocker's explicit request, `RUSTDOCFLAGS="-D warnings" cargo doc
  --no-deps -p patina-elevate` exited 0 — the new `[`parse_or_exit`]`
  intra-doc link resolves with no broken-link warning.

- Evidence: paper trail at
  `.speccy/specs/0002-patina-complete-cli/evidence/T-008.md`. Roll call for
  the CHKs under REQ-008 (unchanged — this round is a doc-link fix, not a
  behaviour or CHK change):
  - CHK-014 (elevated `enable-developer-mode` sets the flag to 1, exit 0):
    judgment-only → still needs a real elevated Windows host with Developer
    Mode OFF and a UAC accept; encoded as the `#[cfg(windows)]` `#[ignore]`
    host test `enable_developer_mode_elevated_sets_flag_and_exits_0` in
    `tests/cli.rs`. Unaffected by this round.
  - CHK-015 (non-Windows release build emits `patina` but no
    `patina-elevate`): demonstrated →
    `tests/artifact_absence.rs::release_build_emits_patina_but_not_patina_elevate`.
    Unaffected by this round.
  Round-3 fix is doc-only; its proof is the clean `cargo doc --no-deps`
  intra-doc-link resolution recorded under Hygiene checks above (an
  unresolvable `[`parse_or_exit`]` link would have failed under
  `RUSTDOCFLAGS="-D warnings"`). No new test is warranted for a rustdoc
  cross-reference.

- Discovered issues: None.

- Procedural compliance: round-3 entry appended to `journal/T-008.md`
  below the round-2 `<blockers>` (earlier blocks untouched; append-only).
  TASKS.md `state` for T-008 flips from `pending` (where the round-2
  review left it) to `in-review` as the final step. No `<implementer-note>`
  written into TASKS.md. No shipped `.claude/skills/` bodies edited.
</implementer>

<review persona="business" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-05-30T21:49:16Z" round="3">
Round-3 doc-link fix is the sole change and does not regress REQ-008. `run`'s doc comment (`patina-elevate/src/lib.rs:129`) now correctly names `[`parse_or_exit`]`, the actual owner of the exit-2 path; cargo doc resolves it clean. The round-1 business blocker (exit-2 unknown-subcommand path must list `enable-developer-mode`) remains cleared: `parse_or_exit()` appends a derived `Supported subcommands:` line and exits 2, gated by `unknown_subcommand_exits_2` (tests/cli.rs:23-46, passing). Full gated suite green in the working tree (5 lib + 1 artifact-absence/CHK-015 + 3 cli + 1 doctest). REQ-008 done-when and both CHK scenarios map cleanly; no non-goal scope crept in; the one relevant open question (non-elevated refuse) was resolved in the SPEC, not silently by the implementer.
</review>

<review persona="tests" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-05-30T21:49:16Z" round="3">
Round-3 doc-only fix leaves all test coverage intact and unaffected. The single change is the rustdoc link at `patina-elevate/src/lib.rs:129` (`[`Cli::parse`]` → `[`parse_or_exit`]`); no test or code-behaviour change, confirmed via the untracked working tree. CHK roll call is complete and valid: CHK-015 `demonstrated` (`tests/artifact_absence.rs::release_build_emits_patina_but_not_patina_elevate` — real `cargo build --workspace --release --message-format=json` JSON parse, ran green in 9.93s), CHK-014 `judgment-only` (correctly deferred — needs an elevated Windows host with Dev Mode OFF + UAC accept, not scriptable in CI; encoded as the `#[cfg(windows)] #[ignore]` host test at `tests/cli.rs:99-118` driving the real binary and reading the flag back via `RegQueryValueEx`). The round-2 exit-2-listing assertion (`unknown_subcommand_exits_2`, `tests/cli.rs:42-45`) is intact and non-vacuous — asserts both `status.code()==Some(2)` and `stderr.contains("enable-developer-mode")` against the real spawned binary. I re-ran `cargo test -p patina-elevate --features windows`: all green (5 lib + 1 artifact + 3 cli + 1 doctest), and a forced `RUSTDOCFLAGS="-D warnings" cargo doc --no-deps -p patina-elevate` exits 0, proving the new intra-doc link resolves (an unresolvable `[`parse_or_exit`]` would have failed). Evidence file present and non-fabricated; its Scenario-2 prose (`evidence/T-008.md:102-112`) still reflects round-1 wording, a non-blocking doc-freshness nit already noted in round 2 — not a tests blocker.
</review>

<review persona="security" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-05-30T21:49:16Z" round="3">
Round-3 change is a single rustdoc intra-doc-link fix (`[`Cli::parse`]` → `[`parse_or_exit`]`) at `patina-elevate/src/lib.rs:129` in `run`'s doc comment — no code, parse logic, registry path, dependency, or test touched. No new security surface: a doc-comment string compiles to nothing observable; the fail-closed parse boundary (bad subcommand exits 2 before any elevated action), the hard-coded `KEY::SET_VALUE` Developer Mode DWORD write, the argv-never-reaches-a-sink property, and the DEC-002 minimal-dependency trust surface are all byte-identical to the round-2 state I cleared. `speccy check SPEC-0002/T-008` renders both scenarios with no proof-shape error. Clean.
</review>

<review persona="style" verdict="pass" model="claude-sonnet-4-6[1m]/medium" date="2026-05-30T21:49:16Z" round="3">
Round-2 blocker cleared. `patina-elevate/src/lib.rs:129` now reads `see [`parse_or_exit`]` — the `run` doc comment correctly names the actual entry point that owns the exit-2 path since round 2 switched `src/main.rs` to `parse_or_exit()`.

The remaining `[`Cli::parse`]` reference at `lib.rs:53` is inside the `parse_or_exit` doc comment itself, where it accurately describes analogous behavior ("It behaves like [`Cli::parse`] for the success, `--help`, and `--version` paths, but..."). This is a valid intra-doc link (`Cli` derives `Parser` which provides `parse`), semantically correct documentation of the comparison, and resolves cleanly under `RUSTDOCFLAGS="-D warnings"` per the implementer's hygiene run.

No other stale doc references, dead imports, or convention drift visible in the diff. Round-3 fix is a clean one-line doc change with no collateral side effects.
</review>

<review persona="correctness" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-05-30T21:49:16Z" round="3">
Round-3 change is confined to a single rustdoc intra-doc link in `run`'s doc comment (`patina-elevate/src/lib.rs:129`: `[`Cli::parse`]` → `[`parse_or_exit`]`); the link resolves clean under `RUSTDOCFLAGS="-D warnings" cargo doc --no-deps`. Production logic — `parse_or_exit` parse/exit-2 path, `run`'s exit-0/exit-1 mapping, the registry `RegCreateKeyEx`+`RegSetValueEx` path with its `RegCloseKeyGuard`, and the typed `DevModeError` — is byte-identical to the round-2 pass. No new control-flow, boundary, resource-leak, or error-handling defect; no panic/unwrap/expect outside tests. Full `patina-elevate` suite green (4 lib + 1 artifact + 3 cli + 1 doctest). No correctness findings.
</review>
