---
spec: SPEC-0002
task: T-009
generated_at: 2026-05-30T22:30:00Z
---

<implementer date="2026-05-30T22:30:00Z" model="claude-opus-4-8[1m]/medium" round="1">
- Completed: Wired the Developer Mode elevation flow end-to-end (REQ-007),
  connecting T-007's detection to T-008's helper.
  - `patina-core/src/windows/elevate.rs` (`#[cfg(windows)]`, new): added
    `pub fn launch_elevate_helper() -> Result<ElevationOutcome, WindowsError>`.
    It resolves `patina-elevate.exe` as a sibling of `std::env::current_exe`,
    launches it with the `runas` verb via `winsafe::ShellExecuteEx`
    (`enable-developer-mode` subcommand, `SW::HIDE`), then re-reads the
    Developer Mode flag. The `ElevationOutcome` enum models the three
    REQ-007 outcomes: `Declined` (mapped from `ERROR_CANCELLED` — the
    canonical declined-UAC pattern), `EnabledNow` (flag now reads 1), and
    `RanButStillDisabled` (helper ran, flag still off). The main process is
    never launched elevated — only the helper is. Re-exported from
    `lib.rs` under `#[cfg(windows)]`.
  - Engine gate in `patina-core/src/apply/engine.rs::execute`: after
    `recover_orphans` and BEFORE the first `backup_before_overwrite` /
    `materialize`, added a Windows-only gate driven by
    `decide_symlink_gate(resolved, &HostDevModeProbe::default())`.
    `RequireElevation` returns the new typed `EngineError::DevModeRequired`
    (the prompt-free backstop per DEC-008); `ProceedElevatedWarning` emits a
    `tracing::warn!` recommending against running elevated; `Proceed` (the
    only verdict reachable on macOS/Linux, since the probe is `NotWindows`)
    falls through with no registry read. Added `tracing` to
    `patina-core/Cargo.toml` for that one warning.
  - New `EngineError::DevModeRequired` variant in `patina-core/src/error.rs`
    whose message names `Developer Mode` and `patina doctor --fix`.
  - Public `DEV_MODE_REGISTRY_PATH` constant in `windows/mod.rs` (re-exported)
    so the post-helper-readback failure names the registry path on every
    platform (REQ-007).
  - CLI orchestration in `patina-cli/src/cmd/apply.rs`: a cross-platform
    `drive_dev_mode_gate` runs before `execute_plan` in BOTH the human and
    the `--json --yes` paths. On `RequireElevation` it calls the
    `#[cfg(windows)]` `drive_elevation`, which launches the helper and maps
    `ElevationOutcome`: `EnabledNow` → proceed; `Declined` → exit 5
    (`ExitCode::UserDeclined`) with stderr naming `Developer Mode` and
    `patina doctor --fix` (CHK-012); `RanButStillDisabled` → `anyhow!` error
    naming the registry path (exit 1). `ProceedElevatedWarning` warns and
    proceeds. Exit code 5 stays a command-layer control-flow decision, not
    an `EngineError` mapping, per `exit_code.rs`.
  - winsafe `shell` feature added per-crate in `patina-core/Cargo.toml`
    (atop the workspace `advapi`+`kernel`) for `ShellExecuteEx`; the
    `patina-elevate` helper's feature set is left untouched.
- Undone: nothing in scope is deferred. The real Windows accept/decline
  round-trips (CHK-012 / CHK-013) ship as `#[cfg(windows)]` `#[ignore]`
  host tests since CI is not Windows and the path needs a real UAC dialog —
  exactly as the task specifies.
- Hygiene checks:
  - `cargo test --workspace`: exit 0 (all suites pass; the two new
    `#[cfg(windows)]` `#[ignore]` tests are not compiled on the host).
  - `cargo clippy --workspace --all-targets --all-features -- -D warnings`:
    exit 0. Also ran clippy for `--target x86_64-pc-windows-gnu`: exit 0
    (covers the `#[cfg(windows)]` `elevate.rs` and engine warning arm).
  - `cargo +nightly fmt --all --check`: exit 0 (the
    `float_literal_trailing_zero` warnings are pre-existing config noise).
  - `cargo deny check`: exit 0 (`advisories ok, bans ok, licenses ok,
    sources ok`; the `winnow` duplicate is a pre-existing transitive
    warning via toml/toml_edit, not introduced here).
- Evidence: paper trail at
  `.speccy/specs/0002-patina-complete-cli/evidence/T-009.md`.
  Roll call for CHKs under REQ-007:
  - CHK-012 (Windows, Dev Mode OFF, declined UAC → no symlink, stderr names
    `Developer Mode` + `patina doctor --fix`, exit 5): judgment-only on the
    CI host → `windows_declined_uac_exits_5_and_creates_no_symlink`
    (`#[cfg(windows)]` `#[ignore]`) encodes the contract; needs a real
    Windows host + declined UAC dialog. The `#[cfg(windows)]` decline →
    exit-5 + substring path is type-checked and clippy-clean via the
    windows-gnu cross-compile.
  - CHK-013 (Windows, Dev Mode ON → symlink created, no prompt, no helper
    spawn): judgment-only on the CI host →
    `windows_dev_mode_on_creates_symlink_without_prompt`
    (`#[cfg(windows)]` `#[ignore]`); needs a Windows host with Developer
    Mode enabled. Cross-platform sibling demonstrated: task scenario 1
    (`non_windows_symlink_apply_skips_dev_mode_flow`,
    `#[cfg(not(windows))]`) proves on the CI host that a symlink apply
    proceeds with no registry read and no `patina-elevate` spawn — exit 0,
    real symlink at the target.
  - Backstop message (REQ-007 typed-error contract):
    `dev_mode_required_message_is_actionable` in `patina-core/src/error.rs`
    is `demonstrated` under `cargo test` — asserts the message names both
    `Developer Mode` and `patina doctor --fix`.
- Discovered issues: the winsafe 0.0.27 safe `SHELLEXECUTEINFO` wrapper does
  not expose the spawned process `hProcess` (it lives only in the private
  `to_raw` buffer), so `SEE_MASK::NOCLOSEPROCESS` + `WaitForSingleObject`
  cannot be threaded through the safe API to block on helper exit. The
  implementation therefore relies on `ShellExecuteEx`'s synchronous return
  through the modal UAC consent UI and re-reads the flag immediately after.
  In practice the helper does a single quick registry write; this is
  documented on `launch_elevate_helper` and is only reachable on a real
  Windows host. Flagging it for the reviewer's awareness — a future tighten
  could drop to the raw FFI to wait on the handle if a race is observed.
- Procedural compliance: SPEC.md untouched. TASKS.md edited only to flip
  T-009 `pending → in-progress → in-review` (no other hand-edits). No
  `unwrap`/`expect`/`panic!` in production code; the one suppression
  (`#[expect(clippy::unnecessary_wraps)]` on the non-Windows `drive_elevation`
  stub) carries a `reason` justifying the cfg signature parity. No
  `println!`/`eprintln!` outside the Reporter layer; the engine warning uses
  `tracing::warn!`. New dependency surface (`tracing` for `patina-core`,
  winsafe `shell` feature) passed `cargo deny check`.
</implementer>

<review persona="business" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-05-30T22:04:55Z" round="1">
T-009 delivers REQ-007 end-to-end and matches the scenario contracts.

All eight REQ-007 done-when items map to the diff:
- macOS/Linux no-op (probe `NotWindows` → `GateDecision::Proceed`), proven by `non_windows_symlink_apply_skips_dev_mode_flow` (passes; asserts symlink lands, exit 0, and neither `patina-elevate` nor `Developer Mode` is named).
- Dev-Mode-ON proceed, zero-symlink-plan proceed, already-elevated proceed-with-warning, and disabled+unelevated require-elevation — all proven on the CI host via the pure `decide_symlink_gate` unit tests (`patina-core/src/windows/mod.rs`).
- Accept→re-read: `RanButStillDisabled` → exit 1 with `anyhow!` naming `DEV_MODE_REGISTRY_PATH` (`patina-cli/src/cmd/apply.rs` `drive_elevation`). Decline→exit 5 with stderr naming `Developer Mode` and `patina doctor --fix` (`ExitCode::UserDeclined` = 5 confirmed). The engine-side `EngineError::DevModeRequired` backstop message names both substrings.

CHK-012 (decline→exit 5, no link, stderr substrings) and CHK-013 (Dev-Mode-ON→link, no prompt/spawn) are honestly deferred to `#[cfg(windows)] #[ignore]` host tests in `patina-cli/tests/apply_cli.rs`, exactly as the task body prescribes (both need a real UAC host). Verified the Windows path is real, not dead code: `cargo check --target x86_64-pc-windows-gnu` is clean for both `patina-core`/`patina-cli` production and the `--tests` target, so `elevate.rs`, the engine warning arm, and the gated host tests all typecheck.

Gate placement matches the SPEC: CLI `drive_dev_mode_gate` runs after the apply-confirm and before `execute_plan` in both the human and `--json --yes` paths; the engine backstop fires after `recover_orphans` and before the first `materialize`. No scope creep (the `error.rs` `ConfigWrite` variant in the working-tree diff predates this task, from T-001). No open question was silently resolved — all five are `[x]` in SPEC.md.

One nuance considered and cleared: REQ-007/DEC-008 phrase the consent as "render the one-time UAC prompt"; the implementation uses the OS UAC dialog itself as the consent gate (a `reporter.line` informational notice precedes it, `ERROR_CANCELLED` → exit 5). This is the SPEC's own intent — goals say "a single one-time UAC prompt; accepting it toggles the registry," and non-goals state "The UAC prompt is the consent gate" — so it is faithful, not a dropped requirement.
</review>

<review persona="tests" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-05-30T22:04:55Z" round="1">
T-009 (REQ-007) passes tests review. Evidence file present at the named path with a complete roll call for REQ-007's CHKs (CHK-012, CHK-013 both `judgment-only` with specific host-test cites; one `hygiene` item naming `dev_mode_required_message_is_actionable`; one `demonstrated` cross-platform item). No fabrication patterns: every test named in the evidence appears in the diff, and the evidence command is a scoped Windows-target clippy, not the full-suite hygiene run.

(1) The cross-platform proof `non_windows_symlink_apply_skips_dev_mode_flow` (`apply_cli.rs`) is non-vacuous: it spawns the real `patina apply --yes` subprocess, asserts a real symlink materializes via `symlink_metadata().is_symlink()` at exit 0, and asserts neither `Developer Mode` nor `patina-elevate` appears in combined output — which would catch a `ProceedElevatedWarning` or helper-spawn regression on the non-Windows path. The engine backstop's substring contract is independently gated cross-platform by `dev_mode_required_message_is_actionable` (`error.rs`), asserting `EngineError::DevModeRequired` names both `Developer Mode` and `patina doctor --fix`. Note for the record: the CLI decline→exit-5+stderr mapping and the `RanButStillDisabled`→exit-1+registry-path mapping live entirely in `#[cfg(windows)]` `drive_elevation` (`apply.rs:163-184`) and are exercised only by the host `#[ignore]` test, not cross-platform — `ElevationOutcome`/`launch_elevate_helper` are not compiled on non-Windows, so that mapping genuinely cannot be CI-tested. This matches the evidence's own framing (it does not overclaim a cross-platform decline proof) and does not block.

(2) The `#[ignore]` host tests `windows_declined_uac_exits_5_and_creates_no_symlink` and `windows_dev_mode_on_creates_symlink_without_prompt` drive the real flow: both invoke the real binary and assert real exit codes, real symlink presence/absence, and real stderr substrings — on Windows the live `drive_elevation → launch_elevate_helper → ShellExecuteEx(runas)` path executes. Not stubs; the Windows arms are confirmed type-checking/lint-clean via the `x86_64-pc-windows-gnu` cross-compile in the evidence.

(3) The `judgment-only` labelling for CHK-012/CHK-013 is honest: both require a real Windows host plus a UAC consent modal (accept/decline), which the macOS/Linux CI cannot supply deterministically, so they are genuinely not demonstrable by a deterministic command. Per the verdict contract these CHKs are deferred to reviewer judgment, the label is plausible, and the roll call covers every CHK this task touches with no silent omission.
</review>

<review persona="security" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-05-30T22:04:55Z" round="1">
T-009 elevation flow is secure on all six audited axes; no blocking findings.

- Helper path: `helper_path()` (`patina-core/src/windows/elevate.rs:98-108`) resolves `patina-elevate.exe` via `std::env::current_exe().parent().join(HELPER_EXE)` — no PATH/cwd/env-var lookup, so a planted helper cannot be run elevated. Correct.
- Fixed argv: the elevated child receives only the compile-time constant `parameters: Some("enable-developer-mode")` (`elevate.rs:32,80`); no user/repo/env string is interpolated, and the helper rejects any other subcommand with exit 2 (`patina-elevate/src/lib.rs`).
- Declined UAC: `co::ERROR::CANCELLED` → `ElevationOutcome::Declined` → CLI `drive_elevation` exit 5 with a warning naming `Developer Mode` + `patina doctor --fix`, returning `Ok(Some(..))` to short-circuit before `execute_plan` (`patina-cli/src/cmd/apply.rs`). No silent proceed (CHK-012).
- Post-launch readback: `reread_outcome()` (`elevate.rs:113-118`) ignores the child exit/`ShellExecuteEx` success and re-reads the actual registry flag; only `Ok(Some(1))` proceeds, all else (incl. read error) → `RanButStillDisabled` (CLI exit 1, no materialize). Engine backstop `decide_symlink_gate` in `execute` (`apply/engine.rs`) independently re-probes and returns `DevModeRequired` if still off. Fail-safe two-layer defense.
- Dependencies: `cargo deny check` → `advisories ok, bans ok, licenses ok, sources ok`. New `winsafe 0.0.27` and the `toml_edit`/`winnow 0.7.15` graph pass; the `winnow` duplicate is a non-failing `[duplicate]` warning only.
- Logging: only static remediation strings via `tracing::warn!` / Reporter; no paths, tokens, or registry contents leaked.

Non-blocking note (already disclosed by the implementer): the winsafe safe wrapper does not surface `hProcess`, so the launch cannot `WaitForSingleObject` and instead re-reads the flag immediately after the modal UAC return. The worst-case race resolves to `RanButStillDisabled` (clean exit 1), never a false-positive that materializes symlinks with Dev Mode off — so the race is fail-safe, not a security defect.
</review>

<review persona="style" verdict="blocking" model="claude-sonnet-4-6[1m]/medium" date="2026-05-30T22:04:55Z" round="1">
`patina-core/src/error.rs:109–123`: `dev_mode_required_message_is_actionable` is a vacuous constant-copy test — a prohibited shape per the project's Test Hygiene rules in `AGENTS.md`. The `#[error("Developer Mode is disabled; ... Run \`patina doctor --fix\` ...")]` attribute string is defined in the same file four lines above the test. The test asserts that `EngineError::DevModeRequired.to_string()` contains `"Developer Mode"` and `"patina doctor --fix"` — both of which are hard-coded literals in the very `#[error]` macro being tested. The test cannot fail unless someone edits the `#[error]` string without updating the test, which is the classic constant-copy shape: it asserts editorial choices, not a real behavioral invariant.

To gate a meaningful invariant, the test should exercise the gate path itself — e.g., call `execute` (or the engine gate codepath) with a fake probe that returns `Disabled`/unelevated and a plan with a symlink op, and assert that the returned `Err` is `EngineError::DevModeRequired`. That test can actually fail on a regression (if someone removes the gate check or changes the variant). Alternatively, if the only intent is to lock the message content, delete the test and rely on the well-established convention of test-in-review (since message wording is editorial).

Separately, `patina-cli/src/cmd/apply.rs:202` uses `let _ = DEV_MODE_REGISTRY_PATH;` inside the non-Windows stub `drive_elevation` to prevent the import from being "dead" on non-Windows. The idiomatic fix is `#[cfg(windows)]` on the import at line 29 — the constant is only consumed on Windows. The `let _ = CONSTANT;` workaround is a non-idiomatic code smell. This is non-blocking on its own (Clippy passes, the comment explains it), but it should be addressed alongside the import at line 29.

The blocking item is the constant-copy test.
</review>

<review persona="correctness" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-05-30T22:04:55Z" round="1">
T-009 passes correctness review.

(1) Gate placement verified: the engine gate (engine.rs:373-382) runs after `recover_orphans` (361) and strictly before the plan-journal flush (407, the durability point) and the first `backup_before_overwrite`/`materialize` (424-426). A `RequireElevation` verdict returns `Err(DevModeRequired)` before any journal write or filesystem mutation, so crash-safety and idempotency invariants hold — a declined/required path mutates nothing.

(2) Gate-decision mapping is correct and exhaustive in both consumers. `decide_symlink_gate` (windows/mod.rs:276-292): no-symlink/Enabled/Unsupported/NotWindows → `Proceed`; Disabled+elevated → `ProceedElevatedWarning`; Disabled+unelevated → `RequireElevation`. Engine maps all three arms (Proceed → no-op, warn → `tracing::warn!` + proceed, RequireElevation → typed `EngineError::DevModeRequired`); CLI maps all three (Proceed → Ok(None), warn → reporter.warn + Ok(None), RequireElevation → drive_elevation). All branches unit-tested green on host.

(3) CLI `drive_dev_mode_gate` runs before `execute_plan` in both the human path (apply.rs:118, after the apply/decline decision) and the `--json --yes` path (223, after the preview short-circuit). Decline → exit 5 with stderr naming `Developer Mode` + `patina doctor --fix`; post-readback-still-off → `anyhow!` naming `DEV_MODE_REGISTRY_PATH` → exit 1. No double-gate hang: after the helper enables Dev Mode, the engine's independent re-read of the live registry returns `Proceed`.

(4) `elevate.rs` `ShellExecuteEx`/`SHELLEXECUTEINFO` usage, `co::ERROR::CANCELLED`→`Declined` mapping, and the flag re-read independently verified by a forced `--target x86_64-pc-windows-gnu` clippy build with `-D warnings` (clean). `reread_outcome` maps `Ok(Some(1))` → EnabledNow and `Ok(_)|Err(_)` → RanButStillDisabled — conservative, no silent proceed. `helper_path` handles the no-parent case with a typed error.

(5) No unwrap/expect/panic in production; the one `#[expect]` is the cfg-parity stub. No path materializes a symlink with Dev Mode off — the engine gate is an independent backstop and `ExecutorError::WindowsSymlinkPermission` remains the final guard.

Noted but not blocking: the documented inability to `WaitForSingleObject` on the helper handle leaves a theoretical post-`ShellExecuteEx` flag-read race, but its only failure mode is a conservative, recoverable false-negative (exit 1, actionable message) on a real-Windows-only path — below the threshold for a correctness blocker.
</review>

<blockers date="2026-05-30T22:04:55Z" round="1">
One blocker landed in round 1 (style). Business, tests, security, and correctness all passed. Fix the single blocker before respawning the review fan-out.

1. **Vacuous constant-copy test `dev_mode_required_message_is_actionable` (style).** `patina-core/src/error.rs:109-123` asserts that `EngineError::DevModeRequired.to_string()` contains `"Developer Mode"` and `"patina doctor --fix"` — but both are hard-coded literals in the `#[error("...")]` attribute defined four lines above in the same file. Per the AGENTS.md test-hygiene rule (shape 2: "copying production constants into the test"), this gates only that someone updated both sites in sync, not a real behavioral invariant. Fix (pick one):
   - Preferred: replace it with a test that exercises the engine gate path — drive `execute` (or the gate codepath) with a fake probe returning `Disabled`/unelevated and a plan containing a symlink op, and assert the returned `Err` is `EngineError::DevModeRequired`. That fails on a real regression (gate removed or variant changed). Note the gate is `#[cfg(windows)]`; if the engine gate cannot be exercised cross-platform, the meaningful surface is the pure `decide_symlink_gate` returning `GateDecision::RequireElevation` for the disabled+unelevated+symlink-plan input (which is already unit-tested) — in that case simply DELETE the redundant message-substring test rather than keep a constant-copy.
   - Acceptable: delete the test; message wording is editorial and is covered by test-in-review.
   While there, address the non-blocking companion: `patina-cli/src/cmd/apply.rs:202` `let _ = DEV_MODE_REGISTRY_PATH;` should become a `#[cfg(windows)]` gate on the import at apply.rs:29 (the constant is only consumed on the Windows path), removing the non-idiomatic keep-alive.

Already correct and must not regress: REQ-007 is delivered end-to-end across business, tests, security, and correctness — the engine gate placement (after `recover_orphans`, before any journal flush/materialize), the three-arm `decide_symlink_gate` mapping, the CLI `drive_dev_mode_gate` running before `execute_plan` in both paths with decline→exit-5 and readback-still-off→exit-1, the `current_exe`-adjacent `helper_path` (no PATH/cwd lookup), the fixed-argv elevated launch, the `ERROR_CANCELLED`→Declined fail-safe, and the honest `#[cfg(windows)] #[ignore]` CHK-012/CHK-013 host tests. The retry is a single test fix (plus the small cfg-import tidy); do not rework the elevation flow.
</blockers>

<implementer date="2026-05-30T22:10:00Z" model="claude-opus-4-8[1m]/medium" round="2">
- Completed: addressed the sole round-1 style blocker and its companion;
  the elevation flow itself is untouched.
  - Deleted the vacuous constant-copy test
    `dev_mode_required_message_is_actionable` (and its now-empty
    `#[cfg(test)] mod tests` in `patina-core/src/error.rs`). Per the
    blocker's preferred path: the meaningful invariant is already gated
    cross-platform by the pure
    `decide_symlink_gate(..) == GateDecision::RequireElevation` test
    `gate_disabled_not_elevated_requires_elevation`
    (`patina-core/src/windows/mod.rs`) for the disabled+unelevated+
    symlink-plan input. That test is the real behavioural surface driving
    the engine into `EngineError::DevModeRequired`, so the substring test
    was redundant — deleted rather than duplicated. Message wording is
    editorial, covered by test-in-review.
  - Replaced the non-idiomatic `let _ = DEV_MODE_REGISTRY_PATH;` keep-alive
    in the non-Windows `drive_elevation` stub
    (`patina-cli/src/cmd/apply.rs`) with a `#[cfg(windows)]` gate on the
    `use patina_core::DEV_MODE_REGISTRY_PATH;` import. The constant is only
    consumed on the Windows `RanButStillDisabled` arm, so gating the import
    drops the keep-alive cleanly with no dead-symbol warning on either
    platform.
- Undone: nothing. The blocker scoped a single test fix plus the cfg-import
  tidy; both are done. The elevation flow, gate placement, and CHK-012 /
  CHK-013 `#[cfg(windows)] #[ignore]` host tests are unchanged from round 1.
- Hygiene checks:
  - `cargo test --workspace`: exit 0.
  - `cargo clippy --workspace --all-targets --all-features -- -D warnings`:
    exit 0.
  - `cargo +nightly fmt --all --check`: exit 0 (applied `cargo +nightly
    fmt --all` to tidy a dangling comment line left by the keep-alive
    removal and pre-existing round-1 re-export ordering in `lib.rs` /
    `windows/mod.rs`; the `float_literal_trailing_zero` lines are
    pre-existing unknown-config warnings, not diffs).
  - `cargo deny check`: exit 0.
- Evidence: paper trail at
  `.speccy/specs/0002-patina-complete-cli/evidence/T-009.md` (round-1).
  Roll call for CHKs under REQ-007 is unchanged from round 1; this round
  only removed a redundant test and tidied an import, touching no CHK
  coverage:
  - CHK-012 (Windows, Dev Mode OFF, declined UAC → no symlink, stderr names
    `Developer Mode` + `patina doctor --fix`, exit 5): judgment-only →
    `windows_declined_uac_exits_5_and_creates_no_symlink`
    (`#[cfg(windows)]` `#[ignore]`); needs a real Windows host + declined
    UAC dialog. Unchanged.
  - CHK-013 (Windows, Dev Mode ON → symlink created, no prompt, no helper
    spawn): judgment-only →
    `windows_dev_mode_on_creates_symlink_without_prompt`
    (`#[cfg(windows)]` `#[ignore]`). Cross-platform sibling
    `non_windows_symlink_apply_skips_dev_mode_flow` still proves a symlink
    apply proceeds with no registry read / no helper spawn on the CI host.
    Unchanged.
  - Engine backstop (REQ-007 typed-error contract): the deleted
    substring test was redundant with the pure-gate test
    `gate_disabled_not_elevated_requires_elevation` (windows/mod.rs),
    which gates `RequireElevation` for the disabled+unelevated+symlink-plan
    input — the actual surface driving `EngineError::DevModeRequired`.
    `demonstrated` under `cargo test`.
- Discovered issues: none new this round.
- Procedural compliance: SPEC.md untouched. TASKS.md edited only to flip
  T-009 `pending → in-progress → in-review`. Journal appended (not
  rewritten). No `unwrap`/`expect`/`panic!` added; net suppression count
  dropped by zero (the cfg-parity `#[expect(clippy::unnecessary_wraps)]` on
  the non-Windows stub remains, still carrying its `reason`). No
  `println!`/`eprintln!` added. No new dependency surface.
</implementer>

<review persona="business" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-05-30T22:13:47Z" round="2">
T-009 (REQ-007) still delivers end-to-end; the round-2 change is test-only + cfg-import tidy and regresses no `<done-when>` item.

Round-2 deltas verified against merge-base `9d3752b`:
- Deleted the vacuous `dev_mode_required_message_is_actionable` test from `patina-core/src/error.rs` (no `#[cfg(test)] mod tests` residue remains). The real REQ-007 invariant — disabled+unelevated+symlink-plan → `RequireElevation`, which drives the engine into `EngineError::DevModeRequired` — is gated cross-platform by `gate_disabled_not_elevated_requires_elevation` (`patina-core/src/windows/mod.rs:382-390`), a non-vacuous `FakeProbe`-driven assertion on `decide_symlink_gate`. Deletion-not-duplication matches the round-1 style blocker's preferred remedy.
- Replaced `let _ = DEV_MODE_REGISTRY_PATH;` with `#[cfg(windows)] use patina_core::DEV_MODE_REGISTRY_PATH;` in `patina-cli/src/cmd/apply.rs`; the constant is still consumed on the Windows `RanButStillDisabled` arm so the post-readback error still names the registry path per REQ-007.

REQ-007 delivery is unchanged: the `DevModeRequired` `#[error(...)]` message still names both `Developer Mode` and `patina doctor --fix`; the gate placement, CLI `drive_dev_mode_gate` (decline→exit 5, readback-still-off→exit 1, already-elevated warn), and the CHK-012/CHK-013 `#[cfg(windows)] #[ignore]` host tests are untouched. No non-goal introduced (the `ConfigWrite` variant in the diff range predates T-009), no open question silently resolved. Round-1 business pass stands.
</review>

<review persona="tests" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-05-30T22:13:47Z" round="2">
Round-2 deletion of the vacuous `dev_mode_required_message_is_actionable` is clean and the REQ-007 invariant remains genuinely covered.

(1) Invariant intact: `gate_disabled_not_elevated_requires_elevation` (`patina-core/src/windows/mod.rs:382-391`) is non-vacuous — it builds a `FakeProbe { status: Disabled, elevated: false }`, runs the real `decide_symlink_gate` over a symlink-bearing plan, and asserts `GateDecision::RequireElevation`. Mentally flip the elevated branch or return `Proceed` and the test fails. `engine.rs:381` wires `GateDecision::RequireElevation => Err(EngineError::DevModeRequired)`, so this test gates the actual surface driving the typed error. The deleted substring test only re-checked the `#[error]` literal four lines above it (constant-copy), so it was correctly redundant, not a coverage loss.

(2) The authoritative round-2 `Evidence:` roll call in the journal is reconciled: CHK-012 and CHK-013 (the only two CHKs under REQ-007, per `speccy check`) are both `judgment-only` with specific `#[cfg(windows)] #[ignore]` host-test cites — plausible, since both need a real Windows host plus a UAC modal CI cannot supply — and the engine backstop is re-pointed to `gate_disabled_not_elevated_requires_elevation` (`demonstrated`). No CHK is silently omitted; no fabrication patterns (the deletion is a deliberate, journaled removal, not a phantom red-phase test name).

(3) Full suite green: `cargo test --workspace` passes every target (patina-cli 51 unit + all integration incl. `non_windows_symlink_apply_skips_dev_mode_flow`, patina-core 176 unit, patina-elevate 5, 23 doctests; the two host tests are `#[ignore]`/not-compiled on this host). Nothing regressed.

Non-blocking nit for a future tidy: the standalone `evidence/T-009.md:76-79` still lists the deleted `dev_mode_required_message_is_actionable` as a hygiene supporting item. The journal's round-2 roll call supersedes and corrects this, so coverage legibility is preserved where the contract reads it, but the round-1 evidence file is now stale and would mislead a reader who consults only that file.
</review>

<review persona="security" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-05-30T22:13:47Z" round="2">
Round-2 change (Windows-gated `DEV_MODE_REGISTRY_PATH` import tidy in `patina-cli/src/cmd/apply.rs:29-30` plus a test-only deletion in `patina-core/src/error.rs`) introduces no new security surface and weakens none of the round-1 properties. Verified intact: sibling-only helper-path resolution via `current_exe()` parent (`patina-core/src/windows/elevate.rs:97-108`, no PATH/env/relative lookup); fully constant argv into `ShellExecuteEx` with no user input (`elevate.rs:77-83`); `ERROR_CANCELLED` → fail-closed `Declined`/exit-5 (`elevate.rs:89`); fail-safe readback that proceeds only on `Ok(Some(1))` (`elevate.rs:113-117`); and the engine backstop returning `DevModeRequired` before any mutation (`patina-core/src/apply/engine.rs:360+`). No new dependency, no sensitive-data logging, no injection/auth vector. `cargo build -p patina-cli -p patina-core` is clean.
</review>

<review persona="style" verdict="pass" model="claude-sonnet-4-6[1m]/medium" date="2026-05-30T22:13:47Z" round="2">
Both round-1 blockers are resolved. `patina-core/src/error.rs` ends at line 103 with no `#[cfg(test)] mod tests` block and no `dev_mode_required_message_is_actionable` test — the vacuous constant-copy test and its now-empty module are fully deleted. `patina-cli/src/cmd/apply.rs` lines 29–30 carry `#[cfg(windows)] use patina_core::DEV_MODE_REGISTRY_PATH;` and the `let _ = DEV_MODE_REGISTRY_PATH;` keep-alive is gone; the constant is consumed only inside `#[cfg(windows)] fn drive_elevation` at line 182, so neither platform accumulates a dead-import warning. The meaningful behavioral invariant (`gate_disabled_not_elevated_requires_elevation` in `windows/mod.rs`) is untouched. No new style issues introduced.
</review>

<review persona="correctness" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-05-30T22:13:47Z" round="2">
T-009 round-2 passes correctness review. The round-1-pass elevation logic is intact: the engine gate (engine.rs:373-382) still runs after `recover_orphans` and before the journal durability flush/materialize, the `decide_symlink_gate` three-arm mapping and CLI `drive_dev_mode_gate`/`drive_elevation` (apply.rs) are unchanged, and the decline→exit-5 / readback-still-off→exit-1 mapping is byte-identical. The round-2 delta is confined to deleting the vacuous `dev_mode_required_message_is_actionable` test (and its now-empty test module — confirmed absent) plus replacing the `let _ = DEV_MODE_REGISTRY_PATH;` keep-alive with a `#[cfg(windows)]` gate on the import at apply.rs:30. The cfg gate is correct: the constant has a single use at apply.rs:182 inside the `#[cfg(windows)]` `drive_elevation` fn, so the non-Windows build neither imports nor references it (host `cargo check`/`clippy -D warnings` clean), and the windows-gnu target resolves and uses it (`cargo check`/`clippy -D warnings --target x86_64-pc-windows-gnu` clean). No correctness defect found.
</review>
