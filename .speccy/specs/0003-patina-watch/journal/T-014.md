---
spec: SPEC-0003
task: T-014
generated_at: 2026-06-01T00:00:00Z
---

<implementer date="2026-06-01T00:00:00Z" model="claude-opus-4-8[1m]/high" round="1">
- Reuse survey: Mapped the task area (covered REQ-001 plus DEC-005 /
  DEC-009 / DEC-010, the suggested files `docs/USER_GUIDE.md` and
  `patina-cli/tests/docs_structure.rs`, and their immediate context —
  the watch CLI surface in `patina-cli/src/cli.rs` and `debug.rs` to
  keep docs matching shipped code). This is a documentation task, so no
  production symbols were added. Decisions: reuse-as-is — the existing
  six REQ-027 H2 anchors in `USER_GUIDE.md` (`Installation`,
  `Declaring dotfiles`, `Apply flow`, `State directory`, `Recovery`,
  `Troubleshooting`) and the `user_guide_has_required_h2_headings` test
  set are left untouched; the existing `patina debug journal` reference
  site in `## Recovery` is reused, with the `drift-cache` decoder
  described immediately alongside it. Extend — the stale placeholder
  paragraph ("Enabling the Linux watcher service to survive logout ...
  is a SPEC-0003 concern and is not covered here") is replaced by the
  real content; the `## Troubleshooting` list gains two watcher bullets.
  Write-fresh — a new `## Watch service` H2 section; searched `docs/`
  for any existing `patina watch` / `enable-linger` section (`grep`)
  and found only the placeholder, so the section is new. The
  2026-05-27 changelog row explicitly permits a new section name
  ("REQ-027 does not constrain the section name"), and REQ-027's six
  required anchors are unchanged, so no docs_structure test edit was
  needed. No new top-level code symbol introduced (docs-only change).
- Completed: Documented the watch subsystem in `docs/USER_GUIDE.md`.
  Added a `## Watch service` section covering all six lifecycle
  subcommands (`install` / `uninstall` / `start` / `stop` / `restart` /
  `status`) in a purpose table plus `patina watch --foreground`, the
  three per-OS service descriptor locations
  (`~/Library/LaunchAgents/com.patina.watcher.plist`,
  `~/.config/systemd/user/patina-watcher.service`, the Windows
  `Patina Watcher` HKCU Scheduled Task with logon trigger) in a second
  table, a "Surviving logout on Linux" subsection carrying the literal
  `sudo loginctl enable-linger $USER` snippet with the
  no-`--linger`-flag / no-privilege-escalation note (DEC-005), a
  "Non-systemd init systems" subsection pointing at
  `--foreground`-under-your-own-supervisor (DEC-010), and a "Drift
  notifications" subsection explaining the notification-when-running vs.
  `DRIFTED`-in-`patina status`-always distinction and the
  `patina apply` (revert) / `patina promote` (update source) resolution
  paths. Documented `patina debug drift-cache <path>` in `## Recovery`
  alongside the existing `patina debug journal` reference. Added two
  watcher troubleshooting bullets (survive-logout, and DRIFTED-without-
  notification). Replaced the stale SPEC-0003-placeholder paragraph.
- Undone: Nothing in T-014's scope is deferred. T-014 is the final
  task in SPEC-0003's task list; the watch install/foreground/drift
  behaviour itself was implemented and reviewed under T-001..T-013.
- Hygiene checks: `cargo test --workspace --locked` (via `just test`)
  exited 0; the `docs_structure` integration test passed all three
  cases (`architecture_has_required_h2_headings`,
  `user_guide_has_required_h2_headings`,
  `user_guide_state_directory_lists_cloud_sync_providers`), confirming
  the six REQ-027 anchors and the six cloud-sync bullets survive the
  edit. `just lint` exited 0, which runs CI's four jobs: `cargo
  +nightly fmt --all --check` (0), `cargo clippy --workspace
  --all-targets --all-features -- -D warnings` (0), `RUSTDOCFLAGS="-D
  warnings" cargo doc --workspace --no-deps --all-features` (0 — the
  load-bearing gate for a docs change; no intra-doc link broken), and
  `cargo deny check` (0 — advisories/bans/licenses/sources ok; no
  dependency change).
- Evidence: no `evidence/T-014.md` paper trail — this is a docs-only
  change with no runtime behaviour to demonstrate red-then-green; the
  proof surface is the docs_structure test plus `cargo doc` (both
  hygiene) and reviewer judgment on the prose. Roll call for the CHKs
  under the covered REQ-001:
  - CHK-001 (macOS install writes `com.patina.watcher.plist`,
    `result: installed`): judgment-only → this task documents, not
    implements, the install behaviour (shipped under earlier tasks);
    reviewer-business judges that the `## Watch service` macOS table
    row and prose faithfully describe the CHK-001 behaviour. The
    docs-structure assertion that the section names the macOS service
    location is hygiene-covered by the task scenario below.
  - CHK-002 (Linux install writes `patina-watcher.service`,
    `result: installed`): judgment-only → same as CHK-001;
    reviewer-business judges the Linux table row and the
    survive-logout / systemd prose against the CHK-002 behaviour.
  - CHK-003 (Windows HKCU `Patina Watcher` Scheduled Task, `OnLogon`,
    `result: installed`): judgment-only → reviewer-business judges the
    Windows table row (HKCU, logon trigger) against the CHK-003
    behaviour.
  Task-scenario coverage (the docs-task acceptance surface, covering
  REQ-001 / DEC-005 / REQ-027):
  - Scenario 1 (section names six subcommands + `--foreground` + three
    per-OS locations + the literal `sudo loginctl enable-linger $USER`
    snippet with the no-`--linger` note): hygiene for the structural
    portion — the `## Watch service` and `## Troubleshooting` anchors
    and the unchanged REQ-027 anchors are gated by
    `user_guide_has_required_h2_headings` in
    `patina-cli/tests/docs_structure.rs` under `cargo test --workspace`;
    judgment-only for the prose content (subcommand names, locations,
    and the linger snippet are reviewer-verified on the diff).
  - Scenario 2 (docs_structure test and `cargo doc -D warnings` both
    pass after the edit): hygiene → `cargo test --workspace --locked`
    and `RUSTDOCFLAGS="-D warnings" cargo doc --workspace --no-deps
    --all-features` both exited 0, as recorded under Hygiene checks.
- Discovered issues: none. The replaced placeholder was the only stale
  SPEC-0003 reference in `USER_GUIDE.md`.
- Procedural compliance: This implementer entry lands directly in
  `journal/T-014.md` per the journal-file schema; no `<implementer-note>`
  was written into TASKS.md. The TASKS.md `state="..."` attribute for
  T-014 was flipped `pending` → `in-progress` at the start of this turn
  and `in-progress` → `in-review` as the final mutation before this
  journal write. No shipped skill bodies under `.claude/skills/` were
  edited.
</implementer>

<review persona="business" verdict="pass" model="claude-opus-4-8[1m]/high" date="2026-06-01T04:22:51Z" round="1">
T-014 docs satisfy every done-when surface it covers. `docs/USER_GUIDE.md` (new `## Watch service` section + Recovery/Troubleshooting additions) documents: the six lifecycle subcommands + `--foreground` (matches `WatchCommand` / `WatchArgs.foreground` in `patina-cli/src/cli.rs`); the three per-OS service locations (matches the `com.patina.watcher.plist` / `patina-watcher.service` / `Patina Watcher` HKCU-logon-trigger constants in `patina-core/src/watch/service/{launchd,systemd,scheduled_task}.rs`); the Linux `sudo loginctl enable-linger $USER` survive-logout opt-in with the explicit no-`--linger`-flag / never-elevates note (DEC-005); the non-systemd `--foreground`-under-your-own-supervisor fallback (DEC-010); and `patina debug drift-cache <path>` with its envelope/timestamp/hashes/detection-time render, newer-version refusal, and exit-1-on-bad-path (matches `patina-cli/src/cmd/debug.rs`). The Drift-notifications subsection correctly captures the post-2026-05-29 intent — notification only while the watcher runs, DRIFTED in `patina status` always via live re-hash, drift cache is the watcher's notification ledger and not a status input — avoiding the stale-intent trap; notification title "Patina: drift detected" and the 60s rate limit match `patina-core/src/watch/drift.rs`. The `patina promote` "updates source then re-applies" claim matches shipped SPEC-0002 behavior. The six REQ-027 H2 anchors survive (docs_structure.rs unchanged), and the new section name is permitted by the 2026-05-27 changelog row. No phantom behavior, no smuggled non-goal scope.
</review>

<review persona="docs" verdict="pass" model="claude-sonnet-4-6[1m]" date="2026-06-01T04:22:51Z" round="1">
All documented behavior in the `## Watch service` section, the `## Recovery` drift-cache paragraph, and the two `## Troubleshooting` bullets checks out against the implementation.
- Six lifecycle subcommands and their descriptions match `WatchSubcommand` variants in `patina-cli/src/cli.rs`.
- `--foreground` documented correctly; matches the `foreground: bool` field in `WatchArgs`.
- macOS `~/Library/LaunchAgents/com.patina.watcher.plist` confirmed against `launchd.rs`.
- Linux `~/.config/systemd/user/patina-watcher.service` confirmed against `systemd.rs` (docs show the `$HOME` default; `XDG_CONFIG_HOME` override exists but the default path is correct and not misleading).
- Windows `Patina Watcher` HKCU logon trigger confirmed against `scheduled_task.rs`.
- `sudo loginctl enable-linger $USER` snippet accurate; no-`--linger`-flag/no-privilege note aligns with DEC-005.
- Notification title `"Patina: drift detected"` confirmed as `NOTIFICATION_TITLE` in `drift.rs`; 60-second rate limit confirmed as `RATE_LIMIT_WINDOW_SECS = 60`.
- `install` exits 1 if already installed confirmed by `ServiceError::AlreadyInstalled`.
- `patina debug drift-cache <path>` confirmed as `DebugCommand::DriftCache` in `cli.rs`/`debug.rs`.
- `patina status` drift independence from the drift cache confirmed by the module-level comment in `drift.rs`.
- `patina watch status` counters (`subscriptions_count`, `re_applies_since_start`, `last_exit_code`, `last_fired_at`) confirmed in `service/mod.rs`.
No inaccuracies, broken intra-doc references, or material omissions found.
</review>

<review persona="style" verdict="pass" model="claude-sonnet-4-6[1m]" date="2026-06-01T04:22:51Z" round="1">
All style criteria pass. The `## Watch service` H2 is correctly placed between `## Commands` and `## State directory`, and its three H3 subsections (`### Surviving logout on Linux`, `### Non-systemd init systems`, `### Drift notifications`) are hierarchically consistent with the existing `### Windows symbolic-link elevation` pattern. All code fences use the `sh` language tag matching every other fence in the file. Command and flag names are uniformly backtick-quoted. Tables are properly formatted with aligned column separators. Bullet lists use `-` markers consistently. The `(see "Watch service")` cross-reference in `## Troubleshooting` correctly names the new H2. The `## Recovery` extension is parallel in structure to the existing `patina debug journal` paragraph. Trailing newline confirmed. No orphaned placeholder text, trailing cruft, or bare suppressions.
</review>

<review persona="tests" verdict="pass" model="claude-opus-4-8[1m]/high" date="2026-06-01T04:22:51Z" round="1">
Docs-only task; no test added or modified. `patina-cli/tests/docs_structure.rs` is unchanged against merge-base — the implementer correctly relies on the pre-existing structural gate rather than writing a vacuous prose-substring test. The existing `docs_structure` test is a legitimate structural gate, not a forbidden prose-substring match: `h2_headings` parses CommonMark and asserts H2 anchor existence by exact heading text, and `section_list_items` asserts cloud-sync bullet membership inside `## State directory` by exact list-item text. It never substring-matches surrounding prose — exactly the stable-structural-surface carve-out the test-hygiene rule permits. All six required anchors and all six cloud-sync bullets survive the edit despite the new `## Watch service` H2 and the rewritten Recovery/Troubleshooting bodies; a renamed/dropped anchor or clobbered bullet would fail the test. Evidence roll call complete: CHK-001/002/003 labelled `judgment-only` (install behaviour shipped under T-001..T-013; this task only documents it), task-scenario coverage cites `user_guide_has_required_h2_headings` as hygiene. The declared absence of an `evidence/T-014.md` paper trail is correct for a docs-only change; no fabricated runner output, no mock-the-subject risk.
</review>

<review persona="security" verdict="pass" model="claude-opus-4-8[1m]/high" date="2026-06-01T04:22:51Z" round="1">
Docs-only change to `docs/USER_GUIDE.md`; no production code, dependency, or test-behavior changes. The sole privileged snippet, `sudo loginctl enable-linger $USER`, is minimal-scope correct: `$USER` confines linger to the invoking user (not all users / not a broad target), `sudo` is genuinely required for the polkit `manage-linger` action in non-interactive/SSH contexts, and `enable-linger` for one's own UID is the least privilege that achieves survive-logout — not over-broad guidance. The doc correctly states Patina never runs it for the user and never escalates privilege on their behalf. Per-OS service descriptors documented at per-user scope only (macOS `~/Library/LaunchAgents`, not root `LaunchDaemons`; Windows HKCU, not HKLM); no system-wide guidance. No secret exposure, no credential-leaking command examples, no advice to disable a safeguard, and the debug decoders are described as version-envelope-checked and read-only. No security blockers.
</review>

<review persona="correctness" verdict="pass" model="claude-opus-4-8[1m]/high" date="2026-06-01T04:22:51Z" round="1">
Docs-only change to `docs/USER_GUIDE.md` is factually correct against the shipped CLI surface. Verified every documented command, flag, path, and behavioral claim: all six watch lifecycle subcommands (`install`/`uninstall`/`start`/`stop`/`restart`/`status`), `--foreground`, the global `--json` (accepted after the subcommand as documented), and `patina debug drift-cache <path>` all parse and exist (confirmed via `--help`). Per-OS service descriptors (`~/Library/LaunchAgents/com.patina.watcher.plist`, `~/.config/systemd/user/patina-watcher.service`, the `Patina Watcher` HKCU logon-trigger task), the `install`-exits-1-if-already-installed behavior, the `sudo loginctl enable-linger $USER` snippet with no `--linger` flag, the notification title "Patina: drift detected", the 60s rate limit, the `<state>/patina/drift.cache` path, the SIGINT/SIGTERM clean shutdown, and the drift-cache decoder's exit-1-on-invalid-path / newer-version-refusal all match the source. No documented invocation would error or behave differently than described.
</review>
