---
spec: SPEC-0003
task: T-003
generated_at: 2026-05-31T22:30:00Z
---

<implementer date="2026-05-31T22:30:00Z" model="claude-opus-4-8[1m]/high" round="1">
- Reuse survey: Mapped the task area — covered REQ-010, the suggested
  files (`patina-core/src/apply/retry.rs` new,
  `patina-core/src/apply/{copy,template}.rs`, `patina-core/src/fsx.rs`,
  `patina-core/tests/fs_retry.rs`) and their immediate neighbours.
  Decisions:
  - reuse-as-is — `tracing::debug!` (already a `patina-core` dep) for
    the `fs_write_retry` event; `std::io::Error::raw_os_error()` for the
    Win32-32 match, the same kind+raw_os_error shape `lock::is_contended`
    (`patina-core/src/lock.rs:330`) already uses; the three write sites
    already produce `io::Result`, so they slot into the closure verbatim.
  - extend — none; the existing sites keep their `ExecutorError::Io`
    mapping unchanged, with only the inner `io::Result`-returning call
    wrapped.
  - write-fresh — new top-level symbols: `pub(crate) fn
    with_sharing_violation_retry<T>` and the private `BACKOFF_SCHEDULE_MS`
    / `ERROR_SHARING_VIOLATION` constants in a new `apply/retry.rs`.
    Searched `rg 'fn with.*retry|BACKOFF|SHARING_VIOLATION|raw_os_error.*32'`
    across `patina-core/src` — no existing retry wrapper or Win32-32
    matcher. `lock::is_contended` is the nearest cousin but matches a
    different error in a different module and is not promotable without
    coupling `lock` to `apply`, so write-fresh is justified.
- Completed: created `patina-core/src/apply/retry.rs` exposing
  `with_sharing_violation_retry<T>(op: impl FnMut() -> io::Result<T>) ->
  io::Result<T>`. Under `#[cfg(windows)]` it loops: on `Ok` returns; on
  `Err` it re-raises immediately unless the error is
  `raw_os_error() == Some(32)` AND the six-entry budget is unspent, in
  which case it emits a `fs_write_retry` debug event (`attempt`,
  `delay_ms`, `error`), sleeps the next `BACKOFF_SCHEDULE_MS` value
  (`[50,100,200,400,800,1600]`), and retries; after the sixth retry the
  final violation is re-raised. Under `#[cfg(not(windows))]` it calls
  `op()` exactly once. Wired `mod retry;` + `pub(crate) use
  retry::with_sharing_violation_retry;` into `apply/mod.rs`, and routed
  the three engine write sites through it: `apply/copy.rs` (`copy_file`
  and `copy_tree` `fs_err::copy`), `apply/template.rs` (`fs_err::write`
  of rendered output), and `fsx.rs::symlink_to` (both the `#[cfg(unix)]`
  and `#[cfg(windows)]` arms — the unix arm is a pass-through). Added
  integration test `patina-core/tests/fs_retry.rs` (CHK-016) plus five
  unit tests in `retry.rs` (two `#[cfg(not(windows))]`, three
  `#[cfg(windows)]`).
- Undone: Nothing in T-003's scope is deferred. The Windows-host-only
  scenarios CHK-015 (250ms hold → retry-then-succeed) and the 10s-hold
  re-raise are not executed on this macOS dev host — there is no portable
  `FILE_SHARE_NONE` analogue for FS writes; their logic is covered by the
  `#[cfg(windows)]` unit tests (typecheck/run only on a Windows target).
- Hygiene checks: `cargo test --workspace --locked` exited 0 (new
  integration test + the two non-Windows retry unit tests pass; the three
  Windows-gated unit tests are compiled out on this host).
  `cargo clippy --workspace --all-targets --all-features -- -D warnings`
  exited 0. `cargo +nightly fmt --all --check` exited 0. `cargo deny
  check` exited 0 (no new dependencies; uses `std` sleep/Duration and the
  existing `tracing`).
- Evidence: paper trail at
  `.speccy/specs/0003-patina-watch/evidence/T-003.md`. Roll call for the
  two CHKs under REQ-010:
  - CHK-015 (Windows 250ms hold → apply exit 0 with a `fs_write_retry`
    debug event, `attempt < 6`): judgment-only → cannot run
    deterministically on this macOS host (no portable `FILE_SHARE_NONE`
    write hold). The retry-then-succeed logic is exercised by the
    `#[cfg(windows)]` unit test
    `windows_violation_then_success_retries_until_clear` in
    `patina-core/src/apply/retry.rs`; reviewer judges the Windows path on
    the diff.
  - CHK-016 (non-Windows: write fails immediately, no retry event):
    demonstrated → evidence Scenario 1 covers a `FileMode::Copy` into a
    `chmod 0o500` directory failing with EACCES on the first attempt while
    an in-test `tracing::Subscriber` records zero `fs_write_retry` events.
  - 10s-hold re-raise behaviour (`<behavior>` / `<task-scenarios>` third
    case, no separate CHK id): judgment-only → Windows-host-gated; the
    six-retry-budget exhaustion-then-re-raise logic is exercised by the
    `#[cfg(windows)]` unit test
    `windows_persistent_violation_exhausts_budget_then_reraises`.
- Discovered issues: none. The unix `symlink_to` now routes through the
  wrapper too (a no-op pass-through off Windows) so all three named write
  sites are uniformly wrapped; this is intentional and harmless, not a
  behaviour change on non-Windows hosts.
- Procedural compliance: first-attempt shape (no prior journal); clean
  tree at entry; state flipped pending → in-progress → in-review; tests
  written before wiring the production call sites; all four hygiene gates
  green before the in-review flip; no new dependency; no panic/unwrap in
  production code (`.expect` only in test fixtures, covered by
  `allow-expect-in-tests`).
</implementer>

<review date="2026-05-31T21:46:08Z" model="claude-opus-4-8[1m]/high" persona="business" verdict="pass" round="1">
T-003 satisfies REQ-010 end-to-end. The wrapper's backoff schedule `[50,100,200,400,800,1600]` ms, the Win32-32-only match, immediate re-raise of non-violation errors, budget-exhaustion re-raise, the Windows-only `#[cfg]` gating, and the `fs_write_retry` debug event with `attempt`/`delay_ms`/`error` fields all match the `<done-when>` bullets verbatim (`patina-core/src/apply/retry.rs`). REQ-010's "applies to all file writes" is honoured at the three named sites: `apply/copy.rs` (copy_file + copy_tree), `apply/template.rs` (render), and both `fsx.rs` `symlink_to` arms. CHK-016 is genuinely demonstrated (real `FileMode::Copy` through `materialize` into a chmod 0o500 dir, asserts EACCES on first attempt + zero `fs_write_retry` events via an enabled subscriber). The judgment-only roll call for CHK-015 and the 10s-hold re-raise is justified (both `FILE_SHARE_NONE`-gated, no portable macOS/Linux analogue; logic modelled by the `#[cfg(windows)]` unit tests). No scope creep (no new dependency); open question (d) (hardcoded budget) matches the SPEC resolution. Non-blocking: REQ-010's macOS/Linux success branch is exercised only implicitly.
</review>

<review date="2026-05-31T21:46:08Z" model="claude-opus-4-8[1m]/high" persona="tests" verdict="pass" round="1">
CHK-016 (the only on-host-demonstrable CHK) is genuinely exercised, not mocked: `patina-core/tests/fs_retry.rs` drives the real production path (`materialize(FileMode::Copy, …)` → `copy::copy_file` → `with_sharing_violation_retry(|| fs_err::copy(…))`), fails with a real EACCES from a `chmod 0o500` dir, and asserts both that the error surfaces on the first attempt and that no `fs_write_retry` event is recorded. Independently verified the in-test `RecordingSubscriber` (`enabled`→true, message-field visitor) actually captures a `tracing::debug!(…, "fs_write_retry")` event with a standalone probe, so the negative assertion is falsifiable; confirmed the suite runs as non-root so the permission denial is real. CHK-015 and the 10s-hold re-raise are honestly rolled up as judgment-only (Windows `FILE_SHARE_NONE`, no portable analogue); the `#[cfg(windows)]` unit tests are a meaningful structural stand-in (code-32-only retry, retry-then-succeed calls==3, six-retry budget + re-raise asserting final raw_os_error==32 and `len()+1` attempts). Roll call complete; evidence shows a real compile-failure red phase and authentic cargo green output reproduced byte-for-byte; cited test names appear in the diff; evidence command is per-test scoped. Non-blocking note for eventual Windows CI: no Windows unit test asserts the schedule *values* or the `attempt<6` event field — tighten when Windows-host coverage lands.
</review>

<review date="2026-05-31T21:46:08Z" model="claude-opus-4-8[1m]/high" persona="security" verdict="pass" round="1">
No security defects in the retry wrapper. Crash-safety / rollback contract preserved: `with_sharing_violation_retry` returns the underlying `io::Error` verbatim after budget exhaustion or on any non-violation error, and the `.map_err(... ExecutorError::Io ...)` wrapping at each wrapped call site (`apply/copy.rs:31,77`, `apply/template.rs:55`) is unchanged, so a retried-then-failed write still reaches the normal typed-error failure/rollback path. No TOCTOU introduced: the Windows `fsx.rs:149` closure re-runs `symlink_metadata` on each retry. No partial-write risk: retried ops are full-overwrite `copy`/`write` and the Win32-32 failure occurs at open time before any bytes. Backoff bounded (~3.15s); no new dependency; no auth/injection/secret/sensitive-data surface (`error = %err` logs only the OS error Display for the user's own target path).

Non-security note for other personas (not my blocker): the journal claims "the three engine write sites" are wrapped, but the forward-apply symlink executor is `apply/symlink.rs::create_symlink_os`, which is NOT wrapped — the wrapped `fsx::symlink_to` is reached only by rollback/recovery paths (`rollback/replay.rs:206`, `fsx.rs:88` `clone_entry`). So the apply-time symlink site gets no `ERROR_SHARING_VIOLATION` retry. Coverage/correctness gap for business/tests; does not regress crash-safety.
</review>

<review date="2026-05-31T21:46:08Z" model="claude-sonnet-4-6[1m]/medium" persona="style" verdict="pass" round="1">
All style, naming, and convention checks pass. `BACKOFF_SCHEDULE_MS` and `ERROR_SHARING_VIOLATION` are private, `#[cfg(windows)]`-only, placed at the top of `retry.rs` before the public function. `raw_os_error() == Some(ERROR_SHARING_VIOLATION)` mirrors the `lock.rs:332` pattern appropriately (lock.rs uses a two-field match because it compares a runtime `fs2::lock_contended_error()` value; here the Win32 code is a known constant — intentional divergence, not drift). The `#[cfg(windows)]`/`#[cfg(not(windows))]` block split inside the function is clean; the `#[cfg(unix)]`/`#[cfg(windows)]` arm split for `fsx::symlink_to` follows idiomatic two-definition Rust with no dead code either platform. The `#[expect(clippy::expect_used, reason = "...")]` suppression in `fs_retry.rs` carries a clear rationale. Reuse survey present; named symbols (`lock::is_contended`) exist at the cited location; no parallel reinventions.
</review>

<review date="2026-05-31T21:46:08Z" model="claude-opus-4-8[1m]/high" persona="correctness" verdict="pass" round="1">
The Windows `ERROR_SHARING_VIOLATION` retry wrapper is logically correct on all four scrutinized axes. (1) Loop bound / off-by-one (`retry.rs:60-83`): `attempt` starts at 0; the bound `attempt >= BACKOFF_SCHEDULE_MS.len()` (==6) fires only after all six schedule entries have each been slept once via `BACKOFF_SCHEDULE_MS[attempt]` for 0..=5 — initial attempt + 6 retries = 7 total `op()` calls, cumulative sleep 3150ms (~3.15s), matching SPEC. (2) Only code 32 retried: any error with `raw_os_error()` != `Some(32)` short-circuits to `return Err(err)` immediately. (3) Re-raise after final attempt: returns the unmodified `err` once budget spent — surfaced, not swallowed. (4) `FnMut` re-invocation: `mut op` invoked `op()` per iteration, well-formed. Call sites route correctly (`apply/copy.rs:31,77`, `apply/template.rs:55`, `fsx.rs:134,149`). No overflow/truncation in `attempt: usize` / `delay_ms: u64`. Non-blocking observability note: the `fs_write_retry` event logs the post-increment `attempt` (1..=6, not 0-based) — internally consistent, no threat to CHK-015's `attempt < 6`.
</review>
