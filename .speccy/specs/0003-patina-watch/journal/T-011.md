---
spec: SPEC-0003
task: T-011
generated_at: 2026-06-01T03:16:20Z
---

<implementer date="2026-06-01T03:16:20Z" model="claude-opus-4-8[1m]" round="1">
- Reuse survey: Mapped the task area (covered REQ-001 / REQ-003, DEC-012,
  suggested files `patina-core/src/watch/service/mod.rs`,
  `service/launchd.rs`, `patina-cli/src/cli.rs`, `cmd/watch.rs`,
  `tests/watch_service_cli.rs`, plus the sibling `watch/` modules and the
  `lock` / `state_dir` / `paths` / `output::reporter` neighbours).
  Decisions:
  - reuse-as-is — `state_dir::HostOs` + `HostOs::current()` for the factory
    dispatch; `state_dir::resolve` (re-exported `resolve_state_dir`) for the
    state root; `lock::{acquire, LockKind, exclusive_timeout, SHARED_TIMEOUT}`
    with the established `state.join("lock")` path (engine.rs:191,
    status/mod.rs:108) for the advisory lock; the CLI `Reporter` trait,
    `ExitCode`, and `BufferReporter` for output and tests;
    `std::env::current_exe` + `Utf8PathBuf::from_path_buf` for the canonical
    binary path; the `logging` module's `watch.log` prefix / `logs/` layout
    convention for counter recovery.
  - extend — `cli::WatchArgs` / `Command::Watch` (added the
    `Option<WatchCommand>` subcommand enum and made `--json` `global = true`,
    keeping `--foreground` working); `cmd::watch::run` (added lifecycle
    dispatch alongside the existing foreground arm); `watch/mod.rs` module
    list and doc header (added `pub mod service`); reused the sibling
    `ignore_fmt(_: std::fmt::Result)` idiom from `journal::render` /
    `watch::drift_cache` rather than a bare `let _`.
  - write-fresh — `ServiceBackend` trait, `ServiceError`, `ServiceStatus`,
    `LifecycleResult`, the `current()` factory, `canonical_binary_path`, and
    `recover_log_counters` (+ `parse_counters` / `field_value` /
    `most_recent_log`) in `service/mod.rs`; the `LaunchdBackend` and its
    `render_plist` / `xml_escape` / `parse_launchctl_print` in `launchd.rs`;
    the `UnsupportedBackend` stub in `unsupported.rs`. Searched the crate
    (`rg 'trait ServiceBackend'`, `'launchctl'`, `'ServiceError'`) — all
    empty, so these are genuinely new. New top-level symbols, each accounted
    for above; the only `unsafe`/dependency temptation (`libc::getuid`) was
    rejected in favour of shelling `id -u` to avoid a new `patina-core`
    dependency and a `deny.toml` gate.
- Completed: Landed the per-OS service abstraction and the cross-platform
  `patina watch` lifecycle CLI with the macOS backend as the first concrete
  implementation. `service/mod.rs` defines the `ServiceBackend` trait
  (`install`/`uninstall`/`start`/`stop`/`restart`/`status`) returning a typed
  `ServiceError`, the `ServiceStatus` object, the `LifecycleResult` enum, the
  `current()` factory dispatching on `HostOs` (macOS → launchd; everything
  else → the unsupported stub pending T-012/T-013), `canonical_binary_path`
  (`current_exe` → canonicalize), and the DEC-012 log-counter recovery
  (`recover_log_counters` reads the most recent rotated `<state>/logs/watch.log*`
  and recovers `subscriptions_count` from the last `subscriptions=` field and
  `re_applies_since_start` by counting `re_apply` successes since the last
  `watch_started`). `launchd.rs` writes
  `~/Library/LaunchAgents/com.patina.watcher.plist` at mode `0644`
  (`RunAtLoad = true`, `KeepAlive`/`SuccessfulExit = false`, `ProgramArguments`
  = canonical binary + `watch --foreground`), bootstraps via
  `launchctl bootstrap gui/$(id -u)`, drives `start`/`stop` via `launchctl
  start`/`stop`, `uninstall` stops + `bootout` + removes the plist, and
  `status` parses `launchctl print` for liveness/last-exit. `unsupported.rs`
  returns the foreground-escape-hatch error (DEC-010) and a clean
  not-installed `status`. CLI: `WatchCommand` subcommand enum on `WatchArgs`;
  `cmd/watch.rs` resolves the state dir, acquires the exclusive lock for every
  mutating action and the shared lock (warn-and-proceed on timeout, mirroring
  `patina status`) for `status`, dispatches the backend, and renders `--json`
  envelopes (`result` for lifecycle; the six-field object for `status`, with
  absent counters as JSON `null`) or human output. A not-installed lifecycle
  action exits 1 with `service not installed; run \`patina watch install\`
  first` (REQ-003 behavior block); an already-installed `install` exits 1 with
  the typed error (REQ-001). 17 new `patina-core` unit tests + 4 `patina-cli`
  bin unit tests + 5 integration tests in `tests/watch_service_cli.rs`.
- Undone: The Linux `systemd --user` backend + non-systemd fallback (T-012)
  and the Windows Scheduled Task backend (T-013) are deliberately deferred to
  their own tasks per TASKS.md — on those hosts `current()` returns the
  `UnsupportedBackend` stub today. The genuinely live-supervisor happy paths
  (`launchctl bootstrap` loading a real LaunchAgent for CHK-001; `launchctl
  print` of a live service for CHK-006) are not driven end-to-end by a test
  because that escapes tempdir isolation and is unsafe in CI — their internals
  are unit-demonstrated instead (see Evidence).
- Hygiene checks: `cargo test --workspace --locked` (via `just test`) exited 0
  — 48 test binaries, all `test result: ok`, 0 failed, including the 17 new
  service unit tests, 4 new CLI bin tests, and 5 new integration tests.
  `cargo clippy --workspace --all-targets --all-features -- -D warnings`
  exited 0 (fixed pedantic `format_push_string`, `redundant_closure`,
  `unnecessary_wraps`, collapsible-`if`, doc-backtick, and
  `map_err`-wildcard findings during the gate). `cargo +nightly fmt --all
  --check` exited 0. `cargo deny check` exited 0 — advisories/bans/licenses/
  sources ok; no new dependency added (the macOS backend shells out to `id`
  and `launchctl` rather than pulling in `libc`).
- Evidence: red-then-green paper trail at
  `.speccy/specs/0003-patina-watch/evidence/T-011.md`. Roll call for the CHKs
  under REQ-001 / REQ-003:
  - CHK-001 (macOS install: plist at 0644, JSON `result = installed`,
    `launchctl print` reports the service): hygiene + judgment-only → the
    plist bytes and REQ-001 keys are demonstrated by evidence Scenario 2
    (`render_plist_*` unit tests under `cargo test`); the `0644` mode and the
    live `launchctl bootstrap` registration are NOT scriptable in CI (escapes
    tempdir isolation), so reviewer-business / reviewer-security judge the
    `set_mode_0644` + `launchctl bootstrap` wiring on the diff.
  - CHK-002 (Linux systemd install): judgment-only here → out of this task's
    backend scope (T-012); on Linux `current()` returns the stub today.
    Demonstrated when T-012 lands.
  - CHK-003 (Windows Scheduled Task install): judgment-only here → out of
    scope (T-013).
  - CHK-005 (Linux uninstall): judgment-only here → out of scope (T-012).
  - CHK-006 (macOS status installed-but-stopped: `installed = true`,
    `running = false`, `last_exit_code` = recorded or null): demonstrated →
    evidence Scenario 3 covers the `launchctl print` parse (stopped state +
    recorded exit code + the never-run null arm); the not-installed `status`
    object shape is demonstrated by evidence Scenario 1
    (`status_json_on_a_not_installed_service_reports_a_clean_object`). The
    live installed-but-stopped end-to-end query is the unsafe-in-CI part,
    covered by the parser unit test rather than a real `launchctl print`.
  - REQ-003 not-installed no-op + exclusive/shared lock split: demonstrated →
    evidence Scenario 1 (the five integration tests) plus the CLI bin unit
    tests (`render_lifecycle_not_installed_warns_and_exits_one`,
    `render_lifecycle_already_installed_error_exits_one`,
    `status_envelope_carries_the_six_fields_with_null_for_absent_counters`).
- Discovered issues: None new. The `cli.rs` `DoctorArgs` doc comments still
  reference a stale "(T-011)" for the doctor `--fix` exclusive lock — that is
  an unrelated pre-existing label in another command's docs (doctor, not
  watch) and is left untouched as out-of-scope.
- Procedural compliance: This implementer entry lands directly in
  `journal/T-011.md` per the journal-file schema; no `<implementer-note>` was
  written into TASKS.md (the parser rejects that element). The TASKS.md
  `state="..."` attribute for T-011 flips from `in-progress` to `in-review`
  as the final step of this turn. No shipped skill bodies under
  `.claude/skills/` were edited. The `model` attribute omits an effort suffix:
  this turn ran in the main Claude Code loop (the host states the resolved
  model ID `claude-opus-4-8[1m]`), not under a sub-agent definition file that
  would supply an `effort:` value.
</implementer>

<review persona="business" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-06-01T03:18:30Z" round="1">
T-011 (REQ-001 / REQ-003) faithfully delivers the macOS LaunchAgent backend, the cross-platform `ServiceBackend` abstraction, and the `install`/`uninstall`/`start`/`stop`/`restart`/`status` lifecycle CLI; Linux/Windows are correctly deferred to T-012/T-013 (factory returns the unsupported stub), with no `--linger`, daemon-binary, IPC, or metrics-subcommand scope creep.

The flagged judgment call is sound and does not contradict the SPEC. REQ-003's done-when "no-op" means "no spurious supervisor error / no mutation," not "exit 0"; the binding behavior block and the task scenario both pin exit 1 with the exact message `service not installed; run \`patina watch install\` first`. `patina-cli/src/cmd/watch.rs` emits that string byte-for-byte and returns exit 1 (`render_lifecycle` `NotInstalled` arm), while the `NotInstalled` path performs no supervisor call and no filesystem mutation. Verified by integration test `start_with_no_installed_service_exits_one_with_a_clear_message` and CLI unit `render_lifecycle_not_installed_warns_and_exits_one`. Already-installed `install` exits 1 via `ServiceError::AlreadyInstalled` (REQ-001), also tested.

Non-blocking observation: the launchd backend always reports `last_fired_at: None` even when running (`parse_launchctl_print` extracts no last-fired timestamp), a slight under-fill of REQ-003's "last-fired time" done-when prose. Not a contradiction — the binding scenario CHK-006 asserts only `installed`/`running`/`last_exit_code`, the field is present in the object (rendered null), and `launchctl print` has no clean standard last-fired field. No open question was silently resolved.
</review>

<review persona="tests" verdict="blocking" model="claude-opus-4-8[1m]" date="2026-06-01T03:18:30Z" round="1">
The unit/integration tests that ARE present genuinely gate behavior — confirmed by mutation that breaking the `launchctl print` exit-code parse, the re-apply success discriminator (`parse_counters_does_not_count_failed_reapplies`), the JSON envelope field set, the not-installed exit code, and the plist `ProgramArguments` each fail a test. The evidence roll call is well-formed, the cited test names all appear in the diff, the red/green output is structurally real and scoped per-slice, and no fabrication pattern matched. The `launchctl bootstrap` / live-`launchctl print` deferral is a legitimate judgment-only call (it escapes tempdir isolation).

But CHK-001 names a second, distinct Then-clause — "`~/Library/LaunchAgents/com.patina.watcher.plist` exists with mode `0644`" — that the evidence wrongly folds into the unsafe-in-CI bucket. The evidence claims the `0644` mode is "NOT scriptable in CI (escapes tempdir isolation)." That is factually wrong: `set_mode_0644` / `write_plist` (`patina-core/src/watch/service/launchd.rs`) operate on a path argument with pure filesystem work and require no `launchctl`, so a tempdir-scoped test is trivially feasible (`Permissions::from_mode(0o644)` round-trips to `0o644` on a temp file). No test anywhere asserts the written plist's mode. I mutated `from_mode(0o644)` → `from_mode(0o600)` and the entire T-011 suite (17 core unit + 5 integration + 4 CLI bin) still passes green — a silent permission regression on a named CHK-001 clause goes undetected.

Required to clear: add a deterministic test (a `#[cfg(test)]` test in `launchd.rs` calling `write_plist(tempdir_path, &binary)`, or a HOME-isolated install-path test) that asserts the resulting file's Unix mode is `0o644`, and re-label CHK-001's mode clause `demonstrated` rather than judgment-only. The `launchctl bootstrap` registration half may remain judgment-only.
</review>

<review persona="security" verdict="pass" model="claude-opus-4-8[1m]" date="2026-06-01T03:18:30Z" round="1">
No security blockers in the macOS launchd service backend (T-011).

Command construction is injection-safe: every supervisor call uses `Command::new("launchctl"|"id").args([...])` with argv arrays and no shell, so the `gui/$(id -u)` form in the doc comment is descriptive only — the uid is resolved in a separate process and passed as a single argv element, never interpolated into a shell (`patina-core/src/watch/service/launchd.rs`).

Plist injection is handled: `render_plist` only interpolates the hardcoded `SERVICE_LABEL` and the `ProgramArguments` tokens (self-derived `current_exe()`-canonicalized binary path + static `FOREGROUND_ARGS`), and every token passes through `xml_escape`, which covers all five XML metacharacters.

No privilege/elevation surface: per-user GUI-domain LaunchAgent only, plist written to the invoking user's own `~/Library/LaunchAgents` (path derived from `$HOME`, not from `canonicalize`), `0644` mode on a secret-free descriptor. The install TOCTOU between `is_installed()` and the plist write is closed by the exclusive advisory lock acquired in `run_lifecycle` before `backend.install()` (`patina-cli/src/cmd/watch.rs`). No secrets in errors/logs; no new dependency (deny check clean).

Non-blocking observation: `uid()` falls back to `UID` env then literal `"0"` when `id -u` fails. This only forms the `launchctl` domain-target string; a non-root caller cannot bootstrap into `gui/0`, so the worst case is a failed `Supervisor` error, not privilege escalation. Acceptable as-is.
</review>

<review persona="style" verdict="blocking" model="claude-sonnet-4-6[1m]" date="2026-06-01T03:18:30Z" round="1">
`patina-core/src/watch/service/mod.rs` re-declares `const LOGS_DIR: &str = "logs"` while acknowledging in its own comment that it "mirrors `crate::watch::logging`'s `LOGS_DIR`". The project's `rust-let-types-carry-invariants.md` rule is explicit: "if a private sibling already has it, promote it to `pub(super)` / `pub(crate)` and call it rather than duplicating." This is called out as a write-time responsibility that the implementer's reuse survey lists as a concern (`logging` module's `watch.log` prefix / `logs/` layout) but did not resolve.

Fix: in `patina-core/src/watch/logging.rs`, change `const LOGS_DIR: &str = "logs"` to `pub(super) const LOGS_DIR: &str = "logs"` (or `pub(crate)` if other non-sibling modules need it). Then in `patina-core/src/watch/service/mod.rs`, remove the local re-declaration and replace the two uses with the shared constant. The constant is currently private in `logging.rs` and newly duplicated in `service/mod.rs`; the fix is one visibility promotion and one substitution.
</review>

<review persona="correctness" verdict="pass" model="claude-opus-4-8[1m]" date="2026-06-01T03:18:30Z" round="1">
No correctness defects found at the >=80 confidence bar across the T-011 surfaces.

Scrutinized: the lifecycle state machine (`launchd.rs` install/start/stop/uninstall/restart/status guard on `is_installed()`; `restart` sequences `stop()? -> start()?`; install returns `AlreadyInstalled`); `launchctl print` parsing (independent prefix arms, absent fields degrade to `running=false`/`None`, `pid` arm gated on valid `u32`); lock acquisition (`watch.rs` shared lock warn-and-proceed for `status`, exclusive lock with error-propagated timeout for mutating actions; the unreachable `WatchCommand::Status` arm in the mutating match is a defensive no-op); the not-installed exit-1 path; and DEC-012 `recover_log_counters` (`service/mod.rs` — `state_dir/logs` join matches the producer in `logging.rs`; `watch.log*` filter matches `FILENAME_PREFIX`; lexical `.max()` over daily suffixes picks the newest; `parse_counters` resets re-apply count on each `watch_started`; the space-delimited `is_reapply_success` match correctly excludes `re_apply_failed`; `field_value` byte-indexing is panic-safe; all absent/non-numeric cases return `None`). Build note: two transient unit-test failures observed on first `cargo test` were stale-`target/` rebuild artifacts, not reproducible — the suite is deterministically green across three consecutive settled-build runs.
</review>

<review persona="architecture" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-06-01T03:18:30Z" round="1">
The `ServiceBackend` abstraction is shaped right for the two backends not yet written, honours DEC-010 and DEC-012, and introduces no layering violation or cycle.

Verified: the trait is not over-fitted to launchd (`install`/`uninstall`/`start`/`stop`/`restart`/`status` map cleanly onto systemd and the Windows Scheduled Task; all launchd-isms — `gui/<uid>` domain, plist rendering, `launchctl print` parsing — are private to `launchd.rs`; no `bootstrap`/`bootout`-shaped method leaked into the trait). Vocabulary generalizes (`LifecycleResult`, `ServiceStatus`'s six fields are the REQ-003 `--json` contract verbatim, `ServiceError::Supervisor(String)` is the right seam for systemd/Windows failures). Factory layering is sound: `current(state_dir)` threads `state_dir` to every backend and `recover_log_counters` / `canonical_binary_path` are shared free functions so T-012/T-013 reuse them; `unsupported.rs` is declared unconditionally, `launchd.rs` is `#[cfg(target_os = "macos")]` — the pattern T-012's `systemd.rs` and T-013's `scheduled_task.rs` will follow. CLI written once against the trait via `current_service_backend`, no per-OS branching; thiserror-in-core / anyhow-in-cli honoured.

Non-blocking note for T-012 (not a T-011 blocker): the factory dispatches on `HostOs::current()` with a `_ => unsupported` catch-all that collapses Linux and Windows. T-012 requires a runtime systemd-availability decision within Linux, for which this match has no seam — T-012 will need an explicit `HostOs::Linux` arm and a systemd probe. Localized, predictable edit; the trait, `state_dir` threading, and `recover_log_counters` all stay reusable. `ServiceError::Unsupported` already points at `patina watch --foreground`, so no new error variant is needed.
</review>

<blockers date="2026-06-01T03:18:30Z" round="1">
Two blocking findings to clear before the next review: a missing deterministic test on CHK-001's plist-mode clause, and a duplicated `LOGS_DIR` constant that the project reuse rule forbids.
- tests (blocking): CHK-001 names a deterministically demonstrable Then-clause — the written plist exists with mode `0644` — that the round-1 evidence wrongly bucketed as "NOT scriptable in CI." No test asserts the written plist's Unix mode; mutating `Permissions::from_mode(0o644)` → `from_mode(0o600)` leaves the entire T-011 suite green, so a permission regression on a named CHK clause goes undetected. Required: add a deterministic `#[cfg(test)]` test (in `patina-core/src/watch/service/launchd.rs`, calling `write_plist(tempdir_path, &binary)`, or a HOME-isolated install-path test) asserting the resulting file's Unix mode is `0o644`, and re-label CHK-001's mode clause `demonstrated` in the evidence rather than judgment-only. The `launchctl bootstrap` live-registration half may remain judgment-only.
- style (blocking): `patina-core/src/watch/service/mod.rs` re-declares `const LOGS_DIR: &str = "logs"` while its own comment acknowledges it "mirrors `crate::watch::logging`'s `LOGS_DIR`", violating the `rust-let-types-carry-invariants.md` reuse rule ("if a private sibling already has it, promote it to `pub(super)`/`pub(crate)` and call it rather than duplicating"). Required: promote `LOGS_DIR` in `patina-core/src/watch/logging.rs` to `pub(super)` (or `pub(crate)`), remove the local re-declaration in `service/mod.rs`, and reference the shared constant at both use sites.
</blockers>

<implementer date="2026-05-31T00:00:00Z" model="claude-opus-4-8[1m]" round="2">
- Reuse survey: Retry amending round-1 WIP in place; the two blockers are
  both non-reuse-shaped on the production surface (one new test, one
  visibility promotion), so no full area re-map. Per-symbol floor: the one
  new top-level symbol this round is the test fn
  `write_plist_sets_the_file_mode_to_0644` in `launchd.rs` — it reuses the
  existing `write_plist` / `set_mode_0644` write path and `tempfile::tempdir`
  (the established fixture idiom across `service/mod.rs` and `logging.rs`
  tests), asserting via `std::os::unix::fs::PermissionsExt::mode`; no new
  production symbol added. The style fix introduces no symbol — it promotes
  the existing `logging::LOGS_DIR` to `pub(super)` and deletes the duplicate.
- Completed: Addressed both round-1 blockers by amending the uncommitted WIP.
  (tests) Added `#[cfg(unix)] write_plist_sets_the_file_mode_to_0644` to
  `patina-core/src/watch/service/launchd.rs`: it calls `write_plist` against a
  tempdir path and asserts the resulting file's permission bits (`mode & 0o777`)
  are exactly `0o644` — pure filesystem work, no `launchctl`. Verified the
  reviewer's mutation: `Permissions::from_mode(0o644)` → `from_mode(0o600)`
  flips this test red while leaving the rest green; reverted. Re-labelled
  CHK-001's mode clause `demonstrated` in
  `.speccy/specs/0003-patina-watch/evidence/T-011.md` (scope note + Scenario 2
  now cite the new test and the mutation check; only the live `launchctl
  bootstrap` registration remains judgment-only). (style) Promoted
  `const LOGS_DIR` in `patina-core/src/watch/logging.rs` from private to
  `pub(super)`, removed the duplicate `const LOGS_DIR` in
  `patina-core/src/watch/service/mod.rs`, and replaced it with
  `use crate::watch::logging::LOGS_DIR` — the `most_recent_log` use site and
  the `recover_log_counters_reads_the_most_recent_rotated_log` test now read
  the single shared definition.
- Undone: Nothing newly deferred this round. The round-1 deferrals stand
  unchanged: the Linux systemd backend (T-012) and Windows Scheduled Task
  backend (T-013) remain their own tasks, and the live `launchctl bootstrap`
  / live `launchctl print` happy paths remain judgment-only (unsafe in CI).
- Hygiene checks: `just lint` (nightly `fmt --all --check`, `clippy
  --workspace --all-targets --all-features -- -D warnings`, `cargo doc
  --workspace --no-deps --all-features -D warnings`, `cargo deny check`)
  exited 0 — advisories/bans/licenses/sources ok (the `wit-bindgen`
  duplicate-crate entry is a pre-existing transitive-graph warning, not a
  ban failure). `just test` (`cargo test --workspace --locked`) exited 0 —
  48 test binaries, all `test result: ok`, 0 failed, including the new mode
  test. Mapping to the four standard gates: fmt = 0, clippy = 0, doc = 0,
  deny = 0, test = 0. Note: a transient `0o600`-mode failure on the first
  full `just test` was a stale-`target/` artifact left by the in-place
  mutation experiment; `cargo clean -p patina-core` + re-run confirmed the
  real `0o644` source is green (the same stale-target effect the round-1
  correctness reviewer recorded).
- Evidence: paper trail at
  `.speccy/specs/0003-patina-watch/evidence/T-011.md`. Roll call for the CHKs
  under REQ-001 / REQ-003 (delta from round 1 in CHK-001):
  - CHK-001 (macOS install: plist at 0644, JSON `result = installed`,
    `launchctl print` reports the service): now demonstrated (mode) + hygiene
    + judgment-only (live registration). The `0644` mode clause is
    demonstrated → `write_plist_sets_the_file_mode_to_0644` writes the plist
    to a tempdir and asserts `mode & 0o777 == 0o644`, mutation-verified. The
    plist bytes / REQ-001 keys remain demonstrated by Scenario 2's
    `render_plist_*` tests. Only the live `launchctl bootstrap` registration
    stays judgment-only (escapes tempdir isolation), judged on the diff.
  - CHK-002 (Linux systemd install): judgment-only here → out of scope (T-012).
  - CHK-003 (Windows Scheduled Task install): judgment-only here → out of
    scope (T-013).
  - CHK-005 (Linux uninstall): judgment-only here → out of scope (T-012).
  - CHK-006 (macOS status installed-but-stopped): demonstrated → Scenario 3's
    `parse_launchctl_print_*` tests cover the stopped state + recorded/null
    exit code; the not-installed object shape by Scenario 1. The live
    installed-but-stopped query stays judgment-only (unsafe in CI).
  - REQ-003 not-installed no-op + exclusive/shared lock split: demonstrated →
    Scenario 1's five integration tests plus the CLI bin unit tests.
- Discovered issues: None new. The unrelated stale `(T-011)` label in the
  doctor `--fix` docs noted in round 1 is still out of scope and untouched.
- Procedural compliance: Retry-mode amend — iterated on the dirty working-tree
  WIP in place (no `git restore`/`clean`/`checkout`, no rewrite-from-scratch).
  This round-2 block is appended after the round-1 `<implementer>` and
  `<blockers>` without modifying either, per the append-only journal rule.
  TASKS.md `state` for T-011 flips `in-progress` → `in-review` as the final
  step; no `<implementer-note>` was inlined into TASKS.md. No shipped
  `.claude/skills/` bodies were edited. The `model` attribute omits an effort
  suffix: this turn ran in the main Claude Code loop (host-stated resolved
  model ID `claude-opus-4-8[1m]`), not under a sub-agent definition file that
  would supply an `effort:` value.
</implementer>

<review persona="tests" verdict="pass" model="claude-opus-4-8[1m]" date="2026-06-01T03:28:42Z" round="2">
Round-1 tests blocker resolved. `write_plist_sets_the_file_mode_to_0644` (`patina-core/src/watch/service/launchd.rs`) drives the real production `write_plist`/`set_mode_0644` path against a tempdir and asserts `mode & 0o777 == 0o644`; I re-ran the reviewer's `from_mode(0o644)`→`from_mode(0o600)` mutation and the test went red (`got 600`), reverting to green — CHK-001's plist-mode clause is now genuinely gated. Evidence re-label is accurate: Scenario 2 cites the new test and the mutation check, and the round-2 roll call marks CHK-001's mode clause `demonstrated` with only the live `launchctl bootstrap` registration left judgment-only. All five covered CHKs (001/002/003/005/006) appear with valid labels, every evidence-cited test name exists in the source, and no fabrication pattern matched.
</review>

<review persona="style" verdict="pass" model="claude-sonnet-4-6[1m]" date="2026-06-01T03:28:42Z" round="2">
Round-1 blocker fully resolved. `patina-core/src/watch/logging.rs` now reads `pub(super) const LOGS_DIR: &str = "logs"` with an explanatory doc comment. `patina-core/src/watch/service/mod.rs` has no remaining `const LOGS_DIR` re-declaration; both former use sites now consume the single shared definition via `use crate::watch::logging::LOGS_DIR`. `grep -rn 'const LOGS_DIR' watch/` returns exactly one hit. `cargo +nightly fmt --check` and `cargo clippy -D warnings` both exit 0 on the current tree. The `use` item is placed mid-file rather than with the top-of-module imports, a minor idiom gap that `rustfmt` does not flag and no project linter enforces — not a blocker. No new dead imports, naming drift, or suppression annotations introduced by the round-2 delta.
</review>
