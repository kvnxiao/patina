---
spec: SPEC-0003
task: T-004
generated_at: 2026-05-31T00:00:00Z
---

<implementer date="2026-05-31T00:00:00Z" model="claude-opus-4-8[1m]/high" round="1">
- Reuse survey: Mapped the task area (covered REQ-007 format slice;
  suggested files `patina-core/src/watch/mod.rs`,
  `patina-core/src/watch/drift_cache.rs`, `patina-core/src/lib.rs`,
  `patina-core/tests/drift_cache.rs`; neighbours
  `patina-core/src/version_envelope.rs`,
  `patina-core/src/journal/{record.rs,render.rs,mod.rs,plan.rs}`).
  Decisions:
  - Reuse-as-is: `version_envelope::{encode_with_envelope,
    decode_envelope, EnvelopeError, read_envelope_version}` (the T-001
    helper) for the envelope codec — `DriftCache::{encode,decode}`
    call them directly, exactly mirroring `ApplyRecord::{encode,decode}`.
    `postcard::{to_stdvec, from_bytes}` for the body codec. The
    `From<EnvelopeError>` mapping onto a format-local error enum is
    reused as a pattern from `journal/mod.rs`'s
    `From<EnvelopeError> for JournalError`. `journal::timestamp_to_rfc3339`
    is reused (re-exported as `journal_ts_rfc3339`) rather than
    re-spelling the compact-UTC reformat. `journal::content_hash` is
    reused in tests to build comparable 32-byte blake3 digests.
  - Extend: none. The drift cache is a new format with its own major,
    so nothing existing grows.
  - Write-fresh: `DRIFT_CACHE_MAJOR_VERSION`, `DriftEntry`,
    `DriftCache`, `DriftCacheError`, `write_drift_cache`,
    `load_drift_cache_file`, `render_drift_cache`, and the private
    `sibling_temp_path` / `hex_encode` / `render_detected_at` helpers.
    Searched the crate for a reusable atomic-write helper
    (`rg 'fn .*atomic|persist|with_extension|rename' patina-core/src`)
    and for an existing drift-cache surface
    (`rg 'DriftCache|DRIFT_CACHE'`) — both empty. The journal uses a
    bare `fs_err::write`; the SPEC's `<done-when>` mandates
    tempfile-then-rename for the drift cache specifically, so the small
    `sibling_temp_path` + `fs_err::rename` helper is local to
    `drift_cache.rs`. No reusable hex helper exists in-crate (the
    journal renders no hashes), so `hex_encode` is local.
- Completed: Created `patina-core/src/watch/mod.rs` (module root,
  `pub mod drift_cache;`) and `patina-core/src/watch/drift_cache.rs`.
  Landed `pub const DRIFT_CACHE_MAJOR_VERSION: u16 = 1` (independent of
  the journal's `FILE_MAJOR_VERSION = 2`), `DriftEntry { target:
  Utf8PathBuf, expected_hash: [u8;32], actual_hash: [u8;32],
  detected_at_unix: i64 }`, and `DriftCache { journal_ts: String,
  entries: Vec<DriftEntry> }`. `DriftCache::{encode,decode}` go over the
  T-001 `version_envelope` helper using the drift cache's own major;
  `DriftCacheError` (thiserror, `#[non_exhaustive]`) maps the envelope
  arms via `From<EnvelopeError>`, its `VersionMismatch` naming both
  `found` and `supported`. `write_drift_cache` encodes, writes a sibling
  `<path>.tmp`, then `fs_err::rename`s it over the destination (atomic
  replace). `load_drift_cache_file` reads + decodes (parallel to
  `journal::load_plan_file`); `render_drift_cache` formats the version,
  bound journal timestamp, and one block per entry with both hex hashes
  and an RFC-3339 detection time (parallel to `journal::render_plan`).
  Re-exported `DRIFT_CACHE_MAJOR_VERSION`, `DriftCache`,
  `DriftCacheError`, `DriftEntry`, `load_drift_cache_file`,
  `render_drift_cache` from `lib.rs` and wired `pub mod watch;`. Added
  `patina-core/tests/drift_cache.rs` (4 integration tests) plus 10
  in-module unit tests.
- Undone: No watcher, no notifications, no drift detection, and no
  `patina debug drift-cache` subcommand — all deliberately out of
  T-004's scope per its `<task>` element. They land in T-005 (the debug
  subcommand consuming `load_drift_cache_file` + `render_drift_cache`),
  T-008 (the event loop), and T-010 (drift detection + cache upsert).
  `write_drift_cache`, `DriftEntry`, and `journal_ts_rfc3339` are
  exposed for those callers but are exercised here only by tests; that
  is expected for a format-only task.
- Hygiene checks: `just check` exited 0 (the project's standard gate,
  preferred over ad-hoc cargo so the docs gate runs). Component exit
  codes: `cargo +nightly fmt --all --check` exit 0;
  `cargo clippy --workspace --all-targets --all-features -- -D warnings`
  exit 0; `RUSTDOCFLAGS="-D warnings" cargo doc --workspace --no-deps
  --all-features` exit 0 (this gate caught and I fixed a
  `redundant_explicit_links` warning on the module-doc
  `version_envelope` link); `cargo deny check` exit 0 (no new
  dependency added — the task reuses `blake3`, `postcard`, `jiff`,
  `fs-err`, `camino`, all already in the graph);
  `cargo test --workspace --locked` exit 0.
- Evidence: paper trail at
  `.speccy/specs/0003-patina-watch/evidence/T-004.md`. The task's
  behaviour is proved against the four `<task-scenarios>` in TASKS.md
  (the format-level contract). Roll call for the CHKs under REQ-007:
  - CHK-011 (drift detected → cache entry + exactly one notification):
    judgment-only for T-004 → out of scope; drift detection and the
    notification sink land in T-010, which writes the cache entries
    this task's format defines. T-004 builds no watcher to demonstrate
    it.
  - CHK-012 (`patina status --json` reports DRIFTED from the live
    re-hash, NOT the cache): judgment-only for T-004 → out of scope;
    status already derives DRIFTED independently (SPEC-0001 REQ-018)
    and T-004 adds no status coupling. The "cache is not read by
    status" property holds vacuously here because nothing reads the
    cache yet; T-010 carries the demonstrable scenario.
  - CHK-018 (`patina debug drift-cache <path>` prints version,
    timestamp, path, both hashes; exit 0): judgment-only for T-004 →
    out of scope; the subcommand lands in T-005 over this task's
    `load_drift_cache_file` + `render_drift_cache`. The render surface
    it will call is unit-tested here
    (`render_names_version_journal_ts_target_and_both_hashes`) so the
    substrings CHK-018 asserts are already produced.
  All three covered CHKs are runtime/CLI scenarios deferred to later
  tasks by T-004's own scope statement; the format proofs (task
  scenarios 1-4) are `demonstrated` in the evidence file.
- Discovered issues: None. The journal's plain `fs_err::write` (no
  tempfile-rename) is intentional for the journal — crash recovery
  keys on sentinel existence, not atomic body replacement — and is not
  drift introduced by this task; noted only to explain why the atomic
  helper is local to the drift cache rather than hoisted.
- Procedural compliance: This entry lands directly in
  `journal/T-004.md` per the journal-file schema; no
  `<implementer-note>` was written into TASKS.md (the parser rejects
  that element). The TASKS.md `state="..."` attribute for T-004 flips
  from `in-progress` to `in-review` as the final step of this turn. No
  shipped skill bodies under `.claude/skills/` required edits.
</implementer>

<review date="2026-05-31T21:58:20Z" model="claude-opus-4-8[1m]/high" persona="business" verdict="pass" round="1">
T-004 delivers exactly REQ-007's drift-cache format slice and nothing more. The file shape matches every format-relevant `<done-when>` bullet: postcard body with a `u16` major-version envelope at offset 0, an independent `DRIFT_CACHE_MAJOR_VERSION = 1` (journal's `FILE_MAJOR_VERSION = 2`, with a test pinning they differ and that decode validates against the drift cache's own major), reuse of the shared `version_envelope` helper, the exact per-entry schema, a top-level record carrying envelope + bound `journal_ts` + entries, atomic tempfile-then-rename write, and a typed `VersionMismatch { found, supported }` refusal. All four format `<task-scenarios>` demonstrated; 10 unit + 4 integration tests pass. Deferral of the three runtime CHKs (011/012/018) is honest and verifiable — each requires the watcher + `notify-rust` + live status re-hash or the `patina debug drift-cache` subcommand (owned by T-005/T-008/T-010). No scope creep, and the REQ-007 invariant "drift cache is never read by `patina status`" is upheld. Open question (c) was human-resolved 2026-05-26 and this task implements that pinned decision.
</review>

<review date="2026-05-31T21:58:20Z" model="claude-opus-4-8[1m]/high" persona="tests" verdict="pass" round="1">
T-004 drift-cache tests genuinely exercise the format slice. Round-trip (`write_then_load_round_trips_through_the_public_api`) drives real `write_drift_cache`→`load_drift_cache_file` through the envelope codec and asserts `loaded == cache` plus both `content_hash`-derived 32-byte hashes — no mock, breaks under a stubbed encode/decode. Newer-major refusal (`newer_major_load_is_refused_naming_both_versions`) bumps the leading u16 to `DRIFT_CACHE_MAJOR_VERSION + 1` and asserts `VersionMismatch { found, supported }` naming both, referencing the drift major never the journal's; `decode_uses_its_own_major_not_the_journals` and `drift_cache_major_is_independent_of_the_journal_major` independently pin the encoded envelope carries `DRIFT_CACHE_MAJOR_VERSION` and that it differs from `FILE_MAJOR_VERSION`. Atomic-write test exercises the tempfile-then-rename path with the sibling-path helper unit-pinned. Evidence follows canonical red(compile-failure)→green shape with scoped per-test capture; named tests exist in the diff and pass. CHK roll call covers all three REQ-007 CHKs as judgment-only, plausibly so. Non-blocking: the atomic-write assertions are enforced structurally (only caller of `sibling_temp_path` is `write_drift_cache`) rather than observed mid-write, acceptable for this format-only slice.
</review>

<review date="2026-05-31T21:58:20Z" model="claude-opus-4-8[1m]/high" persona="security" verdict="pass" round="1">
T-004 drift-cache format slice is security-clean for its contracted scope. Untrusted-bytes decode is panic-free: `DriftCache::decode` runs `decode_envelope` first (bounds-checked via `split_first_chunk`, no indexing, no unwrap/expect), refusing a newer major with a typed `VersionMismatch` before the body decoder runs, then `postcard::from_bytes` mapped to `DriftCacheError::Decode`; fixed `[u8;32]` hash fields + postcard length-prefix validation avoid the giant-prealloc class. `render_detected_at` falls back via `map_or_else` instead of unwrapping. Atomic write is correct: `sibling_temp_path` appends `.tmp` via `set_file_name`, keeping the staging file in the target's own directory so `fs_err::rename` is a same-filesystem atomic replace. No fsync is performed, but that is in-scope-correct: REQ-007's `<done-when>` requires only reader-isolation via tempfile+rename, not crash durability (the cache is the watcher's regenerable notification ledger). State-dir-on-cloud-sync is not relevant (path is caller-supplied; placement belongs to SPEC-0002). No sensitive-data exposure (debug render emits paths + hex hashes only); no new dependency.
</review>

<review date="2026-05-31T21:58:20Z" model="claude-sonnet-4-6[1m]/medium" persona="style" verdict="pass" round="1">
All key style criteria pass. `version_envelope::{encode_with_envelope, decode_envelope}` from T-001 are called directly in `DriftCache::encode`/`decode` — no hand-rolled copy. `DRIFT_CACHE_MAJOR_VERSION = 1` is distinct from the journal's `FILE_MAJOR_VERSION = 2`; it is `pub const` (matching the journal's own `pub const FILE_MAJOR_VERSION`) and re-exported from `lib.rs`. `watch/mod.rs` + `watch/drift_cache.rs` layout and `pub mod watch;` wiring follow the existing module convention. `DriftCacheError` uses `thiserror` with `#[non_exhaustive]`; the `From<EnvelopeError>` mapping mirrors `journal/mod.rs`. Paths use `camino::Utf8Path`/`Utf8PathBuf`; I/O via `fs_err::{write,rename,read}`. All public items carry doc comments; `# Errors` sections present; module-level doc carries a `no_run` example. `ignore_fmt`/`hex_encode` are private helpers. No `unwrap()`/`expect()`/`println!`/bare lint suppressions introduced.
</review>

<review date="2026-05-31T21:58:20Z" model="claude-opus-4-8[1m]/high" persona="correctness" verdict="pass" round="1">
No correctness defects found in the T-004 drift-cache slice. The encode/decode round-trip is symmetric over `version_envelope` (2-byte LE u16 prefix written by `encode_with_envelope`, stripped by `split_first_chunk::<2>` before `postcard::from_bytes`); the `DRIFT_CACHE_MAJOR_VERSION` refusal path correctly rejects only `found > supported` and accepts equal/older, with the `From<EnvelopeError>` arms preserving `found`/`supported`; the atomic write encodes, writes a same-directory sibling `.tmp`, then `fs_err::rename`s over the destination with `?`-propagated errors in correct order (`std::fs::rename` replaces existing on both POSIX and Windows `MOVEFILE_REPLACE_EXISTING`); and `render_drift_cache` is deterministic (Vec-order iteration, fixed lowercase hex, deterministic RFC-3339, no now()/PID/random/map iteration). All 14 tests pass. The `sibling_temp_path` no-filename fallback is unreachable for real cache paths and is panic-free.
</review>
