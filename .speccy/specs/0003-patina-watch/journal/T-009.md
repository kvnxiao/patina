---
spec: SPEC-0003
task: T-009
generated_at: 2026-05-31T23:55:00Z
---

<implementer date="2026-05-31T23:55:00Z" model="claude-opus-4-8[1m]/high" round="1">
- Reuse survey: Mapped the task area (covered REQ-006 / REQ-008, the
  suggested files `watch/reapply.rs`, `watch/mod.rs`,
  `watch_foreground_cli.rs`, plus the engine apply path and the journal
  module they lean on). Decisions:
  - reuse-as-is — the re-apply is pure orchestration over existing
    crate-root exports: `plan_apply` + `execute_plan` with
    `LockPolicy::NonBlocking`, `current_timestamp` (the T-002 hoisted
    helper), `ApplyRequest::default()`, `read_latest_commit`, the T-007
    `subscriptions::compute_subscriptions`, and the
    `EngineError::Lock(LockError::Contended { .. })` match for the skip
    arm. No new wrappers around any of these.
  - extend — `watch/mod.rs`'s T-008 select-loop (added batch
    classification + dispatch and made the debouncer/watch-set
    re-armable on rescan); `subscriptions.rs` (added `WatchSet` +
    `compute_watch_set` rather than a parallel module, reusing
    `compute_subscriptions` verbatim for the `watched` list).
  - write-fresh — `watch/reapply.rs` (`run_reapply`, `ReapplyOutcome`,
    `result_label`); searched the crate with `rg 'fn .*reapply|re_apply'`
    and found nothing. New top-level symbols: `reapply::run_reapply`,
    `reapply::ReapplyOutcome`, `subscriptions::WatchSet`,
    `subscriptions::compute_watch_set`, and the private `mod.rs`
    classifiers `journal_event` / `is_journal_sentinel` / `source_event`
    / `rescan`. The journal-sentinel suffix matching reuses the existing
    `crate::journal::{PLAN_SUFFIX, COMMIT_SUFFIX, PROGRESS_SUFFIX,
    ROLLED_BACK_SUFFIX}` consts rather than re-spelling the literals.
- Completed: Landed the watcher re-apply handler end-to-end (REQ-006 /
  REQ-008 / DEC-007). `watch/reapply.rs::run_reapply` plans + executes an
  apply under `LockPolicy::NonBlocking`, keying the journal `<ts>` with
  the hoisted `current_timestamp`; on success it emits an info `re_apply`
  event with `re_apply_id` / `re_apply_duration_ms` /
  `re_apply_files_changed` / `re_apply_result` (REQ-009 metric fields,
  flattened to underscore field names because tracing's dotted-field
  macro grammar is ambiguous with value expressions in this version —
  the existing `retry.rs` / `subscriptions.rs` events use the same flat
  convention). On `EngineError::Lock(LockError::Contended { .. })` it
  emits a debug `lock_contention_skip` (`skip_reason="lock_held"`) and
  skips, mutating nothing (the engine resolved the lock before any write
  or recovery, per SPEC-0001 REQ-030); on any other error it logs
  `re_apply_failed` and survives. The `watch/mod.rs` select-loop now
  classifies each debounced batch: a journal-sentinel event re-reads the
  latest commit and re-arms the debouncer over a recomputed `WatchSet`
  (logging `journal_rescan` naming the new `.COMMIT`) and does NOT
  re-apply; a repository source-path event drives `run_reapply`; a
  content-target-only event is a no-op left for the drift handler
  (T-010). Routing re-apply on *source* paths only — via the new
  `WatchSet.sources` partition — is the loop guard: a re-apply's own
  content-target rewrite and journal write never re-trigger a re-apply,
  so a self-triggered apply against unchanged source does not loop
  (CHK-017 / REQ-005). The classifier matches journal sentinels on the
  path leaf (parent dir named `journal` + sentinel suffix) rather than a
  `starts_with` against the resolved journal dir, because `notify`
  reports canonicalized paths (macOS `/tmp` → `/private/tmp`) that never
  share the un-canonicalized state-dir prefix.
- Undone: T-010 (drift detection on content targets — the no-op branch
  in the loop becomes the drift dispatch) and the per-OS service install
  (T-011+) are left for their own turns per TASKS.md. Nothing from
  T-009's scope is deferred. The full concurrent-CLI lock race of
  CHK-013 (watcher wins, Blocking CLI waits) is verified at the engine
  level by SPEC-0001's `non_blocking_contention_*` unit tests rather than
  re-raced from the integration harness (a sub-millisecond race on a
  small repo is not deterministically reproducible from a subprocess);
  the watcher-side observable outcome (one new COMMIT, zero skips) is
  covered by Scenario 2.
- Hygiene checks: `cargo test --workspace --locked` (via `just test`)
  exited 0 — all test binaries `test result: ok`, including the three
  new integration tests and the five new `patina-core` unit tests.
  `cargo clippy --workspace --all-targets --all-features -- -D warnings`
  exited 0. `cargo +nightly fmt --all --check` exited 0. `cargo deny
  check` exited 0 (no new dependencies — `run_reapply` is pure
  orchestration over existing crate APIs).
- Evidence: red-then-green paper trail at
  `.speccy/specs/0003-patina-watch/evidence/T-009.md`. Roll call for the
  CHKs under REQ-006 / REQ-008:
  - CHK-010 (five touches within the window coalesce to exactly one
    re_apply): demonstrated → evidence Scenario 1 shows the T-008
    no-re-apply baseline vs the single coalesced `re_apply` event.
  - CHK-013 (concurrent CLI apply: zero `lock_contention_skip` for the
    CLI's apply, two committed COMMITs): demonstrated → evidence
    Scenario 2 covers the watcher-side observable (one new COMMIT, zero
    skips on the uncontended re-apply); the concurrent lock race itself
    is judgment-backed by the cited engine `non_blocking_contention_*`
    unit tests.
- Discovered issues: the copy-mode executor (`apply/copy.rs::copy_file`)
  rewrites the target unconditionally — it is not byte-identical-idempotent
  — so the SPEC's "re-applying unchanged source is a no-op" holds only at
  the *journal-record* level, not at the FS-event level. The watcher
  therefore cannot rely on copy idempotency to break the loop; the
  source-vs-target classification is what makes the loop terminate. This
  is a property of the existing engine, not a regression introduced here,
  and the watcher works correctly around it. Flagged for the reviewer's
  awareness; no change made outside T-009 scope.
- Procedural compliance: flipped T-009 `pending → in-progress` before
  implementing and `in-progress → in-review` after the hygiene gate
  passed; did not mark it `completed` (the review personas decide).
  Followed the first-attempt branch (no prior journal, clean tree at
  entry). Wrote tests first where practical and ran the project's own
  `just lint` / `just test` rather than ad-hoc cargo invocations. No
  locked-SPEC edit, no `TASKS.md` hand-edit beyond the two state
  transitions, no new dependency.
</implementer>

<review persona="business" verdict="pass" model="claude-opus-4-8[1m]/high" date="2026-05-31T23:54:06Z" round="1">
T-009 satisfies its REQ-006 / REQ-008 done-when slice and serves the named users (active-editor auto-reapply; CLI-priority lock coordination with no IPC, no recursive watch, no separate daemon — non-goals respected).

Both implementer-flagged findings are honestly disclosed and do NOT compromise SPEC intent:

(1) Copy-mode non-idempotency: confirmed at `patina-core/src/apply/copy.rs:31` (`fs_err::copy` rewrites the target unconditionally), so the north-star "re-apply unchanged source is a no-op" holds at the plan/journal level but not the FS-event level. The watcher does not depend on copy idempotency for loop termination — it depends on the source-vs-target classification: `run_reapply` fires only on `source_event` (paths in `WatchSet.sources`), and a content-target's own path is in `watched` but never in `sources`. A re-apply's own target rewrite and journal write therefore cannot re-trigger an apply; the journal write only drives a rescan (`rescan()` recomputes the set, never calls `run_reapply`). Loop termination is sound.

(2) CHK-013 concurrent race verified at component level: the watcher-side observable (one new COMMIT, zero `lock_contention_skip`) is covered by `a_watcher_reapply_commits_exactly_one_new_journal_record`; the contention race itself is backed by the engine's `non_blocking_apply_on_contended_lock_errors_and_writes_no_journal` / `non_blocking_contention_leaves_pending_orphan_untouched`. The CLI's apply uses the default `Blocking` policy, so the "CLI blocks behind the watcher, both commit" half holds structurally. A sub-millisecond subprocess re-race would be flaky, which the test-hygiene rules forbid; the decomposition is the right call.

Notes (non-blocking): the `re_apply` event uses flat field names (`re_apply_id`, etc.) rather than REQ-009's dotted `re_apply.id`. This is a tracing-grammar detail with no user-facing/JSON/CLI surface, the fields remain distinct/countable/joinable, and REQ-009 field fidelity is T-006's coverage. REQ-008's stale-lock-detection done-when is not implemented in T-009, but it is not named in T-009's task body or any of its four task-scenarios and is out of this task's decomposed scope.
</review>

<review persona="tests" verdict="pass" model="claude-opus-4-8[1m]/high" date="2026-05-31T23:54:06Z" round="1">
T-009 tests genuinely exercise the covered scenarios. CHK-010's `five_touches_within_the_debounce_window_coalesce_to_one_reapply` (`patina-cli/tests/watch_foreground_cli.rs:326`) spawns a real watcher subprocess, performs 5 real source writes, and counts actual `re_apply` event lines == 1 — catching both the absent-handler regression (0) and the loop-storm regression (>1, shown in the evidence red phase). CHK-017's rescan test drives a real `patina apply --yes` subprocess and asserts the real `journal_rescan` event (emitted only inside `mod.rs::rescan`, which runs the real `read_latest_commit`+`compute_watch_set`+`debounce::spawn`) plus a bounded-rescan guard. The lock-contention-skip path is tested at a meaningful level via the engine `non_blocking_apply_on_contended_lock_errors_and_writes_no_journal` / `non_blocking_contention_leaves_pending_orphan_untouched` tests (real held lock, real `execute(NonBlocking)`, typed `LockError::Contended` + zero mutation), plus the loop-guard unit test `watch_set_separates_sources_from_content_targets`. On CHK-013: the implementer's decision to verify the full concurrent-CLI mid-re-apply race at the engine level rather than re-racing it from the harness is adequate, not a gap — a `lock_contention_skip` only ever arises from watcher-side contention, which the engine tests fully characterize; re-racing the sub-millisecond interleaving from a subprocess would be flaky-by-construction. No flakiness observed across 3 repeated runs; the same-second timestamp collision is explicitly mitigated (1100ms wait at `watch_foreground_cli.rs:446`). Evidence roll call covers both CHKs under REQ-006/REQ-008, all cited test names exist in the diff, red/green outputs differ materially, and commands are per-test scoped. No fabrication patterns matched.
</review>

<review persona="security" verdict="pass" model="claude-opus-4-8[1m]/high" date="2026-05-31T23:54:06Z" round="1">
T-009 watcher re-apply is security-clean. The NonBlocking lock boundary is correct: the watcher never pre-acquires the lock, the engine acquires-then-recovers (try_acquire before recover_orphans and any write at `patina-core/src/apply/engine.rs:355-361`), and a held CLI lock yields `LockError::Contended` with zero mutation, mapped to `ReapplyOutcome::Skipped` — a concurrent CLI apply is never clobbered. Log events `re_apply` / `lock_contention_skip` / `re_apply_failed` (`patina-core/src/watch/reapply.rs:94-129`) expose only a timestamp id, duration, op count, static result label, and a transparent `EngineError` Display; no file contents or hashes, and at most filesystem paths that `patina apply` already prints, written to the machine-local `<state>/patina/logs/` sink. Re-apply is a verbatim reuse of `plan_apply` + `execute_plan`, so it introduces no new canonicalization, symlink-traversal, or write site; the new classifiers in `watch/mod.rs` do no filesystem I/O and make no authorization decision, so there is no added TOCTOU surface beyond the engine's already-reviewed lock sequence.
</review>

<review persona="style" verdict="blocking" model="claude-sonnet-4-6[1m]/medium" date="2026-05-31T23:54:06Z" round="1">
`patina-core/src/watch/mod.rs:320` — `is_journal_sentinel` takes `path: &Utf8PathBuf` where the project's established convention (and the explicit blocker rule in `.claude/rules/rust/rust-let-types-carry-invariants.md`) requires `path: &Utf8Path`. Every other path-reading function in the codebase (`drift_cache.rs::sibling_temp_path`, `discovery/repo.rs::validate_root`, `apply/hooks.rs::path_candidates`, etc.) takes `&Utf8Path`; this function's signature fights that pattern. The rule file specifically names `&Utf8PathBuf` vs `&Utf8Path` as a recurring review blocker on this repo.

Fix: change the signature to `fn is_journal_sentinel(path: &Utf8Path) -> bool` and update the call site from `.any(is_journal_sentinel)` to `.any(|p| is_journal_sentinel(p))`.
</review>

<review persona="correctness" verdict="pass" model="claude-opus-4-8[1m]/high" date="2026-05-31T23:54:06Z" round="1">
No blocking correctness defect found in the T-009 watcher re-apply handler.

- Loop guard (CHK-017): sound. `compute_watch_set` derives `sources` solely from `target.source()`, so a content-target rewrite is never in `sources` and a re-apply's own target write falls to the no-op branch (`patina-core/src/watch/mod.rs:275`). The journal write during re-apply is classified journal-first (`mod.rs:268`) and routes to `rescan`, which only reads the journal (`read_latest_commit`) and writes nothing, so it cannot retrigger a re-apply. Termination does not depend on copy-mode byte-idempotency, matching the implementer's flagged constraint.
- Lock-contention control flow (`reapply.rs:104-117`): correct. Only `LockError::Contended` maps to `Skipped`; `LockError::Io`/other engine errors route to `fail`. `LockError::Timeout` cannot arise on the `NonBlocking` path (it is `acquire`-only per `lock.rs:155`), so the narrow match is exhaustive in practice.
- No panics: `u64::try_from(...).unwrap_or(u64::MAX)` (`reapply.rs:93`) is a saturating fallback, not a panic; no `unwrap`/`expect` in production; the CLI shutdown-signal path degrades gracefully on handler-install failure.
- Journal-rescan re-arm: a narrow lost-event window exists (the rebuilt debouncer drops batches buffered on the old channel, and journal-vs-source coalescing within the 500ms window resolves journal-first, deferring a co-arriving source edit one event). Both are bounded — the recomputed set re-watches the same sources and the watcher converges — and are a documented design tradeoff rather than a defect. Confidence it requires a fix is below the 80 threshold, so not reported as blocking.
</review>

<review persona="architecture" verdict="pass" model="claude-opus-4-8[1m]/xhigh" date="2026-05-31T23:54:06Z" round="1">
T-009 honors the system architecture across all four axes. (1) Layering: `watch/reapply.rs` is pure orchestration over genuine crate-root engine re-exports (`plan_apply`, `execute_plan`, `LockPolicy::NonBlocking`, `EngineError`/`LockError`, `current_timestamp`, `ApplyRequest::default` — all verified as `pub use` in `lib.rs`); it reimplements no apply logic and reaches into no engine internals, and correctly lets the engine self-acquire under `NonBlocking` rather than pre-acquiring (REQ-008's explicit directive — engine.rs:355). (2) The `WatchSet`/`compute_watch_set` partition (subscriptions.rs:46-76) is a justified abstraction, not premature: `watched` is `compute_subscriptions` verbatim and the new code reuses it, while the `sources` partition is the load-bearing loop guard (REQ-005/CHK-017) that stops a re-apply's own content-target rewrite and journal write from re-triggering; the journal-sentinel classifier reuses the journal's published `PLAN_SUFFIX`/`COMMIT_SUFFIX`/`PROGRESS_SUFFIX`/`ROLLED_BACK_SUFFIX` consts rather than re-spelling literals. (3) The T-008 binding directive (carry the subscriber into any spawned re-apply work) is satisfied the clean way: the re-apply runs **inline** (`reapply::run_reapply().await`, mod.rs:276) inside the `with_subscriber`-wrapped run future and is NOT `tokio::spawn`ed, so `re_apply`/`lock_contention_skip`/`journal_rescan` events route through the watcher's subscriber with no `.with_current_subscriber()` needed — which also satisfies DEC-011's explicit rejection of spawn-per-event. (4) DEC-002/DEC-007/DEC-008/DEC-011 and the REQ-006/REQ-008 NonBlocking-lock + zero-mutation-on-contention + journal-rescan decisions are all implemented faithfully; no Decisions entry is silently revisited.
Non-blocking forward note for T-010: `WatchSet` exposes `sources` but no `content_targets` partition; T-010's drift dispatch will need content-target paths (derivable as `watched − sources − journal_dir`, or via a new field). The struct extends cleanly and this does not block the next task.
</review>

<blockers date="2026-05-31T23:54:06Z" round="1">
One style blocker to address before the next review round; all other personas (business, tests, security, correctness, architecture) passed.
- style: `patina-core/src/watch/mod.rs:320` — `is_journal_sentinel` takes `path: &Utf8PathBuf`, violating the project convention (and the explicit blocker rule in `.claude/rules/rust/rust-let-types-carry-invariants.md`) that path-reading functions take `&Utf8Path`. Change the signature to `fn is_journal_sentinel(path: &Utf8Path) -> bool` and fix the call site (e.g. `.any(|p| is_journal_sentinel(p))`). Re-run `just lint` to confirm clippy/fmt stay green.
</blockers>

<implementer date="2026-05-31T23:59:00Z" model="claude-opus-4-8[1m]/high" round="2">
- Reuse survey: Round-2 amend addresses a single style blocker, not a
  reuse-related one, so the full round-1 area-map is not re-run. No new
  top-level symbols introduced — the only change is a parameter-type and
  call-site edit on the existing private `is_journal_sentinel`
  classifier, so the per-symbol floor has nothing new to account for.
- Completed: Resolved the round-1 style blocker in
  `patina-core/src/watch/mod.rs`. `is_journal_sentinel` now takes
  `path: &Utf8Path` (line 320) instead of `&Utf8PathBuf`, matching the
  project's parameter-type convention and the explicit rule in
  `.claude/rules/rust/rust-let-types-carry-invariants.md` (path-reading
  functions take `&Utf8Path`, not `&Utf8PathBuf`). The `journal_event`
  call site is `.any(|p| is_journal_sentinel(p))` (line 315), passing the
  `&Utf8PathBuf` yielded by the iterator through deref coercion to the
  `&Utf8Path` parameter. The `camino::Utf8Path` import is already present
  (line 33). No behaviour change — the classifier logic is byte-identical;
  only the borrow type narrowed.
- Undone: Nothing from T-009's scope is deferred. The forward-note items
  (T-010 drift dispatch, T-011+ service install) remain out of scope per
  TASKS.md, unchanged from round 1.
- Hygiene checks: `cargo +nightly fmt --all --check` exited 0.
  `cargo clippy --workspace --all-targets --all-features -- -D warnings`
  exited 0. `RUSTDOCFLAGS="-D warnings" cargo doc --workspace --no-deps
  --all-features` exited 0. `cargo deny check` exited 0 (no new deps;
  `advisories ok, bans ok, licenses ok, sources ok`). `cargo test
  --workspace --locked` exited 0 — all test binaries `test result: ok`,
  including the three T-009 integration tests and the five new
  `patina-core` unit tests. (Run via `just lint` + `just test`.)
- Evidence: round-1 red-then-green paper trail at
  `.speccy/specs/0003-patina-watch/evidence/T-009.md` is unchanged and
  still valid — the amend is a type-only refinement that does not alter
  any observable behaviour, so no new red/green pair was warranted. Roll
  call for the CHKs under REQ-006 / REQ-008 (unchanged from round 1):
  - CHK-010 (five touches within the window coalesce to exactly one
    re_apply): demonstrated → evidence Scenario 1, exercised by
    `five_touches_within_the_debounce_window_coalesce_to_one_reapply`,
    still green.
  - CHK-013 (concurrent CLI apply: zero `lock_contention_skip`, two
    committed COMMITs): demonstrated → evidence Scenario 2 (watcher-side
    observable) plus judgment-backed by the engine
    `non_blocking_contention_*` unit tests, still green.
- Discovered issues: none new this round. The round-1 copy-mode
  non-idempotency observation (`apply/copy.rs::copy_file` rewrites
  unconditionally) stands as a pre-existing engine property the watcher
  works around via source-vs-target classification; the business and
  correctness personas confirmed loop termination is sound. No change
  made outside T-009 scope.
- Procedural compliance: followed the retry branch — amended the prior
  pass's WIP in the working tree in place, did not run `git restore` /
  `git clean` / `git checkout` against the dirty paths, did not rewrite
  the touched files from scratch, did not reset state. Flipped T-009
  `pending → in-review` only after the hygiene gate passed; did not mark
  it `completed` (the review personas decide). Incremented the
  implementer round monotonically from 1 to 2. No locked-SPEC edit, no
  `TASKS.md` hand-edit beyond the state transition, no new dependency.
</implementer>

<review persona="business" verdict="pass" model="claude-opus-4-8[1m]/high" date="2026-06-01T02:25:52Z" round="2">
T-009 satisfies its REQ-006 / REQ-008 done-when slice and the round-2 change is a no-behavior-change type fix; the round-1 business pass carries forward.

REQ-006 mapping: the 500ms debounce coalesces a burst into one `re_apply` (CHK-010, `five_touches_within_the_debounce_window_coalesce_to_one_reapply`), and the re-apply runs under `LockPolicy::NonBlocking` with the journal `<ts>` keyed by the hoisted `current_timestamp` (`patina-core/src/watch/reapply.rs:80-118`). REQ-008 mapping: contention maps only to `Skipped` via `EngineError::Lock(LockError::Contended { .. })` (`reapply.rs:104`), emitting `lock_contention_skip` (`skip_reason="lock_held"`) and mutating nothing — verified against the engine's acquire-then-recover sequence and its `non_blocking_contention_*` tests, which honestly back the half of CHK-013 (CLI-blocks-behind-watcher race) that is flaky to re-race from a subprocess. The watcher does not pre-acquire the lock (REQ-008's explicit directive); the engine self-acquires under `NonBlocking`.

Non-goals respected: no IPC (coordination is the SPEC-0001 advisory lock), no recursive watch (re-apply fires only on `WatchSet.sources`, the loop guard at `watch/mod.rs:435`), no separate daemon, no metrics subcommand. User story (active-editor auto-reapply + CLI-priority coordination) is served. Open question (e) was resolved upstream by the 2026-05-30 SPEC-0001 amendment, not silently by this implementer.

Non-blocking note (unchanged from round 1, not a T-009 contract): `re_apply.files_changed` is sourced from `resolved.operations.len()` (planned op count, not bytes-actually-changed) and flattened to `re_apply_files_changed`. REQ-009 field fidelity is T-006's coverage, not T-009's, and the field has no user-facing/JSON/CLI surface, so this does not block.
</review>

<review persona="tests" verdict="pass" model="claude-opus-4-8[1m]/high" date="2026-06-01T02:25:52Z" round="2">
Round-2 retry verified: the sole change is narrowing `is_journal_sentinel` from `&Utf8PathBuf` to `&Utf8Path` (`patina-core/src/watch/mod.rs:320`, call site `:315`) with byte-identical classifier logic and no test-source change. The test suite is unaffected and still genuinely exercises the covered behaviors — re-ran all patina-core watch unit tests and the `watch_foreground_cli` integration tests, all green. CHK-010 (`five_touches_within_the_debounce_window_coalesce_to_one_reapply`, real subprocess + 5 real writes, asserts exactly one `re_apply` and zero `re_apply_failed`) and CHK-013 surface (`a_watcher_reapply_commits_exactly_one_new_journal_record`, polls real `.COMMIT` sentinels to 2, asserts zero contention skip) drive actual code paths, not mocks; the journal-rescan path (`a_parallel_cli_apply_triggers_a_journal_rescan`) asserts a real `journal_rescan` event with a bounded-loop guard; and the deferred contention half is backed by `engine.rs:724`/`:804`, which take a real held lock and assert typed `LockError::Contended` + zero mutation. Evidence roll call covers both rendered CHKs with per-test-scoped commands and materially-different red/green output; no fabrication pattern matched. Pre-existing/round-1-adjudicated note (not blocking, out of retry scope): no test drives the watcher-side `lock_contention_skip` *emission* — `run_reapply`'s `Contended → Skipped` arm (`reapply.rs:104-114`) is exercised only as the `== 0` uncontended assertion; the round-1 tests persona already accepted this decomposition and the round-2 type fix neither touches nor regresses it.
</review>

<review persona="security" verdict="pass" model="claude-opus-4-8[1m]/high" date="2026-06-01T02:25:52Z" round="2">
Round-2 amend is security-neutral: the only change is the `is_journal_sentinel` parameter narrowing from `&Utf8PathBuf` to `&Utf8Path` in `patina-core/src/watch/mod.rs:320` plus the `.any(|p| is_journal_sentinel(p))` call site — a borrow-type refinement with byte-identical classifier logic and no behavioral change. The three security surfaces are unchanged from the round-1 pass: (1) NonBlocking lock boundary is correct — the watcher never pre-acquires, the engine self-acquires under `NonBlocking` and returns `LockError::Contended` with zero mutation on a held CLI lock, mapped to `ReapplyOutcome::Skipped` (`patina-core/src/watch/reapply.rs:91-117`); a concurrent CLI apply is never clobbered. (2) Log events `re_apply` / `lock_contention_skip` / `re_apply_failed` expose only a timestamp id, duration, op count, a static `re_apply_result` label, a static `skip_reason="lock_held"`, and the `EngineError` Display (`reapply.rs:94-129`) — no file contents, hashes, or secrets, and at most paths `patina apply` already prints, to the machine-local `<state>/patina/logs/` sink. (3) Automatic-reapply traversal/TOCTOU surface is unchanged — `run_reapply` is verbatim reuse of `plan_apply` + `execute_plan` (no new canonicalization, symlink resolution, or write site), and the new classifiers do no filesystem I/O and make no authorization decision. No new exposure or TOCTOU surface introduced this round.
</review>

<review persona="style" verdict="pass" model="claude-sonnet-4-6[1m]/medium" date="2026-06-01T02:25:52Z" round="2">
Round-1 blocker resolved. `patina-core/src/watch/mod.rs:320` now reads `fn is_journal_sentinel(path: &Utf8Path) -> bool` and the call site at line 315 is `.any(|p| is_journal_sentinel(p))`, matching the project convention and the explicit rule in `.claude/rules/rust/rust-let-types-carry-invariants.md`. No other changes were introduced in round 2.
</review>

<review persona="correctness" verdict="pass" model="claude-opus-4-8[1m]/high" date="2026-06-01T02:25:52Z" round="2">
Round-2 amend confirmed correctness-neutral. The sole change is narrowing `is_journal_sentinel(path: &Utf8PathBuf)` → `&Utf8Path` (`patina-core/src/watch/mod.rs:320`) with the `journal_event` call site changed to `batch.paths.iter().any(|p| is_journal_sentinel(p))` (`mod.rs:315`). The function body is byte-identical — same parent-dir-name `== "journal"` guard plus the same suffix tests — and `&Utf8PathBuf` → `&Utf8Path` deref coercion at the call site is a transparent borrow narrowing with no runtime effect.

Round-1 correctness assessment re-confirmed:
- Loop guard (REQ-005/CHK-017): sound. `sources` is derived solely from `target.source()` in `compute_watch_set`; the loop classifies journal-first then source-only, so a re-apply's own content-target rewrite (in `watched`, never in `sources`) falls to no-op and its journal write routes to `rescan`, which only reads and rebuilds. Termination does not rely on copy-mode byte-idempotency.
- Lock-contention skip (`reapply.rs:104-116`): only `LockError::Contended` maps to `Skipped` with zero mutation; all other engine errors route to `fail` and the watcher survives.
- Journal-rescan re-arm: read-or-rebuild failures log a warning and return `None`, retaining the prior debouncer/watch-set. The narrow lost-event/coalescing window is bounded and converging — below the 80 reporting threshold, as in round 1.
- No unwrap/panic on async/lock paths: `u64::try_from(...).unwrap_or(u64::MAX)` (`reapply.rs:93`) is a saturating fallback; no `unwrap`/`expect` in production; the new closure adds no fallible path.

No blocking correctness defect.
</review>

<review persona="architecture" verdict="pass" model="claude-opus-4-8[1m]/high" date="2026-06-01T02:25:52Z" round="2">
T-009 round-2 still honors the system architecture across all four axes; the sole round-2 delta (the `&Utf8PathBuf`→`&Utf8Path` narrowing on the private `is_journal_sentinel` classifier at `patina-core/src/watch/mod.rs:320`, call site `.any(|p| is_journal_sentinel(p))` at line 315) is a borrow-type refinement with zero layering, abstraction, or decision impact.

(1) Layering — `patina-core/src/watch/reapply.rs` orchestrates purely over crate-root re-exports (`crate::{plan_apply, execute_plan, LockPolicy, EngineError, LockError, ApplyRequest, ApplyResult, current_timestamp}`, all verified `pub use` at `patina-core/src/lib.rs:41-99`); it reaches into no `apply::engine::` internal and matches contention on the public `EngineError::Lock(LockError::Contended { .. })` arm (reapply.rs:104). It lets the engine self-acquire under `NonBlocking` rather than pre-acquiring (REQ-008).

(2) WatchSet loop-guard — `compute_watch_set` (subscriptions.rs:66) reuses `compute_subscriptions` verbatim for `watched` and adds the `sources` partition derived from `target.source()`; `source_event` routes re-apply on `sources` only, so a re-apply's own content-target rewrite and journal write cannot re-trigger it. Load-bearing partition, not premature abstraction.

(3) Inline-not-spawned — `reapply::run_reapply().await` (mod.rs:276) runs inline inside the `with_subscriber`-wrapped run future, not `tokio::spawn`ed, satisfying the T-008 subscriber-routing directive and DEC-011's rejection of spawn-per-event.

(4) DEC adherence — DEC-002 (500ms hardcoded; `debounce_ms` typed-warned), DEC-007 (skip-on-contention → `ReapplyOutcome::Skipped`), DEC-008 (symlink target paths never enter `sources`/`watched` target side), and DEC-011 (single `tokio::select!` loop) are all honored; no Decisions entry is silently revisited. The round-1 forward note for T-010 (WatchSet exposes `sources` but no `content_targets` partition) still stands as non-blocking — the struct extends cleanly.
</review>
