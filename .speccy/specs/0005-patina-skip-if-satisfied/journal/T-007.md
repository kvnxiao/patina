---
spec: SPEC-0005
task: T-007
generated_at: 2026-06-02T20:55:00Z
---

<implementer date="2026-06-02T20:55:00Z" model="claude-opus-4-8[1m]/high" round="1">
- Reuse survey: Mapped the task area (covered REQ-005; suggested file
  `patina-core/src/journal/recovery.rs`; plus the `Disposition` enum and
  the `PlannedOperation::disposition()` accessor in
  `patina-core/src/journal/{disposition.rs,plan.rs}`, and the `crate::fsx`
  restore/delete helpers `reverse_operation` already drives). Decisions:
  reuse-as-is — `PlannedOperation::disposition()` (the T-002 per-op
  aggregate accessor) for the new guard, `Disposition::Unchanged` for the
  comparison, `crate::fsx::{entry_present, clone_entry, remove_entry}` and
  `mirror_backup_path` unchanged for the existing two arms. Reuse-as-is —
  the test module's `dirs()`, `write_plan()`, and `PlannedOperation::copy`
  fixtures for the two new crafted-orphan tests. Write-fresh — none; the
  change is a single early-return arm plus two tests. No new top-level
  symbols introduced, so no per-symbol search was owed; grepped
  `recovery.rs` and the `journal` module for any existing
  disposition/Unchanged handling and found the only gap was this arm.
- Completed: Added a third arm to `reverse_operation`
  (`patina-core/src/journal/recovery.rs`) ahead of the backup-presence
  branch: an op whose plan-recorded disposition is `Disposition::Unchanged`
  returns `Ok(())` and is left in place regardless of backup presence
  (REQ-005). `Create`-no-backup (delete) and `Update`-with-backup (restore)
  behavior is unchanged. Read via `op.disposition()`, the durable per-op
  aggregate, so tree ops reverse whole-directory per DEC-007. Updated the
  `reverse_operation` doc comment to describe the new arm and added the
  `Disposition` import. Added two crafted-orphan-plan unit tests (no
  COMMIT, no ROLLED_BACK): `unchanged_marked_orphan_target_is_left_in_place`
  (CHK-008) and `create_marked_orphan_target_with_no_backup_is_deleted`
  (CHK-009). Wrote the red/green evidence file at
  `.speccy/specs/0005-patina-skip-if-satisfied/evidence/T-007.md`.
- Undone: Nothing in REQ-005 scope is deferred. T-008 (rollback's matching
  `Unchanged` arm in `revert_target`) is a separate task and out of scope
  here.
- Hygiene checks: `cargo test --workspace --locked` exit 0;
  `cargo clippy --workspace --all-targets --all-features --locked -- -D
  warnings` exit 0; `cargo +nightly fmt --all --check` exit 0;
  `cargo deny check` exit 0. (Run via `just test` / `just lint`, which wrap
  these in CI order and add the `cargo doc -D warnings` gate, also clean.)
  Hygiene temp logs were written under `$env:TEMP` and removed; the working
  tree holds only `recovery.rs`, `TASKS.md`, and the new journal/evidence
  files.
- Evidence: paper trail at
  `.speccy/specs/0005-patina-skip-if-satisfied/evidence/T-007.md`.
  Roll call for CHKs under REQ-005:
  - CHK-008 (Unchanged orphan target with no backup is preserved):
    demonstrated → evidence Scenario 1 covers the red (pre-edit no-backup
    arm deletes the target) / green (new arm leaves it byte-for-byte)
    pair via `unchanged_marked_orphan_target_is_left_in_place`.
  - CHK-009 (Create orphan target with no backup is deleted):
    demonstrated → evidence Scenario 2 covers the guard that the new
    `Unchanged` arm does not swallow the `Create`/no-backup delete, via
    `create_marked_orphan_target_with_no_backup_is_deleted`.
- Discovered issues: None. The `Update`-with-backup restore path already
  had unit coverage (`overwrite_of_a_pre_existing_symlink_restores_the_symlink`,
  unix-gated) and is unchanged by this arm.
- Procedural compliance: TASKS.md `state` for T-007 flips
  `pending → in-progress → in-review` across this turn; no
  `<implementer-note>` was inlined into TASKS.md (the journal file is the
  canonical home). No locked `SPEC.md` was edited. No shipped
  `.claude/skills/speccy-*` bodies were touched. Reused existing accessors
  and helpers rather than adding parallel code.
</implementer>

<review date="2026-06-02T17:11:59-04:00" model="claude-opus-4-8[1m]/high" persona="business" verdict="pass" round="1">
T-007 (REQ-005) faithfully delivers the crash-recovery half of the
skip-if-satisfied fidelity goal. The new `Unchanged` arm in `reverse_operation`
(`patina-core/src/journal/recovery.rs:205-210`) is placed ahead of the
backup-presence branch and reads `op.disposition()` — the durable per-op aggregate
(`patina-core/src/journal/plan.rs:123`), satisfying DEC-007's "tree ops reverse
whole-directory" clause. All three REQ-005 `<done-when>` branches hold: Unchanged
orphan is left byte-for-byte (CHK-008, `unchanged_marked_orphan_target_is_left_in_place`),
Create-with-no-backup is still deleted (CHK-009,
`create_marked_orphan_target_with_no_backup_is_deleted`), and Update-with-backup
restore is provably untouched because the arm only short-circuits `Unchanged` and any
other disposition falls through to the unchanged backup-presence logic. Both new unit
tests pass. No scope creep — the diff touches only `reverse_operation`, correctly
defers the rollback arm to T-008, and adds no force-rewrite flag or classification
cache. No open questions were silently resolved.
</review>

<review date="2026-06-02T17:11:59-04:00" model="claude-opus-4-8[1m]/high" persona="tests" verdict="pass" round="1">
Both crafted-orphan-plan tests drive the real `recover_orphans` → `reverse_orphan` →
`reverse_operation` path end-to-end: each writes a real `Plan` to disk via
`plan.encode()` and `recover_orphans` decodes it and reads `op.disposition()` off the
deserialized field — no mock of the system under test, and the postcard round-trip is
exercised. Mutation-thinking holds: CHK-008
(`unchanged_marked_orphan_target_is_left_in_place`) stashes no backup, so removing the
new `Unchanged` arm drops the op into the no-backup branch → `remove_entry(target)` →
the `target.exists()` + byte-for-byte assertions go red; CHK-009
(`create_marked_orphan_target_with_no_backup_is_deleted`) guards that the new arm does
not over-match a `Create` op. The no-backup preconditions
(`assert!(!entry_present(mirror_backup_path(...)))`) are genuine fixture guards
pinning the tests to the no-backup branch. Roll call complete: CHK-008 and CHK-009
(the only CHKs under REQ-005) both `demonstrated`, both with named test symbols in the
diff. Minor non-blocking: the evidence body is a red/green prose narrative rather than
captured `cargo test` output; it is accurate and verifiable against the diff, so it
does not rise to a fabrication block, but a future pass should prefer scoped runner
output.
</review>

<review date="2026-06-02T17:11:59-04:00" model="claude-opus-4-8[1m]/high" persona="security" verdict="pass" round="1">
T-007 preserves the crash-safety convergence guarantee and introduces no new attack
surface. The `Unchanged` arm in `reverse_operation` (`patina-core/src/journal/recovery.rs:205`)
is correctly ordered ahead of the backup-presence branch and reads the durable per-op
aggregate via `op.disposition()`. The apply-side skip (`materialize_target`,
`patina-core/src/apply/engine.rs:977`) and the recovery-side leave are symmetric, and
the plan is fsync'd (`engine.rs:1138`) before the materialize loop, so no crash window
exists where an `Unchanged`-marked target was mutated — leaving it in place reproduces
pre-apply state at every crash point. The new arm trusts the disposition byte in the
orphan plan, but that plan resides in the per-machine state journal dir written only
by Patina's own fsync; an actor able to forge it already has write access to backups,
sentinels, and targets and is outside the trust boundary. The two existing arms
already trust the same plan's targets and backup layout, so this is one more trusted
field on an already-fully-trusted local file, not a new authority surface — no
injection, path-traversal, or privilege vector is added.
</review>

<review date="2026-06-02T17:11:59-04:00" model="claude-sonnet-4-6[1m]/medium" persona="style" verdict="blocking" round="1">
The module-level doc comment (`patina-core/src/journal/recovery.rs:18-21`) describes
the reversal algorithm as having exactly two branches — "A target with a backup is an
*overwrite*: restore ... A target with no backup is a *fresh creation*: delete it" —
but the new `Unchanged` arm is now evaluated first and exits before the
backup-presence check is ever reached. Step 3 of the enumerated list no longer
describes what the code does; the third case (disposition == Unchanged → leave in
place, do not consult the backup directory) is missing entirely.

CLAUDE.md's hard rule is "Never let docs drift. If your change alters observable
behavior ... the corresponding docs change in the same PR." The module doc is the
canonical description of the recovery algorithm and now omits the first-evaluated and
most important new case.

Fix: update step 3 of the module-level doc to describe all three reversal outcomes —
Unchanged (skip), backup present (restore), no backup (delete) — in the order the code
evaluates them.

No other findings: the `Disposition` import is correctly alphabetically ordered, the
new arm uses `?`-propagation not panics, the `reverse_operation` function doc comment
is accurate, and the two new tests gate real invariants with non-trivial assertions.
</review>

<review date="2026-06-02T17:11:59-04:00" model="claude-opus-4-8[1m]/high" persona="correctness" verdict="pass" round="1">
Arm placement is correct: the `Disposition::Unchanged` early return
(`patina-core/src/journal/recovery.rs:205-210`) is the first statement in
`reverse_operation`, ahead of `mirror_backup_path`/`entry_present` (lines 213-225), so
an Unchanged op is left in place regardless of backup presence. Create-no-backup still
deletes (line 224) and Update-with-backup still restores (line 219) — that block is
byte-identical to the prior code, only an insertion above it, no arm reordering.
`op.disposition()` reads the field stored on the durable `PlannedOperation` (DEC-007
per-op aggregate, confirmed in `plan.rs:120-129`), decoded from the fsync'd `.plan`,
so the disposition is authoritative at any crash point. The per-op early `return
Ok(())` does not bypass the post-loop `remove_if_present` cleanup in `reverse_orphan`
or any resource release. No off-by-one, overflow, or control-flow defect.
</review>

<blockers date="2026-06-02T17:11:59-04:00" round="1">
Update the recovery.rs module doc to match the new three-branch reversal behavior before the next implementer pass.
- style (blocking): the module-level doc comment at `patina-core/src/journal/recovery.rs:18-21` enumerates the reversal algorithm as two branches (backup present → restore; no backup → delete), but the new `Unchanged` arm is now evaluated first and returns before the backup-presence check. This is doc drift, violating CLAUDE.md's "Never let docs drift" hard rule. Update step 3 to describe all three outcomes in evaluation order: disposition == Unchanged → leave in place (do not consult the backup directory); backup present → restore; no backup → delete.
</blockers>

<implementer date="2026-06-02T21:20:00Z" model="claude-opus-4-8[1m]/high" round="2">
- Reuse survey: Not re-run. The round-1 blocker is a documentation-drift
  fix (style persona), not a reuse-related blocker, so the round-1
  area-map stands. The amend introduces no new top-level symbol, so the
  round-agnostic per-symbol floor is vacuously satisfied — no `fn`/type
  was added, only module-level doc-comment prose was rewritten.
- Completed: Addressed the sole round-1 blocker by amending the
  module-level doc comment in `patina-core/src/journal/recovery.rs`. Step 3
  of the recovery algorithm enumeration now lists all three reversal
  outcomes in the order `reverse_operation` evaluates them: disposition ==
  `Unchanged` (REQ-005) → leave in place and do **not** consult the backup
  directory; a target with a backup → restore; a target with no backup →
  delete. The prior two-branch text omitted the first-evaluated `Unchanged`
  case, which had drifted from the `Disposition::Unchanged` early-return arm
  added in round 1 (recovery.rs:205-210). No code behavior changed; the
  round-1 arm, tests, and evidence file are untouched.
- Undone: Nothing in REQ-005 scope is deferred. T-008 (rollback's matching
  `Unchanged` arm in `revert_target`) remains a separate task, out of scope.
- Hygiene checks: `cargo test --workspace --locked` exit 0;
  `cargo clippy --workspace --all-targets --all-features --locked -- -D
  warnings` exit 0; `cargo +nightly fmt --all --check` exit 0;
  `cargo deny check` exit 0. Run via `just lint` (CI order, includes the
  `cargo doc -D warnings` docs gate — the gate that proves a doc-comment
  edit does not break intra-doc links) and `just test`, both exit 0. The
  first `just lint` failed on a nightly-rustfmt comment reflow of the new
  doc lines; the wrap was applied and the re-run passed. The lint temp log
  was written under `$env:TEMP` and removed; the working tree holds only
  `recovery.rs`, `TASKS.md`, and the journal/evidence files.
- Evidence: paper trail at
  `.speccy/specs/0005-patina-skip-if-satisfied/evidence/T-007.md` (round-1,
  unchanged). Roll call for CHKs under REQ-005:
  - CHK-008 (Unchanged orphan target with no backup is preserved):
    demonstrated → round-1 evidence Scenario 1 red/green pair via
    `unchanged_marked_orphan_target_is_left_in_place`; unaffected by this
    doc-only amend and re-verified green under `just test`.
  - CHK-009 (Create orphan target with no backup is deleted):
    demonstrated → round-1 evidence Scenario 2 via
    `create_marked_orphan_target_with_no_backup_is_deleted`; unaffected by
    this doc-only amend and re-verified green under `just test`.
- Discovered issues: None.
- Procedural compliance: Retry-shape amend — the dirty `recovery.rs` WIP
  was edited in place (no `git restore`/`clean`/`checkout`, no rewrite from
  scratch). TASKS.md `state` for T-007 flips `pending → in-progress →
  in-review` this turn. `round` incremented by exactly 1 (1 → 2). No
  `<implementer-note>` inlined into TASKS.md; no locked `SPEC.md` edited;
  no shipped `.claude/skills/speccy-*` body touched.
</implementer>

<review date="2026-06-02T17:17:56-04:00" model="claude-opus-4-8[1m]/high" persona="business" verdict="pass" round="2">
T-007 (REQ-005) still faithfully delivers the crash-recovery half of the
skip-if-satisfied fidelity goal after the round-2 doc-only amend. The round-2 change
touches only the module-level doc comment and the `reverse_operation` function doc —
it enumerates all three reversal branches in evaluation order (Unchanged → leave
without consulting backups; backup present → restore; no backup → delete), resolving
the round-1 style doc-drift blocker. The production `Disposition::Unchanged`
early-return arm (`recovery.rs:211-216`) is byte-identical to round 1: still placed
ahead of the backup-presence branch and still reads `op.disposition()`, the durable
per-op aggregate (`plan.rs:123`), satisfying DEC-007's whole-directory tree reversal.
All three REQ-005 `<done-when>` branches hold unchanged — Unchanged orphan left
byte-for-byte (CHK-008), Create-no-backup still deleted (CHK-009), Update-with-backup
still restored. No goals/non-goals drift; no open questions silently resolved.
</review>

<review date="2026-06-02T17:17:56-04:00" model="claude-opus-4-8[1m]/high" persona="tests" verdict="pass" round="2">
Round-2 change is doc-only as claimed: the module doc and `reverse_operation` fn doc
were rewritten to enumerate all three reversal outcomes, while the `Unchanged`
early-return arm and both crafted-orphan tests are byte-identical to the
round-1-passing state. Both tests still gate the real `recover_orphans` →
`reverse_orphan` → `reverse_operation` path: CHK-008
(`unchanged_marked_orphan_target_is_left_in_place`) pins the no-backup branch and
asserts the target survives byte-for-byte — removing the arm drops it into
`remove_entry` and goes red; CHK-009
(`create_marked_orphan_target_with_no_backup_is_deleted`) guards that the arm does not
over-match Create. Roll call complete: CHK-008 and CHK-009 (the only REQ-005 CHKs)
both `demonstrated` with named symbols present in the diff. No fabrication patterns
matched. No tests-scope regression from the doc edit.
</review>

<review date="2026-06-02T17:17:56-04:00" model="claude-opus-4-8[1m]/high" persona="security" verdict="pass" round="2">
Round-2 is doc-only: the module-doc step 3 and the `reverse_operation` function doc
now describe the three-branch reversal in evaluation order. The round-1 `Unchanged`
early-return arm and both crafted-orphan tests are byte-identical to the round-1 PASS
— no code behavior changed. No security or crash-safety regression: the `Unchanged`
arm remains correctly ordered ahead of the backup-presence branch, preserving
deterministic convergence to pre-apply state at any crash point; the disposition byte
is read from a Patina-fsync'd plan inside the per-machine state journal dir, an
already-fully-trusted local file, so no new injection, path-traversal, privilege, or
data-exposure surface is added. The new prose accurately states the
fsync-before-mutation invariant without overstating any guarantee.
</review>

<review date="2026-06-02T17:17:56-04:00" model="claude-sonnet-4-6[1m]/medium" persona="style" verdict="pass" round="2">
Round-1 blocker resolved. `patina-core/src/journal/recovery.rs` lines 18-27 now
enumerate all three reversal outcomes in evaluation order: `Unchanged` → leave in
place (do not consult backup); target with backup → restore; target with no backup →
delete. The bullet-list structure under step 3 is clear, accurate, and matches the
`reverse_operation` arm ordering. No new style regressions introduced — the round-2
diff is doc-comment prose only.
</review>

<review date="2026-06-02T17:17:56-04:00" model="claude-opus-4-8[1m]/high" persona="correctness" verdict="pass" round="2">
Round-2 change is doc-only and the production logic is unchanged and correct. The
`Disposition::Unchanged` early return (`patina-core/src/journal/recovery.rs:211-216`)
is still the first statement in `reverse_operation`, ahead of
`mirror_backup_path`/`entry_present`, so an Unchanged orphan op is left in place
regardless of backup presence (CHK-008). Create-no-backup falls through to the `else`
delete branch (CHK-009); Update-with-backup falls through to the `entry_present(&backup)`
restore branch — that block is byte-identical to round-1, the doc edit touched only
module-level prose and the function doc. `op.disposition()` reads the durable per-op
aggregate (`plan.rs:123`) decoded from the fsync'd plan, authoritative at any crash
point per DEC-007. The per-op `return Ok(())` does not bypass `reverse_orphan`'s
post-loop plan/progress cleanup. The module-doc step-3 enumeration now lists the three
outcomes in the exact order the code evaluates them, so the doc accurately describes
the code. Both new tests pass. No off-by-one, overflow, resource-leak, or control-flow
defect.
</review>
