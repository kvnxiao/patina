---
spec: SPEC-0005
task: T-006
generated_at: 2026-06-02T20:30:00Z
---

<implementer date="2026-06-02T20:30:00Z" model="claude-opus-4-8[1m]/high" round="1">
- Reuse survey: Mapped the task area (covered REQ-007 / REQ-008 /
  REQ-009; suggested files `patina-core/src/apply/engine.rs`,
  `patina-cli/src/cmd/apply.rs`, the two integration suites; plus the
  `Reporter` trait and the `ApplyResult` consumers). Decisions:
  extend — `ApplyResult::Applied` grew an `up_to_date: bool` field
  rather than adding a parallel `UpToDate` variant (every match site
  already used `{ .. }` / `{ warnings }`, so the field is the
  lower-churn carrier). Extend — factored the orphan-detection body
  out of the existing `reap_orphans` into a new private
  `detect_orphans`, which `reap_orphans` now drives and the no-op gate
  reuses, rather than copying the "what is an orphan" walk. Reuse-as-is
  — `Disposition::Unchanged` for the all-unchanged check,
  `crate::journal::read_latest_commit` for the prior-commit guard,
  `Reporter::line` for the up-to-date message. Write-fresh — the
  `is_full_noop` predicate and the public `plan_is_full_noop` probe;
  grepped `engine.rs` and the crate for an existing no-op / "nothing to
  do" predicate and found none. New top-level symbols introduced:
  `detect_orphans` (extends `reap_orphans`), `is_full_noop`
  (private, the single source of truth), and `plan_is_full_noop`
  (public probe over `is_full_noop`).
- Completed: Added the REQ-007 full-no-op short-circuit to
  `execute` (`patina-core/src/apply/engine.rs`): after `recover_orphans`
  and the symlink gate, `if is_full_noop(resolved, reap)?` returns
  `ApplyResult::Applied { warnings: vec![], up_to_date: true }` before
  `flush_plan_and_fsync`, so no `<ts>.plan`, `<ts>.COMMIT`, or backup
  cycle is written. `is_full_noop` is gated to the reaping (whole-plan
  `Blocking` / `NonBlocking`) policies — a non-reaping `Held` run
  (`patina remove` / `promote`) is a deliberate surgical re-journal and
  is never a no-op — then checks all-`Unchanged` dispositions, requires
  a prior commit (so an empty first apply still commits a baseline), and
  requires an empty reap set. The normal commit path now sets
  `up_to_date: false`. Re-exported `plan_is_full_noop` from
  `patina-core/src/lib.rs`. In the CLI human path
  (`patina-cli/src/cmd/apply.rs`), a `plan_is_full_noop` branch now runs
  before the diff render and the `(yes, tty)` prompt decision (REQ-009),
  executing and reporting without prompting or reading stdin;
  `report_result` prints `Already up to date. No changes to apply.` via
  `Reporter::line` for an `up_to_date` result (REQ-008). Updated the
  one `ApplyResult::Applied` literal in `patina-core/src/watch/reapply.rs`
  and the CLI unit test. Added `#[expect(clippy::too_many_lines, ...)]`
  with a justification to `execute` (the no-op gate pushed the linear
  orchestrator four lines past the ceiling; splitting a phase would hide
  the crash-safety ordering without removing a step). Tests:
  `fully_satisfied_reapply_writes_no_new_journal_or_backup` and
  `fully_satisfied_apply_without_yes_skips_prompt_and_reports_up_to_date`
  in `patina-cli/tests/skip_if_satisfied.rs`, and
  `fully_satisfied_applies_are_byte_identical_and_report_up_to_date` in
  `patina-cli/tests/deterministic_stdout.rs`.
- Undone: The `--json` per-entry `state` field and the JSON no-op
  envelope (REQ-011 / REQ-012) are T-010, not this task; the JSON path
  already routes a no-op through the standard `"applied"` envelope, so
  no special-casing was added here. Crash-recovery (REQ-005, T-007) and
  rollback (REQ-006, T-008) leave-Unchanged behaviour are their own
  tasks. Nothing in T-006's scope is deferred.
- Hygiene checks: ran the project gate `just check` (= `just lint` +
  `just test`), which is CI's exact lint order plus the workspace test
  run. `cargo +nightly fmt --all --check` exit 0. `cargo clippy
  --workspace --all-targets --all-features --locked -- -D warnings`
  exit 0 (host + cross `x86_64-unknown-linux-gnu` +
  `x86_64-pc-windows-gnu`; macOS leg is CI's backstop). `RUSTDOCFLAGS="-D
  warnings" cargo doc --workspace --no-deps --all-features` exit 0.
  `cargo deny check` exit 0 (no new dependencies). `cargo test
  --workspace --locked` exit 0 (all suites, including the three new
  tests and the `promote_cli` / `remove_cli` / `held_policy_*`
  regression guards). `just check` overall exit 0.
- Evidence: red-then-green paper trail at
  `.speccy/specs/0005-patina-skip-if-satisfied/evidence/T-006.md`.
  Roll call for the three CHKs under REQ-007 / REQ-008 / REQ-009:
  - CHK-011 (no new `*.plan` / `*.COMMIT` / backup cycle on a
    fully-satisfied re-apply): demonstrated → evidence Scenario 1
    (`fully_satisfied_reapply_writes_no_new_journal_or_backup`) compares
    the journal and backups entry sets before and after the no-op apply.
  - CHK-012 (two no-op applies byte-identical, both carry the up-to-date
    message): demonstrated → evidence Scenario 2
    (`fully_satisfied_applies_are_byte_identical_and_report_up_to_date`).
  - CHK-013 (no prompt invocation, completes without reading stdin):
    demonstrated → evidence Scenario 3
    (`fully_satisfied_apply_without_yes_skips_prompt_and_reports_up_to_date`)
    asserts the up-to-date line is printed and no `Apply?` prompt text
    reaches stderr, proving the no-op branch precedes the prompt/stdin
    path. (The injected-test-reporter framing in the scenario text is
    satisfied here by the subprocess observing stdout/stderr; the human
    path's no-op branch sits before the `(yes, tty)` decision, so neither
    interactive nor non-interactive reaches the prompt.)
  No CHK in T-006's scope is `judgment-only`.
- Discovered issues: none. The pre-existing `execute` orchestrator was
  already at the `too_many_lines` ceiling; the added `#[expect]` is the
  in-codebase-precedented way to carry one more linear phase (a sibling
  `materialize_target` already uses `#[expect(clippy::too_many_arguments,
  ...)]`).
- Procedural compliance: this entry lands in `journal/T-006.md` per the
  journal-file schema; no `<implementer-note>` was written into TASKS.md.
  The TASKS.md `state` for T-006 flips from `in-progress` to `in-review`
  as the final step of this turn. No shipped skill bodies under
  `.claude/skills/speccy-*` required edits.
</implementer>

<review date="2026-06-02T16:48:29-04:00" model="claude-opus-4-8[1m]/high" persona="business" verdict="blocking" round="1">
REQ-009's core observable ships unverified. The production code is correct — the
no-op short-circuit in `patina-cli/src/cmd/apply.rs:100` precedes the prompt
decision at line 112, so an interactive TTY is correctly short-circuited — but no
test exercises that interactive path. REQ-009's `<done-when>` ("A full no-op on an
interactive TTY presents no confirmation prompt and reads no stdin"), its
`<behavior>`, and CHK-013 ("a test reporter that records prompt invocations ...
records zero prompt invocations") are all explicitly about the interactive TTY path
— the path where the prompt would otherwise fire.

The shipped test `fully_satisfied_apply_without_yes_skips_prompt_and_reports_up_to_date`
(`patina-cli/tests/skip_if_satisfied.rs:155`) runs only via the subprocess fixture,
whose stdin is not a TTY, so the prompt is suppressed unconditionally by pre-existing
code independent of this feature. Its assertion `!stderr.contains("Apply?")` passes
whether or not the no-op short-circuit exists — a vacuous, loose-outcome assertion
(test-hygiene rule 5) for the behavior REQ-009 names. Fix: `run` is fully injectable
(`Tty`, `PromptReader`, `Reporter`) — add an in-process test driving a fully-satisfied
plan with `Tty::Interactive`, a recording `Reporter` (counting `prompt` invocations),
and a `PromptReader` whose `read_line` panics if called; assert zero prompts and no
stdin read.

REQ-007 (no-write short-circuit ordering: `recover_orphans` → gate → `is_full_noop`
→ return before `flush_plan_and_fsync`) and REQ-008 (deterministic up-to-date
message) are correctly implemented; no scope creep, no silently-resolved open
questions, no non-goal violations.
</review>

<review date="2026-06-02T16:48:29-04:00" model="claude-opus-4-8[1m]/high" persona="tests" verdict="blocking" round="1">
CHK-011's test does not gate REQ-007's no-write invariant — the core guarantee of
this task. `fully_satisfied_reapply_writes_no_new_journal_or_backup`
(`patina-cli/tests/skip_if_satisfied.rs:93`) asserts the *set of basenames* in the
journal dir is unchanged across the second apply. But journal filenames are
`{timestamp}.plan`/`{timestamp}.COMMIT` where `current_timestamp()` is
second-resolution (`patina-core/src/clock.rs:27`, `%Y%m%dT%H%M%SZ`), and on commit
the `.plan`/`.progress` are deleted leaving only `<ts>.COMMIT`. Two back-to-back
subprocess applies almost always land in the same wall-clock second, so even a full
plan→commit write cycle overwrites `<ts>.COMMIT` in place and the basename set is
identical. Confirmed empirically: mutating `is_full_noop` to `return Ok(false)`
(disabling the entire no-op short-circuit) leaves this test GREEN, while CHK-012 and
CHK-013's up-to-date assertions correctly go red. The test passes whether or not the
feature exists, and is timing-dependent (would behave differently across a second
boundary).

Fix: make the no-write assertion insensitive to filename collision — capture the
prior `<ts>.COMMIT` identity (mtime or full bytes) before the second apply and assert
it is unchanged afterward, OR observe `flush_plan_and_fsync` not being called. The
mutation that removes the feature must turn this test red.

Secondary (tighten on the same pass): CHK-013's `!stderr.contains("Apply?")`
assertion is loose — the subprocess harness pins stdin to non-TTY, where the prompt
branch is unreachable regardless of the short-circuit. Use the existing
`ScriptedReader` + `Tty::Interactive` in-process pattern to drive a full no-op and
assert zero prompt invocations, exercising CHK-013's actual interactive contract.
</review>

<review date="2026-06-02T16:48:29-04:00" model="claude-opus-4-8[1m]/medium" persona="security" verdict="pass" round="1">
No security or crash-recovery defect in the T-006 no-op short-circuit.
`recover_orphans` runs unconditionally under the held exclusive lock
(`patina-core/src/apply/engine.rs:1083`) before the `is_full_noop` short-circuit
(`engine.rs:1108`), so a prior crash is always reversed first; the short-circuit
returns before `flush_plan_and_fsync` (`engine.rs:1138`) and writes no
`.plan`/`.COMMIT`/`.progress`/backup, leaving nothing for future recovery and keeping
the prior commit authoritative (crash-safe by construction, DEC-001/REQ-007). The
CLI-side `plan_is_full_noop` probe (`patina-cli/src/cmd/apply.rs:100`) is advisory
only — the engine re-checks under the held lock, so the prompt-skip cannot cause a
TOCTOU write/no-write divergence. `Held` policy is correctly excluded from no-op
(`engine.rs:1437`), so `remove`/`promote` always commit. The up-to-date line is a
fixed literal with no path/timestamp/PID leak (REQ-008). No new dependencies.
</review>

<review date="2026-06-02T16:48:29-04:00" model="claude-sonnet-4-6[1m]/medium" persona="style" verdict="blocking" round="1">
`patina-cli/tests/skip_if_satisfied.rs:6` — the module-level doc comment describes
only T-005's scope but T-006 added two new tests covering REQ-007 and REQ-009. The
header currently reads "Skip-if-satisfied execute behaviour (SPEC-0005 T-005,
REQ-003 / REQ-004)" and the prose only covers CHK-006/CHK-007. After T-006's
additions the file now also covers the full-noop short-circuit scenarios (CHK-011 /
REQ-007 and CHK-013 / REQ-009). The doc must be updated to reflect all requirements
the file gates, per the project convention "Docs match code." Append a note covering
the T-006 scope, e.g. that a fully-satisfied re-apply writes no new journal or backup
entries and skips the diff-and-prompt confirmation (CHK-011 / CHK-013, REQ-007 /
REQ-009).
</review>

<review date="2026-06-02T16:48:29-04:00" model="claude-opus-4-8[1m]/high" persona="correctness" verdict="pass" round="1">
T-006 full no-op short-circuit is correct. Ordering holds: `recover_orphans` runs at
`engine.rs:1083` (under the held lock, before any fresh work), `reap` is computed
before, and the `is_full_noop` short-circuit fires at line 1108 — strictly after
recovery and after `reap` is known, strictly before `flush_plan_and_fsync` (line
1138). `is_full_noop` ANDs the three conditions with correct early returns and no
inverted logic: `!reap` (Held) returns `false` first; then all-`Unchanged` over
`op.dispositions`; then a present prior commit; then empty `detect_orphans`.
`plan_is_full_noop` fixes `reap=true`, matching the CLI's `Blocking` policy. The
`Held` re-journal can never no-op, so its fresh commit is never suppressed. No
stranded crash-recovery obligation on the early return (recovery already completed,
only the read-only `decide_symlink_gate` runs between, no writes, lock/journal
released by RAII). The CLI no-op branch returns before `reporter.prompt`/
`reader.read_line` and the diff render (REQ-009). One non-blocking observation
(below the 80 bar, non-destructive): on Windows with Developer Mode toggled off
out-of-band between two identical applies, a symlink-bearing no-op surfaces a typed
`DevModeRequired` refusal rather than the up-to-date line; not reachable through the
normal flow (the elevation helper persists the registry flag) and the outcome is a
safe typed error, not wrong results or data loss.
</review>

<blockers date="2026-06-02T16:48:29-04:00" round="1">
Make the no-op tests actually gate the feature and update the test module doc before the next implementer pass.
- tests (blocking): CHK-011's `fully_satisfied_reapply_writes_no_new_journal_or_backup` (`patina-cli/tests/skip_if_satisfied.rs:93`) compares the journal-dir *basename set*, but journal filenames are second-resolution timestamps and `<ts>.COMMIT` is overwritten in place — two back-to-back applies in the same second produce an identical basename set even with a full write cycle. Disabling `is_full_noop` leaves the test green. Capture the prior `<ts>.COMMIT` mtime/bytes (and backup-cycle dir mtime) before the second apply and assert they are unchanged afterward, so removing the feature turns the test red. Also confirm no new backup *cycle* dir appears via a collision-proof signal, not a basename-set compare.
- business + tests (blocking): REQ-009 / CHK-013's interactive prompt-skip is untested. `fully_satisfied_apply_without_yes_skips_prompt_and_reports_up_to_date` runs only on the non-TTY subprocess path where the `Apply?` prompt is suppressed regardless, so `!stderr.contains("Apply?")` is vacuous. Add an in-process test driving a fully-satisfied plan with `Tty::Interactive` + a recording `Reporter` (count `prompt` invocations) + a `PromptReader`/`ScriptedReader` whose `read_line` panics if called; assert zero prompt invocations and no stdin read (the existing add/remove/promote/doctor/rollback tests show the pattern).
- style (blocking): `patina-cli/tests/skip_if_satisfied.rs:6` module doc still scopes the file to "T-005, REQ-003 / REQ-004" only; update it to also cover the T-006 full-noop short-circuit (REQ-007 / REQ-009, CHK-011 / CHK-013) per the docs-match-code convention.
</blockers>

<implementer date="2026-06-02T20:59:33Z" model="claude-opus-4-8[1m]/high" round="2">
- Reuse survey: round-2 amend in place; no full area re-map (the
  blockers are test-quality and a doc, not reuse-related). Per-symbol
  floor for the one new production symbol: `confirm_apply` /
  `Confirmation` (private, `patina-cli/src/cmd/apply.rs`) — grepped the
  crate for an existing apply-prompt decision helper and found the logic
  was inline in `run`; this extracts that existing inline `match (yes,
  tty)` (no new behaviour) into a testable seam, so it is "extend" of the
  existing decision, not a fresh policy. Test helpers: reused the
  `ScriptedReader` shape already used by `remove`/`add`/`rollback` test
  modules and `BufferReporter` (the existing recording reporter — its
  `.err` buffer counts prompt invocations); added a small
  `RecordingReader` (counts `read_line` calls) rather than a `panic!`
  reader because `clippy::panic` is `-D` even in `#[cfg(test)]` bin-crate
  modules.
- Completed: addressed all three round-1 blockers.
  (1, tests/CHK-011) Made `fully_satisfied_reapply_writes_no_new_journal_or_backup`
  collision-proof: it now captures the sole `<ts>.COMMIT` file's path,
  bytes, and mtime before the second apply and asserts all three are
  unchanged after, via a new `sole_commit_file` helper. The basename-set
  compares are kept as a secondary (catches a stray `.progress`) but are
  no longer the load-bearing signal. Verified by mutation: forcing
  `is_full_noop → Ok(false)` turns the test RED on the bytes assertion
  (the rewritten record's per-entry `skipped` disposition flag differs
  even within one wall-clock second); reverted.
  (2, business+tests/CHK-013) Added the interactive prompt-skip test.
  Refactored `run`'s inline `(yes, tty)` decision (plus the no-op skip)
  into `confirm_apply(is_full_noop, yes, tty, reader, reporter) ->
  Confirmation`; `run` skips the diff render and the prompt on a no-op
  and execute-as-no-op exactly as before. New unit test
  `full_noop_interactive_skips_prompt_and_reads_no_stdin` drives
  `is_full_noop=true` + `Tty::Interactive` + a `RecordingReader` +
  `BufferReporter`, asserting `reads == 0` and `err.is_empty()` (zero
  prompts, no stdin read). Red guard
  `non_noop_interactive_does_prompt_and_reads_the_answer` proves the
  non-no-op interactive path still prompts and reads. Added
  `yes_proceeds_without_prompting_on_any_tty` and
  `non_tty_without_yes_previews_only` for the other arms.
  (3, style) Rewrote the `skip_if_satisfied.rs` module doc into a
  T-005 section and a T-006 section covering the full-noop short-circuit
  (REQ-007 / REQ-009, CHK-011 / CHK-013).
- Undone: nothing in T-006's scope is deferred. An in-process test of the
  whole `run` (not just `confirm_apply`) remains out of reach: `run`
  resolves repo/state from process env and the workspace forbids the
  `unsafe` `set_var` in tests, so the injectable seam is `confirm_apply`,
  which carries the REQ-009 prompt-skip contract.
- Hygiene checks: ran the project gate. `cargo +nightly fmt --all
  --check` exit 0. `cargo clippy --workspace --all-targets --all-features
  --locked -- -D warnings` exit 0 (host + cross
  `x86_64-unknown-linux-gnu` + `x86_64-pc-windows-gnu`; macOS leg is CI's
  backstop). `RUSTDOCFLAGS="-D warnings" cargo doc --workspace --no-deps
  --all-features` exit 0. `cargo deny check` exit 0 (no new
  dependencies). `cargo test --workspace --locked` exit 0 (all suites,
  including the four new `cmd::apply::tests::*` unit tests and the
  strengthened `skip_if_satisfied` integration test).
- Evidence: red-then-green paper trail updated at
  `.speccy/specs/0005-patina-skip-if-satisfied/evidence/T-006.md` (Scenario 1
  gains a round-2 mutation note; Scenario 3 rewritten for the in-process
  interactive test). Roll call for the three CHKs under REQ-007 / REQ-008 /
  REQ-009:
  - CHK-011 (no new `*.plan` / `*.COMMIT` / backup cycle on a
    fully-satisfied re-apply): demonstrated → evidence Scenario 1; the
    collision-proof commit identity (path + bytes + mtime) assert turns
    red under the `is_full_noop → false` mutation.
  - CHK-012 (two no-op applies byte-identical, both carry the up-to-date
    message): demonstrated → evidence Scenario 2
    (`fully_satisfied_applies_are_byte_identical_and_report_up_to_date`,
    unchanged this round).
  - CHK-013 (no prompt invocation, completes without reading stdin):
    demonstrated → evidence Scenario 3; the in-process unit test
    `full_noop_interactive_skips_prompt_and_reads_no_stdin` asserts zero
    prompt text and zero stdin reads on the interactive TTY, with the
    `non_noop_interactive_does_prompt_and_reads_the_answer` red guard.
  No CHK in T-006's scope is `judgment-only`.
- Discovered issues: none. The `clippy::panic` lint fires in `#[cfg(test)]`
  bin-crate modules, so the recording-reader (count + assert zero) is the
  lint-clean equivalent of the blocker's "panics if called" reader; the
  count assertion fails on a single stray read just as a panic would.
- Procedural compliance: this entry is appended to `journal/T-006.md`
  after the round-1 `<blockers>` (append-only; earlier blocks untouched).
  No `<implementer-note>` was written into TASKS.md. The dirty round-1 WIP
  was amended in place (no `git restore`/`clean`/`checkout`). The TASKS.md
  `state` for T-006 flips from `pending` to `in-review` as the final step.
  No shipped skill bodies under `.claude/skills/speccy-*` required edits.
</implementer>

<review date="2026-06-02T17:03:18-04:00" model="claude-opus-4-8[1m]/high" persona="business" verdict="pass" round="2">
Round-1 blocker resolved: REQ-009's interactive prompt-skip is now genuinely gated.
The implementer factored the inline `(yes, tty)` decision into a
`confirm_apply(is_full_noop, yes, tty, reader, reporter) -> Confirmation` seam
(`patina-cli/src/cmd/apply.rs`) and added the in-process unit test
`full_noop_interactive_skips_prompt_and_reads_no_stdin`, which drives
`is_full_noop=true` + `Tty::Interactive` with a `RecordingReader` (counts
`read_line`) and a `BufferReporter` (whose `prompt` impl writes to `.err`),
asserting `reads == 0` and `err.is_empty()` — exactly CHK-013's "records zero prompt
invocations ... run completes" against the interactive TTY path. Non-vacuous:
deleting the `is_full_noop` early-return routes into the `(false, Tty::Interactive)`
arm, firing `reporter.prompt` and `reader.read_line` and turning both assertions
red. The seam is faithfully wired — `run` computes `is_full_noop` via
`plan_is_full_noop(&resolved)` and passes it to `confirm_apply`. REQ-009's second arm
(a Create/Update plan still prompts) is guarded by
`non_noop_interactive_does_prompt_and_reads_the_answer`. REQ-007/REQ-008 remain
correct; no scope creep, no silently-resolved open questions, no non-goal violations.
</review>

<review date="2026-06-02T17:03:18-04:00" model="claude-opus-4-8[1m]/high" persona="tests" verdict="pass" round="2">
Round-2 fixes for both round-1 blockers are non-vacuous; mutation analysis confirms
each test fails when the corresponding production behavior is removed. Blocker 1
(CHK-011 no-write): `fully_satisfied_reapply_writes_no_new_journal_or_backup` now
captures the sole `<ts>.COMMIT` path + bytes + mtime via `sole_commit_file` before
the second apply and asserts all three unchanged. Collision-proof: `ExpectedTarget`
carries a `disposition` on the postcard wire, so forcing `is_full_noop → Ok(false)`
rewrites `<ts>.COMMIT` with `disposition: Unchanged` vs the first apply's `Create` —
payload bytes genuinely differ even within one wall-clock second, turning the bytes
assertion red; if the applies straddle a second boundary, a second `<ts>.COMMIT`
appears and both the basename-set and `commits.len() == 1` assertions go red. Robust
in both timing branches. Blocker 2 (CHK-013 interactive prompt-skip): the `(yes,
tty)` decision was extracted into `confirm_apply` and is exercised in-process by
`full_noop_interactive_skips_prompt_and_reads_no_stdin` (`reads == 0`,
`err.is_empty()`); `non_noop_interactive_does_prompt_and_reads_the_answer` scripts
`"n\n"` and asserts `Declined` + `err.contains("Apply?")`, so the skip cannot be a
blanket never-prompt. Evidence roll call complete: CHK-011/012/013 all `demonstrated`
with matching scenario blocks; all named test functions exist in the diff, red/green
narratives are structurally distinct, and the evidence command is scoped per-test.
</review>

<review date="2026-06-02T17:03:18-04:00" model="claude-opus-4-8[1m]/medium" persona="security" verdict="pass" round="2">
No security or crash-recovery regression in the T-006 round-2 delta. The
`confirm_apply` seam is a verbatim extraction of the round-1 inline `(yes, tty)`
decision with the no-op short-circuit folded in; branch arms and the `y`/`Y`
prompt-match are byte-identical, so no authz, input-validation, or control-flow
semantics changed. The no-op `Confirmation::Proceed` is safe because the engine
writes nothing on a no-op. The engine ordering is unchanged this round —
`recover_orphans` (`patina-core/src/apply/engine.rs:1083`) → symlink gate →
`is_full_noop` (`engine.rs:1108`) → return before `flush_plan_and_fsync`
(`engine.rs:1138`) — so a prior crash is still reversed first and the prior commit
stays authoritative; the CLI `plan_is_full_noop` probe remains advisory with the
engine re-checking under the held lock, so no TOCTOU prompt-skip/write divergence.
Test infra (`RecordingReader`, `ScriptedReader`, `BufferReporter`) is
`#[cfg(test)]`-only; `BufferReporter::prompt` writes to `.err`, so the zero-prompt
assertion is sound. No new dependencies; up-to-date line is a fixed literal with no
leak.
</review>

<review date="2026-06-02T17:03:18-04:00" model="claude-sonnet-4-6[1m]/medium" persona="style" verdict="pass" round="2">
Round-1 blocker resolved. `patina-cli/tests/skip_if_satisfied.rs` lines 6–26 now
carry two clearly separated sections: "Per-entry skip (T-005, REQ-003 / REQ-004;
CHK-006 / CHK-007)" and "Full-no-op short-circuit (T-006, REQ-007 / REQ-009; CHK-011
/ CHK-013)". The doc correctly notes the interactive prompt-skip is unit-tested in
`apply.rs` while the subprocess suite covers the no-write property — matching the
code. No new style regressions in the round-2 follow-on code: `confirm_apply` and
`Confirmation` use no `println!`/`eprintln!`, no bare `.unwrap()` in production, all
output routes through `Reporter`. `RecordingReader` and `ScriptedReader` are both
`#[cfg(test)]`-scoped inside `mod tests` and match the identical per-module
test-helper pattern used in every sibling `cmd/*.rs` file — no reinvention. Path
types are `&Utf8Path` throughout the new helpers.
</review>

<review date="2026-06-02T17:03:18-04:00" model="claude-opus-4-8[1m]/high" persona="correctness" verdict="pass" round="2">
Round-2 refactor is behavior-preserving and the engine ordering is intact.
`confirm_apply` (`patina-cli/src/cmd/apply.rs:152`) reproduces the round-1 inline
`(yes, tty)` decision across all four paths with no inverted condition or dropped
arm: full no-op returns `Proceed` via the early `if is_full_noop` before the
`(yes, tty)` match, so the interactive path neither calls `reporter.prompt` nor
`reader.read_line` (REQ-009); `--yes` still proceeds without reading stdin via
`(true, _)`; non-TTY-without-`--yes` maps to `PreviewOnly`→exit 0; interactive
prompts, reads, and proceeds only on `y`/`Y`. The diff render stays gated to
`!is_full_noop`. Engine ordering holds: lock → `recover_orphans` (engine.rs:1083) →
read-only symlink gate → `is_full_noop(resolved, reap)?` short-circuit
(engine.rs:1108) returning before `flush_plan_and_fsync` (engine.rs:1138). `reap` is
computed at engine.rs:1061; `LockPolicy::Held` yields `reap=false`, and
`is_full_noop` returns `false` first on `!reap`, so a `Held` re-journal can never
no-op. Skipping the gated `reap_orphans` on the no-op path is sound because
`is_full_noop` already requires `detect_orphans` empty. The new `up_to_date` field is
set `true` only on the short-circuit and `false` on a real commit; all other
`ApplyResult::Applied` consumers destructure with `{ .. }`. No correctness finding at
or above the confidence bar.
</review>
