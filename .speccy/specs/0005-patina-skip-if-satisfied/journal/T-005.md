---
spec: SPEC-0005
task: T-005
generated_at: 2026-06-02T20:30:00Z
---

<implementer date="2026-06-02T20:30:00Z" model="claude-opus-4-8[1m]/high" round="1">
- Reuse survey: Mapped the task area (covered REQ-003 / REQ-004,
  suggested files `apply/engine.rs`, `apply/copy.rs`, `apply/symlink.rs`,
  and the new `patina-cli/tests/skip_if_satisfied.rs`) and its immediate
  neighbours (`apply/mod.rs` `materialize`, `backups/mirror.rs`,
  `journal/record.rs`). Decisions:
  reuse-as-is — `backup_before_overwrite` (only stashes a pre-existing
  target, so it is already a no-op for an absent `Create`), `materialize`
  (single-target executors), `content_hash` (`record.rs`),
  `walk_files` (the executors' sorted leaf walk, reused to enumerate a
  fresh tree's leaves in `build_apply_record`), `ExpectedTarget::{Symlink,
  Content}`, the T-004 `TargetDisposition` / `LeafDisposition` carriers on
  `ResolvedOperation`, `mirror_backup_path` (reused in the test to locate a
  backup exactly, not re-derive the layout).
  Extend — `copy_tree` / `tree_symlink` grew a `write: LeafWrite<'_>`
  parameter for the per-leaf write-skip; the existing `materialize`
  dispatch and the executor unit tests pass `LeafWrite::All`, preserving
  today's behaviour. The execute materialize loop now iterates
  `(target, disposition)` pairs instead of backing up + materializing all
  targets unconditionally. `build_apply_record` was reworked to source
  every `ExpectedTarget` from the resolved plan (so `Unchanged` targets
  that produced no `CompletionRecord` are still recorded) and dropped its
  `&completed` parameter.
  Write-fresh — three new top-level symbols, each grepped for first and
  found absent: `LeafWrite` (`apply/mod.rs`) — the relative-leaf write
  filter the tree executors consult; searched `apply` for an existing
  leaf-selection type and found only the all-leaves walk. `materialize_tree`
  (`apply/mod.rs`) — a `pub(crate)` engine-facing dispatch that routes a
  tree target to `copy_tree` / `tree_symlink` with its `LeafWrite`, keeping
  the executor calls localized in `mod.rs` rather than reaching into
  `pub(super)` items from the engine. `materialize_target` (`engine.rs`) —
  the per-target write-skip decision (Unchanged → skip; single-target →
  back-up + materialize; tree → whole-dir backup + drifted-leaf write);
  grepped the engine for an existing per-target skip seam and found only
  the unconditional loop. `expected_target` / `record_tree_targets`
  (`engine.rs`) are the plan-sourced commit-record builders; the prior
  `build_apply_record` body folded straight over `completed` with no
  per-target/per-leaf helper.
- Completed: Implemented REQ-003 and REQ-004. (1) Execute write/backup
  skip: the `execute` materialize loop now pairs each `op.targets` entry
  with its `op.dispositions` entry and calls `materialize_target`. An
  `Unchanged` aggregate returns before any backup or write (inode/mtime
  preserved, no backup entry, no `CompletionRecord`). A single-target
  `Create`/`Update` is backed up (a no-op for absent `Create`) and
  materialized as today. A tree `Create`/`Update` backs up the target
  directory as a unit (DEC-007) and then writes only the leaves whose
  per-leaf disposition is not `Unchanged` via `materialize_tree` +
  `LeafWrite::Only`; a `Create` tree (no per-leaf entries) writes every
  leaf via `LeafWrite::All`. The per-leaf write-skip lives in `copy_tree`
  and `tree_symlink`, which now skip a leaf not in the `LeafWrite` set.
  (2) Commit record: `build_apply_record` is rebuilt from `resolved.
  operations`, emitting one `ExpectedTarget` per single-target and one per
  tree leaf (walking the source for a fresh `Create` tree, otherwise using
  the plan-time per-leaf dispositions), each carrying its real
  `Disposition`. Content hashes are read back from the live target (correct
  for `Create`/`Update` just-written and `Unchanged` matching targets,
  matching what `status` computes). This keeps `Unchanged` targets in the
  commit so `status` reports them `Clean` and `reap_orphans` never removes
  them. Removed the now-dead `PLACEHOLDER_DISPOSITION` constant. Added
  per-leaf write-skip executor unit tests and three
  `patina-cli/tests/skip_if_satisfied.rs` integration tests (CHK-006, the
  DEC-007 tree path, CHK-007).
- Undone: Nothing in T-005's scope is deferred. The full no-op
  short-circuit (no plan/commit/backup when every target is Unchanged and
  the reap set is empty), the up-to-date stdout/prompt-skip, crash-recovery
  and rollback's `Unchanged` arms, the diff omission, and the `--json`
  `state` field are T-006/T-007/T-008/T-009/T-010 respectively and are
  explicitly out of T-005's scope. T-005 deliberately still writes the
  plan/commit/backup cycle for an all-Unchanged apply (the COMMIT now
  records every target as `Unchanged`); the write-suppression is T-006.
- Hygiene checks: `just lint` exited 0 — nightly `fmt --all --check`
  (exit 0), clippy `--workspace --all-targets --all-features --locked`
  cross-compiled to `x86_64-unknown-linux-gnu` and `x86_64-pc-windows-gnu`
  (exit 0; macOS target skipped off a Mac per the justfile), docs
  `RUSTDOCFLAGS="-D warnings" cargo doc` (exit 0), and `cargo deny check`
  (advisories/bans/licenses/sources ok; pre-existing transitive
  duplicate-crate noise only, no new dependency). `just test`
  (`cargo test --workspace --locked`) exited 0 — all green (59
  `test result: ok` blocks, 0 FAILED), including the new executor and
  integration tests. Per AGENTS.md the gate is `just check`
  (= `just lint` + `just test`), run here so the docs gate is not dropped.
  Captured lint output to a path outside the repo tree, so no temp log was
  swept into the working tree (`git status --porcelain` shows only the four
  edited sources and the new test file).
- Evidence: red-then-green paper trail at
  `.speccy/specs/0005-patina-skip-if-satisfied/evidence/T-005.md`.
  Roll call for the CHKs under T-005's covered REQs (REQ-003 / REQ-004):
  - CHK-006 (one of two entries drifts: untouched entry's mtime unchanged
    and no backup entry for it; drifted entry updated and its prior bytes
    backed up): demonstrated → evidence Scenario 1
    (`unchanged_entry_is_not_rewritten_or_backed_up_while_drift_is`), with
    the skip-disabled mutation flipping it red.
  - CHK-007 (Update + Unchanged re-apply: `status` reports the Unchanged
    target `Clean` and present, and a later reap does not remove it):
    demonstrated → evidence Scenario 3
    (`unchanged_entry_is_recorded_clean_and_survives_reap`), with the
    drop-Unchanged-from-record mutation flipping it red.
  The DEC-007 tree write-skip (not its own CHK, but load-bearing for
  REQ-003 on tree modes) is demonstrated by evidence Scenario 2: the
  exact per-leaf assertion lives in the `copy_tree` / `tree_symlink`
  executor unit tests (an unselected leaf is never written), and the
  whole-tree-backup-as-a-unit path is driven end-to-end by
  `copy_tree_re_apply_restores_drift_and_backs_up_the_tree_as_a_unit`. No
  CHK in T-005's scope is hygiene-only or judgment-only; every one is
  scriptable and demonstrated.
- Discovered issues: None. `build_apply_record`'s ordering is now plan
  order (deterministic) rather than apply-completion order; both are
  deterministic, and `status`/`reap` key by path/entry-index, not list
  position, so no behaviour depends on the change.
- Procedural compliance: This implementer entry lands directly in
  `journal/T-005.md` per the journal-file schema; no `<implementer-note>`
  was written into TASKS.md. The TASKS.md `state` for T-005 flips from
  `pending` to `in-review` as the final step of this turn. No shipped skill
  bodies under `.claude/skills/` were edited.
</implementer>

<review date="2026-06-02T16:21:14-04:00" model="claude-opus-4-8[1m]/high" persona="business" verdict="pass" round="1">
T-005 (REQ-003 / REQ-004) delivers exactly what the SPEC promises, with no scope
creep and no premature resolution of deferred work. REQ-003 `<done-when>` fully
mapped: `materialize_target` (`patina-core/src/apply/engine.rs`) returns before any
`backup_before_overwrite`/`materialize` for an `Unchanged` aggregate, so inode/mtime
are preserved and no backup entry is written; single-target Create/Update back up +
materialize as today; tree Update backs up the directory as a unit (DEC-007) and
rewrites only drifted leaves via `LeafWrite::Only`. CHK-006 is genuinely tested
(`unchanged_entry_is_not_rewritten_or_backed_up_while_drift_is`). REQ-004
`<done-when>` fully mapped: `build_apply_record` now iterates `resolved.operations`
(sourced from the plan, not from `completed`) so every managed target — including
skipped `Unchanged` ones — is recorded with its real per-leaf disposition; the
recorded `ExpectedTarget` content is byte-equivalent to the prior path so `status`
reports `Clean` and `reap_orphans` never removes it. CHK-007 is genuinely tested
(`unchanged_entry_is_recorded_clean_and_survives_reap`). The no-op short-circuit,
recovery/rollback Unchanged arms, diff omission, and `--json state` are correctly
left to T-006–T-010. Non-goals respected; SPEC has no open questions.
</review>

<review date="2026-06-02T16:21:14-04:00" model="claude-opus-4-8[1m]/high" persona="tests" verdict="pass" round="1">
T-005 tests genuinely exercise REQ-003/REQ-004 behaviour. Evidence roll call is
complete: CHK-006 and CHK-007 (the only CHKs under REQ-003/REQ-004) are both
`demonstrated` with named tests and red-mutation proofs; no fabrication patterns
matched (scoped per-test commands, real runner artifacts, materially different
red/green output, all named tests present in the diff).
`patina-cli/tests/skip_if_satisfied.rs` drives the real `patina` binary as a
subprocess, so nothing is mocked. CHK-006 gates the write/backup skip with a
backup-absence assertion that is robust against mtime-resolution flakes and provably
non-vacuous (pre-T-005 backed up every pre-existing target). CHK-007 gates `status`
reporting the Unchanged target `Clean`/present, which depends on the commit record
per REQ-004 and flips red under the drop-Unchanged-from-record mutation. The DEC-007
per-leaf no-touch is gated exactly by `copy_tree_only_writes_selected_leaves` /
`tree_symlink_only_links_selected_leaves` asserting the unselected leaf is never
written. Minor, non-blocking: the CHK-007 third-apply reap-survival assertion is
weaker than it reads (`keep_out` stays in the current managed set so reap could
never remove it regardless), but it is an extra check beyond the scenario text and
the load-bearing status-`Clean` assertion carries the requirement.
</review>

<review date="2026-06-02T16:21:14-04:00" model="claude-opus-4-8[1m]/high" persona="security" verdict="pass" round="1">
No security concerns in SPEC-0005/T-005. The change is local-filesystem
materialization with no auth/authz, network, secret, or crypto surface. Skipping
backup+write for `Unchanged` targets is sound (nothing overwritten = no prior state
to preserve; never-clobber and rollback fidelity are unaffected since the target is
byte-identical to desired state and still recorded in the commit). The live
read-back-for-hash in `expected_target`/`record_tree_targets`
(`patina-core/src/apply/engine.rs`) preserves the pre-existing pattern and runs
under the held exclusive advisory lock, so it introduces no new TOCTOU window.
`content_hash` (blake3) is unchanged, no new dependencies, and no `tracing`/print
macros were added so no file contents or paths leak into logs or error bodies.
</review>

<review date="2026-06-02T16:21:14-04:00" model="claude-sonnet-4-6[1m]" persona="style" verdict="blocking" round="1">
`patina-core/src/apply/engine.rs:962` and `patina-core/src/apply/mod.rs:282` both
declare a `target` parameter as `&Utf8PathBuf` instead of `&Utf8Path`.

Every other path-reading parameter in the same files uses the idiomatic borrowed
form — for example `engine.rs` lines 743, 910, 1282, 1321 all take
`target: &Utf8Path`. The project's code-quality rule (`rust-code-quality.md`
"Prefer borrowed types when you don't need ownership") and `AGENTS.md` ("Paths:
`camino::Utf8PathBuf` and `camino::Utf8Path` everywhere") make `&Utf8Path` the
required form for a read-only path parameter. The callers pass `&Utf8PathBuf` which
coerces to `&Utf8Path` automatically, so no call-site changes are needed — only the
two function signatures (`materialize_target` and `materialize_tree`) need updating
to `target: &Utf8Path`.
</review>

<review date="2026-06-02T16:21:14-04:00" model="claude-opus-4-8[1m]/high" persona="correctness" verdict="pass" round="1">
No correctness defects found at or above the reporting threshold. The execute
write/backup-skip (`materialize_target`, `engine.rs:959`) early-returns for an
`Unchanged` aggregate before any backup or write, preserving inode/mtime and
producing no `CompletionRecord` (progress cursor correctly not advanced). The
per-leaf write-skip in `copy_tree` (`copy.rs:85`) and `tree_symlink` (`symlink.rs:122`)
gates on `LeafWrite::includes` before `ensure_parent`/write; the `Update` `drifted`
set is non-empty by the aggregate's definition and includes `Create` leaves (filter
`!= Unchanged`). `op.targets.iter().zip(&op.dispositions)` is safe — `dispositions`
is built one-per-target in `classify_entry`, parallel by construction, so the zip
cannot silently truncate. `build_apply_record` now sources every target — including
`Unchanged` ones — from `resolved.operations` with an exhaustive `FileMode` match;
the symlink `link_target` (= per-leaf source) and content live-read hash match the
pre-change `CompletionRecord`-derived values. `reap_orphans` is unchanged and reads
the prior commit before this run's commit is written; because the new commit now
records `Unchanged` targets, a future run's reap skips them (exercised end-to-end by
the third-apply test). No read-after-delete.
</review>

<blockers date="2026-06-02T16:21:14-04:00" round="1">
Switch two read-only path parameters from `&Utf8PathBuf` to `&Utf8Path` before the next implementer pass.
- style (blocking): `materialize_target` (`patina-core/src/apply/engine.rs:962`) and `materialize_tree` (`patina-core/src/apply/mod.rs:282`) declare `target: &Utf8PathBuf`, diverging from the project's mandated borrowed-path form (`&Utf8Path`, per AGENTS.md and `rust-code-quality.md`) that every sibling path parameter in these files already uses. Change both signatures to `target: &Utf8Path`; callers coerce automatically, so no call-site edits are needed.
</blockers>

<implementer date="2026-06-02T17:05:00-04:00" model="claude-opus-4-8[1m]/high" round="2">
- Reuse survey: This retry addresses a non-reuse style blocker, so the
  full round-1 area-map is not re-run. No new top-level symbol is
  introduced, so the per-symbol floor has nothing new to account for —
  the change is purely two signature narrowings plus the slice-builder
  adjustments their callers needed.
- Completed: Addressed the round-1 style blocker. Narrowed the two
  read-only path parameters from `&Utf8PathBuf` to `&Utf8Path`:
  `materialize_target` (`patina-core/src/apply/engine.rs`) and
  `materialize_tree` (`patina-core/src/apply/mod.rs`), matching the
  borrowed-path form every sibling path parameter in these files already
  uses (AGENTS.md / `rust-code-quality.md`). Two internal call sites that
  build a one-element `&[Utf8PathBuf]` slice from the parameter had to be
  adjusted because `copy_tree` / `tree_symlink` / `materialize` take
  `&[Utf8PathBuf]` (an owned-element slice, not a coercing call site): in
  `materialize_tree` the `std::slice::from_ref(target)` became a
  `let targets = [target.to_path_buf()];` then `&targets`; in
  `materialize_target`'s single-target arm the `from_ref(target)` became
  `from_ref(&target.to_path_buf())`. No behaviour change — the executor
  signatures are untouched and the materialized targets are identical.
- Undone: Nothing. The blocker's two-signature scope is fully addressed;
  the round-1 deferral set (T-006–T-010) is unchanged.
- Hygiene checks: `just lint` exited 0 — nightly `fmt --all --check`,
  clippy `--workspace --all-targets --all-features --locked`
  cross-compiled to `x86_64-unknown-linux-gnu` and
  `x86_64-pc-windows-gnu` (macOS skipped off a Mac per the justfile),
  docs `RUSTDOCFLAGS="-D warnings" cargo doc`, and `cargo deny check` all
  clean. `just test` (`cargo test --workspace --locked`) exited 0 — 59
  `test result: ok` blocks, 0 FAILED. Lint/test output was captured to
  `$TEMP` outside the repo; `git status --porcelain` shows only the four
  edited sources, the TASKS.md state flip, and the round-1 evidence /
  journal / integration-test files — no temp log was swept in.
- Evidence: no new evidence file for this round — the round-1 paper trail
  at `.speccy/specs/0005-patina-skip-if-satisfied/evidence/T-005.md`
  still applies unchanged, since this retry is a type-signature narrowing
  with no behaviour delta. CHK roll call (REQ-003 / REQ-004) is unchanged
  from round 1:
  - CHK-006 (untouched entry not rewritten/backed-up while drift is):
    demonstrated → round-1 evidence Scenario 1, still green.
  - CHK-007 (Unchanged target recorded `Clean` and survives reap):
    demonstrated → round-1 evidence Scenario 3, still green.
- Discovered issues: None. Worth noting for the reviewer: the blocker's
  "callers coerce automatically, so no call-site edits are needed" held
  for true call sites, but the two `std::slice::from_ref` uses construct
  a `&[Utf8PathBuf]` slice from the parameter and so are not coercion
  points — they needed an explicit `to_path_buf()` to rebuild the owned
  element. This is a one-element allocation per single-target / per-tree
  materialize, on the write path only, and matches the existing pattern
  of passing an owned-`Utf8PathBuf` slice to the executors.
- Procedural compliance: Retry-shape pass — amended the existing
  uncommitted WIP in place (no `git restore`/`clean`/`checkout`, no
  rewrite-from-scratch, no state reset beyond the normal
  in-progress → in-review flip). This block lands directly in
  `journal/T-005.md` as `round="2"` (round-1 highest + 1); no
  `<implementer-note>` was written into TASKS.md. No shipped skill bodies
  under `.claude/skills/` were edited.
</implementer>

<review date="2026-06-02T16:28:00-04:00" model="claude-opus-4-8[1m]/high" persona="business" verdict="pass" round="2">
T-005 (REQ-003 / REQ-004) delivers exactly the SPEC's scope for round 2; the
round-1 style blocker was a behavior-neutral `&Utf8PathBuf`→`&Utf8Path` narrowing
on `materialize_target`/`materialize_tree` and changes no business mapping. REQ-003
`<done-when>` fully mapped: `materialize_target` returns before any
`backup_before_overwrite`/`materialize` on an `Unchanged` aggregate (inode/mtime
preserved, no backup entry); single-target Create/Update back up + materialize; tree
Update backs up the directory as a unit (DEC-007) and rewrites only drifted leaves
via `LeafWrite::Only`. CHK-006 genuinely tested by
`unchanged_entry_is_not_rewritten_or_backed_up_while_drift_is`. REQ-004
`<done-when>` fully mapped: `build_apply_record` iterates `resolved.operations`
instead of `completed`, recording every managed target — including skipped Unchanged
ones — with its real per-leaf disposition; CHK-007
(`unchanged_entry_is_recorded_clean_and_survives_reap`) drives the real binary
through `status`→`Clean` and a reap-survival re-apply. No scope creep: the no-op
short-circuit (REQ-007/T-006), recovery/rollback arms, diff omission, and
`--json state` are correctly deferred; the plan/commit/backup cycle still runs for
an all-Unchanged apply. Non-goal "no change to reap policy" respected. No open
question silently resolved.
</review>

<review date="2026-06-02T16:28:00-04:00" model="claude-opus-4-8[1m]/high" persona="tests" verdict="pass" round="2">
T-005 tests genuinely exercise REQ-003/REQ-004 behaviour through the real engine.
Evidence roll call is complete: CHK-006 and CHK-007 (the only scenario CHKs under
REQ-003/REQ-004) are both `demonstrated` with named tests present in the
working-tree diff and materially-different red/green runner output; no fabrication
pattern matched (scoped per-test invocations, real panic locations/messages, not the
workspace-wide `just test`). `patina-cli/tests/skip_if_satisfied.rs` drives the real
`patina` binary as a subprocess. CHK-006
(`unchanged_entry_is_not_rewritten_or_backed_up_while_drift_is`) gates the
write/backup skip with an mtime-preservation assertion plus a backup-absence
assertion provably non-vacuous against the pre-T-005 always-backup behaviour and the
cited red mutation. CHK-007 gates `status` reporting the Unchanged target
`Clean`/present, which depends on `build_apply_record` recording it and flips red
under the drop-Unchanged-from-record mutation. The DEC-007 per-leaf no-touch is
gated exactly by `copy_tree_only_writes_selected_leaves` /
`tree_symlink_only_links_selected_leaves`. The round-2 delta is a behavior-preserving
signature narrowing with the `from_ref` sites rebuilt via `to_path_buf()`; no test
surface changed and the round-1 evidence remains valid. Non-blocking carry-over:
CHK-007's third-apply reap-survival assertion is weaker than it reads but the
load-bearing status-`Clean` assertion carries the requirement.
</review>

<review date="2026-06-02T16:28:00-04:00" model="claude-opus-4-8[1m]/high" persona="security" verdict="pass" round="2">
No security concerns in SPEC-0005/T-005 (round 2). The change is local-filesystem
materialization with no auth/authz, network, secret, or crypto surface. Skipping
backup+write for `Unchanged` targets is sound — nothing is overwritten so there is
no prior state to lose, and never-clobber/rollback fidelity hold because the target
is byte-identical to desired state and still recorded in the commit (REQ-004). The
live read-back-for-hash in `expected_target`/`record_tree_targets` (`fs_err::read`)
preserves the pre-existing pattern and runs under the held exclusive advisory lock,
so it opens no new TOCTOU window. `content_hash` (blake3) unchanged, no new
dependencies, and no `tracing`/print macros added in the diff (the lone `warn!` at
`engine.rs:1083` is pre-existing and untouched), so no file contents or paths leak.
The tree per-leaf `relative` paths come from `walk_files(source)` over the user's
own repo joined onto the user's own resolved target — same trust domain, no new
path-traversal vector. The round-2 delta (narrowing to `&Utf8Path` plus the two
`to_path_buf()` owned-slice rebuilds) is a pure type-signature change with no
security impact.
</review>

<review date="2026-06-02T16:28:00-04:00" model="claude-sonnet-4-6[1m]" persona="style" verdict="pass" round="2">
Round-1 blocker resolved: both `materialize_target` (`patina-core/src/apply/engine.rs:962`)
and `materialize_tree` (`patina-core/src/apply/mod.rs:282`) now declare
`target: &Utf8Path`, matching every sibling path parameter in those files. The two
`to_path_buf()` calls the implementer added at the `std::slice::from_ref` sites are
the correct adaptation — `copy_tree`, `tree_symlink`, and `materialize` all accept
`&[Utf8PathBuf]` (owned-element slices), so a `.to_path_buf()` to rebuild the owned
element is the right idiom and matches the existing pattern. No new naming drift,
dead imports, suppression annotations, or parallel helpers introduced in round 2.
</review>

<review date="2026-06-02T16:28:00-04:00" model="claude-opus-4-8[1m]/high" persona="correctness" verdict="pass" round="2">
No correctness defects at or above the reporting threshold; round-2 change is
behavior-preserving. The two parameter narrowings to `target: &Utf8Path` and the two
new owned-slice builders are sound and behavior-identical to round 1:
`materialize_tree`'s `let targets = [target.to_path_buf()]` coerces to the executors'
`&[Utf8PathBuf]` (same length-1 slice, same path string), and `materialize_target`'s
`std::slice::from_ref(&target.to_path_buf())` keeps the temporary alive to the end of
the consuming `materialize(...)` statement (no dangling borrow). Invariants
re-confirmed: backup-skip for Unchanged early-returns before any backup or write
(inode/mtime preserved, no `CompletionRecord`); per-leaf write-skip `continue`s on
`!write.includes(rel)` with `LeafWrite::Only(&drifted)` outliving the call;
`build_apply_record` iterates `resolved.operations` so every target — including
skipped Unchanged ones — yields an `ExpectedTarget` with its real disposition and a
live-read hash, and `reap_orphans` (reading the prior commit) never removes a
recorded Unchanged target. The non-tree arm of `materialize_tree` is an unreachable
defensive arm, not a defect.
</review>
