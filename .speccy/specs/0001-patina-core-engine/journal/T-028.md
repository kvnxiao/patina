---
spec: SPEC-0001
task: T-028
generated_at: 2026-05-30T19:00:00Z
---

<implementer date="2026-05-30T19:00:00Z" model="claude-opus-4-8[1m]/low" round="1">
- Completed: Moved orphan recovery under the exclusive lock
  (acquire-then-recover) in `patina-core/src/apply/engine.rs`. The
  `recover_orphans(&journal_dir, &backups_dir)?` call now runs AFTER
  the `let _guard = match policy { ... }` block instead of before it.
  For `Blocking` and `Held` the recovery now runs with the lock held;
  for `NonBlocking`, `try_acquire_lock` returns the typed contention
  error before `recover_orphans` is ever reached, so a contended
  attempt mutates nothing — no recovery, no plan, no COMMIT, no
  backup. Updated the `execute` doc comment and the inline comment to
  describe the reorder and why it upholds REQ-030's zero-write
  guarantee and prevents a second apply from reversing a live
  in-flight apply's operations (REQ-013). `run_rollback`
  (`patina-core/src/rollback/mod.rs`) was verified untouched: it does
  its own `acquire_lock(.., Exclusive, ..)` and does not call
  `recover_orphans`. Added a CHK-068 unit test
  (`non_blocking_contention_leaves_pending_orphan_untouched`) in the
  `mod tests` module of `engine.rs` plus a `Scene::plant_orphan`
  helper that plants a pending orphan `<ts>.plan` + `<ts>.progress`;
  the test holds the exclusive lock, drives a `NonBlocking` apply, and
  asserts the contention error, the orphan bytes left untouched, and
  zero new plan/COMMIT/backup writes.

  Secondary deliverable (test-only): hardened the load-dependent flaky
  test `blocked_exclusive_exits_with_lock_timeout_code` in
  `patina-core/tests/lock_concurrency.rs` (F2). Replaced the fixed
  `std::thread::sleep(150ms)` head-start with a `wait_for_acquired`
  helper that reads the holder process's piped stdout until its
  `ACQUIRED` marker line, then spawns a background thread to drain the
  rest of the holder's stdout (so its later `RELEASED` write does not
  hit a broken pipe). The contender now launches only after the
  holder's *observed* acquisition, making the exit-4 timeout outcome
  deterministic under machine load. No production behaviour change.

- Undone: Nothing in T-028's scope is deferred. The watcher
  `NonBlocking` call-site wiring (SPEC-0003) and remove/promote `Held`
  wiring (SPEC-0002) remain owned by those SPECs and are out of scope.

- Hygiene checks: `cargo test --workspace` → exit 0. `cargo clippy
  --workspace --all-targets --all-features -- -D warnings` → exit 0.
  `cargo +nightly fmt --all --check` → exit 0. `cargo deny check` →
  exit 0 (no new dependencies).

- Evidence: paper trail at
  `.speccy/specs/0001-patina-core-engine/evidence/T-028.md`. Roll call
  for the CHKs under the covered REQs (REQ-013, REQ-030):
  - CHK-068 (NonBlocking contention with a pending orphan returns the
    typed error, leaves the orphan plan/progress untouched, and writes
    no new plan/COMMIT/backup): demonstrated → evidence Scenario 1
    (`non_blocking_contention_leaves_pending_orphan_untouched`).
  - CHK-024 (REQ-013 N-of-M crash recovery restores targets and clears
    orphan files): hygiene → recovery semantics are unchanged by the
    reorder; the pre-existing `patina-core/tests/recovery_crash.rs`
    suite drives `recover_orphans` directly and stays green under
    `cargo test --workspace`.
  - CHK-065 (NonBlocking contention returns the typed error, writes no
    journal): hygiene → the pre-existing
    `non_blocking_apply_on_contended_lock_errors_and_writes_no_journal`
    test in `engine.rs` `mod tests` still passes under the workspace
    test command.
  - CHK-066 (Held policy commits against the caller's guard): hygiene →
    the pre-existing `held_policy_applies_with_callers_guard_and_commits`
    test still passes; recovery now runs under the held guard.
  - CHK-067 (default `Blocking` byte-identical stdout across two
    applies): hygiene → `patina-cli/tests/deterministic_stdout.rs`
    drives `patina apply --yes` end-to-end on the `Blocking` path and
    asserts byte-identical stdout; for one process the reorder is
    observably identical (recovery still precedes the plan flush).

- Discovered issues: none new. The previously-documented flaky test
  `blocked_exclusive_exits_with_lock_timeout_code` (F2 from the
  amendment's verification pass) is fixed by this task's secondary
  deliverable; it ran green three consecutive times after the change.

- Procedural compliance: this entry lands directly in
  `journal/T-028.md`; no `<implementer-note>` was written into
  TASKS.md. The TASKS.md `state` for T-028 flips from `in-progress` to
  `in-review` as the final step of this turn. No shipped skill bodies
  under `.claude/skills/` were edited.
</implementer>

<review date="2026-05-30T06:17:16Z" persona="business" verdict="pass" model="claude-opus-4-8[1m]/medium" round="1">
T-028 (REQ-013/REQ-030) faithfully delivers the acquire-then-recover reorder the 2026-05-30 hardening amendment promised. In `patina-core/src/apply/engine.rs:345-357`, `recover_orphans` now runs AFTER the `let _guard = match policy {...}` block, so the `NonBlocking` contention path returns the typed `LockError::Contended` via `try_acquire_lock` before any recovery or journal write — satisfying REQ-030's strengthened "writes nothing" done-when bullet and CHK-068. The new `non_blocking_contention_leaves_pending_orphan_untouched` test genuinely gates the reorder (under the old order the planted orphan would be decoded/processed before contention surfaced, failing the `Contended` assertion). Default `Blocking` is preserved at both CLI call sites (`patina-cli/src/cmd/apply.rs:110,131`) and `lib.rs:154` via `LockPolicy::default()`; `run_rollback` retains its own exclusive acquire and never calls `recover_orphans` (verified `patina-core/src/rollback/mod.rs:118`), upholding REQ-030's "apply and rollback use Blocking" bullet. Recovery semantics are unchanged (recovery.rs has no production diff); CHK-024 (`recovery_crash.rs`), CHK-065/066 (engine unit tests), and CHK-067 (`deterministic_stdout.rs`) all pass green under the reorder. The secondary deliverable matches its spec: the flaky `blocked_exclusive_exits_with_lock_timeout_code` now gates the contender on the holder's observed `ACQUIRED` marker rather than a fixed sleep. No non-goal scope crept in — the SPEC-0003 watcher and SPEC-0002 remove/promote remain unwired (only doc-comment references in `lock.rs`), and no open question was silently resolved (the recovery-ordering question was resolved by a human in the Changelog). SPEC promises match diff delivery.
</review>

<review date="2026-05-30T06:17:16Z" persona="tests" verdict="pass" model="claude-opus-4-8[1m]/medium" round="1">
T-028 tests genuinely exercise the acquire-then-recover reorder. The CHK-068 unit test `non_blocking_contention_leaves_pending_orphan_untouched` (patina-core/src/apply/engine.rs) is a real gate: I reverted the engine to the pre-reorder ordering and the test FAILED (returned `Journal(VersionMismatch)` instead of the asserted `Lock(Contended)`), then PASSED again on restore — it catches exactly the regression this task fixes. Assertions are specific (typed contention error, orphan plan/progress bytes unchanged, no new plan/COMMIT/backup). Evidence roll call covers all five CHKs under REQ-013/REQ-030; the one `demonstrated` CHK cites a test present in the diff with scoped runner output, and every `hygiene` CHK cites a specific named test/file (recovery_crash.rs and deterministic_stdout.rs both verified green). CHK-024's hygiene label is justified — recovery semantics are unchanged by the call-site move. The F2 secondary deliverable removes a timing flake (fixed sleep → observed ACQUIRED marker) rather than adding one. `run_rollback` confirmed untouched. No fabrication pattern matched. Minor (non-blocking): the evidence red-phase prose attributes the failure to recovery deleting the orphan, when the real pre-reorder failure is `Plan::decode` rejecting the invalid planted bytes — the transition is real, only the mechanism description is imprecise.
</review>

<review date="2026-05-30T06:17:16Z" persona="security" verdict="pass" model="claude-opus-4-8[1m]/low" round="1">
Acquire-then-recover reorder is a sound race/TOCTOU hardening, not a regression. The change in `patina-core/src/apply/engine.rs:337-358` resolves the exclusive advisory lock per `LockPolicy` before `recover_orphans` runs, closing the prior hole where a contended `NonBlocking` attempt executed destructive orphan recovery (backup reversal, plan/progress deletion) before its contention check — a real data-integrity race against a live in-flight apply. `try_acquire` (`lock.rs:282-299`) returns the typed `LockError::Contended` before any guard or write, so the zero-mutation guarantee holds; CHK-068's `non_blocking_contention_leaves_pending_orphan_untouched` proves the orphan bytes survive byte-for-byte. `Held` reuses the caller's guard with no second acquisition (no self-deadlock, no drop window). No secrets, crypto, network, or auth surface in scope; lock-path strings in errors are the local user's own state dir (not a cross-tenant disclosure); lock-file contents are advisory-only; `LockGuard` releases on drop including SIGKILL. No new dependencies (`cargo deny` clean; test harness uses std `io`/`process` only). No injection/path-traversal/deserialization vectors introduced. No security blockers.
</review>

<review date="2026-05-30T06:17:16Z" persona="style" verdict="blocking" model="claude-sonnet-4-6[1m]/medium" round="1">
`patina-core/src/apply/engine.rs:604` — the `mod tests` inner module doc says "Unit coverage for the REQ-030 lock-acquisition policy (T-027)" but T-028 adds `non_blocking_contention_leaves_pending_orphan_untouched` (CHK-068), which covers the acquire-then-recover reorder and verifies the REQ-013 orphan-untouched invariant — both out of scope for T-027's description. The "docs match code" convention in AGENTS.md requires that any comment you touch or add describes what the code actually does. T-028 touches this module and should update the module header to reflect that it now covers both REQ-030 (lock-acquisition policy) and the REQ-013 / REQ-030 acquire-then-recover invariant introduced by T-028 (e.g. "Unit coverage for the REQ-030 lock-acquisition policy (T-027) and the acquire-then-recover orphan-safety reorder (T-028)").
</review>

<review date="2026-05-30T06:17:16Z" persona="correctness" verdict="pass" model="claude-opus-4-8[1m]/low" round="1">
Acquire-then-recover reorder in `patina-core/src/apply/engine.rs:345-357` is logically sound for all three `LockPolicy` variants. On `NonBlocking` contention, `try_acquire_lock(...)?` (line 351) short-circuits before `recover_orphans` (357) and the plan flush, so the contended attempt performs no recovery/plan/COMMIT/backup write — only paths and an in-memory `Engine` are computed before the guard match (333-335). The `Held` arm moves the caller's guard into `_guard`, holding it for the full body; the `Blocking` arm is byte-equivalent to the prior behavior with recovery still preceding the plan flush, preserving CHK-067 single-process determinism. `run_rollback` (`patina-core/src/rollback/mod.rs`) genuinely does not call `recover_orphans` (only a comment references it). The flaky-test fix in `patina-core/tests/lock_concurrency.rs:170-193` correctly `.take()`s the holder's stdout and drains it on a background thread while `wait_with_output` reads stderr — the concurrent-drain pattern avoids pipe-buffer deadlock, and the subsequent `holder.wait_with_output()` tolerates the already-taken stdout without panic or double-read. Verified green: the three new engine unit tests, the 4-test lock_concurrency suite (including the hardened `blocked_exclusive_exits_with_lock_timeout_code`), the 5-test recovery_crash suite, and `cargo clippy -p patina-core --all-targets`. No correctness defect at or above the reporting threshold.
</review>

<blockers date="2026-05-30T06:17:16Z" round="1">
Update the stale `mod tests` module-doc provenance in `engine.rs` so it describes what the test module now actually covers; one blocking style finding, all other personas passed.
- style (blocking): `patina-core/src/apply/engine.rs` `mod tests` module doc reads "Unit coverage for the REQ-030 lock-acquisition policy (T-027)", but T-028 added `non_blocking_contention_leaves_pending_orphan_untouched` (CHK-068) covering the acquire-then-recover reorder and the REQ-013 orphan-untouched invariant. Update the module header so it reflects both REQ-030 (lock-acquisition policy, T-027) and the T-028 REQ-013/REQ-030 acquire-then-recover orphan-safety reorder, per the "docs match code" convention in AGENTS.md. Comment-only change; no production behaviour or test logic should move.
</blockers>

<implementer date="2026-05-30T19:30:00Z" model="claude-opus-4-8[1m]/low" round="2">
- Completed: Addressed the single round-1 style blocker. Updated the
  `mod tests` module doc in `patina-core/src/apply/engine.rs` (line
  604) so its provenance reflects what the module now covers: the
  REQ-030 lock-acquisition policy (T-027) AND the REQ-013/REQ-030
  acquire-then-recover orphan-safety reorder (T-028). Comment-only
  change; no production behaviour, no test logic, and no test name
  moved. The prior pass's WIP (the engine reorder, the CHK-068 test,
  and the `lock_concurrency.rs` flaky-test hardening) is unchanged
  and amended in place per the retry contract.
- Undone: Nothing in T-028's scope is deferred.
- Hygiene checks: `cargo test --workspace` → exit 0. `cargo clippy
  --workspace --all-targets --all-features -- -D warnings` → exit 0.
  `cargo +nightly fmt --all --check` → exit 0. `cargo deny check` →
  exit 0 (no new dependencies).
- Evidence: paper trail at
  `.speccy/specs/0001-patina-core-engine/evidence/T-028.md`. The
  round-1 CHK roll call (CHK-068 demonstrated; CHK-024/065/066/067
  hygiene) is unchanged — this round is a comment-only doc fix that
  touches no test or production path, so no CHK label changes. The
  workspace test command remains green.
- Discovered issues: none new.
- Procedural compliance: this entry appends to `journal/T-028.md`
  after the round-1 blockers; no earlier block was rewritten. No
  `<implementer-note>` was written into TASKS.md. TASKS.md `state`
  for T-028 flips from `in-progress` to `in-review` as the final
  step. No shipped skill bodies under `.claude/skills/` were edited.
</implementer>

<review date="2026-05-30T06:23:46Z" persona="business" verdict="pass" model="claude-opus-4-8[1m]/medium" round="2">
T-028 (REQ-013/REQ-030) round 2 faithfully delivers what the 2026-05-30 hardening amendment promised, and the round-1 style blocker is the only thing that changed since the round-1 business pass. The working-tree reorder in `patina-core/src/apply/engine.rs` moves `recover_orphans(&journal_dir, &backups_dir)?` to AFTER the `let _guard = match policy {...}` block (was before it in committed HEAD ab8162d/T-027). This satisfies REQ-030's strengthened done-when bullets: the `NonBlocking` path reaches `try_acquire_lock` (`lock.rs:282`, returns `LockError::Contended` before any guard or write) before `recover_orphans` is ever called, so a contended attempt with a pending orphan writes nothing. New test `non_blocking_contention_leaves_pending_orphan_untouched` (CHK-068) genuinely gates this — it plants an orphan plan+progress, holds the lock, drives a NonBlocking apply, and asserts the typed contention error, byte-for-byte orphan survival, and zero new plan/COMMIT/backup. Done-when coverage verified: default Blocking preserved at both CLI call sites (`patina-cli/src/cmd/apply.rs:110,131`) and `lib.rs:154` (`LockPolicy::default()`); `run_rollback` (`rollback/mod.rs:118`) keeps its own exclusive acquire and never calls `recover_orphans`, upholding the "apply/rollback use Blocking" bullet. Recovery semantics are unchanged — `recovery.rs` has zero production diff and the 5-test `recovery_crash` suite is green, so CHK-024 (REQ-013 N-of-M restore) still holds. The round-2 change is exactly the comment-only fix the round-1 style blocker named: the `mod tests` module doc at `engine.rs:604` now names both T-027 and T-028. No scope crept in (SPEC-0003 watcher and SPEC-0002 remove/promote remain unwired); no open question silently resolved (the recovery-ordering question was decided by a human in the Changelog). SPEC promises match diff delivery.
</review>

<review date="2026-05-30T06:23:46Z" persona="tests" verdict="pass" model="claude-opus-4-8[1m]/medium" round="2">
T-028 tests genuinely exercise the acquire-then-recover reorder across REQ-013/REQ-030. I empirically reverted the engine ordering (recover-before-lock) and the CHK-068 test `non_blocking_contention_leaves_pending_orphan_untouched` (patina-core/src/apply/engine.rs:766) FAILED with `Err(Journal(VersionMismatch))` instead of the asserted `Lock(Contended)`, then PASSED on restore — it is a real gate, not vacuous. Its assertions are specific (typed `Contended { kind: Exclusive }` via `matches!`, orphan plan/progress survive byte-for-byte, no new plan/COMMIT, no backups dir) and drive the real `execute` entry point, not a mock. The two T-027 engine unit tests CHK-065/066 and the recovery_crash.rs / deterministic_stdout.rs cites for CHK-024/067 all resolve to real named tests; the three engine unit tests pass green (verified). The round-1 style blocker (stale `mod tests` provenance) was addressed in the diff by the round-2 comment-only doc fix at engine.rs:600-604 with no test or production logic moved. Evidence five-step walk passes: every CHK under REQ-013/REQ-030 (CHK-024/065/066/067/068) appears in the roll call with a label; the one `demonstrated` CHK cites a test present in the diff with scoped (not full-suite) runner output; each `hygiene` label cites a specific named test/file. No fabrication pattern matched. Minor (non-blocking, already noted in round 1): the evidence red-phase prose attributes the pre-reorder failure to "recovery removed the orphan plan/progress" when the actual mechanism is `Plan::decode` rejecting the planted bytes (`VersionMismatch`) before any deletion — the red→green transition is real, only the mechanism description is imprecise.
</review>

<review date="2026-05-30T06:23:46Z" persona="security" verdict="pass" model="claude-opus-4-8[1m]/high" round="2">
Acquire-then-recover reorder is a sound TOCTOU/data-integrity hardening, not a regression. In `patina-core/src/apply/engine.rs:334-356` the exclusive advisory lock is resolved per `LockPolicy` BEFORE `recover_orphans` runs, closing the prior hole where a contended `NonBlocking` (watcher) attempt executed destructive orphan recovery (backup reversal, plan/progress deletion) before its contention check — a real race against a live in-flight apply. `try_acquire` (`lock.rs:282-299`) returns the typed `LockError::Contended` before any guard or write, so REQ-030's zero-mutation guarantee holds; CHK-068 proves the planted orphan survives byte-for-byte with no new plan/COMMIT/backup. `Held` reuses the caller's guard with no second acquisition (no self-deadlock, no drop window). `recover_orphans`/recovery production code is byte-identical in the committed range; `run_rollback` (`rollback/mod.rs:118`) retains its own exclusive acquire and never calls recovery. Error-embedded lock paths are the local user's own state dir (single-tenant, not a cross-tenant disclosure); `LockGuard` releases on Drop incl. SIGKILL. No secrets/crypto/network/auth/injection/deserialization surface; no new dependencies. Round-2 delta is comment-only (module-doc provenance) plus the round-1 CHK-068 test amended in place. No security blockers.
</review>

<review date="2026-05-30T06:23:46Z" persona="style" verdict="pass" model="claude-sonnet-4-6[1m]/medium" round="2">
Round-2 fix resolves the round-1 blocker. `patina-core/src/apply/engine.rs:604-606` now reads "Unit coverage for the REQ-030 lock-acquisition policy (T-027) and the REQ-013/REQ-030 acquire-then-recover orphan-safety reorder (T-028)." — precisely what the blocker required. The git diff showed the old text because the file appears as a large wholesale addition from the merge base; the on-disk byte state (confirmed by grep) is authoritative and correct. No other style issues in scope: no dead imports, no parallel helpers, no naming drift, no stale prose elsewhere in the touched files.
</review>

<review date="2026-05-30T06:23:46Z" persona="correctness" verdict="pass" model="claude-opus-4-8[1m]/medium" round="2">
No correctness defect at or above the reporting threshold. The round-2 delta is a comment-only module-doc update (`patina-core/src/apply/engine.rs:601-604`) with no behavioral effect; the underlying T-028 logic is unchanged and sound. Control flow verified in `patina-core/src/apply/engine.rs:328-357`: the exclusive-lock match (345-353) runs before `recover_orphans` (357) and the plan flush (382). On `LockPolicy::NonBlocking`, `try_acquire_lock(...)?` (351) short-circuits via the `?` operator the moment `lock.rs:282` returns `LockError::Contended`; that path opens only the state-dir lock file and writes no journal/plan/COMMIT/backup, so the zero-mutation guarantee holds with a pending orphan present (CHK-068). The `Held` arm reuses the caller's guard without a second acquire (no self-deadlock), and the `Blocking` arm is semantically equivalent to the prior ordering for a single process (recovery still precedes the plan flush), preserving CHK-067 determinism. `run_rollback` (`patina-core/src/rollback/mod.rs`) references `recover_orphans` only in a comment and never calls it. Concurrency check on the flaky-test fix (`patina-core/tests/lock_concurrency.rs:170-193, 269-273`): `wait_for_acquired` `.take()`s the holder's stdout, so the later `holder.wait_with_output()` (273) skips the already-moved pipe — no double-read, no panic. The spawned drain thread keeps a live reader on stdout while `wait_with_output` reads stderr, so neither pipe buffer can fill and stall the holder — no deadlock. The drain thread and `wait_with_output` touch disjoint resources; no data race.
</review>
