---
spec: SPEC-0001
task: T-024
generated_at: 2026-05-29T04:58:49Z
---

<implementer date="2026-05-29T04:58:49Z" model="claude-opus-4-8[1m]/medium" round="1">
- Completed: Shipped the two documentation files REQ-027 mandates and a
  docs-structure integration test that gates their structural anchors.
  `docs/ARCHITECTURE.md` (contributor-facing) carries the four required
  `##`-level headings by exact text — `## Engine layers`, `## Journal format`,
  `## Apply phases`, `## Recovery` — with Mermaid diagrams (per the AGENTS.md
  diagrams rule) covering the `patina-core` lib / `patina-cli` bin layer split,
  the postcard journal version envelope + single-fsync write protocol, the
  three apply phases (plan → diff → mutate), and the recovery/rollback
  primitives; relevant SPEC-0001 REQs are cross-linked inline by ID.
  `docs/USER_GUIDE.md` (user-facing) carries the six required `##`-level
  headings — `## Installation`, `## Declaring dotfiles`, `## Apply flow`,
  `## State directory`, `## Recovery`, `## Troubleshooting` — and its
  `## State directory` section contains a markdown bullet list naming the six
  cloud-sync providers the per-machine state directory must not live on
  (`iCloud Drive`, `OneDrive`, `Dropbox`, `Box`, `Google Drive`, `Syncthing`),
  consistent with the existing `docs/operating-environment.md`. New test
  `patina-cli/tests/docs_structure.rs` parses both files as CommonMark via
  `pulldown-cmark` (added as a workspace dev-dependency at 0.13 and referenced
  from `patina-cli`'s `[dev-dependencies]`), extracts the H2 heading set and the
  `## State directory` bullet-item set, and asserts each required literal is
  present. The heading/bullet text is read from `pulldown-cmark` Text/Code
  events between Start/End tag events, so markers are stripped but literal words
  preserved — exact-text, not substring or prefix match.
- Undone: Nothing in scope. The `--linger` snippet for SPEC-0003 named in the
  task body is explicitly deferred ("lands in this same file once SPEC-0003
  starts work") and out of scope here. `deny.toml` already exists at repo root
  (T-025's deliverable, pre-existing); I did not touch it.
- Hygiene checks: `cargo test --workspace` exited 0 (all suites pass, including
  the new `patina-cli/tests/docs_structure.rs`, 3 cases). `cargo clippy
  --workspace --all-targets --all-features -- -D warnings` exited 0 (after one
  write-time fix: `doc_markdown` wanted `CommonMark` backticked in the test's
  module doc). `cargo +nightly fmt --all --check` exited 0 (after `fmt`
  reformatted the test's imports to one-per-line and split the `for required in
  [...]` arrays onto multiple lines). `cargo deny check` exited 0 (the new
  `pulldown-cmark` 0.13.4 + transitive `unicase`/`memchr`/`bitflags` resolve
  clean; pre-existing unmatched-license-allowance warnings are non-fatal).
- Evidence: paper trail at
  `.speccy/specs/0001-patina-core-engine/evidence/T-024.md`. Roll call for the
  CHKs under REQ-027:
  - CHK-056 (ARCHITECTURE.md H2 anchors present by exact text): demonstrated →
    evidence Scenario 1, gated by
    `docs_structure.rs::architecture_has_required_h2_headings`.
  - CHK-057 (USER_GUIDE.md H2 anchors present by exact text): demonstrated →
    evidence Scenario 2 (with red half: renaming `## State directory` →
    `## State dir` fails naming the missing literal), gated by
    `docs_structure.rs::user_guide_has_required_h2_headings`.
  - CHK-058 (State directory section lists the six providers as literal
    bullets): demonstrated → evidence Scenario 3 (with red half: replacing
    `Dropbox` with `Dropbox (via Smart Sync)` fails naming the missing literal,
    confirming exact-text not prefix match), gated by
    `docs_structure.rs::user_guide_state_directory_lists_cloud_sync_providers`.
  All three CHKs in T-024's scope are `demonstrated`; none is `hygiene`- or
  `judgment-only`-labelled.
- Discovered issues: None. The two `<behavior>` bullets in REQ-027 (renamed
  heading fails; prefix-extended bullet fails) are not separate CHK ids but I
  verified both by mutating a working copy of `docs/USER_GUIDE.md`, observing
  the named failure, then reverting — both behave exactly as specified. Noted
  that `deny.toml` is already present (T-025 territory) and left it untouched.
- Procedural compliance: First-attempt pass on a clean working tree (verified
  `git status --porcelain` empty before any edit). This `<implementer round="1">`
  block is created directly in `journal/T-024.md` per the journal-file schema
  (no `<implementer-note>` written into TASKS.md). The TASKS.md `state`
  attribute for T-024 flips `pending → in-progress → in-review` this turn. No
  shipped skill bodies under `.claude/skills/` were edited; the only
  non-doc/non-test production change is adding `pulldown-cmark` to
  `[workspace.dependencies]` (root `Cargo.toml`) and `patina-cli`'s
  `[dev-dependencies]`.
</implementer>

<review persona="business" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-05-29T05:02:16Z" round="1">
T-024 satisfies REQ-027 end to end. `docs/ARCHITECTURE.md` carries all four required exact-text H2 anchors (`Engine layers`, `Journal format`, `Apply phases`, `Recovery`) and `docs/USER_GUIDE.md` all six (`Installation`, `Declaring dotfiles`, `Apply flow`, `State directory`, `Recovery`, `Troubleshooting`); the `## State directory` section lists all six cloud-sync providers as literal bullets. `patina-cli/tests/docs_structure.rs` gates structural presence via exact-text `BTreeSet::contains` on H2 headings and section-scoped list items (not substring/prefix), which structurally enforces both REQ-027 `<behavior>` red-half cases — a renamed heading or a prefix-extended bullet (`Dropbox (via Smart Sync)`) drops out of the set and fails. All 3 tests pass; `pulldown-cmark` is correctly a workspace-managed dev-dependency. No non-goal scope crept in: the `--linger`/SPEC-0003 snippet is correctly deferred and `deny.toml` (T-025) untouched. The "Patina does not detect cloud-sync directories in v1.0" prose matches the north star's "SPEC-0001 documents only" intent, and no open question was silently resolved.
</review>

<review persona="tests" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-05-29T05:02:16Z" round="1">
Tests faithfully exercise all three covered scenarios and both negative paths for REQ-027. `patina-cli/tests/docs_structure.rs` parses both docs as CommonMark and asserts exact-text H2-heading set membership (CHK-056/057) and section-scoped bullet membership (CHK-058). I reproduced both negative mutations from the evidence live — renaming `## State directory` → `## State dir` and replacing `- Dropbox` with `- Dropbox (via Smart Sync)` — and both fail naming the missing literal, with runner output matching the evidence file byte-for-byte (no fabrication). I additionally confirmed section scoping by relocating `- Box` to `## Troubleshooting`; the test correctly refuses to count it. Evidence roll call covers CHK-056/057/058 all as `demonstrated` with named test cites; no fabrication patterns matched. All three tests pass; docs restored to original state after mutation probes.
</review>

<review persona="security" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-05-29T05:02:16Z" round="1">
Documentation-only task (REQ-027) plus a repo-local docs-structure test; no security-sensitive code paths touched. New dependency `pulldown-cmark` 0.13.4 is dev-dependency-only (`patina-cli/[dev-dependencies]`, `default-features = false`) so it adds zero shipped-binary attack surface; `cargo deny check advisories` returns "advisories ok" (no CVEs on it or its transitives unicase/memchr/bitflags). No secrets, credentials, tokens, or env-var leaks in `docs/USER_GUIDE.md` or `docs/ARCHITECTURE.md` — TOML examples use placeholder paths only. Security-relevant doc guidance is accurate (Windows symlink elevation via Developer Mode/UAC; never-overwrite-without-backup; cloud-sync avoidance for the state dir/lock/journal). Test (`patina-cli/tests/docs_structure.rs`) reads only `CARGO_MANIFEST_DIR`-relative repo files via `fs-err`, no tainted input or path-traversal vector, and `.expect()` is scoped to the test crate with a documented `#![expect(...)]`.
</review>

<review persona="style" verdict="pass" model="claude-sonnet-4-6[1m]/medium" date="2026-05-29T05:02:16Z" round="1">
All three new files (`docs/ARCHITECTURE.md`, `docs/USER_GUIDE.md`, `patina-cli/tests/docs_structure.rs`) and the two modified manifests (`Cargo.toml`, `patina-cli/Cargo.toml`) conform to the project's style conventions.

- `#[expect(clippy::expect_used, reason = "...")]` annotation at the crate root of `docs_structure.rs` matches the identical pattern used by every sibling test crate that exercises `.expect()` in setup helpers.
- Imports are one-per-line (post-`rustfmt`), no unused entries; `fs_err::` is used as a qualified-path call consistent with all sibling integration test files, none of which carry a `use fs_err;` import.
- `pulldown-cmark` is declared in `[workspace.dependencies]` and pulled into `patina-cli`'s `[dev-dependencies]` via `workspace = true`, matching the workspace dependency pattern documented in `rust-workspaces.md`.
- Function and module names are `snake_case`; the module-level `//!` doc comment references REQ-027 / CHK-056/057/058 and explains the exact-text assertion rationale per the documentation rule.
- Trailing-newline probe (`0a`) confirmed on all three new files.
- No dead imports, no suppression annotations without justification, no parallel reinvention of existing helpers.
</review>

<review persona="docs" verdict="pass" model="claude-sonnet-4-6[1m]" date="2026-05-29T05:02:16Z" round="1">
All three CHK scenarios are structurally satisfied and the documentation is accurate against the implementation. `docs/ARCHITECTURE.md` carries the four required H2 headings by exact text and its narrative (Mermaid diagrams, REQ cross-links, crate-boundary description) accurately reflects the two-crate workspace, the postcard plan file with version envelope, the three apply phases, and the recovery/rollback primitives. `docs/USER_GUIDE.md` carries all six required H2 headings and its `## State directory` section lists all six cloud-sync providers as literal plain-text bullet items, consistent with `docs/operating-environment.md`. The `patina-cli/tests/docs_structure.rs` test extracts H2 heading sets and the `## State directory` bullet set via pulldown-cmark event parsing and asserts exact-text membership — it does not substring-match any prose body around the headings, in compliance with the AGENTS.md test-hygiene rule. The `pulldown-cmark` crate is correctly scoped as a dev-dependency only and does not enter the production binary.
</review>
