---
task: T-005
spec: SPEC-0001
generated_at: 2026-05-28T00:00:00Z
---

<implementer date="2026-05-28T00:00:00Z" model="claude-opus-4-7[1m]" round="1">

- Completed:
  - Added `patina_core::state_dir` exposing the public surface
    required by REQ-016: a `HostOs` enum (`Linux | MacOs | Windows`)
    with a `HostOs::current()` constructor that maps
    `std::env::consts::OS` to the family; a public `resolve()` that
    inspects the running host and reads the real process
    environment; and a testable core `resolve_with_env(host, env)`
    that takes an explicit host family and an environment-lookup
    closure so every OS branch runs on every CI host.
  - Layout per REQ-016 / task body: Linux uses
    `$XDG_STATE_HOME/patina/` when the variable is set and non-empty
    (per the SPEC's "set and non-empty" wording), falling back to
    `$HOME/.local/state/patina/`; macOS uses
    `$HOME/Library/Application Support/patina/`; Windows uses
    `%LOCALAPPDATA%\patina\`. The empty-string case is treated the
    same as unset, matching the existing `repo_discovery` pattern
    (`empty_env_var_is_treated_as_unset`).
  - First-call materialization creates the three required
    directories (`<state>/patina/`, `<state>/patina/journal/`,
    `<state>/patina/backups/`) via `fs_err::create_dir_all`, which
    is idempotent by construction. The lazily-created files
    `profile` (T-007), `default_repo` (T-003), and `lock` (T-013)
    are *not* created here — `resolve_does_not_create_lazy_files`
    is the regression gate.
  - Added `StateDirError` with three variants:
    `MissingEnv { name: &'static str }` (named, so callers can
    match on the specific env var), `NonUtf8Path { raw: String }`
    (reserved for the OS-API boundary, no current call site uses
    it; introduced to keep the public surface forward-compatible
    without later widening), and `Io { path, source }` (with
    `#[source]` on the inner `io::Error` so the error chain stays
    intact).
  - `EngineError` gains a `StateDir` `#[from]` variant so
    state-directory failures surface through the engine's public
    API once T-007 / T-010 / T-013 wire `state_dir::resolve()` into
    `apply` / `status` / `rollback`.
  - Re-exported `HostOs`, `StateDirError`, and
    `resolve as resolve_state_dir` at the crate root so callers
    can write `patina_core::resolve_state_dir()` consistently with
    the other top-level entry points.
  - Did NOT add the `dirs` crate. The task body permits it ("if
    needed") but the layout rules are simple enough that a direct
    `HOME` / `XDG_STATE_HOME` / `LOCALAPPDATA` read is clearer than
    delegating to `dirs` and re-asserting the layout on top of it.
    No `deny.toml` change required.
  - Tests: 8 unit tests in `patina-core/src/state_dir.rs` cover
    every layout branch and every missing-env error path
    cross-platform via `compute_root` (pure function); 8
    integration tests in `patina-core/tests/state_dir_resolution.rs`
    drive the public `resolve_with_env` surface against real
    tempdir filesystems to exercise tree creation, idempotency, the
    lazy-files regression, and the "never write to the dotfiles
    repo" invariant (REQ-016).
- Undone:
  - CHK-027 requires a full `patina apply --yes` to verify the
    journal `.plan` / `.COMMIT` files and a `backups/<ts>/`
    subdirectory exist. That is the journal task (T-010 / T-012)
    and the executor task (T-014) end-to-end behaviour; T-005 owns
    the directory tree that hosts those artefacts. Roll call below
    labels CHK-027 `judgment-only` and cites the demonstrated
    prerequisite.
- Hygiene checks:
  - `cargo test --workspace`: exit 0 (16 new state-dir tests pass;
    all prior tests still pass).
  - `cargo clippy --workspace --all-targets --all-features -- -D warnings`:
    exit 0.
  - `cargo +nightly fmt --all --check`: exit 0.
  - `cargo deny check`: exit 0 (same three pre-existing
    `license-not-encountered` baseline warnings for
    `Unicode-DFS-2016`, `Zlib`, and `MPL-2.0`; non-blocking).
- Evidence: paper trail at
  `.speccy/specs/0001-patina-core-engine/evidence/T-005.md`.
  Roll call for CHKs under REQ-016:
  - CHK-027 (`.plan` and `.COMMIT` files exist under
    `T/patina/journal/` plus a `backups/<ts>/` subdirectory after
    `patina apply --yes`): judgment-only → end-to-end scenario
    requiring the journal subsystem (T-010 / T-012) and the apply
    executor (T-014). T-005's prerequisite (the `journal/` and
    `backups/` directories exist at the OS-correct location after
    `resolve()`) is demonstrated by
    `linux_xdg_state_home_creates_tree_and_is_idempotent`,
    `macos_resolves_under_application_support`, and
    `windows_resolves_under_localappdata` in
    `patina-core/tests/state_dir_resolution.rs` (each asserts both
    `journal/` and `backups/` are `is_dir()` after `resolve`).
  - REQ-016 done-when bullets 1–4 (Linux+XDG, Linux fallback,
    macOS, Windows): demonstrated → one test per branch in
    `tests/state_dir_resolution.rs`
    (`linux_xdg_state_home_creates_tree_and_is_idempotent`,
    `linux_without_xdg_resolves_under_home_dot_local_state`,
    `macos_resolves_under_application_support`,
    `windows_resolves_under_localappdata`), each asserting the
    resolved path equals the SPEC-specified layout.
  - REQ-016 done-when bullet 5 (subdirectories `journal/` and
    `backups/` exist; files `profile`, `default_repo`, and `lock`
    are lazily created by their owners): demonstrated → directory
    existence asserted in every layout test above; the
    lazy-files regression is gated by
    `resolve_does_not_create_lazy_files`.
  - REQ-016 invariant "the dotfiles repository is never written
    to": demonstrated → `resolve_does_not_write_to_external_repository`
    seeds a tempdir repo with a `patina.toml`, snapshots its
    mtime + content, runs `resolve_with_env`, and asserts mtime,
    content, and directory listing are unchanged.
- Discovered issues:
  - The clippy lint `clippy::useless_conversion` fired on
    `source.into()` when constructing `StateDirError::Io` from a
    `fs_err::Error` (which `Deref`s to `io::Error` but the closure
    parameter is already typed `std::io::Error` via the
    `create_dir_all` signature). Resolved by dropping the
    `.into()`.
  - The rustfmt nightly check rewrote two test sites: the
    `windows_missing_localappdata_errors_with_localappdata_named`
    `matches!` pattern expanded to a multi-line form (struct
    pattern with a single named field exceeds rustfmt's
    one-line width threshold once the variant name is qualified),
    and a long `assert_eq!` call got split. Accepted both
    rewrites — they are mechanical and consistent with the file's
    other multi-line `assert_eq!`s.
- Procedural compliance: clean working tree at entry
  (`git status --porcelain` empty); first-attempt shape (no prior
  T-005 journal). State flipped `pending` → `in-progress` →
  `in-review` with all four hygiene gates green before the
  `in-review` flip. No edits to the locked SPEC.md. Journal and
  evidence files are siblings of SPEC.md / TASKS.md as required.

</implementer>

<review date="2026-05-28T00:00:00Z" model="claude-opus-4-7[1m]/medium" persona="business" verdict="pass" round="1">
REQ-016 fully covered. Each of the four OS branches in `<done-when>` is gated by a dedicated test in `patina-core/tests/state_dir_resolution.rs` (Linux+XDG, Linux fallback to `$HOME/.local/state`, macOS Application Support, Windows LOCALAPPDATA), and the layout strings match the SPEC verbatim. The "files `profile` / `default_repo` / `lock` are created lazily by their owners" half of REQ-016's bullet 5 is correctly deferred to T-007 / T-003 / T-013 and regression-gated by `resolve_does_not_create_lazy_files`. The "dotfiles repository is never written to" invariant is exercised end-to-end by `resolve_does_not_write_to_external_repository` (mtime + content + dir-listing snapshot).

CHK-027 deferral is honest: that scenario requires `patina apply --yes` to produce `.plan` and `.COMMIT` files plus a `backups/<ts>/` subdir, which depends on T-010 (journal), T-012 (backups), and T-014 (executors). T-005's contribution — that `journal/` and `backups/` exist at the OS-correct location after `resolve()` — is what each per-OS layout test asserts as a prerequisite. No scope creep into those downstream tasks.

Non-goals respected (no `dirs` dependency dragged in, no GUI/CLI surface added, no `patina gc`, no `--repo` flag). User-story alignment: the "multi-machine syncer" story is served by the per-OS layout branches; the "cautious user" story is served by the never-touch-the-repo invariant test.

Minor observation, not blocking: the task body claims `resolve()` returns the path "in canonical absolute form," but `compute_root` only joins `patina` to the raw env-var value without canonicalization. With a relative env value (e.g. `LOCALAPPDATA=foo`) the return would be `foo/patina`, not absolute. REQ-016's `<done-when>` bullets don't actually require canonicality (they check the literal layout strings, which all pass), the journal explicitly defers canonicalization to T-009 (REQ-010), and SPEC scenarios use absolute env values throughout — so this is a doc-vs-behavior gap on the task contract rather than a SPEC breach. Worth fixing when T-009 wires the canonical-path helper into discovery and journal writes; not a reason to block T-005.
</review>

<review date="2026-05-28T00:00:00Z" model="claude-opus-4-7[1m]/high" persona="tests" verdict="pass" round="1">
Tests in `patina-core/tests/state_dir_resolution.rs` and the in-source `state_dir::tests` module exercise every REQ-016 layout branch (Linux+XDG, Linux fallback, macOS, Windows) with exact-path equality, plus the idempotency contract (`linux_xdg_state_home_creates_tree_and_is_idempotent` calls `resolve_with_env` twice and asserts identical results), the lazy-files regression (`resolve_does_not_create_lazy_files` gates against drift from T-003/T-007/T-013 owners leaking into state_dir), and the no-dotfiles-repo-write invariant (`resolve_does_not_write_to_external_repository` snapshots mtime + content + directory listing before and after). Negative paths assert the specific `StateDirError::MissingEnv { name: ... }` variant rather than catching any error. Closure-based env-lookup is a portability seam (so the Windows-layout branch runs on Linux CI), not a mock of the SUT — real `create_dir_all` runs against tempdirs. CHK-027 is correctly labelled `judgment-only` in the evidence roll call because it requires the journal subsystem (T-010/T-012) and the executor (T-014); T-005's prerequisite directories are demonstrated. Evidence file at `.speccy/specs/0001-patina-core-engine/evidence/T-005.md` carries real cargo runner output whose test names match the diff 1:1; the scoped invocation (`cargo test --test state_dir_resolution`) is distinct from the workspace hygiene run.
</review>

<review date="2026-05-28T00:00:00Z" model="claude-opus-4-7[1m]/medium" persona="security" verdict="pass" round="1">
No security blockers. T-005 lands a pure path-resolver plus idempotent `mkdir -p` for the per-machine state directory. Env-var reads (`XDG_STATE_HOME`, `HOME`, `LOCALAPPDATA`) are treated as opaque path components — no shell execution, no untrusted parsing, no logging path that could leak secrets. Errors include env-var names and paths, which is appropriate disclosure for a single-user CLI. The "never write to the dotfiles repository" invariant (REQ-016) is gated by `resolve_does_not_write_to_external_repository` in `patina-core/tests/state_dir_resolution.rs`. No new dependencies, so no `deny.toml` surface change. Permissions on created directories rely on the process umask — acceptable for v1.0 state-dir scope; backup-file mode preservation is correctly deferred to T-012 per the spec. `#[non_exhaustive]` on both `StateDirError` and `EngineError` keeps the error surface forward-compatible and forces downstream match arms to be defensive.
</review>

<review date="2026-05-28T00:00:00Z" model="claude-sonnet-4-6[1m]/medium" persona="style" verdict="blocking" round="1">
`patina-core/src/state_dir.rs:103` — the doc comment on `resolve()` contains a factually incorrect path description:

```
/// On first call this creates `<state>/patina/`, `<state>/journal/`
/// (relative to the patina root), and `<state>/backups/`.
```

`<state>` here denotes the OS-level parent (e.g. `$XDG_STATE_HOME`). The two subdirectories are created *inside* `<state>/patina/`, not as siblings to it — their actual paths are `<state>/patina/journal/` and `<state>/patina/backups/`. The parenthetical `(relative to the patina root)` contradicts the path notation that precedes it: if they were relative to the patina root the notation would be `<patina-root>/journal/`, not `<state>/journal/`. A reader could mistake `journal/` and `backups/` for siblings of the `patina/` directory, which is structurally wrong.

The module-level doc at the top of the same file correctly writes all three paths as `<state>/patina/`, `<state>/patina/journal/`, `<state>/patina/backups/`. The function doc should match that spelling:

```rust
/// On first call this creates `<state>/patina/`,
/// `<state>/patina/journal/`, and `<state>/patina/backups/`. On
/// subsequent calls the function is a filesystem no-op and returns
/// the same path.
```

AGENTS.md requires: "Never let docs drift. If your change alters observable behavior, architecture, or testing conventions, the corresponding docs/ files change in the same PR." This doc describes the API contract itself, not adjacent documentation, so the inaccuracy must be corrected before the task can be marked complete.

No other style violations were found: the `#![expect(clippy::expect_used, reason = "...")]` pattern in the integration test file matches the existing convention in `config_file_entry.rs` and `config_hook_entry.rs` exactly; the `use<>` precise-capture syntax is valid and idiomatic for Rust edition 2024; imports are correctly ordered; no unused imports or dead code was introduced; error propagation style (`?` + typed errors) is consistent throughout.
</review>

<blockers date="2026-05-28T00:00:00Z" round="1">
Fix the inaccurate doc comment on `resolve()` in `patina-core/src/state_dir.rs:103` before T-005 can be marked complete.

- Style (blocking): `resolve()`'s doc comment writes the two subdirectories as `<state>/journal/` and `<state>/backups/` with a contradictory parenthetical "(relative to the patina root)". The actual paths are `<state>/patina/journal/` and `<state>/patina/backups/` (children of the patina root, not siblings). Match the spelling already used in the module-level doc at the top of the same file: `<state>/patina/`, `<state>/patina/journal/`, `<state>/patina/backups/`. No other style violations; business / tests / security passed.
</blockers>

<implementer date="2026-05-28T00:00:00Z" model="claude-opus-4-7[1m]" round="2">

- Completed:
  - Amended `resolve()`'s doc comment in
    `patina-core/src/state_dir.rs` to match the spelling already
    used by the module-level doc at the top of the same file. The
    two subdirectories are now correctly described as
    `<state>/patina/journal/` and `<state>/patina/backups/`
    (children of the patina root), and the contradictory
    parenthetical "(relative to the patina root)" is removed. No
    other lines in the file changed.
- Undone:
  - Same as round 1: CHK-027's end-to-end `patina apply --yes`
    scenario remains owned by T-010 / T-012 / T-014.
- Hygiene checks:
  - `cargo test --workspace`: exit 0.
  - `cargo clippy --workspace --all-targets --all-features -- -D warnings`:
    exit 0.
  - `cargo +nightly fmt --all --check`: exit 0.
  - `cargo deny check`: exit 0 (same pre-existing
    `license-not-encountered` baseline; non-blocking).
- Evidence: roll call unchanged from round 1 — the amend is a
  doc-comment correction with no behavioural or test surface
  change. See round-1 entry for the CHK-by-CHK mapping under
  REQ-016.
- Discovered issues: none this round.
- Procedural compliance: retry-shape entry confirmed via the
  round-1 `<implementer>` + matching `<blockers round="1">` in
  this journal. Dirty working tree preserved and amended in
  place (no `git restore` / `git clean` / `git checkout` against
  the prior pass's paths). State flipped `pending` →
  `in-progress` → `in-review` with all four hygiene gates green
  before the `in-review` flip. Round monotonically incremented
  from 1 to 2.

</implementer>

<review date="2026-05-28T00:00:00Z" model="claude-opus-4-7[1m]/medium" persona="business" verdict="pass" round="2">
Round 2 is a surgical doc-comment fix to `resolve()` in `patina-core/src/state_dir.rs:100-106` and nothing else. REQ-016 coverage is unchanged from round 1's pass: all four `<done-when>` layout branches (Linux+XDG, Linux fallback to `$HOME/.local/state`, macOS Application Support, Windows LOCALAPPDATA), the idempotency contract, the lazy-files regression (`resolve_does_not_create_lazy_files`), and the never-write-to-the-dotfiles-repo invariant (`resolve_does_not_write_to_external_repository`) are still gated by the same tests. CHK-027 remains correctly deferred to T-010/T-012/T-014. No scope creep, no non-goal touched, no open question silently resolved.

The corrected doc now matches the module-level doc at the top of the same file and matches the SPEC's REQ-016 layout strings verbatim — `<state>/patina/`, `<state>/patina/journal/`, `<state>/patina/backups/` — so the public API contract no longer contradicts the executable behaviour (`create_tree` in `state_dir.rs:177-182` joins `journal`/`backups` to `root`, where `root = <env-derived-base>/patina`). The round-1 minor observation about non-canonical paths under relative env values is unchanged in round 2 and remains explicitly deferred to T-009 (REQ-010) per the prior review; not a regression here.
</review>

<review date="2026-05-28T00:00:00Z" model="claude-opus-4-7[1m]/medium" persona="tests" verdict="pass" round="2">
Round 2 is a doc-comment-only amend in `patina-core/src/state_dir.rs:103-106`; the test surface is byte-identical to round 1. The four REQ-016 layout branches (`linux_xdg_state_home_creates_tree_and_is_idempotent`, `linux_without_xdg_resolves_under_home_dot_local_state`, `macos_resolves_under_application_support`, `windows_resolves_under_localappdata` in `patina-core/tests/state_dir_resolution.rs`) still drive `resolve_with_env` against real tempdir filesystems with exact-path equality; the idempotency contract is still gated by a second-resolve call asserting equal path with no error (line 53-54); the lazy-files regression is still gated by `resolve_does_not_create_lazy_files`; and the no-dotfiles-repo-write invariant is still gated by `resolve_does_not_write_to_external_repository` with mtime + content + dir-listing snapshot. Negative paths still assert the specific `StateDirError::MissingEnv { name: ... }` variant rather than any error. CHK-027 stays correctly `judgment-only` in the unchanged evidence roll call (full `patina apply --yes` end-to-end belongs to T-010/T-012/T-014). Evidence file referenced from the round-1 implementer block is present at `.speccy/specs/0001-patina-core-engine/evidence/T-005.md` with real runner output naming tests that match the diff. No new fabrication signals on the round-2 entry — it explicitly carries the round-1 roll call forward, which is the honest move for a docs-only amend.
</review>

<review date="2026-05-28T00:00:00Z" model="claude-opus-4-7[1m]/medium" persona="security" verdict="pass" round="2">
Round 2 is a doc-comment-only correction on `patina-core/src/state_dir.rs:103`. No behavioural, dependency, error-message, env-handling, or filesystem-call changes. The security surface reviewed (and passed) in round 1 is unchanged: env reads (`XDG_STATE_HOME`, `HOME`, `LOCALAPPDATA`) are still opaque path components, `StateDirError` still discloses only env-var names and paths (appropriate for a single-user CLI), `resolve_does_not_write_to_external_repository` still gates the never-touch-the-dotfiles-repo invariant under REQ-016, and no new third-party crate was introduced (so `deny.toml` is untouched). The round-1 style blocker is addressed with no security regression.
</review>

<review date="2026-05-28T00:00:00Z" model="claude-sonnet-4-6[1m]/medium" persona="style" verdict="pass" round="2">
The round-1 blocker is resolved. `patina-core/src/state_dir.rs:103-106` now correctly spells all three paths as `<state>/patina/`, `<state>/patina/journal/`, and `<state>/patina/backups/`, and the contradictory parenthetical `(relative to the patina root)` has been removed. The wording now matches the module-level doc at the top of the same file exactly. No other lines changed; no new style violations are present.
</review>
