---
spec: SPEC-0001
task: T-016
generated_at: 2026-05-28T23:30:00Z
---

<implementer date="2026-05-28T23:30:00Z" model="claude-opus-4.8[1m]/high" round="1">
- Completed: Landed the end-to-end `patina apply` surface (REQ-017),
  composing every prior subsystem into a two-phase orchestrator plus the
  CLI presentation layer.

  Engine (`patina-core`):
  - New `patina-core/src/apply/engine.rs` (`pub mod engine` in
    `apply/mod.rs`). `plan(request, timestamp) -> ResolvedPlan` resolves
    the repository root, the per-machine state dir, the active profile
    (via `resolve_profile` against `PATINA_PROFILE` / persisted /
    `[[auto_match]]`), builds the variable `Resolver` (built-ins + `-v`
    CLI overrides + per-module `[variables]` + profile), enumerates
    modules, parses each manifest, canonicalizes each `[[file]]` source
    and tilde-expanded targets, and emits a durable `Plan` plus a parallel
    `Vec<ResolvedOperation>` and the owned `Vec<HookEntry>`. Planning does
    **no** mutation. `execute(resolved, request) -> ApplyResult` recovers
    orphans, takes the exclusive lock, resolves hook shells up front, runs
    `pre_apply` hooks (a `must_succeed` failure → `Aborted` before any
    mutation), flushes the journal, materializes every op (calling
    `backup_before_overwrite` per target first, recording per-object
    progress), runs `post_apply` hooks, then commits + `gc_retain` on
    success or reverses the completed records to the pre-apply state on a
    `must_succeed` `post_apply` failure (`RolledBack`). The built
    `Resolver` is stored on `ResolvedPlan` so the executors, the hook
    `when` evaluator, and the CLI diff renderer all see the same rendered
    template output (fixed a first-cut bug where `execute` rebuilt a bare
    resolver and dropped the `-v` overrides).
  - New public types `ApplyRequest`, `ResolvedPlan`, `ResolvedOperation`,
    `ApplyResult` (`Applied { warnings }` / `RolledBack { failed_hook }` /
    `Aborted { failed_hook }`); `plan`/`execute`/`is_content_materialization`
    re-exported from `lib.rs` as `plan_apply` / `execute_plan` /
    `is_content_materialization`. `ApplyOptions` gained `request` +
    `timestamp` and `apply()` now plans-then-executes (mirroring
    `--yes`), returning `ApplyResult`. `ApplyRequest`/`ApplyResult` are
    deliberately not `#[non_exhaustive]` so the CLI in the sibling crate
    can construct/exhaustively-match them.
  - The rollback path reverses file ops in-process and drops the journal
    handle; the proper `.ROLLED_BACK` sentinel is T-018's job. Recovery is
    idempotent over already-reversed targets, so leaving the orphan plan
    is safe until T-018 lands.

  CLI (`patina-cli`), full rewrite of `main.rs` from the T-001 wiring stub:
  - `cli.rs` — clap-derived `patina apply` with `--yes`, `--force-deploy`,
    `--json`, `--pager=<delta|difft>`, and repeatable `-v key=value`.
  - `output/reporter.rs` — the `Reporter` trait (the only sanctioned print
    site; diff/json/line → stdout, prompt/warn → stderr) with a production
    `StreamReporter` and a `#[cfg(test)] BufferReporter`.
  - `output/diff.rs` — embedded `similar` line diff for copy/render modes
    and an `old → new` link-target render for symlink modes; deterministic
    (plan order, no timestamps/PIDs in output).
  - `cmd/apply.rs` — owns the REQ-017 decision tree (`--yes` → mutate;
    non-TTY no-`--yes` → preview exit 0; TTY no-`--yes` → prompt
    `Apply? [y/N]`, apply on `y`/`Y` else exit 5; `--json` alone → preview
    envelope, `--json --yes` → mutate) and the exit-code map (Applied 0 /
    Aborted 2 / RolledBack 3 / declined 5). TTY detection via
    `is-terminal` is injected as a `Tty` enum and the prompt read behind a
    `PromptReader` seam so the decision logic is unit-testable. The
    `--pager` miss falls back to the embedded renderer with a one-line
    stderr warning. Timestamp via `jiff` (`%Y%m%dT%H%M%SZ`), used only for
    the journal/backup filename — never in user output.
  - Workspace deps added: `clap`, `is-terminal`, `jiff`, `serde_json`,
    `similar`, `tracing`; `camino`/`fs-err`/`serde` referenced by the CLI.
- Undone: Nothing in REQ-017 scope deferred. Out of scope by SPEC design:
  piping the diff through a *resolved* external pager (only the PATH-miss
  fallback is specified for v1; the embedded renderer is always the
  rendered-string source); the `.ROLLED_BACK` sentinel + `patina rollback`
  (T-018); `status` (T-017) and the `debug` family.
- Hygiene checks:
  - `cargo test --workspace`: exit 0.
  - `cargo clippy --workspace --all-targets --all-features -- -D warnings`: exit 0.
  - `cargo +nightly fmt --all --check`: exit 0.
  - `cargo deny check`: exit 0 (advisories / bans / licenses / sources ok).
- Evidence: paper trail at
  `.speccy/specs/0001-patina-core-engine/evidence/T-016.md`.
  Roll call for CHKs under REQ-017:
  - CHK-028 (non-TTY `patina apply` previews, exits 0, no symlink, diff on
    stdout): demonstrated → evidence Scenario 1 shows the green run
    (exit 0, diff naming `.rc`, target absent) against the pre-edit
    "no apply subcommand / NotImplemented" baseline; reproduced by
    `non_tty_apply_previews_without_mutating`.
  - CHK-029 (`post_apply` `must_succeed` hook fails under `--yes` → file
    ops reversed, exit 3): demonstrated → evidence Scenario 2 (exit 3,
    `.rc` reversed, "rolled back all file operations" on stderr);
    reproduced by `post_apply_hook_failure_rolls_back_and_exits_3`.
  - CHK-030 (same hook + `--force-deploy` → not rolled back, stderr names
    the hook, exit 0): demonstrated → evidence Scenario 3 (exit 0, `.rc`
    survives with `payload`, stderr names `exit 1`); reproduced by
    `force_deploy_downgrades_hook_failure_and_exits_0`.
- Discovered issues: (1) A real parallel-test flake: on Windows the state
  dir resolves from `LOCALAPPDATA`, which the first cut of the integration
  tests did not override, so all parallel CLI tests shared one real
  `%LOCALAPPDATA%\patina` journal/lock/backups and one test's startup
  `recover_orphans` reversed another's just-applied files. Fixed by
  redirecting `LOCALAPPDATA` (alongside `XDG_STATE_HOME`) per fixture.
  (2) Hooks run the user's actual shell, so a `post_apply` `exit 1` hook
  under `pwsh` sources the host PowerShell profile and emits unrelated
  profile-load noise on stderr; this is orthogonal to T-016 (a hook is the
  user's command) and does not affect exit codes or the rollback decision.
- Procedural compliance: First-attempt shape (no prior journal, clean
  tree at entry). Re-read REQ-017's `<done-when>` / `<behavior>` /
  `<scenario>` blocks before writing tests. No `unwrap`/`expect`/`panic`
  in production code (the print sinks discard infallible IO/fmt results
  via named helper params, not bare `let _`; integration-test `expect` is
  covered by a crate-level `#![expect(clippy::expect_used)]` per the repo
  convention). New deps checked against `deny.toml` (all pass). Task state
  flipped pending → in-progress → in-review by this skill; not
  self-marked completed.
</implementer>

<review persona="business" verdict="pass" model="claude-opus-4-8[1m]/high" date="2026-05-28T22:11:13Z" round="1">
REQ-017 fully delivered: TTY prompt/decline (exit 5), non-TTY preview (exit 0, CHK-028), `--yes` unconditional apply, `--force-deploy` hook downgrade with stderr warning (exit 0, CHK-030), `post_apply must_succeed` rollback (exit 3, CHK-029), `--pager` PATH-miss fallback with one-line warning, and the `--json` envelope (`repo_root`/`profile`/`plan`/`result`) where `--json` alone previews and `--json --yes` mutates. All three locked CHK-028/029/030 scenarios are reproduced by integration tests in `patina-cli/tests/apply_cli.rs`; the exit-code map in `patina-cli/src/cmd/apply.rs:222` matches the SPEC's 0/2/3/5 contract; output is routed through a single `Reporter` seam (`patina-cli/src/output/reporter.rs`) with no stray prints, serving the deterministic-stdout goal. No scope creep into `status`/`rollback`/`debug`; no `--repo` flag. Two non-blocking observations: (1) the `-v` task-scenario's "JSON `plan` shows rendered content" phrasing is met via a `--yes` materialized-file assertion instead — acceptable because the locked REQ-017 `<done-when>` only requires `plan` to be an array of ops, which `patina-cli/src/cmd/apply.rs:143` satisfies; (2) the `--json --yes` + failing-post_apply-hook → `rolled_back`/exit-3 cell has correct code paths but no dedicated integration test (a tests-persona matter). Neither breaks the SPEC contract.
</review>

<review persona="tests" verdict="pass" model="claude-opus-4-8[1m]/high" date="2026-05-28T22:11:13Z" round="1">
All three REQ-017 scenarios (CHK-028/029/030) are driven by real subprocess integration tests in `patina-cli/tests/apply_cli.rs` that spawn the actual `patina` binary against an isolated tempdir repo + state dir and assert on real exit codes and live filesystem state — no mocking of the system under test. I re-ran `cargo test -p patina-cli --test apply_cli` and all 7 pass.

Scenario fidelity: CHK-028 (`non_tty_apply_previews_without_mutating`) asserts exit 0 + `symlink_metadata().is_err()` (target genuinely absent) + diff on stdout. CHK-029 (`post_apply_hook_failure_rolls_back_and_exits_3`) asserts the *specific* exit code 3 and that the materialized copy was reversed — catches both a missing-rollback regression and a wrong exit code. CHK-030 (`force_deploy_downgrades_hook_failure_and_exits_0`) asserts exit 0, the exact surviving byte content `payload\n` (catches a spurious rollback), and stderr naming the hook. Negative paths use exact assertions, not loose "any error." The engine (`patina-core/src/apply/engine.rs`) implements real materialize→backup→hook→reverse_completed behaviour, not stubs.

Evidence (`.speccy/specs/0001-patina-core-engine/evidence/T-016.md`): `Evidence:` field present, file exists, CHK roll call covers all three CHKs under REQ-017 each `demonstrated` with a named reproducing test that exists in the diff. Pre-edit baseline independently confirmed against `git show HEAD:patina-cli/src/main.rs`. Red/green differ materially; the cited command is per-test scoped. No fabrication pattern matched.

Note (non-blocking): the TTY-interactive prompt cases (`n`→exit 5, `y`→apply) appear in the task-scenarios prose but were not promoted to a CHK under REQ-017, and the `Tty::Interactive`/`PromptReader` decision branch in `cmd/apply.rs:90-106` has no test exercising it despite the seam being built for testability. Outside blocking authority since no CHK maps to it. Worth a follow-up unit test feeding a fake `PromptReader`.
</review>

<review persona="security" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-05-28T22:11:13Z" round="1">
T-016 wires the `patina apply` surface for a local, single-user CLI with no network, auth boundary, credential storage, or multi-tenant data — the classic injection/secret/authz surfaces do not apply. Hooks running arbitrary shell commands (`patina-core/src/apply/hooks.rs`, integrated at `patina-core/src/apply/engine.rs:303-358`) is documented REQ-006 behavior sourced from the user's own manifest, not an untrusted input; explicit unresolved shells abort before any mutation. `-v` overrides (`patina-cli/src/cmd/apply.rs:248-258`) feed the user's own MiniJinja context under strict-undefined. The backup-before-overwrite ordering is preserved (`engine.rs:336-340`) and the rollback path restores from backup or removes fresh targets (`reverse_completed`, `engine.rs:433-462`). No sensitive-data exposure: the diff and `--json` envelope (`patina-cli/src/output/diff.rs`, `patina-cli/src/cmd/apply.rs:143-164`) surface only user-declared repo paths, sources, targets, and the user's own hook command on stderr (required by CHK-030) — no state-dir, backup paths, secrets, timestamps, or PIDs leak. No security concerns found.
</review>

<review persona="style" verdict="blocking" model="claude-sonnet-4-6[1m]/medium" date="2026-05-28T22:11:13Z" round="1">
`patina-cli/src/cmd/apply.rs:272-301` introduces private `resolve_on_path` + `path_candidates` helpers that duplicate — and diverge from — the identical-named private helpers already present in `patina-core/src/apply/hooks.rs:296-335`.

The implementer even comments "Mirrors the hook-shell resolver's PATH walk," confirming awareness of the existing implementation. The `rust-let-types-carry-invariants.md` rule explicitly names this shape (recursive walk / enum-label / path helper duplicated across sibling modules) as a write-time review blocker.

Two concrete deviations confirm the drift has already started:

1. **Worse signature:** `cmd/apply.rs` uses `&std::path::Path` / `std::path::PathBuf` for `dir` and the return type; `hooks.rs` uses the project-canonical `&camino::Utf8Path` / `Utf8PathBuf` as required by AGENTS.md "Code conventions" and `rust-code-quality.md`.

2. **Missing behavior:** The Windows `path_candidates` in `cmd/apply.rs` lacks the `already_has_ext` guard present in `hooks.rs` (lines 320-326 of hooks.rs), so a binary like `delta.exe` would have `.EXE` appended again in the CLI's copy but not in the engine's copy.

**Required fix:** Promote `resolve_on_path` (and the `path_candidates` + `is_executable_file` helpers it depends on) from private to `pub(crate)` in `patina-core/src/apply/hooks.rs`, re-export from `patina-core/src/lib.rs` or expose via the existing `pub use apply::…` block, and call it from `cmd/apply.rs` instead of maintaining a second copy. The CLI only needs the presence check (`is_file()`), which the existing helper already provides.

Secondary style note (non-blocking): `patina-cli/src/cmd/apply.rs:163` uses `.unwrap_or_else(|_| "{}".to_owned())` on `serde_json::to_string_pretty`. While `serde_json` serialization of a `serde_json::Value` constructed entirely from literals is infallible in practice, the suppression swallows a genuine-if-rare failure silently. Not blocking on its own.
</review>

<blockers date="2026-05-28T22:11:13Z" round="1">
Eliminate the duplicated PATH-resolver helpers in `patina-cli/src/cmd/apply.rs` by reusing the canonical implementation in `patina-core/src/apply/hooks.rs`.
- style (blocking): `cmd/apply.rs:272-301` reintroduces private `resolve_on_path` + `path_candidates` that duplicate `hooks.rs:296-335` and have already drifted — the CLI copy uses `std::path::Path`/`PathBuf` instead of the canonical `camino::Utf8Path`/`Utf8PathBuf`, and its Windows branch is missing the `already_has_ext` guard (so `delta.exe` gets `.EXE` re-appended). Promote `resolve_on_path` + `path_candidates` (+ `is_executable_file`) to `pub(crate)` in `hooks.rs`, expose them through the `patina-core` public surface, and call them from `cmd/apply.rs` instead of keeping a second copy. This is the exact duplication shape `rust-let-types-carry-invariants.md` names as a review blocker.
</blockers>

<implementer date="2026-05-28T00:00:00Z" model="claude-opus-4-8[1m]/high" round="2">
- Completed: Addressed the round-1 style blocker by deleting the
  duplicated PATH-resolver helpers in `patina-cli/src/cmd/apply.rs`
  (the private `resolve_on_path` + `path_candidates`, which used
  `std::path::Path`/`PathBuf` and were missing the Windows
  `already_has_ext` guard) and reusing the canonical implementation
  in `patina-core`. Promoted `resolve_on_path` in
  `patina-core/src/apply/hooks.rs` from private to `pub` (with a
  `#[must_use]` message, required by the pedantic clippy gate once it
  joined the public surface), re-exported it through
  `patina-core/src/apply/mod.rs` and `patina-core/src/lib.rs`, and
  changed the pager PATH-miss check in `render_diff` to call
  `patina_core::resolve_on_path(pager.binary())`. The canonical
  helper supersets the deleted copy: it keeps the project-canonical
  `camino::Utf8Path`/`Utf8PathBuf` signature and the `already_has_ext`
  guard, and also treats a separator-containing binary as a direct
  path. Pager binaries (`delta`/`difft`) carry no separator, so the
  PATH-walk behaviour the CLI relies on is unchanged.
- Undone: Nothing further in REQ-017 scope. The two non-blocking
  notes from round 1 (a TTY-prompt unit test feeding a fake
  `PromptReader`; the `to_string_pretty` suppression at the former
  line 163) were not promoted to blockers and are left as-is.
- Hygiene checks:
  - `cargo test --workspace`: exit 0.
  - `cargo clippy --workspace --all-targets --all-features -- -D warnings`: exit 0.
  - `cargo +nightly fmt --all --check`: exit 0.
  - `cargo deny check`: exit 0 (advisories / bans / licenses / sources ok;
    only pre-existing unmatched-license-allowance warnings).
- Evidence: paper trail at
  `.speccy/specs/0001-patina-core-engine/evidence/T-016.md` (round-1
  CHK roll call unchanged — this amend is a no-behaviour-change
  refactor of the pager PATH-miss path).
  Roll call for CHKs under REQ-017:
  - CHK-028 (non-TTY preview, exit 0, no symlink, diff on stdout):
    demonstrated → unchanged; `non_tty_apply_previews_without_mutating`
    still green.
  - CHK-029 (`post_apply must_succeed` failure → rollback, exit 3):
    demonstrated → unchanged; `post_apply_hook_failure_rolls_back_and_exits_3`
    still green.
  - CHK-030 (same hook + `--force-deploy` → not rolled back, exit 0):
    demonstrated → unchanged; `force_deploy_downgrades_hook_failure_and_exits_0`
    still green.
- Discovered issues: None this round. The blocker fix is a pure
  de-duplication; observable behaviour (exit codes, rollback
  decision, pager fallback warning) is identical.
- Procedural compliance: Retry shape (round-1 `<implementer>` + a
  round-1 `<blockers>` matching the highest implementer round).
  Amended the prior pass's uncommitted WIP in place — no `git
  restore`/`clean`/`checkout`, no rewrite-from-scratch. Reused the
  existing canonical helper rather than maintaining a second copy,
  per `rust-let-types-carry-invariants.md`. No `unwrap`/`expect`/
  `panic` introduced. State flipped pending → in-progress →
  in-review by this skill; not self-marked completed.
</implementer>

<review persona="business" verdict="pass" model="claude-opus-4-8[1m]/high" date="2026-05-29T01:03:39Z" round="2">
Round-2 state preserves the full REQ-017 contract; the retry was a behavior-preserving de-duplication, not a feature change.

The round-1 style blocker (duplicated PATH-resolver helpers) was fixed correctly and the fix touches only the one observable behavior REQ-017 names on that path — the `--pager=delta`/`difft` PATH-miss fallback. `resolve_on_path` is now `pub` in `patina-core/src/apply/hooks.rs:297`, re-exported via `apply/mod.rs` and `lib.rs`, and `patina-cli/src/cmd/apply.rs:186` calls `patina_core::resolve_on_path(pager.binary())`. The canonical helper keeps the `camino::Utf8Path` signature and `already_has_ext` guard. Pager binaries (`delta`/`difft`, `cli.rs:67-72`) are separator-free bare names, so the PATH-walk semantics the CLI relies on are identical to the deleted copy — the fallback still warns on stderr and falls through to the embedded `similar` renderer (REQ-017 `<done-when>` pager bullet).

All three locked scenarios remain satisfied and tested in `patina-cli/tests/apply_cli.rs`: CHK-028 (`non_tty_apply_previews_without_mutating` — exit 0, no symlink, diff naming target on stdout), CHK-029 (`post_apply_hook_failure_rolls_back_and_exits_3` — exit 3, copy reversed), CHK-030 (`force_deploy_downgrades_hook_failure_and_exits_0` — exit 0, byte-exact `payload\n` survival, stderr naming the hook). `--json` mutation semantics match resolved Open-Question (f): `--json` alone → `previewed`/no mutation, `--json --yes` → `applied`/`rolled_back` (`cmd/apply.rs:116-140`). Exit-code map 0/2/3/5 (`cmd/apply.rs:222`, `run:105`) matches the SPEC. No scope creep into `status`/`rollback`/`debug`, no `--repo` flag. I re-ran `cargo test -p patina-cli --test apply_cli`: 7/7 pass, including `missing_pager_falls_back_with_warning`, which directly exercises the refactored fallback path. No open question was silently decided; the diff reflects the latest Changelog intent, not an older one.
</review>

<review persona="tests" verdict="pass" model="claude-opus-4-8[1m]/high" date="2026-05-29T01:03:39Z" round="2">
Round-2 state passes the tests bar. All three locked scenarios are driven by real subprocess integration tests in `patina-cli/tests/apply_cli.rs` that spawn the actual `patina` binary against an isolated tempdir repo + state dir (HOME/USERPROFILE/XDG_STATE_HOME/LOCALAPPDATA all redirected) and assert on real exit codes and live filesystem state — no mock-the-SUT shape. CHK-028 (`non_tty_apply_previews_without_mutating`) asserts exit 0 + `symlink_metadata().is_err()` (target genuinely absent) + diff on stdout; CHK-029 (`post_apply_hook_failure_rolls_back_and_exits_3`) asserts the *specific* exit 3 + the copy reversed; CHK-030 (`force_deploy_downgrades_hook_failure_and_exits_0`) asserts exit 0 + exact surviving bytes `payload\n` (catches a spurious rollback) + stderr naming the hook. Each would fail under a realistic regression. The engine (`patina-core/src/apply/engine.rs:433-462` `reverse_completed`) performs genuine backup-restore/remove reversal, not stubs. The round-2 blocker fix (deleting the duplicated PATH resolver, calling `patina_core::resolve_on_path` at `cmd/apply.rs:186`) is a behaviour-neutral refactor of the pager PATH-miss path, which `missing_pager_falls_back_with_warning` exercises through the real binary; I ran `cargo test -p patina-cli --test apply_cli` and all 7 pass. Evidence: `Evidence:` field present, file exists, roll call covers CHK-028/029/030 each `demonstrated` with a named test that exists in the diff; red baseline independently confirmed against `git show HEAD:patina-cli/src/main.rs`; red/green differ materially; cited command is per-test scoped. No fabrication pattern matched.
</review>

<review persona="security" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-05-29T01:03:39Z" round="2">
Round-2 change is a pure de-duplication refactor with no new security surface. `resolve_on_path` was promoted to `pub` in `patina-core/src/apply/hooks.rs:296` and re-exported (`apply/mod.rs`, `lib.rs`); `patina-cli/src/cmd/apply.rs:186` now calls it instead of a divergent private copy. At the CLI call site it is fed only a hardcoded enum-derived binary name (`delta`/`difft` via `Pager::binary()`), never a user-controlled string, and the result is used purely as a boolean presence check — the pager is not spawned (piping deferred), so no command-execution or injection vector is introduced. The promoted helper only performs a read-only `PATH`/`PATHEXT` lookup returning a path; no privileged capability is exposed by widening the API. No sensitive-data exposure: the diff renderer (`patina-cli/src/output/diff.rs`) and the fallback warning surface only user-declared sources/targets/link-targets and the hardcoded pager name — no state-dir paths, backups, secrets, timestamps, or PIDs. The underlying local-single-user posture from round 1 (hooks run the user's own manifest commands per documented REQ-006; no network/auth/credential/multi-tenant surface) is unchanged. No security concerns found.
</review>

<review persona="style" verdict="pass" model="claude-sonnet-4-6[1m]/medium" date="2026-05-29T01:03:39Z" round="2">
Round-1 blocker resolved. The duplicate `resolve_on_path` + `path_candidates` + `is_executable_file` helpers have been deleted from `patina-cli/src/cmd/apply.rs`. `resolve_on_path` is now `pub` in `patina-core/src/apply/hooks.rs` (line 297) with the canonical `camino::Utf8PathBuf` return type, the Windows `already_has_ext` guard intact, and a `#[must_use]` annotation added as required by the pedantic clippy gate. The re-export chain through `apply/mod.rs:54` → `lib.rs:41` is clean. The call site at `cmd/apply.rs:186` correctly calls `patina_core::resolve_on_path(pager.binary())`. No new style concerns introduced by the round-2 amend.
</review>

