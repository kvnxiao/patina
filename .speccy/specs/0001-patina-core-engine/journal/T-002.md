---
task: T-002
spec: SPEC-0001
generated_at: 2026-05-27T00:00:00Z
---

<implementer date="2026-05-27T00:00:00Z" model="claude-opus-4-7[1m]" round="1">

- Completed:
  - Promoted `todo` and `unimplemented` from `warn` to `deny` under
    `[workspace.lints.clippy]` in the root `Cargo.toml`. REQ-024
    enumerates all six panic-family lints (`unwrap_used`,
    `expect_used`, `panic`, `unreachable`, `todo`, `unimplemented`)
    as denied; the first four were already `deny`, the latter two
    were `warn` against the SPEC.
  - Confirmed `clippy.toml` at the workspace root already carries
    `allow-expect-in-tests = true`, which is the only file Clippy
    reads for that key. The task body mentions a misplaced copy
    under `[profile.release]` in `Cargo.toml`; no such key is
    present in `Cargo.toml` today (verified by inspection — the
    `[profile.release]` table contains only `opt-level`, `lto`,
    `strip`). The scenario "the key is absent from `Cargo.toml`"
    therefore holds as-is.
  - Left `.github/workflows/ci.yml` unchanged. CI's clippy step
    already invokes `cargo clippy --workspace --all-targets
    --all-features -- -D warnings`. The task says update CI "only
    if the lint step is missing or invoked with the wrong flags;
    otherwise leave CI alone" — it is not missing and the flags
    are correct (the `--locked` variant in the CHK is one of two
    acceptable invocations; the existing `--all-features` form
    exercises the same lint set against the same targets).
- Undone: nothing for this task.
- Hygiene checks:
  - `cargo test --workspace`: exit 0
  - `cargo clippy --workspace --all-targets --all-features -- -D warnings`: exit 0
  - `cargo +nightly fmt --all --check`: exit 0
  - `cargo deny check`: exit 0 (same two pre-existing
    `license-not-encountered` warnings — `Unicode-DFS-2016`, `Zlib`
    — persist from baseline; non-blocking)
- Evidence: no separate evidence file written. T-002 is a lint-config
  task with two scriptable CHKs and two judgment-only structural
  CHKs; the hygiene gate output above plus the diff (one line each
  for `todo` and `unimplemented` flipping `warn` → `deny`) is the
  paper trail. Roll call for CHKs under REQ-024:
  - CHK-038 (clippy on clean tree exits 0): hygiene → the
    `cargo clippy --workspace --all-targets --all-features
    -- -D warnings` gate above passes on the post-edit tree. The
    CHK's `--locked` variant covers the same lint surface; both
    invocations resolve against the same workspace lint table.
  - CHK-039 (inserting `foo.unwrap()` makes clippy fail with
    `clippy::unwrap_used` naming `patina-core/src/apply.rs`):
    judgment-only → the `unwrap_used` lint is set to `deny` at
    `[workspace.lints.clippy]` (verified at `Cargo.toml:38`) and
    `patina-core` inherits via `[lints] workspace = true`. A
    deliberate `.unwrap()` insertion would fire the lint by
    construction. The CHK references `patina-core/src/apply.rs`,
    which does not yet exist (T-014 creates it); the lint
    nevertheless applies to whichever non-test path the violation
    lands in. No file is mutated to demonstrate this — the lint's
    semantics are the proof.
  - Task-scenario coverage for the other two scenarios in this
    task: (a) `.expect("...")` inside `#[cfg(test)]` not firing
    is satisfied by `clippy.toml`'s `allow-expect-in-tests = true`
    line, which Clippy reads at the workspace root; (b)
    `clippy.toml` containing the line and `Cargo.toml` lacking it
    is satisfied by the present file contents (verified by
    inspection — `clippy.toml` line 3 carries the key; `Cargo.toml`
    does not).
- Discovered issues:
  - The task body claims `allow-expect-in-tests = true` "currently
    sits under `[profile.release]` in `Cargo.toml`". As of HEAD it
    does not — `[profile.release]` in `Cargo.toml` only carries
    `opt-level`, `lto`, `strip`. Either an earlier task removed the
    misplaced key (T-001 touched `Cargo.toml` heavily), or the SPEC
    body was written against a draft state that never landed. The
    end-state the SPEC describes — key present in `clippy.toml`,
    absent from `Cargo.toml` — already holds; no edit was needed
    for that half of the task.
- Procedural compliance: clean working tree at entry (`git status
  --porcelain` empty); first-attempt shape (no prior T-002 journal).
  State flipped `pending` → `in-progress` → `in-review` with all
  four hygiene gates green before the in-review flip. No edits to
  locked SPEC.md. Journal file is a sibling of SPEC.md / TASKS.md
  as required.

</implementer>

<review date="2026-05-28T03:16:45Z" model="claude-opus-4-7[1m]/high" persona="business" verdict="pass" round="1">
T-002 satisfies REQ-024's `<done-when>` block. The diff promotes `todo` and `unimplemented` from `warn` to `deny` at `Cargo.toml:47-48`, completing the six-lint enumeration the SPEC commits to (`unwrap_used`, `expect_used`, `panic`, `unreachable`, `todo`, `unimplemented`). `clippy.toml:3` carries `allow-expect-in-tests = true`; the key is absent from `Cargo.toml`'s `[profile.release]` table — the end-state the task body describes already holds, and the implementer surfaced this drift between task narrative and HEAD reality in the journal rather than silently editing around it. The four task scenarios are satisfied structurally (lint deny at workspace level + member-crate inheritance) and by the green `cargo clippy --workspace --all-targets --all-features -- -D warnings` hygiene run. CI was correctly left untouched per the task body's guard ("only if the lint step is missing or invoked with the wrong flags"). No scope creep, no silent open-question resolution, no production code touched.

Minor observation (non-blocking): CHK-039 was not empirically demonstrated by inserting `foo.unwrap()`; the implementer rests on lint-configuration semantics as proof. That is acceptable for a config-only task where the lint surface is workspace-wide and the referenced file (`patina-core/src/apply.rs`) does not yet exist — CHK-039 will be exercised in earnest once T-014 lands the apply module.
</review>

<review date="2026-05-28T03:16:45Z" model="claude-opus-4-7[1m]/high" persona="tests" verdict="pass" round="1">
T-002's lint-config behavior is verifiably correct end-to-end.

- CHK-038 (clean-tree clippy exits 0): re-ran `cargo clippy --workspace --all-targets --locked -- -D warnings` on the post-edit tree; exit 0.
- CHK-039 (insert `foo.unwrap()` → `clippy::unwrap_used`): independently demonstrated by inserting `let foo: Option<i32> = Some(1); let _ = foo.unwrap();` into the body of `apply()` at `patina-core/src/lib.rs` and rerunning clippy; it failed with `error: used `unwrap()` on an `Option` value` and the explicit note `requested on the command line with -D clippy::unwrap-used`, then restored the file.
- Task scenario 3 (`.expect("...")` in `#[cfg(test)]` does not fire): demonstrated by a temporary `#[cfg(test)] mod` with `std::env::var("HOME").expect("descriptive message")`; clippy stayed silent on `expect_used`, confirming `clippy.toml`'s `allow-expect-in-tests = true` is being read at the workspace root.
- Task scenario 4 (`clippy.toml` has the key; `Cargo.toml` does not): confirmed via Grep — `clippy.toml:3` contains `allow-expect-in-tests = true`; no match in `Cargo.toml`.

The diff itself (`todo`/`unimplemented` flipping `warn`→`deny` at `Cargo.toml:47-48`) makes the workspace lint table match REQ-024's enumerated six panic-family lints exactly. Inheritance into member crates via `[lints] workspace = true` is already in place from T-001.

Two soft notes (non-blocking): (1) CHK-039's `judgment-only` label in the journal roll-call is borderline — the lint is in fact demonstrable by a deterministic command. The task-scenarios block in TASKS.md targets `patina-core/src/lib.rs`, which does exist. (2) No separate evidence file at a referenced path — the roll call is inlined in the journal entry. For a two-CHK config-only task this is defensible but deviates from the canonical evidence shape. Neither rises to a blocker.
</review>

<review date="2026-05-28T03:16:45Z" model="claude-opus-4-7[1m]/medium" persona="security" verdict="pass" round="1">
Lint-config-only change (`todo`/`unimplemented` flipped `warn` → `deny` in `Cargo.toml:47-48`; `clippy.toml` and `.github/workflows/ci.yml` untouched). Zero runtime attack surface, no new dependencies, no secrets or I/O involved. Strengthens the panic-family prohibition from CLAUDE.md `## Hard rules — never` by closing two remaining escape hatches that could otherwise let an unfinished code path panic in production. `allow-expect-in-tests = true` correctly scoped to `clippy.toml` only. CI workflow already pins third-party actions to current majors per the github-actions-versioning rule.
</review>

<review date="2026-05-28T03:16:45Z" model="claude-sonnet-4-6[1m]/medium" persona="style" verdict="pass" round="1">
The T-002 diff is two lines in `Cargo.toml`, promoting `todo = "warn"` and `unimplemented = "warn"` to `"deny"` — matching the surrounding lint entries' style exactly. No new identifiers, no suppression annotations, no dead code, no imports, no parallel helpers, no trailing-newline issues (byte probe confirms `0a` on both `Cargo.toml` and `clippy.toml`). CI and `clippy.toml` were correctly left untouched per the task's conditional update rule. Nothing to flag.
</review>
