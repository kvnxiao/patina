---
spec: SPEC-0001
generated_at: 2026-05-29T05:15:42Z
---

## Invocation 1 — 2026-05-29T05:15:42Z

<drift-review verdict="blocking" round="1" date="2026-05-29T05:42:10-04:00" model="claude-opus-4-8[1m]/high">
Template-render keys the `.tmpl` suffix off the TARGET, not the SOURCE — applying any SPEC-shaped `.tmpl` entry (target declared without `.tmpl`) fails at exit 1, breaking REQ-005 / CHK-011 / CHK-044 and the fresh-laptop user story end-to-end. Every template test masks it by declaring `.tmpl`-suffixed targets, which the SPEC never does.

- REQ-005 (implicit template render) / CHK-011 / CHK-044 / user-story "developer renders gitconfig.tmpl to ~/.gitconfig" → The template executor strips `.tmpl` from the **target** path and returns `ExecutorError::NotATemplate` when the target lacks that suffix. REQ-005 keys templating off the **source** `.tmpl` suffix and declares the target as its final, suffix-less path (`source = "gitconfig.tmpl"`, `target = "~/.gitconfig"`). Reproduced against the built binary with the exact CHK-011 fixture: `patina apply --yes` exits 1 with `template source ...\.gitconfig does not carry a `.tmpl` suffix to strip`, and `$HOME/.gitconfig` is never created (CHK-011 requires it to be a regular file containing the rendered output). See `patina-core/src/apply/template.rs:42` and `strip_tmpl_suffix` at `patina-core/src/apply/template.rs:59`. The fix must derive the output path from the source suffix / declared target, not require the target to carry `.tmpl`.
- REQ-005 test coverage is inverted, so green tests do not gate the requirement → Every template test declares the target WITH a `.tmpl` suffix and asserts the stripped path, encoding the inverted contract the SPEC forbids: `patina-core/tests/executor_modes.rs:216` (`target = home/gitconfig.tmpl`), `patina-core/tests/multi_target.rs:117-118` (`~/.claude/agent.toml.tmpl`), `patina-core/src/apply/template.rs:92-93`, and the CLI path `patina-cli/tests/apply_cli.rs:167` (`target = "~/.gitconfig.tmpl"`). CHK-011 declares `target = "~/.gitconfig"` and CHK-044 declares `targets = ["~/.claude/agent.toml", "~/.codex/agent.toml"]` — none with `.tmpl`. After the executor fix, these tests must be re-pointed at SPEC-shaped (suffix-less) targets or they will keep certifying the broken contract.
- REQ-010 / REQ-011 / REQ-020 (journal records canonical target paths) → Consequent to the above: `engine.rs::plan` (`patina-core/src/apply/engine.rs:234`, `planned_operation` at `:261`) records the template op's `PlannedOperation::Render` target as the declared canonical target, while the executor materializes (and the COMMIT record stores) the suffix-stripped path. For a SPEC-shaped entry these are the same path only because apply fails first; once the executor is fixed to honour a suffix-less target the two must be reconciled so the `.plan` target, the COMMIT `ExpectedTarget`, the recovery probe (`patina-core/src/journal/probe.rs:59`), and `debug journal` output all name the actual materialized file. Verify a template op's recorded `.plan` target matches the file the executor writes.
</drift-review>

<holistic-fix verdict="addressed" round="1" date="2026-05-29T06:18:44-04:00" model="claude-opus-4-8[1m]/high">
Summary: Re-keyed template-render off the source `.tmpl` suffix and write to the declared (suffix-less) target verbatim, re-pointing all inverted tests at SPEC-shaped targets.

Addressed:
- REQ-005 / CHK-011 / CHK-044 / fresh-laptop user story — executor keyed `.tmpl` off the TARGET and failed when the target lacked the suffix → Rewrote `patina-core/src/apply/template.rs:21` `render`: it now validates the **source** carries `.tmpl` (new `has_tmpl_suffix` at `template.rs:66`) and writes the rendered bytes to each declared target verbatim via `ensure_parent(target)` + `fs_err::write(target, ...)` (no target stripping). A SPEC-shaped entry (`source = "gitconfig.tmpl"`, `target = "~/.gitconfig"`) now materializes `~/.gitconfig` as a regular file. Removed the old `strip_tmpl_suffix` helper; repurposed `ExecutorError::NotATemplate` to guard a non-`.tmpl` source the engine should never classify as a render (`patina-core/src/apply/mod.rs:159` doc + message updated to say "source ... does not carry a `.tmpl` suffix"). Module-doc prose at `patina-core/src/apply/mod.rs:12` corrected ("written to each declared (suffix-less) target").
- REQ-005 inverted test coverage (green tests certified the forbidden contract) → Re-pointed every template test at SPEC-shaped suffix-less targets and added a guard test: unit tests `renders_once_and_writes_each_declared_target` and `non_tmpl_source_is_rejected` plus `has_tmpl_suffix_handles_case_and_absence` (`patina-core/src/apply/template.rs:87`); `patina-core/tests/executor_modes.rs:208` (`template_render_mode_renders_to_declared_target`, target `home/gitconfig`); `patina-core/tests/multi_target.rs:109` (targets `claude/agent.toml`, `codex/agent.toml`, now asserting `records[i].target` equals the declared target); CLI `patina-cli/tests/apply_cli.rs:167` (`target = "~/.gitconfig"`); and fixture `patina-cli/tests/deterministic_stdout.rs:38` (`target = "~/.shellrc"`). Tests now assert the rendered file lands at the declared target and is a regular file, gating the SPEC contract.
- REQ-010 / REQ-011 / REQ-020 (journal records canonical target paths) → Reconciled by construction: `engine.rs::planned_operation` (`patina-core/src/apply/engine.rs:261`) already records `PlannedOperation::Render` with the declared canonical (suffix-less) target, and the executor now materializes that same path and stores it in the COMMIT `CompletionRecord` (`patina-core/src/apply/template.rs:54`). The recovery probe's `operation_target` (`patina-core/src/journal/probe.rs:59`) reads the plan op's target, which is now identical to the materialized file, so the `.plan` target, COMMIT `ExpectedTarget`, recovery probe, and `debug journal` output all name the same file. The new `records[i].target == declared target` assertions in the multi-target test verify this directly.

Side discoveries:
- The lib-test clippy config denies `clippy::indexing_slicing` (it fired on `records[0]` inside `src/apply/template.rs` unit tests); integration tests under `tests/` permit indexing. I used an iterator-collected `Vec<&Utf8PathBuf>` comparison in the lib unit test to stay clean. No SPEC ambiguity surfaced — REQ-005's prose, `<done-when>`, `<behavior>`, and CHK-011/044 are all consistently source-keyed and suffix-less-target.
</holistic-fix>

<drift-review verdict="pass" round="2" date="2026-05-29T07:02:18-04:00" model="claude-opus-4-8[1m]/high">
All three round-1 drift findings are genuinely fixed in the current working tree: template-render now keys off the source `.tmpl` suffix and writes each declared suffix-less target verbatim (CHK-011/CHK-044 shapes pass end-to-end, fresh-laptop story unblocked), the inverted tests are re-pointed at SPEC-shaped targets, and the plan/COMMIT/probe/debug-journal target paths are reconciled by construction. No new drift, no new public surface, render-once preserved.
</drift-review>

<simplifier-scan verdict="clean">
The SPEC-0001 working-tree holistic template-render fix (and the surrounding committed diff) is already minimal and idiomatic; no behavior-preserving simplification remains within the diff's scope.

- `patina-core/src/apply/template.rs:68` — `has_tmpl_suffix` is single-use but kept as a named, unit-tested helper; inlining it would delete a real unit test (`has_tmpl_suffix_handles_case_and_absence`), so this is not a simplification.
- The `extension().is_some_and(|ext| ext.eq_ignore_ascii_case("tmpl"))` check is duplicated in `patina-core/src/config/file_entry.rs:123` (the `is_tmpl` classification), but consolidating would require editing a file outside the working-tree fix, couples two distinct layers (config classification vs executor guard), and is therefore out of the Phase 2 scope boundary — deliberately skipped.
- The per-iteration `source.to_path_buf()` / `target.clone()` in the render loop (`patina-core/src/apply/template.rs:53`) matches the sibling copy/symlink executors' allocation pattern; diverging would break local consistency for no clarity gain — skipped.
</simplifier-scan>

<gate verdict="passed" tasks_hash="b6ad9ba25bb00b0ef1345d15882139ddf629298bd7c40515efe1f2e81d9b4fa6" date="2026-05-29T05:28:20Z">
Drift cleared on round 2 (one drift-fix round: template-render re-keyed off the source `.tmpl` suffix, inverted tests re-pointed at SPEC-shaped targets); simplifier scan clean. Holistic gate passed.
</gate>
