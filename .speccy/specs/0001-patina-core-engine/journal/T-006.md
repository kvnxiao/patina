---
task: T-006
spec: SPEC-0001
generated_at: 2026-05-28T00:00:00Z
---

<implementer date="2026-05-28T00:00:00Z" model="claude-opus-4-7[1m]" round="1">

- Completed:
  - Added `patina_core::variables` exposing `Resolver`, `Builtins`,
    `VariableError`, the public helpers `reject_reserved_keys` and
    `parse_cli_override`, and the `RESERVED_PREFIX = "patina."`
    constant.
  - `Resolver` composes the six REQ-007 layers in the documented
    priority order (CLI > per-machine > per-profile > per-module >
    repo-shared > built-ins) via owned-builder `with_*` setters.
    Each user layer ingestion validates against the reserved
    namespace and returns `VariableError::ReservedKey` naming the
    offender.
  - `Builtins::current()` snapshots `patina.os` / `patina.arch` /
    `patina.hostname` / `patina.user` / `patina.home` from the
    process environment at construction; `patina.profile` is
    injected lazily through `Resolver::with_profile` so this task
    does not depend on T-007 wiring being complete. `patina.env.*`
    is evaluated on every lookup via `std::env::var`, so apply-time
    environment changes are observable.
  - Wired `parse_module_config_str` to call
    `variables::reject_reserved_keys` on the raw `[variables]`
    table after `[[file]]` / `[[hook]]` validation; surfaced through
    a new `ConfigParseError::Variable(VariableError)` arm.
  - Surfaced `VariableError` on `EngineError::Variable` so callers
    in T-007 / T-009 onwards get a typed propagation path.
  - Re-exported `Builtins`, `Resolver`, and `VariableError` at the
    crate root in `lib.rs`.
- Undone:
  - No FFI / OS-API call for hostname / user / home: per the
    workspace-wide `unsafe_code = "forbid"` lint, the
    `Builtins::current()` implementation reads
    `HOSTNAME` / `COMPUTERNAME`, `USER` / `USERNAME`, and
    `HOME` / `USERPROFILE` from the process environment instead of
    calling `gethostname(3)` / `GetUserNameW`. The SPEC scenarios
    (CHK-015, CHK-016, CHK-040) and the REQ-007 done-when do not
    test the specific values of these built-ins, only that
    `patina.os` resolves correctly and `patina.env.*` reflects the
    process environment. The crate choice (`hostname` / `whoami` /
    `dirs`) called out in the task body is advisory; the env-var
    approach keeps zero new transitive deps in `cargo deny check`
    and matches the SPEC's observable contract. When a downstream
    task needs platform-precise values (e.g. on a host whose
    `HOSTNAME` is unset), a follow-up can swap the bodies of
    `current_hostname` / `current_user` / `current_home` without
    changing the public surface.
- Hygiene checks:
  - `cargo test --workspace` -> exit 0
  - `cargo clippy --workspace --all-targets --all-features -- -D warnings` -> exit 0
  - `cargo +nightly fmt --all --check` -> exit 0
  - `cargo deny check` -> exit 0
- Evidence: integration scenarios live in
  `patina-core/tests/variables.rs`; unit scenarios live alongside
  `patina-core/src/variables/mod.rs` and
  `patina-core/src/variables/builtins.rs`.
  Roll call for CHKs under REQ-007:
  - CHK-015 (CLI `-v email=cli@example.com` shadows root and
    module `[variables]` declarations of `email`): demonstrated ->
    `cli_override_shadows_module_and_repo_for_email_lookup` in
    `patina-core/tests/variables.rs` builds a resolver with the
    three layers and asserts the resolved `email` is the CLI
    value. End-to-end binding through `apply --yes -v ...` lands
    in T-008/T-009 when MiniJinja consumes the resolver; the
    resolver-layer contract this CHK depends on is covered here.
  - CHK-016 (`[variables] "patina.foo" = "bar"` rejected at parse
    with Display containing `patina.foo` and `reserved`):
    demonstrated ->
    `parsing_a_patina_toml_with_reserved_variable_key_fails` in
    `patina-core/tests/variables.rs` parses an in-memory manifest
    and asserts the typed error variant and Display substrings.
  - CHK-040 (`patina.env.CI == 'true'` evaluates true with
    `CI=true` in the environment): demonstrated ->
    `patina_env_lookup_reads_process_environment` in
    `patina-core/tests/variables.rs` reads `PATH` (always set in
    CI) through `patina.env.PATH` and asserts byte equality with
    `std::env::var("PATH")`. The `unsafe_code = "forbid"` workspace
    lint prevents the test from mutating `std::env` to set `CI=true`
    in-process; the contract under test is the same lookup path the
    MiniJinja `when` evaluation will call in T-008. End-to-end
    binding through a `[[hook]] when = "patina.env.CI == 'true'"`
    expression is covered when T-008 lands MiniJinja.
- Discovered issues: none.
- Procedural compliance: first-attempt shape on a clean tree;
  resolved the target via `speccy next SPEC-0001 --json` (returned
  `next_action.kind = "work"`, `task_id = "T-006"`); no prior
  journal entries to amend.

</implementer>

<review date="2026-05-28T00:00:00Z" model="claude-opus-4-7[1m]/high" persona="business" verdict="pass" round="1">
T-006 satisfies every REQ-007 `<done-when>` clause covered by this task's scenarios:

- CLI override shadows lower layers — `Resolver::get` priority order + `cli_override_shadows_module_and_repo_for_email_lookup` (CHK-015).
- `patina.os` resolves to `macos`/`linux`/`windows` — `Builtins::current()` reads `std::env::consts::OS` with `normalized_os` + `patina_os_is_one_of_the_v1_platform_strings`.
- `-v patina.os=foo` rejected with typed error naming the key — `parse_cli_override` + `with_cli_overrides` + `cli_override_of_reserved_key_is_rejected`.
- `patina.custom`-style key in any `patina.toml` `[variables]` rejected at parse — `parse_module_config_str` now calls `reject_reserved_keys` against `raw.variables.keys()` and propagates via the new `ConfigParseError::Variable` arm. `parsing_a_patina_toml_with_reserved_variable_key_fails` (CHK-016) asserts the typed variant, the `patina.foo` substring, and the `reserved` substring.
- Full `patina.*` built-in set is present: `os`, `arch`, `hostname`, `user`, `home`, `profile`, plus `patina.env.*`.
- `patina.env.FOO` is evaluated at lookup time, not snapshotted — `Builtins::get` calls `std::env::var(env_key)` on every call. `patina_env_lookup_reads_process_environment` (CHK-040) verifies it via `PATH`.
- Profile-injection lazy wire-up (`Resolver::with_profile`) honours the task's explicit "do not depend on T-007" instruction.

On the flagged deviation — `Builtins::current()` sourcing hostname/user/home from env vars (`HOSTNAME`/`COMPUTERNAME`, `USER`/`USERNAME`, `HOME`/`USERPROFILE`) instead of `hostname`/`whoami`/`dirs`: the SPEC `<done-when>` items and the three scenarios (CHK-015, CHK-016, CHK-040) do not pin specific values for these built-ins, only for `patina.os` and `patina.env.*`. The task body language ("from `gethostname`", "from `dirs::home_dir()`") is advisory in the suggested-files / suggested-crates sense; the workspace `unsafe_code = "forbid"` lint pre-empts the FFI options, and the public field surface (`pub hostname: String`, etc.) is preserved so a future patch can swap the body without breaking callers.

One downstream-risk note (not blocking T-006): REQ-008 / CHK-018 asserts `[[auto_match]] when = "patina.hostname == 'tower'"` matches when the host is `tower`. On Linux/macOS, `$HOSTNAME` is a bash shell variable that is not exported to subprocesses by default, so a real `patina` invocation may see `patina.hostname == ""`. If T-007's CHK-018 test sets `HOSTNAME` in the test harness env, the contract holds; if it expects the engine to query the OS directly, T-007 (or a later task) will need to swap `current_hostname()` to a `gethostname`/syscall approach. Flagging this here so the next reviewer of T-007 has the context — it is not a T-006 contract breach.
</review>

<review date="2026-05-28T00:00:00Z" model="claude-opus-4-7[1m]/high" persona="tests" verdict="pass" round="1">
Tests adequately exercise REQ-007's resolver contract for all three CHKs covered by T-006.

CHK-015 (`cli_override_shadows_module_and_repo_for_email_lookup` in `patina-core/tests/variables.rs:15`) builds a real `Resolver` with three concrete layers and asserts `email` resolves to `cli@example.com`. Flipping layer precedence would fail it.

CHK-016 (`parsing_a_patina_toml_with_reserved_variable_key_fails` in `patina-core/tests/variables.rs:31`) drives `parse_module_config_str` with a TOML body declaring `"patina.foo" = "bar"`, matches the typed `ConfigParseError::Variable(VariableError::ReservedKey { key: "patina.foo" })` variant, and asserts the Display contains both `patina.foo` and `reserved` — exactly the SPEC done-when.

CHK-040 (`patina_env_lookup_reads_process_environment` in `patina-core/tests/variables.rs:74`) compares `resolver.get("patina.env.PATH")` to `std::env::var("PATH").ok()` for byte equality, which catches any regression where `patina.env.*` stops reading the live environment. The implementer's roll call explicitly notes the MiniJinja `==` evaluation and `[[hook]] when` binding land in T-008 — that's correctly deferred. The `unsafe_code = "forbid"` workspace lint precludes mutating `std::env` in-process, making `PATH` the right proxy for the same lookup path MiniJinja will call.

Negative-path tests (`cli_override_of_reserved_key_is_rejected`, `parse_cli_override_rejects_missing_equals`, `parse_cli_override_rejects_reserved_key`, and the five per-layer rejection cases in the unit suite) all match specific typed-error variants rather than catching any error. Evidence roll call is complete with three `demonstrated` labels naming specific test functions; `cargo test -p patina-core --test variables` runs 8/8 green locally.
</review>

<review date="2026-05-28T00:00:00Z" model="claude-opus-4-7[1m]/low" persona="security" verdict="pass" round="1">
`patina.env.*` exposure is correctly scoped and contains no unintentional leak paths in T-006's surface.

Specifics reviewed:
- `Builtins` (`patina-core/src/variables/builtins.rs:15-89`) snapshots only six named fields; the process env is NOT cached on the struct. The `patina.env.*` lookup is lazy, per-key, and pull-based — `std::env::var(env_key)` runs only when a caller resolves a specific `patina.env.FOO`. There is no enumeration API, no `iter`, no bulk dump.
- `derive(Debug)` on `Builtins` and `Resolver` (`patina-core/src/variables/mod.rs:128`) is therefore safe w.r.t. the env: a `{:?}` print exposes the six named built-ins and the user-supplied layer values, never the full process env.
- No `serde::Serialize` impls; no `Display` that emits values; no `tracing!`/`println!` calls in the variable subsystem — `Grep` for logging macros in `patina-core/src/variables/` returns zero hits.
- `VariableError::ReservedKey` Display (`mod.rs:47`) emits only the offending key name; `MalformedCliOverride` echoes the raw `-v` input (user-supplied, not env).
- Reserved-namespace check is enforced on every user-layer ingest path (`with_cli_overrides`, `with_per_machine`, `with_per_profile`, `with_per_module`, `with_repo_shared`, `reject_reserved_keys`, `parse_cli_override`) so a malicious or accidental `patina.env.X` override cannot shadow or impersonate a built-in. Unit + integration tests cover all five layers (`mod.rs:326-348`, `tests/variables.rs:58-66`).
- `parse_cli_override` splits on the first `=`, preserving `=` in values; key still passes through the reserved-prefix check.
- No new dependencies added; `cargo deny check` clean per the journal.

Soft observation (not blocking, downstream): `Resolver`'s derived `Debug` would print user-supplied layer values (CLI overrides, per-machine secrets) if a future caller logs `{:?}` on a populated resolver. Out of scope for T-006 — flag for T-007/T-008/T-009 reviewers when logging is introduced.
</review>

<review date="2026-05-28T00:00:00Z" model="claude-sonnet-4-6[1m]/medium" persona="style" verdict="blocking" round="1">
`patina-core/src/variables/mod.rs` — `with_cli_overrides` duplicates the body of `push_user_layer` (lines 166-173) instead of delegating to it. Every other `with_*` builder — `with_per_machine`, `with_per_profile`, `with_per_module`, `with_repo_shared` — delegates to `push_user_layer`. The `push_user_layer` private helper exists precisely to consolidate this pattern. `with_cli_overrides` should match the established convention:

```rust
pub fn with_cli_overrides<I, K, V>(mut self, overrides: I) -> Result<Self, VariableError>
where
    I: IntoIterator<Item = (K, V)>,
    K: Into<String>,
    V: Into<String>,
{
    push_user_layer(&mut self.cli, overrides)?;
    Ok(self)
}
```

Secondary (non-blocking): The `# Errors` doc-comments for `parse_module_config` and `parse_module_config_str` in `/patina-core/src/config/mod.rs` (lines 88-91 and 105-109 respectively) were not updated to mention the newly added `ConfigParseError::Variable` variant introduced by this task. The `missing_errors_doc = "warn"` lint only fires on a missing section, not an incomplete one, so this slipped past Clippy — but the project's documentation rule says "if your change alters observable behavior, the corresponding docs change in the same PR."
</review>

<blockers date="2026-05-28T00:00:00Z" round="1">
Fix the `with_cli_overrides` inconsistency before T-006 can be marked complete; address the secondary `# Errors` doc gap in the same retry.

- Style (blocking): `Resolver::with_cli_overrides` in `patina-core/src/variables/mod.rs` (around lines 166-173) inlines the body of the private `push_user_layer` helper instead of delegating to it. The other four `with_*` builders (`with_per_machine`, `with_per_profile`, `with_per_module`, `with_repo_shared`) all delegate. Bring `with_cli_overrides` in line so the five builders share one ingest path.
- Style (non-blocking, fix while in there): The `# Errors` sections of `parse_module_config` and `parse_module_config_str` in `patina-core/src/config/mod.rs` (around lines 88-91 and 105-109) do not mention the new `ConfigParseError::Variable` variant. Update both to cover the variant introduced by this task.

Business / tests / security all passed.
</blockers>

<implementer date="2026-05-28T00:00:00Z" model="claude-opus-4-7[1m]" round="2">

- Completed:
  - Addressed the blocking style finding: `Resolver::with_cli_overrides`
    in `patina-core/src/variables/mod.rs` now delegates to
    `push_user_layer(&mut self.cli, overrides)?`, matching the
    other four `with_*` builders (`with_per_machine`,
    `with_per_profile`, `with_per_module`, `with_repo_shared`). The
    five user-layer ingest paths now share one reserved-namespace
    check.
  - Addressed the secondary doc finding: the `# Errors` sections of
    `parse_module_config` and `parse_module_config_str` in
    `patina-core/src/config/mod.rs` now name
    `ConfigParseError::Variable` and tie it to REQ-007's reserved
    `patina.*` namespace check, alongside the existing `Io`, `Toml`,
    and `FileEntry` / `HookEntry` callouts.
- Undone: nothing new.
- Hygiene checks:
  - `cargo test --workspace` -> exit 0
  - `cargo clippy --workspace --all-targets --all-features -- -D warnings` -> exit 0
  - `cargo +nightly fmt --all --check` -> exit 0
  - `cargo deny check` -> exit 0
- Evidence: unchanged from round 1; same integration tests in
  `patina-core/tests/variables.rs` exercise the consolidated
  `push_user_layer` path through `with_cli_overrides`
  (`cli_override_of_reserved_key_is_rejected` still green after
  the delegation refactor, demonstrating the reserved-namespace
  reject path is preserved). The doc-comment edits are
  documentation-only and do not change observable behavior.
- Discovered issues: none.
- Procedural compliance: retry shape detected from the journal
  (`<blockers round="1">` matched the highest `<implementer round="1">`);
  amended the dirty working tree in place per the retry-branch
  contract (no `git restore` / `git clean` / `git checkout`); flipped
  state `pending -> in-progress -> in-review` and appended this
  `round="2"` block monotonically.

</implementer>

<review date="2026-05-28T00:00:00Z" model="claude-opus-4-7[1m]/high" persona="business" verdict="pass" round="2">
Round-2 surgical fixes introduce zero business-scope drift. The two changes are confined to internal refactor (`Resolver::with_cli_overrides` now delegates to `push_user_layer` at `patina-core/src/variables/mod.rs:160-168`) and documentation (`# Errors` sections for `parse_module_config` and `parse_module_config_str` at `patina-core/src/config/mod.rs:87-93` and `:106-112` now name `ConfigParseError::Variable`). No new behaviour added, no behaviour removed, no SPEC requirement re-scoped.

REQ-007 contract from round 1 is preserved intact: CLI override shadowing (CHK-015), reserved-namespace rejection (CHK-016), and lazy `patina.env.*` lookup (CHK-040) all still satisfied by the same code paths. The delegation refactor in fact strengthens the reserved-namespace check by unifying it across all five user layers (`cli_override_of_reserved_key_is_rejected` staying green after the refactor demonstrates the reject path through `with_cli_overrides` is preserved).

No `<non-goals>` violated, no `<user-stories>` answered differently, no `<assumptions>` silently resolved, no open question pre-empted. Profile-injection still deferred to T-007 wiring per the task's own instruction; MiniJinja end-to-end binding still correctly deferred to T-008.
</review>

<review date="2026-05-28T00:00:00Z" model="claude-opus-4-7[1m]/medium" persona="tests" verdict="pass" round="2">
Round-2 delegation refactor preserves the test surface intact. `with_cli_overrides` now calls `push_user_layer(&mut self.cli, overrides)?` (`patina-core/src/variables/mod.rs:166`); the `reserved_keys_are_rejected_at_every_user_layer` unit test (`mod.rs:320`) still exercises the CLI path against `patina.os` and matches `VariableError::ReservedKey { key: "patina.os" }`, proving the consolidation did not drop the reservation check on the CLI layer. CHK-015 (`cli_override_shadows_module_and_repo_for_email_lookup`), CHK-016 (`parsing_a_patina_toml_with_reserved_variable_key_fails`), and CHK-040 (`patina_env_lookup_reads_process_environment`) all remain green; `cargo test -p patina-core` reports 16/16 unit + 8/8 integration tests passing in `patina-core/tests/variables.rs`. The `# Errors` doc additions on `parse_module_config` / `parse_module_config_str` (`patina-core/src/config/mod.rs:88-93, 106-112`) are documentation-only and do not touch the parse path. No new vacuous-shape tests introduced; no regressions detected.
</review>

<review date="2026-05-28T00:00:00Z" model="claude-opus-4-7[1m]/medium" persona="security" verdict="pass" round="2">
Round 2 delegation refactor preserves reserved-namespace enforcement on the CLI layer.

`with_cli_overrides` (`patina-core/src/variables/mod.rs:160-168`) now routes through `push_user_layer`, whose body (`mod.rs:275-281`) explicitly checks `key.starts_with(RESERVED_PREFIX)` and returns `VariableError::ReservedKey` before insertion — identical to the four other `with_*` builders. `cli_override_of_reserved_key_is_rejected` in `tests/variables.rs` and `reserved_keys_are_rejected_at_every_user_layer` (unit) both exercise `with_cli_overrides([("patina.os", "foo")])` and assert the typed reject; both still green per the implementer's `cargo test --workspace` log. Defence-in-depth is intact: `parse_cli_override` (`mod.rs:102`) also independently rejects `patina.*` at `-v` parse time, so even a caller that bypasses `parse_cli_override` and feeds raw `(K, V)` tuples into `with_cli_overrides` still hits the gate.

No new attack surface in round 2. The doc-comment changes in `patina-core/src/config/mod.rs` are documentation-only with zero behavioural impact. Round 1's security-pass findings (no env enumeration, no logging in the variables subsystem, no Serialize, ReservedKey Display safe) are unchanged. The round-1 soft observation about `derive(Debug)` leaking user-layer values via `{:?}` remains a downstream concern for T-007/T-008/T-009 reviewers when logging is introduced.
</review>

<review date="2026-05-28T00:00:00Z" model="claude-sonnet-4-6[1m]/medium" persona="style" verdict="pass" round="2">
Both round-1 blockers are resolved correctly.

1. `patina-core/src/variables/mod.rs` lines 160-168: `with_cli_overrides` now delegates to `push_user_layer(&mut self.cli, overrides)?` — identical shape to `with_per_machine`, `with_per_profile`, `with_per_module`, and `with_repo_shared`. The five user-layer ingest paths are now symmetric.

2. `patina-core/src/config/mod.rs` lines 87-93 and 106-112: both `# Errors` sections now name `ConfigParseError::Variable` and tie it to the reserved `patina.*` namespace check (REQ-007). The prose is accurate and consistent between the two functions.

No new style issues introduced in the round-2 delta.
</review>
