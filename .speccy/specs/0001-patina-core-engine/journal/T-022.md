---
task: T-022
spec: SPEC-0001
generated_at: 2026-05-29T00:00:00Z
---

<implementer date="2026-05-29T00:00:00Z" model="claude-opus-4-8[1m]/medium" round="1">

## Completed

Operationalised REQ-025's cross-platform parity rule in
`.github/workflows/ci.yml`:

- The `test` matrix job (`strategy.matrix.os` = `[ubuntu-latest,
  macos-latest, windows-latest]`, `fail-fast: false`) now runs the
  exact REQ-025 command pair per OS: `cargo test --workspace --locked`
  followed by `cargo clippy --workspace --all-targets --locked -- -D
  warnings`. Added `components: clippy` to the toolchain step so the
  clippy command resolves on every runner. Replaced the prior
  `cargo build --all-features` + `cargo test --all-features` steps,
  which neither used `--locked` nor ran clippy per-OS as the
  requirement demands.
- Added a leading workflow comment (the task's documentation
  deliverable) naming the three required-status-check names the
  operator must add to branch protection on `main`:
  `Test (ubuntu-latest)`, `Test (macos-latest)`,
  `Test (windows-latest)`. Branch protection is GitHub-administered
  and not expressible in the file; the comment is the contract.
- Brought every third-party `uses:` reference to its current latest
  published major per the github-actions-versioning rule:
  `actions/checkout@v5` → `@v6` (all 7 occurrences) and
  `codecov/codecov-action@v5` → `@v6`. Both v6 bumps are node24-runtime
  updates with no workflow-input breaking changes for our usage
  (verified against the upstream v6.0.0 changelogs). `Swatinem/rust-cache@v2`,
  `taiki-e/install-action@v2`, `EmbarkStudios/cargo-deny-action@v2`
  were already at latest majors; `dtolnay/rust-toolchain@<channel>`
  is channel-floating and exempt per the rule.

## Undone

Nothing in scope. Branch-protection required-status-checks
configuration is operator-administered on GitHub by design (the task
explicitly scopes the deliverable to the workflow file plus the
operator note); it cannot be set from this repository.

## Hygiene checks

- `cargo test --workspace` (run as `--locked`): exit 0.
- `cargo clippy --workspace --all-targets --all-features -- -D warnings`: exit 0.
- `cargo +nightly fmt --all --check`: exit 0.
- `cargo deny check`: exit 0 (pre-existing `Zlib` unmatched-allowance
  warning only; advisories/bans/licenses/sources all ok).

## Evidence

No source-code paper trail file: this task's deliverable is a CI
configuration file, and both covered CHKs are CI-config / CI-runtime
properties rather than production-code invariants provable under the
local test command.

Roll call for CHKs under REQ-025:

- CHK-051 (workflow YAML has a job whose `strategy.matrix.os` contains
  `macos-latest` + `ubuntu-latest` + `windows-latest`, and `on:` has
  both `push` and `pull_request`): judgment-only → asserted by reading
  `.github/workflows/ci.yml`. Confirmed during implementation by
  parsing the file as YAML: `test.strategy.matrix.os` =
  `[ubuntu-latest, macos-latest, windows-latest]` and `on` keys =
  `[push, pull_request]`. No production-code test gates a static
  workflow file; reviewer-business judges the structure on the diff.
- CHK-052 (a Windows-only `compile_error!` forced into
  `patina-core/src/lib.rs` fails `windows-latest` while macOS/Linux
  pass, overall conclusion `failure`): judgment-only → requires a real
  cross-OS CI run, which is not reproducible in the local hygiene
  gate. The `fail-fast: false` matrix with the per-OS
  `cargo test --workspace --locked` step is the mechanism that makes a
  single-OS compile failure surface as that OS's job failing while the
  others stay green; reviewer judges the workflow shape on the diff.

## Discovered issues

The standalone `clippy` job (ubuntu-only, `--all-features`) and the
matrix job's per-OS clippy (`--locked`) now overlap on ubuntu. Left
the standalone job untouched: it is consumed by the `coverage` job's
`needs: [fmt, clippy, test]` and provides `--all-features` coverage the
matrix's `--locked` run does not. Removing or rewiring it is out of
T-022's scope.

## Procedural compliance

First-attempt shape (no prior T-022 journal, clean tree). Read
REQ-025's `<done-when>`, `<behavior>`, and both scenarios before
editing. Change is confined to `.github/workflows/ci.yml` plus the
TASKS.md state transitions. No SPEC.md edits. Hygiene gate run in full
before the `in-review` flip.

</implementer>

<review persona="business" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-05-29T04:40:06Z" round="1">
T-022 faithfully operationalises REQ-025. The `test` matrix job in `.github/workflows/ci.yml` (lines 58–72) declares `strategy.matrix.os: [ubuntu-latest, macos-latest, windows-latest]` with `fail-fast: false` and runs both required commands per OS (`cargo test --workspace --locked`, `cargo clippy --workspace --all-targets --locked -- -D warnings`); the `on:` block (lines 20–23) triggers on `push` to `main` and `pull_request` — satisfying CHK-051 and the first two done-when bullets. The `fail-fast: false` per-OS structure is the correct mechanism for CHK-052 and both `<behavior>` edge cases (a single-OS regression fails only that OS's job while the workflow conclusion is `failure`).

The branch-protection done-when bullet is GitHub-administered and the task explicitly scopes the deliverable to "the workflow file plus a one-paragraph note." The leading operator comment (lines 1–16) names the exact three check names `Test (ubuntu-latest)` / `Test (macos-latest)` / `Test (windows-latest)`, which correctly match GitHub's rendered `<job-name> (<matrix-value>)` form for `name: Test (${{ matrix.os }})`. This surfaces the operator action rather than silently resolving it — no open question was quietly decided.

Verified the versioning done-when bullet against upstream: `actions/checkout@v6` and `codecov/codecov-action@v6` are both the current latest published majors (checkout v6.0.2, codecov v6.0.1), and `dtolnay/rust-toolchain@<channel>` is the exempt channel-floating reference. No scope creep into non-goals; T-023's `cargo deny check` was not silently pulled forward, and the noted ubuntu clippy overlap is a defensible out-of-scope boundary, not a SPEC decision. The 2026-05-27 changelog row introduced REQ-025 with this exact intent and no later row altered it, so the diff reflects current intent.
</review>

<review persona="tests" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-05-29T04:40:06Z" round="1">
T-022 ships only `.github/workflows/ci.yml`; both covered CHKs (CHK-051, CHK-052) are correctly labelled `judgment-only` and are not the tests persona's to block. I verified the working-tree file against all four `<task-scenarios>` by parsing it as YAML: `test.strategy.matrix.os` = `[ubuntu-latest, macos-latest, windows-latest]`, `on:` triggers on both `push`→`main` and `pull_request` (scenario 1); the per-OS steps are exactly `cargo test --workspace --locked` then `cargo clippy --workspace --all-targets --locked -- -D warnings` (scenario 2); `fail-fast: false` is present, which is the mechanism that lets a Windows-only `compile_error!` fail `windows-latest` while macOS/Linux stay green (scenario 3, a live cross-OS-CI property not locally reproducible — `judgment-only` is plausible); and every third-party non-channel-floating `uses:` is at its latest major (`actions/checkout@v6` ×7, `codecov/codecov-action@v6`, rust-cache/install-action/cargo-deny at v2; `dtolnay/rust-toolchain@<channel>` exempt) (scenario 4). No fabricated evidence: the journal honestly declares no paper-trail file because the deliverable is static CI config, and no `hygiene`-labelled CHK makes an uncited test claim. Writing a project test that substring-matches this hand-authored YAML would be the vacuous-prose shape AGENTS.md forbids, so the absence of a project test is correct rather than a coverage gap. Nothing to block from the tests persona.
</review>

<review persona="security" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-05-29T04:40:06Z" round="1">
No security defect in the T-022 change to `.github/workflows/ci.yml`. The workflow triggers on `pull_request` (not `pull_request_target`), so forked-PR code gets no secret/write-token access; there is no `${{ github.event.* }}` interpolation in any `run:` block, so no script-injection sink (the only expansion is the controlled `${{ matrix.os }}`). Third-party actions are pinned to floating major tags (`@v6`/`@v2`), which is within the project's own `github-actions-versioning.md` policy for low-blast-radius CI that handles no deploy secrets; codecov upload is tokenless-safe and non-fatal (`fail_ci_if_error: false`). Non-blocking hardening note (out of scope, no vuln introduced): the workflow has no top-level `permissions:` block, so it inherits the repo default `GITHUB_TOKEN` scope — a future least-privilege pass could add `permissions: contents: read`.
</review>

<review persona="style" verdict="pass" model="claude-sonnet-4-6[1m]/medium" date="2026-05-29T04:40:06Z" round="1">
All `uses:` references in `.github/workflows/ci.yml` are correctly pinned to their current latest published major (`actions/checkout@v6`, `codecov/codecov-action@v6`, `Swatinem/rust-cache@v2`, `taiki-e/install-action@v2`, `EmbarkStudios/cargo-deny-action@v2`), with `dtolnay/rust-toolchain@<channel>` correctly left as channel-floating per the versioning rule. Comments are purposeful (operator note, REQ-025 reference, fail-fast rationale), no lint suppressions are introduced, no dead code, and the YAML structure is consistent with the project's existing conventions.
</review>
