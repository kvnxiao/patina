---
spec: SPEC-0001
task: T-025
generated_at: 2026-05-29T05:30:00Z
---

<implementer date="2026-05-29T05:30:00Z" model="claude-opus-4-8[1m]/medium" round="1">
- Completed: Closed out REQ-028's supply-chain gate. The root `deny.toml`
  and the `deny` CI job were already scaffolded by earlier infrastructure
  work, so this round (a) verified the existing artifacts satisfy every
  done-when clause, (b) filled the one real gap — the workflow's leading
  comment documented only the REQ-025 test-matrix required-status-check
  names, not the cargo-deny job — and (c) added the scriptable structural
  test. `deny.toml` carries the four required top-level tables
  (`[licenses]`, `[advisories]`, `[bans]`, `[sources]`); `[licenses].allow`
  lists only permissive licences (MIT / Apache-2.0 / BSD / ISC / Unicode /
  Zlib / CC0 / MPL) with no GPL-family entry; `[bans].wildcards = "deny"`;
  `[sources]` denies unknown registries and git. The CI job uses
  `EmbarkStudios/cargo-deny-action@v2`, which is the latest published major
  (v2.0.20, confirmed via `gh release list`), satisfying the
  github-actions-versioning rule, and inherits the workflow-level `on:`
  block triggering on `push` to `main` and `pull_request`. I extended the
  workflow's leading comment with a `Supply-chain gate (REQ-028)` block
  naming `Cargo deny` as the required-status check the operator must add to
  branch protection. New test `patina-cli/tests/cargo_deny_config.rs` parses
  `deny.toml` via the `toml` dev-dependency and asserts: the four top-level
  tables exist and are tables (CHK-060); no allow-listed licence contains
  `GPL` (CHK-061 policy half); `[bans].wildcards == "deny"` (wildcard
  task-scenario policy half). Modelled on the existing
  `patina-cli/tests/docs_structure.rs` (same `workspace_root_path` helper
  shape, `#![expect(clippy::expect_used)]` header, `fs-err` + `camino`).
- Undone: Nothing in scope. The branch-protection required-status-checks
  set is administered on GitHub and is not expressible in-repo; the
  workflow comment is the documented contract for it, per the same pattern
  REQ-025 uses. CHK-059 (binary exits 0 at HEAD) and CHK-061's
  binary-execution half are CI/hygiene-execution facts, not unit-testable
  without invoking the `cargo-deny` binary from a test; they are covered by
  the hygiene gate and CI, with the falsifiable policy half unit-tested.
- Hygiene checks: `cargo test --workspace` exited 0 (all suites including
  the new `cargo_deny_config.rs`, 3 cases). `cargo clippy --workspace
  --all-targets --all-features -- -D warnings` exited 0 (after one
  write-time fix: replaced an `unwrap_or_else(|| panic!(...))` — which trips
  `clippy::panic`, not covered by allow-expect-in-tests — with an
  `is_some_and(Value::is_table)` + `assert!`). `cargo +nightly fmt --all
  --check` exited 0. `cargo deny check` exited 0 (`advisories ok, bans ok,
  licenses ok, sources ok`; the pre-existing unmatched-license-allowance
  lines are non-fatal warnings).
- Evidence: paper trail at
  `.speccy/specs/0001-patina-core-engine/evidence/T-025.md`. Roll call for
  the CHKs under REQ-028:
  - CHK-059 (`cargo deny check` exits 0 at HEAD): hygiene → demonstrated by
    the `cargo deny check` hygiene gate (exit 0) and the `deny` CI job; not
    a unit test (would require invoking the cargo-deny binary from a test).
  - CHK-060 (deny.toml has the four top-level tables): demonstrated →
    `cargo_deny_config.rs::deny_toml_has_four_top_level_tables` parses the
    real `deny.toml` and asserts each table is present and is a table.
  - CHK-061 (a GPL-3.0 dep fails `cargo deny check` with a `licenses`
    error): demonstrated (policy half) → evidence Scenario 1 shows
    `cargo_deny_config.rs::licenses_allowlist_excludes_gpl_family` going red
    when `GPL-3.0` is injected into the allowlist and green when absent; the
    binary-exits-non-zero half is a CI/hygiene-execution fact backed by the
    `deny` job.
- Discovered issues: None. The pre-existing `deny.toml` and `deny` CI job
  already met the structural and trigger requirements; only the
  required-status-check documentation and the test were missing.
- Procedural compliance: First-attempt shape (no prior journal); clean tree
  at entry. Did not edit the locked SPEC.md. Did not hand-edit TASKS.md
  beyond the prescribed `pending → in-progress → in-review` state
  transitions on T-025. The temporary GPL-3.0 edit to `deny.toml` (for the
  red-evidence capture) was reverted; `deny.toml` is byte-identical to its
  committed contents.
</implementer>

<review persona="business" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-05-29T05:14:04Z" round="1">
T-025 satisfies every REQ-028 `<done-when>` clause and all of CHK-059/060/061 plus the task-body scenarios. Verified empirically: `cargo deny check` exits 0 at HEAD (`advisories ok, bans ok, licenses ok, sources ok`); the `deny` CI job at `.github/workflows/ci.yml:129-134` runs `EmbarkStudios/cargo-deny-action@v2` (confirmed latest major, 2.0.20) under an `on: { push: [main], pull_request }` trigger; the required-status-check name `Cargo deny` is documented in the workflow leading comment (ci.yml:18-28), the same in-repo contract pattern REQ-025 uses. The out-of-allowlist → named-`licenses`-error path is real: removing a used license from `deny.toml`'s allowlist made `cargo deny check licenses` exit 101 with `error[rejected]` naming `unicode-ident v1.0.24` (GPL-3.0 is one instance of this). `patina-cli/tests/cargo_deny_config.rs` (3 tests) gates the falsifiable policy halves and passes; `deny.toml` is byte-identical to its committed state after my probing (git reports clean). The task body's mention of granular advisories keys (`vulnerability`/`unsound`/`unmaintained`/`notice`, `unlicensed`) is superseded by the cargo-deny v2 schema (`version = 2`) the file actually uses, and the authoritative SPEC `<done-when>` does not require those keys — observable contract is met. No non-goals breached, no open question silently decided, Changelog intent (REQ-028 added 2026-05-27) matches the diff.
</review>

<review persona="tests" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-05-29T05:14:04Z" round="1">
T-025 tests genuinely exercise the REQ-028 policy. I independently reproduced the red-then-green transition: with `GPL-3.0` injected into `deny.toml [licenses].allow`, `licenses_allowlist_excludes_gpl_family` fails naming the licence (`patina-cli/tests/cargo_deny_config.rs:79`); reverted, all 3 cases pass. `cargo deny check` exits 0 at HEAD (warnings non-fatal), confirming CHK-059. The three tests parse the real `deny.toml` by key and assert table presence (CHK-060), GPL exclusion (CHK-061 policy half), and `[bans].wildcards == "deny"` (wildcard task-scenario policy half) — none are vacuous; each fails on a realistic regression. Evidence roll call covers all three CHKs (CHK-059 hygiene with a scoped, re-runnable `cargo deny check` cite; CHK-060/CHK-061 demonstrated with named tests), the evidence command is per-slice scoped not the full hygiene run, and runner output structure matches genuine `cargo test`. The CI-workflow and binary-exit-non-zero halves are correctly deferred to CI-execution facts (`deny` job uses `cargo-deny-action@v2`, `on: push:[main]`/`pull_request`). deny.toml verified byte-clean after my temporary edit. No fabrication, no blocking gaps.
</review>

<review persona="security" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-05-29T05:14:04Z" round="1">
Supply-chain gate is genuinely enforced, not cosmetic. `deny.toml` carries all four tables; `[licenses]` is a deny-by-default permissive allowlist with no GPL/copyleft, `exceptions = []`, and no escape-hatch fields, so an unlisted (e.g. GPL-3.0) dependency fails the `licenses` check (CHK-061). `[advisories]` v2 treats RustSec vulnerabilities as hard errors, `yanked = "deny"`, `ignore = []`. `[bans].wildcards = "deny"` closes the `*`-version semver-bypass. `[sources]` denies unknown registries and git, allowing only crates.io — blocking dependency-confusion / arbitrary-git vectors. `cargo deny check` exits 0 at HEAD (CHK-059 verified locally); the `deny` CI job runs `EmbarkStudios/cargo-deny-action@v2` (latest major 2.0.20, default `check --all-features` running all four checks) on `push`/`pull_request`; the `Cargo deny` required-status-check is documented as the branch-protection contract per the REQ-025 precedent (GitHub-administered, not in-repo expressible — out of scope). New `patina-cli/tests/cargo_deny_config.rs` gates falsifiable policy invariants, not prose or copied constants. No injection, secret-handling, or auth surface in this task. No blockers.
</review>

<review persona="style" verdict="pass" model="claude-sonnet-4-6[1m]/medium" date="2026-05-29T05:14:04Z" round="1">
Re-run verdict. The first style pass in this round read `deny.toml` mid-mutation while the concurrent tests/security personas had temporarily injected `GPL-3.0` for their red-then-green reproductions, and returned a `blocking` finding on that transient state. The orchestrator verified ground truth (`deny.toml` absent from `git status --porcelain`, `grep GPL deny.toml` returns no matches — byte-identical to committed, never modified by this task) and re-ran the style persona against the stable clean tree; this is the valid superseding verdict.

All changed files conform to project conventions. `patina-cli/tests/cargo_deny_config.rs` matches the `docs_structure.rs` sibling shape exactly: `#![expect(clippy::expect_used, reason = "...")]` with a justification (not a bare silencer), `workspace_root_path` helper reused from the established pattern, `camino` + `fs-err` imports, and consistent test-function naming. `deny.toml` carries no GPL-family entries (confirmed by grep returning no matches and `git status --porcelain` showing the file clean). The workflow's leading comment block extends the existing REQ-025 operator note with a matching REQ-028 block at the same indentation and register. `EmbarkStudios/cargo-deny-action@v2` is the current latest major, satisfying the github-actions-versioning rule. No dead imports, duplicate helpers, or suppression annotations without justification.
</review>
