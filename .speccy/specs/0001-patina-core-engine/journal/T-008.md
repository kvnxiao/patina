---
task: T-008
spec: SPEC-0001
generated_at: 2026-05-28T00:00:00Z
---

<implementer date="2026-05-28T00:00:00Z" model="claude-opus-4-8[1m]/high" round="1">

- Completed:
  - Added `patina_core::template` exposing `Engine` (re-exported at the
    crate root as `TemplateEngine`) and `TemplateError` (re-exported as
    `TemplateError`, wired onto `EngineError::Template(#[from])`).
  - `Engine` holds one `Arc<minijinja::Environment<'static>>` configured
    with `UndefinedBehavior::SemiStrict`. `Engine::render(body, resolver)`
    renders a `.tmpl` body; `Engine::eval_when(expr, resolver)` evaluates
    a `[[file]]` / `[[hook]]` `when` predicate. Both build the same
    `minijinja::Value` context per call from the six-layer T-006
    `Resolver`. `Engine` is `Clone`; clones share the one `Environment`
    via the `Arc` (`shared_environment()` exposes the handle for the
    single-instance property check).
  - Chose `SemiStrict` over `Strict` deliberately. `Strict` errors the
    moment an undefined value is *tested*, including the `{% if defined %}`
    condition — which breaks REQ-009's `{% else %}` carve-out
    (`{% if defined %}{{ x }}{% else %}fallback{% endif %}` would error
    instead of rendering `fallback`). `SemiStrict` errors on *emit* /
    coercion of an undefined value (CHK-019) while treating a bare
    `{% if missing %}` test as falsy (the carve-out). Verified against
    the minijinja 2.20 source (`utils.rs` `is_true` / `handle_undefined`
    and `vm/mod.rs` `Emit`) and a throwaway harness before settling on
    `SemiStrict`. The choice and its reasoning are documented in the
    `mod.rs` module docs so a future reader does not "fix" it back to
    `Strict`.
  - Named-variable error recovery: minijinja's `UndefinedError` carries
    only the generic `"undefined value"` message — it does **not** name
    the missing variable, but REQ-009's done-when requires the typed
    error's Display to name it (`user_email`, `missing_var`). Solved by
    making the template context a dynamic `minijinja::Object`
    (`src/template/strict.rs`): top-level lookups route through
    `PatinaContext::get_value`, `patina.*` through
    `PatinaBuiltins::get_value`, `patina.env.*` through
    `PatinaEnv::get_value`. Every miss records the requested key in a
    per-evaluation `UndefinedTracker` (a `Mutex<RefCell<BTreeSet>>`,
    behind `Mutex` to satisfy minijinja's `Send + Sync` `Object` bound;
    `BTreeSet` keeps the reported list deterministic for REQ-021).
    `classify()` reads the tracker after a render error;
    `coerce_when_result()` reads it after a `when` eval that returned an
    undefined value.
  - `when`-result coercion: `compile_expression().eval()` returns an
    undefined *value* (not an `Err`) when the expression resolves to
    undefined — e.g. the short-circuit result of `<true> and missing_var`.
    `eval_when` checks `value.is_undefined()` and converts that into
    `TemplateError::UndefinedVariable` (CHK-020) rather than silently
    treating the predicate as false; a defined result returns
    `value.is_true()`.
  - Added `minijinja = "2.20"` to `[workspace.dependencies]` and to
    `patina-core/Cargo.toml`. MIT-licensed; passes `cargo deny`.
  - Integration scenarios in `patina-core/tests/template_strict.rs` plus
    unit tests in `src/template/mod.rs`.
- Undone:
  - **End-to-end `patina apply --yes` exit-1 + stderr surface (CHK-019).**
    The apply plan computation (file-mode executors T-014, hook execution
    T-015) and the CLI surface / exit-code mapping (T-016, REQ-026
    `output::Reporter`) are downstream — there is no apply plan to run a
    `.tmpl` through yet. This task ships the plan-computation contract
    those layers consume: an undefined template reference produces a
    typed `TemplateError::UndefinedVariable` naming the variable, which
    `EngineError::Template(#[from])` propagates. The CLI maps it to exit 1
    with the name on stderr once T-016 lands. This mirrors the precedent
    T-006 / T-007 set when end-to-end `--json` assertions were deferred to
    T-009 / T-016. CHK-020 is fully demonstrated at the engine layer here.
  - **Wiring the engine into the apply pipeline.** `apply()` still returns
    `EngineError::NotImplemented`; T-014 / T-015 construct the `Engine`
    once per apply and call `render` / `eval_when`. The single-shared-
    instance property (the wiring CHK names) is demonstrated structurally
    by `Engine`'s `Arc<Environment>` and the `Arc::ptr_eq` clone test.
- Hygiene checks:
  - `cargo test --workspace` -> exit 0 (7 new integration tests in
    `tests/template_strict.rs`, 6 new unit tests in `src/template/mod.rs`,
    1 new doc-test on the `template` module; no regressions across the
    existing suites).
  - `cargo clippy --workspace --all-targets --all-features -- -D warnings`
    -> exit 0
  - `cargo +nightly fmt --all --check` -> exit 0
  - `cargo deny check` -> exit 0 (new `minijinja` (MIT) + transitive
    `memo-map` pass advisories / bans / licenses / sources; only the
    three pre-existing `license-not-encountered` baseline warnings remain).
- Evidence: paper trail at
  `.speccy/specs/0001-patina-core-engine/evidence/T-008.md`.
  Roll call for CHKs under REQ-009:
  - CHK-019 (undefined `{{ user_email }}` in a `.tmpl` → exit 1, stderr
    contains `user_email`): demonstrated at the plan-computation layer →
    `template_with_undefined_variable_fails_naming_it` in
    `patina-core/tests/template_strict.rs` renders the literal CHK-019
    body `[user]\nemail = {{ user_email }}` against a resolver with no
    `user_email` at any layer and asserts the error is
    `TemplateError::UndefinedVariable` whose Display contains
    `user_email`. The CLI exit-1 + stderr binding lands with T-016; the
    engine contract the CLI reads from is covered here.
  - CHK-020 (`when = "patina.os == 'macos' and missing_var"` →
    typed error whose Display contains `missing_var`): demonstrated →
    `when_predicate_with_undefined_variable_fails_naming_it` in
    `patina-core/tests/template_strict.rs`. Because `and` short-circuits,
    the test pins the left operand to the host's own resolved `patina.os`
    value so the predicate always reaches `missing_var` regardless of
    where tests run, then asserts `TemplateError::UndefinedVariable`
    naming `missing_var`. The else-block carve-out
    (`else_block_undefined_renders_fallback_without_error`) and the
    single-shared-environment property
    (`render_and_when_share_one_environment_instance`, via `Arc::ptr_eq`)
    are also demonstrated — both are REQ-009 done-when clauses.
- Discovered issues:
  - REQ-009's done-when is split across two evaluation paths with
    *different* minijinja semantics under one `UndefinedBehavior`: render
    (emit) errors on undefined, but `compile_expression().eval()` returns
    an undefined value rather than erroring. The `eval_when` undefined-
    result coercion is load-bearing for CHK-020 — a future refactor that
    drops it would silently treat an undefined `when` as `false`. Flagged
    so T-015 (hook execution, which calls `eval_when`) does not
    re-introduce the silent-false behaviour.
  - The dynamic `Object` context advertises no keys via
    `Enumerator::Empty`, so `{% for k in <bare-context> %}` is a no-op
    rather than iterating the variable layers. No SPEC scenario needs
    context enumeration; recorded in case a later template feature does.
- Procedural compliance: first-attempt shape on a clean tree (empty
  `git status --porcelain`, no prior `journal/T-008.md`); resolved the
  target via `speccy next SPEC-0001 --json` (`next_action.kind = "work"`,
  `task_id = "T-008"`); no prior journal entries to amend.

</implementer>

<review persona="business" verdict="pass" model="claude-opus-4-8[1m]/high" date="2026-05-28T17:35:48Z" round="1">
T-008 satisfies REQ-009's observable behavior contract. All four done-when clauses map to passing tests: undefined `{{ var }}` → typed error naming the variable; undefined `when` → typed error naming the variable (plus a bare-predicate guard against silent-false); single shared `MiniJinja` environment (`Arc::ptr_eq`); and the `{% else %}` carve-out rendering `fallback`. CHK-020 is fully delivered at the engine layer. CHK-019's end-to-end CLI exit-1/stderr surface is legitimately deferred to T-014 (the `.tmpl` executor) and T-016 (exit-code mapping) — there is no apply pipeline yet (`apply()` returns `NotImplemented`), and the engine-layer contract the CLI consumes (undefined → `TemplateError::UndefinedVariable` propagated via `EngineError::Template(#[from])`, `error.rs:54`) is delivered and tested. This deferral matches the SPEC's task sequencing and the T-006/T-007→T-009/T-016 precedent.

The one deviation worth noting is the engine uses `UndefinedBehavior::SemiStrict` (`patina-core/src/template/mod.rs:124`) while REQ-009's prose/title literally say `Strict`. This is not a contract miss: verified in the locked minijinja 2.20.0 source that `Strict` would error on the `{% if defined %}` condition and break the SPEC's own done-when clause 4 and its assumptions-block carve-out (SPEC.md:128-131). `SemiStrict` is the only variant satisfying all four done-when clauses simultaneously. The implementer documented the deviation and surfaced it rather than silently resolving it. The literal keyword is best reconciled via a cosmetic `/speccy-amend` to REQ-009 prose; it is not a task blocker.
</review>

<review persona="tests" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-05-28T17:35:48Z" round="1">
T-008's tests genuinely exercise REQ-009's behavior at the plan-computation layer through the real `TemplateEngine` public API (no mocking of the SUT). All four task-scenarios are covered with specific assertions: `template_with_undefined_variable_fails_naming_it` (`tests/template_strict.rs:26`) drives CHK-019's literal `[user]\nemail = {{ user_email }}` body and asserts both the `UndefinedVariable { names }` variant naming `user_email` and the Display substring; `when_predicate_with_undefined_variable_fails_naming_it` (`:50`) covers CHK-020, correctly pinning the `and` left operand to the host's resolved `patina.os` so the short-circuit always reaches `missing_var`; `bare_undefined_when_predicate_is_an_error_not_false` (`:72`) directly guards the silent-false regression; `else_block_undefined_renders_fallback_without_error` (`:86`) asserts exact `"fallback"` output; `render_and_when_share_one_environment_instance` (`:98`) proves the single-instance property via `Arc::ptr_eq`. Each assertion would fail under the obvious mutations. Ran `cargo test -p patina-core --test template_strict` — 7/7 pass, matching the evidence runner output verbatim. The CHK-019 end-to-end surface is honestly deferred to T-014/T-016. Evidence roll call is complete: CHK-019 and CHK-020 both present, each citing a named test. Minor non-blocking note: the evidence captures green-only runner output rather than a red-then-green transition, but the output is structurally genuine and the tests demonstrably catch mutations.
</review>

<review persona="security" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-05-28T17:35:48Z" round="1">
T-008 (strict-undefined MiniJinja template engine, REQ-009) passes security review. MiniJinja is sandboxed and the engine registers no custom functions/filters/globals/loaders; `render_named_str` renders in-memory strings so `{% include %}`/`{% import %}` have no loader and cannot reach the filesystem — no SSTI escalation or path-traversal vector. Error surfaces leak only variable *names*, never resolved values (`TemplateError::UndefinedVariable` in `patina-core/src/template/mod.rs:84-89`); the trust boundary is the user's own dotfiles repo and own machine. `patina.env.*` reading arbitrary process env at access time (`strict.rs:174-185`) is the documented REQ-007 contract and self-targeting — acceptable, not a finding. Production paths are panic-free: mutex-poison degrades gracefully (`strict.rs:108-113`) and `coerce_when_result` fails closed on an undefined `when` predicate rather than silently treating it as false (`mod.rs:183-202`), a desirable security property. Dependency `minijinja 2.20.0` (MIT) passed `cargo deny` advisories/bans/licenses; no known CVE at this version.
</review>

<review persona="style" verdict="blocking" model="claude-sonnet-4-6[1m]/medium" date="2026-05-28T17:35:48Z" round="1">
`patina-core/src/template/strict.rs:85` — `UndefinedTracker` wraps its set in `RefCell<BTreeSet<String>>` inside a `Mutex`, giving `Mutex<RefCell<BTreeSet<String>>>`. The `RefCell` is redundant: `Mutex::lock()` already yields a `&mut MutexGuard<T>` providing exclusive mutable access, so the `RefCell` adds a second interior-mutability layer that does nothing. The module doc at line 79 even acknowledges the `Mutex` is what satisfies `Send + Sync`; the `RefCell` note only explains `&self` mutability, which the `Mutex` already covers.

The project's AGENTS.md mandate ("Write the minimum code that solves the problem") and CLAUDE.md ("Clear intent over clever code") both apply. Replace the two-layer `Mutex<RefCell<BTreeSet<String>>>` with a plain `Mutex<BTreeSet<String>>`: have `UndefinedTracker` hold `names: BTreeSet<String>` with `&mut self` `record`/`names` methods, and `record_undefined` lock the mutex and call `guard.record(name)`. Remove `use std::cell::RefCell` (dead import after the change) and update the struct doc comment to drop the `RefCell` mention. The `tracker.lock().map(|t| t.names())` calls in `mod.rs` stay as-is since `lock()` returns a guard that deref-coerces to `&UndefinedTracker`. No other blocking findings. Clippy, fmt, and tests all pass clean.
</review>

<blockers date="2026-05-28T17:35:48Z" round="1">
Simplify the `UndefinedTracker` interior-mutability layering before the next review pass.
- style: `patina-core/src/template/strict.rs:85` wraps `RefCell<BTreeSet<String>>` inside a `Mutex`, but `Mutex::lock()` already grants exclusive `&mut` access, so the `RefCell` is a redundant second layer. Collapse `Mutex<RefCell<BTreeSet<String>>>` to a plain `Mutex<BTreeSet<String>>` (struct holds `names: BTreeSet<String>` with `&mut self` `record`/`names` methods), remove the now-dead `use std::cell::RefCell` import, and drop the `RefCell` mention from the struct doc comment. Verify clippy/fmt/test stay green.
</blockers>

<implementer date="2026-05-28T00:00:00Z" model="claude-opus-4-8[1m]/medium" round="2">

- Completed:
  - Addressed the round-1 style blocker by collapsing the redundant
    interior-mutability layering in
    `patina-core/src/template/strict.rs`. `UndefinedTracker` now holds a
    plain `names: BTreeSet<String>` (was `RefCell<BTreeSet<String>>`); the
    surrounding `Mutex<UndefinedTracker>` is the single interior-mutability
    layer.
  - `record` now takes `&mut self` and inserts directly into the set;
    `record_undefined` locks the mutex mutably (`if let Ok(mut tracker)`)
    and calls `tracker.record(name)`. `names()` stays `&self` (read-only,
    and the `mod.rs` callers invoke it through a non-`mut` `MutexGuard`
    binding in `tracker.lock().map(|t| t.names())`, which deref-coerces to
    `&UndefinedTracker` — those call sites are unchanged).
  - Removed the now-dead `use std::cell::RefCell` import.
  - Updated the `UndefinedTracker` struct doc comment to drop the `RefCell`
    mention and explain that the `Mutex` alone provides both the
    `Send + Sync` bound and the exclusive `&mut` access the `&self`
    `Object::get_value` implementors need.
- Undone:
  - No change from round 1. The end-to-end `patina apply --yes` exit-1 +
    stderr surface (CHK-019) and the apply-pipeline wiring remain deferred
    to T-014 / T-015 / T-016, exactly as recorded in round 1. This amend
    is scoped to the style blocker; no behavior or test changed.
- Hygiene checks:
  - `cargo test --workspace` -> exit 0 (all suites green, including the 7
    `tests/template_strict.rs` integration tests and the
    `src/template/mod.rs` unit + doc tests; no regressions from the
    refactor).
  - `cargo clippy --workspace --all-targets --all-features -- -D warnings`
    -> exit 0
  - `cargo +nightly fmt --all --check` -> exit 0
  - `cargo deny check` -> exit 0 (only the three pre-existing
    `license-not-encountered` baseline warnings remain).
- Evidence: paper trail at
  `.speccy/specs/0001-patina-core-engine/evidence/T-008.md` (round-1
  evidence stands; this amend is a behavior-preserving simplification, so
  the CHK demonstrations are unchanged). Roll call for CHKs under REQ-009:
  - CHK-019 (undefined `{{ user_email }}` → typed error naming it):
    demonstrated → `template_with_undefined_variable_fails_naming_it` in
    `patina-core/tests/template_strict.rs` still passes after the refactor;
    the tracker still records the missing name and the error names it.
  - CHK-020 (`when = "... and missing_var"` → typed error naming
    `missing_var`): demonstrated →
    `when_predicate_with_undefined_variable_fails_naming_it` and the
    `bare_undefined_when_predicate_is_an_error_not_false` guard both still
    pass; the else-block carve-out
    (`else_block_undefined_renders_fallback_without_error`) and the
    single-shared-environment property
    (`render_and_when_share_one_environment_instance`) are unaffected by a
    change confined to the tracker's internal storage.
- Discovered issues:
  - None new. The round-1 note about `eval_when`'s undefined-result
    coercion being load-bearing for CHK-020 still stands for T-015.
- Procedural compliance: retry shape detected from the journal (latest
  `<blockers round="1">` matches the highest `<implementer round="1">`);
  amended the prior pass's uncommitted WIP in place rather than rewriting
  from scratch (no `git restore`/`clean`/`checkout` against the dirty
  paths); flipped T-008 `pending` -> `in-progress` -> `in-review` and
  appended this `round="2"` block (monotonic +1) without modifying earlier
  journal entries.

</implementer>

<review persona="business" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-05-28T17:41:17Z" round="2">
T-008 (round 2) preserves REQ-009's business contract. The round-2 change is a behaviour-preserving simplification confined to `patina-core/src/template/strict.rs` (UndefinedTracker storage `Mutex<RefCell<BTreeSet>>` → `Mutex<BTreeSet>`); no public API, scenario coverage, or observable behaviour changed. All four REQ-009 done-when clauses still map to passing tests (undefined `{{ var }}`/`when` → typed error naming the variable; single shared MiniJinja environment via `Arc::ptr_eq`; `{% else %}` carve-out renders `fallback`) — verified 7/7 green via `cargo test -p patina-core --test template_strict`. CHK-019's end-to-end `apply --yes` exit-1/stderr surface remains legitimately deferred to T-014/T-016 (no apply pipeline yet; `apply()` returns `NotImplemented`), consistent with the round-1 panel and the T-006/T-007 precedent; the engine-layer contract the CLI consumes (`TemplateError::UndefinedVariable` via `EngineError::Template(#[from])`) is delivered. The `SemiStrict`-vs-literal-`Strict` keyword gap was adjudicated non-blocking in round 1 (cosmetic /speccy-amend), justified by SPEC.md:128-131, and is untouched here. No scope creep, no non-goal violation, no silently-resolved open question.
</review>

<review persona="tests" verdict="pass" model="claude-opus-4-8[1m]/high" date="2026-05-28T17:41:17Z" round="2">
T-008's tests genuinely exercise REQ-009 at the plan-computation layer through the real `TemplateEngine` public API — no mocking of the SUT. Independently ran `cargo test -p patina-core --test template_strict` (7/7 pass, matching the evidence output) and ran two mutation experiments to confirm the assertions are load-bearing: (1) forcing `coerce_when_result` to silently return `Ok(false)` on an undefined `when` value failed `when_predicate_with_undefined_variable_fails_naming_it` (CHK-020) and `bare_undefined_when_predicate_is_an_error_not_false`; (2) disabling undefined-name recording in `strict.rs` failed all three naming tests. All four REQ-009 done-when clauses map to specific assertions. The round-2 change is a behavior-preserving `RefCell` removal — confirmed no `RefCell` remains in `src/template/` and the full test set still passes, so no scenario coverage regressed. Evidence roll call under REQ-009 is complete: CHK-019 and CHK-020 both present with named tests that exist in the diff; runner output structurally genuine and the evidence command scoped per-slice. CHK-019's end-to-end CLI surface honestly deferred to T-014/T-016. Non-blocking note (carried from round 1): green-only evidence rather than red-then-green, but tests demonstrably catch mutations.
</review>

<review persona="security" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-05-28T17:41:17Z" round="2">
T-008 round-2 (strict-undefined MiniJinja engine, REQ-009) passes security review. The round-2 change is confined to collapsing `UndefinedTracker`'s `Mutex<RefCell<BTreeSet<String>>>` to a plain `Mutex<BTreeSet<String>>` — a behavior-preserving interior-mutability simplification with no security surface. All security-relevant properties from the round-1 pass are intact: error surfaces leak only variable *names*, never resolved values (`mod.rs:84-89`); `coerce_when_result` still fails closed on an undefined `when` predicate rather than treating it as false, including the no-recorded-name fallback (`mod.rs:183-202`); `record_undefined` still degrades gracefully on a poisoned lock via `if let Ok(mut tracker)` (`strict.rs:107-112`) — strict semantics hold even if the name is lost, no panic; no template loader/custom function/filter/global is registered and `render_named_str` renders in-memory strings (`mod.rs:147`), so no SSTI or path-traversal vector. `patina.env.*` reading process env at access time remains the documented self-targeting REQ-007 contract. No new dependency introduced in round 2; `minijinja 2.20` (MIT) already cleared `cargo deny` with no known CVE.
</review>

<review persona="style" verdict="pass" model="claude-sonnet-4-6[1m]/medium" date="2026-05-28T17:41:17Z" round="2">
Round-1 blocker resolved. `patina-core/src/template/strict.rs` now holds `UndefinedTracker { names: BTreeSet<String> }` with `record(&mut self)` and a plain `Mutex<UndefinedTracker>` as the sole interior-mutability layer. The `use std::cell::RefCell` import is absent from the file; all remaining imports are live. The struct doc comment describes only the `Mutex` and omits any `RefCell` mention. No dead imports, no lint suppressions, and the poison-fallback pattern in `record_undefined` matches the surrounding error-handling style. No other style findings.
</review>
