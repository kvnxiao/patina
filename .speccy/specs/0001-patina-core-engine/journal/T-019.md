---
spec: SPEC-0001
task: T-019
generated_at: 2026-05-28T03:00:00Z
---

<implementer date="2026-05-28T03:00:00Z" model="claude-opus-4-8[1m]/low" round="1">
- Completed: Landed `patina debug journal <path>` end-to-end (REQ-020). New
  `patina_core::journal::render` module: `load_plan_file(&Utf8Path)` reads
  the file and decodes it through the existing `Plan::decode` (reusing the
  T-010 version envelope check), returning the decoded `Plan` plus the `<ts>`
  recovered from the `<ts>.plan` filename; `render_plan(&Plan, &str)` formats
  a header line (compact journal timestamp + its RFC 3339 rendering via the
  existing `timestamp_to_rfc3339` helper, plus the operation count) and one
  `mode / source / target` block per operation. Mode labels reuse the same
  lowercase hyphenated vocabulary as the apply `--json` envelope's
  `mode_label` (`symlink` / `template-render` / `copy`). A typed
  `PlanRenderError` distinguishes a `Read` failure (names the path) from a
  `Decode` failure (wraps `JournalError`, including `VersionMismatch`). Wired
  re-exports through `journal/mod.rs` (`load_plan_file`, `render_plan`,
  `PlanRenderError`) and `lib.rs`, and re-exported `FILE_MAJOR_VERSION` at the
  crate root (was only `journal::FILE_MAJOR_VERSION`) for the integration
  test. CLI: added a hidden-but-documented `Debug(DebugCommand)` clap
  namespace with a `Journal(DebugJournalArgs { path: Utf8PathBuf })` member
  (`#[command(hide = true, subcommand)]`), `cmd/debug.rs` owning the dispatch
  and exit-code mapping (decode failure → reporter `warn` on stderr + exit 1,
  success → reporter `diff` to stdout + exit 0), wired into `main.rs`. `run`
  returns `i32` directly (it is infallible — failures are exit codes, not
  `anyhow` bubbles), consistent with the rest of the CLI's exit-code contract.

- Undone: Hooks and the resolved variable context are not rendered, because
  the `Plan` body (`PlannedOperation`) does not serialize them — only the
  resolved file operations (symlink / render / copy with source + target) are
  journaled. The task description mentions rendering hook and variable-context
  blocks, but the on-disk plan has no such fields to decode; rendering them
  would require fabricating data the file does not contain. The module doc
  states this explicitly. T-020 (formal CLI exit-code enum) is a separate
  task; `debug journal` maps its two terminal states (0 / 1) inline here, and
  T-020 may later route them through its centralised `EngineError -> ExitCode`
  mapping.

- Hygiene checks: `cargo test --workspace` exited 0 (all suites pass,
  including the new `patina-cli/tests/debug_journal_cli.rs` (4 cases), the new
  `cmd::debug` CLI unit tests (3 cases), and the new `journal::render` lib
  unit tests (5 cases)). `cargo clippy --workspace --all-targets --all-features
  -- -D warnings` exited 0 (resolved `format_push_string` via `writeln!` + an
  `ignore_fmt` helper mirroring the reporter's `ignore_io`, `let_underscore_must_use`,
  `unnecessary_wraps` by returning `i32` from `run`, `clippy::panic` in unit
  tests by switching to `assert!(matches!(...))`, and an unfulfilled
  `clippy::panic` lint-expectation on the integration crate during self-review
  before the gate). `cargo +nightly fmt --all --check` exited 0. `cargo deny
  check` exited 0 (no new dependencies — the module uses `fs-err`, `camino`,
  `thiserror`, and `std` only).

- Evidence: red-then-green paper trail at
  `.speccy/specs/0001-patina-core-engine/evidence/T-019.md`. Roll call for the
  CHKs under REQ-020:
  - CHK-034 (apply, then `patina debug journal <ts>.plan` prints a mode word
    matching the fixture plus an absolute target, exit 0): demonstrated →
    evidence Scenario 1. A committed apply deletes its plan file at commit, so
    the test writes a `<ts>.plan` via the public `Plan::encode` (the same bytes
    the engine fsyncs before mutating) and points the built binary at it,
    exercising the full read → envelope-check → decode → render path the
    scenario asserts. Mirrored by the `render_names_each_mode_source_and_target`
    lib unit.
  - CHK-050 (`patina debug --help` names `journal`, exit 0): demonstrated →
    evidence Scenario 4 captures the built-binary `debug --help` output naming
    `journal` and exiting 0; mirrored by `debug_help_names_journal_subcommand`.
  Additional REQ-020 done-when coverage (not separate CHK ids): missing path
  → exit 1 naming the path (Scenario 2); newer version envelope → exit 1
  naming both versions plus `version` (Scenario 3). No CHK in T-019's scope is
  `judgment-only`.

- Discovered issues: None.

- Procedural compliance: This implementer entry lands directly in
  `journal/T-019.md` per the journal-file schema; no `<implementer-note>` was
  written into TASKS.md. The TASKS.md `state="..."` attribute for T-019 flips
  from `in-progress` to `in-review` as the final step of this turn. No shipped
  skill bodies under `.claude/skills/` were edited during this task.
</implementer>

<review persona="business" verdict="blocking" model="claude-opus-4-8[1m]/medium" date="2026-05-28T04:00:00Z" round="1">
T-019 silently narrows REQ-020's locked `<done-when>` instead of surfacing the conflict for a human, and ships against three unmet `<done-when>` bullets.

REQ-020 `<done-when>` (SPEC.md:1364-1378) commits `debug journal` to print, on a valid plan, "the recorded operations, **hooks, variable context**, and timestamps" and to identify "each op's mode, source, target, and **pre-state hash where applicable**." The T-019 task body (TASKS.md:1481-1485) repeats this. The shipped `render_plan` (`patina-core/src/journal/render.rs`, `describe`) renders only mode / source / target plus the timestamp header. Hooks, variable context, and pre-state hash are not rendered. Three of the five `<done-when>` bullets are unmet.

The implementer's factual claim is correct and verified: the serialized `Plan` / `PlannedOperation` model (`patina-core/src/journal/plan.rs:39-107`, committed under T-011, not part of this diff) carries only `Vec<PlannedOperation>` where each op is `Symlink`/`Render`/`Copy { source, target }`. No hook field, no variable-context field, no hash field. Rendering the missing fields would mean fabricating data the file does not contain.

The blocker is not "implement the missing fields." It is that this is an upstream SPEC inconsistency the implementer resolved unilaterally rather than escalating. REQ-011 prose (SPEC.md:851-852) asserts the plan is the "full plan (list of file operations **and hook invocations**)", yet REQ-011's own `<done-when>` and the shipped model omit hooks. REQ-020's `<done-when>` then promises to render fields the model cannot supply. Per the project's own guidance to surface ambiguity rather than pick silently, the correct move was to flag this for `/speccy-amend` (either amend REQ-020/REQ-011 `<done-when>` to match the file-operation-only model, or expand the plan model) so a human records the decision — not to absorb it into an implementer "Undone" note and a module doc.

Mitigants for the record: the authoritative testable acceptance criteria — `<scenario>` CHK-034 and CHK-050, plus the `<task-scenarios>` missing-path and version-mismatch cases — are all satisfied. No non-goal scope was smuggled in. The code is shippable in isolation; the block is purely that REQ-020's `<done-when>` cannot be marked satisfied as written, and the divergence needs a human-owned SPEC reconciliation before this task is `completed`.

Resolution path: either (a) `/speccy-amend` REQ-020 (and the REQ-011 "hook invocations" prose) to scope the debug render to the file operations the journal actually persists, with a Changelog row recording why; or (b) expand the plan model to carry hooks / variable context / pre-state hash and render them here. Decision belongs to a human, not the implementer's "Undone" section.
</review>

<review persona="tests" verdict="pass" model="claude-opus-4-8[1m]/low" date="2026-05-28T04:00:00Z" round="1">
T-019 tests genuinely exercise REQ-020. CHK-034 (`decodes_a_plan_and_prints_its_modes_and_targets`, `patina-cli/tests/debug_journal_cli.rs:42`) and CHK-050 (`debug_help_names_journal_subcommand`, same file `:113`) drive the real built binary (`CARGO_BIN_EXE_patina`) end-to-end through `main.rs → cmd::debug::run → load_plan_file/render_plan`, not a mock; both assert specific properties (mode word + absolute target + exit 0; `journal` named + exit 0) that fail under an obviously-wrong implementation. Negative paths are specific, not catch-any: missing-path asserts exit 1 + the path on stderr + empty stdout; version-mismatch asserts exit 1 + `65535` + the supported major + the word `version`. Lib units in `render.rs` mirror the same behaviour with `matches!` on the typed error variants. Evidence contract satisfied: `Evidence:` field present, file exists at the named path, roll call covers both CHKs as `demonstrated` with test names that all exist in the diff, red phase consistent with the new-file diff, per-test scoped command — no fabrication patterns matched.
Non-blocking note (out of tests-persona scope, flagging for reviewer-business / vet): the SPEC `<done-when>` and the T-019 task body call for rendering hooks, variable context, and pre-state hash, but `PlannedOperation` (`patina-core/src/journal/plan.rs:41`) carries only `source`/`target` and `Plan` holds only `operations`, so that data is not serialized into the on-disk plan and cannot be rendered or tested. The "Undone" claim in the journal is accurate; this is a SPEC-vs-format drift decision, not a missing test.
</review>

<review persona="security" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-05-28T04:00:00Z" round="1">
No security blockers. `patina debug journal <path>` is a read-only post-mortem command: it reads a user-supplied path, decodes it as a `postcard` `Plan` behind a length-checked version envelope (`patina-core/src/journal/render.rs:77-95`, `patina-core/src/journal/plan.rs:174-187`), and renders only string fields to stdout. The decode of attacker-influenceable input is bounded — truncation and version-mismatch are handled before `postcard::from_bytes`, which executes no code and allocates per encoded length. Target/source strings from the plan are formatted as text only and never dereferenced on the filesystem (`render_plan`/`describe` at `render.rs:103-140`), so a crafted plan with traversal sequences cannot trigger any read or write. No auth/authz boundary, no network, no new dependencies, no crypto, no secret material (the variable context and hook bodies are deliberately not serialized into the plan, so no sensitive values surface in debug output). Error messages naming the path and both versions are appropriate for a local owner-invoked tool with no remote caller to disclose existence to. No panic paths added (`ignore_fmt` discards an infallible `fmt::Result`; no `unwrap`/`expect` in production).
</review>

<review persona="style" verdict="blocking" model="claude-sonnet-4-6[1m]" date="2026-05-28T04:00:00Z" round="1">
`patina-cli/src/cmd/debug.rs:60-72` introduces `describe_failure`, a function that re-implements `PlanRenderError`'s own `Display` rather than calling `err.to_string()`.

The sibling `rollback.rs:81` does exactly this — `reporter.warn(&err.to_string())` — for its typed error. That is the established convention in this codebase.

`describe_failure` also drifts from the type's own phrasing: the `#[error]` annotations say `"could not read"` / `"could not decode"`, while `describe_failure` says `"cannot read"` / `"cannot decode"` — two separate phrasings of the same concept.

The reason `describe_failure` exists at all is that `PlanRenderError::Decode`'s `#[error]` annotation (`"could not decode plan file \`{path}\`"`) omits `{source}`, so `err.to_string()` would drop the version-mismatch detail. The fix is to add `": {source}"` to the `Decode` variant's `#[error]` string in `patina-core/src/journal/render.rs` and then use `reporter.warn(&err.to_string())` in `debug.rs`, matching how `rollback.rs` handles typed errors. `describe_failure` can then be deleted entirely.

Specifically:

1. In `patina-core/src/journal/render.rs`, change `#[error("could not decode plan file \`{path}\`")]` to `#[error("could not decode plan file \`{path}\`: {source}")]`.
2. In `patina-cli/src/cmd/debug.rs`, replace the `describe_failure` call and the function body with `reporter.warn(&err.to_string())`, matching `rollback.rs:81`.

This removes the parallel implementation, aligns wording with the type's own Display, and lets the error type be the single source of truth for its human-readable form.
</review>

<blockers date="2026-05-28T04:00:00Z" round="1">
Two blocking verdicts (business, style); tests and security passed. Before the next implementer pass: resolve the REQ-020 SPEC-vs-format drift (human decision required) and remove the parallel error-Display implementation.
- business (blocking): REQ-020 `<done-when>` (SPEC.md:1367-1372) requires `debug journal` to render hooks, variable context, and pre-state hash, but the on-disk `Plan`/`PlannedOperation` model (plan.rs:39-107) serializes only file operations (source/target) — three `<done-when>` bullets cannot be satisfied as written. This is an upstream SPEC inconsistency (REQ-011 prose at SPEC.md:851-852 also claims "hook invocations" the model omits). It needs a human-owned reconciliation, not an implementer "Undone" note: either (a) `/speccy-amend` REQ-020 + REQ-011 to scope the render to the file operations the journal actually persists (with a Changelog row), or (b) expand the plan data model (T-011 territory) to carry hooks/variable-context/pre-state-hash and render them.
- style (blocking): `patina-cli/src/cmd/debug.rs` `describe_failure` re-implements `PlanRenderError`'s `Display` (and drifts its wording: "cannot" vs the type's "could not"). Fix: add `: {source}` to the `Decode` variant's `#[error]` string in `render.rs`, then replace `describe_failure` with `reporter.warn(&err.to_string())`, matching `rollback.rs:81`; delete `describe_failure`.
</blockers>

<blockers date="2026-05-28T05:00:00Z" round="1">
SPEC amended (Changelog 2026-05-28): REQ-020 `<done-when>` narrowed to the file-operations-only plan format and REQ-011 prose dropped "and hook invocations". The round-1 **business** blocker (REQ-020 over-promising hooks / variable context / pre-state hash that the `Plan` model never serializes) is **resolved by this amendment** — do NOT attempt to render those fields and do NOT expand the plan data model. T-019 as implemented already satisfies the amended `<done-when>` (prints recorded operations + timestamps; identifies each op's mode / source / target; CHK-034 + CHK-050 unchanged).
The only remaining work for the next implementer pass is the round-1 **style** blocker: in `patina-core/src/journal/render.rs` add `: {source}` to the `PlanRenderError::Decode` variant's `#[error]` string, then in `patina-cli/src/cmd/debug.rs` delete `describe_failure` and replace its use with `reporter.warn(&err.to_string())`, matching `rollback.rs:81`. Re-run the hygiene suite (test / clippy -D warnings / nightly fmt --check / deny check).
</blockers>

<implementer date="2026-05-28T06:00:00Z" model="claude-opus-4-8[1m]/low" round="2">
- Completed: Addressed the round-1 **style** blocker by amending the round-1 WIP in place. (1) `patina-core/src/journal/render.rs`: added `: {source}` to the `PlanRenderError::Decode` variant's `#[error]` string, so its `Display` now carries the wrapped `JournalError` (most notably the version-mismatch detail naming both majors). (2) `patina-cli/src/cmd/debug.rs`: deleted the `describe_failure` free function entirely and replaced its call site in `run_journal` with `reporter.warn(&err.to_string())`, matching `rollback.rs:81`'s established convention for surfacing typed errors. Removed the now-unused `use patina_core::PlanRenderError;` import. The error type is now the single source of truth for its human-readable form, eliminating the parallel Display implementation and its "cannot" vs "could not" wording drift.

- Undone: Nothing for the amended scope. The round-1 **business** blocker was resolved upstream by the 2026-05-28 SPEC amendment (REQ-020 `<done-when>` narrowed to the file-operations-only plan format; REQ-011 dropped "and hook invocations"), per the latest `<blockers>` directive — no hook / variable-context / pre-state-hash rendering and no plan-model expansion were attempted.

- Hygiene checks: `cargo test --workspace` exited 0 (all suites pass, including `patina-cli/tests/debug_journal_cli.rs`, the `cmd::debug` units, and the `journal::render` lib units; no test asserted on the old `describe_failure` wording). `cargo clippy --workspace --all-targets --all-features -- -D warnings` exited 0 (the dropped `PlanRenderError` import would otherwise have tripped `unused_imports`). `cargo +nightly fmt --all --check` exited 0. `cargo deny check` exited 0 (no dependency changes; pre-existing unmatched-license-allowance warnings are non-fatal).

- Evidence: paper trail unchanged at `.speccy/specs/0001-patina-core-engine/evidence/T-019.md` — this round is a style-only refactor that does not alter observable behaviour (the failure line still names the path and, for a mismatch, both majors and the word `version`; the negative-path tests at `debug_journal_cli.rs` still assert exactly that and pass). Roll call for CHKs under REQ-020 carries over from round 1:
  - CHK-034 (apply, then `patina debug journal <ts>.plan` prints a mode word + absolute target, exit 0): demonstrated → evidence Scenario 1; mirrored by `render_names_each_mode_source_and_target`. Unaffected by this round.
  - CHK-050 (`patina debug --help` names `journal`, exit 0): demonstrated → evidence Scenario 4; mirrored by `debug_help_names_journal_subcommand`. Unaffected by this round.
  No CHK in T-019's scope is `judgment-only`.

- Discovered issues: None.

- Procedural compliance: Retry pass amended the prior round's uncommitted WIP in place (no `git restore`/`clean`/`checkout`, no rewrite-from-scratch, no state reset). This `<implementer round="2">` block is appended after the existing journal contents; earlier blocks are untouched. TASKS.md `state` flips `pending → in-progress → in-review` this turn. No shipped skill bodies under `.claude/skills/` were edited.
</implementer>

<review persona="business" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-05-28T07:00:00Z" round="2">
T-019 satisfies the amended REQ-020 `<done-when>` end-to-end against the locked SPEC; the round-1 business blocker is genuinely resolved by the 2026-05-28 amendment.

Verified against the current (amended) SPEC: REQ-020 `<done-when>` is now file-operations-only and every bullet maps to passing behaviour (`debug --help` lists `journal`, exit 0 — CHK-050; valid plan prints recorded operations + timestamps in structured mode/source/target blocks — CHK-034; invalid path → typed error naming path, exit 1; newer-major envelope → typed error naming both versions, exit 1). REQ-011 prose now reads "list of file operations". Changelog row dated 2026-05-28 records the human-owned reconciliation. The implementer correctly did NOT fabricate hook/variable-context/pre-state-hash data and did NOT expand the plan model. `speccy verify` passes. No non-goal scope smuggled in.

Non-blocking drift flagged for reconciliation (now addressed by the orchestrator during commit): the TASKS.md T-019 prose body step 3 still instructed rendering "any pre-state hash", "one block per hook", and "the resolved variable context", which the amendment deleted from REQ-020. The authoritative surfaces (REQ-020 `<done-when>`/`<scenario>`/`<behavior>` and the TASKS.md `<task-scenarios>` block) are all correctly file-operations-only and satisfied — this was a stale-prose reconciliation gap, not a business contract violation.
</review>

<review persona="tests" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-05-28T07:00:00Z" round="2">
Round-2 tests still genuinely exercise the amended REQ-020. CHK-034 (`decodes_a_plan_and_prints_its_modes_and_targets`, `patina-cli/tests/debug_journal_cli.rs:42`) and CHK-050 (`debug_help_names_journal_subcommand`, same file `:113`) drive the real `CARGO_BIN_EXE_patina` binary end-to-end through `main.rs → cmd::debug::run_journal → load_plan_file/render_plan` against a real tempdir `<ts>.plan` written via `Plan::encode` — no mock of the SUT — and assert specific properties (mode word + absolute target + exit 0; `journal` named + exit 0). Negative paths are specific: missing-path asserts exit 1 + path on stderr + empty stdout; version-mismatch asserts exit 1 + `65535` + the supported major + the word `version`. The round-2 style fix is verified load-bearing: adding `: {source}` to `PlanRenderError::Decode`'s `#[error]` (`render.rs:55`) is what now carries the wrapped `JournalError::VersionMismatch` Display through `err.to_string()` in `debug.rs:53` — dropping `{source}` would regress the version-mismatch tests. `describe_failure` is fully deleted; no test asserted on its old wording. Evidence contract satisfied; no fabrication patterns matched.
</review>

<review persona="security" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-05-28T07:00:00Z" round="2">
No security blockers. The round-2 delta is a style-only error-formatting change (`PlanRenderError::Decode` now interpolates `{source}` and `cmd/debug.rs` uses `reporter.warn(&err.to_string())`); it surfaces only the version-mismatch/corruption detail from `JournalError` plus the user-supplied path — no secret material (variable context and hook bodies are not serialized into the plan). `patina debug journal <path>` remains a read-only, owner-invoked post-mortem: bounded decode behind a length-checked version envelope before `postcard::from_bytes` (`render.rs:77-87`), source/target strings rendered as text only and never dereferenced on the filesystem (`describe`/`render_plan`, `render.rs:103-140`), no auth/authz boundary, no network, no new dependencies, no crypto, and no production `unwrap`/`expect` or panic paths.
</review>

<review persona="style" verdict="pass" model="claude-sonnet-4-6[1m]" date="2026-05-28T07:00:00Z" round="2">
Round-1 style blocker fully resolved. On-disk state verified:
- `patina-core/src/journal/render.rs:55`: `PlanRenderError::Decode` now reads `#[error("could not decode plan file \`{path}\`: {source}")]`, making the wrapped `JournalError` (including version-mismatch detail) part of the type's own `Display`.
- `patina-cli/src/cmd/debug.rs:53`: `reporter.warn(&err.to_string())` — `describe_failure` is entirely absent from the file. Matches `rollback.rs:81` exactly.
- No dead imports: the formerly-present `use patina_core::PlanRenderError` import is gone; only `load_plan_file` and `render_plan` are imported, both used.
No other style concerns in the changed files.
</review>
