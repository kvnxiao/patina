---
spec: SPEC-0001
task: T-012
generated_at: 2026-05-28T20:00:00Z
---

<implementer date="2026-05-28T20:00:00Z" model="claude-opus-4.8[1m]/high" round="1">
- Completed: Landed `patina_core::backups` covering both REQ-014
  (backup-on-overwrite) and REQ-015 (count-based retention). New files:
  `patina-core/src/backups/mod.rs` (module doc, `BackupError` typed error,
  the `RETENTION_COUNT = 10` constant), `patina-core/src/backups/mirror.rs`
  (`backup_before_overwrite`), and `patina-core/src/backups/retention.rs`
  (`gc_retain`). Wired the module into `patina-core/src/lib.rs` (`pub mod
  backups;` plus root re-exports of `BackupError`, `RETENTION_COUNT`,
  `backup_before_overwrite`, `gc_retain`) and added a `Backup` variant to
  `EngineError` in `patina-core/src/error.rs` so the subsystem surfaces
  through the engine's typed error.
  `backup_before_overwrite(backups_dir, ts, target)` probes the target with
  `symlink_metadata`; if absent it returns `Ok(false)` and writes nothing
  (REQ-014: a fresh target produces no backup entry), and if present it
  copies the original bytes to `<backups>/<ts>/<mirrored-target>` and
  returns `Ok(true)`. Crucially it reuses `journal::mirror_backup_path` —
  the layout T-011's recovery already reads — rather than reinventing the
  mapping, so the backup writer and the recovery reader agree by
  construction (the round-3 forward-declaration note in T-011's journal is
  now satisfied by a consumer). `gc_retain(backups_dir, keep)` reads the
  backup root, keeps only directory entries (stray files are ignored),
  lex-sorts the names (== chronological for the timestamp format), removes
  everything before the trailing `keep`, and returns the removed names. A
  missing backup dir or a count at/under `keep` removes nothing.
- Undone: Nothing within REQ-014/REQ-015's scope for the layer T-012 owns.
  Deferred by the task decomposition: the executor that calls
  `backup_before_overwrite` immediately before each overwrite, and the
  apply path that calls `gc_retain` after `COMMIT` is durable, land with the
  executor and apply subcommand (T-014 / T-016) — so the literal
  `patina apply --yes` phrasing in CHK-025/CHK-026 runs end-to-end only once
  those exist. The TASKS.md "no `gc` subcommand in `patina --help`"
  scenario is judgment-only here: `backups` exposes retention solely as a
  library function with no CLI surface, and clap subcommands do not land
  until T-016; nothing in this diff could add a `gc` command.
- Hygiene checks:
  - `cargo test --workspace`: exit 0.
  - `cargo clippy --workspace --all-targets --all-features -- -D warnings`:
    exit 0.
  - `cargo +nightly fmt --all --check`: exit 0.
  - `cargo deny check`: exit 0 (advisories ok, bans ok, licenses ok,
    sources ok).
- Evidence: paper trail at
  `.speccy/specs/0001-patina-core-engine/evidence/T-012.md`.
  Roll call for CHKs under REQ-014 and REQ-015:
  - CHK-025 (pre-existing `~/.zshrc` backed up with its original bytes
    before overwrite, final target a symlink): demonstrated → evidence
    Scenario 1 (`overwriting_a_pre_existing_target_stashes_the_original_bytes`
    in `patina-core/tests/backups.rs`) seeds `~/.zshrc` = "original", calls
    `backup_before_overwrite`, and asserts the backup at
    `mirror_backup_path(...)` is a regular file holding "original" nested
    under `<backups>/<TS>/`. The "final target is a symlink" clause is the
    executor's responsibility (T-014); T-012 owns and proves the
    backup-before-overwrite half, with `made == true` signalling the caller
    may now replace the target.
  - CHK-026 (15 cycles prune to exactly 10 after a successful apply,
    keeping the newest): demonstrated → evidence Scenario 4
    (`a_successful_apply_prunes_to_exactly_ten_keeping_the_newest` in
    `patina-core/tests/backup_retention.rs`) seeds 15 timestamped cycles,
    calls `gc_retain(backups, RETENTION_COUNT)`, and asserts the five
    oldest are removed and the ten most recent survive. The "after a
    successful apply" trigger (GC fires only post-`COMMIT`) is enforced by
    the caller in T-016; Scenario 5 demonstrates the negative half (no
    `gc_retain` call ⇒ historical cycles untouched).
  - REQ-014 "fresh target produces no backup entry": demonstrated →
    Scenario 2; REQ-014 "repo never written": demonstrated → Scenario 3
    (byte-for-byte repo snapshot equality).
  - REQ-015 "no `patina gc` subcommand": judgment-only → no scriptable
    proof at this layer; `backups` has no CLI surface and the clap surface
    lands in T-016, where the reviewer/business persona judges the
    `--help` output. Recorded in the evidence file's "CLI surface" section.
- Discovered issues: none affecting other tasks. Note for the reviewer:
  T-012 is the first writer of the `<backups>/<ts>/<mirrored-target>`
  layout that T-011 defined as a reader; both halves now route through the
  single `journal::mirror_backup_path`, so the two agree by construction.
- Procedural compliance: no `unwrap`/`expect`/`panic`/`todo` in production
  paths (test helpers use `.expect` under the `allow-expect-in-tests`
  carve-out plus the `#![expect(clippy::expect_used)]` attribute in the two
  integration test crates); all filesystem access via `fs-err`; `camino`
  UTF-8 path types throughout; typed `BackupError` returns only, threaded
  into `EngineError` via `#[from]`. No new dependencies. `SPEC.md` not
  edited. `TASKS.md` touched only to flip T-012
  `pending → in-progress → in-review`. Journal is append-only.
</implementer>

<review persona="business" verdict="pass" model="claude-opus-4.8[1m]/high" date="2026-05-28T18:41:25Z" round="1">
T-012 faithfully delivers the REQ-014/REQ-015 contract for the `patina_core::backups` layer it owns. Backup-on-overwrite (`backup_before_overwrite`) copies the original bytes to the mirrored `<backups>/<ts>/<target>` path before an overwrite and returns `false` (no write) for fresh targets; retention (`gc_retain`) keeps the newest ten cycles by lex/chronological sort. Every REQ-014 and REQ-015 `<done-when>` and `<behavior>` bullet maps to a demonstrated test, including the repo-never-written guarantee (byte-for-byte snapshot in `patina-core/tests/backups.rs`) and the failed-apply-no-GC negative case (`patina-core/tests/backup_retention.rs`). The CHK-026 arithmetic ("exactly 10 = just-completed + 9 prior") is handled correctly: `gc_retain` retains the newest 10 of whatever is present, and the post-COMMIT trigger sequencing is correctly deferred to the caller in T-016. The literal `patina apply --yes` end-to-end surface in CHK-025/CHK-026 cannot run at this layer; deferring the executor/apply wiring to T-014/T-016 is the task decomposition's explicit boundary, transparently disclosed. No scope creep: no `patina gc` subcommand or CLI surface added, consistent with the v1.0 non-goal and REQ-015. The writer reuses `journal::mirror_backup_path` so backup and recovery agree by construction. No business blockers.
</review>

<review persona="tests" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-05-28T18:41:25Z" round="1">
T-012 backup tests genuinely exercise real code paths and would catch broken implementations. CHK-025 (`overwriting_a_pre_existing_target_stashes_the_original_bytes`) asserts `made == true`, the backup is a regular file holding the exact pre-overwrite bytes `b"original"`, nests under `<backups>/<TS>/`, and keeps file name `.zshrc` — a writer that skipped the copy, wrote post-overwrite bytes, or reported `false` each fails a distinct assertion; the symlink clause is honestly deferred to the T-014 executor. CHK-026 (`a_successful_apply_prunes_to_exactly_ten_keeping_the_newest`) seeds 15 cycles and asserts both `removed == oldest_five` and `survivors == newest_ten` — a keep-oldest, wrong-count, or off-by-one mutation all fail. Negative paths carry specific assertions. Verified non-circular: the test asserts against `journal::mirror_backup_path`, the same function the writer uses, so writer/reader layout agreement is proven by construction. Re-ran all suites green. Non-blocking notes for downstream: (1) the evidence file is prose-only with no pasted runner transcripts — confirmed authenticity by re-running; (2) `the_retention_count_constant_is_ten` brushes the test-hygiene rule against copying production constants, but behavioural retention is independently covered by the 15→10 test.
</review>

<review persona="security" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-05-28T18:41:25Z" round="1">
No security blockers in T-012 (REQ-014/REQ-015 backup-on-overwrite + retention GC). Path-traversal containment is sound: `backup_before_overwrite` reuses `journal::mirror_backup_path` (`probe.rs:89`), which folds away `RootDir`/`CurDir`/`ParentDir` and the platform prefix, so no crafted target path can escape `<backups>/<ts>/`. `gc_retain` (`retention.rs:39`) only removes directory names enumerated from `read_dir(backups_dir)` and joined under the backup root; stray files are skipped — no arbitrary-deletion vector. No secret handling, no `tracing`/log output, no path leakage in `BackupError`'s `Display`. Backups are the invoking user's own files under their own per-machine state dir — no privilege or trust boundary crossed. No new dependencies; no CLI surface. Non-blocking note: `fs_err::copy` follows a symlinked target (copies pointed-to bytes) while existence is probed with `symlink_metadata` — consistent with the recovery reader's byte-restore design and within the user's own filesystem domain, a fidelity nuance not a defect.
</review>

<review persona="style" verdict="blocking" model="claude-sonnet-4-6[1m]" date="2026-05-28T18:41:25Z" round="1">
`patina-core/tests/backup_retention.rs:125-131` — The test `the_retention_count_constant_is_ten` does `assert_eq!(RETENTION_COUNT, 10)`, which is exactly the vacuous shape #2 prohibited by the test hygiene rules in `AGENTS.md` ("Copying production constants into the test … proves only that someone updated both sites in sync — it cannot fail in any interesting way").

The inline comment claims this guards "a property of the policy, not a copy of an internal literal — the SPEC names the number ten directly", but the test still asserts the constant equals its own value. If someone changes `RETENTION_COUNT` to 7 and updates the assertion to `7`, the test continues to pass while violating the SPEC's requirement. The assertion cannot catch that failure.

Remove this test or replace it with a structural assertion that would fail on a policy-violating value without mirroring the constant — e.g. asserting `RETENTION_COUNT >= 10` (enforces a minimum floor the SPEC requires), or delete it entirely and rely on the GC behavioral tests (which seed 15 cycles and assert exactly 10 survive using `RETENTION_COUNT`) to enforce the invariant transitively.
</review>

<blockers date="2026-05-28T18:41:25Z" round="1">
Remove the vacuous constant-copy test in the retention suite before the next review pass.
- style: `patina-core/tests/backup_retention.rs:125-131` — `the_retention_count_constant_is_ten` asserts `assert_eq!(RETENTION_COUNT, 10)`, the prohibited "copying production constants into the test" shape (AGENTS.md test hygiene rule #2). Delete the test entirely and rely on the behavioural GC tests (which seed 15 cycles and assert exactly 10 survive via `RETENTION_COUNT`) to enforce the invariant transitively. Verify clippy/fmt/test stay green.
</blockers>

<implementer date="2026-05-28T20:30:00Z" model="claude-opus-4.8[1m]/medium" round="2">
- Completed: Addressed the round-1 style blocker by deleting the vacuous
  `the_retention_count_constant_is_ten` test (formerly
  `patina-core/tests/backup_retention.rs:124-131`), which asserted
  `assert_eq!(RETENTION_COUNT, 10)` — the prohibited constant-copy shape
  (AGENTS.md test hygiene rule #2). No replacement assertion was added: the
  ten-cycle retention invariant is enforced transitively by the behavioural
  GC test `a_successful_apply_prunes_to_exactly_ten_keeping_the_newest`,
  which seeds 15 cycles, calls `gc_retain(&scene.backups, RETENTION_COUNT)`,
  and asserts the five oldest are removed and the newest ten survive. The
  `use patina_core::backups::RETENTION_COUNT;` import is retained because
  that behavioural test still consumes the constant (line 85). This was the
  only change this round; the prior pass's WIP (the `backups` module,
  `mirror.rs`/`retention.rs`, lib re-exports, `EngineError::Backup`, and the
  other tests) was amended in place, not rewritten.
- Undone: Nothing newly deferred. The executor/apply wiring boundaries
  (T-014/T-016) noted in round 1 remain unchanged and out of T-012's scope.
- Hygiene checks:
  - `cargo test --workspace`: exit 0 (all suites pass; the deleted test no
    longer appears, the 15→10 behavioural test still passes).
  - `cargo clippy --workspace --all-targets --all-features -- -D warnings`:
    exit 0 (no unused-import warning — `RETENTION_COUNT` still used).
  - `cargo +nightly fmt --all --check`: exit 0.
  - `cargo deny check`: exit 0 (advisories ok, bans ok, licenses ok,
    sources ok; the two unmatched-license-allowance warnings are pre-existing
    and unrelated to this change).
- Evidence: paper trail at
  `.speccy/specs/0001-patina-core-engine/evidence/T-012.md` (round-1
  evidence unchanged; the deleted test was never load-bearing evidence).
  Roll call for CHKs under REQ-014 and REQ-015 (unchanged from round 1):
  - CHK-025 (pre-existing target backed up with original bytes before
    overwrite): demonstrated → `overwriting_a_pre_existing_target_stashes_the_original_bytes`
    in `patina-core/tests/backups.rs`.
  - CHK-026 (15 cycles prune to exactly 10, newest kept): demonstrated →
    `a_successful_apply_prunes_to_exactly_ten_keeping_the_newest` in
    `patina-core/tests/backup_retention.rs` — now the sole enforcer of the
    ten-cycle invariant after the constant-copy test's removal.
  - REQ-014 "fresh target produces no backup entry": demonstrated →
    Scenario 2; "repo never written": demonstrated → Scenario 3.
  - REQ-015 "no `patina gc` subcommand": judgment-only → no CLI surface at
    this layer; clap surface lands in T-016.
- Discovered issues: none.
- Procedural compliance: only the one blocker-targeted test was removed; no
  production code touched this round. No `unwrap`/`expect`/`panic`/`todo` in
  production paths. `SPEC.md` not edited. `TASKS.md` touched only to flip
  T-012 `pending → in-progress → in-review`. Journal is append-only (this
  block appended after the round-1 blockers). No `git restore`/`clean`/
  `checkout` run against the WIP.
</implementer>

<review persona="business" verdict="pass" model="claude-opus-4.8[1m]/medium" date="2026-05-28T18:45:38Z" round="2">
T-012 round 2 still faithfully delivers the REQ-014/REQ-015 contract for the `patina_core::backups` layer it owns; the only round-2 change was deleting the vacuous `the_retention_count_constant_is_ten` test, which removed nothing load-bearing. Every REQ-014 and REQ-015 `<done-when>` bullet still maps to a substantive test: backup-on-overwrite copies original bytes to the mirrored `<backups>/<ts>/<target>` path (`backups.rs:82`), fresh targets produce no entry (`:111`), the repo is never written (byte-for-byte snapshot, `:130`), retention keeps exactly the newest ten of fifteen (`backup_retention.rs:78`), and a failed apply triggers no GC (`:100`). The ten-cycle invariant is now enforced solely by the 15→10 behavioural test, the stronger enforcer; `RETENTION_COUNT` remains a live consumed import. No scope creep, no `patina gc` subcommand; the `patina apply --yes` end-to-end clause is transparently deferred to T-014/T-016 per the task decomposition. No business blockers.
</review>

<review persona="tests" verdict="pass" model="claude-opus-4.8[1m]/medium" date="2026-05-28T18:45:38Z" round="2">
T-012 round 2: the round-1 style blocker is fully resolved and test coverage is intact. The vacuous `the_retention_count_constant_is_ten` constant-copy test is confirmed deleted (zero grep matches), the `RETENTION_COUNT` import remains consumed by the behavioural test (`backup_retention.rs:85`), and the 15→10 GC test now solely enforces the retention invariant via a real behavioural assertion — `removed==oldest_five` AND `survivors==newest_ten` with `len()==10`, which catches keep-oldest, wrong-count, and off-by-one mutations. CHK-025 drives the real `backup_before_overwrite` (no mocks), asserting `made==true`, exact `b"original"` bytes at `mirror_backup_path`, `<backups>/<TS>/` nesting, and `.zshrc` filename; the symlink-final clause is honestly deferred to the T-014 executor. Negative paths carry specific assertions. Writer/reader layout agreement is proven non-circularly against `journal::mirror_backup_path`. Re-ran all suites (backups 3/3, backup_retention 2/2, lib 8/8) green. No blockers.
</review>

<review persona="security" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-05-28T18:45:38Z" round="2">
No security blockers in T-012 round 2. The round-2 change deleted a single vacuous test only — production code is unchanged, so the round-1 security pass continues to hold. Re-verified: backup path-traversal containment is sound (`mirror.rs:61` routes through `journal::mirror_backup_path` / `probe.rs:105-128`, which folds the platform prefix and drops `RootDir`/`CurDir`/`ParentDir`, so no `..`-laden target escapes `<backups>/<ts>/`); `gc_retain` (`retention.rs:39-73`) only removes directories it enumerated under the backup root, skipping strays — no arbitrary-deletion vector. No new dependencies, no CLI surface, no secret handling, no path leakage in `BackupError::Display`; backups stay within the invoking user's own filesystem domain.
</review>

<review persona="style" verdict="pass" model="claude-sonnet-4-6[1m]" date="2026-05-28T18:45:38Z" round="2">
Round-1 blocker resolved: `the_retention_count_constant_is_ten` is gone from `patina-core/tests/backup_retention.rs`; the file now contains only the two behavioural tests. The `use patina_core::backups::RETENTION_COUNT;` import at line 24 is still live — consumed by `a_successful_apply_prunes_to_exactly_ten_keeping_the_newest` at line 85 — so no dead import was introduced. All other style criteria pass: `#[expect(clippy::expect_used)]` suppressions carry `reason` attributes; production code uses `?` propagation throughout, `fs-err` for filesystem ops, `camino` types for paths, and typed `BackupError` returns; `lib.rs` and `error.rs` additions follow the project's existing alphabetical-module and `#[from]`-variant patterns.
</review>
