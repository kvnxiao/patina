---
spec: SPEC-0001
task: T-013
generated_at: 2026-05-28T00:00:00Z
---

<implementer date="2026-05-28T00:00:00Z" model="claude-opus-4.8[1m]" round="1">
- Completed: Added `patina_core::lock` (`patina-core/src/lock.rs`)
  wrapping `fs2`'s advisory file lock over `<state>/patina/lock`.
  Public surface: `LockKind { Exclusive, Shared }`, the typed
  `LockError { Timeout { kind, path, waited }, Io { path, source } }`,
  a `#[must_use]` `LockGuard` whose `Drop` releases the OS lock, and
  `acquire(path, kind, timeout) -> Result<LockGuard, LockError>` plus
  the `EXCLUSIVE_TIMEOUT` (60s) / `SHARED_TIMEOUT` (5s) caps the SPEC
  names. `acquire` polls `fs2`'s non-blocking `try_lock_exclusive` /
  `try_lock_shared` every 25ms until success or the caller's
  timeout elapses (the bounded poll is what makes the cap
  enforceable and parameterisable down to milliseconds for fast
  tests); contention is distinguished from real I/O errors via
  `fs2::lock_contended_error`. Wired the module into
  `patina-core/src/lib.rs` (module + re-exports) and added
  `EngineError::Lock(#[from] LockError)` to
  `patina-core/src/error.rs`. Added `fs2 = "0.4"` to the workspace
  `[workspace.dependencies]` and to `patina-core/Cargo.toml`.
  Added the `lock_helper` example
  (`patina-core/examples/lock_helper.rs`) as the cross-process test
  harness and the `lock_concurrency` integration suite
  (`patina-core/tests/lock_concurrency.rs`): four tests covering
  exclusive serialization (CHK-037), exclusive timeout exiting with
  the reserved code 4 and a `TIMEOUT exclusive` stderr line, OS lock
  release after an abnormal `process::abort` while holding the lock,
  and shared/shared coexistence. Five `src/lock.rs` unit tests cover
  exclusive-blocks-shared, shared-coexist, shared-blocks-exclusive,
  the timeout-error fields, and lazy lock-file creation.

- Undone: The shared-lock warn-and-proceed behaviour (REQ-023's
  read-only escape hatch) and the exit-code-4 mapping are
  deliberately left to their owning tasks. T-013 surfaces the typed
  `LockError::Timeout`; the `status` call site decides to warn and
  proceed, and T-020 maps the exclusive timeout to process exit
  code 4. The integration test asserts the helper's own exit-4
  contract as a stand-in, since the `patina apply` / `patina status`
  subcommands and their exit-code mapping do not exist yet (T-014 /
  T-020). No part of T-013's own scope is deferred.

- Hygiene checks: `cargo test --workspace` exited 0 (all suites
  green, including the 5 new `lock::tests` unit tests and the 4 new
  `lock_concurrency` integration tests). `cargo clippy --workspace
  --all-targets --all-features -- -D warnings` exited 0 (the example
  obeys the full denied-lint set — no indexing, no `let _ =` on
  `#[must_use]`, no `panic!`). `cargo +nightly fmt --all --check`
  exited 0. `cargo deny check` exited 0 (`advisories ok, bans ok,
  licenses ok, sources ok`); the one new dependency `fs2 v0.4.3` is
  `MIT/Apache-2.0`, on the allow-list.

- Evidence: red-then-green paper trail at
  `.speccy/specs/0001-patina-core-engine/evidence/T-013.md`.
  Roll call for the one CHK under REQ-023:
  - CHK-037 (two concurrent exclusive applies do not interleave;
    union of journal `.plan` files numbers exactly two, timestamp
    windows non-overlapping): demonstrated → evidence Scenario 1.
    The red runs the same `lock_helper` harness in shared mode and
    shows the two windows interleaving (the disjointness predicate
    is false); the green flips to exclusive mode in
    `two_exclusive_applies_do_not_interleave` and the windows become
    disjoint with exactly two `.plan` markers. Same harness, same
    timing — only the lock mode changed.
  No CHK under REQ-023 is `hygiene`-only or `judgment-only`; the
  three supporting integration tests (timeout-exit-4, OS-release-
  on-abort, shared-coexistence) cover the remaining `<done-when>`
  bullets and are backed by the hygiene-gate run in the evidence
  file.

- Discovered issues: The SPEC scenario and `state_dir.rs` docs name
  the lock file `<state>/patina/lock`, but T-005 (`state_dir`)
  resolves the root already including the `patina` segment, so the
  lock path the engine will pass is `<resolved_state>/lock`. The
  lock module is path-agnostic (it locks whatever `Utf8Path` it is
  handed), so there is no defect here — flagged only so the task
  that wires `acquire` into the `apply` / `status` entry points
  passes `state.join("lock")` against the already-`patina`-suffixed
  root rather than re-appending `patina`.

- Procedural compliance: This implementer entry lands directly in
  `journal/T-013.md` per the journal-file schema; no
  `<implementer-note>` was written into TASKS.md (the parser
  rejects that element). The TASKS.md `state="..."` attribute for
  T-013 flips from `in-progress` to `in-review` as the final step
  of this turn. No shipped skill bodies under `.claude/skills/`
  were edited.
</implementer>

<review date="2026-05-28T19:02:57Z" model="claude-opus-4.8[1m]/medium" persona="business" verdict="pass" round="1">
T-013 delivers the `patina_core::lock` primitive REQ-023 requires, with no SPEC-vs-diff gap inside its scope. CHK-037 is faithfully covered by `two_exclusive_applies_do_not_interleave` (exactly two `.plan` markers + disjoint ACQUIRED/RELEASED windows); re-ran the suite and all 4 integration + 6 unit tests pass on Windows. The two `<done-when>` bullets not yet observable (status-blocks-momentarily, 5s warn-and-proceed) and the exit-code-4 mapping are partitioned to T-014/T-018/T-020 in the task body — declared scope hand-offs, not silent drops; the module surfaces typed `LockError::Timeout` and the `EXCLUSIVE_TIMEOUT`(60s)/`SHARED_TIMEOUT`(5s) caps the SPEC names. The path-segment ambiguity (`<state>/patina/lock` vs `<resolved_state>/lock`) was surfaced as a flag for the wiring task rather than silently resolved. No non-goal scope crept in.
</review>

<review date="2026-05-28T19:02:57Z" model="claude-opus-4.8[1m]/medium" persona="tests" verdict="pass" round="1">
T-013 lock concurrency tests genuinely exercise the real `patina_core::lock::acquire` across spawned child processes — no mocking of the system under test. CHK-037 (the sole scenario under REQ-023) is demonstrated by `lock_concurrency.rs:146 two_exclusive_applies_do_not_interleave`, asserting exactly two `<nanos>.plan` markers and the disjointness predicate `first.released <= second.acquired` on windows parsed from the real `lock_helper` stdout — a faithful translation of "union numbers exactly two, timestamps non-overlapping." Evidence red/green is a legitimate same-harness counterfactual. Supporting bullets have real teeth: `blocked_exclusive_exits_with_lock_timeout_code` asserts exit code 4 + `TIMEOUT`/`exclusive` stderr; `os_releases_lock_when_holder_aborts` proves OS release; unit tests assert specific `LockError::Timeout` field matches. Scenario 3 (status warn-and-proceed) correctly scoped to T-019/T-020.
</review>

<review date="2026-05-28T19:02:57Z" model="claude-opus-4-8[1m]/medium" persona="security" verdict="pass" round="1">
No security defects in the T-013 advisory-lock slice. Attack surface is a single coordination primitive with no auth/crypto/network/deserialization boundary. Lock file holds no secrets (`patina-core/src/lock.rs:163-164`); inherits umask consistently with sibling state-dir artifacts. `acquire` is path-agnostic with caller-supplied `&Utf8Path` — no traversal vector. Wait is a bounded non-blocking poll capped at `EXCLUSIVE_TIMEOUT`/`SHARED_TIMEOUT` with `POLL_INTERVAL.min(remaining)` (no busy-spin, no unbounded wait/DoS). The crash-release invariant is verified across real processes in `os_releases_lock_when_holder_aborts` (`patina-core/tests/lock_concurrency.rs:238`). New dep `fs2 v0.4.3` (MIT/Apache-2.0) passed `cargo deny check`. Error messages disclose only local paths in a single-user CLI.
</review>

<review date="2026-05-28T19:02:57Z" model="claude-sonnet-4-6[1m]/medium" persona="style" verdict="blocking" round="1">
`patina-core/examples/lock_helper.rs:164–169` defines a private free function `kind_label(kind: LockKind) -> &'static str` that is byte-for-byte identical to the already-existing `LockKind::label()` method in `patina-core/src/lock.rs:87–92`. This violates the project rule in `.claude/rules/rust/rust-let-types-carry-invariants.md` ("Reuse the existing helper; grep before you write one… if a sibling's private helper is what you need, promote it to `pub(super)` / `pub(crate)` and call it rather than duplicating").

Fix: in `patina-core/src/lock.rs`, change `const fn label(self) -> &'static str` to `pub(crate) const fn label(self) -> &'static str`. Then in `patina-core/examples/lock_helper.rs`, delete the `kind_label` free function (lines 164–169) and replace its call site (line 124) with `kind.label()`.
</review>

<blockers date="2026-05-28T19:02:57Z" round="1">
Eliminate the duplicated lock-kind label helper before the next implementer pass.
- style: `examples/lock_helper.rs:164–169` `kind_label` duplicates `LockKind::label()` (`src/lock.rs:87–92`). Promote `label` to `pub(crate)` and call `kind.label()` at `lock_helper.rs:124`, deleting the free function. (rust-let-types-carry-invariants.md)
</blockers>

<implementer date="2026-05-28T20:30:00Z" model="claude-opus-4.8[1m]" round="2">
- Completed: Addressed the round-1 style blocker by eliminating the
  duplicated lock-kind label helper. Removed the private `kind_label`
  free function from `patina-core/examples/lock_helper.rs` and routed
  its single call site through the canonical `LockKind::label`. The
  blocker's literal suggestion (`pub(crate)`) is insufficient here:
  `examples/lock_helper.rs` compiles as a *separate crate* from
  `patina-core`, so a `pub(crate)` method is not reachable from it.
  Promoted `LockKind::label` to a fully public method instead — this
  is coherent because `LockKind` is already `pub` and `label` returns
  exactly the word that already appears in the public
  `LockError::Timeout` Display output, so the accessor is part of the
  type's legitimate public surface, not a leak. Added the required
  rustdoc summary + `# Examples` doctest and a `#[must_use]` attribute
  to satisfy the public-API documentation rule and the strict lint
  set. The example now imports nothing new (it already used
  `LockKind`) and calls `kind.label()` at the former call site.
- Undone: Nothing new. The scope hand-offs declared in round 1
  (shared-lock warn-and-proceed to T-019, exit-code-4 mapping to
  T-020) remain owned by their tasks; this round changed no behaviour
  and added no scope.
- Hygiene checks: `cargo test --workspace` exited 0 (all suites green,
  including the new `lock::LockKind::label` doctest). `cargo clippy
  --workspace --all-targets --all-features -- -D warnings` exited 0
  (the now-public `label` satisfies `missing_docs` / doc-example lints
  and the `#[must_use]` convention). `cargo +nightly fmt --all
  --check` exited 0. `cargo deny check` exited 0 (`advisories ok, bans
  ok, licenses ok, sources ok`; no dependency change this round).
- Evidence: the round-1 red/green paper trail at
  `.speccy/specs/0001-patina-core-engine/evidence/T-013.md` is
  unchanged and still authoritative — this round is a pure
  deduplication refactor with no behavioural delta. Roll call for the
  one CHK under REQ-023:
  - CHK-037 (two concurrent exclusive applies do not interleave;
    union of journal `.plan` files numbers exactly two, timestamp
    windows non-overlapping): demonstrated → still proven by
    `two_exclusive_applies_do_not_interleave` in
    `patina-core/tests/lock_concurrency.rs`, which exercises the same
    `lock_helper` harness whose `TIMEOUT <kind>` stderr line now flows
    through `LockKind::label`; the suite re-ran green this round.
  No CHK under REQ-023 is `hygiene`-only or `judgment-only`.
- Discovered issues: None new. The round-1 path-segment flag
  (`<state>/patina/lock` vs `<resolved_state>/lock`) still stands for
  the wiring task; no change here.
- Procedural compliance: Amended the prior pass's uncommitted WIP in
  place — no `git restore`/`clean`/`checkout`, no rewrite from
  scratch, no reset of the touched files. State flipped
  `pending → in-progress → in-review` this turn. This `round="2"`
  block was appended below the round-1 blockers without modifying any
  earlier journal block. No shipped `.claude/skills/` bodies were
  edited.
</implementer>

<review date="2026-05-28T19:08:10Z" model="claude-opus-4.8[1m]/medium" persona="business" verdict="pass" round="2">
T-013 still satisfies REQ-023 with no SPEC-vs-diff gap. CHK-037 remains faithfully covered by `two_exclusive_applies_do_not_interleave` (`patina-core/tests/lock_concurrency.rs:146`): exactly two `.plan` markers asserted and the disjointness predicate `first.released <= second.acquired`, both exclusive processes spawned within the scenario's 100ms window. The round-2 change is a pure deduplication refactor — `kind_label` deleted from `examples/lock_helper.rs`, call site routed through the now-`pub` `LockKind::label` (`patina-core/src/lock.rs:98`) — leaving the `TIMEOUT <kind>` stderr contract and journal `.plan` marker path behaviourally untouched. Declared scope hand-offs (shared warn-and-proceed → T-019, exit-code-4 → T-020) and the `<state>/patina/lock` path-segment ambiguity remain surfaced for their owning tasks. No non-goal scope crept in.
</review>

<review date="2026-05-28T19:08:10Z" model="claude-opus-4-8[1m]/medium" persona="tests" verdict="pass" round="2">
T-013 tests genuinely drive the real `patina_core::lock::acquire` across spawned `lock_helper` child processes — no mocking of the system under test. CHK-037 (the sole rendered scenario under REQ-023) is faithfully covered by `lock_concurrency.rs:146 two_exclusive_applies_do_not_interleave`: asserts exactly two `<nanos>.plan` markers and the order-independent disjointness predicate `first.released <= second.acquired`. Mental-rewrite check passes: a non-excluding exclusive branch makes windows interleave (evidence red proves this), a non-timing-out `acquire` breaks the exit-4 test, a non-releasing Drop breaks `os_releases_lock_when_holder_aborts`. Scenario 3 (status warn-and-proceed after 5s) correctly partitioned to T-019/T-020. The round-2 dedup routes the asserted `TIMEOUT {label}` stderr line through the canonical accessor and the new `label()` doctest pins the mapping — strengthening rather than weakening the assertion.
</review>

<review date="2026-05-28T19:08:10Z" model="claude-opus-4-8[1m]/medium" persona="security" verdict="pass" round="2">
No security defects in the round-2 state of the T-013 advisory-lock slice. The retry delta is a pure deduplication refactor — `LockKind::label` promoted to `pub const fn` (`patina-core/src/lock.rs:98`), duplicated `kind_label` removed, call site routed through `kind.label()`. No behavioural delta, no new dependency. `label()` returns only static `"exclusive"`/`"shared"` literals — no secret, no user-controlled data, no injection vector; the same word is already in the public `LockError::Timeout` Display. `acquire` still a bounded non-blocking poll capped at caller timeout (`lock.rs:222-223`) — no busy-spin/DoS. Lock file holds no secrets; errors disclose only local paths in a single-user CLI. OS crash-release invariant preserved and covered by the cross-process abort test. `fs2 v0.4.3` (MIT/Apache-2.0) unchanged, previously cleared `cargo deny`.
</review>

<review date="2026-05-28T19:08:10Z" model="claude-sonnet-4-6[1m]/medium" persona="style" verdict="pass" round="2">
Round-1 blocker resolved. `patina-core/examples/lock_helper.rs` contains no `kind_label` free function; line 124 calls `kind.label()` directly. `patina-core/src/lock.rs:98` declares `pub const fn label(self)` with `#[must_use]`, a prose summary, and a `# Examples` doctest satisfying the public-API documentation rule. No dead imports, no naming drift, no new parallel helpers, no `unwrap()` in production code, `#[expect(..., reason = "...")]` in the integration test file is properly attributed. Nothing blocking.
</review>
