---
spec: SPEC-0001
task: T-020
generated_at: 2026-05-29T05:00:00Z
---

<implementer date="2026-05-29T05:00:00Z" model="claude-opus-4-8[1m]/medium" round="1">
- Completed: Formalised the CLI exit-code contract (REQ-022) behind a single
  source of truth. New `patina-cli/src/exit_code.rs` with a
  `#[repr(i32)]` `ExitCode` enum whose pinned discriminants *are* the
  contract (`Success = 0`, `Generic = 1`, `PreApplyAbort = 2`,
  `PostApplyRollback = 3`, `LockTimeout = 4`, `UserDeclined = 5`),
  `ExitCode::code() -> i32`, an `ExitCode::from_engine_error(&EngineError)`
  mapping (only `EngineError::Lock(LockError::Timeout { .. })` → `4`; every
  other engine failure → `1`), and `ExitCode::from_error_chain(&anyhow::Error)`
  that walks the `anyhow` chain with `downcast_ref::<EngineError>()` so a
  timeout wrapped under command-layer `.context(..)` still maps to 4. New
  `cli::resolve_exit_code(anyhow::Result<i32>, &mut impl Reporter) -> i32`:
  the single funnel every subcommand terminates through — `Ok(code)` passes
  verbatim (terminal states under the command's own control: success,
  hook-abort, declined prompt), `Err` renders the full context chain to the
  reporter's err stream and maps via `from_error_chain`. `main` now returns
  `!`, builds one `StreamReporter`, collects each command's
  `anyhow::Result<i32>` (no `?`), and calls
  `std::process::exit(resolve_exit_code(outcome, &mut reporter))`. The four
  `cmd` modules now return their codes through the named `ExitCode` variants
  instead of bare integer literals (`apply` 0/2/3/5, `rollback` 0/1/5,
  `status` 0, `debug` 0/1), so the contract reads from one vocabulary. To
  make the exit-4 path provokable from the test harness without waiting the
  production minute, added `patina_core::lock::exclusive_timeout()` (reads the
  `PATINA_LOCK_TIMEOUT_MS` test override, falling back to `EXCLUSIVE_TIMEOUT`)
  and routed both exclusive-lock acquisition sites (`apply/engine.rs`,
  `rollback/mod.rs`) through it; re-exported `exclusive_timeout` /
  `EXCLUSIVE_TIMEOUT_ENV` from `lib.rs`.

- Undone: Nothing in scope. Exit 5 (declined prompt) is not exercised by the
  new subprocess integration test because a piped subprocess stdin is never a
  TTY; that path is covered by the existing in-process unit tests on the
  injected `PromptReader` (`cmd::rollback::declined_prompt_exits_five`, and
  the apply declined path), which assert the `ExitCode::UserDeclined (5)`
  return directly. No `--repo` flag, no JSON-schema field, no other non-goal
  was touched.

- Hygiene checks: `cargo test --workspace` exited 0 (all suites pass,
  including the new `patina-cli/tests/exit_codes.rs` (5 cases: exit 2 / 3 / 1
  / 4 / 0) and the new `exit_code.rs` unit tests (6 cases)).
  `cargo clippy --workspace --all-targets --all-features -- -D warnings`
  exited 0 (fixed one `clippy::duration_suboptimal_units` in a test fixture:
  `Duration::from_secs(60)` → `from_mins(1)`). `cargo +nightly fmt --all
  --check` exited 0 (after `fmt` collapsed the `resolve_exit_code` signature
  onto one line). `cargo deny check` exited 0 (no new dependencies — the work
  uses `anyhow`, `patina_core`, and `std` only; pre-existing
  unmatched-license-allowance warnings are non-fatal).

- Evidence: red-then-green paper trail at
  `.speccy/specs/0001-patina-core-engine/evidence/T-020.md`. Roll call for the
  CHKs under REQ-022:
  - CHK-036 (`pre_apply` `command = "false"`, `must_succeed = true`,
    `patina apply --yes` exits 2): demonstrated → evidence Scenario 1;
    mirrored by `patina-cli/tests/exit_codes.rs::pre_apply_hook_failure_exits_2`.
  REQ-022 attaches exactly one `<scenario>` (CHK-036). The other `<done-when>`
  / `<behavior>` bullets are additional demonstrated coverage (not separate
  CHK ids): exit 3 (Scenario 2), exit 1 on TOML error (Scenario 3), exit 4 on
  lock timeout (Scenario 4 — the load-bearing T-020 fix: the chain-walk
  mapping is what turns the previously-flat `1` into `4`), exit 0 (Scenario
  5). No CHK in T-020's scope is `judgment-only`.

- Discovered issues: The lock-timeout case previously exited a flat `1` (via
  `anyhow`'s default `main` handler) rather than the `4` REQ-022 requires —
  this was a latent contract gap that no test caught before T-020, now closed
  by `ExitCode::from_error_chain` and the `exit_codes.rs::exclusive_lock_timeout_exits_4`
  regression test. No other issues.

- Procedural compliance: First-attempt pass on a clean working tree; this
  implementer entry is created directly in `journal/T-020.md` per the
  journal-file schema (no `<implementer-note>` written into TASKS.md). The
  TASKS.md `state` attribute for T-020 flips `pending → in-progress →
  in-review` this turn. No shipped skill bodies under `.claude/skills/` were
  edited.
</implementer>

<review persona="business" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-05-29T06:00:00Z" round="1">
REQ-022 is fully delivered. Every `<done-when>` bullet maps to the diff: exit 0 (`ApplyResult::Applied`), exit 1 (`from_error_chain` fall-through), exit 2 (`Aborted`, test also asserts no file op ran), exit 3 (`RolledBack`, test asserts the copy was reversed), exit 4 (chain-walk for `LockError::Timeout` — the load-bearing fix that turns the previously-flat `1` into `4`), exit 5 (`UserDeclined` in both apply and rollback paths). CHK-036 is directly mirrored by `pre_apply_hook_failure_exits_2`. The `<behavior>` "exits 2 (not 7)" promise holds structurally since the hook's own exit value is never read — the code derives from the `ApplyResult` variant alone. The contract is centralised in one `resolve_exit_code` funnel per the SPEC's "one place" requirement, and all five `exit_codes.rs` integration tests pass. No non-goal scope crept in; the `PATINA_LOCK_TIMEOUT_MS` test knob is exactly the "parameterised in the test harness" affordance the scenarios call for, with a safe production fallback. Serves the CI-script-author user story.
Non-blocking note: the journal narrative claims an in-process unit test covers the apply declined-prompt → exit 5 path; only `rollback::declined_prompt_exits_five` exists. The exit-5 mapping is nonetheless delivered identically in apply (`ExitCode::UserDeclined.code()`) and validated at the rollback site — SPEC behaviour satisfied; flagged for the tests persona.
</review>

<review persona="tests" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-05-29T06:00:00Z" round="1">
Tests genuinely exercise REQ-022's exit-code contract. CHK-036 (the only `<scenario>` on REQ-022) is demonstrated by `patina-cli/tests/exit_codes.rs::pre_apply_hook_failure_exits_2`, which asserts both exit code 2 and that `~/.rc` was never created — catching a regression that ran file ops despite the failed `must_succeed` pre_apply hook, not just the integer. The load-bearing exit-4 path is real: `exclusive_lock_timeout_exits_4` holds the advisory lock in-process and spawns the binary as a subprocess capped at 200ms via `PATINA_LOCK_TIMEOUT_MS`, and production apply/rollback route through `patina_core::lock::exclusive_timeout()`, so the env var only shrinks the wait — it bypasses no logic. Removing `ExitCode::from_error_chain`'s chain-walk would drop that case back to anyhow's flat `1` and fail `assert_eq!(code, 4)`; the `exit_code.rs` unit tests independently pin the 4-vs-1 distinction. Evidence roll call is complete (CHK-036 `demonstrated` with a named test), every cited test name appears verbatim in the diff, per-scenario evidence uses scoped invocations (not the workspace hygiene run). Exit-5's deferral to in-process `PromptReader` unit tests (`rollback.rs::declined_prompt_exits_five`) is a legitimate constraint (piped subprocess stdin is never a TTY) and exit-5 is not a CHK. No mocking-the-SUT, empty-body, or flake patterns found.
</review>

<review persona="security" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-05-29T06:00:00Z" round="1">
No security concerns in T-020. The new `PATINA_LOCK_TIMEOUT_MS` override (`patina-core/src/lock.rs:84`) only shortens/lengthens the exclusive-lock *wait* cap and fails closed (exit 4) — it does not weaken mutual exclusion or the crash-safety invariant, parses as `u64` with a safe fallback, and exposes no injection/panic surface. The full-chain stderr rendering in `cli::resolve_exit_code` emits only paths and parse diagnostics; patina has no secret/credential domain in v1.0, so no sensitive-data exposure. No new dependency, no auth/crypto/network boundary touched.
</review>

<review persona="style" verdict="blocking" model="claude-sonnet-4-6[1m]/medium" date="2026-05-29T06:00:00Z" round="1">
Two issues, both in `patina-cli/src/exit_code.rs`.

**1. Stale doc link (docs-drift, line 6).** The module-level doc comment references `[\`crate::cli::exit\`]` as the "single funnel" function. That symbol does not exist; the actual function introduced by this task is `crate::cli::resolve_exit_code`. `cargo doc --no-deps` will emit a broken-intra-doc-link warning (or error under `#[deny(rustdoc::broken_intra_doc_links)]`). Per AGENTS.md Hard Rule "Never let docs drift." Fix line 6 to reference `[\`crate::cli::resolve_exit_code\`]`.

**2. Vacuous constant-copy test (`discriminants_match_the_req022_contract`, lines 99–106).** This test asserts `ExitCode::Success.code() == 0` … `ExitCode::UserDeclined.code() == 5`. Because `ExitCode` is `#[repr(i32)]` with pinned discriminants, `code()` returns `self as i32`, which is the pinned literal by definition. The test compares each constant against the same hard-coded value that already appears as its discriminant — it cannot fail except by simultaneously removing `#[repr(i32)]` and changing all discriminants. This is the exact "Copying production constants into the test" shape AGENTS.md prohibits (test hygiene rule 2). Drop `discriminants_match_the_req022_contract`; the discriminant values are already enforced by `#[repr(i32)]` + compile-time pinning, and the `exclusive_lock_timeout_exits_4` integration test gates the contract end-to-end over a real subprocess.
</review>

<blockers date="2026-05-29T06:00:00Z" round="1">
One blocking verdict (style); business, tests, security passed. Two mechanical fixes in `patina-cli/src/exit_code.rs` before the next pass:
- Fix the stale rustdoc intra-doc link on line 6: `[\`crate::cli::exit\`]` → `[\`crate::cli::resolve_exit_code\`]` (the symbol `crate::cli::exit` does not exist; the funnel is `resolve_exit_code`). Will otherwise trip `cargo doc --no-deps` broken-intra-doc-link.
- Delete the vacuous test `discriminants_match_the_req022_contract` (lines ~99–106): it compares `ExitCode::<V>.code()` against the same literal pinned as the `#[repr(i32)]` discriminant — the "copying production constants into the test" shape banned by AGENTS.md test hygiene rule 2. The discriminants are already enforced by `#[repr(i32)]` pinning and the `exclusive_lock_timeout_exits_4` end-to-end subprocess test; no replacement needed.
</blockers>

<implementer date="2026-05-29T07:00:00Z" model="claude-opus-4-8[1m]/medium" round="2">
- Completed: Addressed both round-1 style blockers in
  `patina-cli/src/exit_code.rs`, amending the round-1 WIP in place. (1) Fixed
  the stale module-doc intra-doc link `[`crate::cli::exit`]` →
  `[`crate::cli::resolve_exit_code`]` (the `crate::cli::exit` symbol does not
  exist; the funnel is `resolve_exit_code`); `cargo +nightly fmt` then
  rewrapped the doc line, which is the only formatting delta. (2) Deleted the
  vacuous `discriminants_match_the_req022_contract` test (it compared each
  `ExitCode::<V>.code()` against the same literal pinned as its `#[repr(i32)]`
  discriminant — AGENTS.md test-hygiene rule 2). No replacement: the
  discriminants are enforced by `#[repr(i32)]` pinning and the
  `exclusive_lock_timeout_exits_4` end-to-end subprocess test gates the
  contract.
- Undone: Nothing in scope. No production behaviour changed this round — both
  edits are a doc-link correction and a test deletion.
- Hygiene checks: `cargo test --workspace` exited 0. `cargo clippy --workspace
  --all-targets --all-features -- -D warnings` exited 0. `cargo +nightly fmt
  --all --check` exited 0 (after the doc-line rewrap). `cargo deny check`
  exited 0 (pre-existing unmatched-license-allowance warning is non-fatal).
  `cargo doc --no-deps` exited 0; the `exit_code.rs` link now resolves (no
  warning references `exit_code` / `resolve_exit_code` / `cli::exit`). The
  remaining doc warnings are all pre-existing `patina-core` items
  (`apply/mod.rs`, `apply/engine.rs`), untouched by T-020.
- Evidence: unchanged from round 1
  (`.speccy/specs/0001-patina-core-engine/evidence/T-020.md`). Roll call for
  CHKs under REQ-022 stands: CHK-036 (`pre_apply` `must_succeed` failure →
  exit 2): demonstrated → `pre_apply_hook_failure_exits_2`. This round added
  no test and removed only a vacuous one; the demonstrated coverage is intact.
- Discovered issues: None this round.
- Procedural compliance: Retry pass — amended the prior pass's uncommitted WIP
  in place (no `git restore`/`clean`/`checkout`, no rewrite from scratch).
  Round monotonically incremented 1 → 2. State flipped `pending →
  in-progress → in-review`. No shipped skill bodies edited.
</implementer>

<review persona="business" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-05-29T08:00:00Z" round="2">
Round-2 delta is business-neutral and the REQ-022 contract remains fully delivered. The two round-1 style fixes in `patina-cli/src/exit_code.rs` are (1) a doc-link correction (line 6 now `[\`crate::cli::resolve_exit_code\`]`, the real funnel) and (2) deletion of the vacuous `discriminants_match_the_req022_contract` test — neither alters production behavior nor drops any `<done-when>`/`<behavior>`/`<scenario>` coverage. The exit-code mapping (0/1/2/3/4/5) is intact with pinned `#[repr(i32)]` discriminants, and `exit_codes.rs` still exercises exit 0/1/2/3/4 over real subprocesses; CHK-036 is mirrored by `pre_apply_hook_failure_exits_2`, which also asserts no file op ran. Exit 5 stays correctly deferred to in-process TTY-injected unit tests. No non-goal scope crept in; no open question silently resolved; CI-script-author user story served via the single deterministic `resolve_exit_code` funnel.
</review>

<review persona="tests" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-05-29T08:00:00Z" round="2">
Round-2 changes verified: both round-1 style blockers addressed and the deletion removed nothing load-bearing. The vacuous `discriminants_match_the_req022_contract` is gone (5 genuine unit tests remain, pinning the 4-vs-1 `from_engine_error`/`from_error_chain` distinction), and the module doc link at `exit_code.rs:6` now resolves to `crate::cli::resolve_exit_code` (defined at `cli.rs:30`). The deleted test only compared `ExitCode::<V>.code()` against its own `#[repr(i32)]` discriminant literal (hygiene rule 2); the `code()` behaviour is genuinely exercised end-to-end. REQ-022 contract holds: `exit_codes.rs` spawns the real binary and asserts the actual process status — `pre_apply_hook_failure_exits_2` (CHK-036, also asserts `~/.rc` never created), `post_apply_hook_failure_exits_3` (rollback reversal), `toml_syntax_error_exits_1_and_names_the_failure`, `exclusive_lock_timeout_exits_4` (holds the real lock in-process, 200ms cap via `PATINA_LOCK_TIMEOUT_MS` that shrinks the wait without bypassing logic), and `successful_apply_exits_0`. `exit_codes_match_outcomes` gates the `ApplyResult`→code mapping non-vacuously; exit-5 covered by `declined_prompt_exits_five` (not a CHK). Evidence roll call complete with no fabrication patterns.
</review>

<review persona="security" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-05-29T08:00:00Z" round="2">
No security regression in round 2. The delta is confined to `patina-cli/src/exit_code.rs`: a comment-only intra-doc-link correction (line 6, `crate::cli::exit` → `crate::cli::resolve_exit_code`) and the deletion of the vacuous `discriminants_match_the_req022_contract` test. Neither touches an executable path. The `PATINA_LOCK_TIMEOUT_MS` wait-cap override (fails closed to exit 4, `u64` parse with safe fallback), the lock mutual-exclusion / crash-safety invariant, and the error-chain stderr rendering (paths and parse diagnostics only — no secret domain in v1.0) are all unchanged. No new dependency, no auth/crypto/network boundary touched. The round-1 security pass holds.
</review>

<review persona="style" verdict="pass" model="claude-sonnet-4-6[1m]/medium" date="2026-05-29T08:00:00Z" round="2">
Both round-1 blockers are resolved correctly.
1. `patina-cli/src/exit_code.rs:6` — the intra-doc link now reads `[\`crate::cli::resolve_exit_code\`]`, and the function exists at `patina-cli/src/cli.rs:30` as a public function in `crate::cli`. The link resolves.
2. `discriminants_match_the_req022_contract` is deleted. No replacement was added. The remaining five tests in `exit_code.rs` each gate a real behavioral property. All imports in the test module (`Utf8PathBuf`, `LockKind`, `Duration`) remain used. No dead code introduced by the deletion.
No new style issues found in the round-2 delta.
</review>
