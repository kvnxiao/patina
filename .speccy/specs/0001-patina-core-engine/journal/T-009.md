---
spec: SPEC-0001
task: T-009
generated_at: 2026-05-28T00:00:00Z
---

<implementer date="2026-05-28T00:00:00Z" model="claude-opus-4.8[1m]/high" round="1">
- Completed: Landed `patina_core::paths` (`patina-core/src/paths.rs`)
  exposing the REQ-010 surface `pub fn canonicalize(p: &Utf8Path) ->
  Result<Utf8PathBuf, PathError>` plus a companion
  `pub fn expand_tilde(p: &Utf8Path, home: &Utf8Path) -> Utf8PathBuf`
  for the user-input `~` variant. `canonicalize` has the two branches
  the task body specifies: an existing path canonicalizes through the
  filesystem via `Utf8Path::canonicalize_utf8` (symlinks and `.` /
  `..` resolved); a non-existent path falls back lexically — when the
  parent exists it canonicalizes the parent through the filesystem and
  re-joins the leaf (so symlinks in the parent chain resolve while a
  not-yet-created leaf is tolerated), otherwise it joins onto the
  canonical CWD and folds `.` / `..` segments out lexically via a
  private `fold_dot_segments` helper. `expand_tilde` honours only the
  leading-`~` shell convention (`~` and `~/...`), is purely lexical,
  and does not touch the filesystem. Added a typed `PathError` enum
  (`CwdUnavailable`, `CwdNotUtf8`, `Filesystem`, `ResultNotUtf8`),
  surfaced it from `lib.rs` (`pub mod paths` + re-exports
  `PathError`, `canonicalize_path`, `expand_tilde`), and threaded a
  `EngineError::Path(#[from] PathError)` variant into
  `patina-core/src/error.rs`. Wired the helper into the discovery
  layer (`patina-core/src/discovery/repo.rs`): both former
  `canonicalize_utf8` call sites (`validate_root` for the PATINA_REPO
  / persisted-default sources and `walk_up_for_root` for the walk-up
  source) now route through `crate::paths::canonicalize`, and the
  `RepoDiscoveryError::Canonicalize` variant's `source` type changed
  from `std::io::Error` to `crate::paths::PathError`. Added the
  integration suite `patina-core/tests/path_canon.rs` (5 tests) plus
  6 in-module unit tests and 2 doctests.

- Undone: The end-to-end CHK-021 surface — `patina apply --yes --json`
  emitting a `repo_root` JSON field — is deliberately deferred: the
  `apply` subcommand, the `--json` envelope, and the `repo_root`
  output field all land in T-016 (REQ-017), which is still `pending`.
  T-009 owns and proves the library-level property (relative repo →
  canonical absolute dot-free root) that surface will report. The
  task body also lists wiring into file-entry parsing (T-004) and
  journal writes (T-010): file-entry target paths are stored verbatim
  by T-004's parser (no canonicalization point exists there yet — it
  is the executor in T-014 that resolves targets), and the journal
  module (T-010) is not yet landed, so there is nothing to wire into
  for either. Module enumeration (T-003) derives module paths from
  `read_dir` over the already-canonical root, so those handles are
  already absolute and canonical without an added call. `expand_tilde`
  is provided now so T-014's executor can call it without re-deriving
  the convention.

- Hygiene checks: `cargo test --workspace` exited 0 (all suites green,
  including the 6 `paths` unit tests, the 5 `path_canon` integration
  tests, and the 2 `paths` doctests). `cargo clippy --workspace
  --all-targets --all-features -- -D warnings` exited 0 (one
  `clippy::map_err_ignore` finding on the first pass was fixed by
  capturing the non-UTF-8 `PathBuf` in `PathError::CwdNotUtf8` rather
  than discarding it). `cargo +nightly fmt --all --check` exited 0
  (after a `cargo +nightly fmt --all` pass). `cargo deny check` exited
  0 (advisories / bans / licenses / sources ok; no new dependencies —
  `camino` and `fs-err` were already in `[workspace.dependencies]`).

- Evidence: paper trail at
  `.speccy/specs/0001-patina-core-engine/evidence/T-009.md`.
  Roll call for the CHK under REQ-010:
  - CHK-021 (relative `PATINA_REPO=./dot` → `repo_root == /tmp/work/dot`):
    demonstrated → evidence Scenario 1 covers the red (no `paths`
    module; bare `canonicalize_utf8` errors on a non-existent path)
    versus green (`relative_repo_resolves_to_canonical_absolute_root`
    asserts the resolved root equals the canonical absolute `T/dot`
    with no `/./` or `/../` segments, plus the not-yet-existing-target
    lexical fallback). The end-to-end `patina apply --yes --json` form
    of the assertion is deferred to T-016, which builds the CLI
    surface; T-009 demonstrates the canonical-path property at the
    layer that computes the `repo_root` value.

- Discovered issues: None. The pre-existing `repo.rs` comment that
  named T-009 as the task to "replace the direct `canonicalize_utf8`
  call with a lexical-fallback helper" is now satisfied and was
  rewritten to describe the landed wiring rather than a forward
  reference.

- Procedural compliance: This entry lands directly in
  `journal/T-009.md` per the journal-file schema; no
  `<implementer-note>` was written into TASKS.md. The TASKS.md
  `state="..."` attribute for T-009 flips from `in-progress` to
  `in-review` as the final step of this turn. No shipped skill bodies
  under `.claude/skills/` were edited.
</implementer>

<review persona="business" verdict="pass" model="claude-opus-4-8[1m]/medium" date="2026-05-28T17:52:24Z" round="1">
T-009 (REQ-010) delivers the requirement at the layer it owns. `patina_core::paths::canonicalize` implements both branches the SPEC names (filesystem canonicalization for existing paths; lexical fallback joining the canonical parent or CWD for non-existent paths), `expand_tilde` covers the user-input `~` variant, and both are wired into the discovery layer's `validate_root` and `walk_up_for_root` (`patina-core/src/discovery/repo.rs`), replacing the prior direct `canonicalize_utf8` stub and re-typing `RepoDiscoveryError::Canonicalize` to `crate::paths::PathError`. The `relative_repo_resolves_to_canonical_absolute_root` test proves CHK-021's load-bearing property (relative `PATINA_REPO` → canonical absolute, dot-free root) at the layer that computes `repo_root`. The three deferrals are legitimate and match the SPEC's own decomposition: the end-to-end `apply --yes --json` → `repo_root` surface is T-016 (pending); the "journal stores only canonical absolute paths" done-when depends on T-010 (pending, nothing to wire yet); target resolution is T-014's executor, for which `expand_tilde` was provided ahead of time. No non-goal scope crept in, no open question silently resolved.
</review>

<review persona="tests" verdict="pass" model="claude-opus-4.8[1m]/high" date="2026-05-28T17:52:24Z" round="1">
T-009 tests genuinely exercise the REQ-010 canonicalization behaviour and would catch regressions. Evidence roll call is complete (CHK-021 = `demonstrated`, the only scenario under REQ-010), cites `relative_repo_resolves_to_canonical_absolute_root` in `patina-core/tests/path_canon.rs` which appears in the diff, and the red phase is real (pre-edit `canonicalize_utf8` returns `Err(NotFound)` on a non-existent path; the green test asserts a lexical fallback instead). Re-ran `cargo test -p patina-core --test path_canon` — 5/5 pass. Assertions have teeth: `existing_path_resolves_absolute_with_no_dot_segments` asserts equality to the canonical dir plus absence of `/./` and `/../` (a no-op identity fails); `nonexistent_target_under_missing_grandparent_does_not_error` asserts the success branch the old code errored on; `relative_repo_resolves_to_canonical_absolute_root` proves `work/./dot` folds to `work/dot`. No mocking-the-SUT, no vacuous patterns. The CHK-021 end-to-end form is legitimately deferred to T-016; the symlink-passthrough `<task-scenarios>` case is exercised under `#[cfg(unix)]` only and skipped on Windows (elevation needed), disclosed in a comment, with parent-canonicalization still asserted cross-platform — and REQ-010's sole scenario is CHK-021. Evidence command is scoped, red/green outputs differ materially.
</review>

<review persona="security" verdict="pass" model="claude-opus-4.8[1m]/medium" date="2026-05-28T17:52:24Z" round="1">
No security blockers. `patina-core::paths` is a local single-user CLI path-canonicalization helper with no auth boundary, remote caller, secret handling, network surface, or new dependency (`cargo deny check` clean; `camino`/`fs-err` already vendored). The TOCTOU between `p.exists()` and `p.canonicalize_utf8()` (`paths.rs:109-117`) is benign here: the race degrades to a typed `PathError::Filesystem`, never a silently-wrong path, and the "never overwrite without consent" guarantee is enforced at apply-time (T-014 executor), not at this pure path-computation layer. Symlink and `.`/`..` handling matches the SPEC scenarios; `fold_dot_segments` is purely lexical and only on the CWD-join branch. Full paths in `PathError` messages are appropriate disclosure for a local CLI. No injection vectors (no shell/SQL/template surface), no secret handling, no `unwrap`/`expect`/`panic` in production (`.expect()` confined to `#[cfg(test)]` and integration tests under an explicit `#[expect(...)]`).
</review>

<review persona="style" verdict="pass" model="claude-sonnet-4-6[1m]/medium" date="2026-05-28T17:52:24Z" round="1">
All style criteria pass. `paths.rs` and `path_canon.rs` are clean new files: naming follows `Utf8Path`/`Utf8PathBuf` conventions throughout, `PathError` variants carry struct-form field names with doc comments matching the project pattern, `#[must_use]` on `expand_tilde` carries a message per `rust-code-quality.md`, and `#[non_exhaustive]` is present. The `#[expect(clippy::expect_used, reason = "...")]` suppression in `path_canon.rs` has a `reason` string and mirrors the identical pattern in `repo_discovery.rs`. The `lib.rs` re-exports `canonicalize as canonicalize_path` — the `as`-alias disambiguation follows the established project pattern (`resolve as resolve_state_dir`, `resolve as resolve_profile`). The forward-reference T-009 comment in `repo.rs` is properly cleaned up. Nightly rustfmt, Clippy, and all tests exit 0.
</review>
