# CI and contributors use nightly rustfmt because the options below
# (group_imports, imports_granularity, normalize_comments,
# normalize_doc_attributes, wrap_comments, float_literal_trailing_zero)
# are unstable:
#
#   rustup toolchain install nightly --component rustfmt
#   cargo +nightly fmt --all              # apply
#   cargo +nightly fmt --all --check      # verify (matches CI)
#
# `unstable_features = true` enables the unstable options. Stable rustfmt
# warns about this setting and ignores those options, so `cargo fmt` on stable
# does not match CI. Use `cargo +nightly fmt` for the repository format.

unstable_features = true

reorder_imports = true
group_imports = "One"
imports_granularity = "Item"
newline_style = "Unix"
normalize_comments = true
normalize_doc_attributes = true
wrap_comments = true
float_literal_trailing_zero = "Always"
